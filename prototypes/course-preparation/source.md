# Tide table field note

## Reading range

An observation time and a prediction time answer different questions. Record
which one a value represents before comparing two entries.

When a table omits the timezone, stop and verify the source metadata. Do not
silently infer local time.

## Outside the assigned range

This paragraph proves that the prototype opens a bounded selection rather than
claiming the whole file was assigned.
