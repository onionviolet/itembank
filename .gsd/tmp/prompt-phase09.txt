Run with max steps 0 / no round cap. EXECUTE Phase 09 (subject-invariant loop — EMT prose, Math LaTeX, runnable CS code) in the itembank repo (C:\Users\wayba\Downloads\CTF\itembank). Work ONLY in your own worktree — do NOT touch the main working tree or any .phase*-wt dir (phases 05/08/06.1/06.2/09.1/10/11 and 999.4-exec are active elsewhere).

STEP 0 — PHASE-5 GATE (mandatory, do not skip): Phase 09 depends on Phase 3 (done), Phase 6 (done), and Phase 5 (check item type) which is NOT complete yet. Phase 5 is being executed on branch gsd/phase-05-check in the LINKED worktree C:\Users\wayba\Downloads\CTF\itembank-phase5 (a sibling directory of this repo; its branch IS visible from this repo's git since it is a linked worktree). It was ALIVE as of 2026-08-11 01:57 CDT (mid-plan 05-02; only 05-01-SUMMARY.md written). Detect completion with MULTIPLE signals — never trust a single file:
- STRONG set (all required): for every plan n in 01..07, the committed file .planning/phases/05-check-item-type-code-editor/05-n-SUMMARY.md exists on branch gsd/phase-05-check, AND 05-VERIFICATION.md and 05-UAT.md exist there too. Check with git ls-tree / git cat-file -e on the branch ref (e.g. git ls-tree -r --name-only gsd/phase-05-check .planning/phases/05-check-item-type-code-editor/) — do NOT rely on the sibling worktree's working tree or any untracked files.
- CORROBORATING (at least one): gsd/phase-05-check HEAD is a docs(05) close/verification commit; or HANDOFF-PHASE05.md is committed on that branch; or the branch is merged into main (git branch --merged main).
If the strong set is met, phase 5 is complete — proceed. If NOT met: first check phase 5 is still ALIVE via commit recency (git log -1 --format=%ci gsd/phase-05-check; a new commit in the last ~45 min means alive). If DEAD (no new commit > 45 min) AND strong set unmet: STOP and report — do not proceed on the CS leg and do not guess. If ALIVE but incomplete: wait with a polling loop — bash sleep 300 then re-check the strong set (up to ~10 polls); between polls do all Phase-09 prep that does NOT require the check type (read .planning\phases\09-subject-invariant-loop-emt-math-cs-integration\: CONTEXT, DISCUSSION-LOG, RESEARCH, UI-SPEC, PRE-FLIGHT, plans 09-01..05; verify tooling; note which plans need check items). If the polling window expires with phase 5 incomplete, STOP and report with a .continue-here marker rather than proceeding.

STEP 1: git worktree add -b gsd/phase-09-subject-loop C:\Users\wayba\Downloads\CTF\itembank\.phase09-wt (from main tip; if it already exists, cd in and continue).
STEP 2: execute plans 09-01..05 in order. The CS leg requires Phase 5's check type — if a plan references check items before phase 5 is complete, hold that plan and execute the others first. One atomic commit per plan (prefix feat(09-0N): / test(09-0N): / docs(09-0N):). Run tests\*_roundtrip.py inside your worktree: direct python per-file calls or small &&-chains only — this environment's approval gate blocks pipelines with ; echo suffixes, ad-hoc runner scripts, background loops, and git merge; omit them (if you need git merge, leave the branch ready and report).
STEP 3: write per-plan SUMMARYs, phase VERIFICATION + UAT, update .planning\config.json and STATE.md, write HANDOFF-PHASE09.md. Do not merge or push; report the branch, which plans depended on phase 5, and results.