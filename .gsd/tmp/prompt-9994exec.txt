Run with max steps 0 / no round cap. EXECUTE Phase 999.4 (Canvas LMS integration via LTI) in the itembank repo (C:\Users\wayba\Downloads\CTF\itembank). The plan-phase is DONE on branch gsd/phase-999.4-plan (worktree .phase9994-wt) — do NOT re-plan. Work ONLY in your own new worktree — do NOT touch the main working tree or .phase9994-wt (phases 05/08/06.1/06.2/09.1/10/11 and 09 are active elsewhere).

STEP 1: git worktree add -b gsd/phase-999.4-exec C:\Users\wayba\Downloads\CTF\itembank\.phase9994x-wt gsd/phase-999.4-plan (branch FROM the planning branch so CONTEXT/RESEARCH/UI-SPEC/VALIDATION and the 3 plans are present; if the worktree already exists, cd in and continue).
STEP 2: read .planning\phases\999.4-canvas-lms-integration-lti\ and execute the 3 plans in wave order:
- 999.4-01 (wave 1, protocol spine + tracer): LTI 1.3 OIDC launch + JWKS verification, one platforms registration store, optional pinned cryptography/PyJWT guard, and an in-process wrap that calls the daemon's handle_api_* JSON commands — no new /api/* routes, no second scorer, no accounts. Tested by a stdlib fake-platform harness executable with zero Canvas access.
- 999.4-02 (wave 2, UI-CONTRACT): instructor deep-link picker (one objective per assignment, no sensitive bank content) + the embedded learner player reusing the quiz surface, public_item()-only until a recorded response exists, frame-ancestors CSP, verbatim privacy copy.
- 999.4-03 (wave 3, passback + hardening): AGS grade passback (outbound score copy at completion, idempotent, short items never auto-graded -> PendingManual) and opt-in stdlib-TLS/reverse-proxy hosting.
STEP 3: one atomic commit per plan (prefix feat(999.4-0N): / test(999.4-0N): / docs(999.4-0N):). Run the phase tests + full suite (tests\*_roundtrip.py) inside your worktree: direct python per-file calls or small &&-chains only — this environment's approval gate blocks pipelines with ; echo suffixes, ad-hoc runner scripts, background loops, and git merge; omit them (if you need git merge, leave the branch ready and report).
STEP 4: write per-plan SUMMARYs, phase VERIFICATION + UAT, update .planning\config.json and STATE.md, write HANDOFF-PHASE999.4.md. Do not merge or push; report the branch and results.