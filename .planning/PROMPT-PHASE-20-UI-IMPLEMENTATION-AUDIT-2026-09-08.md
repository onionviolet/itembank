# Prompt: Phase 20 UI implementation audit and repair chain

Use `$daisy-chain-work`, `$efficient-agent-routing`, `$evidence-gated-handoff`,
`$user-vision`, and `$problem-intake` for this bounded chain link.

## Handoff packet

**GOAL:** Reconcile Phase 20's claimed UI repairs against the actual local
learner journey, repair every settled Phase 20-owned defect exposed by that
comparison, and present meaningfully distinct profile candidates to Weibao.

**OWNER:** Weibao owns perceived visual quality, profile selection, the future
default, and whether existing profiles are retained, combined, superseded, or
retired.

**BUILDER:** One balanced-capability agent at medium reasoning. Keep audit,
bounded implementation, and targeted verification together because they touch
the same learner flow and files. Do not spawn another agent unless a later
independent review has a distinct gate and no overlapping writes.

**VERIFIER:** Deterministic tests verify behavior. Weibao verifies perceived
clarity, profile character, touch behavior, and screen-reader-oriented use.

**SCOPE:** `.planning/PROBLEM-LEDGER.md`, Phase 20's audit and verification
owners, the normal Courses to course to lesson to practice journey, shared
presentation/profile code, and the smallest tests needed for repaired findings.

**DO NOT TOUCH:** Parser, scorer, keyed-disclosure policy, evidence authority,
canonical course or bank data, real learner evidence, external services, Canvas,
or unrelated dirty checkout work. Do not commit or push. Do not change the
default or remove a profile before Weibao chooses from comparable working
candidates and reviews the migration plan.

**CONTEXT:** `P-20260908-01` has a local presentation-layer repair whose focused
`python3 tests/serve_roundtrip.py` gate passed, but it still needs live human
recheck. `P-20260908-02` records Weibao's direct finding that the UI looks the
same as before. `P-20260908-03` registers possible new default profiles and
retirement of the old pair as a decision, not authorization. Phase 20 has five
plans and three summaries. Its transition record still has human and full
daemon gates owed.

**TOOLS NEEDED:** Local terminal, actual loopback daemon, and an interactive
browser. A real touch device or screen-reader setup is optional and must
be marked unavailable when absent.

**GATE:** The saved audit maps every applicable prior finding to direct live
evidence, deterministic-only evidence, open defect, exact blocker, human owed,
or external owner. All settled owned defects repaired in this link pass their
targeted tests. At least two structurally distinct working profile candidates
are shown on identical state. Weibao's profile/default/removal decision remains
open until directly supplied.

**RETURN:** Exact flows exercised, prior findings reopened or confirmed,
changed paths, targeted commands and results, human observations, unavailable
legs, candidate profile differences, migration risks, and one next chain link.

## Outcome

Determine whether the current Phase 20 implementation actually delivers a
meaningfully different, clear learner experience, not merely new CSS classes or
settings labels. Reconcile each relevant prior audit finding against the live
normal learner flow. Repair settled, Phase 20-owned failures with bounded diffs
and targeted tests. Do not claim human acceptance without Weibao's direct notes.

## Read first

- `AGENTS.md`
- `.planning/STATE.md`
- `.planning/PROBLEM-LEDGER.md`
- `.planning/phases/20-extensible-ui-foundation-and-interaction-clarity-pass/20-CONTEXT.md`
- `.planning/phases/20-extensible-ui-foundation-and-interaction-clarity-pass/20-UI-SPEC.md`
- `.planning/phases/20-extensible-ui-foundation-and-interaction-clarity-pass/20-AUDIT-CROSSWALK.md`
- `.planning/phases/20-extensible-ui-foundation-and-interaction-clarity-pass/20-HOME-COMPARISON.md`
- `.planning/phases/20-extensible-ui-foundation-and-interaction-clarity-pass/20-TRANSITION-GATES.md`
- `.planning/UI-CHARACTER-AUDIT-2026-09-06.md`
- `.planning/AUDIT-REMEDIATION-PLAN-2026-09-06.md`

## Scope

Start at the normal local learner entry point. Exercise visible Courses,
course overview, lesson, practice, feedback pause, return or recovery, and
Settings profile preview. Compare both profiles on identical synthetic state.
Use normal visible navigation, not guessed URLs. Check each retained home mode
only through visible supported controls. Do not report a mode or surface as
reviewed when it could not be reached.

For every inherited and current finding, record one of: fixed and directly
observed, fixed only by deterministic evidence, open defect, blocked with an
exact condition, human owed, or owned elsewhere. Reopen any current claim that
fails in the live flow. Test the reported feedback pause and position contract
from `P-20260908-01` before judging visual character.

Treat `P-20260908-03` as a product-direction gate. If the current profiles are
not meaningfully distinct, propose replacement candidates as reversible
prototypes. Do not change the default or purge a shipped profile until Weibao
selects a result and the proposal names migration, fallback, recovery,
compatibility, rollback, and deterministic checks.

## Required review questions

1. Can a learner identify the app, course, location, task, response format,
   feedback state, and next action without reading the whole screen?
2. Do Field Guide and Trajectory Deck differ in composition and decision
   priority, or do they only look cosmetically different?
3. Does a submitted answer visibly receive the runtime-issued correct or
   incorrect verdict before the learner reaches the next item?
4. Does every position remain truthful, including final item, completion,
   refresh, Back, and receipt recovery?
5. Are touch, keyboard, screen-reader-oriented, narrow-width, offline,
   unavailable, interrupted, and long-content findings supported by direct
   evidence, or still owed?

## Boundaries

Do not change parser, scorer, keyed disclosure, evidence authority, canonical
course or bank data, home-mode values, external services, or saved settings
during audit and candidate comparison. Do not treat an automated test, a
screenshot, or a guessed route as human accessibility certification. Preserve
unrelated dirty checkout work. Do not commit or push.

## Evidence and return

Run the smallest relevant deterministic checks first. Record commands and
results. State unavailable environment legs plainly. Update
`.planning/PROBLEM-LEDGER.md` and the designated Phase 20 audit or verification
owner only after direct evidence exists. Review the actual diff before handoff.

Create the next Codex task only after this link reaches a stable evidence point,
meaningful work remains, the next unit and gate are known, and no unresolved
Weibao decision blocks it. The next prompt must again invoke
`$daisy-chain-work` and `$efficient-agent-routing`, preserve all exclusions,
and use the same saved project and local checkout while this work remains
uncommitted. Stop the chain for Weibao's profile decision or after three links
hit the same blocker.
