# Probe C: pointer message, fork_turns="all" (20260810165252)

This file is your complete task. Read it first and follow it exactly.

1. Create the file exactly at:
   `C:\Users\wayba\Downloads\CTF\itembank\.planning\probes\probe-c-pointer-forkall-20260810165252.txt`
2. Its content must be exactly `payload-ok` with NO trailing newline,
   carriage return, or spaces (9 bytes).
3. Write it byte-exact, e.g. in PowerShell:
   `[System.IO.File]::WriteAllText('C:\Users\wayba\Downloads\CTF\itembank\.planning\probes\probe-c-pointer-forkall-20260810165252.txt', 'payload-ok')`
4. Verify the file exists and its raw byte length is 9, then reply with a
   one-line confirmation: `PROBE_DONE c`

Do not create any other files.
