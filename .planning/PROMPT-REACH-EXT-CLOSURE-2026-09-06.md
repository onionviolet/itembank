# Prompt: finish Reach, EXT, and audit closure

Paste the prompt below into a fresh Codex task opened on this repository.

```text
Continue the itembank project from the live checkout and finish the remaining
Reach and EXT work as far as evidence permits. Work continuously until every
automatable gate is passed or a genuine human or external-state blocker is
reached. Do not claim the whole user vision complete merely because Reach
closes.

AUTHORITY AND REQUIRED READING

Read AGENTS.md first. Then read the current portions of:

- .planning/USER-VISION.md and .planning/USER-VISION-INBOX.md that apply to
  extensions, first use, course creation, agent operation, UI coherence,
  recovery, external installation, and broader course evidence.
- .planning/SOURCE-TO-COURSE.md, .planning/AGENT-WORKFLOW.md,
  .planning/REACH-MILESTONE.md, .planning/STATE.md, and
  .planning/AUDIT-REMEDIATION-PLAN-2026-09-06.md.
- .planning/EXTENSION-DELIVERY-2026-09-06.md and EXT-01, EXT-02, and EXT-03
  plans.
- The 19A, 19B, 19C, 19D, and 19E phase seeds, contexts, summaries,
  verifications, UI contracts, and pattern maps that exist in the live tree.
- .planning/UI-CHARACTER-AUDIT-2026-09-06.md, the Phase 17C loss audit,
  Phase 18 gates, and the current vision-audit output.

Treat user quotations as authoritative intent. Keep interpretations visibly
separate. Preserve every viable idea with a disposition. Never certify a
human accessibility, visual, instructional, or cold-install gate yourself.

GOAL

Produce a verified Reach outcome through the product's own doors. Finish the
first-party EXT pilot. Prove the agent operation door and generated MCP tool
table. Build and sit the representative Math 1400 unit. Repair every defect
that blocks that journey. Publish a compact closure index that gives every
remaining vision and audit finding an evidence-backed state and owner.

The only valid Reach completion label is:

Reach achieved, broader vision and human legs open.

CURRENT KNOWN STATE TO VERIFY, NOT BLINDLY TRUST

- EXT-01, EXT-02, and EXT-03 are committed as 02b36b02, af386920, and
  a1c1263a.
- A later review found and repaired three uncommitted EXT defects:
  build.py omitted extension_registry.py from packaged staging, source adapter
  descriptors used placeholder metadata, and heterogeneous extra descriptor
  keys could raise raw TypeError instead of RegistrationError.
- Focused extension, source-adapter, packaging, model-adapter, capability,
  and diff checks passed after those repairs. Reverify against the live tree.
- capabilities.json has an uncommitted generated route entry associated with
  the current 19A-09 course-read surface.
- 19A-09 planning, summary, verification, code, and shared planning files may
  still be uncommitted. Resolve ownership and exact scope before writing.
- 19B-CONTEXT.md, 19B-DISCUSSION-LOG.md, 19B-UI-SPEC.md, and
  19B-PATTERNS.md exist uncommitted. The UI contract passed its six-dimension
  check. No 19B PLAN.md was produced.
- 19C is complete. 19D and 19E remain seeds. The 19E seed says eight prior
  success criteria, while the current Phase 999.3 roadmap section may contain
  ten. Reconcile this by treating the live numbered criteria as authoritative
  and correcting stale count prose.

EXECUTION SHAPE AND MODEL ROUTING

Use one coordinator and one active code writer per shared checkout. Parallelize
only independent lanes with disjoint file ownership. Do not have multiple
agents edit daemon.py, journal.py, schemas, STATE.md, ROADMAP.md, or the same
phase directory concurrently.

Use the least expensive capable route:

- GPT-5.6 Luna with low reasoning for inventories, deterministic metadata
  checks, criteria extraction, and focused documentation comparisons.
- GPT-5.6 Terra with medium reasoning for bounded fixes with settled contracts
  and immediate tests.
- GPT-5.6 Sol with high reasoning for 19B, 19E, and 19D cross-system
  implementation and integration.
- GPT-6 Astra with high reasoning only for independent review of assessment
  authority, security, supply-chain, recovery, or a repeated failed gate.

Do not run a full GSD chain merely because it exists. Use the existing 19B
context and UI contract as the implementation packet. Create only the smallest
missing plan or context needed for 19E and 19D. Do not duplicate evidence in
new summaries when an existing verification record can link to it.

ORDER AND PARALLEL LANES

1. Reconcile the live tree. Identify every dirty path and its owner. Preserve
   unrelated edits. Finish and verify the EXT repair as one atomic packet.
   Finish and verify 19A-09 as its own atomic packet. Regenerate derived
   capability data from the accepted route table. Commit only packet-owned
   files by explicit path. Never use git add -A.

2. Execute 19B as one thin vertical slice after 19A shared-file ownership is
   clear. In parallel, a read-only or planning-only lane may prepare 19E from
   the current API_ROUTES, SURFACE_PARITY, resources.py, schema_validate.py,
   and scripts/ocr_mcp.py. The 19E lane must not edit shared route files while
   19B owns them.

3. After 19B is committed and verified, implement 19E. Generate MCP tools from
   the canonical route and parity tables and published schemas. Do not create
   a second operation catalogue. Prove stdio and any HTTP mount share one
   dispatcher. Preserve runtime-owned scoring, tier refusal, keyed disclosure,
   evidence, rights, and undo.

4. Prepare 19D source and objective inputs as soon as read dependencies permit.
   Close 19D only after 19A, 19B, and 19C are available. Treat 19E as a Reach
   deliverable with its own gate, not an extra prerequisite for the learner
   walkthrough. Run one representative Math 1400 unit from the canonical shelf
   through source binding, justified direct reading, one accepted generated
   treatment, Learn, Practice, Test, evidence, next recommendation, exact
   resume, agent interruption and recovery, undo, export, and restore.

5. After the representative journey, repair and rerun only failed steps. Then
   publish the Reach closure index. Continue into post-Reach work only in the
   existing owner order. Do not invent a general cleanup phase.

19B ACCEPTANCE GATE

- A skill control in the Agent tab performs a real operation.
- Proposal bytes are stored server-side under an opaque proposal identity.
  Clients never gain authority by reposting draft bytes.
- Proposed, accepted, rejected, conflicted, and undone records remain durable
  and inspectable for future review.
- Any future compaction is an explicit maintenance operation that preserves
  identity, disposition, provenance, review facts, and journal linkage.
- One acceptance produces exactly one applied journal entry. Duplicate
  settlement is idempotent. Reject writes no accepted content.
- A stale expected fingerprint becomes a visible conflict. It never
  overwrites current content.
- Undo restores byte-equivalent prior valid content and records the reversal.
- UI, API, and CLI reach one shared operation function with published schema
  validation, token gating, keyboard and screen-reader operation, mobile
  disclosure, static fallback, actionable empty and unavailable states, and no
  scoring or key authority.

19E AND EXT-04 ACCEPTANCE GATE

- Generated MCP names and count equal the canonical API_ROUTES and
  SURFACE_PARITY projection. Legacy aliases do not mint duplicate tools.
- Tool input and output schemas load through the existing resource path.
  A schema mutation changes tools/list without a code edit.
- tools/call reaches the same handler as HTTP and CLI. Locked-tier calls return
  typed MCP errors. Public assessment output never leaks a key.
- Initialization, discovery compatibility, cancellation, structured content,
  stdout-only JSON-RPC, and transport failure behavior match the current MCP
  contract recorded by the phase.
- An agent client can build a course through the tool table alone.
- EXT-04 is recorded as two proven composition recipes, Agent-tab composition
  and MCP projection. Do not generalize a loader or extension framework unless
  both recipes prove one missing shared primitive.

EXT-05 BOUNDARY

Do not implement arbitrary external package loading without a concrete package
need and an accepted design for package identity, exact version and digest,
host compatibility, dependency conflicts, grants, enforcement, installed
versus enabled state, activation failure, rollback, safe-mode startup, and
clean offline restore. First inspect metadata without importing package code.
Current first-party registries are not a sandbox. Preserve the no runtime fetch
rule unless the supply-chain policy is deliberately changed through its own
decision process.

AUDIT AND VISION CLOSURE

Before declaring Reach complete:

- Run the current quick and full preflight gates at the accepted revision.
  Attribute pre-existing or platform-only failures exactly.
- Run vision_audit.py and reconcile each current missing relationship. Do not
  make a user quotation say something it did not say.
- Recheck the UI character audit, 17C F-LOSS findings, Phase 18 gates, UAT and
  verification debt, rights and egress, source fidelity, accessibility,
  recovery, and external install evidence.
- Repair every applicable correctness, lost-work, navigation, keyed-content,
  or unusable-instruction defect that blocks the representative Reach journey.
- Create one compact closure index. Each finding must be exactly one of:
  verified closed, human owed, deferred with trigger, superseded with
  replacement evidence, or rejected with evidence and reconsideration
  condition. Every open finding needs an owner and next route.

POST-REACH OUTCOMES THAT MUST REMAIN VISIBLE

- Human accessibility, visual, and instructional acceptance.
- Clean portable recovery after every applicable F-LOSS-1 through F-LOSS-5
  owner repairs and a clean-machine offline rerun.
- A second-person cold install, removable sample, first-run guidance, return
  journey, Windows artifact evidence, and the signing decision.
- Comprehensive visual character selected through comparative prototypes and
  implemented accessibly.
- Broader evidence from more than one course and subject, plus triggered note,
  OCR, annotation, and executable-treatment prototypes.

Human-only work is a valid blocker, not permission to self-certify. When a
human gate is reached, prepare one concise packet with exact steps, expected
evidence, and the files to update after the human run. Continue all independent
automatable work before stopping.

COMMIT AND RECOVERY DISCIPLINE

Use one atomic commit per executable packet after that packet's verification
passes. Stage only named files. Never commit another writer's in-flight work.
Do not push unless separately authorized. Every durable mutation must state
its expected base, validate before acceptance, journal the accepted change,
preserve a recoverable before-image, and report its undo route. Never commit a
real learner question bank or private evidence.

RETURN CONTRACT

At each packet boundary report only:

- changed paths and commit hash;
- exact gates, exit codes, skipped legs, and observed failures;
- which user-vision and audit findings the evidence closes;
- remaining defects, human gates, deferred work, and their owners;
- the single next executable packet.

Do not report file counts or passing plans as product completion. The final
report must distinguish verified facts from inference and must use the exact
Reach completion label only when its real-course gate passes.
```
