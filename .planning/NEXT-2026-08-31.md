# What needs Weibao next (2026-08-31)

One session executed 17A-04 Task 1, then planned and executed Phase 16D
(the paced-lesson treatment) to its own human gate. Two human reviews and
one standing question are what remain yours; everything else agent-executable
is done or halts correctly behind these.

## 0. The 16D paced-lesson review (added later the same day)

Phase 16D shipped the paced lesson projection end to end: authored
`[STEP:]` markers, the lesson-run session, the tiered wrong-checkpoint
disclosure, and the paced view, with an 8-scenario tracer green. Sit the
synthetic fixture once:

```
python itembank.py daemon . --no-open
```

then open `/lesson/paced_lesson_bank?view=paced` on the printed URL:
keyboard once, pointer once, answer the mechanism-step checkpoint wrong
twice (watch tier 1 mark only your own picks, then the full reveal), jump
ahead from the Steps list, close the tab and reopen to see the resume.
Then write `16D-FREEZE.md` per 16D-04-PLAN Task 2 (freeze or withholding).
This review is independent of the 17A one below; either can go first.

## 1. The 17A accessibility review (the one blocking gate)

Everything is prepared. The scripted pass is in
`.planning/phases/17A-visual-system-component-foundation/17A-QA.md` under
"What remains human"; it covers keyboard, touch, VoiceOver, zoom, themes,
reduced motion, and the equivalent-task check, against the one-file fixture:

```
python -c "import sys; sys.path.insert(0,'.'); from surfaces import visual_fixture; open('/tmp/fixture.html','w').write(visual_fixture.single_file())" && open /tmp/fixture.html
```

The automated layout matrix is already green (13 gates, three themes, all
eight screens, the hover-only negative failing as required), so the review
is a confirmation pass over rendered reality, not a hunt. Expect 30 to 45
minutes.

Then either write `## Frozen at 17A` in `17A-FREEZE.md` (Task 2 of
`17A-04-PLAN.md` lists the required contents; verify the 13.9 summaries
first, which exist and were walked 2026-08-25) or `## Freeze withheld` with
the exact gap. If you would rather dictate the verdict and have an agent
write the record, the plan allows that; what it does not allow is an agent
signing the review itself.

## 2. The standing agent-signature question (one decision, not five)

16A, 16B, 16C, 15A, and 15B all closed their human-review legs with agent
signatures under your general delegation, and 15A/15B added six one-way
design decisions, one schema change, and one amendment to a frozen record.
Each is individually recorded with its reversal cost. The pattern needs one
ruling from you, roughly one of:

- **Ratify:** the recorded waivers stand as they are; future phases may
  close reviews the same way when you are absent, except accessibility.
- **Ratify with re-review:** the records stand, and you name which (if any)
  of the six one-way decisions you want re-opened.
- **Tighten:** future human-review legs halt for you, full stop.

Whichever you pick, A11Y-01 stays yours in every case; that was never
delegated.

## 3. What unblocks after the freeze

`17B-01` (the production vertical tracer's precondition run) halts today on
the missing `17A-FREEZE.md` and on nothing else; 14A through 16C are all
frozen. After your review, the 17B chain (exit gates G1 through G11), then
17C's restore sweep and Phase 18's cold install are all agent-executable to
their own human checkpoints (17B has two: the G4 screen-reader item and the
final visual acceptance).
