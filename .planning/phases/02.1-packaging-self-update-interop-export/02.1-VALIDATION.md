---
phase: 02.1
slug: packaging-self-update-interop-export
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-07
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Direct Python script execution via `python tests/<name>_roundtrip.py` — no pytest/unittest runner; matches every existing file in `tests/` [VERIFIED against `ls tests/`] |
| **Config file** | none — `.github/workflows/ci.yml` iterates `tests/*.py` directly [VERIFIED: `.github/workflows/ci.yml:70-74`] |
| **Quick run command** | `python tests/<touched>_roundtrip.py` (the file matching whatever this task touches) |
| **Full suite command** | `for t in tests/*.py; do python "$t" || exit 1; done` (existing CI pattern) |
| **Estimated runtime** | ~30 seconds (in line with existing suite scale; planner/executor to confirm once new files exist) |

---

## Sampling Rate

- **After every task commit:** Run the relevant new `*_roundtrip.py` file for whatever the task touches
- **After every plan wave:** Run the full `tests/*.py` suite
- **Before `/gsd-verify-work`:** Full suite must be green, plus the manual GIFT-import checkpoint below
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

Task ID / Plan / Wave are unassigned — no PLAN.md exists yet (VALIDATION.md is seeded before planning per the draft lifecycle above). `validate-phase` back-fills these columns once real task IDs exist. Rows below are keyed by requirement instead.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | DEL-01 | — | `.pyz` builds and its `__main__.py` runs itembank's existing CLI/daemon entry points without error | integration (subprocess: build then invoke) | `python tests/packaging_roundtrip.py` | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | DEL-02 | — | Built `.pyz` is a plain zip of readable `.py` source (not compiled/frozen) | unit (`zipfile` open, assert `.py` text-readable) | `python tests/packaging_roundtrip.py` | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | DEL-03 | — | Browser-path detection returns a plausible path or `None` per platform; `--app=` subprocess call attempted only when a browser is found | unit, mocked subprocess (a real GUI window can't be asserted automatically) | `python tests/launcher_roundtrip.py` | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | DEL-06 | Tampering — malicious/compromised release asset | `verify_digest()` accepts known-good and rejects known-bad checksums; version-tuple comparison rejects non-newer versions even with a valid checksum | unit | `python tests/update_roundtrip.py` | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | DEL-06 | Tampering — path traversal via asset filename | Side-by-side layout never opens the currently-running path for writing; manifest write is atomic (tmp+replace); asset filename is sanitized before joining into `versions/` | unit/integration | `python tests/update_roundtrip.py` | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | DEL-07 | — | Offline/unreachable GitHub API produces a silent no-op, not an exception or a blocked startup | unit, mocked `urllib.request.urlopen` raising `URLError` | `python tests/update_roundtrip.py` | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | DEL-08 | Input Validation (V5) — unescaped GIFT special chars | GIFT export of each of the six item types produces the expected mapping, or a loud per-item-number error for `build` | unit | `python tests/gift_export_roundtrip.py` | ❌ Wave 0 | ⬜ pending |
| — | — | — | DEL-08 | — | A real LMS importer accepts the exported file | **manual-only** — see Manual-Only Verifications below | manual | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/packaging_roundtrip.py` — stubs for DEL-01, DEL-02
- [ ] `tests/launcher_roundtrip.py` — stubs for DEL-03
- [ ] `tests/update_roundtrip.py` — stubs for DEL-06, DEL-07
- [ ] `tests/gift_export_roundtrip.py` — stubs for DEL-08 (automatable half)
- [ ] No new test framework install needed — existing direct-script-execution convention covers all of the above

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| A real LMS importer accepts the GIFT export | DEL-08 | No automated Moodle (or equivalent) instance available in this environment; Success Criterion 5 requires "a real LMS importer accepts" the file, which only a live import proves | Export a bank covering all six item types to GIFT; import into a real Moodle (or GIFT-compatible LMS) question bank; confirm `mc`/`multi`/`table`/`dnd`/`short` items import as expected and `build`-type items are excluded with a loud, per-item-number error at export time, not a silent drop or a mangled import |
| Frameless-window launch and browser-tab fallback | DEL-03 | Requires observing an actual OS window/GUI on Windows, macOS, and Linux; no Linux or macOS machine available this research session | Double-click the built launcher on each target OS; confirm a frameless app window opens when a supported browser is found, and an ordinary browser tab opens otherwise |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
