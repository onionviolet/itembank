---
phase: 10-retention-pacing-trends
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - evidence.py
  - retention.py
  - surfaces/lesson.py
  - surfaces/cli.py
  - schemas/response.schema.json
  - surfaces/protocol_cli.py
  - fixtures/lesson_retention_events.jsonl
  - tests/lesson_retention_roundtrip.py
  - tests/protocol_roundtrip.py
autonomous: true
requirements: [SCHED-01, SCHED-04, TREND-05]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A lesson enters the objective review queue only after the learner explicitly completes a named Phase 3 lesson heading; opening or scrolling a lesson writes nothing (SCHED-04, D-01, D-02)."
    - "The versioned completion event names the server/CLI-resolved bank, lesson slug, subject, and sorted referenced objectives, and the queue exposes its configured interval and due reason (D-04, D-13)."
    - "Completion and queue state are reproducible from live evidence; retraction removes the completion, and neither model activity nor a page-view event can create it (D-02, D-16)."
    - "D-21: lesson-to-item transfer measurement is deliberately deferred because it is not computable from the current evidence log; this plan records the cost rather than building an ungrounded metric."
    - "D-24: the lesson-completion seam is defined by this plan's explicit versioned event; no completion is ever inferred from a page view, and the queue surface renders only after this event and its configured interval exist."
  artifacts:
    - path: fixtures/lesson_retention_events.jsonl
      provides: "Synthetic explicit completion, retraction, and due-boundary fixture."
    - path: schemas/response.schema.json
      provides: "Published versioned lesson completion evidence event."
    - path: surfaces/lesson.py
      provides: "Explicit `lesson --ref … --complete` action reusing Phase 3 lesson resolution."
  key_links:
    - from: surfaces/lesson.py
      to: model.lesson_slug
      via: "completion resolves the same heading slug and question references as the Phase 3 reader"
      pattern: "lesson_slug|lesson_ref"
    - from: surfaces/lesson.py
      to: evidence.py
      via: "the explicit action calls the sole append_event writer"
      pattern: "append_event|lesson_completion_event"
    - from: retention.py
      to: lesson completion evidence
      via: "the queue derives next review and provenance from captured live completion events"
      pattern: "lesson_complete"
---

<objective>
Close SCHED-04 with an explicit, versioned Phase 3 lesson-completion seam and a transparent objective review-queue entry (D-01, D-02, D-04, D-16).

Purpose: Make lesson completion observable without guessing from page views and without adding mutable scheduler state.
Output: A documented completion event, synthetic fixture, CLI action, and derived queue behavior consumed by the Phase 10 snapshot.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-retention-pacing-trends/10-CONTEXT.md
@.planning/phases/10-retention-pacing-trends/10-RESEARCH.md
@.planning/phases/10-retention-pacing-trends/10-UI-SPEC.md
@.planning/phases/03-lesson-format-in-app-reader/03-06-SUMMARY.md
@.planning/phases/10-retention-pacing-trends/10-01-SUMMARY.md
@C:/Users/wayba/Downloads/CTF/itembank/.planning/phases/10-retention-pacing-trends/10-PATTERNS.md
@model.py
@surfaces/lesson.py
@evidence.py
@retention.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace an explicit lesson completion into the derived review queue</name>
  <files>evidence.py, retention.py, surfaces/lesson.py, surfaces/cli.py, tests/lesson_retention_roundtrip.py</files>
  <precondition>Phase 3 publishes `model.lesson_slug`, `model.parse_lesson`, question `lesson_slug`, and `surfaces.lesson.cmd_lesson`; `python tests/lesson_roundtrip.py` passes.</precondition>
  <behavior>
    - "`itembank lesson BANK --ref HEADING --complete` resolves the heading through Phase 3's slugifier, discovers the sorted unique objectives of items referencing it, appends exactly one completion event, and reports the next review date/interval from the resulting retention snapshot (SCHED-04, D-01, D-04)."
    - "The same command retried with the same completion identity is idempotent; a compensating retraction removes the queue entry without deleting its original event (D-02)."
    - "Rendering the lesson through CLI or `GET /lesson/<stem>`, including scrolling to the heading, writes no completion event; a heading with no referenced objective is refused with a named explanation (D-04)."
    - "A captured live `lesson_complete` event enters `retention.py`'s deterministic objective review queue with the configured interval, exact next-review date, full snapshot provenance, and reason `lesson review`; a compensating retraction removes that entry from a fresh snapshot (D-01, D-02, D-04, D-13)."
  </behavior>
  <action>Write the focused fixed-clock cases in `tests/lesson_retention_roundtrip.py` before production changes, using invented event ids/timestamps and temporary evidence only. Add a versioned `lesson_complete` event builder to `evidence.py`; it must require a non-empty Phase 3 lesson slug, namespaced subject, bank basename, sorted unique referenced objectives, local-day zone, timestamp/event id, and a deterministic dedupe key. It must still be appended only through `evidence.append_event`. Extend the existing `itembank lesson` command with a boolean `--complete` that is valid only with `--ref`; resolve the exact heading and objective list using `model.parse_lesson`, the question records, and `model.lesson_slug`, never a second parser, slugger, or filesystem path from client input. In `retention.py`, implement the dependent projection that consumes captured live `lesson_complete` events, creates one stable queue row per referenced objective, computes `next_review_date` from event local date plus the bounded `lesson_review_after_days`, and carries the capture's complete evidence claim into the queue row and resulting due reason. Since capture already applies compensating retractions, a fresh snapshot after retraction must contain neither the queue row nor its lesson-review due reason; do not special-case raw retraction lines in the surface. After append, capture once and print event status, configured interval, derived next date, reason, and snapshot id from that projection. Do not add a queue database, scheduler file, or surface-specific calculation. A read/render path must remain side-effect-free. This explicitly resolves the UI-SPEC OPEN seam without reopening a user preference: Phase 3 supplies the deterministic lesson→objective contract and the blocker audit says not to re-ask resolved derivation choices.</action>
  <verify>
    <automated>python tests/lesson_retention_roundtrip.py &amp;&amp; python tests/retention_roundtrip.py &amp;&amp; python tests/lesson_roundtrip.py</automated>
  </verify>
  <done>An explicit heading completion produces one append-only event and `retention.py` deterministically derives one provenance-bearing objective queue entry and next-review date; retries dedupe, a fresh capture after retraction removes the entry/reason, due boundaries are exact, and all lesson render/view paths remain read-only (SCHED-04, D-01, D-02, D-04, D-13).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish and regression-test the completion evidence contract</name>
  <files>schemas/response.schema.json, surfaces/protocol_cli.py, fixtures/lesson_retention_events.jsonl, tests/lesson_retention_roundtrip.py, tests/protocol_roundtrip.py</files>
  <behavior>
    - "The response/evidence schema validates the real versioned completion event and rejects missing slug, empty objective list, non-namespaced subject, client path, and duplicate objectives."
    - "`itembank schema --all` publishes the additive event without changing prior response, hint, mark, retraction, or selection event validity."
    - "No model interaction/proposal field is accepted as completion or mastery evidence (D-16)."
  </behavior>
  <action>Author `fixtures/lesson_retention_events.jsonl` with invented ids/timestamps covering completion, its deterministic next-review boundary, and compensating retraction, then add the lesson-completion shape to the existing additive evidence-event definitions in `schemas/response.schema.json`; do not create a Phase 10 event schema or writer. Describe that the event proves an explicit completion action, not reading time or page visibility. Keep the Phase 3 bank path private: public payload carries bank basename and stable lesson/objective identifiers only. Extend protocol and fixture tests to validate a real CLI-written completion, validate the committed fixture, reject malformed variants, and prove earlier event variants still validate. Add a structural assertion that the lesson GET/render call graph cannot reach `append_event`, while the explicit completion branch reaches exactly the existing writer. Assert model interaction/proposal events do not match the completion shape and are ignored by retention mastery/queue derivation per D-16.</action>
  <verify>
    <automated>python tests/protocol_roundtrip.py &amp;&amp; python tests/lesson_retention_roundtrip.py</automated>
  </verify>
  <done>The completion event is published through the existing protocol, malformed/private/model-derived alternatives are rejected, and previous evidence documents remain valid (D-02, D-16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI completion intent → bank/lesson resolver | User text selects a heading whose bank and objectives must be resolved locally. |
| lesson resolver → append-only evidence | A read-only lesson surface gains one explicit mutation branch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Spoofing | lesson completion | high | mitigate | Require an explicit `--complete` plus resolved existing heading and referenced objective; page views and arbitrary objective input cannot produce the event. |
| T-10-07 | Tampering | bank/lesson identifiers | high | mitigate | Resolve the provided bank through the normal CLI path and heading through `model.lesson_slug`; store basename/stable identifiers, never a client-derived output path. |
| T-10-08 | Repudiation | completion retry/retraction | medium | mitigate | Deterministic dedupe identity plus timestamped append-only completion and compensating retraction provide an auditable trail. |
| T-10-09 | Elevation of Privilege | model proposal → completion/mastery | high | mitigate | Schema and derivation allow only the explicit completion event and accepted human marks; model events are ignored. |
| T-10-SC | Tampering | package supply chain | low | accept | No package-manager install occurs; implementation uses existing stdlib/project code. |
</threat_model>

<verification>
- `python tests/lesson_retention_roundtrip.py` passes explicit action, due-boundary, idempotency, retraction, and read-only render cases.
- `python tests/protocol_roundtrip.py` validates the real completion event and all existing evidence variants.
- `python tests/lesson_roundtrip.py` preserves the Phase 3 reader, slug, source containment, and CLI/route parity contracts.
</verification>

<success_criteria>
- SCHED-04 has a real explicit event and transparent configured queue date.
- No view, scroll, model event, duplicate parser, queue store, or second writer can manufacture completion.
- Completion state remains disposable and reproducible from live evidence.
</success_criteria>

<output>
Create `.planning/phases/10-retention-pacing-trends/10-02-SUMMARY.md` when done.
</output>
