---
phase: 10-retention-pacing-trends
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - selection.py
  - surfaces/session.py
  - evidence.py
  - schemas/session.schema.json
  - schemas/response.schema.json
  - tests/selection_retention_roundtrip.py
  - tests/selection_roundtrip.py
  - tests/protocol_roundtrip.py
autonomous: true
requirements: [SCHED-01, TREND-01, TREND-04, TREND-05]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Phase 7 remains the sole item chooser; Phase 10 supplies only a server-derived normalized objective-weight map and evidence snapshot through an additive selector context (D-10)."
    - "Weak/due/at-risk objectives become more likely in the next ordinary practice/remediation sitting, while mastered objectives leave ordinary rotation when other work exists but remain reachable as a bounded fallback (TREND-01, D-11)."
    - "Weight movement is bounded and explainable per snapshot; existing miss-rate/difficulty/recency components compose visibly, and exam selection is byte-for-byte evidence-neutral (D-04, D-11, D-12)."
    - "The session, selection evidence event, returned trace, and every chosen reason name one identical snapshot id (D-01, D-05, D-10)."
    - "D-22: a pending Phase 8 model suggestion never advances an interval or grants mastery, though it counts as an attempt; scheduler replay reads accepted marks only."
  artifacts:
    - path: tests/selection_retention_roundtrip.py
      provides: "Fixed selection fixture proving weight influence, gradual updates, fallback reachability, and exam neutrality."
    - path: selection.py
      provides: "Phase 7 selector with one additive server-only retention context; no second chooser."
    - path: schemas/session.schema.json
      provides: "Published sitting-bound snapshot/weight authorization fields."
  key_links:
    - from: surfaces/session.py
      to: retention.py
      via: "start captures once and derives the context before calling the selector"
      pattern: "capture|objective_weights"
    - from: surfaces/session.py
      to: selection.select
      via: "existing sole selection seam receives the additive keyword-only context"
      pattern: "select"
    - from: selection.py
      to: evidence.py
      via: "the existing Phase 7 selection event records snapshot id, weight map, and component trace"
      pattern: "selection_event|append_event"
---

<objective>
Feed evidence-derived objective weights into Phase 7's sole selector with one sitting-wide snapshot and complete component trace (D-01, D-04, D-10 through D-12).

Purpose: Turn observed weak/at-risk evidence into the next session's bounded focus without adding a second picker or allowing clients/models to choose weights.
Output: Additive selector/session contracts, selection evidence provenance, and deterministic integration tests.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-retention-pacing-trends/10-CONTEXT.md
@.planning/phases/10-retention-pacing-trends/10-RESEARCH.md
@.planning/phases/07-selection-engine/07-06-SUMMARY.md
@.planning/phases/10-retention-pacing-trends/10-01-SUMMARY.md
@C:/Users/wayba/Downloads/CTF/itembank/.planning/phases/10-retention-pacing-trends/10-PATTERNS.md
@selection.py
@surfaces/session.py
@evidence.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one weak objective through the sole selector into a sitting</name>
  <files>selection.py, surfaces/session.py, evidence.py, tests/selection_retention_roundtrip.py, tests/selection_roundtrip.py</files>
  <precondition>Phase 7 plan 07-06 has published `selection.select(questions, spec, history) -> (items, trace)`, `surfaces.session.do_select`, selection events, selection modes, and its fixed-seed roundtrip suite.</precondition>
  <behavior>
    - "Given two otherwise equal objectives and a fixed seed, a weak/due objective receives the higher normalized weight and is chosen for a practice sitting; the trace names the same snapshot id and the exact miss/due/risk/mastery/normalization components (TREND-01, D-01, D-04, D-10, D-11)."
    - "`retention.py` returns no item indices; only `selection.select` chooses and records them (D-10)."
    - "Mastered objectives are outside ordinary practice/remediation rotation while a non-mastered eligible objective exists; when all eligible objectives are mastered, the bounded lower-weight fallback remains selectable (D-11)."
    - "Exam output and trace choice are unchanged when retention context is present versus absent; diagnostic preserves Phase 7's coverage policy, while practice/remediation consume weights (D-12)."
    - "The prior live selection event from the captured evidence supplies the previous map, so each new normalized value moves no more than configured `max_weight_step`; no mutable weight cache exists (D-02, D-12)."
  </behavior>
  <action>Create the fixed synthetic test first against Phase 7's real public seam. Extend `selection.select` with one optional keyword-only `retention_context=None`, keeping every existing three-argument caller and fixed-seed result compatible. The context contains only server-derived `snapshot` claim and normalized `objective_weights`; reject unknown keys, unbounded/non-finite weights, and a missing/mismatched snapshot. Compose objective weight after Phase 7's existing eligibility/cooldown and miss-rate/difficulty/recency terms, and record each named component plus final normalized multiplier in the existing trace. In practice/remediation, prioritize non-mastered work and place mastered objectives in the explicit all-mastered fallback; diagnostic retains its coverage contract and exam ignores the context completely. In `surfaces.session.do_select`/`do_start`, capture live evidence once, derive the weight context in `retention.py`, pass that exact object to `selection.select`, store its snapshot id and public weight map in the session, and append through Phase 7's existing selection-event builder/writer. Derive max-step history from the most recent live selection event already in the captured snapshot; do not add a weight file/cache. `retention.py` must not import/call selection, parse banks, pick item indices, or write evidence. Ignore Phase 8 proposal/model events and never let model output alter a component (D-16). Under D-22, a pending model suggestion increments the attempt/pacing count but contributes nothing to interval, due, or mastery; only accepted human marks feed the replayed scheduler state.</action>
  <verify>
    <automated>python tests/selection_retention_roundtrip.py &amp;&amp; python tests/selection_roundtrip.py</automated>
  </verify>
  <done>A fixed weak objective changes the next practice choice through Phase 7's sole selector, with bounded traceable weights and one shared snapshot; mastered fallback and exam/diagnostic neutrality are proven without another chooser/cache/writer (D-01, D-02, D-10 through D-12).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the sitting and selection-event provenance fields</name>
  <files>schemas/session.schema.json, schemas/response.schema.json, tests/selection_retention_roundtrip.py, tests/protocol_roundtrip.py</files>
  <behavior>
    - "A real started session validates with its retention snapshot id and public normalized weight map, and its matching selection event validates with the same values."
    - "A forged map, client path, answer key, hidden tier, model payload, or mismatched snapshot is rejected rather than echoed into session/trace (D-01, D-10, D-16)."
    - "Sessions and selection events written before Phase 10 remain readable through the existing additive/upgrade policy."
  </behavior>
  <action>Extend the existing session and selection-event schema variants additively with the public `retention_snapshot_id`, bounded normalized objective map, and component trace. Keep the full evidence claim in the selection trace/event, not duplicated as mutable session authority; the session stores only the authorization needed to keep the sitting bound to it. Add tests that start a real sitting, locate its live selection event, and compare identical snapshot/weight fields. Preserve Phase 7 schema versions unless its published upgrade policy requires a version bump; if bumped, add the normal forward upgrade rather than guessing old fields. Add negative cases for raw paths, keys, hidden tiers, non-finite/unbounded weights, model content, and mismatched snapshots.</action>
  <verify>
    <automated>python tests/protocol_roundtrip.py &amp;&amp; python tests/selection_retention_roundtrip.py</automated>
  </verify>
  <done>Session and selection evidence publish only the safe additive retention context, older documents remain supported, and snapshot mismatch/private/model data are rejected (D-01, D-10, D-16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retention derivation → selector | Derived weights can affect which authored items are served. |
| CLI/API start → server session | Client input must not inject weights, snapshots, paths, or private item data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-10 | Tampering | selector retention context | high | mitigate | Server derives an allowlisted bounded context, validates snapshot/map agreement, and never accepts client/model weights. |
| T-10-11 | Elevation of Privilege | alternate chooser | high | mitigate | Tests assert the Phase 10 module returns no item indices and all choices/selection events originate in Phase 7 `selection.select`. |
| T-10-12 | Repudiation | selected-item reason | medium | mitigate | Existing selection event gains the shared snapshot and named component trace for every chosen item. |
| T-10-13 | Information Disclosure | session/trace payload | high | mitigate | Schemas allowlist derived fields and negative tests reject keys, hidden tiers, paths, and provider/model payloads. |
| T-10-SC | Tampering | package supply chain | low | accept | No external dependency is installed. |
</threat_model>

<verification>
- `python tests/selection_retention_roundtrip.py` proves the weighted happy path, bounded change, mastered fallback, and exam/diagnostic behavior.
- `python tests/selection_roundtrip.py` preserves Phase 7 fixed-seed selection and trace contracts.
- `python tests/protocol_roundtrip.py` validates session and selection evidence payloads, including negative private-field cases.
</verification>

<success_criteria>
- Trend evidence changes the next eligible ordinary session without bypassing Phase 7.
- Every weight and choice is bounded, component-explainable, snapshot-labeled, and model-independent.
- Exam selection remains evidence-neutral and mastered material is out of ordinary rotation without becoming unreachable.
</success_criteria>

<output>
Create `.planning/phases/10-retention-pacing-trends/10-03-SUMMARY.md` when done.
</output>
