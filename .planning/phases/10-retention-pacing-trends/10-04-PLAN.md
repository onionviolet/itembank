---
phase: 10-retention-pacing-trends
plan: 04
type: execute
wave: 4
depends_on: [10-03]
files_modified:
  - surfaces/session.py
  - surfaces/cli.py
  - evidence.py
  - schemas/session.schema.json
  - schemas/response.schema.json
  - surfaces/day.py
  - surfaces/daemon.py
  - tests/pacing_roundtrip.py
  - tests/day_roundtrip.py
  - tests/daemon_roundtrip.py
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03, TREND-02, TREND-05]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The per-subject daily cap uses live ordinary response evidence in the configured local day, including pending manual responses and excluding retracted/deduped events; tick rows, Anki counts, models, and session files cannot change it (D-07, D-09)."
    - "At cap, ordinary start and further ordinary submit are blocked with the exact subject count/cap explanation; a deliberate override records one append-only event bound to one sitting and expires when that sitting ends (D-07, D-08)."
    - "Day load and scope are re-cut from the same snapshot after each live event and explain why; all derived counts retain the shared evidence claim (TREND-02, D-01, D-13)."
    - "itembank due objectives and Anki due/new cards remain separately owned/labeled and are never summed; Anki outage cannot block local cap/report/session behavior or cause a write to Anki (D-05, SCHED-03)."
    - "D-20: return rate is the phase's usage measure - sittings that occurred over sittings that came due, with a stated denominator; it is a property of pacing, never a streak, and never renders as a consecutive count, target, or loss state."
  artifacts:
    - path: tests/pacing_roundtrip.py
      provides: "Fixed local-day boundary, pending/retraction, cap block, scoped override, and submit guard fixture."
    - path: surfaces/session.py
      provides: "Server/runtime cap enforcement and sitting-bound override authorization."
    - path: surfaces/day.py
      provides: "Evidence-derived subject load and separated optional Anki signal."
  key_links:
    - from: surfaces/session.py
      to: retention.py
      via: "start/submit evaluate cap against one server-captured snapshot"
      pattern: "cap_decision|snapshot"
    - from: surfaces/session.py
      to: evidence.append_event
      via: "confirmed override uses the sole writer and is bound to generated session id"
      pattern: "cap_override_event|append_event"
    - from: surfaces/day.py
      to: surfaces/session.py
      via: "focused start delegates enforcement rather than trusting displayed count"
      pattern: "do_start|cap"
---

<objective>
Enforce evidence-derived per-subject pacing at runtime, including one-sitting audited override and honest separate Anki display (D-01, D-05, D-07 through D-09).

Purpose: Make the daily cap non-cosmetic and non-bypassable through a long sitting while preserving a deliberate, auditable exception.
Output: Runtime start/submit guard, override event/session authorization, evidence-derived `day` load, and fixed-clock pacing tests.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-retention-pacing-trends/10-CONTEXT.md
@.planning/phases/10-retention-pacing-trends/10-UI-SPEC.md
@.planning/phases/10-retention-pacing-trends/10-03-SUMMARY.md
@C:/Users/wayba/Downloads/CTF/itembank/.planning/phases/10-retention-pacing-trends/10-PATTERNS.md
@surfaces/session.py
@surfaces/day.py
@surfaces/daemon.py
@evidence.py
@retention.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace an at-cap subject through block, confirmed override, and sitting expiry</name>
  <precondition>Phase 8 plan 08-05 delivers the SURFACE_PARITY table and route/CLI parity registration; if SURFACE_PARITY is absent at execution, define it in this plan before registering tool names. Update `tests/daemon_roundtrip.py:check_api_route_scope` from the exact-4 assertion to route count 5 when POST /api/override lands, and keep its route-without-CLI-twin assertion intact.</precondition>
  <files>surfaces/session.py, surfaces/cli.py, evidence.py, schemas/session.schema.json, tests/pacing_roundtrip.py</files>
  <behavior>
    - "At four live ordinary responses against cap four, `itembank start BANK --subject emt` exits non-zero with `Today’s emt cap is reached (4 of 4 ordinary attempts).`; at three it starts at most one ordinary item and cannot submit a second genuine attempt without a block (D-07)."
    - "A pending manual response counts as an ordinary attempt, a retracted response does not, a duplicate retry does not, and events immediately outside the configured local-day boundary do not (D-07, D-09)."
    - "`--override-cap` without the exact explicit confirmation is rejected; with confirmation it appends one override event tied to generated session id/subject/local day/snapshot and authorizes only that sitting (D-08)."
    - "Cancelling or a rejected/forged override writes nothing; finishing the sitting removes authority, and a new sitting at cap is blocked again (D-08)."
    - "The override action is served by a named daemon route (POST /api/override with session, snapshot, subject, and explicit action token) registered in API_ROUTES/ROUTES/ROUTE_CLI with the CLI twin `itembank override`, and reserved MCP tool names are registered in SURFACE_PARITY per Extensibility Rule 9(a)."
  </behavior>
  <action>Build `tests/pacing_roundtrip.py` first with fixed UTC timestamps around two local-midnight boundaries and synthetic response/pending/retraction evidence. Add a versioned `cap_override` event builder in `evidence.py`, still using only `append_event`, with server-generated session id, namespaced subject, local date/zone, snapshot id, cap/count, timestamp, and deterministic dedupe identity. Extend the server-side `surfaces.session.do_start` path to derive subject from the Phase 7 selection specification/objective namespace and call `retention.cap_decision` on the same capture used for weights. At cap, block before choosing/writing a session. Below cap, limit ordinary selection to remaining capacity and persist a non-forgeable sitting authorization with snapshot/subject/cap; `do_submit` re-captures live evidence before a genuine append so retries do not count twice and a sitting cannot run past the cap. Add CLI `--subject`, `--override-cap`, and an exact confirmation token, but never a persistent ignore setting. Generate the session id before a confirmed override append, bind the event/authorization to it, and accept the exception only within that session's active lifetime. Preserve existing path-forbidding and server-generated output rules. Publish session fields here; the response event variant for the override is completed in Task 2. Use the locked UI copy exactly for the block. No day tick, session cursor, Anki result, model event, or cached value may enter the count. Read 10-PATTERNS.md before writing pacing code and follow its cited analog excerpts (surfaces/session.py do_start/do_submit guard shape, evidence.py day_tick_event builder analog for the cap_override event).</action>
  <verify>
    <automated>python tests/pacing_roundtrip.py &amp;&amp; python tests/evidence_roundtrip.py</automated>
  </verify>
  <done>Local-day live evidence gates both start and genuine submit; pending/retraction/dedupe boundaries are correct, and one explicitly confirmed audited override expires with its sitting without a persistent bypass (D-07, D-08, D-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace day tick-load pacing with snapshot-derived load and separated Anki signal</name>
  <files>surfaces/day.py, surfaces/daemon.py, schemas/response.schema.json, tests/day_roundtrip.py, tests/daemon_roundtrip.py</files>
  <precondition>Phase 4 has supplied the authoritative `day_document.snapshot/save`, presentation primitives, and themed day surface; Phase 10 may adapt their data source but not fork their writer or shell.</precondition>
  <behavior>
    - "`itembank day PLAN --check` reports each configured subject's ordinary count/cap and due objective count from one snapshot, then prints `Anki: D due · N new` separately when available (D-01, D-05, D-07)."
    - "Changing Anki due/new, closing Anki, or returning malformed Anki data changes only the Anki line; it never changes due objectives, cap count, recommendation, weight, or snapshot id (D-05, SCHED-03)."
    - "When Anki is unavailable the exact text is `Anki is unavailable; card counts are not shown.` and all local evidence functions remain usable offline (D-05)."
    - "After a recorded/retracted response, `day` captures again and explains the new load/re-cut reason; the old already-rendered payload remains internally consistent (TREND-02, D-01)."
    - "The D-20 return-rate ratio (sittings occurred / sittings due) is exposed with its stated denominator and never as a consecutive count, target, or loss state."
  </behavior>
  <action>Replace `surfaces.day.lane_load` as the pacing authority with `retention` subject summaries from one capture per `day_render`/`--check` call. Keep Phase 4's day-document writer and presentation primitives; keep Phase 1 day ticks only as the legacy checklist activity they represent, not course-load or cap evidence. Have focused-start paths delegate to `surfaces.session.do_start` so rendered numbers never authorize work. Preserve `anki_read` as a read-only optional external signal and label its due/new counts with owner `Anki`; label retention counts `itembank` and never calculate a total across them. Use the exact unavailable copy and omit stale/zero-looking card counts on failure. Remove the 60-second cache as a claim authority: if render caching remains for external Anki latency, key/segment it so the evidence snapshot and all itembank claims are captured together and Anki failure/age is disclosed separately. Publish/validate the override event variant in the existing response schema, including its sitting scope. Extend day and daemon tests for fourth-profile namespaced subjects, local-day recut, client-forged subject/cap/override rejection, offline operation, optional Anki zero/populated/unavailable states, and absence of Anki write methods.</action>
  <verify>
    <automated>python tests/day_roundtrip.py &amp;&amp; python tests/daemon_roundtrip.py &amp;&amp; python tests/pacing_roundtrip.py</automated>
  </verify>
  <done>`day` presents and enforces snapshot-derived per-subject pacing, re-cuts after live events, preserves legacy checklist ticks without using them as load, and keeps optional Anki counts owner-labeled/read-only/non-blocking (D-01, D-05, D-07 through D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/CLI start input → session runtime | A caller may try to forge subject, cap, snapshot, override, session path, or output path. |
| optional AnkiConnect → day presentation | Unavailable or hostile local-service data must not influence itembank pacing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14 | Tampering | cap subject/count/snapshot | high | mitigate | Server derives from captured evidence and authored objective namespaces; client values are forbidden/validated, and submit rechecks live evidence. |
| T-10-15 | Elevation of Privilege | cap override | high | mitigate | Exact confirmation plus server-generated sitting id, one append-only event, active-session check, and no persistent config flag. |
| T-10-16 | Denial of Service | Anki unavailable/malformed | medium | mitigate | Timeout/parse failure yields locked omission copy and never blocks local report/day/start. |
| T-10-17 | Tampering | day cache drift | high | mitigate | One capture per itembank render; any external cache is segregated and cannot supply evidence claims. |
| T-10-18 | Repudiation | ordinary-attempt count | medium | mitigate | Count live response event ids in the named local-day window, including pending and excluding retracted/deduped entries; expose count/cap/claim. |
| T-10-SC | Tampering | package supply chain | low | accept | No package-manager dependency is introduced. |
</threat_model>

<verification>
- `python tests/pacing_roundtrip.py` passes cap, boundary, pending/retraction, submit, and scoped-override cases.
- `python tests/day_roundtrip.py` passes evidence-load/re-cut and owner-labeled Anki states.
- `python tests/daemon_roundtrip.py` passes forged-input containment and offline server behavior.
- `python tests/evidence_roundtrip.py` preserves one writer, append-only history, and dedupe/retraction semantics.
</verification>

<success_criteria>
- A subject cannot be binged past its daily ordinary-attempt cap through start or a long sitting.
- The only exception is explicit, recorded, and expires with one sitting.
- Day load comes from live evidence, not ticks/session files/Anki/model/cache.
- Anki remains a separate read-only optional card signal and never becomes an objective scheduler.
</success_criteria>

<output>
Create `.planning/phases/10-retention-pacing-trends/10-04-SUMMARY.md` when done.
</output>
