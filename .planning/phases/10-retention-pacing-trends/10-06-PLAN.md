---
phase: 10-retention-pacing-trends
plan: 06
type: execute
wave: 6
depends_on: [10-05]
files_modified:
  - tests/phase10_uat.py
  - tests/retention_roundtrip.py
  - tests/retention_ui_roundtrip.py
  - tests/pacing_roundtrip.py
autonomous: false
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04, TREND-01, TREND-02, TREND-03, TREND-04, TREND-05]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One end-to-end fixture proves Today → evidence disclosure → focused start → Phase 7 trace → report uses one evidence claim, while subsequent evidence forces an explicit refresh instead of mixed snapshots (D-01, D-10, D-15)."
    - "Automated UAT covers all six states, retraction, pending/manual settlement, local-day caps, scoped override, lesson queue, weight bounds, prior-success silence, separate Anki states, offline/model outage, and public/private boundaries."
    - "Human verification at 1280, 768, and 375 pixels confirms visual hierarchy, readable uncertainty/evidence, semantic trend fallback, keyboard/focus behavior, and exact recovery copy."
    - "Round two: FSRS replay, stage/retired naming, utility-weighted ordering, return-rate ratio, deferred transfer, and pending-suggestion rules are proven end to end (D-17 through D-24)."
  artifacts:
    - path: tests/phase10_uat.py
      provides: "One-command Phase 10 cross-surface and security/UAT gate."
    - path: .planning/phases/10-retention-pacing-trends/10-VALIDATION.md
      provides: "Nyquist requirement-to-command and manual UI matrix used by this plan."
  key_links:
    - from: tests/phase10_uat.py
      to: CLI and daemon surfaces
      via: "same fixture drives `itembank trends`, Today/start API, selector trace, and `/report`"
      pattern: "snapshot_id"
    - from: manual UAT
      to: 10-UI-SPEC.md
      via: "approved widths, copy, accessibility, uncertainty, and evidence-disclosure checks"
      pattern: "1280|768|375"
---

<objective>
Close Phase 10 with cross-surface automated and human verification of evidence consistency, pacing safety, explainability, accessibility, and offline/public boundaries.

Purpose: Prove the assembled behavior, not just its individual functions, before downstream auditing can treat retention claims as trustworthy.
Output: One command-driven Phase 10 UAT harness, full regression gate, and a blocking UI verification checkpoint.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/UI-SPEC.md
@.planning/phases/10-retention-pacing-trends/10-UI-SPEC.md
@.planning/phases/10-retention-pacing-trends/10-VALIDATION.md
@.planning/phases/10-retention-pacing-trends/10-05-SUMMARY.md
@C:/Users/wayba/Downloads/CTF/itembank/.planning/phases/10-retention-pacing-trends/10-PATTERNS.md
@tests/retention_roundtrip.py
@tests/retention_ui_roundtrip.py
@tests/pacing_roundtrip.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one snapshot across Today, start, selector evidence, and report</name>
  <files>tests/phase10_uat.py, tests/retention_roundtrip.py, tests/retention_ui_roundtrip.py</files>
  <behavior>
    - "A synthetic weak/due objective appears on Today, opens evidence with complete claim, starts a focused session, is chosen by Phase 7, and appears in CLI/browser report with one identical snapshot id and component trace."
    - "Appending a response after Today render makes the old start request fail stale, and the refreshed Today/report consistently use a new snapshot id; neither response mixes fields from both."
    - "No answer/rubric/path/key/hidden tier/provider/model payload appears in HTML, JSON, text, session public view, or selection trace."
  </behavior>
  <action>Create `tests/phase10_uat.py` using temporary synthetic bank/evidence/settings paths and the existing daemon test harness; make no real network or Anki/model call. Drive the real CLI, daemon Today route, focused-start handler, Phase 7 selection event, and report route, comparing the complete evidence claim at every boundary. Append one event between render and start to prove fail-closed stale handling, then refresh and prove a wholly new consistent claim. Import shared fixture helpers from existing test modules instead of introducing another parser, scorer, runner, evidence writer, or report calculation. Scan every public serialization and DOM body against an explicit private-field allow/deny contract.</action>
  <verify>
    <automated>python tests/phase10_uat.py</automated>
  </verify>
  <done>The phase's headline path is proven end to end with one snapshot, explicit stale refresh, Phase 7 sole selection, report parity, and public/private containment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute the complete deterministic, offline, and security matrix</name>
  <files>tests/phase10_uat.py, tests/retention_roundtrip.py, tests/retention_ui_roundtrip.py, tests/pacing_roundtrip.py</files>
  <behavior>
    - "Matrix covers unknown/weak/stable/due/at-risk/mastered, month-old success, week windows, hint averages/highest, raw counts, pending/manual settlement, retraction, lesson completion/retraction, and bounded weights."
    - "Matrix covers per-subject local-day cap, below/at-cap start and submit, cancel/confirm/duplicate/expired override, fourth profile, separate Anki zero/populated/unavailable, and model/network outage."
    - "Every requirement maps to at least one focused command and the full pre-existing suite remains green."
  </behavior>
  <action>Fill any remaining cases named by `10-VALIDATION.md`, with fixed clocks and invented data. Ensure tests independently vary Anki and model availability while holding the evidence snapshot constant. Include exam neutrality, mastered fallback reachability, recent-selection max-step bounds, completed-lesson due boundary, pending pacing-only behavior, retracted disappearance, accepted-human-mark settlement, and ignored Phase 8 proposal events. Add ASVS1 negative cases for forged path/subject/cap/snapshot/override/weights, malformed settings, report filter/window bounds, private-field leaks, and large/sparse logs. Then run all Phase 10 focused tests, the upstream evidence/lesson/teaching/selection/day/daemon/protocol/config suites, and the repository-wide PowerShell suite command exactly as listed below.</action>
  <verify>
    <automated>Get-ChildItem tests\*.py | Sort-Object Name | ForEach-Object { python $_.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }</automated>
  </verify>
  <done>All SCHED-01..04 and TREND-01..05 deterministic/security/offline cases pass, along with every existing repository test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify responsive, accessible, and honest retention surfaces</name>
  <what-built>Phase 10 Today, evidence drawer, focused start, cap recovery/override, lesson completion, and overview→subject→objective report, all backed by the automated matrix.</what-built>
  <how-to-verify>
    1. Start `python itembank.py daemon fixtures --no-open --port 8730` with the synthetic Phase 10 fixture; keep browser devtools network offline for the offline pass and ensure no model server or Anki is required.
    2. At 1280px, open Today. Confirm the primary hierarchy is activity first; owner labels read `itembank` and `Anki`; counts are not totaled; due/unknown cards show raw evidence and the same snapshot id in card and adjacent evidence disclosure.
    3. Use keyboard only: Tab order reaches Today action → objective card → Why button → evidence; open/close disclosure and confirm focus returns. Verify state/count meaning is present in text and screen-reader names, not color alone.
    4. Start the due objective and confirm the resulting session/trace/report display the same snapshot id. Add one fixture response in another terminal, submit the old page, and confirm it asks for refresh and creates no sitting; refresh and confirm the marker changes consistently.
    5. Open the report at 1280px, 768px, and 375px. Drill overview → subject → objective, change week windows, and confirm raw counts, hint fields, pending, last date, risk reason, lesson queue, semantic trend table, text fallback, and evidence claim remain readable without horizontal page overflow (table may use its labeled scroll wrapper).
    6. At cap, confirm exact block/recovery copy. Open override confirmation; use Escape and `Keep today’s cap`, confirming focus returns and no event appears. Confirm once with `Start one additional sitting`, verify one event, complete the sitting, and verify the next start is blocked again.
    7. Open/read a lesson and confirm no completion is recorded. Use explicit `Mark lesson complete`, confirm its objectives and next review date appear, and verify a retraction removes the queue entry.
    8. With Anki unavailable, confirm exact text `Anki is unavailable; card counts are not shown.` and that Today/report/start still work. Inspect page/API output for absence of answer keys, rubrics, filesystem paths, credentials, hidden tiers, provider payloads, and raw model interactions.
  </how-to-verify>
  <resume-signal>Type `approved` or describe the failed width, copy, focus step, evidence mismatch, or behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthetic UAT client → public CLI/daemon | Final hostile/stale/forged inputs exercise all public boundaries. |
| automated result → human acceptance | Structural tests cannot alone prove readable hierarchy, responsive layout, or focus behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-24 | Tampering | cross-surface snapshot | high | mitigate | End-to-end stale-append test compares complete claims and asserts no write on mismatch. |
| T-10-25 | Information Disclosure | all public outputs | high | mitigate | Central negative allowlist scan plus human devtools/DOM inspection. |
| T-10-26 | Denial of Service | offline/large/sparse inputs | medium | mitigate | Fixed offline and volume cases prove local functions degrade honestly and remain bounded. |
| T-10-27 | Repudiation | UAT approval | medium | mitigate | Blocking checkpoint names exact URLs/actions/copy/results and records approval or a concrete failure. |
| T-10-SC | Tampering | package supply chain | low | accept | Verification installs no dependency and contacts no external service. |
</threat_model>

<verification>
- `python tests/phase10_uat.py` passes the cross-surface shared-snapshot tracer.
- The full PowerShell test loop passes every repository test.
- Human checkpoint verifies the approved UI contract at 1280/768/375, keyboard/focus, exact copy, offline behavior, and public-field containment.
</verification>

<success_criteria>
- Every Phase 10 requirement has automated evidence and the approved browser behavior has human evidence.
- No duplicate parser, scorer, runner, evidence store/writer, selector, or browser derivation was introduced.
- Snapshot consistency, uncertainty, cap safety, Anki separation, offline behavior, and security boundaries hold end to end.
</success_criteria>

<output>
Create `.planning/phases/10-retention-pacing-trends/10-06-SUMMARY.md` when done.
</output>
