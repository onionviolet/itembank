---
phase: 09-subject-invariant-loop-emt-math-cs-integration
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified:
  - vendor/katex/
  - build.py
  - resources.py
  - surfaces/daemon.py
  - surfaces/lesson.py
  - tests/math_offline_roundtrip.py
  - tests/packaging_roundtrip.py
autonomous: true
ui_classification: UI-BLOCKED
requirements:
  - LOOP-02
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Inline `$...$` and display `$$...$$` math render inside lesson content from the approved local KaTeX distribution with no CDN or build step (D-07)."
    - "Invalid or unsupported math remains readable as source with an honest unavailable/error state (D-06, D-07)."
    - "Math enhancement never enters `runtime.score_response`, item verification, feedback policy, or evidence (D-08)."
    - "The complete CSS/JS/font distribution is present in a checkout and the built `.pyz`, and every asset request is allowlisted."
    - "Math follows `09-UI-SPEC.md`: enhancement is scoped to `#lesson-content`, inline/display/error/unavailable/overflow states remain observable and accessible, and the page has no horizontal overflow at 320 CSS px or 200% zoom."
  artifacts:
    - path: ".planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-UI-SPEC.md"
      provides: "Binding learner-facing Math DOM, copy, lifecycle, accessibility, and responsive contract"
    - path: "vendor/katex/"
      provides: "Approved immutable KaTeX browser distribution, fonts, and license"
    - path: "surfaces/lesson.py"
      provides: "Lesson-scoped local math adapter and source-preserving fallback"
      contains: "renderMathInElement"
    - path: "surfaces/daemon.py"
      provides: "Allowlisted local KaTeX asset responses"
      contains: "KATEX_ASSETS"
    - path: "tests/math_offline_roundtrip.py"
      provides: "Inline/display/error/offline/no-network regression"
  key_links:
    - from: "surfaces/lesson.py"
      to: "surfaces/daemon.py"
      via: "reader page references only `/assets/katex/...` URLs"
      pattern: "/assets/katex/"
    - from: "surfaces/daemon.py"
      to: "resources.py"
      via: "known asset names are loaded through `resources.read_bytes()`"
      pattern: "read_bytes"
    - from: "build.py"
      to: "vendor/katex/"
      via: "explicit `STAGE_DIRS` inclusion preserves the complete distribution"
      pattern: "vendor"
    - from: ".planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-UI-SPEC.md"
      to: "surfaces/lesson.py"
      via: "Math adapter implements the specified lesson-content target, display wrapper, persistent source fallback, exact state copy, and accessible MathML behavior"
      pattern: "lesson-content|lesson-math-display|Math unavailable|Math could not be rendered"
---

<objective>
Ship offline mathematical presentation as a local, packaged enhancement of the one lesson reader.

Purpose: Satisfy LOOP-02 without introducing a network dependency, browser scorer, or second lesson parser (D-05 through D-08).
Output: The approved KaTeX distribution, explicit package/resource wiring, a lesson-scoped adapter, and offline regressions.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/UI-SPEC.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-CONTEXT.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-RESEARCH.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-UI-SPEC.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-VALIDATION.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-02-SUMMARY.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-03-SUMMARY.md
@.planning/phases/03-lesson-format-in-app-reader/03-06-SUMMARY.md

<interfaces>
- Phase 3 owns `surfaces.lesson.render_markdown()` and its escaped semantic HTML. Phase 9 enhances only the dedicated lesson-content node and does not parse markdown again (D-05).
- `subjects.session_profile()` exposes the stored `lesson.math` capability; client markup is a presentation hint, while the daemon/session snapshot remains authoritative.
- `resources.read_bytes(relpath)` is the only checkout/zipapp resource reader. `build.STAGE_DIRS` is the explicit bundled-directory allowlist.
- KaTeX is presentation-only. `runtime.score_response()` and the configured verifier remain the sole correctness path (D-08).
</interfaces>
</context>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `vendor/katex/` | Reviewed `katex.min.css`, `katex.min.js`, `contrib/auto-render.min.js`, complete CSS-referenced `fonts/`, and `LICENSE` |
| `build.py` | Explicit vendor directory staging |
| `resources.py` | Existing archive-safe byte reader retained as the only asset source |
| `surfaces/daemon.py` | Closed `KATEX_ASSETS` route map with exact MIME types |
| `surfaces/lesson.py` | `$...$`/`$$...$$` lesson-content adapter with safe bounds and readable fallback |
| `tests/math_offline_roundtrip.py` | Network-disabled checkout and packaged-artifact proof |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor the approved complete distribution and package it</name>
  <precondition>`09-03-SUMMARY.md` contains an approved immutable KaTeX version, official source/tag, MIT license result, tarball SHA-256, and required distribution inventory.</precondition>
  <files>vendor/katex/, build.py, resources.py, tests/packaging_roundtrip.py</files>
  <read_first>
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-03-SUMMARY.md` — exact approved version, checksum, source, license, and inventory; do not choose another release
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-RESEARCH.md` — Package Legitimacy Audit and Pitfall 1
    - `build.py` — `STAGE_FILES`, `STAGE_DIRS`, `stage()`, and zipapp creation
    - `resources.py` — `read_bytes()` checkout/archive behavior
    - `tests/packaging_roundtrip.py` — existing built-artifact inventory conventions
  </read_first>
  <behavior>
    - Test 1: the fetched archive SHA-256 equals the approved 64-hex value before any repository copy.
    - Test 2: the vendored tree contains the three browser files, `LICENSE`, and every font URL referenced by `katex.min.css`.
    - Test 3: a built `.pyz` contains the identical vendor members and `resources.read_bytes()` returns the same bytes in checkout and archive execution.
    - Test 4: no package manifest, npm install output, CDN URL, source map, documentation bundle, or unrelated archive member is added.
  </behavior>
  <action>
Fetch only the immutable tarball recorded in `09-03-SUMMARY.md` into a temporary directory, compute SHA-256, and stop on any mismatch. Copy the reviewed browser distribution to `vendor/katex/` with stable paths `katex.min.css`, `katex.min.js`, `contrib/auto-render.min.js`, `fonts/...`, and `LICENSE`; copy every CSS-referenced font and no unrelated package content. Record the approved version and tarball checksum in a short header comment or adjacent `VENDORING.md` only if the existing repository vendor convention requires it; the summary remains the decision record.

Add `vendor` to `build.STAGE_DIRS` so the explicit allowlist stages the tree. Keep `resources.read_bytes()` as the sole reader; only adjust its tests/docstring if needed for the new directory. Extend `tests/packaging_roundtrip.py` to build a real zipapp, inventory the exact required members, compare checksums to checkout bytes, and assert private banks/evidence remain excluded. Do not add npm metadata, a runtime install, or a browser build command (D-07).
  </action>
  <verify>
    <automated>python tests/packaging_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Vendored bytes match the human-approved tarball checksum and license/source decision.
    - Every font referenced by the CSS exists at the relative path the CSS names.
    - Checkout and `.pyz` reads return identical assets through `resources.read_bytes()`.
    - The artifact includes no private bank/evidence and no network/build dependency is introduced.
  </acceptance_criteria>
  <done>The complete reviewed math distribution is reproducibly bundled in the plain-Python artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render lesson-scoped math locally with readable failure states</name>
  <files>surfaces/daemon.py, surfaces/lesson.py, tests/math_offline_roundtrip.py</files>
  <read_first>
    - `surfaces/lesson.py` — Phase 3 `render_markdown()`, `lesson_page()`, dedicated lesson-content element, escaping, fence classes, and table containers
    - `surfaces/daemon.py` — route parsing, response helpers, CSP/security headers, and existing static-resource handlers
    - `subjects.py` — stored profile snapshot and `lesson.math` capability from 09-01/09-02
    - `vendor/katex/katex.min.css` — relative font URLs that the route map must serve exactly
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-CONTEXT.md` — D-05 through D-08
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-RESEARCH.md` — Pattern 2 and Pitfalls 2/5
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-UI-SPEC.md` - Math Presentation, Component/State Matrix, Copywriting, Accessibility, Responsive, and UI Considerations contracts
  </read_first>
  <behavior>
    - Test 1: a Math profile lesson emits local CSS/JS/auto-render references and ordered display-before-inline delimiters; EMT/plain profiles emit no math adapter.
    - Test 2: the adapter targets only `#lesson-content`, ignores `pre`/`code`, uses `trust:false`, `throwOnError:false`, `maxExpand:1000`, and `maxSize:50`.
    - Test 3: malformed or unsafe TeX remains visible and creates no arbitrary HTML class, image, link, external request, score, or evidence event.
    - Test 4: with outbound networking disabled, every page dependency resolves from the daemon or packaged artifact and inline/display examples remain renderable.
  </behavior>
  <action>
Define a closed `KATEX_ASSETS` map in `surfaces/daemon.py` from exact URL suffixes to archive-relative paths and MIME types. Serve only `GET /assets/katex/<known-name>` lookups through `resources.read_bytes()`; an unknown, encoded, nested, traversal, or query-manipulated name returns 404 and is never joined to disk. Include CSS-referenced fonts in the same map or derive the closed font names once from the reviewed inventory, never from the request path.

Extend `lesson_page()` through its existing profile/presentation metadata seam. When the stored profile enables math, load local assets in order and call `renderMathInElement(document.getElementById('lesson-content'), ...)` with delimiters `$$...$$` before `$...$`, `ignoredTags` including `pre` and `code`, `throwOnError:false`, `trust:false`, `maxExpand:1000`, and `maxSize:50`. Scope enhancement to lesson content, preserve Phase 3 escaped fence/source nodes, and add a readable unavailable notice when assets are absent. A parse error must leave the delimited source visible via KaTeX's error output; no error handler may delete it. Do not pass any math-derived value to scoring, feedback, or evidence (D-08).

Implement the binding `09-UI-SPEC.md` states explicitly: raw source stays readable while enhancement loads; successful display math uses `.lesson-math-display` with contained horizontal overflow and accessible MathML; a parse failure appends exactly **Math could not be rendered. Formula source is shown.**; a missing or corrupt local asset set appends exactly one lesson-level **Math unavailable. Formula source is shown.** notice. Retain source and surrounding document order with JavaScript disabled, at 320 CSS px, and at 200% zoom, without page-level horizontal scrolling or focus loss.

Create `tests/math_offline_roundtrip.py` using a temporary Math bank and real daemon/resource handlers. Assert local-only asset URLs, exact MIME responses, hostile/path cases, profile gating, code-fence immunity, packaged membership, and absence of `http://`, `https://`, `cdn`, or network fallback in the rendered page. The end-of-phase human check may confirm glyph layout, but automated completion must prove the offline resource graph.
  </action>
  <verify>
    <automated>python tests/math_offline_roundtrip.py &amp;&amp; python tests/lesson_roundtrip.py &amp;&amp; python tests/packaging_roundtrip.py</automated>
    <human-check>Open the packaged Math lesson with networking disabled and confirm one inline and one display expression render, malformed TeX remains readable, and code fences containing dollar signs are unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - Math-enabled lesson HTML references only local allowlisted assets and orders display before inline delimiters.
    - Invalid/unsafe TeX remains readable; `trust:false` and bounded expansion/size are present.
    - Code fences are not transformed and EMT/plain profiles remain ordinary shared-reader output.
    - Math code has no call or import path to scorer, verifier, teaching transition, or evidence writer.
    - `#lesson-content`, `.lesson-math-display`, accessible MathML, exact error/unavailable copy, source-visible loading/no-script behavior, and contained wide-formula overflow satisfy `09-UI-SPEC.md` in automated DOM assertions.
    - At 320 CSS px and 200% zoom, inline and display Math, raw-source fallback, notices, and keyboard-reachable contained overflow remain readable without page-level horizontal scrolling.
  </acceptance_criteria>
  <done>Math lessons render from bundled local assets offline while source, scoring authority, and non-Math readers remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| reviewed tarball -> repository | Third-party executable browser assets become shipped bytes |
| browser asset path -> daemon resource map | Untrusted URL text requests packaged resources |
| bank-authored TeX -> lesson DOM | Untrusted lesson text enters a math renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-08 | Tampering | KaTeX vendoring | high | mitigate | Require 09-03 approval, verify immutable tarball SHA-256 before copy, and inventory license/browser/font files. |
| T-09-09 | Information Disclosure | asset route | high | mitigate | Closed URL-to-resource map; never convert client path text into a filesystem path. |
| T-09-10 | Tampering | KaTeX trust features | high | mitigate | `trust:false`, escaped Phase 3 source, no raw-HTML adapter, hostile-command regression. |
| T-09-11 | Denial of Service | TeX expansion/size | medium | mitigate | `maxExpand:1000`, `maxSize:50`, non-throwing readable fallback. |
| T-09-SC | Tampering | vendored browser asset | high | mitigate | Human-approved official source/checksum/license from 09-03; no package install occurs. |
</threat_model>

<verification>
- `python tests/math_offline_roundtrip.py`
- `python tests/lesson_roundtrip.py`
- `python tests/packaging_roundtrip.py`
- End-of-phase network-unplugged glyph/layout check against the `.pyz`.
</verification>

<success_criteria>
- Inline and display math use only local approved bytes in checkout and package.
- Invalid or unsupported math is readable rather than absent.
- Math remains presentation-only and cannot score or write evidence.
</success_criteria>

<output>
Create `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-04-SUMMARY.md` when done.
</output>
