---
phase: 09-subject-invariant-loop-emt-math-cs-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - subjects.py
  - schemas/settings.schema.json
  - itembank.json
  - surfaces/settings.py
  - tests/subject_loop_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - LOOP-01
  - LOOP-05
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "EMT, Math, and CS are versioned profile data whose only variation is lesson capabilities, allowed item types, and verifier (D-01, D-02)."
    - "The conservative fallback remains code-owned, while shipped and additional known subjects come from the one validated settings registry (D-02, D-03)."
    - "A fourth synthetic subject added only to temporary configuration resolves through the same selector without a production-code edit (D-15, LOOP-05)."
  artifacts:
    - path: "schemas/settings.schema.json"
      provides: "Published versioned subject-profile registry and three shipped defaults"
      contains: "subject_profiles"
    - path: "subjects.py"
      provides: "Settings-backed registry loader over the selector built in 09-01"
      exports: ["load_registry"]
    - path: "tests/subject_loop_roundtrip.py"
      provides: "Schema/default parity and configuration-only fourth-profile proof"
  key_links:
    - from: "subjects.py"
      to: "surfaces/settings.py"
      via: "load_registry consumes validated merged settings"
      pattern: "load_settings|subject_profiles"
    - from: "schemas/settings.schema.json"
      to: "itembank.json"
      via: "schema default and checked-in registry are structurally identical"
      pattern: "subject_profiles"
---

<objective>
Publish the tracer's subject-profile seam as one versioned settings contract and prove configuration-only extension.

Purpose: Make D-02 and D-15 mechanically true before any media adapter consumes a profile.
Output: The `subject_profiles` settings group, EMT/Math/CS entries, a settings-backed loader, and a fourth-profile regression.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/UI-SPEC.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-CONTEXT.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-RESEARCH.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-VALIDATION.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-01-SUMMARY.md
@.planning/phases/02-daemon-consolidation-settings-foundation/02-03-SUMMARY.md

<interfaces>
- `subjects.validate_registry()` owns exact profile-shape and cross-field validation from 09-01; settings schema validation does not replace it.
- `surfaces.settings.load_settings(base)` merges complete schema defaults and validates known top-level keys; `defaults_from_schema()` requires nested defaults to be complete.
- The conservative unknown-subject profile from D-03 remains an internal constant. EMT, Math, CS, and any fourth known subject come only from settings data.
</interfaces>
</context>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `schemas/settings.schema.json` | Required `subject_profiles` object with `version` and closed `entries` profile definitions; each entry's `lesson` object carries `lesson_layout` (`"separate"` \| `"inline"`, folded from Phase 3.1 D-04) |
| `itembank.json` | Shipped EMT, Math, and CS entries matching the schema default |
| `subjects.py` | `load_registry(base)` over validated settings |
| `surfaces/settings.py` | Phase-9 reader status for `subject_profiles` |
| `tests/subject_loop_roundtrip.py` | Default parity, invalid-registry, and fourth-profile configuration tests |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish and load the versioned subject-profile registry</name>
  <files>subjects.py, schemas/settings.schema.json, itembank.json, surfaces/settings.py, tests/subject_loop_roundtrip.py</files>
  <read_first>
    - `subjects.py` — 09-01 exact profile keys, validator, selector, and conservative default
    - `schemas/settings.schema.json` — current `additionalProperties`, `required`, nested-default, and `x-itembank-phase` conventions
    - `itembank.json` — checked-in settings values and intentional deviations from schema defaults
    - `surfaces/settings.py` — `THIS_PHASE`, `defaults_from_schema()`, `merge_over_defaults()`, and `load_settings()`
    - `tests/subject_loop_roundtrip.py` — 09-01 selector/tracer helpers
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-CONTEXT.md` — D-01 through D-03 and D-15
  </read_first>
  <behavior>
    - Test 1: a missing settings file, the schema default, and checked-in `itembank.json` expose structurally identical EMT/Math/CS registries.
    - Test 2: duplicate ids, extra keys, invalid capability types, unsupported verifier ids, and malformed allowed-item lists fail before selection.
    - Test 3: `load_registry(base)` returns only validated profile data and never fabricates a subject entry in code.
  </behavior>
  <action>
Add required top-level `subject_profiles` to `schemas/settings.schema.json`, marked `x-itembank-phase: 9`, with exactly `version` and `entries`. Each entry contains exactly `id`, `version`, `lesson`, `allowed_item_types`, and `verifier`; `lesson` contains boolean `markdown`, `tables`, and `math`, a unique `runnable_languages` string array, and the `lesson_layout` enum `"separate" | "inline"` (folded from Phase 3.1 D-04: EMT and Math default `separate`, CS `inline`). Use only keywords supported by `schema_validate.py`.

Set registry version `1` and complete defaults for `emt`, `math`, and `cs`: EMT enables markdown/tables, Math enables markdown/tables/math, CS enables markdown/tables with runnable language `python`; only CS permits `check`; every other allowed item type must be copied from the established runtime types rather than invented. Use the existing runtime verifier id for EMT/Math and Phase 5's check verifier id for CS, per D-01/D-02. EMT and Math declare `lesson_layout: "separate"`, CS declares `lesson_layout: "inline"` (the Phase 3.1 two-file layout default with its per-subject override). Mirror the registry in checked-in `itembank.json` exactly.

Implement `subjects.load_registry(base)` by calling `surfaces.settings.load_settings(base)` and then `validate_registry()` on `subject_profiles`. Remove any temporary shipped-entry constant from 09-01; keep only D-03's conservative fallback. Advance `surfaces.settings.THIS_PHASE` to `9` so `itembank config` reports the group as active. Write the tests first and keep the public registry JSON-native and deterministic.
  </action>
  <verify>
    <automated>python tests/subject_loop_roundtrip.py &amp;&amp; python tests/config_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Schema defaults, missing-file settings, and checked-in EMT/Math/CS entries agree exactly.
    - `itembank config` reports `subject_profiles` as read by Phase 9 and publishes the closed entry contract.
    - Every malformed or duplicate registry case fails with a named settings/profile error.
    - No known-subject registry exists outside settings data.
  </acceptance_criteria>
  <done>The three shipped subjects enter the runtime through one validated, versioned data registry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove a fourth profile is configuration-only</name>
  <files>subjects.py, tests/subject_loop_roundtrip.py</files>
  <read_first>
    - `subjects.py` — `load_registry()`, `validate_registry()`, and `select_profile()` after Task 1
    - `tests/subject_loop_roundtrip.py` — the EMT tracer and temporary-bank helpers
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-CONTEXT.md` — D-02 and D-15
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-RESEARCH.md` — Pattern 4 profile-matrix fixture
  </read_first>
  <behavior>
    - Test 1: append a valid `fourth` entry to temporary settings, load it through `load_registry()`, and select it from a matching namespaced objective.
    - Test 2: the selected fourth snapshot has the same keys and version semantics as the three shipped profiles.
    - Test 3: an AST/source guard finds no production comparison or dispatch branch on literal subject ids.
  </behavior>
  <action>
Extend the shared roundtrip with a temporary settings root whose registry appends `fourth` as a plain-markdown/runtime-verifier profile, then resolve a temporary `fourth:` bank through the public loader and selector. Do not monkey-patch a registry, add a route, subclass a profile, or edit production code for the fourth case. Add an AST guard over application Python that rejects `if`/`match`/mapping dispatch keyed by the shipped ids while permitting those strings inside JSON profile data and tests. Keep selection/error behavior identical to 09-01.
  </action>
  <verify>
    <automated>python tests/subject_loop_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The temporary fourth entry validates, loads, and resolves without a production edit.
    - Its snapshot shape matches EMT/Math/CS and its requested unavailable capabilities remain explicit.
    - The structural guard finds no subject-name dispatch in application Python.
  </acceptance_criteria>
  <done>LOOP-05 has an executable configuration-only proof before surfaces consume the registry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| settings JSON -> profile registry | Editable local configuration becomes runtime capability policy |
| profile data -> selector | A valid-looking entry must not bypass closed shape, allowlist, or verifier validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-05 | Tampering | `subject_profiles` settings | high | mitigate | Closed schema plus `validate_registry()` duplicate/cross-field checks before selection. |
| T-09-06 | Elevation of Privilege | verifier/capability fields | high | mitigate | Accept only named verifier/capability allowlists and reject extra profile keys. |
| T-09-07 | Repudiation | registry version | medium | mitigate | Persist registry/profile versions in the session snapshot created by 09-01. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan performs no package operation. |
</threat_model>

<verification>
- `python tests/subject_loop_roundtrip.py`
- `python tests/config_roundtrip.py`
- Inspect the AST guard result: profile ids may occur as data, never application dispatch.
</verification>

<success_criteria>
- EMT, Math, and CS ship as validated configuration.
- A fourth subject enters the selector from temporary configuration only.
- The conservative unknown-subject behavior remains distinct and unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-02-SUMMARY.md` when done.
</output>
