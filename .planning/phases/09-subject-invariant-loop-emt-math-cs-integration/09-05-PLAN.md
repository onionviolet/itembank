---
phase: 09-subject-invariant-loop-emt-math-cs-integration
plan: 05
type: execute
wave: 4
depends_on:
  - 09-02
  - 09-04
files_modified:
  - runner.py
  - surfaces/cli.py
  - surfaces/session.py
  - surfaces/daemon.py
  - surfaces/lesson.py
  - fixtures/subject_loop_emt.md
  - fixtures/subject_loop_math.md
  - fixtures/subject_loop_cs.md
  - tests/check_roundtrip.py
  - tests/lesson_code_roundtrip.py
  - tests/subject_loop_roundtrip.py
autonomous: true
ui_classification: UI-BLOCKED
requirements:
  - LOOP-01
  - LOOP-03
  - LOOP-04
  - LOOP-05
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A profile-enabled CS lesson code fence runs through the Phase 5 process boundary and LAN/language/bounds policy, with observation-only output beside the block (D-05, D-09, D-10)."
    - "Pressing lesson Run advances no cursor or hint, creates no score, and appends no response/hint/correctness evidence (D-11)."
    - "CLI, API, reader, and resumed sessions resolve one profile id through the same server-side selector and stored snapshot (D-01 through D-04)."
    - "EMT, Math, and CS fixtures complete the identical lesson -> learner action/prediction -> wrong -> targeted hint -> retry -> evidence transition; only profile-controlled medium/type/verifier varies (D-12 through D-14)."
    - "A fourth temporary profile completes the same shared driver with configuration alone (D-15)."
    - "Runnable examples follow `09-UI-SPEC.md`: source and edits persist through ready/loading/completed/error/refusal states, keyboard users can enter and escape the editor, concise status is announced without live raw output, and every block remains usable at 320 CSS px and 200% zoom."
  artifacts:
    - path: ".planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-UI-SPEC.md"
      provides: "Binding runnable-code, EMT-table, guided-loop, copy, focus, accessibility, and responsive contract"
    - path: "runner.py"
      provides: "Observation-only `run_source()` primitive reused by `run_cases()`"
      exports: ["run_source", "run_cases"]
    - path: "surfaces/daemon.py"
      provides: "Guarded `POST /api/lesson/run` over stored profile and Phase 5 refusal policy"
      contains: "/api/lesson/run"
    - path: "surfaces/lesson.py"
      provides: "Runnable code-fence adapter with static/unavailable states"
    - path: "tests/lesson_code_roundtrip.py"
      provides: "One-runner, refusal, timeout/output, and zero-evidence proof"
    - path: "tests/subject_loop_roundtrip.py"
      provides: "Four-profile common-loop matrix and guided-discovery phase gate"
  key_links:
    - from: "surfaces/daemon.py"
      to: "runner.py"
      via: "lesson run calls `run_source()` exactly once after shared refusal checks"
      pattern: "run_source"
    - from: "surfaces/lesson.py"
      to: "surfaces/daemon.py"
      via: "Run posts session/block/language/source to `/api/lesson/run`"
      pattern: "/api/lesson/run"
    - from: "surfaces/session.py"
      to: "subjects.py"
      via: "start resolves a profile once; all later actions consume the stored snapshot"
      pattern: "select_profile|session_profile"
    - from: ".planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-UI-SPEC.md"
      to: "surfaces/lesson.py"
      via: "Stable code-block ids, labels, editor escape behavior, exact lifecycle/refusal copy, persistent status, independent output, semantic table wrappers, and responsive hooks are implemented and tested"
      pattern: "data-code-block|Run example|role=\"status\"|lesson-table-scroll"
---

<objective>
Complete the subject-invariant loop with runnable lesson code and one four-profile integration proof.

Purpose: Converge profile, lesson, math, runner, feedback, scorer, and evidence seams into one shared teaching path rather than three subject applications (D-01 through D-15).
Output: Observation-only lesson execution, public profile selection across existing clients, three guided-discovery fixtures, and the final common-loop matrix.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-CONTEXT.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-RESEARCH.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-UI-SPEC.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-VALIDATION.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-02-SUMMARY.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-04-SUMMARY.md
@.planning/phases/03-lesson-format-in-app-reader/03-06-SUMMARY.md
@.planning/phases/05-check-item-type-code-editor/05-07-SUMMARY.md
@.planning/phases/06-hint-ladder-cursor-hold-feedback-modes/06-02-SUMMARY.md

<interfaces>
- Phase 5 supplies `runner.run_cases(q, source, timeout_seconds, max_output_bytes, languages)`, `runner.UnknownLanguage`, process-tree cleanup, and settings keys `check.timeout_seconds`, `check.max_output_bytes`, `check.languages`, `check.allow_lan`.
- Phase 6 supplies `surfaces.session.do_action()`/`do_hint()` and `runtime.teaching_transition()`; the integration driver must use these public paths, not simulate transitions.
- Phase 3 fenced blocks are escaped `<pre><code class="language-X">` nodes. The existing renderer remains the only parser; Phase 9 may add stable block ids and controls at that seam (D-05/D-06).
- 09-01/09-02 supply `select_profile()`, `session_profile()`, and `load_registry()`. Only a profile id crosses a client boundary; capability objects never do.
</interfaces>
</context>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `runner.py` | `run_source(language, source, stdin_text, timeout_seconds, max_output_bytes, languages)` shared by lesson observations and `run_cases()` |
| `surfaces/daemon.py` | `execution_refusal()` shared by check submission and lesson Run; `POST /api/lesson/run` |
| `surfaces/lesson.py` | Stable code-block ids, editable Run control when available, readable unavailable/static states |
| `surfaces/cli.py` / `surfaces/session.py` | Optional profile id on existing start/lesson clients and one-time persisted resolution |
| `fixtures/subject_loop_{emt,math,cs}.md` | Synthetic guided-discovery lessons with matched loop structure |
| `tests/lesson_code_roundtrip.py` | Runner-reuse and observation-only evidence isolation |
| `tests/subject_loop_roundtrip.py` | EMT/Math/CS/fourth matrix over one transition driver |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run a lesson code block through the one bounded runner</name>
  <precondition>`runner.py`, `tests/check_roundtrip.py`, the `check` settings group, and Phase 5 LAN refusal tests exist and pass.</precondition>
  <files>runner.py, surfaces/daemon.py, surfaces/lesson.py, tests/check_roundtrip.py, tests/lesson_code_roundtrip.py</files>
  <read_first>
    - `runner.py` — `run_cases()`, per-case spawn/drain/timeout/output-cap/tree-cleanup implementation, and `UnknownLanguage`
    - `surfaces/daemon.py` — both Phase 5 check-submit LAN/language gates, API identifier allowlists, session index, JSON response helpers, and route table
    - `surfaces/lesson.py` — escaped `language-*` fence output and lesson page script conventions
    - `surfaces/settings.py` and `schemas/settings.schema.json` — `check` bounds, languages, and `allow_lan`
    - `evidence.py` — `events()`, `session_events()`, and the one append writer used to prove zero Run evidence
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-CONTEXT.md` — D-05, D-06, and D-09 through D-11
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-UI-SPEC.md` - Runnable Lesson Code, Component/State Matrix, Copywriting, Focus/Announcements, Accessibility, Responsive, and UI Considerations contracts
  </read_first>
  <behavior>
    - Test 1: an enabled Python fence posts edited source and receives bounded stdout/stderr/exit/timeout/truncation observation with no `passed`, `score`, `correct`, or `verdict` field.
    - Test 2: disabled profile language, missing runner setting, LAN-default refusal, unknown session/block, oversized source, extra authority-shaped fields, timeout, and output cap produce explicit bounded states before unsafe work.
    - Test 3: `run_cases()` retains all Phase 5 behavior after it delegates its process invocation to the same new primitive used by lesson Run.
    - Test 4: Run leaves session cursor/teaching state and live evidence counts byte-for-state unchanged.
  </behavior>
  <action>
Write `tests/lesson_code_roundtrip.py` first. Extract the already-proven Phase 5 process invocation into `runner.run_source(language, source, stdin_text, timeout_seconds, max_output_bytes, languages)`, returning only `stdout`, `stderr`, `exit_code`, `timed_out`, and `truncated`. Make `run_cases()` call this primitive for each authored case and retain its existing comparison/result contract; do not add another subprocess path, timeout, cap, cleanup implementation, or scoring function (D-09).

Factor the two Phase 5 daemon checks into `execution_refusal(handler, language, check_settings)`, returning the existing LAN or language refusal text or `None`; keep the existing check-submit copy byte-for-behavior and call the same helper from lesson Run. Add identifier-addressed `POST /api/lesson/run` accepting exactly `session_id`, `block_id`, `language`, and `source`. Resolve the session through `session_index`, load its stored profile and bank, confirm the block id/language against Phase 3 lesson output, confirm the language in both profile `runnable_languages` and `check.languages`, apply `check.allow_lan`, reject source above 65536 UTF-8 bytes, then call `run_source()` once with settings bounds. Reject extra fields including answer, action, score, correct, tier, evidence, path, bank, verifier, capabilities, and argv.

Add stable sequential `data-code-block` ids to Phase 3 fence output without reparsing source. A daemon-backed profile-enabled fence gets an editable source field, a Run button, and an adjacent `role="status"` result region; static output or a disabled/refused language keeps escaped source and says why Run is unavailable (D-06/D-10). Merely running does not call session submit/hint, score, or evidence APIs (D-11).

Implement every observable runnable-code state from `09-UI-SPEC.md`: a visible **Example code** and language label, labeled textarea, native **Run example** button, exact keyboard-help copy, block-local **Running example...** state, concise persistent `role="status"`, separately labeled non-live stdout/stderr regions, neutral exit-code completion, timeout/truncation/request-error recovery, and distinct disabled-language/static/LAN refusal copy. Preserve edits and prior bounded output while running or after error; keep focus on the triggering button; make blocks independent; support Tab/Shift+Tab editing and Escape-then-Tab exit; retain native Enter/Space activation, visible focus, 44px targets, reduced-motion behavior, 320 CSS px/200% zoom reflow, and contained code/output scrolling. Never announce raw output as a live region or use assessment terms for Run observations.
  </action>
  <verify>
    <automated>python tests/lesson_code_roundtrip.py &amp;&amp; python tests/check_roundtrip.py &amp;&amp; python tests/scoring_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Lesson execution and check execution share `run_source()` plus the existing bounds/tree cleanup; no second process implementation exists.
    - Every server-side profile/language/settings/LAN/source check occurs before invocation.
    - Observation payloads contain no correctness authority and Run changes no session/evidence state.
    - Static and refused fences retain escaped readable source and an explicit unavailable reason.
    - Automated DOM/state assertions cover `data-code-block` identity, visible labels, exact ready/loading/completed/timeout/truncated/request-error/refusal copy, persistent concise status, non-live labeled streams, edit retention, independent block state, focus hooks, and editor escape instructions required by `09-UI-SPEC.md`.
    - Keyboard-only operation, visible focus, reduced motion, 44px controls, contained overflow, and 320 CSS px/200% zoom behavior meet the UI contract without page-level horizontal scrolling.
  </acceptance_criteria>
  <done>A CS learner can execute lesson code through the existing safe-for-accidents runner boundary without turning exploration into assessment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire one profile id through existing clients and resumes</name>
  <files>surfaces/cli.py, surfaces/session.py, surfaces/daemon.py, surfaces/lesson.py, tests/subject_loop_roundtrip.py</files>
  <read_first>
    - `surfaces/cli.py` — Phase 3 `lesson`, Phase 6 `start`, and their argparse/cmd wiring
    - `surfaces/session.py` — 09-01 profile selection at `do_start`, legacy one-time fill, and shared `do_action()`/`do_hint()` path
    - `surfaces/daemon.py` — `/api/start`, lesson route/query parsing, `SystemExit` containment, and path-field rejection
    - `surfaces/lesson.py` — route-shared reader and profile presentation metadata
    - `subjects.py` — `load_registry()`, `select_profile()`, and stored snapshot/error contract
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-CONTEXT.md` — D-01 through D-04
  </read_first>
  <behavior>
    - Test 1: CLI `start --profile X`, API start `{profile:X}`, CLI `lesson --profile X`, and `/lesson/<bank>?profile=X` resolve equivalent public metadata.
    - Test 2: only the id crosses the client boundary; profile objects/capabilities/verifiers are rejected and recomputed server-side.
    - Test 3: unknown ids and mixed banks without an explicit id fail before session/evidence creation and disclose no absolute path.
    - Test 4: registry changes after start cannot change a resumed session's stored profile.
  </behavior>
  <action>
Add optional `--profile PROFILE_ID` to existing `start` and `lesson` commands, optional string `profile` to `/api/start`, and the same optional lesson query parameter. Extend `do_start(..., profile_id=None)` at the end of its signature for compatibility; it loads the served-root registry, calls the 09-01 selector once, validates allowed item types, and persists the complete snapshot. The daemon accepts only the id, resolves the bank through its existing stem allowlist, and returns path-free 4xx errors under the existing `SystemExit` containment. Lesson presentation uses the same selector before a session and the stored snapshot once a session id exists. Do not accept client-supplied profile content or add a subject route/surface (D-02/D-04).
  </action>
  <verify>
    <automated>python tests/subject_loop_roundtrip.py &amp;&amp; python tests/daemon_roundtrip.py &amp;&amp; python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - All existing clients resolve the same id to equivalent public profile metadata.
    - Mixed/unknown cases fail before writes; explicit valid selection persists and resumes unchanged.
    - No client can set verifier, allowed types, or capabilities directly.
  </acceptance_criteria>
  <done>Profile choice is one data option over existing clients and one reproducible stored session fact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove four subjects traverse one guided-discovery loop</name>
  <files>fixtures/subject_loop_emt.md, fixtures/subject_loop_math.md, fixtures/subject_loop_cs.md, tests/subject_loop_roundtrip.py</files>
  <read_first>
    - `tests/subject_loop_roundtrip.py` — 09-01 EMT tracer, 09-02 fourth-profile case, and common helpers
    - `tests/hint_roundtrip.py` — authoritative wrong/hint/retry/evidence action order and expected event shape
    - `tests/math_offline_roundtrip.py` — Math profile/media assertions
    - `tests/lesson_code_roundtrip.py` — CS profile/media and Run-isolation assertions
    - `model.py` — Phase 3 synthetic lesson grammar and Phase 5 `check` fixture grammar
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-CONTEXT.md` — D-12 through D-15
    - `.planning/ROADMAP.md` — Phase 9 Success Criteria 1 through 6
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-UI-SPEC.md` - Shared Lesson Layout, EMT Prose/Table, Runnable Code, State Matrix, Accessibility, and Verification-Ready contracts
  </read_first>
  <behavior>
    - Test 1: EMT, Math, and CS cases call the same driver and produce equal transition names, cursor progression, hint tiers, response/hint event keys, and final evidence outcome.
    - Test 2: the only case differences are fixture path, profile id, medium assertion, allowed item type, verifier id, and response value.
    - Test 3: each fixture orders context, learner action/prediction, immediate targeted feedback/hint, retry, and explanation.
    - Test 4: the temporary fourth profile from 09-02 completes the same driver without production changes.
  </behavior>
  <action>
Create three synthetic, non-course fixtures with parallel guided-discovery structure. EMT uses shared headings/prose/list/table markup and a runtime-scored item; Math uses `$...$` and `$$...$$` plus a runtime-scored item; CS uses a `python` fence, observation-only Run, and a `check` item. Each lesson must place context before a prediction/action, make the wrong response unlock one targeted authored hint, permit a materially changed retry, and present explanation after the learner move. Keep all real course/private content out of the repository.

Refactor `tests/subject_loop_roundtrip.py` to one table-driven `run_subject_case(case)` that drives the real lesson/start/action/hint/retry/evidence APIs. The EMT/Math/CS rows may vary only profile id, fixture, media assertion, permitted item type, verifier id, and response. Assert identical runtime transitions and evidence key sets; also call Math and CS specialty roundtrips. Append the temporary fourth plain profile from configuration and pass it through the same driver. Retain the AST guard against subject-name dispatch. This is D-14/D-15's executable acceptance proof, not a documentation-only assertion.

Make the final matrix assert the observable `09-UI-SPEC.md` contract as part of the shared loop: one `#lesson-content` source order, native labeled and focusable `.lesson-table-scroll` semantics for EMT, local inline/display/error/unavailable Math hooks, independent runnable-code lifecycle/status/output hooks for CS, and readable media-degraded content. Record the manual 320 CSS px/200% zoom, keyboard/focus, assistive-table/MathML, light/dark, reduced-motion, and network-unplugged observations in the phase verification/UAT evidence.
  </action>
  <verify>
    <automated>python tests/subject_loop_roundtrip.py &amp;&amp; python tests/math_offline_roundtrip.py &amp;&amp; python tests/lesson_code_roundtrip.py &amp;&amp; python tests/hint_roundtrip.py</automated>
    <human-check>At 320 CSS px and 200% zoom, verify EMT table semantics/scroll, Math inline/display layout, CS Run keyboard/status behavior, and one complete wrong-hint-retry flow per shipped subject.</human-check>
  </verify>
  <acceptance_criteria>
    - Three shipped subjects and a configured fourth use one driver and the same transition/evidence shape.
    - EMT structure, Math offline presentation, and CS runnable prose are the only medium-specific assertions.
    - Each fixture demonstrates context -> action/prediction -> targeted feedback -> retry -> explanation.
    - The full direct-script suite exits 0 after the phase-specific commands.
    - The automated matrix proves the semantic hooks, exact lifecycle/error/unavailable states, source preservation, and observation-only wording required by `09-UI-SPEC.md`; manual phase evidence records keyboard/focus/accessibility and narrow/zoom behavior for all three learner-facing media.
  </acceptance_criteria>
  <done>LOOP-01 through LOOP-05 are proven as one configurable teaching loop across EMT, Math, CS, and a fourth profile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser lesson Run -> daemon | Editable source and identifiers request host code execution |
| stored session/profile -> runner gate | Persisted capability data constrains a dangerous action but never supplies argv |
| subject fixture/profile -> shared runtime | Media/type/verifier data may vary while scoring, feedback, cursor, and evidence remain common |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-12 | Elevation of Privilege | `/api/lesson/run` | high | mitigate | Server resolves session/block/profile, intersects profile and settings languages, reuses default-closed LAN refusal, and rejects authority fields before `run_source()`. |
| T-09-13 | Denial of Service | lesson source execution | high | mitigate | 65536-byte source ceiling plus Phase 5 timeout, output cap, drain, and process-tree cleanup. |
| T-09-14 | Repudiation | exploratory Run vs response | high | mitigate | Observation payload contains no verdict; zero session/evidence delta is asserted around every Run. |
| T-09-15 | Tampering | subject dispatch | medium | mitigate | Data registry, one table-driven matrix, and AST guard prohibit subject-name application branches. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install occurs; this plan consumes the already-approved/vendored 09-04 asset. |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Feature / decision | Plan | Status |
|---|---|---|---|---|
| GOAL | — | One loop across EMT prose, offline Math, runnable CS | 09-01, 09-04, 09-05 | COVERED |
| REQ | LOOP-01 | Subject-invariant loop | 09-01, 09-02, 09-05 | COVERED |
| REQ | LOOP-02 | Offline vendored LaTeX | 09-03, 09-04 | COVERED |
| REQ | LOOP-03 | Runnable lesson code | 09-05 | COVERED |
| REQ | LOOP-04 | EMT prose and tables | 09-01, 09-05 | COVERED |
| REQ | LOOP-05 | Fourth subject by configuration | 09-02, 09-05 | COVERED |
| RESEARCH | — | Persisted profile, local assets, one runner, profile matrix | 09-01 through 09-05 | COVERED |
| CONTEXT | D-01–D-04 | Profile data, fallback, reproducible selection | 09-01, 09-02, 09-05 | COVERED |
| CONTEXT | D-05–D-08 | Semantic media seam and offline presentation-only math | 09-04, 09-05 | COVERED |
| CONTEXT | D-09–D-11 | One runner, server gating, zero Run evidence | 09-05 | COVERED |
| CONTEXT | D-12–D-15 | EMT semantics and four-profile integration proof | 09-01, 09-02, 09-05 | COVERED |

Deferred Phase 10 scheduling/pacing, private subject authoring, and additional language runners are exclusions, not gaps.

<verification>
- `python tests/subject_loop_roundtrip.py`
- `python tests/math_offline_roundtrip.py`
- `python tests/lesson_code_roundtrip.py`
- `python tests/check_roundtrip.py`
- `python tests/hint_roundtrip.py`
- Run every `tests/*.py` direct script as the phase gate.
</verification>

<success_criteria>
- EMT, Math, CS, and a configured fourth subject traverse one runtime/feedback/evidence loop.
- Lesson code reuses the Phase 5 runner and produces observation only.
- Guided discovery, offline math, semantic EMT tables, resume fidelity, and configuration-only extension are all executable assertions.
</success_criteria>

<output>
Create `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-05-SUMMARY.md` when done.
</output>
