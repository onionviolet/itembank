---
phase: 13.5-reading-teaching-surface-quality-pass
plan: 08
subsystem: teaching-ui
status: complete-machine-evidence
tags: [aria-live, mutation-observer, accessibility, compatibility-golden]
requires:
  - phase: 13.5-07
    provides: progressive quiz baseline and authored ladder
provides:
  - bounded lesson, quiz, generated-help and visual ActionStatus announcements
  - mutation-order proof for both action-blocking failure paths
  - independent pre-13.5 public-item compatibility golden
  - RTS-01 through RTS-12 machine and human gate roll-up
affects: [phase-13.5-verification, reading-teaching-surface]
tech-stack:
  added: []
  patterns:
    - live attributes are written before action-blocking failure text
    - asynchronous generated-help status is live only while pending
key-files:
  created: [tests/js/live_regions.test.mjs, fixtures/quiz_public_item_pre_13_5.json]
  modified: [surfaces/lesson.py, surfaces/quiz_page.py, tests/lesson_roundtrip.py, tests/model_ui_roundtrip.py, tests/visual_accessibility_roundtrip.py, .planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-GATES.md]
key-decisions:
  - "The page-level lesson status is the only steady-state polite reader region; runnable readouts are aria-live=off until a blocking error promotes them."
  - "The 06.1 visual ActionStatus remains the sole additional persistent polite region on visual items."
requirements-completed: [RTS-10, RTS-12]
duration: 35min
completed: 2026-08-14
---

# Phase 13.5 Plan 08: Live Regions and Additive Floor Summary

**Reader and quiz announcements now have bounded lifecycles with ordered blocking alerts, while a historical key-free public-item golden proves optional teaching grammar remains additive.**

## Accomplishments

- Reduced a multi-block lesson to one steady-state polite region, with each Run control describing an idle non-live readout.
- Made generated-help status transient and preserved the 06.1 visual ActionStatus exception with assertive, attribute-first blocking failures.
- Captured the canonical public-item bytes from commit `8e397b66b8f23d6706ec2fce27e8d9e817903296` and compare current synthetic no-TERMS/no-LESSON output directly to that fixture.
- Added an RTS-01 through RTS-12 evidence roll-up without claiming browser, geometry, no-network or assistive-technology results.

## Verification

Passed:

- `PYTHON=python3 node --test tests/js/live_regions.test.mjs`
- `python3 tests/lesson_roundtrip.py`
- `python3 tests/model_ui_roundtrip.py`
- `python3 tests/visual_accessibility_roundtrip.py`
- `python3 tests/gate_roundtrip.py`
- `python3 itembank.py lint fixtures/sample_bank.md`, 0 errors and 6 expected warnings
- `python3 itembank.py guard .`, 0 offending files

The complete `tests/*.py` suite was attempted. Plan-owned targeted suites pass. The broad run also surfaced pre-existing or out-of-scope failures already represented by the repository's current state: the optional `cryptography` dependency is absent in `tests/lti_roundtrip.py`; `tests/audit_writer_roundtrip.py` selects the shadow backend in its isolated clean fixture; desktop stem-size debt remains in `tests/surface_roundtrip.py`; and existing phase/subject fixtures still assert the prior persistent runnable status markup. `tests/lesson_code_roundtrip.py` specifically expects `role=status` plus `aria-live=polite`, which is the behavior RTS-10 intentionally replaces. That file is outside this plan's ownership and was not modified.

## Human Verification

PENDING HUMAN: responsive geometry, 200 percent zoom, real screen-reader announcements, sticky-header clearance, glossary popover placement, and the loopback-only no-network and script-free quiz walkthroughs. The exact procedures and result fields remain in `13.5-GATES.md`. Phase verification must remain open until those observations are recorded.

## Deviations from Plan

None. The repository's one-commit-per-plan rule superseded generic per-task TDD commits; RED/GREEN behavior was exercised within this plan's single atomic commit.

## Known Stubs

None.

## Threat Flags

None beyond the plan threat register. This plan changes announcement semantics and test evidence only; it adds no endpoint, authority path, persistence boundary or schema.

## Self-Check: PASSED

All plan-owned production, test, fixture, gate and summary files exist. Targeted automated verification passes, the independent golden is checked in, and every browser or assistive-technology result remains explicitly pending.
