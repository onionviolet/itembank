---
phase: 13.5-reading-teaching-surface-quality-pass
plan: 08
type: execute
wave: 7
depends_on: ["13.5-07"]
files_modified:
  - surfaces/lesson.py
  - surfaces/quiz_page.py
  - tests/js/live_regions.test.mjs
  - tests/lesson_roundtrip.py
  - tests/model_ui_roundtrip.py
  - tests/visual_accessibility_roundtrip.py
  - fixtures/quiz_public_item_pre_13_5.json
  - .planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-GATES.md
  - .planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-08-SUMMARY.md
autonomous: true
requirements: [RTS-10, RTS-12]

must_haves:
  truths:
    - "An ordinary quiz has one steady-state polite region, a visual item has the one additional ActionStatus region required by 06.1, and a lesson with multiple runnable blocks has one page-level polite region."
    - "Generated-help status is live only from the learner's request until its result renders, then returns to static text."
    - "Runnable-code and visual action-blocking failures set live attributes before failure text and never combine role=alert with aria-live=off."
    - "A bank with neither TERMS nor LESSON follows the unchanged item-rendering branch byte for byte against a checked-in pre-feature golden."
    - "Every Phase 13.5 gate names machine evidence or remains explicitly pending for human browser verification."
  artifacts:
    - path: "tests/js/live_regions.test.mjs"
      provides: "Mutation-order and transient-live-region behavior tests"
    - path: "tests/lesson_roundtrip.py"
      provides: "Lesson live-region count and additive item-rendering compatibility proof"
    - path: ".planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-GATES.md"
      provides: "Phase gate and requirement evidence roll-up"
  key_links:
    - from: "surfaces/lesson.py"
      to: "tests/js/live_regions.test.mjs"
      via: "real RUNNABLE_JS exercised with MutationObserver"
      pattern: "function setStatus"
    - from: "surfaces/quiz_page.py"
      to: "tests/visual_accessibility_roundtrip.py"
      via: "06.1 ActionStatus remains the sole visual-item exception"
      pattern: "visual-status"
---

<objective>
Close Phase 13.5's live-region and additive-format requirements, then roll up
machine and human evidence without overstating either.

Purpose: reduce competing announcements without silencing action-blocking
failures, preserve the 06.1 visual interaction contract, and prove that banks
which use none of the optional teaching grammar keep the same item markup.

Output: corrected ARIA lifecycles, executable count and mutation-order tests, a
real byte-identity golden, and a gate document ready for human browser rows.
</objective>

<execution_context>
@/Users/weiwei/Documents/Dev/itembank/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weiwei/Documents/Dev/itembank/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/UI-SPEC.md
@.planning/phases/06.1-interactive-visual-assessment-protocol/06.1-UI-SPEC.md
@.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-UI-SPEC.md
@.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-07-SUMMARY.md
@surfaces/lesson.py
@surfaces/quiz_page.py
@tests/js/hint_ladder.test.mjs

<interfaces>
`06.1-UI-SPEC.md` requires one persistent polite ActionStatus for visual items
and `role=alert` only for action-blocking failure. The quiz card's `.feedback`
is the normal page region. The reader's page-level `.status` is its normal
region. `#assist-status` is an asynchronous requested-result exception, not a
second steady-state region. Runnable blocks use `setStatus(block,text,isError)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make runnable-lesson announcements exact</name>
  <files>surfaces/lesson.py, tests/js/live_regions.test.mjs, tests/lesson_roundtrip.py</files>
  <behavior>
    - A lesson with four runnable blocks has exactly one steady-state polite region, the page-level `.status`; each `.run-status` is an `aria-live=off` readout when idle.
    - Runnable-code action-blocking errors set `role=alert` and `aria-live=assertive` before text; ordinary updates remove the alert role and restore the idle readout state.
  </behavior>
  <action>
Write failing assertions first. Use rendered-byte tests for the lesson count and
jsdom plus `MutationObserver` for lifecycle and source-order behavior. Load the
shipped `RUNNABLE_JS` from its Python module constant, not a copied snippet.

Implement the smallest production changes that satisfy the contract. De-live
runnable block readouts while idle and associate each with its run control using
`aria-describedby`. In `setStatus`, set or remove role and live attributes
before writing text. Do not move focus or change run semantics.
  </action>
  <verify>
    <automated>node --test tests/js/live_regions.test.mjs &amp;&amp; python tests/lesson_roundtrip.py &amp;&amp; python tests/lesson_code_roundtrip.py</automated>
  </verify>
  <done>The lesson count and runnable failure behavior pass, with mutation-order proof and no run-semantic change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound quiz and visual announcements</name>
  <files>surfaces/quiz_page.py, tests/js/live_regions.test.mjs, tests/model_ui_roundtrip.py, tests/visual_accessibility_roundtrip.py</files>
  <behavior>
    - An ordinary served quiz has exactly one steady-state element that announces politely; a visual item has exactly two, with `.visual-status` named as the 06.1 exception.
    - During an optional-guidance request, `#assist-status` becomes polite before waiting text is written; after success, unavailable, policy drop, cancellation, or request failure renders, the live attribute is removed.
    - Visual action-blocking errors set `role=alert` and `aria-live=assertive` before text; ordinary ActionStatus updates return to the persistent polite state required by 06.1.
  </behavior>
  <action>
Extend the live-region jsdom harness with the shipped `ASSIST_JS` and served
visual client source. Remove the permanent status role and polite attribute from
`#assist-status`; set polite before waiting copy and remove it after every
terminal result. Apply ordered alert behavior to visual ActionStatus blocking
failures while retaining its persistent polite normal updates. Do not delete
`.visual-status`, collapse it into card feedback, move focus, or change scoring
and response serialization.

Do not modify `surfaces/session.py`: `do_hint(stumped=...)` is not dead, because
the model route and tests still call `do_hint`, while authored teaching uses
`do_teach`. Removing it is outside RTS-10 and would conflate two APIs.
  </action>
  <verify>
    <automated>node --test tests/js/live_regions.test.mjs &amp;&amp; python tests/model_ui_roundtrip.py &amp;&amp; python tests/visual_accessibility_roundtrip.py</automated>
  </verify>
  <done>The ordinary and visual counts pass, assist status is transient, both visual states use the correct ordered live attributes, and 06.1 remains the named authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the additive floor and finish the evidence roll-up</name>
  <files>tests/lesson_roundtrip.py, fixtures/quiz_public_item_pre_13_5.json, .planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-GATES.md</files>
  <behavior>
    - A synthetic bank containing neither `## TERMS` nor `## LESSON` produces the embedded public-item rendering input byte-identically to an exact pre-Phase-13.5 golden.
    - The comparison excludes document chrome, CSS, support-region chrome, and form enhancement that Phase 13.5 intentionally changed, but includes the complete canonical public item projection consumed by both renderers.
    - Every RTS-01 through RTS-12 row names its delivering plan and executable evidence; browser-only geometry, screen-reader behavior, and no-network walkthroughs remain pending until a human records results.
  </behavior>
  <action>
Extend the existing compatibility section in `tests/lesson_roundtrip.py`, which
already contains byte-identity goldens. The historical source is commit
`8e397b66b8f23d6706ec2fce27e8d9e817903296`, the direct parent of Phase 13.5's
first production commit `e28c7eafafd8226784c42a326350b67901447189`.
At that source revision, render a temporary synthetic one-item MC bank whose
file contains a title and one question only, with no `## TERMS`, `## LESSON`, or
other preamble registry. Extract the exact canonical public-item JSON object
embedded for the active item, using UTF-8 and the renderer's stable compact JSON
separators, and check it in as the exact expected byte fixture
`fixtures/quiz_public_item_pre_13_5.json`.
This is the item-rendering input boundary shared by the server baseline and the
scripted renderer. It intentionally excludes document CSS, page chrome,
Phase 13.5's support region, and progressive form controls, all of which this
phase changes by contract. The current test must extract the same boundary from
the current served page and compare bytes directly. It must also assert the
golden has no key, rationale, correct answer, TERMS data, or LESSON data. Do not
compute expected and actual through the current renderer, and do not use a real
bank.

Complete `13.5-GATES.md` additively. Preserve Plan 07's architecture exception
and all existing human procedures. For machine gates, name the exact command and
test function. For responsive geometry, 200 percent zoom, real screen-reader
announcement behavior, header clearance, popover placement, and the local
no-network walkthrough, leave a result field and label it `PENDING HUMAN`; a
green jsdom or CSS inspection cannot close those claims. Add the RTS-01 through
RTS-12 roll-up, distinguishing complete machine evidence, pending human evidence,
and Plan 07's completed script-free presentation path. Do not mark the phase completely
verified while any human row is pending.
  </action>
  <verify>
    <automated>python tests/lesson_roundtrip.py &amp;&amp; python tests/gate_roundtrip.py &amp;&amp; python itembank.py lint fixtures/sample_bank.md &amp;&amp; python itembank.py guard .</automated>
    <human-check>Use the procedures already recorded in 13.5-GATES.md for responsive geometry, 200 percent zoom, screen-reader announcements, header clearance, popover placement, and the loopback-only no-network quiz flow; record observed results before phase verification is marked complete.</human-check>
  </verify>
  <done>The byte comparison uses an independent pre-feature golden; all twelve requirements have measurable evidence; every browser or assistive-technology claim remains visibly pending until performed by a human.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime/browser status to assistive technology | Announcement order and multiplicity affect what the learner perceives |
| bank markdown to item renderer | Optional grammar must not mutate the legacy item path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.5-08-01 | Repudiation | blocking failures | mitigate | MutationObserver tests prove live attributes precede failure text and reject alert plus live-off |
| T-13.5-08-02 | Denial of Service | competing announcements | mitigate | rendered-page count tests bound ordinary pages at one and visual pages at the named two-region exception |
| T-13.5-08-03 | Tampering | optional grammar compatibility | mitigate | current output is compared byte for byte with an independent pre-feature synthetic golden |
| T-13.5-08-04 | Spoofing | phase verification | mitigate | human-only claims stay pending and cannot be closed by jsdom or CSS inspection |
| T-13.5-08-SC | Tampering | package installs | accept | no package is added; the existing pinned jsdom dependency is restored from its lockfile |
</threat_model>

<verification>
Run both task command groups and then the complete `tests/*.py` suite. The plan
executor makes one atomic commit containing only this plan's production, test,
fixture, gate, and `13.5-08-SUMMARY.md` files listed in `files_modified` after
automated verification passes. Human gate results
may be appended later by the verifier and are not fabricated by execution.
</verification>

<success_criteria>
- RTS-10 has structural count tests and mutation-order proof for both blocking
  failure classes.
- RTS-12 compares the current item branch to an independent pre-feature golden.
- Every dependency and cross-plan reference uses the current `13.5-*` phase identifiers.
- The phase roll-up separates automated completion from pending human evidence.
</success_criteria>

<output>
Create `.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-08-SUMMARY.md`
and include it in this plan's single atomic commit.
</output>
