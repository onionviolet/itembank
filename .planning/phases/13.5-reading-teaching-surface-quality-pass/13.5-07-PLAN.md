---
phase: 13.5-reading-teaching-surface-quality-pass
plan: 07
type: execute
wave: 6
depends_on: ["13.5-03", "13.5-06"]
files_modified:
  - surfaces/quiz_page.py
  - surfaces/daemon.py
  - tests/js/hint_ladder.test.mjs
  - tests/model_ui_roundtrip.py
  - tests/serve_roundtrip.py
  - tests/daemon_roundtrip.py
  - .planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-GATES.md
  - .planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-07-SUMMARY.md
autonomous: true
requirements: [RTS-08, RTS-09]
gap_closure: true

must_haves:
  truths:
    - "A served practice item, its response form, held feedback, shown tiers, locked previews, and next legal hint action are present in HTML before script runs."
    - "Submitting an answer or opening a hint without script delegates to session.do_action or session.do_teach, then redirects to a fresh server render."
    - "The existing POST /quiz/<stem>/answer is the sole served-quiz mutation route for JSON and form clients; no parallel form endpoint or session adapter exists."
    - "The server renderer consumes only runtime.public_item, session_view, transition result, and teaching payload fields; it never loads a bank, scores, derives entitlement, or exposes keyed or unshown content."
    - "The scripted client enhances the same baseline card and produces the same actions and ladder states rather than maintaining a divergent quiz behavior."
    - "Each form transition is same-origin and single-use, so replay, reload, or double activation cannot advance two tiers or record two attempts."
    - "A one-use public flash receipt carries a sanitized transition or refusal projection across PRG and validates the expected post-transition cursor/status, without changing durable session schema or rebuilding feedback from evidence."
    - "Nothing in either presentation pre-announces a reveal or permits a caller to name a tier."
  artifacts:
    - path: "surfaces/quiz_page.py"
      provides: "Key-free server presentation adapter and in-place client enhancement"
    - path: "surfaces/daemon.py"
      provides: "PRG form adapter over the existing session runtime commands"
    - path: "tests/serve_roundtrip.py"
      provides: "Script-free answer, held-state, hint, replay, and no-leak round trip"
    - path: "tests/js/hint_ladder.test.mjs"
      provides: "Client/server presentation parity and reveal bright-line tests"
  key_links:
    - from: "surfaces/daemon.py"
      to: "surfaces/session.py"
      via: "the existing handle_quiz_answer dispatches JSON or form actions only to do_action and do_teach"
      pattern: "session\.do_(action|teach)"
    - from: "surfaces/daemon.py"
      to: "surfaces/quiz_page.py"
      via: "daemon passes only public session and teaching projections into the renderer"
      pattern: "public_item|session_view|teaching"
    - from: "surfaces/quiz_page.py"
      to: "/api/teach"
      via: "scripted enhancement requests only hint or stumped"
      pattern: "action:\{kind:button\.dataset\.teach\}"
---

<objective>
Complete the halted Plan 07 by adding the locked script-free ladder path as a
presentation adapter over the existing runtime authority.

Purpose: make progressive enhancement real without creating another parser,
scorer, evidence writer, entitlement rule, or session transition. The server
formats public runtime projections as semantic HTML and forms. The same
`session.do_action` and `session.do_teach` commands remain the only authorities.

Output: a server-rendered baseline quiz and ladder, protected POST/Redirect/GET
transitions, in-place scripted enhancement, parity and leakage fixtures, an
updated gate record, and a replacement summary that retains the original halt
as gap evidence.
</objective>

<execution_context>
@/Users/weiwei/Documents/Dev/itembank/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weiwei/Documents/Dev/itembank/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PLANNING-DIRECTIVES.md
@.planning/UI-SPEC.md
@.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-UI-SPEC.md
@.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-03-SUMMARY.md
@.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-06-SUMMARY.md
@.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-07-SUMMARY.md
@surfaces/session.py
@runtime.py
@surfaces/daemon.py
@surfaces/quiz_page.py

<interfaces>
`runtime.public_item(q)` is the pre-answer disclosure boundary.
`runtime.session_view(data, qs)` and `session.do_next(session_file)` expose the
current public item without a key. `session.do_action(session_file, action)` is
the only answer transition, scorer, session writer, and response evidence path.
`session.do_teach(session_file, kind=None)` is the authored-ladder read and
transition adapter; `kind` is absent, `hint`, or `stumped`, and no request shape
can name a tier. Its `teaching` payload alone supplies `shown`, `next_locked`,
`further_locked`, `entitled`, `exhausted`, and `unavailable_reason`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the public session and ladder before script</name>
  <files>surfaces/quiz_page.py, surfaces/daemon.py, tests/model_ui_roundtrip.py, tests/serve_roundtrip.py</files>
  <behavior>
    - GET `/quiz/<bank>` contains the current public item, semantic response controls, reserved feedback, and the teaching payload's ladder before any client script executes.
    - A held practice item renders all shown tiers monotonically, the next locked card, full further-locked previews, and only the runtime-reported next legal action.
    - Drill, diagnostic, and exam render the payload's unavailable reason and no ladder action.
    - HTML contains no key, rationale for an unshown tier, expected code output, accepted visual target, diagnostic or exam verdict, tier address, or hidden answer-bearing attribute.
  </behavior>
  <action>
Add a pure presentation adapter in `surfaces/quiz_page.py` that accepts a public
session view and a `do_teach(kind=None)` result. It may escape and format those
objects only. It must not import `model.load`, inspect a private question, call
`score_response`, count attempts, compute entitlement, synthesize unlock copy,
or select a tier. Render response controls from `public_item.response_schema`
and the public interaction contract. Use ordinary named inputs for mc, multi,
table, dnd, build, short, check, and the semantic-control form of visual items;
the adapter serializes user input but never interprets correctness.

Factor the daemon's existing start-on-demand behavior into one helper returning
the registered session path. On quiz GET, obtain the current view through
`session.do_next`, obtain teaching through `session.do_teach(kind=None)`, and
pass only those public results to `quiz_page.page_for`. Do not pass `qs`, a
private question, a key, or the bank path into the new baseline renderer.
Preserve the already-completed ladder markup, card padding, support-region order,
retirements, and reveal bright-line rules.
  </action>
  <verify>
    <automated>python tests/serve_roundtrip.py &amp;&amp; python tests/model_ui_roundtrip.py &amp;&amp; python tests/protocol_roundtrip.py</automated>
  </verify>
  <done>Every item family has a key-free semantic baseline, a held item has the complete runtime-projected ladder in served bytes, unavailable modes offer no transition, and leakage inspection passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Post forms through the one runtime transition with replay protection</name>
  <files>surfaces/quiz_page.py, surfaces/daemon.py, tests/serve_roundtrip.py, tests/daemon_roundtrip.py</files>
  <behavior>
    - A no-script answer POST to the existing `/quiz/<stem>/answer` serializes the public form fields, calls `session.do_action`, and redirects with 303 to the quiz GET; the resulting held page reflects the sanitized transition result and runtime state.
    - A no-script hint or stumped POST carries no tier, calls `session.do_teach`, and redirects with 303 to a page containing exactly one additional shown tier.
    - Reposting, reloading after redirect, double activation, simultaneous duplicate requests, an expired token, a token bound to another session/item/action, a cross-origin request, or an authority-shaped field cannot create a second transition.
    - Empty and duplicate answers retain runtime behavior and do not advance the ladder.
    - Wrong, duplicate, empty, advance, complete, refusal, and check-result feedback survive the redirect once; a later reload renders ordinary current session state without replaying stale feedback.
    - Media types are normalized before dispatch: JSON remains the existing branch, urlencoded form uses tokenized PRG, and every missing or unsupported type fails with 415 before parsing or mutation.
  </behavior>
  <action>
Extend the existing `POST /quiz/<stem>/answer` and `handle_quiz_answer`; do not
register another served-quiz mutation route. Parse `Content-Type` with the
standard media-type parser and compare the normalized lower-case base type,
ignoring valid parameters such as `charset=UTF-8`. `application/json` preserves
the current JSON behavior byte-for-byte. `application/x-www-form-urlencoded`
uses `read_form`, the public-field serializer, token validation, and PRG. A
missing content type, `multipart/form-data`, `text/plain`, any other media type,
or malformed media-type header returns 415 before a body parser, session helper,
token consumption, transition, or evidence write runs. A malformed JSON or
urlencoded body follows the existing bounded 400 path with no mutation. Add a
route-table test that exactly one served-quiz mutation route exists and that
both accepted branches reach the existing session adapter. Test JSON and form
types with charset parameters, every unsupported category above, malformed
headers, and malformed accepted bodies.
For the form branch, complete bounded decoding and structural validation before
the atomic token pop, so malformed form bytes return 400 without consuming a
valid token or touching session/evidence state.

Apply `_reject_cross_origin` and require a cryptographically random, single-use
form token minted by the daemon and stored in bounded, expiring in-memory server
state. Bind it to session id, current item id/cursor, and an allowlist of the
exact rendered action (`submit`, `hint`, or `stumped`). Protect the token store
with the daemon's dedicated lock and validate plus consume through one atomic
locked `pop`; never perform a check followed by a later deletion. Reject
missing, reused, expired, mismatched, or cross-session tokens with 403 and no
write. Cap and expire the store under the same lock. Add a barrier-controlled
two-request race fixture that submits the same token simultaneously and proves
one request is accepted, one is refused, and exactly one transition and one
evidence event exist. This token protects Origin-less form clients while
preserving the existing served quiz on loopback or an explicitly served LAN.

Reject by name `tier`, `tier_id`, `tier_index`, `requested_tier`, `key`,
`correct`, `score`, `entitled`, and any action outside the token's allowlist.
Serialize answers using only the current `public_item` response schema and
interaction contract. Then call exactly one of `session.do_action` or
`session.do_teach`; do not call runtime scoring or evidence functions directly.
Use POST/Redirect/GET after every accepted transition. Before invoking the
transition, capture the public source item id and pre-transition cursor/status.
After the transition, read the resulting public session view and mint a second
cryptographically random one-use **flash receipt**. Its bounded daemon-memory
record contains: session id; source item id; expected post-transition cursor;
expected post-transition status; optional target public item id; expiry; and
either the already-sanitized public result returned by `session.do_action` or
`session.do_teach`, or the existing sanitized daemon refusal projection.
`target_public_item_id` is the id from `result.next.item` when the result exposes
one, otherwise the id in the post-transition public view. It is null when status
is complete and no public item exists. The redirect carries only the opaque
receipt.

Quiz GET atomically consumes the receipt, resolves the registered session, and
validates session id plus **expected post-transition cursor and status**. It
does not require the current item to equal the source item. For an active held
transition, cursor is unchanged and the current public item must match the
source id. For an active advance, cursor has changed and the current public item
must match `target_public_item_id`. For completion, status must be complete,
cursor must equal the expected terminal cursor, and both target and current
public item are null. Any mismatch discards the receipt and renders ordinary
current state without stale flash copy. A valid receipt passes its public result
or refusal to the presentation adapter exactly once.

The receipt must never store a private question, key, raw evidence event, or
session file contents; never reconstruct feedback from evidence; and never
write the receipt or result into the runtime session schema. After consumption,
reload has no receipt and renders ordinary current public state. Expired, reused,
or foreign-session receipts are ignored with no mutation.

Refusal handling has two explicit pre-PRG cases. A pre-action check refusal is
generated by the existing shared `_check_refusal_body` helper. A mapped session
refusal, including a caught bounded `SystemExit` from `do_action` or `do_teach`,
uses the same existing daemon public-error/refusal mapper as the JSON branch.
Neither path invents refusal copy in the form adapter. Each may mint a receipt
containing the sanitized refusal projection, but neither changes session state
or appends evidence. Do not redirect raw exception text or store it in a
receipt.

Test held wrong, active advance, and complete/null-item receipts as separate
state assertions, including their cursor/status/target validation. Also test
duplicate answer, empty answer, and check-item interaction result. Assert each
appears after exactly one redirect GET, disappears on reload, and contains no
private or keyed fields. Test pre-action check refusal and mapped session
refusal separately: each returns its established public copy after PRG, leaves
cursor/session bytes unchanged, and writes zero response, hint, or other
evidence events.
  </action>
  <verify>
    <automated>python tests/serve_roundtrip.py &amp;&amp; python tests/daemon_roundtrip.py &amp;&amp; python tests/evidence_roundtrip.py &amp;&amp; python tests/hint_roundtrip.py</automated>
  </verify>
  <done>The existing answer route supports JSON and form clients; the no-script walkthrough reaches wrong, two tiers, reveal, and next item through PRG; every public result survives one redirect through a bounded receipt; replay, races, and forged fields write no extra response or hint event.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enhance in place, prove parity, and replace the halted record</name>
  <files>surfaces/quiz_page.py, tests/js/hint_ladder.test.mjs, tests/model_ui_roundtrip.py, .planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-GATES.md, .planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-07-SUMMARY.md</files>
  <behavior>
    - When script boots, it adopts the server-rendered active card and support region instead of clearing or replacing them.
    - Scripted and form paths send the same canonical submit, hint, and stumped intents and reach equivalent public item, feedback, and teaching states.
    - The ladder has no countdown, progressbar, reveal control, disabled reveal affordance, unshown body, tier-addressing field, first-person prose outside the inherited stumped label, or encouragement.
    - The phase record retains the original blocker and records the new adapter as its disposition, with browser-only verification still pending until performed.
  </behavior>
  <action>
Change `SERVED_JS` boot so it locates the existing baseline card by stable data
attributes, attaches handlers to its controls, and reconciles runtime payloads
into the same feedback and support nodes. It must not empty `#host`, construct a
second initial card, or keep a separate ladder state. Both form and fetch paths
render from the same Python markup helpers or an explicitly shared field-to-DOM
contract, and parity tests compare their normalized DOM and action intents for
held-first, tier-shown, duplicate, empty, revealed, and unavailable states.

Extend the real-source jsdom test to exercise adoption and all reveal
bright-line rules. Update `13.5-GATES.md` additively with machine commands and
retain a `PENDING HUMAN` row for disabling script in a real browser and opening
two tiers over loopback. Replace `13.5-07-SUMMARY.md`: preserve the halted
blocker verbatim under historical gap evidence, set status complete only after
all automated checks pass, list the new files and authority decision, and do not
claim the pending browser row passed.
  </action>
  <verify>
    <automated>node --test tests/js/hint_ladder.test.mjs &amp;&amp; python tests/model_ui_roundtrip.py &amp;&amp; python tests/serve_roundtrip.py &amp;&amp; python tests/gate_roundtrip.py &amp;&amp; python itembank.py guard .</automated>
    <human-check>Disable scripting in a real browser on the loopback daemon, answer a synthetic practice item incorrectly, open two tiers, reach reveal, and confirm focus order, visible copy, and lack of keyed leakage; record the result in 13.5-GATES.md.</human-check>
  </verify>
  <done>The same server card survives client boot, parity fixtures pass for every named state, the replacement summary preserves gap history, and the human browser claim remains pending until observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private bank/runtime to public HTML | Only public_item, public session state, and legally disclosed teaching fields may cross |
| browser form to local session adapter | Untrusted fields request one bounded transition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.5-07-01 | Information Disclosure | baseline renderer | mitigate | renderer accepts only public projections; served-byte tests reject keys, unshown bodies, expected outputs, and hidden targets |
| T-13.5-07-02 | Tampering | form transition | mitigate | atomic locked token pop binds session, cursor, item, and action; authority-shaped fields are rejected by name |
| T-13.5-07-03 | Repudiation | replay, race, or double activation | mitigate | simultaneous-request test proves atomic token consumption permits one transition and one evidence event |
| T-13.5-07-04 | Spoofing | cross-origin form | mitigate | same-origin check plus cryptographic token protects browser and Origin-less clients |
| T-13.5-07-05 | Denial of Service | token store | mitigate | bounded size and expiry with deterministic cleanup |
| T-13.5-07-06 | Information Disclosure | PRG flash receipt | mitigate | one-use receipt stores only sanitized public transition/refusal output plus source and expected post-state coordinates; GET validates held, advance, or terminal state before display |
| T-13.5-07-07 | Tampering | content-type confusion | mitigate | normalized allowlist accepts only JSON or urlencoded form; missing, malformed, multipart, plain-text, and other types return 415 before parsing or mutation |
| T-13.5-07-08 | Repudiation | refusal redirect | mitigate | both shared refusal paths mint public receipts with zero session or evidence writes, asserted separately |
| T-13.5-07-SC | Tampering | package installs | accept | no package is added; tests use the existing pinned jsdom lockfile |
</threat_model>

<verification>
Run all task commands and the complete `tests/*.py` suite. Inspect the diff to
confirm no parser, scorer, evidence writer, teaching policy, or keyed field was
added to either surface. Commit exactly the files in `files_modified`, including
the replacement summary, once after automated verification passes.
</verification>

<success_criteria>
- RTS-08 is delivered literally: the whole authored ladder is operable without
  script and without remote networking through the one runtime authority.
- RTS-09 is proven across both baseline and enhanced presentations.
- Replay and cross-origin attempts cannot create extra transitions.
- PRG preserves every bounded public feedback class once, then reload falls back to current public session state.
- Held, advance, and complete receipts validate their distinct expected cursor, status, and target-item shapes.
- Pre-action and mapped session refusals retain shared copy and produce zero evidence writes.
- Media-type dispatch fails closed with 415 before mutation for every unsupported or missing type.
- `/quiz/<stem>/answer` remains the sole served-quiz mutation route.
- The client enhances the server card in place and has no divergent policy.
- Manual browser results are never fabricated by automation.
</success_criteria>

<output>
Replace `.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-07-SUMMARY.md`
as part of the plan's one atomic commit.
</output>
