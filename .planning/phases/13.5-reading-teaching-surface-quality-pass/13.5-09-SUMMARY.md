---
phase: 13.5-reading-teaching-surface-quality-pass
plan: 09
subsystem: testing
status: complete-machine-evidence
tags: [responsive-type, aria-live, accessibility-regression]
requires:
  - phase: 13.5-08
    provides: bounded live-region lifecycle and visual ActionStatus exception
provides:
  - historical quiz coverage for the 32px desktop and 20px narrow stem scale
  - historical runnable coverage for static idle readouts and ordered blocking alerts
  - broad-suite evidence separating Phase 13.5 closure from environment failures
affects: [phase-13.5-verification, reading-teaching-surface]
tech-stack:
  added: []
  patterns:
    - idle result readouts are non-live until a blocking failure promotes them
    - responsive type assertions independently require desktop and narrow values
key-files:
  created: [.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-09-SUMMARY.md]
  modified: [tests/surface_roundtrip.py, tests/lesson_code_roundtrip.py, tests/subject_loop_roundtrip.py]
key-decisions:
  - "Historical suites supplement the focused Plan 08 lifecycle tests with negative regression guards."
  - "The visual ActionStatus remains the sole additional persistent polite region allowed by Phase 06.1."
requirements-completed: [RTS-03, RTS-10]
duration: 14min
completed: 2026-08-14
---

# Phase 13.5 Plan 09: Historical Surface Contract Alignment Summary

**Historical quiz and runnable suites now enforce the intentional responsive type scale and bounded live-region lifecycle without weakening accessibility regressions.**

## Accomplishments

- Updated the historical quiz hierarchy check to require a 32px desktop stem and independently retain the 20px narrow breakpoint.
- Replaced retired persistent polite runnable assertions with stable, button-associated `aria-live="off"` idle readouts and negative guards against `role="status"` or `aria-live="polite"`.
- Required shipped runnable JavaScript to promote blocking errors to assertive alerts and restore ordinary results before writing status text.
- Preserved `.visual-status` as the sole additional persistent polite ActionStatus exception, while proving the idle requested-result readout is not another live region.

## Verification

Targeted command passed:

`python3 tests/surface_roundtrip.py && python3 tests/lesson_code_roundtrip.py && python3 tests/subject_loop_roundtrip.py && PYTHON=python3 node --test tests/js/live_regions.test.mjs && python3 tests/visual_accessibility_roundtrip.py`

The three plan-owned Python suites were rerun after the complete inventory and passed. `git diff --check` also passed.

Complete Python inventory command:

`for test_file in tests/*.py; do python3 "$test_file"; done`

Result: 61 scripts passed and 3 scripts failed. No Phase 13.5-owned suite failed.

### Remaining non-phase failures

| Script | Terminal reason | Classification |
|---|---|---|
| `tests/audit_writer_roundtrip.py` | `FAIL: writer: tracked-clean target must use the Git backend with a commit hash` | Unrelated current-worktree or isolated Git cleanliness/backend-selection failure. No changed assertion is reached. |
| `tests/lti_roundtrip.py` | `ModuleNotFoundError: No module named 'cryptography'` from `make_rsa_keypair()` | Missing optional dependency. No package was installed and no product assertion ran. |
| `tests/phase_062_audit.py` | Reports the same `audit_writer_roundtrip.py` and `lti_roundtrip.py` failures in its full-suite audit | Downstream audit aggregation of the two conditions above, not a distinct Phase 13.5 regression. |

The complete suite is not claimed green. The Phase 13.5-owned broad-suite regressions identified by `13.5-VERIFICATION.md` are closed.

## Human Verification

PENDING HUMAN: every browser, responsive geometry, 200 percent zoom, no-network, script-free, and assistive-technology row in `13.5-GATES.md`. This automated plan did not perform or change any human gate.

## Deviations from Plan

None. The repository's one-commit-per-plan rule governs this execution, so both plan tasks and metadata are included in one atomic commit.

## Known Stubs

None.

## Threat Flags

None. This plan changes regression assertions and evidence only. It adds no endpoint, authority path, persistence boundary, file-access path, or schema.

## Self-Check: PASSED

All three modified test files and this summary exist. The targeted command, final owned-suite rerun, and named-file diff checks passed. Human gates remain pending and untouched.
