---
phase: 13.5-reading-teaching-surface-quality-pass
plan: 09
type: execute
wave: 8
depends_on: ["13.5-08"]
files_modified:
  - tests/surface_roundtrip.py
  - tests/lesson_code_roundtrip.py
  - tests/subject_loop_roundtrip.py
  - .planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-09-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [RTS-03, RTS-10]

must_haves:
  truths:
    - "The historical surface suite accepts the intentional 32px desktop quiz stem and still proves the stem scales to 20px at the narrow breakpoint."
    - "Historical runnable-code suites accept static idle result readouts, while rejecting a return to persistent polite runnable regions or a failure path that cannot announce."
    - "The visual-item ActionStatus remains the sole additional persistent polite region allowed by the Phase 06.1 exception."
    - "Every Phase 13.5-owned Python-suite failure is gone, while optional dependency and unrelated environment failures are reported as such rather than hidden or claimed fixed."
  artifacts:
    - path: "tests/surface_roundtrip.py"
      provides: "Historical quiz hierarchy coverage aligned with the five-size Phase 13.5 scale"
    - path: "tests/lesson_code_roundtrip.py"
      provides: "Historical runnable lesson coverage aligned with the bounded live-region lifecycle"
    - path: "tests/subject_loop_roundtrip.py"
      provides: "Subject-invariant runnable integration coverage aligned with the same lifecycle"
    - path: ".planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-09-SUMMARY.md"
      provides: "Targeted and broad-suite evidence with honest environment classification"
  key_links:
    - from: "tests/surface_roundtrip.py"
      to: "surfaces/quiz_page.py"
      via: "desktop and narrow h1.stem CSS assertions"
      pattern: "h1\\.stem.*font-size:(32|20)px"
    - from: "tests/lesson_code_roundtrip.py"
      to: "surfaces/lesson.py"
      via: "rendered idle readout plus blocking-error lifecycle assertions"
      pattern: "run-status.*aria-live=\\\"off\\\""
    - from: "tests/subject_loop_roundtrip.py"
      to: "surfaces/lesson.py"
      via: "CS profile uses the shared runnable contract without a subject-specific exception"
      pattern: "run-status.*aria-live=\\\"off\\\""
---

<objective>
Close the sole automated Phase 13.5 verification gap by migrating three stale
historical assertions to the intentional type-scale and live-region contracts.

Purpose: restore consistency across the complete runnable regression suite
without reverting the accessibility improvements delivered by Plans 04 and 08
or overstating browser and assistive-technology evidence.

Output: three corrected regression suites, negative accessibility guards, and
a summary recording targeted and broad-suite results.
</objective>

<execution_context>
@/Users/weiwei/Documents/Dev/itembank/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weiwei/Documents/Dev/itembank/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-VERIFICATION.md
@.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-GATES.md
@.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-08-SUMMARY.md
@.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-UI-SPEC.md
@.planning/phases/06.1-interactive-visual-assessment-protocol/06.1-UI-SPEC.md
@surfaces/presentation.py
@surfaces/quiz_page.py
@surfaces/lesson.py
@tests/surface_roundtrip.py
@tests/lesson_code_roundtrip.py
@tests/subject_loop_roundtrip.py
@tests/js/live_regions.test.mjs
@tests/visual_accessibility_roundtrip.py

<interfaces>
`surfaces/quiz_page.py` declares `h1.stem` at 32px by default and 20px in the
narrow media query. `surfaces/lesson.py` renders each runnable block with a
button-associated `.run-status` readout whose idle state is `aria-live="off"`
and no status role. `RUNNABLE_JS.setStatus(block, text, isError)` promotes only
blocking failures to `role="alert"` plus `aria-live="assertive"`, writing those
attributes before `textContent`; ordinary results remove the role and restore
`aria-live="off"`. Phase 06.1 owns `.visual-status` as the one additional
persistent polite region on visual items. Plan 08's focused tests are the
primary lifecycle proof; these historical suites must supplement, not copy or
weaken, that proof.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the three historical contract assertions</name>
  <files>tests/surface_roundtrip.py, tests/lesson_code_roundtrip.py, tests/subject_loop_roundtrip.py</files>
  <behavior>
    - The quiz hierarchy check requires a 32px desktop stem and retains the 20px narrow rule, failing if either side of the responsive contract disappears.
    - A runnable block renders an idle `aria-live="off"` readout associated with its Run button and does not render that idle readout as `role="status"` or `aria-live="polite"`.
    - The shipped runnable adapter still promotes blocking errors to an assertive alert before setting failure text, and ordinary results restore the static readout.
    - The CS subject integration asserts the same shared runnable lifecycle, with no subject-specific status exception.
    - The quiz contract still retains `.visual-status` as a persistent polite ActionStatus exception and does not turn the idle requested-result region into another steady-state live region.
  </behavior>
  <action>
Update only the stale assertions identified in `13.5-VERIFICATION.md`.

In `tests/surface_roundtrip.py`, replace the obsolete 28px desktop expectation
and failure copy with the 32px token delivered by Plan 04. Keep the existing
20px narrow assertion as a separate required condition. Add focused negative
guards using the served quiz source: the idle requested-result
`#assist-status` must have no persistent status role or polite live attribute,
while `.visual-status` must remain the named persistent polite exception for a
visual item. Reuse the existing page and source extraction helpers. Do not
duplicate a renderer, evaluate correctness, or loosen the feedback-region and
responsive checks already in this test.

In `tests/lesson_code_roundtrip.py`, update the module contract prose and the
runnable-page assertion from the retired persistent polite markup to the exact
idle `class="run-status"`, stable id, and `aria-live="off"` markup emitted by
`render_code_block`; retain the button's `aria-describedby` association. Add
negative assertions that idle markup has neither `role="status"` nor
`aria-live="polite"`. Inspect the shipped `lesson.RUNNABLE_JS`, not a copied
snippet, to require both failure promotion (`role="alert"` and
`aria-live="assertive"`) and ordinary restoration (role removal and
`aria-live="off"`), with the live-attribute mutation occurring before the
status text write. Keep stdout and stderr non-live and keep all runner,
authority, timeout, output-cap, and evidence-isolation coverage unchanged.

In `tests/subject_loop_roundtrip.py`, migrate `_medium_cs` to the same exact
idle readout and Run-button association. Add the same negative rejection of
the retired persistent polite markup and a concise assertion that the shared
adapter retains blocking-error announcement behavior. Do not create a CS-only
implementation or alter any non-CS profile coverage.

These changes implement RTS-03 and RTS-10 as already locked by the Phase 13.5
contracts. They update tests only; do not modify production source, fixtures,
GATES.md, verification findings, or human-gate status.
  </action>
  <verify>
    <automated>python3 tests/surface_roundtrip.py &amp;&amp; python3 tests/lesson_code_roundtrip.py &amp;&amp; python3 tests/subject_loop_roundtrip.py &amp;&amp; PYTHON=python3 node --test tests/js/live_regions.test.mjs &amp;&amp; python3 tests/visual_accessibility_roundtrip.py</automated>
  </verify>
  <done>All three historical suites pass the intentional Phase 13.5 contracts, and their negative assertions prevent static readouts, blocking alerts, responsive type, or the visual exception from being silently regressed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the broad suite and record the closure evidence</name>
  <files>.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-09-SUMMARY.md</files>
  <action>
After Task 1's focused command passes, run every `tests/*.py` script, continuing
past failures so the summary contains the full inventory rather than only the
first failure. Record the exact command, passing count, failing count, and each
failing script's terminal reason in `13.5-09-SUMMARY.md`.

Classify results from evidence, not convenience. A missing optional
`cryptography` import in `tests/lti_roundtrip.py` is an environment/dependency
issue and is not a Phase 13.5 product regression. An isolated Git cleanliness
or shadow-backend selection failure in `tests/audit_writer_roundtrip.py` is an
unrelated current-worktree/environment issue unless the actual failure output
shows otherwise. Do not install optional packages, change those tests, suppress
their output, label them passing, or claim the complete suite is green when it
is not. Any failure in the three plan-owned suites, or any other failure whose
trace reaches the changed assertions, blocks completion and must be fixed
within the three owned test files before rerunning.

The summary must state that the Phase 13.5-owned broad-suite regressions are
closed, list any remaining non-phase failures separately, and preserve every
`PENDING HUMAN` browser, geometry, no-network, and assistive-technology gate.
Do not attempt or record human browser or screen-reader results in this
automated plan.

Inspect `git diff --check` and the named-file diff. Then create exactly one
atomic commit for the plan after verification passes, staging only the three
test files and `13.5-09-SUMMARY.md`. Use the repository message shape
`test(13.5-09): align historical surface contracts`. Do not stage unrelated
worktree changes or this PLAN.md if it is already part of the planning handoff.
  </action>
  <verify>
    <automated>python3 tests/surface_roundtrip.py &amp;&amp; python3 tests/lesson_code_roundtrip.py &amp;&amp; python3 tests/subject_loop_roundtrip.py &amp;&amp; git diff --check</automated>
  </verify>
  <done>The summary contains reproducible targeted and complete-suite evidence, no Phase 13.5-owned failure remains, optional or environment failures are truthfully separated, pending human gates remain pending, and the four execution artifacts are in one atomic commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered HTML and JavaScript to assistive technology | Test migration must not make silenced blocking failures or competing steady-state announcements appear acceptable |
| CSS contract to responsive learner surface | Updating the desktop token must not discard the narrow scaling requirement |
| broad-suite output to phase status | Environment failures must not be hidden, and pending human evidence must not be converted into automated proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.5-09-01 | Tampering | historical live-region assertions | mitigate | Require static idle readouts, ordered assertive blocking alerts, and the unchanged visual ActionStatus exception in the migrated tests |
| T-13.5-09-02 | Denial of Service | competing announcements | mitigate | Reject persistent polite attributes on runnable and requested-result idle readouts |
| T-13.5-09-03 | Repudiation | broad-suite result | mitigate | Record exact commands and every residual failure with evidence-based classification in the summary |
| T-13.5-09-04 | Spoofing | human accessibility gates | mitigate | Leave browser, geometry, no-network, and assistive-technology rows pending; this plan performs no human gate |
| T-13.5-09-SC | Tampering | package installs | accept | No package install occurs; a missing optional dependency is reported as environment state rather than repaired in this gap plan |
</threat_model>

<verification>
Run Task 1's targeted command first. Then run the complete stdlib Python suite
with a continuing harness equivalent to `for test in tests/*.py; do python3
"$test"; done`, capture every result, and rerun the three plan-owned suites at
the end. The complete run may remain non-green only for failures demonstrated
to be optional dependency or unrelated environment conditions; the summary
must say so explicitly. Run no human browser or assistive-technology gate.
</verification>

<success_criteria>
- `tests/surface_roundtrip.py`, `tests/lesson_code_roundtrip.py`, and
  `tests/subject_loop_roundtrip.py` pass against the delivered production code.
- The migrated assertions guard both positive behavior and accessibility
  regressions: 32px plus responsive scaling, static idle readouts, announced
  blocking failures, and the unchanged visual exception.
- The broad-suite record distinguishes Phase 13.5 regressions from optional
  dependency and unrelated environment conditions without suppressing either.
- All human rows in `13.5-GATES.md` remain pending and untouched.
- Execution creates one atomic commit containing only the three tests and
  `13.5-09-SUMMARY.md`.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Reader and quiz quality pass leaves its runnable regression suite consistent | 13.5-09 | COVERED | Historical assertions follow the intentional delivered contracts
REQ | RTS-03 | Shared five-size reader and quiz type scale | 13.5-09 | COVERED | Desktop 32px and narrow 20px remain independently asserted
REQ | RTS-10 | Bounded steady-state and transient live-region lifecycle | 13.5-09 | COVERED | Static idle readouts, blocking alerts, and visual exception are all guarded
RESEARCH | - | No new implementation research is required for stale test assertions | 13.5-09 | COVERED | Existing owning source and Plan 08 evidence define the contracts
CONTEXT | - | No Phase 13.5 CONTEXT.md exists; UI-SPEC and delivered summaries are binding | 13.5-09 | COVERED | No decision is invented or reduced
</source_audit>

<output>
Create `.planning/phases/13.5-reading-teaching-surface-quality-pass/13.5-09-SUMMARY.md`
and include it with the three modified tests in this plan's one atomic commit.
</output>
