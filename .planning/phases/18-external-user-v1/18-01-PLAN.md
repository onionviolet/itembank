---
phase: 18-external-user-v1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/phases/18-external-user-v1/18-DECISIONS.md
autonomous: false
requirements: [A10-C1]
must_haves:
  truths:
    - "The Phase 13 shell rebuilds from current main and the rebuilt artifact passes every shipped packaging fixture."
    - "IL-20260815-11 is recorded resolved in 18-DECISIONS.md with the D-01 wording, not re-decided."
    - "The signing question carries a dated cost decision in 18-DECISIONS.md, whatever the answer is."
    - "The SmartScreen workaround copy sits in the README install path, the exact place an unsigned download surfaces the warning."
  artifacts:
    - ".planning/phases/18-external-user-v1/18-DECISIONS.md with the D-01 resolution record and the dated D-02 signing decision."
    - "Rebuilt onedir sidecar and shell evidence (size, SHA-256, fixture results) recorded for 18-03's gates to cite."
  key_links:
    - "18-03's 18-GATES.md criterion 1 row points at this plan's recorded artifact hashes; a rebuild without recorded hashes leaves that row unfillable."
---
<objective>Rebuild the shipped Phase 13 packaged artifact from current main, record the IL-20260815-11 packaging resolution, and settle the V2-DEL-01 signing question as a dated cost decision. Implements 18-CONTEXT D-01 and D-02; the executor transcribes those decisions and never re-litigates them.</objective>
<context>
@.planning/phases/18-external-user-v1/18-CONTEXT.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-03-SUMMARY.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-05-SUMMARY.md
@.planning/READINESS-AUDIT-14A.md (section A10)
@.planning/SUPPLY-CHAIN-POLICY.md (sections 2 and 3)

Convention, stated once for this plan: where a step depends on 17B outcomes that have not shipped at execution time, pin only what is verifiable today and cite ROADMAP.md Phase 17B as the owning contract instead of asserting 17B behavior.
</context>
<tasks>
<task type="auto">
  <name>Task 1: Rebuild the packaged artifact from current main</name>
  <files>(build outputs only; no tracked source edits)</files>
  <read_first>scripts/build_shell.ps1 (whole file), requirements-build.txt, tests/packaging_roundtrip.py (test names in the 13-03 and 13-05 summaries)</read_first>
  <action>
  1. No new packaging tool is adopted. The toolchain is the shipped, pinned one (PyInstaller 6.22.0 in requirements-build.txt, Tauri shell in src-tauri/). If any step would require a new third-party artifact, stop: it must first clear SUPPLY-CHAIN-POLICY.md section 3, and none is expected.
  2. Run the shipped one-command build:
  ```powershell
  powershell -ExecutionPolicy Bypass -File scripts/build_shell.ps1
  ```
  Expected: exit 0; the onedir sidecar tree is reported near 28.8 MiB, inside the 25 to 45 MiB target; the NSIS bundle step either compiles the installer or names the missing makensis toolchain honestly (13-03 recorded makensis absent; the honest skip is a pass, a faked installer is a fail).
  3. Run the shipped fixtures against the rebuilt tree:
  ```powershell
  python tests/packaging_roundtrip.py
  python tests/packaging_shell_roundtrip.py
  cargo test --manifest-path src-tauri/Cargo.toml
  ```
  Expected: each exits 0; packaging_shell reports its check count (10 at Phase 13 seal); cargo reports all tests passed (17 at seal; a grown count is fine, a failure is not).
  4. Record for the summary and for 18-03: the built artifact paths (the onedir tree containing itembank-sidecar-x86_64-pc-windows-msvc.exe, and the installer if makensis exists), the measured size, and Get-FileHash SHA-256 for each shipped file.
  </action>
  <verify>All three commands above exit 0, and the summary carries the size and SHA-256 values. Degraded state also proven: if makensis is absent, the build output names the gap and the plan still passes on the onedir evidence alone.</verify>
</task>
<task type="auto">
  <name>Task 2: Record the IL-20260815-11 resolution</name>
  <files>.planning/phases/18-external-user-v1/18-DECISIONS.md</files>
  <read_first>18-CONTEXT.md D-01 (transcribe, do not paraphrase into new meaning)</read_first>
  <action>Create 18-DECISIONS.md with a dated entry "IL-20260815-11 resolved (18-CONTEXT D-01), 2026-08-17" recording: the delivery vehicle is the Phase 13 desktop shell (the shipped Tauri-sidecar packaging in phases/13-desktop-packaging-tauri-sidecar/) wrapping the browser-served UI that 17A-CONTEXT D-08 fixed as the single canonical shell; nothing chooses between shells, the packaged app and the browser reach the same served pages from the one runtime (IL-20260816-02); the historical "no build step" reading and the miscited DEL-02 ground against PyInstaller (constraint audit F9, F15) are both dead; no new PyInstaller-class freezer is adopted because the Phase 13 shell already shipped. Cite (18-CONTEXT D-01). No em dash characters anywhere in this file.</action>
  <verify>18-DECISIONS.md exists, cites 18-CONTEXT D-01 and IL-20260816-02, and `python itembank.py guard .` still exits 0.</verify>
</task>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Signing cost decision (V2-DEL-01, 18-CONTEXT D-02)</name>
  <files>.planning/phases/18-external-user-v1/18-DECISIONS.md</files>
  <read_first>18-CONTEXT.md D-02; READINESS-AUDIT-14A.md A10 criterion 1</read_first>
  <action>Before asking, gather a current Windows Authenticode OV certificate quote from at least one real vendor (dated, vendor named). Then present Weibao these options:
  1. Buy a Windows Authenticode OV certificate, roughly 100 to 400 USD per year; state the gathered quote as the real figure.
  2. Apple Developer Program at 99 USD per year, only if a macOS artifact ships; no macOS machine is available today, so per 18-CONTEXT this is recorded as a deferral with reason, not an untested claim.
  3. Ship unsigned for v1 with the SmartScreen and Gatekeeper workarounds documented exactly where the user hits them. Recommended default; revisit on wider distribution.
  Whatever the answer, append the dated cost decision to 18-DECISIONS.md, citing (18-CONTEXT D-02). The A10 criterion is satisfied by the record, not by signing; criterion 1 reads (quoted with inline formatting normalized): "Install without folklore. One downloaded artifact plus Python 3.11+, or the Phase 13 shell. The macOS Gatekeeper workaround is documented where the user hits it. V2-DEL-01's recorded trigger ('when a second person runs the tool') has now fired: signing gets a dated cost decision, not silence." An unanswered checkpoint stops the wave; do not default silently. If the answer is option 1 or 2, the purchase itself is Weibao's action, not the executor's; the executor records the decision and the purchase lands in a later session.</action>
  <verify>18-DECISIONS.md contains a dated signing entry naming the chosen option, the gathered quote, and the vendor. If unsigned was chosen, the entry names the workaround documentation (Task 4) as the compensating control.</verify>
</task>
<task type="auto">
  <name>Task 4: SmartScreen workaround where the user hits it</name>
  <files>README.md</files>
  <read_first>README.md "Quick start for someone brand new" and the install/download steps around line 94; installers/nsis/itembank.nsi unsigned-branch notice (verify only, do not edit)</read_first>
  <action>Add to the README install path, immediately beside the release-download instruction (the step that tells the user to fetch the artifact from GitHub Releases), this exact copy:

  "Windows SmartScreen note: this installer is not code signed, so the first launch shows 'Windows protected your PC'. Click 'More info', confirm the file name is the one you downloaded, then click 'Run anyway'. Before that, verify the download: run Get-FileHash on the file in PowerShell and compare the SHA-256 against SHA256SUMS.txt on the same release page. If the hashes differ, delete the file and download it again."

  The macOS Gatekeeper workaround already documented in the Install section stays as is. Do not edit installers/nsis/itembank.nsi; the unsigned install-notice copy there shipped in 13-03 and is fixture-pinned. No em dash characters.</action>
  <verify>`python tests/packaging_roundtrip.py` still exits 0 (the install-notice fixture test_install_notice_cannot_ship_placeholders stays green), and the README copy above appears verbatim in the release-download step.</verify>
</task>
</tasks>
<out_of_scope>MCP implementation; accounts or auth; telemetry or any hosted service; executing a signing purchase without the Task 3 checkpoint answer; adopting any new packaging tool; touching scripts/preflight.py, tests/preflight_roundtrip.py, AGENTS.md, .gitattributes, or fixtures/ (another agent's in-flight files); editing installers/nsis/itembank.nsi.</out_of_scope>
<summary_obligations>Record in 18-01-SUMMARY.md: the rebuilt artifact paths, sizes, and SHA-256 values; every fixture command and its exit state; the makensis present-or-absent state; the checkpoint question, the gathered quote, and Weibao's answer; and any deviation from the shipped build script behavior.</summary_obligations>
