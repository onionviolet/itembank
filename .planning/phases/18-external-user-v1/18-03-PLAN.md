---
phase: 18-external-user-v1
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - README.md
  - .planning/phases/18-external-user-v1/18-GATES.md
  - .planning/phases/18-external-user-v1/18-COLD-AGENT-TRANSCRIPT.md
autonomous: false
requirements: [A10-C1, A10-C2, A10-C3, A10-C4, A10-C5, A10-C6]
must_haves:
  truths:
    - "Every one of the six A10 criteria has a gates row with a concrete check and an evidence pointer; no row is a claim without evidence."
    - "A fresh install with no repo itembank.json resolves update_policy to opt_in and makes no network request without disclosure."
    - "The cold-agent transcript exists, redacted, and a failed walkthrough became fixed defects and a rerun, never a shrugged-off transcript."
    - "The phase closes only on a real second-person cold install, a human checkpoint."
  artifacts:
    - "18-GATES.md with six criterion rows plus the closing second-person row."
    - "18-COLD-AGENT-TRANSCRIPT.md, the A10 check 2 fixture."
  key_links:
    - "Criterion 1 and 6 evidence points at 18-01's rebuilt artifact and its recorded hashes; criterion 2 points at the transcript; criterion 5 points at the fresh-install check, not at the repo's own itembank.json."
---
<objective>Verify external-user v1 against the six-criterion A10 bar: write the gates record, run the fresh-install privacy check, produce the cold-agent onboarding transcript, land the scope-honesty copy, and close on a real second-person cold install. Implements 18-CONTEXT D-04, D-05, and D-06; the executor transcribes those decisions.</objective>
<context>
@.planning/phases/18-external-user-v1/18-CONTEXT.md
@.planning/READINESS-AUDIT-14A.md (section A10, quoted below where a row verifies a criterion)
@.planning/phases/18-external-user-v1/18-DECISIONS.md (from 18-01)
@README.md ("Quick start for someone brand new" and the AI-assistant section)
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-GATES.md (the house gates style: every row carries evidence or an explicit pending)

Convention, stated once for this plan: where a step depends on 17B outcomes that have not shipped at execution time, pin only what is verifiable today and cite ROADMAP.md Phase 17B as the owning contract; the loop the docs sell and the transcript exercises is whatever 14A through 17B actually shipped by execution time, per 18-CONTEXT D-06.
</context>
<tasks>
<task type="auto">
  <name>Task 1: Scope-honesty copy (18-CONTEXT D-06)</name>
  <files>README.md</files>
  <read_first>README.md lines 1 to 45 (the course-workspace framing) and the quick-start section</read_first>
  <action>Place this exact copy in the README, directly under the "Quick start for someone brand new" heading, before the two paths:

  "What you get today is the bank, lesson, and session loop: author a markdown question bank, lint it, sit it graded in your browser or through an AI tutor session, study it as flashcards, and keep every attempt on your own disk. The course workspace described elsewhere in this README (sources bound to cited objectives, per-objective treatments, evidence-driven next actions) is being built and is not something you can run yet. If a sentence describes the course workspace, read it as direction, not as a shipped feature."

  Then sweep the user-facing sections (quick start, AI-assistant steps, install path) and fix any sentence that implies the course workspace is present; the framing sections that already say "next milestone" or "being built" stay. Update the shipped-loop list to match what actually shipped by execution time (the 17B convention above). No em dash characters.</action>
  <verify>The copy above appears verbatim under the quick-start heading; a read of the quick-start and AI-assistant sections finds no sentence selling an unshipped course capability; `python itembank.py guard .` exits 0.</verify>
</task>
<task type="auto">
  <name>Task 2: Fresh-install privacy check (A10 criterion 5, 18-CONTEXT D-05)</name>
  <files>.planning/phases/18-external-user-v1/18-GATES.md</files>
  <read_first>surfaces/update.py disclosure_state; the settings-resolution code path the config command prints; the D-13 note in .claude/CLAUDE.md Constraints</read_first>
  <action>
  1. Stage a fresh install: copy the 18-01 built artifact (or the repo tree minus itembank.json) into a temp directory outside the repository, containing no itembank.json.
  2. From that directory, with the network unplugged or monitored, run:
  ```powershell
  python itembank.py config
  python itembank.py disclosure
  ```
  Expected: exit 0 for both; the resolved update_policy value prints as opt_in (the schema default); the disclosure state shows the notice has not been rendered and no update request has been made. Record the exact printed lines as evidence.
  3. Confirm no network request occurs during a normal command run (lint plus serve startup and shutdown) by monitoring; record the method used and the result.
  4. Record in the draft gates row: the D-13 divergence (the repository's own checked-in itembank.json sets check_on_launch for dogfooding) stays repo-only; a fresh install has no such file, so opt_in holds for a stranger.
  </action>
  <verify>The evidence lines show update_policy resolving to opt_in with no itembank.json present, and the no-request observation is recorded with its method. Degraded state proven by the same run: the loop worked with the network unplugged.</verify>
</task>
<task type="auto">
  <name>Task 3: Cold-agent onboarding transcript (18-CONTEXT D-04)</name>
  <files>.planning/phases/18-external-user-v1/18-COLD-AGENT-TRANSCRIPT.md, README.md</files>
  <read_first>README.md "If you are an AI assistant onboarding a new user"; 18-CONTEXT D-04</read_first>
  <action>
  1. Run one fresh agent session (any agent, one run) given only the public repo URL and the README onboarding section, walking a simulated new user through: verify Python, fetch, launch, author a first bank from the user's own material (synthetic material only), and a first graded sitting.
  2. If the walkthrough fails at any step, that is a defect list against README or error copy: fix the copy, rerun from the top, and keep the defect list in the transcript file's header. A failed run is never the recorded fixture.
  3. Land the passing transcript at 18-COLD-AGENT-TRANSCRIPT.md with these redaction rules, stated in the file header: real absolute filesystem paths replaced with placeholders such as USER-DIR; no real personal names, subjects, emails, or machine names; the simulated user's study material is synthetic and stays synthetic; agent and model identifiers may remain. No real learner content enters this repository; `itembank guard` is the backstop.
  </action>
  <verify>18-COLD-AGENT-TRANSCRIPT.md exists, its header states the redaction rules and lists any defects fixed en route, the transcript reaches a completed graded sitting, and `python itembank.py guard .` exits 0.</verify>
</task>
<task type="auto">
  <name>Task 4: 18-GATES.md, the six-criterion record (18-CONTEXT D-05)</name>
  <files>.planning/phases/18-external-user-v1/18-GATES.md</files>
  <read_first>READINESS-AUDIT-14A.md section A10; 13-GATES.md for the row style</read_first>
  <action>Write 18-GATES.md with one row per criterion. Each row quotes its criterion from A10 with inline formatting normalized (backticks and quote marks may differ from the audit file), then states the concrete check and the evidence pointer. Criteria 1 and 6 are exercised on the packaged artifact from 18-01, per D-05:
  1. "Install without folklore. One downloaded artifact plus Python 3.11+, or the Phase 13 shell. The macOS Gatekeeper workaround is documented where the user hits it. V2-DEL-01's recorded trigger ('when a second person runs the tool') has now fired: signing gets a dated cost decision, not silence." Check: 18-01's rebuilt artifact hashes and fixture results; the README SmartScreen and Gatekeeper copy; the dated signing entry. Evidence: 18-01-SUMMARY.md, 18-DECISIONS.md, README install section.
  2. "Agent-guided onboarding. The README carries a section a coding agent can follow cold: verify Python, fetch, launch, author a first bank from the user's own material, run a first graded sitting. The user's only skill is pasting the repo URL into Claude Code or a peer." Check: the transcript run completed. Evidence: 18-COLD-AGENT-TRANSCRIPT.md.
  3. "First-run self-explanation. A fresh launch shows something that explains itself (sample bank or walkthrough), not an empty directory." Check: launch the packaged artifact in an empty directory and record what the first screen shows; if the shipped first run does not yet self-explain, the row is an honest fail with the gap named, never a soft pass. Evidence: the recorded first-run observation.
  4. "Scope honesty. User-facing docs sell only the shipped bank/lesson/session loop. The course workspace is described as being built, never implied present." Check: Task 1's copy landed and the sweep found no violation. Evidence: README diff.
  5. "Privacy defaults hold for a stranger. Fresh install never phones home without disclosure (the D-13 divergence stays repo-only), evidence stays on disk, and the update check discloses before its first request." Check: Task 2's fresh-install run. Evidence: the recorded config and disclosure output and the no-request observation.
  6. "Recovery. Common failure states (wrong Python, port taken, quarantine flag, offline) name the next safe action in the error itself." Check: provoke each of the four states against the packaged artifact and record the exact error copy; a message that does not name the next safe action is an honest fail with the copy quoted. Evidence: the four recorded error transcripts.
  Every row carries evidence or an explicit pending; a claim without evidence is a defect in this file. Close with a seventh row, "Second-person cold install", marked pending until Task 5. No em dash characters.</action>
  <verify>18-GATES.md contains the six criterion quotes, each with a check and an evidence pointer that exists on disk, plus the pending closing row.</verify>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: The real second-person cold install</name>
  <files>.planning/phases/18-external-user-v1/18-GATES.md</files>
  <read_first>18-GATES.md rows 1 to 6 (all must be filled or honestly failed before asking a human to install)</read_first>
  <action>Ask Weibao to have a second person (his friend is fine, per 18-CONTEXT D-05) perform a cold install on a machine this project has never touched: download, install past SmartScreen using only the README copy, and reach a first graded sitting with or without an AI agent's help. What is being asked: did they get there, where did they stall, and what words were on screen when they stalled. Record the outcome, the installer hash they verified, the date, and the person's role (not their name) in the closing 18-GATES.md row. If they stalled, the stalls are defects; fix and rerun before marking the row passed. An unanswered checkpoint stops the phase; it never gets a silent default.</action>
  <verify>The closing 18-GATES.md row is either passed with date, hash, and outcome notes, or explicitly records the defect list and the rerun state. The phase does not report complete while this row is pending.</verify>
</task>
</tasks>
<out_of_scope>MCP implementation; accounts or auth; telemetry or hosted services; any signing purchase (18-01's checkpoint owns that decision); touching scripts/preflight.py, tests/preflight_roundtrip.py, AGENTS.md, .gitattributes, or fixtures/ (another agent's in-flight files); building new first-run or error-copy features beyond copy fixes the transcript and gates runs demand (a structural gap is recorded as an honest fail, not patched ad hoc here).</out_of_scope>
<summary_obligations>Record in 18-03-SUMMARY.md: each gates row's final state and its evidence pointer; the transcript defect list and rerun count; the fresh-install evidence lines verbatim; the second-person install outcome, date, and any defects it surfaced; and which truths were verified by which command or observation.</summary_obligations>
