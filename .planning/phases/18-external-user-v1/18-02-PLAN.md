---
phase: 18-external-user-v1
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - tools/capabilities_manifest.py
  - capabilities.json
  - tests/capabilities_roundtrip.py
  - build.py
  - README.md
autonomous: true
requirements: [A10-C2]
must_haves:
  truths:
    - "capabilities.json exists at the repository root, is generated by repository tooling, and regenerating it produces byte-identical output or the test fails."
    - "Reading the manifest is never a network event; no code in this plan opens a socket."
    - "The README points a cold agent at capabilities.json before the command walkthrough."
  artifacts:
    - "tools/capabilities_manifest.py (stdlib only), capabilities.json, tests/capabilities_roundtrip.py."
  key_links:
    - "The manifest version field and the CLI --version print come from the same constant; a second hardcoded copy silently drifts."
    - "build.py's release staging ships capabilities.json beside SHA256SUMS.txt; forgetting the allowlist entry ships a repo-only manifest."
---
<objective>Ship the agent-facing capability disclosure manifest: a static, local, machine-readable capabilities.json generated by repo tooling, shipped in the repo and as a release asset beside the packaged artifact. Implements 18-CONTEXT D-03; the promoted Phase 999.3 MCP surface (server/discover payload) is a named future consumer, and no MCP code lands here (18-CONTEXT D-07). This is additive surface on shipped ground (itembank update, usage, schema); no new authority, no runtime behavior change.</objective>
<context>
@.planning/phases/18-external-user-v1/18-CONTEXT.md
@surfaces/cli.py (the argparse registration blocks; schema at ~1027, usage at ~1035, update at ~1110)
@surfaces/daemon.py (the route dispatch table)
@build.py (the release staging allowlist and latest_json_hook)
@README.md ("Using itembank with an AI coding agent" and the quick-start section)

Convention, stated once for this plan: where a step depends on 17B outcomes that have not shipped at execution time, pin only what is verifiable today and cite ROADMAP.md Phase 17B as the owning contract instead of asserting 17B behavior. Concretely: the command and route inventories are generated from what main serves today; 17B additions enter through regeneration, not through this plan guessing them.
</context>
<tasks>
<task type="auto">
  <name>Task 1: The generator</name>
  <files>tools/capabilities_manifest.py, capabilities.json</files>
  <read_first>surfaces/cli.py main() parser construction; surfaces/daemon.py DaemonHandler dispatch; the version constant the CLI --version prints (locate it, import it, never copy it)</read_first>
  <action>
  1. Write tools/capabilities_manifest.py, Python stdlib only, no network code of any kind. Do not touch scripts/preflight.py; it is another agent's in-flight file, which is why this generator lives under tools/.
  2. The generator emits capabilities.json at the repository root with exactly the D-03 contents, as top-level keys: "schema_version" (integer, starts at 1), "itembank_version" (imported from the same constant --version prints), "format_contract_version" (the model-layer spec version; read it from model.py's published value, or from the schema command's source if model.py exposes none, and name the source in a comment), "commands" (one entry per registered CLI subcommand: name, one-line help, "since" version), "routes" (the daemon's served routes with method and a token-gated flag; the probe marker /__itembank__ is listed open), "skills" (an index of skill directories found under the repository's skill roots, empty list if none), and "changed_for_agents" (a list of {"version", "changes"} entries; seed it with one entry for the current version whose changes list names this manifest's introduction).
  3. "since" values come from a SINCE table inside the generator. Seed every command that exists today with the current release version; the generator raises with the command name if a registered command has no SINCE entry, so a future command cannot ship undisclosed.
  4. Enumerate commands and routes by importing the modules and walking their registrations. Never parse source text with regexes and never maintain a second hand-written list.
  5. Emit with json.dumps(..., ensure_ascii=False, indent=2, sort_keys=True) and a trailing newline, so regeneration is byte-stable.
  6. Run it and commit the generated capabilities.json:
  ```powershell
  python tools/capabilities_manifest.py
  ```
  Expected: exit 0, prints the output path and the command count.
  </action>
  <verify>Running the generator twice in a row produces byte-identical capabilities.json (fc /b old new reports no differences). `python itembank.py guard .` exits 0.</verify>
</task>
<task type="auto">
  <name>Task 2: The drift test and the release asset</name>
  <files>tests/capabilities_roundtrip.py, build.py</files>
  <read_first>tests/packaging_roundtrip.py (house test style: plain script, fail() helper, exit 1 on failure); build.py staging allowlist</read_first>
  <action>
  1. Write tests/capabilities_roundtrip.py in the house pattern: regenerate the manifest into a temp path, byte-compare it against the checked-in capabilities.json, and fail with "FAIL: capabilities.json is stale; run python tools/capabilities_manifest.py and commit the result" on any difference. Also assert: no "http", "urllib", or "socket" import appears in tools/capabilities_manifest.py (the manifest is static and local; the existing disclosure-gated updater is the only network path that ever fetches a newer one, per 18-CONTEXT D-03); every command entry has a nonempty "since"; schema_version is 1.
  2. Extend build.py's release staging so capabilities.json is published beside SHA256SUMS.txt from the same tag, following the exact pattern latest.json uses. Additive only; no existing staging entry changes.
  ```powershell
  python tests/capabilities_roundtrip.py
  ```
  Expected: ok, exit 0. Then edit one character in capabilities.json, rerun, confirm exit 1 with the stale message, revert the edit.
  </action>
  <verify>tests/capabilities_roundtrip.py exits 0 on the clean tree and 1 on a deliberately drifted manifest; `python tests/packaging_roundtrip.py` still exits 0 after the build.py edit.</verify>
</task>
<task type="auto">
  <name>Task 3: README pointer and the changelog convention</name>
  <files>README.md</files>
  <read_first>README.md "If you are an AI assistant onboarding a new user" (starts ~line 86)</read_first>
  <action>Add to the AI-assistant onboarding section, as a new step before step 1, this exact copy:

  "0. Read capabilities.json at the repository root first. It is the machine-readable index of this install: version, format contract version, every CLI command with the version it appeared in, the served routes, and a changed-for-agents changelog. It is a static local file; reading it is not a network event. If your last visit was an older version, the changed_for_agents list is the diff that matters to you."

  Immediately after, document the convention in one sentence: "Each release appends one changed_for_agents entry to capabilities.json describing what an agent must know that release notes for humans would bury." In prose only (build nothing), note that the planned MCP surface (Phase 999.3) will serve this same manifest as its discover payload. No em dash characters.</action>
  <verify>The two copies above appear verbatim in README.md; `python tests/capabilities_roundtrip.py` still exits 0.</verify>
</task>
</tasks>
<out_of_scope>MCP implementation (999.3 owns it, 18-CONTEXT D-07); any network code, telemetry, or manifest-fetching logic; accounts or auth; hosted services; touching scripts/preflight.py, tests/preflight_roundtrip.py, AGENTS.md, .gitattributes, or fixtures/ (another agent's in-flight files); changing any existing CLI command or route behavior.</out_of_scope>
<summary_obligations>Record in 18-02-SUMMARY.md: the manifest key inventory as shipped, the command and route counts, the version-constant source imported, both drift-test outcomes (clean pass, deliberate-drift fail), the build.py staging line added, and any command found without an obvious since version and how it was seeded.</summary_obligations>
