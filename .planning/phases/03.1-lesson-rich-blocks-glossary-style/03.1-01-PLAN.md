---
phase: 03.1-lesson-rich-blocks-glossary-style
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - surfaces/presentation.py
  - surfaces/lesson.py
  - surfaces/study.py
  - tests/lesson_roundtrip.py
  - tests/presentation_roundtrip.py
autonomous: true
ui_classification: READING-SURFACE
requirements:
  - LESSON-13
  - LESSON-14
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "The lesson reader renders theme_css + SHARED_CSS + a new LESSON_CSS layer; LESSON_TEMPLATE stops carrying a private type system (03.1-UI-SPEC §2 LOCKED)."
    - "The reading ramp is exactly three levels: h1 text-display, h2 text-heading, h3 text-body at 600, with the asymmetric space-6/space-3 and space-5/space-2 margins (03.1-UI-SPEC §4 LOCKED)."
    - "The voice/measure tokens --font-paper, --font-ledger, --measure-prose, --leading-lesson ship in surfaces/presentation.py SHARED_CSS with the no-vendor fallback (Georgia / ui-monospace) and no literal family name is referenced by any surface (03.1-UI-SPEC §2, §7.4)."
    - "A bank and lesson using none of the new constructs render the same content region byte-for-byte as Phase 3 output, with the shared style block explicitly excluded and that exclusion named in the fixture citing 03.1-UI-SPEC §12 (LESSON-14)."
    - "One callout container exists for the blockquote-marker grammar; [!EXAMPLE] and [!CHECK:] render as kinds of that container, and an unknown kind degrades to the existing paragraph fallback rather than raising (D-18, LESSON-13)."
    - "The [!CHECK:] anchor renders inert on screen with the exact copy 'This check is available when you are reading with a session.' and as a labelled print rule, carrying no key and no scoring path (03.1-UI-SPEC §9.4, D-18)."
    - "text-lesson at 18px/1.65 is the only new type size already locked at project level; this phase adds no sixth size (D-02)."
  prohibitions:
    - statement: "A lesson may never carry author-supplied executable code; the renderer must not execute or embed a <script> from bank text."
      status: unverified
      flagged: true
    - statement: "The renderer must not introduce a second palette, hex literal, or font-family literal outside surfaces/presentation.py tokens."
      status: unverified
      flagged: true
---

<objective>
As a learner, I want the lesson reader to use the shared Phase 4 visual system and one honest callout container, so that every block the phase adds renders on the same tokens without a second private type system.

Purpose: Land the two load-bearing rendering prerequisites the research verified do not exist today — LESSON_TEMPLATE's private hardcoded CSS (21px/19px/16px/1.55) and the absent blockquote/callout branch in `_render_blocks()`. Everything else in the phase (TERMS gloss, [!KEY], [!EXAMPLE], [!CHECK:], the style footer) renders through what this plan builds.
Output: Token layer additions in SHARED_CSS, LESSON_TEMPLATE migrated onto theme_css + SHARED_CSS + LESSON_CSS, and the one callout-container render branch with the explicit unknown-kind degradation.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-CONTEXT.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-PATTERNS.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-VALIDATION.md

<interfaces>
- `surfaces.presentation.SHARED_CSS` is the one token layer; `surfaces.theme.theme_css()` is the one palette source. This plan adds tokens and never a color literal.
- `surfaces.lesson.render_markdown(text)` and `lesson_page(bank_path, qs, lesson, ref=None)` keep their signatures; `_render_blocks()` gains one new branch inside its existing dispatch chain.
- The lesson page composition moves to the `presentation.surface_shell(...)` layering used by `surfaces/study.py`: `theme_css + SHARED_CSS + LESSON_CSS`.
- Tests follow the repo's stdlib fail()/no-framework harness in `tests/lesson_roundtrip.py` and `tests/presentation_roundtrip.py`.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `surfaces/presentation.py` | `--font-paper`, `--font-ledger`, `--measure-prose` (66ch), `--measure-wide` (90ch), `--leading-lesson` (1.65) tokens in `SHARED_CSS`; `@font-face` stub comments for the two vendored faces (actual files land in plan 03.1-02's vendoring task) |
| `surfaces/lesson.py` | `LESSON_CSS` layer constant; `_render_blocks()` callout branch (`^>\s*\[!TYPE\]`); `_callout_html(kind, label, body)` helper; `render_markdown()` threading of the callout branch; print/reader-nav rules in `LESSON_CSS` |
| `tests/lesson_roundtrip.py` | Byte-identity pair fixture (content region + parse identity, style block excluded per 03.1-UI-SPEC §12) and callout-container fixtures |
| `tests/presentation_roundtrip.py` | Token-presence and fallback assertions for the new tokens |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the voice/measure/leading tokens to SHARED_CSS</name>
  <files>surfaces/presentation.py, tests/presentation_roundtrip.py</files>
  <read_first>
    - `surfaces/presentation.py` — SHARED_CSS token block and surface_shell composition (lines 30-112, 160-180)
    - `surfaces/theme.py` — the palette token contract this plan may not extend
    - `surfaces/study.py` — the surface_shell(...) call the lesson template must later reuse
    - `tests/presentation_roundtrip.py` — existing token assertions to extend
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §2, §4, §7.1-§7.3
  </read_first>
  <behavior>
    - Test 1: `SHARED_CSS` contains `--font-paper`, `--font-ledger`, `--measure-prose:66ch`, `--measure-wide:90ch`, and `--leading-lesson:1.65`.
    - Test 2: the fallback stack names Georgia and ui-monospace, and no other surface file references a font family literal.
    - Test 3: no color literal (hex) was added to presentation.py by this task.
  </behavior>
  <action>
Add to `surfaces/presentation.py`'s `SHARED_CSS` the design-token custom properties: `--font-paper` with value `Georgia, "Source Serif 4", serif` and `--font-ledger` with value `ui-monospace, "iA Writer Quattro", monospace` (family ordering per 03.1-UI-SPEC §2: the vendored face is preferred when present, the no-vendor fallback is the named system face); `--measure-prose:66ch`; `--measure-wide:90ch`; `--leading-lesson:1.65`. Do not fetch or vendor any font file in this task (that is plan 03.1-02). Do not add `@font-face` rules yet; add a comment block naming the two vendored faces and the requirement that the `@font-face` block land in this same layer. Preserve the existing 8-point spacing, 14/16/20/28 size scale, and 400/600 weight tokens exactly. Extend `tests/presentation_roundtrip.py` with the three behavior tests, written first.
  </action>
  <verify>
    <automated>python tests/presentation_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - `SHARED_CSS` contains the five tokens with the exact values above.
    - `grep -rn "font-family" surfaces/ | grep -v presentation.py` returns only existing pre-Phase-3.1 call sites; no new family literal exists outside `presentation.py`.
    - `tests/presentation_roundtrip.py` exits 0.
  </acceptance_criteria>
  <done>The reading surface has its token layer; every later rule references tokens, never literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate LESSON_TEMPLATE onto theme_css + SHARED_CSS + LESSON_CSS</name>
  <files>surfaces/lesson.py, surfaces/study.py, tests/lesson_roundtrip.py</files>
  <read_first>
    - `surfaces/lesson.py` — LESSON_TEMPLATE (lines 39-82) and lesson_page() (lines 340-410)
    - `surfaces/study.py` — the presentation.surface_shell(...) composition pattern to reuse
    - `surfaces/presentation.py` — SHARED_CSS after Task 1
    - `tests/lesson_roundtrip.py` — the existing render fixture and its byte-identity assertions
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §2 (Reader CSS LOCKED), §4 (heading ramp), §10 (print contract), §12 (compatibility floor)
  </read_first>
  <behavior>
    - Test 1: the rendered lesson page's `<style>` is composed as theme_css then SHARED_CSS then LESSON_CSS, and LESSON_TEMPLATE contains no `font-size`, `font-family`, `line-height`, or color literal.
    - Test 2: h1 renders at text-display (32px/1.1/600), h2 at text-heading (20px/1.2/600), h3 at text-body 600 (16px/1.4/600), with the asymmetric margins space-6/space-3 and space-5/space-2.
    - Test 3: the content region (__BODY__) of a bank using none of the new constructs is byte-identical to the Phase 3 golden fixture, with the style block excluded and the exclusion named in the fixture citing 03.1-UI-SPEC §12.
    - Test 4: the parsed question dicts and parsed lesson structure for that bank are byte-equal to the Phase 3 goldens (parse identity).
  </behavior>
  <action>
Replace the private `<style>` block inside `LESSON_TEMPLATE` with the three-layer composition: `THEME_CSS` (the existing `__THEME__` substitution), `SHARED_CSS` (imported from `surfaces.presentation`), and a new module constant `LESSON_CSS` defined in `surfaces/lesson.py`. `LESSON_CSS` may set structure and spacing only — it must not introduce a font family, a type size, a weight, or a color literal. Implement the locked heading ramp: `h1` at `text-display` size/weight with `margin:0 0 var(--space-3)`, `h2` at `text-heading` with `margin:var(--space-6) 0 var(--space-3)`, `h3` at `text-body` 600 with `margin:var(--space-5) 0 var(--space-2)`, lesson prose and list items at `text-lesson` (`font-size:18px;line-height:var(--leading-lesson)`) and `max-width:var(--measure-prose)` on the prose column. Add the print contract's `@page { margin: 18mm }` block, `h2{break-after:avoid}`, `[popover]{display:none}` for the `print_gloss: appendix` default, and the `?print=drill` second-stylesheet placeholder hook (cloze blanking lands in plan 03.1-04). Keep the `__WARN_CSS__` substitution and the reduced-motion rule. Route `lesson_page()` through the same composition the study surface uses so `THEME_CSS` and `SHARED_CSS` are not duplicated. Extend `tests/lesson_roundtrip.py` with the four behavior tests; generate the Phase 3 golden content region from the current code before editing (the pre-edit output), store it as the fixture, and assert the post-edit render of the same bank matches it byte-for-byte with the style block excluded.
  </action>
  <verify>
    <automated>python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - `LESSON_TEMPLATE` contains no `font-size`, `font-family`, `line-height`, or hex literal.
    - The lesson page composes exactly `theme_css + SHARED_CSS + LESSON_CSS` and the byte-identity pair (content region, style excluded) passes.
    - The heading-ramp rules match the §4 table exactly and the print block contains `@page{margin:18mm}`.
  </acceptance_criteria>
  <done>The reader renders on the shared visual system; the compatibility floor is proven by fixture, not promised.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the one callout container in _render_blocks</name>
  <files>surfaces/lesson.py, tests/lesson_roundtrip.py</files>
  <read_first>
    - `surfaces/lesson.py` — _render_blocks() (lines 242-308), _inline() (lines 214-224), _protect_code() (lines 155-195)
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md` — Pattern 3 (the callout container does not exist yet) and Pitfall 1
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §9.2-§9.4 (kind markup), §5 (colour rules), §15 (copy contract)
    - `tests/lesson_roundtrip.py` — the extended fixture set after Task 2
  </read_first>
  <behavior>
    - Test 1: a `> [!KEY]` block renders as `<section class="callout callout-key">` with label `Key point`; a `> [!EXAMPLE]` block renders `<section class="callout callout-example">` with label `Example`; a `> [!CHECK: <id>]` block renders the inert screen slot with the exact copy `This check is available when you are reading with a session.`
    - Test 2: an unknown callout kind (e.g. `> [!NOPE]`) falls through to the paragraph branch and renders exactly as the pre-change renderer rendered it (GitHub-alert degradation preserved).
    - Test 3: every rendered callout body and label is HTML-escaped: a body containing `<script>` or `&lt;` renders as escaped text with no executable tag.
    - Test 4: the container is a single new branch — the existing fenced-code, heading, list, and table branches produce byte-identical output to the pre-change golden for a bank with none of the new constructs.
  </behavior>
  <action>
Add to `_render_blocks()`' dispatch chain, before the paragraph fallback, a blockquote-marker branch matching `^>\s*\[!([A-Za-z]+[^\]]*)\]\s*(.*)$` at the start of a run, collecting consecutive `>`-prefixed lines as the callout body and closing at the first non-`>` line. Implement `_callout_html(kind, label, body)` that renders `<section class="callout callout-<slug>">` with a `<p class="callout-label">` (icon slot `aria-hidden="true"`, then the escaped visible label) and `<div class="callout-body">` whose content is produced by `_inline()` on the joined body lines — the same escape-first discipline `_inline()` already documents. Known kinds in this task: `KEY` (label `Key point`), `EXAMPLE` (label `Example`), `NOTE` (label `Note`), `WARNING` (label `Warning`), and the `CHECK:` prefix (label `Check`); the `[!CHECK: <id>]` variant renders the inert reserved slot with the exact Ledger-voice copy `This check is available when you are reading with a session.` and no form, no key, and no scoring path (D-18), plus the print rule rendering it as a labelled horizontal rule with `Check · <objective>` when an objective is present. Any other `[!...]` kind falls through to the existing paragraph path unchanged (explicit unknown-kind degradation, D-18). Add the callout rules to `LESSON_CSS`: `--card` background, 1px `--line` border, `--r-3` radius, one non-blurred `box-shadow: 0 1px 0 var(--line)`, small-caps Ledger label treatment, body at `text-lesson`. Colour must never carry the kind semantics — label + icon + structure carry it (03.1-UI-SPEC §5). Extend `tests/lesson_roundtrip.py` with the four behavior tests, written first.
  </action>
  <verify>
    <automated>python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The three new kinds render their exact locked labels and the [!CHECK:] inert copy appears verbatim.
    - The unknown-kind fixture output is byte-identical to the pre-change paragraph render.
    - The stored-XSS fixture shows escaped text (`&lt;script&gt;`) and no `<script>` element in the rendered HTML.
    - No new renderer branch exists beyond the one callout branch; the existing branches' goldens are unchanged.
  </acceptance_criteria>
  <done>Every block the phase adds has one honest container, and the old renderer's degradation behaviour is preserved as the explicit fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| bank-authored markdown -> rendered HTML | Lesson prose is untrusted author text; the renderer escapes before interpolation |
| callout kind marker -> class/label | A hostile kind string must not close attributes or introduce markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-031-01 | Injection | new callout render path | high | mitigate | Every body and label passes through the existing `_inline()`/`html.escape()` pass before interpolation; the stored-XSS fixture asserts no `<script>` and no unescaped angle bracket reaches the page (research Security Domain; 03.1-UI-SPEC §11) |
| T-031-02 | Tampering | callout kind string | high | mitigate | The kind token is run through `lesson_slug()` for the class name and the visible label is HTML-escaped; a hostile marker cannot close the class attribute or inject a second attribute |
| T-031-03 | Information Disclosure | `[!CHECK:]` reserved slot | medium | mitigate | The anchor renders the inert copy only — no key, no scoring path, no form — per D-18; nothing in the reserved slot derives from item content |
| T-031-SC | Tampering | vendored assets | high | mitigate | This plan performs no package operation and no font fetch (vendoring is plan 03.1-02's task with its own supply-chain rows); token fallbacks mean a missing asset degrades to Georgia/ui-monospace |
</threat_model>

<verification>
- `python tests/lesson_roundtrip.py`
- `python tests/presentation_roundtrip.py`
- `python tests/protocol_roundtrip.py` (regression — renderer changes must not drift the protocol surface)
- Grep `surfaces/lesson.py` for `font-size|font-family|line-height` literals inside `LESSON_TEMPLATE` — must return nothing.
</verification>

<success_criteria>
- The lesson reader composes theme_css + SHARED_CSS + LESSON_CSS and renders the locked heading ramp and 66ch measure.
- One callout container exists; KEY, EXAMPLE, NOTE, WARNING, and CHECK kinds render; unknown kinds degrade to paragraphs byte-identically.
- The byte-identity pair (content region + parse identity) passes for a bank using none of the new constructs.
</success_criteria>

<output>
Create `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-01-SUMMARY.md` when done.
</output>
