---
phase: 03.1-lesson-rich-blocks-glossary-style
verified: 2026-08-11
status: human_needed
score: 7/7 must-haves statically verified; live automated suite pending
behavior_unverified: 0 # every roadmap truth below has a named fixture present on disk; live execution is sandbox-blocked this session
overrides_applied: 0
human_verification:
  - test: "True cross-browser Popover/Anchor-Positioning behavior: a gloss popover opens with scripting disabled, dismisses on Escape, returns focus to the trigger, and stays on screen at 320px; a daemon-stopped sitting renders the exact degraded copy with no dead control."
    expected: "Popover-API gloss behaves correctly in a real browser; 'Definitions are unavailable right now.' renders when the daemon is stopped; the Add-to-review form is omitted with its degraded copy when the runtime is absent."
    why_human: "No browser-automation tooling exists in this stdlib-only suite; DOM/HTML shape is asserted automatically (03.1-GATES.md gates 2/3)."
  - test: "ClearType 18px render check of both vendored faces at 375px and 1280px on Windows."
    expected: "Source Serif 4 and iA Writer Quattro render legibly at 18px on Windows ClearType at both widths (fonts/REGISTRY-SAFETY.md precondition 4)."
    why_human: "Perceptual rendering quality cannot be proven by string assertions; recorded as a human-verify item at phase end."
  - test: "Fixed-width viewport snapshots at 1280/768/375px of the lesson reader with reader_nav and example_layout in both values."
    expected: "No clipping, no page-level horizontal scroll, correct stack/parallel layouts at each width (03.1-UI-SPEC section 17 item 6)."
    why_human: "The suite has no browser-automation tool; breakpoint rules and value combinations are asserted, explicit-width rendering is not."
  - test: "Live execution of the full automated suite and schema validation in a python-capable environment."
    expected: "`for f in tests/*_roundtrip.py; do python \"$f\" || exit 1; done` exits 0, and `python schema_validate.py --all` passes, proving the eight gate fixtures and all pre-existing tests green together."
    why_human: "This session's approval gate declines every python invocation (honest-verification precedent 03.2-05-SUMMARY); every fixture exists on disk (15/15 re-grepped) but no green claim is made until the suite runs."
---

# Phase 3.1: Lesson Rich Blocks, Glossary & Style — Verification Report

**Phase Goal:** As an author, I want a lesson to carry its own vocabulary
(`## TERMS` + `[[term]]`), an explicit must-memorize block (`[!KEY]`), and a
written style contract enforced by the linter (`styles/<id>.md` + a
`## Rules` table), plus one new parse path (`[!CHECK: <id>]`), one callout
kind (`[!EXAMPLE]`), one optional per-item Educational Objective line, and one
reserved TERMS meta field (`zh=`) — so the reading surface expresses teaching
structure instead of plain prose, and adherence to a voice is measured rather
than hoped for.

**Verified:** 2026-08-11 (static; live suite sandbox-blocked)
**Status:** human_needed
**Re-verification:** The full automated suite and `schema_validate.py --all`
must be executed in a python-capable environment (approval gate in this
session declines python); the three perceptual/browser human-verify items
stand regardless.

## User Flow Coverage

| Step | Expected | Evidence | Status |
|------|----------|----------|--------|
| Author a lesson with new grammar | `## TERMS`/`[[term]]` (with `zh=` meta), `> [!KEY: <title>]` (in-bracket title fix present, model.py:890-894), `[!EXAMPLE]`, `[!CHECK: <id>]` (same-bank-only), the Educational Objective line, and a `styles/<id>.md` voice parse and lint clean | `parse_key_blocks` in-bracket fix; `cmd_spec()` documents every construct (surfaces/cli.py:47-101); grammar fixtures in tests/lesson_roundtrip.py, style_roundtrip.py | ✓ static (fixtures present; live run pending) |
| Gloss a term | A runtime `glossable()` gate decides; hover uses the Popover API; the no-JS degradation is a glossary appendix from the same `## TERMS` block; suppressed terms leak nothing | tests/lesson_roundtrip.py::test_gloss_triggers_panels_and_no_leak, test_glossary_appendix_renders_after_lesson, test_gloss_route_and_cli_twin (present); D-20/D-21/D-23 | ✓ static (live browser behavior -> human) |
| Key a must-memorize card | `[!KEY]` mints `[ID:]/[HASH:]` in the item taken-set namespace, exports Anki TSV with a round-tripping `#guid`, supports `{{cloze}}`, and never appears in an item rationale | parse_key_blocks + key lint codes; tests/anki_keys_roundtrip.py (guid fixture), lesson_roundtrip.py key fixtures (present) | ✓ static |
| Enforce a written style | Every lint runs the three cost classes; the catalogue is closed with an earned-severity ceiling; suppressions are counted; locked ids cannot be suppressed; a style error blocks machine writes, never human lint; the pass stays under 50ms on 5000 words | model.py run_style_pass/apply_style_ignore/write_allowed/StylePrompt; 23 style_roundtrip.py fixtures incl. the 50ms budget (present) | ✓ static (live run pending) |
| Read in the shipped voice | The two vendored faces load through the token layer with the Georgia/ui-monospace fallback; no surface names a family literally; the artifact ships fonts/ | fonts/ hash-verified ALL_OK this session; @font-face in presentation.py; build.py STAGE_DIRS | ✓ static (ClearType render -> human) |
| Compatibility floor | A bank/lesson using none of the new constructs renders byte-identically to Phase 3 output | fixtures/lesson_golden_phase3_{content,parse}.* + test_lesson_content_region_byte_identical_phase3/test_lesson_parse_identity_phase3 (present; style-block exclusion named, section 12 cited) | ✓ static (live run pending) |
| Fold lesson_layout before Phase 9 | `lesson_layout` ("separate"\|"inline") is in 09-02-PLAN.md's subject_profiles contract; no registry bump in 3.1 | grep lesson_layout: 09-02-PLAN.md lines 31/83/108/119 | ✓ verified |

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A lesson can carry its own vocabulary: `## TERMS`/`[[term]]` with the reserved `zh=` meta, a `[!KEY]` must-memorize block with minted `[ID:]/[HASH:]` and cloze, an `[!EXAMPLE]` callout kind, a `[!CHECK: <id>]` same-bank-only anchor, and an optional private Educational Objective line. | ✓ VERIFIED (static) | `parse_key_blocks` in-bracket `[!KEY: <title>]` fix (model.py:890-894); TERMS/key/objective lint codes in LINT_CODES; spec documents all constructs (surfaces/cli.py:47-101); fixtures present (lesson_roundtrip.py:2708-3175, protocol_roundtrip.py:479, anki_keys_roundtrip.py). Live execution pending. |
| 2 | A gloss passes through the runtime `glossable()` gate; suppressed terms leak nothing; hover uses the Popover API and the no-JS degradation is a glossary appendix from the same TERMS block. | ✓ VERIFIED (static) | tests/lesson_roundtrip.py::test_gloss_triggers_panels_and_no_leak + test_glossary_appendix_renders_after_lesson + test_gloss_route_and_cli_twin (present; D-20/D-21). True cross-browser behavior -> human. |
| 3 | The style contract is enforced on every lint by three cost classes over a closed catalogue with an earned-severity ceiling; suppressions are counted; locked ids cannot be suppressed; a style error blocks machine writes and never human lint; the whole pass is under 50ms on 5000 words. | ✓ VERIFIED (static) | model.py run_style_pass + STYLE_CHECK_CATALOGUE + apply_style_ignore + write_allowed + StylePrompt; 23 style_roundtrip.py fixtures incl. test_50ms_budget_on_5000_word_lesson (present). Live run pending. |
| 4 | The authoring model receives capped distilled imperatives plus one exemplar, placed last, with no Voice prose; the cap is a setting (default 7). | ✓ VERIFIED (static) | StylePrompt.prompt_context (model.py:2187-2260); style settings group in surfaces/settings.py + schemas/settings.schema.json:337-352 + itembank.json; prompt-contract fixtures present. |
| 5 | The reading voice is physically shipped: two OFL-1.1 woff2 families vendored with MANIFEST + license + reserved-names notes, wired through the token layer, degrading to Georgia/ui-monospace when absent, and staged into the artifact. | ✓ VERIFIED | fonts/ all 4 woff2 hash-verified this session (size/sha256/git-blob-sha1 ALL_OK); @font-face in presentation.py; build.py STAGE_DIRS; presentation_roundtrip.py fixtures present. ClearType render -> human. |
| 6 | The compatibility floor holds: a bank using none of the new constructs renders byte-identically to Phase 3 output, with the style-block exclusion named and UI-SPEC section 12 cited. | ✓ VERIFIED (static) | fixtures/lesson_golden_phase3_content.txt + lesson_golden_phase3_parse.json present; test_lesson_content_region_byte_identical_phase3 (lesson_roundtrip.py:1078) + test_lesson_parse_identity_phase3 (:1093); GATES.md gate 4 records the exclusion + citation. |
| 7 | lesson_layout is folded into 09-02-PLAN.md before Phase 9 executes; no registry version bump in 3.1. | ✓ VERIFIED | lesson_layout in 09-02-PLAN.md action (line 108), artifacts (83), provides (31), acceptance criteria (119); D-04 timing. |

**Score:** 7/7 truths statically verified (0 present-but-behavior-unverified);
live automated runs are recorded as pending, not claimed.

### Deferred Items

| Item | Why deferred | Owner |
|------|--------------|-------|
| Live full-suite green run + `schema_validate.py --all` | This session's approval gate declines python execution (precedent 03.2-05-SUMMARY); fixtures exist (15/15 re-grepped) | python-capable environment / CI |
| True cross-browser Popover behavior | No browser-automation tool in the suite | Human (Gate 2/3) |
| ClearType 18px render of both faces at 375/1280px | Perceptual quality | Human (Gate 7) |
| 1280/768/375px viewport snapshots | No browser-automation tool | Human (Gate 6) |
| Gate 5 inline-print note-count numeric equality; Gate 7 per-string computed-font-family assertion | Recorded gaps in 03.1-GATES.md, not silently claimed | Future fixture work |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `model.py` lesson-grammar + style-enforcement surface | TERMS/key/objective/style parse + lint codes; run_style_pass; suppression; StylePrompt | ✓ PRESENT | parse_key_blocks in-bracket fix (890-894); STYLE_CHECK_CATALOGUE; 23 style fixtures |
| `styles/` registry | house + five styles; Rule 6 one-file proof | ✓ PRESENT | 6 files on disk; stage-dirs + rule-6 fixtures |
| `fonts/` | two vendored families + MANIFEST + REGISTRY-SAFETY + licenses | ✓ PRESENT | hashes verified ALL_OK |
| `surfaces/presentation.py` | @font-face token layer + fallback | ✓ PRESENT | SHARED_CSS lines 49-63 |
| `surfaces/cli.py` | cmd_spec new-grammar sections | ✓ PRESENT | lines 47-101 |
| `build.py` | STAGE_DIRS ships fonts/ | ✓ PRESENT | ("surfaces","schemas","styles","fonts") |
| `tests/phase_031_audit.py` | 11/11 requirement-coverage audit | ✓ PRESENT | checks a-d |
| `03.1-GATES.md` | eight gates mapped with gaps + human-verify | ✓ PRESENT | all 15 claimed fixtures re-verified |
