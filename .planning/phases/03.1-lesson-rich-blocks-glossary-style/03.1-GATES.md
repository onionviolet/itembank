# Phase 03.1 — Verification Gates Checklist

> Contract: `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md`
> section 17 — the eight required verification gates, each mapped to the
> fixture that executes it. Written by plan 03.1-07 Task 1/2. Nothing here
> claims a fixture that does not exist on disk; every fixture name below was
> verified by grep against `tests/` before being recorded.

Legend: **Automated** = an executable fixture in `tests/`. **Human** =
a recorded manual-verification item (no browser-automation tooling exists in
this stdlib-only suite).

---

## Gate 1 — No-leak fixture (C7)

| | |
|---|---|
| **UI-SPEC section proved** | §17 item 1 (no-leak), §9.5 (objective privacy) |
| **Automated fixture** | `tests/lesson_roundtrip.py::test_gloss_triggers_panels_and_no_leak` — a suppressed term (definition leaks a keyed answer) has no trigger, no `aria-details`, no glossary `<dt>`, renders as bare text; the held line `Some definitions are held until you answer.` appears exactly once; the unavailable copy is present. |
| **Automated fixture** | `tests/protocol_roundtrip.py::test_educational_objective_private_until_verdict` — `objective`/`educational_objective` absent from `public_item()` and pre-answer quiz HTML, present in `explain_payload()`. |
| **Automation status** | Automated |
| **Human-verify** | — |

## Gate 2 — Declarative gloss fixture

| | |
|---|---|
| **UI-SPEC section proved** | §17 item 2 (declarative gloss), §8.1 (Popover-API baseline) |
| **Automated fixture** | `tests/lesson_roundtrip.py::test_gloss_triggers_panels_and_no_leak` — Popover-API DOM shape: `popovertarget="gloss-airway"`, `aria-details`, `<div id="gloss-airway" class="gloss" popover>`, dotted-underline treatment. |
| **Automation status** | Automated for DOM/HTML shape |
| **Human-verify** | **True cross-browser Popover/Anchor-Positioning behavior** (opens with scripting disabled, dismisses on Escape, returns focus to the trigger, stays on screen at 320px) — no browser automation exists in this suite; recorded per plan 03.1-02's verification note and 03.1-RESEARCH. |

## Gate 3 — Degraded gloss fixture

| | |
|---|---|
| **UI-SPEC section proved** | §17 item 3 (degraded gloss), §8.3/§8.4 (fetch-primary/navigation-degraded, glossable silence) |
| **Automated fixture** | `tests/lesson_roundtrip.py::test_gloss_triggers_panels_and_no_leak` — asserts the verbatim unavailable copy `Definitions are unavailable right now.` renders (never a spinner, never a dead control). |
| **Automated fixture** | `tests/lesson_roundtrip.py::test_gloss_route_and_cli_twin` — the navigation path serves a gloss page carrying `Back to the question`; unknown/suppressed slugs 404 with no event. |
| **Automated fixture** | `tests/lesson_roundtrip.py::test_key_card_renders_runtime_and_degraded` — with the runtime absent, the `Add to review` form is omitted and the exact degraded copy renders instead of a disabled control. |
| **Automation status** | Automated for copy/DOM shape |
| **Human-verify** | Live daemon-stopped-mid-sitting behavior in a browser (same no-browser-tooling note as Gate 2). |

## Gate 4 — Byte-identity pair (D-23, §12 compatibility floor)

| | |
|---|---|
| **UI-SPEC section proved** | §17 item 4, §12 (LOCKED), Directive §4.4 (C3) |
| **Automated fixture** | `tests/lesson_roundtrip.py::test_lesson_content_region_byte_identical_phase3` — content region byte-identical to the Phase 3 golden with the shared `<style>` block **explicitly excluded and the exclusion named**, citing 03.1-UI-SPEC §12. |
| **Automated fixture** | `tests/lesson_roundtrip.py::test_lesson_parse_identity_phase3` — parsed question dicts and lesson structure byte-equal to the Phase 3 goldens. |
| **Golden data** | `fixtures/lesson_golden_phase3_content.txt`, `fixtures/lesson_golden_phase3_parse.json` (present on disk) |
| **Automation status** | Automated |

## Gate 5 — Three print fixtures

| | |
|---|---|
| **UI-SPEC section proved** | §17 item 5, §10.1 (print_gloss appendix\|inline), §10.2 (drill) |
| **Automated fixture — appendix** | `tests/lesson_roundtrip.py::test_glossary_appendix_renders_after_lesson` — the `<section id="glossary">` appendix renders after the last lesson section; `test_gloss_marks_and_print_modes` covers the `print_gloss: appendix` default. |
| **Automated fixture — inline (first-use note count)** | `tests/lesson_roundtrip.py::test_gloss_marks_and_print_modes` — `print_gloss: inline` ships the `@media print` reflow (`display:block!important`) and `gloss_marks: first-use` marks one occurrence per section. **Gap:** no assertion that the printed-note count equals the distinct-term count (the §16 backstop wording); the reflow and the first-use marking are asserted, the numeric count equality is not. |
| **Automated fixture — ?print=drill** | `tests/lesson_roundtrip.py::test_drill_print_blanks_clozes_and_answers_last` — clozes blanked server-side, `<section id="answers">` present, no answer text above it. |
| **Automation status** | Automated (one sub-assertion gap recorded) |
| **Human-verify** | — |

## Gate 6 — Responsive snapshots

| | |
|---|---|
| **UI-SPEC section proved** | §17 item 6, §7.2 (responsive behaviour) |
| **Automated fixture** | `tests/presentation_roundtrip.py::test_responsive_zoom_and_noscript_fallback` — asserts the narrow breakpoint (`@media (max-width:767px)`), the overflow-safe `min-width:0` rule for 320px/200% zoom, reduced-motion, and the `<noscript>` semantic fallback. |
| **Automated fixture** | `tests/lesson_roundtrip.py::test_gloss_example_layout_and_reader_nav` — `reader_nav` both values (none default / column nav with slug links) and `example_layout` both values (stacked default / parallel class). |
| **Automation status** | Partial — automated for the breakpoint rules and for the `reader_nav`/`example_layout` value combinations |
| **Human-verify** | **Viewport snapshots at 1280 / 768 / 375px** for the reader with `reader_nav` both values and `example_layout` both values: no fixture renders at explicit 1280/768/375 widths (this suite has no browser-automation tool), so the fixed-width snapshot gate is human-verified. |

## Gate 7 — Voice fixture

| | |
|---|---|
| **UI-SPEC section proved** | §17 item 7, §6 (voice assignment), §15 (copy contract) |
| **Automated fixture** | `tests/presentation_roundtrip.py::test_voice_measure_leading_tokens_ship_in_shared_css` — the five voice/measure tokens ship in `SHARED_CSS` with the locked values. |
| **Automated fixture** | `tests/presentation_roundtrip.py::test_font_tokens_name_fallbacks_and_only_presentation_names_families` — fallbacks named (Georgia / ui-monospace) and the vendored family names appear **only** inside `presentation.py`; no surface names a family literally. Also `test_no_family_literal_in_surface_templates`. |
| **Automation status** | Partial — the token layer and the no-literal-family rule are asserted |
| **Human-verify** | **ClearType render check for the vendored fonts** at 375px and 1280px per `fonts/REGISTRY-SAFETY.md` precondition 4 (recorded human-verify item, not claimed done). Gap also recorded: no fixture asserts every §15 string renders in its §6-assigned voice by a **computed** font-family token on rendered output (only the token definitions and the no-literal-family greps are automated). |

## Gate 8 — Extensibility Rule 6 stub

| | |
|---|---|
| **UI-SPEC section proved** | §17 item 8 (Rule 6 stub) |
| **Automated fixture** | `tests/style_roundtrip.py::test_rule6_stub_one_file_extensibility` — a throwaway sixth style file loads through `load_style` and lints clean with **zero code change** (the test writes only the new file). |
| **Automated fixture** | `tests/style_roundtrip.py::test_stage_dirs_allowlist_ships_styles` — `styles/` is in `build.py`'s `STAGE_DIRS` allowlist. |
| **Automation status** | Automated |

---

## Human-verify items (consolidated)

1. **True cross-browser Popover/Anchor-Positioning behavior** — Gate 2/3: Escape dismiss, focus return, top-layer stacking, on-screen at 320px. Recorded as human-verify per plan 03.1-02 and 03.1-RESEARCH (no browser-automation tool in this session).
2. **ClearType render check for the vendored fonts** — Gate 7: 18px rendering of Source Serif 4 and iA Writer Quattro on Windows ClearType at 375px and 1280px, per `fonts/REGISTRY-SAFETY.md` precondition 4. Recorded there as a human-verify item, **not** claimed done.
3. **Fixed-width viewport snapshots (1280/768/375px)** — Gate 6.

## Recorded gaps (not silently claimed)

- **Gate 5** inline print fixture: the first-use **note count equals distinct-term count** numeric assertion (§16 backstop) is not present; the `@media print` reflow and first-use marking are.
- **Gate 6** responsive: no automated snapshot at explicit 1280/768/375 widths.
- **Gate 7** voice: no per-string computed font-family-token assertion on rendered §15 copy.
- **Visible-skip sentence**: plan 03.1-07's audit spec required the sentence
  `deterministic edge probe skipped` to be present in **every** PLAN.md. It was
  originally present only in `03.1-07-PLAN.md`; the sentence was added to
  plans 03.1-01..06 during plan 07 execution, and
  `tests/phase_031_audit.py` now confirms it in all seven (11/11, no gap
  line).

## Audit

Executable coverage check: `tests/phase_031_audit.py` (stdlib only, reads
only `.planning` files). See the file's docstring for checks (a)–(d).
