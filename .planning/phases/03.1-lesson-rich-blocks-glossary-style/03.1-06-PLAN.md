---
phase: 03.1-lesson-rich-blocks-glossary-style
plan: 06
type: execute
wave: 3
depends_on:
  - 03.1-01
files_modified:
  - fonts/
  - surfaces/presentation.py
  - surfaces/cli.py
  - .planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-02-PLAN.md
  - build.py
  - tests/presentation_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - LESSON-14
  - LESSON-17
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: medium
must_haves:
  truths:
  - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run (recorded in each PLAN.md)."
    - "Source Serif 4 and iA Writer Quattro are vendored per the KaTeX precedent: pinned immutable release tag, recorded SHA-256, license file and reserved-font-name note kept beside each asset, and no byte fetched until the four Registry-Safety preconditions are recorded (D-01, Directive 4a supply-chain rule, 03.1-UI-SPEC Registry Safety)."
    - "With no font file present, --font-paper falls back to Georgia and --font-ledger to ui-monospace, and every UI-SPEC rule still holds; no surface names a family literally (03.1-UI-SPEC 2, 7.4)."
    - "spec documents the new grammar - ## TERMS, [[term]], [!KEY], [!EXAMPLE], [!CHECK:], the Educational Objective line, and styles/<id>.md - so an authoring agent can write lessons without seeing source (LESSON-05 discipline extended additively; the new LESSON-* IDs are documented in REQUIREMENTS.md)."
    - "lesson_layout: separate | inline is folded into 09-02-PLAN.md's subject_profiles entry contract before Phase 9 executes - EMT/Math separate, CS inline - with no registry version bump in 3.1 (D-04, LESSON-17)."
  prohibitions:
    - statement: "A vendored font must not be fetched from a non-pinned URL or without its recorded checksum and license review."
      status: unverified
      flagged: true
    - statement: "No surface may reference a font family literal outside the presentation token layer."
      status: unverified
      flagged: true
---

<objective>
As the project, I want the reading voice physically shipped and licensed, the format contract documented for authoring agents, and the lesson-layout decision folded into the phase that owns the subject registry - before any of those become migrations.

Purpose: Close the phase's three remaining seams: the vendored typefaces (D-01), the spec documentation for every new construct, and the D-04 lesson_layout fold into 09-02-PLAN.md.
Output: fonts/ with two licensed woff2 families and their records, @font-face rules in the token layer, cmd_spec coverage of the new grammar, and the 09-02 plan fold.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-CONTEXT.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-VALIDATION.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-02-PLAN.md

<interfaces>
- `surfaces/presentation.py` SHARED_CSS is the one @font-face home; `surfaces/theme.py` stays the palette source and is untouched.
- `build.py` STAGE_DIRS is the allowlist that ships fonts/ in the artifact (the KaTeX precedent in 09-03-PLAN.md).
- `surfaces/cli.py cmd_spec()` is the one contract printer; the new grammar sections land there.
- The 09-02 fold is a plan-text edit before Phase 9 executes; it is not a schema or code change in this phase.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `fonts/` | Vendored Source Serif 4 + iA Writer Quattro woff2 files, each with its LICENSE file and a RESERVED-NAMES note; a MANIFEST.json recording tag, SHA-256, and license review |
| `surfaces/presentation.py` | `@font-face` rules for `--font-paper`/`--font-ledger` with local-file src and the system fallback |
| `surfaces/cli.py` | cmd_spec sections for TERMS/[[term]]/[!KEY]/[!EXAMPLE]/[!CHECK:]/objective/styles grammar |
| `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-02-PLAN.md` | `lesson_layout` enum added to the subject_profiles entry contract and its acceptance criteria |
| `build.py` | `fonts/` added to STAGE_DIRS |
| `tests/presentation_roundtrip.py` | @font-face presence + fallback assertions |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor Source Serif 4 and iA Writer Quattro with full supply-chain records</name>
  <files>fonts/, build.py, tests/presentation_roundtrip.py</files>
  <read_first>
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` Registry Safety (the four unmet preconditions: immutable tag, published SHA-256, woff2 size, ClearType render check) and section 2 (fallback rules)
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-03-PLAN.md` - the KaTeX vendoring precedent (pinned tag, checksum, license review ceremony)
    - `build.py` - STAGE_DIRS allowlist
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md` - the Standard Stack font rows and the A1 assumption (license characterization carried from UI-SPEC, to be re-verified at fetch time)
  </read_first>
  <behavior>
    - Test 1: each vendored font directory contains the woff2 file(s), the OFL 1.1 license text, a RESERVED-NAMES note naming the OFL Reserved Font Names ("Source", "iA Writer", "Plex"), and a MANIFEST.json row with release tag and SHA-256.
    - Test 2: the recorded SHA-256 matches the fetched bytes (recomputed in the test).
    - Test 3: `build.py` STAGE_DIRS includes `fonts/` so the artifact ships the families.
  </behavior>
  <action>
Record the four Registry-Safety preconditions before fetching any byte: the immutable GitHub release tag for Source Serif 4 and for iA Writer Quattro, the published archive SHA-256, the woff2 file sizes, and the ClearType render-check note (a recorded human-verify item at phase end). Then fetch the woff2 assets from the pinned tags, save them under `fonts/source-serif/` and `fonts/ia-writer-quattro/`, and place each family's OFL 1.1 license text plus a RESERVED-NAMES note (the two faces carry OFL Reserved Font Names - "Source" for Source Serif, "iA Writer" and "Plex" for iA Writer Quattro - so a subset build must be renamed or the files shipped unmodified; ship unmodified). Write `fonts/MANIFEST.json` with one row per family: tag, source URL, sha256, size, license path, reserved-names note, and the reviewer date. If the fetch cannot reach GitHub, do not fail the phase: record the fallback decision, keep the no-vendor CSS stack, and leave the vendoring task as a documented re-run. Add `fonts/` to `build.py`'s STAGE_DIRS. Write the behavior tests first.
  </action>
  <verify>
    <automated>python tests/presentation_roundtrip.py &amp;&amp; python build.py</automated>
  </verify>
  <acceptance_criteria>
    - MANIFEST.json exists with the four preconditions recorded and its sha256 rows match the on-disk bytes.
    - License + reserved-names notes sit beside each family; the artifact staging includes fonts/.
    - If the fetch was skipped, the fallback decision is recorded in MANIFEST.json with a re-run pointer and no UI-SPEC rule is withdrawn.
  </acceptance_criteria>
  <done>The reading voice is physically shipped, licensed, checksummed, and honest about its fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register @font-face rules in the token layer</name>
  <files>surfaces/presentation.py, tests/presentation_roundtrip.py</files>
  <read_first>
    - `surfaces/presentation.py` - SHARED_CSS and the tokens from plan 03.1-01
    - `fonts/MANIFEST.json` - the vendored file names after Task 1
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` section 2 (token-layer ownership) and 7.4
    - `tests/presentation_roundtrip.py` - the token assertions from plan 03.1-01
  </read_first>
  <behavior>
    - Test 1: SHARED_CSS contains `@font-face` rules whose src references the vendored woff2 paths and whose family names are the token values --font-paper/--font-ledger resolve to.
    - Test 2: when the font files are absent (temp rename), the CSS still resolves to the Georgia/ui-monospace fallback - the rules degrade, nothing references a literal family outside presentation.py.
    - Test 3: no surface template (lesson.py, study.py, day.py, quiz_page.py) contains a font-family literal after this task.
  </behavior>
  <action>
Add `@font-face` rules to `surfaces/presentation.py`'s SHARED_CSS for `--font-paper` (Source Serif 4, src local-file pointing at `fonts/source-serif/...woff2`) and `--font-ledger` (iA Writer Quattro, same pattern), with the family stack already defined in plan 03.1-01 (vendored face preferred, Georgia/ui-monospace fallback). Ship `font-display: swap` and keep the fallback resolution in the token layer only. Add the three behavior tests.
  </action>
  <verify>
    <automated>python tests/presentation_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The @font-face src paths match the on-disk vendored files.
    - The absence-fallback test passes (temporarily renamed fonts still render the fallback stack).
    - The grep across surfaces/ finds no new family literal outside presentation.py.
  </acceptance_criteria>
  <done>The two faces are wired through the one token layer and degrade exactly as specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document the new grammar in spec and fold lesson_layout into 09-02</name>
  <files>surfaces/cli.py, .planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-02-PLAN.md, tests/lesson_roundtrip.py</files>
  <read_first>
    - `surfaces/cli.py` - cmd_spec() (lines 26-55) and the SPEC text it prints
    - `model.py` - the SPEC section (lines 400-491) holding the current directive list
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-02-PLAN.md` - Task 1's action (line ~108) enumerating the entry contract
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-CONTEXT.md` - D-04 and D-24
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` section 14 (settings) and 9
  </read_first>
  <behavior>
    - Test 1: `itembank spec` output contains sections documenting `## TERMS` + `[[term]]` (including the reserved zh= meta), `[!KEY]` (id/hash, cloze, Anki export), `[!EXAMPLE]`, `[!CHECK: <id>]` (same-bank-only), the Educational Objective line (private-payload note), and `styles/<id>.md` (Voice/Rules/Exemplar and the locked house rows).
    - Test 2: `09-02-PLAN.md`'s Task 1 action enumerates `lesson_layout` (`"separate" | "inline"`) in the subject_profiles entry contract with EMT/Math separate and CS inline, and the plan's acceptance criteria mention it.
    - Test 3: the spec documentation round-trips - a bank written from the spec alone parses and lints clean for the new constructs.
  </behavior>
  <action>
Extend `cmd_spec()` (and the SPEC text it prints, keeping the existing directive list unchanged) with the new grammar sections: `## TERMS`/`[[term]]` with the reserved `zh=` meta note (D-24), `[!KEY]` with the [ID:]/[HASH:] minting, {{cloze}}, and Anki export pointer, `[!EXAMPLE]` as a callout kind, `[!CHECK: <id>]` same-bank-only (D-06), the Educational Objective line with the private-payload-only note, and `styles/<id>.md` with the Voice/Rules/Exemplar contract and the locked-house-rows note (styles document, code decides). Fold `lesson_layout` into `09-02-PLAN.md`: Task 1's action enumerates `lesson_layout` enum `"separate" | "inline"` in each entry's lesson object (EMT and Math `separate`, CS `inline`), and the plan's artifacts/acceptance-criteria rows name it. Do not version-bump the registry in this phase (D-04). Write the three behavior tests first.
  </action>
  <verify>
    <automated>python surfaces/cli.py --spec | grep -E "TERMS|!KEY|!EXAMPLE|!CHECK|Educational|styles/" &amp;&amp; python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The spec prints every new construct with its binding notes, and the existing directive list is byte-unchanged outside the added sections.
    - The 09-02 fold is present in the action and acceptance criteria (grep).
    - A spec-only authoring fixture parses and lints clean.
  </acceptance_criteria>
  <done>An authoring agent with no repo context can write the new grammar, and the lesson-layout decision is captured before Phase 9 makes it a migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| vendored font files -> shipped artifact | Third-party binaries enter the product and the artifact |
| spec text -> authoring agent | The contract an agent writes against must not misdescribe the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-031-20 | Tampering | vendored font supply chain | high | mitigate | Immutable tag + recorded SHA-256 recomputed in test + license/reserved-names review per the KaTeX precedent (Directive 4a); fallback stack means a skipped fetch degrades, never blocks |
| T-031-21 | Spoofing | spec contract drift | medium | mitigate | The spec round-trip fixture (a bank written from spec parses and lints clean) closes the gap between documentation and parser |
| T-031-22 | Elevation of Privilege | lesson_layout fold timing | low | mitigate | The 09-02 edit is plan text before execution; once Phase 9 executes the closed schema makes it a migration, which is why the fold lands in this phase (D-04 one-way warning) |
| T-031-SC | Tampering | package operations | high | mitigate | This plan performs no package operation; fonts are fetched from pinned GitHub release URLs only |
</threat_model>

<verification>
- `python tests/presentation_roundtrip.py`
- `python build.py` (fonts/ staged)
- `itembank spec` grep for the new sections
- Grep 09-02-PLAN.md for `lesson_layout`
</verification>

<success_criteria>
- Both typefaces ship pinned, checksummed, licensed, and falling back correctly.
- spec documents every new construct; an agent can author from it.
- lesson_layout is folded into 09-02 before Phase 9 executes.
</success_criteria>

<output>
Create `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-06-SUMMARY.md` when done.
</output>
