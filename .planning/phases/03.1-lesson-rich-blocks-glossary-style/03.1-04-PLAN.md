---
phase: 03.1-lesson-rich-blocks-glossary-style
plan: 04
type: execute
wave: 2
depends_on:
  - 03.1-01
files_modified:
  - model.py
  - styles/house.md
  - styles/expository.md
  - styles/worked-example.md
  - styles/checked-prose.md
  - styles/artifact-first.md
  - styles/case-narrative.md
  - tests/style_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - LESSON-09
  - LESSON-10
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: high
must_haves:
  truths:
  - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run (recorded in each PLAN.md)."
    - "One style file per style at styles/<id>.md carrying a ## Voice prose zone, a ## Rules pipe table, and a ## Exemplar; the pipe table parses with the existing cell splitter, never a second document format (D-09, research R1.3)."
    - "Resolution order is bank-adjacent styles/ → user data dir → bundled house; first match by id wins; a duplicate id across levels is a warning naming both paths, never a silent shadow (D-09)."
    - "Selection precedence is lesson → bank → subject profile → house, with exactly one inheritance level; [STYLE-PARENT:] naming anything but house is lint error style.parent_unknown (D-10)."
    - "Locked house rows are code constants in LOCKED_RULE_IDS, never data read from styles/house.md at runtime; a style may not override, suppress, or re-severity a locked id (ruling 13, research Open Question 1)."
    - "The registry ships five styles in roadmap order — expository (parent), worked-example, checked-prose (predict_first flag), artifact-first, case-narrative — plus the Extensibility Rule 6 stub proving a sixth style is one file with no parser, lint-code, or renderer change (D-07)."
    - "render_style is runtime, model-free, permutes existing blocks only; restyle is Phase 11's model-driven, human-gated rewrite; the five mechanically impossible transforms are refused by name and never approximated (D-11)."
    - "A rendered lesson states its style in one Ledger line: 'style: <id> · rendered by render_style' or 'style: house' (03.1-UI-SPEC §9.6)."
  prohibitions:
    - statement: "A style file may never define a new lint rule or extend the closed check catalogue."
      status: unverified
      flagged: true
    - statement: "render_style must never fabricate content a source lesson does not already contain."
      status: unverified
      flagged: true
---

<objective>
As an author, I want five named pedagogies the linter actually enforces and the reader honestly labels, so a lesson's written voice is a contract rather than a vibe — and adding a sixth style is one file.

Purpose: Build the style registry and its mechanics: the file shape, the resolution and selection order, the locked house rows as code constants, the five shipped styles plus the Rule 6 stub, and the render_style/restyle split with the five refused transforms.
Output: load_style()/resolve_style()/LOCKED_RULE_IDS, the styles/ directory with six files, the style footer, and the render_style command with its refusal copy.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-CONTEXT.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-PATTERNS.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-VALIDATION.md
@.planning/research/2026-08-10-lesson-style-catalogue.md
@.planning/research/2026-08-10-style-registry-mechanics.md

<interfaces>
- `model.LINT_CODES` is the closed catalogue; the style pass registers parameterization, never new codes.
- `model.lint()` gains an optional style pass behind a `STYLE_UNCHECKED` sentinel, same additive shape as `LESSON_UNCHECKED`.
- `surfaces.lesson.lesson_page()` renders the style footer from the resolved style id.
- `surfaces/cli.py` gains the `render_style` command (route/CLI parity: a `POST /render-style` route if a route is warranted, else the CLI-only seam with the refusal copy).
- The `styles/` directory ships bundled with the artifact via `build.py`'s allowlist (`STAGE_DIRS`).
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `model.py` | `load_style(style_id, base)` (parses Voice/Rules/Exemplar), `resolve_style(qs, bank_path, settings)` (lesson → bank → subject profile → house), `LOCKED_RULE_IDS` frozenset, `STYLE_RULE_KINDS` closed set, lint codes `style.parent_unknown`, `style.rule_unimplemented`, `style.duplicate_id`, `style.file_unreadable`, `style.override_locked`, `style.ignore_locked` |
| `styles/house.md` | Bundled read-only house style: documents the locked rows and the house rules |
| `styles/expository.md` | The parent style every other style inherits from |
| `styles/worked-example.md` | Worked-example style (structure: worked examples precede variations) |
| `styles/checked-prose.md` | Checked-prose style with the `predict_first` flag |
| `styles/artifact-first.md` | Bottom-Up ordering + PRIMM pacing (D-03 assumption, falsify-first task) |
| `styles/case-narrative.md` | Case-narrative style (depends on Phase 9's ## SCENARIO) |
| `surfaces/lesson.py` | Style footer line (`style: <id> · rendered by render_style` / `style: house`) |
| `surfaces/cli.py` | `render_style` command with the five named refusals |
| `tests/style_roundtrip.py` | Registry, resolution, lock, and Rule 6 stub fixtures |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the style loader, resolution order, and LOCKED_RULE_IDS</name>
  <files>model.py, tests/style_roundtrip.py</files>
  <read_first>
    - `model.py` — LINT_CODES (lines 578-590), lesson_slug(), the directive-grab idiom, parse_lesson()
    - `surfaces/lesson.py` — _split_cells()/_is_separator_row() (lines 153-163) to reuse for the ## Rules table
    - `surfaces/settings.py` — load_settings()/base-dir conventions for the user data dir resolution
    - `tests/style_roundtrip.py` — to be created; follow the fail() harness in tests/lesson_roundtrip.py
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md` — Pattern 2 (LOCKED_RULE_IDS), Pitfall 3 (rule-kind allowlist), Open Question 1
    - `.planning/research/2026-08-10-style-registry-mechanics.md` — registry shape and precedence
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §9.6
  </read_first>
  <behavior>
    - Test 1: a style file's ## Voice zone, ## Rules pipe table, and ## Exemplar parse into a dict; a row claiming a kind outside the closed STYLE_RULE_KINDS set is lint error style.rule_unimplemented (D-16).
    - Test 2: resolution order is bank-adjacent styles/ → user data dir → bundled house, first match wins, and a duplicate id across levels produces a warning naming both paths.
    - Test 3: [STYLE-PARENT:] naming anything but house is style.parent_unknown; inheritance applies exactly one level.
    - Test 4: LOCKED_RULE_IDS is a code constant (frozenset in model.py, never read from styles/house.md), and a child row naming a locked id at any severity is style.override_locked.
  </behavior>
  <action>
Implement `load_style(style_id, base)` in `model.py`: read `styles/<id>.md` from bank-adjacent `styles/`, then the user data dir, then the bundled `styles/` directory (first match wins per D-09); return None on absence. Parse the `## Voice` prose zone (never sent to any model — D-17), the `## Rules` pipe table via the existing cell-splitter shape (import from surfaces.lesson — never reimplement), and the `## Exemplar` block. Define the closed `STYLE_RULE_KINDS` set (order.before, density.max, style.require, style.forbid, cadence.section, open.with, plus the house-only locked kinds); a row whose kind is not in the set is `style.rule_unimplemented` before the rule is ever applied (D-16, Pitfall 3). Define `LOCKED_RULE_IDS = frozenset({...})` in model.py encoding the five Directive §4 non-negotiables as rule ids (e.g. the no-second-parser, no-second-scorer, no-evidence-leave, additive-format, and accessibility-gate rows) — the house file documents them but never defines them. Implement `resolve_style(qs, bank_path, settings)` applying the locked precedence lesson → bank → subject profile → house with exactly one inheritance level; `[STYLE-PARENT:]` naming anything but `house` is `style.parent_unknown`; any child row naming a locked id is `style.override_locked` regardless of severity, including `off`. Add the codes to `LINT_CODES`. Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/style_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - load_style/resolve_style return the documented shapes; resolution order and first-match-wins are fixture-proven.
    - Every rule-kind row outside the closed set fires style.rule_unimplemented; locked-row overrides fire style.override_locked.
    - `LOCKED_RULE_IDS` is a literal frozenset in model.py (AST/grep fixture), and no style file can alter it.
  </acceptance_criteria>
  <done>The registry mechanics are real code with a closed, code-owned lock; the house file documents but cannot decide.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship the five styles plus the Rule 6 stub</name>
  <files>styles/house.md, styles/expository.md, styles/worked-example.md, styles/checked-prose.md, styles/artifact-first.md, styles/case-narrative.md, tests/style_roundtrip.py</files>
  <read_first>
    - `.planning/research/2026-08-10-lesson-style-catalogue.md` — the five styles and the four rejections with reasons (a plan may not revive a rejected style)
    - `.planning/research/2026-08-10-style-registry-mechanics.md` — file shape and the lock column
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §9.6 (footer) and §17 item 8 (Rule 6 stub)
    - `styles/` (currently absent) — the bundled house directory the loader resolves last
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-CONTEXT.md` — D-03 (artifact-first assumption), D-07, D-08
  </read_first>
  <behavior>
    - Test 1: all five style files load through load_style() with valid ## Rules rows whose kinds are all in STYLE_RULE_KINDS (D-16 passes).
    - Test 2: expository declares no parent and the other four declare `[STYLE-PARENT: expository]`; checked-prose carries the predict_first flag; house.md documents every LOCKED_RULE_IDS id.
    - Test 3: the Rule 6 stub — a throwaway sixth style file added to a temp styles dir loads with no parser, lint-code, or renderer change, proving Extensibility Rule 6.
    - Test 4: the artifact's bundled styles/ directory is included in build.py's STAGE_DIRS allowlist.
  </behavior>
  <action>
Author the six bundled style files per the catalogue and mechanics research: `house.md` (read-only, documents the locked rows and the house-wide rules — the Diátaxis split, the five non-negotiables rows), `expository.md` (the parent, cost zero, inheriting house), `worked-example.md`, `checked-prose.md` (carrying `predict_first` as a flag, not a second style — D-08), `artifact-first.md` (Bottom-Up ordering plus PRIMM pacing as orthogonal, composable rules — D-03), and `case-narrative.md` (declaring its Phase 9 `## SCENARIO` dependency in a comment and in its rules, last per D-07). Each file carries `## Voice` (prose zone, human-only), `## Rules` (pipe table with id/kind/severity/lock columns using only STYLE_RULE_KINDS values), and `## Exemplar`. Do not revive a rejected style (Feynman, cookbook, written Socratic, explorable explanations) — the four rejections with reasons are binding. Add `styles/` to `build.py`'s `STAGE_DIRS` allowlist so the bundled house ships in the artifact. Write the four behavior tests first; the Rule 6 stub test creates a temp sixth style and asserts load + lint + render paths are unchanged.
  </action>
  <verify>
    <automated>python tests/style_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - All five styles load and lint clean under D-16; expository is the declared parent.
    - The Rule 6 stub passes with no code change outside the temp style file.
    - `python build.py` includes styles/ in the staged artifact (assert via the build manifest or STAGE_DIRS).
  </acceptance_criteria>
  <done>Five pedagogies ship as enforced contracts, and a sixth is one file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: render_style, the style footer, and the named refusals</name>
  <files>surfaces/lesson.py, surfaces/cli.py, tests/style_roundtrip.py, tests/lesson_roundtrip.py</files>
  <read_first>
    - `surfaces/lesson.py` — render_markdown()/lesson_page() and the plan 03.1-01 callout branch (render_style permutes blocks that already exist)
    - `surfaces/cli.py` — command registration and ROUTE_CLI parity
    - `tests/lesson_roundtrip.py` — render fixtures
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §9.6 (footer copy and refusal copy), §15
    - `.planning/research/2026-08-10-style-registry-mechanics.md` — the render/restyle split and the five impossible transforms
  </read_first>
  <behavior>
    - Test 1: a lesson page rendered with a resolved style carries the footer line `style: <id> · rendered by render_style`; a lesson with no style file carries `style: house`.
    - Test 2: `render_style <bank> --style <id>` permutes only existing blocks (reorders sections/blocks per the style's rules) and refuses each of the five named transforms with the exact copy `render_style cannot turn <from> into <to>; that is a rewrite, not a rearrangement. No file was changed.` — no file is written on refusal.
    - Test 3: render_style's call graph contains no model client — structurally proven by signature and import audit.
    - Test 4: an unreadable/missing style file renders the degraded `--warn` copy echoing the author-written id and leaves the reason to lint.
  </behavior>
  <action>
Implement `render_style(bank_path, style_id, out=None)` in `surfaces/lesson.py` as a runtime, model-free permutation of the blocks a lesson already contains (section order and block arrangement per the resolved style's order.before/cadence.section rules), with a signature that cannot accept a model client (no adapter import; add an AST/import audit fixture). Register `itembank render_style <bank> --style <id> [--out <path>]` in `surfaces/cli.py` and ROUTE_CLI. Implement the five named refusals from D-11 with the exact copy: expository→case-narrative, expository→Socratic, expository→worked-example, anything→Bottom-Up, and case-narrative→anything — refused by name, no approximation, no file written. Thread the resolved style into `lesson_page()` so the footer renders as `style: <id> · rendered by render_style` (or `style: house` when no style file resolves); an unreadable or missing style file renders the degraded `--warn` copy echoing the author-written id or path, never a resolved absolute path, per §9.6. Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/style_roundtrip.py &amp;&amp; python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The footer strings match §9.6/§15 verbatim, asserted by fixture.
    - All five refusals fire with the exact copy and write nothing.
    - The import audit proves render_style has no model dependency.
  </acceptance_criteria>
  <done>Rearrangement is honest, labeled, and model-free; rewrites are named refusals, not silent approximations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| style file (model-writable) -> lint/render policy | A governance file an authoring model can write must not demote the runtime's guarantees |
| render_style -> output file | A permutation must never fabricate or destroy content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-031-12 | Elevation of Privilege | style file overriding locked rules | high | mitigate | LOCKED_RULE_IDS is a code constant; override/suppress/re-severity of a locked id is style.override_locked / style.ignore_locked, structurally impossible to launder through a style file (ruling 13, Open Question 1) |
| T-031-13 | Tampering | style file path resolution | medium | mitigate | Resolution walks the three named dirs in order with first-match-wins; duplicate ids warn naming both paths; the bank stem and style id resolve through the existing base-dir conventions (no new path traversal surface — style ids slugify through lesson_slug) |
| T-031-14 | Tampering | render_style content fabrication | high | mitigate | render_style permutes existing blocks only, refuses the five impossible transforms by name, and writes nothing on refusal; the import-audit fixture keeps the model out of the call graph (Directive §4.1) |
| T-031-15 | Injection | style ## Voice / ## Rules text into rendered page | high | mitigate | Voice and Exemplar zones are never interpolated into the learner page unescaped (the footer renders only the slugified style id); lint messages escape author text per existing discipline |
| T-031-SC | Tampering | bundled styles in the artifact | high | mitigate | styles/ enters the artifact only through build.py's STAGE_DIRS allowlist; bundled house styles are read-only at runtime |
</threat_model>

<verification>
- `python tests/style_roundtrip.py`
- `python tests/lesson_roundtrip.py`
- `python build.py` (artifact staging includes styles/)
- AST/import audit: render_style's module imports contain no model-adapter module.
</verification>

<success_criteria>
- Five styles + house ship and load; the Rule 6 stub proves one-file extensibility.
- Locked rows are code constants; overrides, suppressions, and re-severities of locked ids are lint errors.
- render_style permutes, labels, and refuses; restyle remains Phase 11's model-driven, human-gated rewrite.
</success_criteria>

<output>
Create `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-04-SUMMARY.md` when done.
</output>
