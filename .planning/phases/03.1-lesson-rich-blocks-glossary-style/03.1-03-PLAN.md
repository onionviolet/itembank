---
phase: 03.1-lesson-rich-blocks-glossary-style
plan: 03
type: execute
wave: 2
depends_on:
  - 03.1-01
  - 03.1-02
files_modified:
  - model.py
  - evidence.py
  - runtime.py
  - surfaces/lesson.py
  - surfaces/anki.py
  - surfaces/daemon.py
  - surfaces/cli.py
  - schemas/response.schema.json
  - tests/lesson_roundtrip.py
  - tests/anki_keys_roundtrip.py
  - tests/protocol_roundtrip.py
autonomous: true
ui_classification: READING-SURFACE
requirements:
  - LESSON-08
  - LESSON-13
  - LESSON-15
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: high
must_haves:
  truths:
  - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run (recorded in each PLAN.md)."
    - "A [!KEY] callout is lesson-only: a [!KEY] marker inside an item rationale is lint error key.in_rationale (D-05)."
    - "[!KEY] ids are minted through the exact new_item_id()/taken-set call path items use — there is no separate key id namespace (research Pitfall 5)."
    - "The Anki keys export writes a multi-directive TSV with a #guid column whose value round-trips on re-export: export once, edit the block body keeping the [ID:] unchanged, export again, and the guid for that block is byte-identical (D-22)."
    - "{{cloze}} markers render as their enclosed text on screen and compile to Anki native {{c<n>::...}} for the Cloze notetype; a title-bearing body without cloze uses Basic; a body with neither is lint error key.no_front (research Open Question 7)."
    - "key_review is a new additive event type carrying no score, appended to KNOWN_EVENT_TYPES with a builder mirroring the existing shapes (D-19); Phase 10 replays it into scheduler state."
    - "The Educational Objective line is part of explain_payload() only, never public_item() or any pre-answer surface, rendered after the verdict with the Ledger label 'Educational objective'. The existing [OBJECTIVE:] syllabus-reference field is today leaked pre-answer via public_item() (runtime.py:30) and must be moved to explain_payload() in this plan to close C7 (03.1-UI-SPEC §9.5, LESSON-15)."
    - "The 'Add to review' control is a real <form method="post">; without the runtime it is absent with the stated reason, never a dead disabled button (03.1-UI-SPEC §9.2, §15)."
  prohibitions:
    - statement: "No streak, count-up, celebration, or target language may appear on a [!KEY] card or review action."
      status: unverified
      flagged: true
    - statement: "The objective line must not appear in any pre-answer HTML, JSON, ARIA, CSS-off text, or accessible name."
      status: unverified
      flagged: true
---

<objective>
As a learner, I want the things a lesson says to memorize to be visibly set apart, exportable to Anki without ever losing their identity, and reviewable — with the item's educational objective shown only after I have answered.

Purpose: Build the [!KEY] track end-to-end: parse + id/hash minting, the index-card render, the Anki keys export with a round-tripping guid, the key_review event, and the Educational Objective line in the private payload.
Output: parse_key_blocks()/key id minting in the id-assign walk, key card rendering, `itembank export keys`, key_review events, the /key/<id>/review route, and explain_payload()'s objective line.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-CONTEXT.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-PATTERNS.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-VALIDATION.md

<interfaces>
- `model.assign_ids()` and `new_item_id()` are the one id-minting path; key ids thread the same taken set.
- `surfaces.anki.cmd_export(a)` keeps its existing Basic/Cloze item path; the keys path is a new function, not a patch to the item loop.
- `runtime.explain_payload()` gains the objective line; `public_item()` is untouched.
- `evidence.append_event()` remains the one writer; key_review events flow through it.
- The daemon route table gains `POST /key/<id>/review` with a CLI twin (`itembank key-review` or an `export`-adjacent command registered in ROUTE_CLI).
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `model.py` | `parse_key_blocks(bank_path)` (list of key dicts: id, hash, title, body, cloze flag, section slug), key id minting inside `assign_ids()`'s walk, lint codes `key.no_front`, `key.missing_id`, `key.missing_hash`, `key.duplicate_id`, `key.in_rationale` |
| `surfaces/anki.py` | `cmd_export_keys(a)` / `export_keys(bank)` writing `<bank>_keys.tsv` with `#guid column:1`, `#notetype column:2`, `#deck column:3`, `#tags column:6`, `#separator:tab`, `#html:true` |
| `surfaces/lesson.py` | `[!KEY]` index-card markup (callout-key), `?print=drill` cloze-blanking pass and `Answers` list, `{{cloze}}` on-screen substitution |
| `surfaces/daemon.py` | `POST /key/<id>/review` handler recording key_review and returning `Added to review.` |
| `surfaces/cli.py` | `export keys` subcommand and the review CLI twin, both registered in ROUTE_CLI |
| `evidence.py` | `key_review_event(...)` builder; `"key_review"` in `KNOWN_EVENT_TYPES`; schema $def |
| `runtime.py` | `explain_payload()` objective line (Ledger label `Educational objective` + Paper sentence) |
| `tests/anki_keys_roundtrip.py` | Export, guid round-trip, cloze/Basic derivation, and force-refusal fixtures |
| `tests/protocol_roundtrip.py` | Objective absent-from-public / present-in-explain fixture |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse [!KEY] blocks, mint shared-namespace ids, extend lint</name>
  <files>model.py, tests/lesson_roundtrip.py</files>
  <read_first>
    - `model.py` — parse_lesson() (lines 167-256), new_item_id() (lines 297-305), assign_ids() (lines 313-394), LINT_CODES (lines 578-590), lint() (lines 593-650), grab()
    - `tests/lesson_roundtrip.py` — fixture conventions
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md` — Pitfall 5 (id namespace) and the [LESSON-SRC:] grab idiom
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §9.2
  </read_first>
  <behavior>
    - Test 1: a lesson containing `> [!KEY]` blocks parses each into {id, hash, title, body, cloze, section_slug}; a lesson with none returns an empty list; parse_bank output is byte-identical for a bank with no key blocks.
    - Test 2: `itembank id-assign` mints `[!KEY]` ids through the same taken set as item ids — a fixture where an item already holds an id proves the key never collides, and no separate key-id function exists.
    - Test 3: lint fires `key.no_front` for a body with neither title nor cloze, `key.missing_id`/`key.missing_hash` for unminted blocks, `key.duplicate_id` across blocks, and `key.in_rationale` for a `[!KEY]` marker inside an item rationale.
  </behavior>
  <action>
Implement `parse_key_blocks(bank_path)` in `model.py` as an independent reader over the lesson body: walk the `## LESSON` region (reusing parse_lesson's heading walk), and for each `> [!KEY]` callout collect the block's `[ID:]`/`[HASH:]` directive lines (grab idiom, same shape as `[LESSON-SRC:]`), its first line as title (when present), and its body; set `cloze=True` when the body contains `{{...}}`/`{{n::...}}` markers. Extend `assign_ids()`'s walk so every key block without an `[ID:]` gets one from `new_item_id()` through the exact taken set the item walk builds (never a separate `new_key_id()`), and its `[HASH:]` is the sha256 of the block body via the same change-detection discipline evidence.py documents. Extend `lint()` (behind the TERMS/key pass already added in plan 03.1-02) with `key.no_front`, `key.missing_id`, `key.missing_hash`, `key.duplicate_id`, and `key.in_rationale`; add the codes to `LINT_CODES`. Write the behavior tests first.
  </action>
  <verify>
    <automated>python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Key blocks parse with the exact field set; ids mint through the shared taken set (grep proves no second id function).
    - All five key lint codes fire on their fixtures and exist in `LINT_CODES`.
    - A bank with no key blocks parses byte-identically.
  </acceptance_criteria>
  <done>Every [!KEY] is a real, id'd, lint-checked lesson block sharing the item identity discipline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the Anki keys export with a round-tripping #guid</name>
  <files>surfaces/anki.py, surfaces/cli.py, tests/anki_keys_roundtrip.py</files>
  <read_first>
    - `surfaces/anki.py` — the full 47-line file: cmd_export(), tsv_cell(), the header/row shapes
    - `surfaces/cli.py` — the export parser (lines 277-285) and ROUTE_CLI table
    - `tests/anki_keys_roundtrip.py` — to be created following the repo's no-framework harness
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md` — Open Question 7 (exact #guid mechanics)
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §9.2, §15 (copy contract strings)
  </read_first>
  <behavior>
    - Test 1: `itembank export keys <bank>` writes `<bank>_keys.tsv` with the exact header lines `#separator:tab`, `#html:true`, `#guid column:1`, `#notetype column:2`, `#deck column:3`, `#tags column:6`, and one row per key block.
    - Test 2: round-trip — export, mutate a key body keeping its `[ID:]`, export again; the second file's guid column for that block is byte-identical to the first.
    - Test 3: a cloze body compiles to `{{c1::...}}`-style native cloze text; a titled non-cloze body uses Basic with Front/Back/Tags columns; a body with neither is refused with lint error `key.no_front`.
    - Test 4: a bank with lint errors refuses the keys export without `--force`, matching the item export's existing behavior.
  </behavior>
  <action>
Add `export_keys(bank)` and wire `cmd_export(a)` to dispatch `--format keys` (or an `itembank export keys <bank>` subcommand) to it. The new path calls `parse_lesson()` + `parse_key_blocks()` — never `load()`/`parse_bank()` — lints the key blocks (refusing on errors without `--force`), and writes `<bank>_keys.tsv` with the multi-directive header: `#separator:tab`, `#html:true`, `#guid column:1`, `#notetype column:2`, `#deck column:3`, `#tags column:6`. Column 1 is the block's `[ID:]`; column 2 is `Cloze` when the body carries `{{...}}` (compiled to Anki's native `{{c<n>::...}}` numbering, `{{cloze}}` → `{{c1::...}}`) or `Basic` for a titled body; a body with neither is lint error `key.no_front` and refuses export. Deck defaults to the bank stem; tags column contains `itembank` plus the lesson slug. The front/back derive deterministically from title/body; the basic front carries the title and the back carries the body with `{{cloze}}` markers rendered as their enclosed text. Register the export flag in `surfaces/cli.py` and ROUTE_CLI parity. Write the four behavior tests first in `tests/anki_keys_roundtrip.py`.
  </action>
  <verify>
    <automated>python tests/anki_keys_roundtrip.py &amp;&amp; python tests/gift_export_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The TSV header and row shapes match the four directives exactly.
    - The guid round-trip fixture passes (byte-identical guid after body edit).
    - Cloze/Basic derivation and the no-front refusal match the spec, and existing item export tests stay green.
  </acceptance_criteria>
  <done>A learner's [!KEY] cards cross into Anki as updates, never duplicates, with their identity intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the key card, record key_review, and add the objective line</name>
  <files>surfaces/lesson.py, surfaces/daemon.py, surfaces/cli.py, evidence.py, runtime.py, schemas/response.schema.json, tests/lesson_roundtrip.py, tests/evidence_roundtrip.py, tests/protocol_roundtrip.py</files>
  <read_first>
    - `surfaces/lesson.py` — the callout container from plan 03.1-01, LESSON_CSS
    - `surfaces/daemon.py` — route table and the authority-gate pattern for mutating routes (loopback-only writes)
    - `evidence.py` — the builder shapes from plan 03.1-02 and response_event()
    - `runtime.py` — explain_payload() (where the objective line lands) and public_item()
    - `surfaces/study.py` — how rationale fields render today (esc() calls, lines 158-173) — the objective line follows this, not the lesson markdown renderer
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §9.2 (markup), §9.5 (objective), §10.2 (print/drill), §15 (copy), §17 items 1, 5
  </read_first>
  <behavior>
    - Test 1: a `[!KEY]` block renders as `<section class="callout callout-key" id="key-<slug>">` with label `Key point`, the Ledger footer `key: <id> · exports to Anki`, `{{cloze}}` shown as its enclosed text, and a real `<form method="post" action="/key/<id>/review">` with submit `Add to review`; with the daemon stopped, the control is absent and the exact copy `Review scheduling is unavailable without the runtime. Run itembank export anki to take this key to Anki.` renders instead.
    - Test 2: POST /key/<id>/review records a key_review event and the status region announces `Added to review.` once; focus stays on the submitted control.
    - Test 3: `?print=drill` blanks cloze markers, prints the `Answers` list at the end, and shows no answer above it.
    - Test 4: explain_payload() includes `educational_objective` (Ledger label `Educational objective`) after the verdict; public_item() and every pre-answer surface lack it entirely.
  </behavior>
  <action>
Extend the lesson renderer's callout-key kind to the full card per §9.2: the id anchor `key-<slug>`, the Ledger footer `key: <id> · exports to Anki` (omitted when the block has no id — with the card still rendering label + body, per the §16 empty-state rule), the `Add to review` form posting to `/key/<id>/review`, and on-screen `{{cloze}}` substitution to its enclosed text. When the daemon is absent (static render), omit the form and render the exact unavailable copy instead of a disabled control. Add `POST /key/<id>/review` to the daemon with the existing loopback authority gate, recording `key_review_event(session_id, bank, key_id, mode, ts)` (append `"key_review"` to `KNOWN_EVENT_TYPES` + schema $def, no score key) and returning the `Added to review.` status string; register the CLI twin in ROUTE_CLI. Add the `?print=drill` second-stylesheet pass: blank `{{...}}` content server-side, print an `Answers` list of the keyed bodies at the end. Add the Educational Objective line: extend `model.parse_question()` to capture an optional one-sentence `Objective:` line per item (the existing `item.objective_unnamespaced` lint already guards the subject-prefix form), extend `lint()` to check the line is at most one sentence when present, and render it inside `explain_payload()` after the verdict with the Ledger small-caps label `Educational objective` and the Paper sentence (C7 note: the existing [OBJECTIVE:] syllabus-reference field, model.py:55, is today emitted pre-answer by public_item() at runtime.py:30 and must be moved into explain_payload() in this task; verify no downstream consumer of public_item() depends on the pre-answer objective field - session/report code reads it from evidence data) — never in `public_item()` (03.1-UI-SPEC §9.5, C7). Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/lesson_roundtrip.py &amp;&amp; python tests/evidence_roundtrip.py &amp;&amp; python tests/protocol_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The key card markup, footer, degraded copy, and review form match §9.2/§15 verbatim, asserted by fixture.
    - key_review events append and read back; the schema validator passes.
    - The objective-line fixture proves absence from public_item()/pre-answer HTML and presence in explain_payload().
    - The drill-print fixture shows no answer above the `Answers` list.
  </acceptance_criteria>
  <done>Key points are memorable on screen, portable to Anki, reviewable in-session, and never leak their objective before the learner answers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| bank-authored [!KEY] text -> learner HTML / TSV | Untrusted author text becomes learner-facing content and an importable file |
| explain_payload vs public_item | Answer-adjacent material must stay behind the verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-031-08 | Information Disclosure | Educational Objective in pre-answer surfaces | high | mitigate | Objective renders only inside explain_payload(); the C7 fixture asserts absence across HTML, JSON, ARIA, CSS-off text, and accessible names (03.1-UI-SPEC §17 item 1) |
| T-031-09 | Tampering | key id/hash namespace | high | mitigate | Key ids mint through the shared new_item_id()/taken set; a collision between a key and an item is structurally impossible (research Pitfall 5) |
| T-031-10 | Injection | key body into TSV/HTML | high | mitigate | tsv_cell() collapses and strips tabs for the TSV path; `_inline()`/`html.escape()` for the render path; the stored-XSS fixture from plan 03.1-01 covers the shared callout branch |
| T-031-11 | Spoofing | /key/<id>/review | medium | mitigate | The route resolves the key id against the bank's parsed key blocks, applies the loopback-only authority gate, and records the event with bank + id provenance |
| T-031-SC | Tampering | vendored assets | high | mitigate | This plan performs no package operation; the export writes files only and imports nothing external |
</threat_model>

<verification>
- `python tests/lesson_roundtrip.py`
- `python tests/anki_keys_roundtrip.py`
- `python tests/evidence_roundtrip.py`
- `python tests/protocol_roundtrip.py`
- `python schema_validate.py --all`
</verification>

<success_criteria>
- [!KEY] cards render with the locked markup and copy; the degraded control rule holds.
- The keys TSV exports with a round-tripping guid and correct cloze/Basic derivation.
- key_review events record; the objective line is private until the verdict.
</success_criteria>

<output>
Create `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-03-SUMMARY.md` when done.
</output>
