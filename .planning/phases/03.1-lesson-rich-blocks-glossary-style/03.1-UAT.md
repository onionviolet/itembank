---
status: partial
phase: 03.1-lesson-rich-blocks-glossary-style
source: [03.1-VERIFICATION.md]
started: 2026-08-11
updated: 2026-08-11
---

## Current Test

[automated fixtures complete and present on disk; live execution pending]

## Tests

### 1. Author a lesson with the new grammar and read it back
expected: A lesson using `## TERMS`/`[[term]]` (with `zh=` meta), `> [!KEY: <title>]` (in-bracket title), `[!EXAMPLE]`, `[!CHECK: <id>]` (same-bank-only), the Educational Objective line, and a `styles/<id>.md` voice parses and lints clean; `itembank spec` documents every construct additively.
result: pass (static)
source: automated (fixture presence) / live run blocked
evidence: "parse_key_blocks in-bracket fix at model.py:890-894; spec sections at surfaces/cli.py:47-101; grammar fixtures present in tests/lesson_roundtrip.py (2708-3175), tests/style_roundtrip.py (23 fixtures), tests/anki_keys_roundtrip.py. Live `python tests/lesson_roundtrip.py` blocked by the session approval gate."

### 2. Gloss leak-proofing and degradation
expected: A suppressed term (definition leaks a keyed answer) renders as bare text with no trigger/aria-details/glossary dt; the held line appears exactly once; the exact unavailable copy renders when the runtime is absent.
result: pass (static)
source: automated (fixture presence) / live run blocked
evidence: "tests/lesson_roundtrip.py::test_gloss_triggers_panels_and_no_leak, test_key_card_renders_runtime_and_degraded, test_gloss_route_and_cli_twin present (lines 2738, 3073, 2852). Live run blocked."

### 3. Byte-identity compatibility floor
expected: A bank/lesson using none of the new constructs renders byte-identically to Phase 3 output; the shared style-block exclusion is named and 03.1-UI-SPEC section 12 cited.
result: pass (static)
source: automated (fixture presence) / live run blocked
evidence: "fixtures/lesson_golden_phase3_content.txt + lesson_golden_phase3_parse.json present; test_lesson_content_region_byte_identical_phase3 (lesson_roundtrip.py:1078) + test_lesson_parse_identity_phase3 (:1093); GATES.md gate 4 records the exclusion + citation."

### 4. Style enforcement on a real lesson
expected: The three cost classes fire; the closed catalogue's severity ceiling holds; a style error blocks a machine-authored write and never a human's lint; suppressions count; locked ids cannot be suppressed; the pass stays under 50ms on 5000 words.
result: pass (static)
source: automated (fixture presence) / live run blocked
evidence: "model.py run_style_pass/apply_style_ignore/write_allowed; 23 style_roundtrip.py fixtures incl. test_50ms_budget_on_5000_word_lesson, test_write_allowed_blocks_machine_write_never_human_lint, test_locked_suppression_is_itself_an_error (present). Live run blocked."

### 5. Authoring-model style contract
expected: prompt_context emits capped imperatives + exactly one exemplar, last, with no Voice prose; the cap is a settings value (default 7) that round-trips through `itembank config set`/`config`.
result: pass (static)
source: automated (fixture presence) / live run blocked
evidence: "StylePrompt.prompt_context (model.py:2187-2260); style settings group (surfaces/settings.py STYLE_SETTINGS_DEFAULTS, schemas/settings.schema.json:337-352, itembank.json:35-38); fixtures test_prompt_context_capped_imperatives_one_exemplar_no_voice, test_cap_is_a_settings_value, test_style_settings_group_validates_and_shows_in_config present."

### 6. Vendored-font supply chain and rendering
expected: MANIFEST sha256 rows match the on-disk bytes; licenses + reserved-names notes sit beside each family; @font-face rules reference the vendored woff2; absence degrades to Georgia/ui-monospace; no surface names a family literal; build stages fonts/.
result: pass (static for hashes + structure)
source: automated (hash recompute this session; fixture presence) / live run + ClearType render blocked
evidence: "All 4 woff2 hash-verified this session (size/sha256/git-blob-sha1 ALL_OK); presentation_roundtrip.py fixtures test_font_sha256_matches_bytes_or_defers_honestly, test_at_font_face_rules_in_token_layer, test_font_faces_degrade_when_fonts_absent, test_no_family_literal_in_surface_templates present; build.py STAGE_DIRS includes fonts/. ClearType 18px render -> human."

### 7. Requirement-coverage audit and full suite
expected: tests/phase_031_audit.py exits 0 with 11/11 LESSON-07..17 covered; the full suite and schema_validate.py --all pass.
result: partial
source: automated (audit inputs statically verified) / live run blocked
evidence: "Audit inputs verified statically this session: 11/11 IDs covered across plans 01-07, every owner names a verification, visible-skip sentence present in all 7 PLAN files, 15/15 GATES-claimed fixtures present. Live `for f in tests/*_roundtrip.py ...` + `schema_validate.py --all` blocked by the approval gate — recorded as required actions in 03.1-GATES.md and HANDOFF-PHASE03.1.md (no green claim made)."

## Summary

total: 7
passed: 6 (static: fixture presence + hashes + audit inputs)
blocked: 1 (full-suite live execution — approval gate; ClearType/browser items additionally human-verify)
