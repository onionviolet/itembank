# API Coverage — Phase 3.1

No external API integration: this phase extends the markdown parser, linter, evidence store, and local renderer only; the two vendored assets are font files fetched from pinned GitHub release URLs at build time, not an API integration surface.
