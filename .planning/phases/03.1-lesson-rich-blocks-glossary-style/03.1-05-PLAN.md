---
phase: 03.1-lesson-rich-blocks-glossary-style
plan: 05
type: execute
wave: 2
depends_on:
  - 03.1-04
files_modified:
  - model.py
  - surfaces/settings.py
  - schemas/settings.schema.json
  - itembank.json
  - tests/style_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - LESSON-11
  - LESSON-12
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "Style enforcement runs as three cost classes on every lint: structural counts over the parsed heading tree and one shared lexical metrics pass run always; discourse judgement defers to Phase 11 (D-12, criterion 3c)."
    - "The check catalogue is closed: a style file may enable, disable, re-severity (downward only), and parameterize a check, but may never define one; error severity is earned by construction — structural counts or author-controlled literal lists only — and a style may not raise a check to error above the catalogue's rating (D-13)."
    - "Local <!-- style-ignore: --> suppression exists from day one and cannot suppress a locked rule (attempting it is itself style.ignore_locked); suppression counts are a report that retires bad checks (D-14, ruling 13)."
    - "Every warning ships with a false-positive rate recorded against the Phase 3.2 corpus when it exists; above roughly 20% it ships disabled by default (D-14)."
    - "The whole style pass stays under 50ms on a 5000-word lesson, asserted by a time.perf_counter()-based test — reasoned, not benchmarked, so the test is written first (D-12, research Open Question 6)."
    - "A style error blocks a machine-authored write and never a human's lint; a human always gets the diagnosis and keeps the pen (D-15)."
    - "The authoring model receives distilled imperatives capped at a configurable setting defaulting to 7, placed last in the prompt, plus exactly one exemplar; it never receives the style file or the ## Voice prose zone (D-17, criterion 3d)."
  prohibitions:
    - statement: "The style enforcement pass must never be silently disabled by a malformed style file or an unsupported rule-kind row."
      status: unverified
      flagged: true
    - statement: "The authoring prompt must never contain the style file's Voice prose or more than the capped imperative count."
      status: unverified
      flagged: true
---

<objective>
As an author, I want the style contract actually checked — cheaply, closed-catalogue, suppressible without breaking the lock — and the authoring model pointed at distilled imperatives rather than a voice essay, so adherence is measured rather than hoped for.

Purpose: Build the enforcement and authoring-prompt halves of the style system: the three cost classes, the closed catalogue semantics, suppression with counting, the 50ms budget test, and the StylePrompt compiler with the capped imperative set.
Output: The style lint pass in model.py, STYLE_RULE_KINDS-driven enforcement with style.* codes, the suppression report, the perf budget test, and prompt_context() consumed by Phase 3.2/11 authoring loops.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-CONTEXT.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-PATTERNS.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-VALIDATION.md
@.planning/research/2026-08-10-enforcement-and-loose-threads.md

<interfaces>
- `model.lint()` is the one gate every write passes through; the style pass threads behind the STYLE_UNCHECKED sentinel from plan 03.1-04.
- `LINT_CODES` remains the closed catalogue; every style.* code lands in it additively.
- `surfaces.settings` gains the `style` settings group (imperative cap default 7) with a schema row and checked-in default, following the SETTINGS_CODES/validate-then-write precedent.
- `StylePrompt.prompt_context(style, cap)` is a pure function — consumed by Phase 3.2/11 authoring loops, never by this phase's own UI.
- The 50ms budget test follows the repo's fail() harness and reuses the plan 03.1-04 fixture lesson.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `model.py` | `run_style_pass(lesson, style)` (structural + lexical), `apply_style_ignore()`, `style_suppression_report()`, style lint codes (`style.*`), the pre-compiled regex constants for the lexical pass, the 50ms-budget test hook |
| `surfaces/settings.py` | `style` settings group: `imperative_cap` (default 7), `warn_fp_threshold` (default 0.20), reader settings from plan 03.1-02 if not yet registered |
| `schemas/settings.schema.json` | `style` group schema rows with defaults |
| `itembank.json` | Checked-in style defaults |
| `tests/style_roundtrip.py` | Enforcement, suppression, severity-ceiling, and 50ms-budget fixtures; prompt-contract fixtures |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the three cost classes and the closed-catalogue enforcement</name>
  <files>model.py, tests/style_roundtrip.py</files>
  <read_first>
    - `model.py` — lint() (lines 593-650), LINT_CODES (lines 578-590), load_style()/STYLE_RULE_KINDS/LOCKED_RULE_IDS from plan 03.1-04, the WOULD_BE-style module-constant regex precedent
    - `tests/style_roundtrip.py` — registry fixtures from plan 03.1-04
    - `.planning/research/2026-08-10-enforcement-and-loose-threads.md` — the three cost classes and the check catalogue
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §17 (voice fixture)
  </read_first>
  <behavior>
    - Test 1: structural counts (heading-cadence, section-skeleton order.before, example-before-variation) and the one shared lexical pass (filler phrases, sentence-length ceiling, banned hector words) both run on every lint; a rule row whose kind is not in STYLE_RULE_KINDS is style.rule_unimplemented (already in plan 03.1-04) and never silently dropped.
    - Test 2: severity semantics — a style may disable a check, re-severity it downward, or parameterize it; it may not raise it above the catalogue rating, and attempting to define a new rule kind fails with the closed-catalogue error.
    - Test 3: a style error blocks a machine-authored write (the write path refuses) and never a human's lint — a human lint run still returns the diagnosis with exit 0.
  </behavior>
  <action>
Implement `run_style_pass(lesson, style)` in `model.py` returning (errors, warnings) as LintError records: class 1 structural counts over the parsed heading tree (section skeleton via the style's cadence.section/order.before rows, using the MD043-style declared skeleton pattern), class 2 the one shared lexical metrics pass (pre-compiled regex constants at module load — the WOULD_BE-style precedent — for sentence-length ceiling, filler-phrase counts, and the banned-word list), class 3 discourse judgement explicitly deferred to Phase 11 (no model-judged rule implemented; any such row is declared manual and skipped with a note, per criterion 3). Enforce the closed catalogue: a style row may enable/disable/re-severity downward/parameterize an existing catalogue check, never define a kind; error severity is earned only by structural counts or author-controlled literal lists, and the catalogue rating is the ceiling. A style error blocks a machine-authored write path (expose a `write_allowed(style, errors)` helper the authoring loop calls) and never a human's `lint`. Thread the pass into `lint()` behind the `STYLE_UNCHECKED` sentinel. Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/style_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Both always-on cost classes fire on their fixtures; discourse rows are declared manual and never fabricated as checks.
    - The severity-ceiling fixture proves a style cannot raise a check above the catalogue rating.
    - The write-block fixture proves machine-write refusal and human-lint pass-through (D-15).
  </acceptance_criteria>
  <done>The style contract is checked on every lint, and its catalogue is closed by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suppression with counting, warning calibration hooks, and the 50ms budget</name>
  <files>model.py, tests/style_roundtrip.py</files>
  <read_first>
    - `model.py` — the lint() finding-accumulation shape and LOCKED_RULE_IDS from plan 03.1-04
    - `tests/style_roundtrip.py` — the fixtures from Task 1
    - `.planning/research/2026-08-10-enforcement-and-loose-threads.md` — the suppression and false-positive-rate findings
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §17 item 8 (the Extensibility Rule 6 stub context)
  </read_first>
  <behavior>
    - Test 1: `<!-- style-ignore: <code> -->` suppresses a non-locked warning and the suppression is counted; the suppression report lists code, count, and the lesson locations.
    - Test 2: `<!-- style-ignore: <locked-id> -->` is itself lint error style.ignore_locked and never suppresses the finding.
    - Test 3: the 50ms budget — run_style_pass on a synthetic 5000-word lesson completes in under 50ms wall time measured with time.perf_counter(); the test is one of the first written, not a late addition.
    - Test 4: a warning's ship-state is derived from a recorded false-positive rate when the Phase 3.2 corpus exists; above the 0.20 threshold it ships disabled by default, and the rate is recorded beside the code.
  </behavior>
  <action>
Implement suppression: `apply_style_ignore(findings, lesson_text)` consults `<!-- style-ignore: <code> -->` comments, checking `finding.code in LOCKED_RULE_IDS` first — a locked-rule suppression attempt is itself lint error `style.ignore_locked` and the finding still stands. Build `style_suppression_report(lesson_text)` exposing suppression counts per code (the named failure mode is an unsuppressable warning getting its whole category disabled; counts are the report that retires bad checks). Add the calibration hook: `warning_ship_state(code, fp_rate)` returns disabled by default when `fp_rate > 0.20` and the rate is recorded beside the code; the Phase 3.2 corpus drives the rates when it exists (this phase owns the checks, 3.2 owns the corpus — record the seam). Add the 50ms budget test to `tests/style_roundtrip.py` using a checked-in or generated 5000-word fixture and `time.perf_counter()`, asserting `elapsed_ms < 50`; if the plain pre-compiled-regex version fails the budget, the first allowed mitigation is pre-compiled module-constant regexes (already the design), and caching is reached for only if that measurably fails. Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/style_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Suppression works for non-locked warnings, refuses locked ids with style.ignore_locked, and the report exposes per-code counts.
    - The 50ms test passes on a 5000-word lesson and records its measured time in the output.
    - The ship-state helper returns disabled for rates above 0.20 with the rate recorded.
  </acceptance_criteria>
  <done>Warnings are suppressible, counted, calibrated, and the whole pass has a budget the suite actually enforces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build StylePrompt.prompt_context with the capped imperative set</name>
  <files>model.py, surfaces/settings.py, schemas/settings.schema.json, itembank.json, tests/style_roundtrip.py</files>
  <read_first>
    - `model.py` — load_style() from plan 03.1-04 (Voice zone and Rules table shapes)
    - `surfaces/settings.py` — the SETTINGS_CODES/validate-then-write precedent and defaults_from_schema()
    - `schemas/settings.schema.json` — the settings group shape and x-itembank-phase convention
    - `.planning/research/2026-08-10-enforcement-and-loose-threads.md` — the imperative-cap and recency findings (adherence collapses past ~10 instructions; cap and last-position are the finding, not taste)
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` §15 (copy contract — no style file text enters the prompt verbatim)
  </read_first>
  <behavior>
    - Test 1: `prompt_context(style, cap=7)` returns exactly: the cap number of distilled imperatives, the one exemplar, and zero ## Voice prose; the style file itself is never embedded.
    - Test 2: imperatives are placed last in the returned prompt context (the exemplar and imperatives are the final blocks).
    - Test 3: the cap is a settings value with default 7 — changing the setting changes the emitted count without a code change.
    - Test 4: the settings group validates through itembank config (validate-then-write) and appears in `itembank config` output.
  </behavior>
  <action>
Implement `StylePrompt.prompt_context(style, cap)` (in `model.py` beside load_style, or a new `style_prompt.py` — executor's discretion, but the function name and contract are fixed): compile the style's Rules rows into distilled imperatives (one imperative per enforceable rule, capped at the `cap` argument), append exactly one exemplar drawn from the style's ## Exemplar, and place the imperatives + exemplar last in the output. The ## Voice prose zone is never emitted — the contract is explicit that the model never receives it (D-17). Register the `style` settings group in `surfaces/settings.py` with `imperative_cap` default 7 (and `warn_fp_threshold` default 0.20 for Task 2's hook), mirror it in `schemas/settings.schema.json` and checked-in `itembank.json`, following the SETTINGS_CODES/validate-then-write precedent so an invalid value is rejected by `itembank config set`. Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/style_roundtrip.py &amp;&amp; python tests/config_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - prompt_context emits capped imperatives + one exemplar, last, with no Voice text (fixture greps assert absence of the Voice prose strings).
    - The cap setting round-trips through `itembank config set` and `itembank config` output.
    - The schema validator passes on the new settings group.
  </acceptance_criteria>
  <done>The authoring model gets the contract that actually produces adherence — short, last, exemplified — and the cap is a setting to measure against the 3.2 corpus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| style file -> lint severity | A model-writable governance file must not demote runtime guarantees |
| style file -> authoring prompt | The Voice zone and locked content must not leak to the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-031-16 | Elevation of Privilege | style file severity manipulation | high | mitigate | Closed catalogue + earned-severity ceiling + LOCKED_RULE_IDS code constants; a style may never raise a check above its catalogue rating or touch a locked id (D-13, ruling 13) |
| T-031-17 | Information Disclosure | Voice zone / locked rules to the authoring model | medium | mitigate | prompt_context emits imperatives + one exemplar only; a fixture greps assert no Voice prose and no locked-row text in the prompt output (D-17) |
| T-031-18 | Denial of Service | pathological style-pass input | low | mitigate | The lexical pass uses pre-compiled, backtracking-free regexes over a 5000-word bound; the 50ms budget test is the executable guard |
| T-031-19 | Tampering | suppression comments | medium | mitigate | style.ignore_locked fires before the ignore table is consulted; suppression counts are reported, so a flood of suppressions is visible, not silent |
| T-031-SC | Tampering | vendored assets | high | mitigate | This plan performs no package operation; settings changes validate through the existing config gate |
</threat_model>

<verification>
- `python tests/style_roundtrip.py`
- `python tests/config_roundtrip.py`
- `python schema_validate.py --all`
- The 50ms budget test prints its measured elapsed time in its output line.
</verification>

<success_criteria>
- Three cost classes run on every lint; the catalogue is closed and the severity ceiling holds.
- Suppression works with counting, locked ids cannot be suppressed, and the 50ms budget is an executable assertion.
- prompt_context emits capped imperatives plus one exemplar, last, with the cap configurable.
</success_criteria>

<output>
Create `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-05-SUMMARY.md` when done.
</output>
