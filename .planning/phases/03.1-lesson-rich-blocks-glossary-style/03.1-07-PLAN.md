---
phase: 03.1-lesson-rich-blocks-glossary-style
plan: 07
type: verify
wave: 3
depends_on:
  - 03.1-02
  - 03.1-03
  - 03.1-04
  - 03.1-05
  - 03.1-06
files_modified:
  - tests/lesson_roundtrip.py
  - tests/style_roundtrip.py
  - tests/anki_keys_roundtrip.py
  - tests/presentation_roundtrip.py
  - tests/evidence_roundtrip.py
  - tests/protocol_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - LESSON-07
  - LESSON-08
  - LESSON-09
  - LESSON-10
  - LESSON-11
  - LESSON-12
  - LESSON-13
  - LESSON-14
  - LESSON-15
  - LESSON-16
  - LESSON-17
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "The eight required verification gates from 03.1-UI-SPEC section 17 all have executable fixtures: no-leak, declarative gloss, degraded gloss, byte-identity pair, three print fixtures, responsive snapshots, voice fixture, and the Extensibility Rule 6 stub."
    - "Every one of the eleven LESSON-07..LESSON-17 requirement IDs is addressed by at least one plan in the phase and verified by a fixture or command named in that plan's acceptance criteria."
    - "The full test suite (`for f in tests/*_roundtrip.py; do python \"$f\" || exit 1; done`) passes green at phase end, including all pre-existing tests."
    - "The compatibility floor holds at phase level: a bank using none of the new constructs parses and renders byte-identically to Phase 3 output, proven by the checked-in fixture pair (03.1-UI-SPEC section 12, Directive 4.4)."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run (recorded in each PLAN.md)."
  prohibitions:
    - statement: "The phase must not seal with a required verification gate that has no executable fixture and no recorded manual-verification item."
      status: unverified
      flagged: true
    - statement: "No plan may claim a requirement is verified by a test that does not exist."
      status: unverified
      flagged: true
---

<objective>
As the phase, I want every claim in the eleven requirements proven by an executable fixture or a named command, and the whole suite green together — so execution ends with evidence, not promises.

Purpose: The phase-level verification plan: assemble the eight UI-SPEC section 17 gates, the requirement-to-fixture coverage audit, and the full-suite run. This plan is the executable form of the 03.1-VALIDATION.md strategy.
Output: The complete fixture set and the green full-suite run with the verification-gate checklist recorded.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-CONTEXT.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-RESEARCH.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-PATTERNS.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md
@.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-VALIDATION.md

<interfaces>
- The fixture harness is the repo's stdlib fail() pattern; each tests/*_roundtrip.py runs standalone.
- The eight gates map to the fixtures named in 03.1-UI-SPEC section 17; each plan in this phase names the fixtures it owns.
- The requirement audit reads each plan's `requirements` field and each fixture file's assertions; no ID may be claimed without a named verification.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `tests/lesson_roundtrip.py` | The eight gate fixtures consolidated (no-leak, declarative gloss, degraded gloss, byte-identity pair, three print fixtures, responsive snapshots, voice fixture, Rule 6 stub cross-check) |
| `tests/style_roundtrip.py` | The 50ms budget, lock, and Rule 6 stub fixtures finalized |
| `tests/anki_keys_roundtrip.py` | The guid round-trip fixture finalized |
| `tests/presentation_roundtrip.py` | Token and @font-face fixtures finalized |
| `tests/evidence_roundtrip.py` | term_lookup/key_review fixtures finalized |
| `tests/protocol_roundtrip.py` | Objective privacy fixture finalized |
| Phase directory | `03.1-GATES.md` recording the eight gates and their fixture/command mapping |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assemble the eight required verification gates</name>
  <files>tests/lesson_roundtrip.py, tests/style_roundtrip.py, tests/anki_keys_roundtrip.py, tests/presentation_roundtrip.py, tests/evidence_roundtrip.py, tests/protocol_roundtrip.py</files>
  <read_first>
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-UI-SPEC.md` section 17 (the eight gates) and section 12 (the byte-identity contract)
    - `tests/lesson_roundtrip.py` and siblings - the fixture conventions and the daemon-start helper
    - `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-VALIDATION.md` - the phase requirements to test map
    - The other six plans' files_modified and acceptance criteria - each fixture is owned by its plan
  </read_first>
  <behavior>
    - Test 1: the no-leak fixture asserts the keyed term's definition is absent from sitting HTML, JSON, ARIA tree, CSS-off text, and every accessible name; the suppressed term has no element, class, data-*, title, or underline; the objective line is absent from public_item() and present in explain_payload().
    - Test 2: the declarative gloss fixture renders the Popover-API markup with popovertarget/popover and the glossary appendix; the degraded gloss fixture asserts the exact unavailable copy with the daemon stopped.
    - Test 3: the byte-identity pair asserts content-region byte-identity and parse identity for a bank using none of the new constructs, with the style-block exclusion named and 03.1-UI-SPEC section 12 cited.
    - Test 4: the three print fixtures (appendix, inline first-use note count, ?print=drill blanks) and the voice fixture (asserted by computed font-family token, never a literal family) pass.
  </behavior>
  <action>
Consolidate and complete the eight gate fixtures across the test files per the ownership in the other six plans: (1) no-leak, (2) declarative gloss, (3) degraded gloss, (4) byte-identity pair, (5) three print fixtures, (6) responsive snapshots at 1280/768/375 plus 320px and 200% zoom for reader_nav and example_layout values, (7) voice fixture asserting computed font-family token names (a test naming Georgia literally has already broken the rule it tests), and (8) the Extensibility Rule 6 stub cross-check. Each fixture names the UI-SPEC section it proves. The degraded-gloss and popover fixtures may record a human-verify item for true cross-browser behavior (the research environment note) but must still assert DOM/HTML shape automatically. Write the four behavior tests first; no fixture may assert what no code path produces.
  </action>
  <verify>
    <automated>python tests/lesson_roundtrip.py &amp;&amp; python tests/style_roundtrip.py &amp;&amp; python tests/anki_keys_roundtrip.py &amp;&amp; python tests/presentation_roundtrip.py &amp;&amp; python tests/evidence_roundtrip.py &amp;&amp; python tests/protocol_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - All eight gates have a named fixture file and pass.
    - The byte-identity fixture names the style-block exclusion and cites 03.1-UI-SPEC section 12.
    - The voice fixture asserts token names, not family literals.
  </acceptance_criteria>
  <done>The phase's eight gates are executable evidence, not intentions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the requirement-coverage audit and the full suite</name>
  <files>tests/lesson_roundtrip.py, tests/style_roundtrip.py, tests/anki_keys_roundtrip.py, tests/presentation_roundtrip.py, tests/evidence_roundtrip.py, tests/protocol_roundtrip.py</files>
  <read_first>
    - `REQUIREMENTS.md` - the eleven LESSON-07..17 rows and the traceability table
    - `ROADMAP.md` - the Phase 3.1 section with the requirement IDs
    - The six plan files' `requirements` fields and acceptance criteria
    - `03.1-VALIDATION.md` - the phase requirements to test map (used as the audit checklist)
  </read_first>
  <behavior>
    - Test 1: every one of LESSON-07..LESSON-17 appears in at least one plan's `requirements` field, and each such plan's acceptance criteria name a test command or fixture that proves it.
    - Test 2: the full suite - `for f in tests/*_roundtrip.py; do python "$f" || exit 1; done` - exits 0 with all pre-existing and new tests green.
    - Test 3: `python schema_validate.py --all` passes (the new event $defs and settings group validate).
    - Test 4: the deterministic edge-probe skip is recorded in each PLAN.md (grep for the skip sentence), satisfying the visible-skip rule.
  </behavior>
  <action>
Write the audit as an executable check (extend one of the test files or add a small `tests/phase_031_audit.py` in the fail() harness): parse each PLAN.md's requirements field, cross-reference the eleven IDs, and fail on any ID with no plan and no named verification. Run the full suite and schema validation, and fix any failures this surfaces (fixtures only - no new product code in this plan). Write `03.1-GATES.md` recording the eight gates, their fixture file, and any human-verify items (true cross-browser popover behavior; ClearType render check for the fonts). Record that the deterministic edge probe was skipped visibly per the handoff: "deterministic edge probe skipped: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run" (already present in each PLAN.md - the audit greps for it). Write the four behavior tests first.
  </action>
  <verify>
    <automated>for f in tests/*_roundtrip.py; do python "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - The audit exits 0 with 11/11 IDs covered and every plan's named verification exists.
    - The full suite and `schema_validate.py --all` pass.
    - 03.1-GATES.md exists with the eight gates mapped and the human-verify items named.
  </acceptance_criteria>
  <done>Phase 3.1 seals on evidence: every requirement covered, every gate executable, the whole suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixtures -> suite result | A fixture that asserts nothing, or asserts the wrong surface, would launder a regression |
| gate checklist -> phase seal | An unexecuted gate must not be recorded as executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-031-23 | Spoofing | fixture coverage | high | mitigate | The requirement-coverage audit cross-references plan requirements against named verifications; a claim without a fixture fails the audit |
| T-031-24 | Tampering | gate checklist | medium | mitigate | Each gate maps to a fixture file and a command; the phase cannot record a gate as executed without its test passing |
| T-031-25 | Repudiation | human-verify items | low | mitigate | 03.1-GATES.md records which items are automated vs human-verified, so a later auditor can tell evidence from intent |
| T-031-SC | Tampering | package operations | high | mitigate | This plan performs no package operation and no network fetch |
</threat_model>

<verification>
- The full-suite loop command (all tests/*_roundtrip.py).
- `python schema_validate.py --all`.
- The audit's 11/11 requirement-coverage line.
</verification>

<success_criteria>
- The eight gates are executable and green.
- 11/11 requirement IDs map to plans and named verifications.
- The full suite passes together with all pre-existing tests.
</success_criteria>

<output>
Create `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-07-SUMMARY.md` when done.
</output>
