---
context: phase
phase: 03.1-lesson-rich-blocks-glossary-style
plan: 2
task: 0
total_plans: 7
status: in_progress
last_updated: 2026-08-10T20:06:32.264Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through
> failure this session. Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Subagent task payloads do not arrive in this runtime — two
      spawned executors came up idle ("ready and listening, no task content").
      Structural mitigation: either execute plans inline (the orchestrator)
      or, after every dispatch, verify on disk (SUMMARY.md exists + commits
      landed) before trusting the subagent turn.
- [ ] CONSTRAINT: Typed GSD agent spawns (`agent_type="gsd-executor"`, etc.)
      are refused by the model router (roles pin deepseek-v4-flash xhigh,
      which the router rejects). Structural mitigation: generic-agent
      workaround — spawn default agents and inject the role instructions from
      `C:/Users/wayba/.codex/agents/<role>.toml` as the system prompt — or
      run inline. Do not retry typed spawns more than once.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Subagent payload loss | Spawned agents report idle/ready with no task content; resending also fails | blocking | Run plans inline, or verify on disk (SUMMARY.md + commits) after dispatch before trusting a turn |
| Typed GSD spawn refusal | gsd-executor/gsd-planner typed spawns are rejected at launch by the model router | blocking | Generic-agent workaround with role .toml injection, or inline execution |
| Broad staging | Working tree carries pre-existing uncommitted .planning docs from prior sessions | advisory | Stage only plan-touched files by explicit path; never `git add -A` / `git add .` |
| Worktree assumption | `workflow.use_worktrees` is unset → false; no worktrees exist | advisory | Sequential execution on `main`; branching_strategy is "none" — commit directly on main |

<current_state>
Autonomous milestone v1.0 run in progress. Phase 03.1 (Lesson Rich Blocks,
Glossary & Style) is the active phase: plan 01 is COMPLETE (1/7 plans, 3/3
tasks, all suites green), plan 02 is next. The autonomous queue holds 13
phases — 3.1, 3.2, 5, 6, 06.1, 6.2, 7, 8, 9, 9.1, 10, 11, 13 — all with
CONTEXT/RESEARCH/PLANs already on disk (discuss is skipped everywhere;
planning is done). Phases 2.1/3/4 are deferred (verification_deferred_human)
and are NOT in the queue. Phase 12 is retired (moved to 2.1) and 999.x are
backlog — neither is in the queue.
</current_state>

<completed_work>

Plan 03.1-01 complete (commits 01650ab, f567489, 0696317, 57c9f74):
- Task 1: voice/measure/leading/radius tokens in SHARED_CSS
  (--font-paper, --font-ledger, --measure-prose:66ch, --measure-wide:90ch,
  --leading-lesson:1.65, --r-2, --r-3); fallbacks Georgia/ui-monospace named
  only inside presentation.py.
- Task 2: LESSON_TEMPLATE migrated onto theme_css + SHARED_CSS + LESSON_CSS
  (locked order); locked heading ramp (32/20/16 at 600 with space-6/space-3
  and space-5/space-2 margins), text-lesson prose 18px/1.65 on
  --measure-prose, Ledger labels via --font-ledger, print contract
  (@page 18mm, h2 break-after, [popover] hide, ?print=drill hook).
  Zero font-size/font-family/line-height/color literals left in
  LESSON_TEMPLATE.
- Task 3: one callout container — KEY/EXAMPLE/NOTE/WARNING render with the
  exact Ledger labels; [!CHECK: <id>] renders the inert reserved slot (exact
  copy, no key/form/scoring path, id dropped); unknown [!...] kinds degrade
  to the pre-change paragraph output byte-for-byte; bodies escape-first
  (stored-XSS fixture).
- Phase 3 compatibility floor: content-region byte-identity + parse-identity
  goldens (fixtures/lesson_golden_phase3_content.txt + _parse.json), style
  block excluded per 03.1-UI-SPEC §12.
- Tracking: STATE.md advanced to plan 02; ROADMAP 03.1 shows 1/7 summaries;
  LESSON-13/LESSON-14 marked complete; decisions logged.
- Full regression sweep green: lesson, presentation, protocol, daemon,
  serve, evidence, scoring, surface, config, theme, day.
</completed_work>

<remaining_work>

- Plan 03.1-02: Glossary — ## TERMS + [[term]] hover gloss + printed
  appendix, glossable() silence rules, term_lookup evidence events
  (append-only; decide source provenance per CONTEXT OPEN item 1).
- Plan 03.1-03: [!KEY] memorizables — Anki #guid round-trip, review path,
  objective shown only after answering.
- Plan 03.1-04: five-style registry (expository parent + worked-example,
  checked-prose, artifact-first, case-narrative; one file per style; house
  vs style split) — adding a sixth style is one file.
- Plan 03.1-05: style contract lint enforcement (closed catalogue,
  suppressible without breaking the lock) + seven-imperative prompt cap.
- Plan 03.1-06: vendor Source Serif 4 + iA Writer Quattro (OFL 1.1, pinned
  release, SHA-256, measured woff2 — C6 gate), format doc, and fold
  lesson_layout into 09-02-PLAN.md BEFORE Phase 9 executes (D-04 one-way).
- Plan 03.1-07: requirement-proof fixture suite proving all 11 requirements
  green together.
- After phase 03.1: phases 3.2, 5, 6, 06.1, 6.2, 7, 8, 9, 9.1, 10, 11, 13
  (execute → code review → verification routing → UI review → transition).
- After all phases: lifecycle audit → complete → cleanup.
- Deferred (not autonomous): $gsd-verify-work 2.1 / 3 / 4.
</remaining_work>

<decisions_made>

- Lesson page keeps its own LESSON_TEMPLATE but composes theme_css +
  SHARED_CSS + LESSON_CSS in the locked order (surface_shell appends
  SHARED_CSS last); the composition is imported, never duplicated, so
  surfaces/study.py needed no change.
- LESSON_CSS uses only the locked project scale sizes (12/16/18/20/32) and
  the 400/600 weight pair; fonts resolve only through --font-paper /
  --font-ledger tokens (never a literal family).
- --r-2/--r-3 radius tokens landed in SHARED_CSS per UI-SPEC §2 ownership
  (Task 1's plan text enumerated voice/measure/leading only; the callout
  contract requires --r-3).
- [!CHECK: <id>] drops the id entirely and renders only the exact inert
  copy — no key, no form, no scoring path (D-18).
- Phase 3 warn-note tests moved from the raw var(--warn) substring to the
  rendered-element contract (.warn element / .warn rule) because SHARED_CSS
  legitimately carries the token after migration.
</decisions_made>

<blockers>
- Subagent payload delivery unreliable (see BLOCKING CONSTRAINTS) — plan
  03.1-01 was executed inline by the orchestrator; expect to do the same.
- Typed GSD agent spawns refused by the router — use the generic-agent
  workaround or inline execution.
- Deferred human verification for phases 2.1/3/4 — user action, not blocking
  the autonomous queue.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable state (resume tooling).
2. `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-01-SUMMARY.md` —
   what plan 01 built and the deviations/decisions to build on.
3. `.planning/STATE.md` — Deferred Verification table and decisions log.
4. `.planning/PLANNING-DIRECTIVES.md` — project directives (§2-§5, §4a).
5. `C:/Users/wayba/.codex/agents/gsd-executor.toml` — executor role
   instructions if dispatching subagents (generic-agent workaround).

## Critical Anti-Patterns (do NOT repeat these)

- SPAWN-AND-TRUST: Dispatching a subagent and trusting its return without
  on-disk verification → verify SUMMARY.md + commits, or run inline.
- TYPED-SPAWN: Requesting typed GSD agents → router refuses at launch; use
  default agents with role .toml injection.
- BLANKET-STAGE: `git add -A` in this dirty tree sweeps prior-session docs
  into unrelated commits → stage explicit paths only.

## Infrastructure State

- Environment: Windows PowerShell; project root C:/Users/wayba/Downloads/CTF/itembank.
- gsd-tools: `node C:/Users/wayba/.codex/gsd-core/bin/gsd-tools.cjs` (no repo-local gsd-core).
- Git: branch `main`, ahead of origin by ~196 commits; branching_strategy
  "none" — work commits directly on main. commit_docs true.
- Worktrees: disabled (workflow.use_worktrees unset → false). Sequential
  execution only.
- No running servers/watchers. No async jobs in .planning/async-jobs.
- Pre-existing dirty working tree: many uncommitted .planning docs from prior
  sessions (config.json, other phases' PLAN/RESEARCH/VALIDATION/UI-SPEC
  files, research .cache). Preserve them untouched.

<context>
The autonomous run discovered the queue from ROADMAP/STATE: 13 phases,
deferred phases dropped, retired/backlog excluded. All phases already have
context + research + plans, so this run is execution-heavy. Plan 03.1-01 was
executed inline (subagent delivery failed twice) and closed cleanly with all
regressions green. The next plan is 03.1-02 (glossary); it adds evidence
event types (term_lookup), so it will touch model.py/evidence.py/runtime.py
and needs the append-only event discipline from Phase 1. Remember the
append-only constraint: decide term_lookup source provenance before first
write (CONTEXT OPEN item 1).
</context>

<next_action>
Start with: `$gsd-autonomous` in a fresh chat (or `$gsd-autonomous --from 3.1`).
It will skip discuss (contexts exist), confirm plans exist, and execute plan
03.1-02 next. Given the runtime, plan to execute plans inline (or verify each
subagent's SUMMARY.md + commits on disk before trusting it).
</next_action>
