---
context: phase
phase: 03.1-lesson-rich-blocks-glossary-style
plan: 4
task: 0
total_plans: 7
status: in_progress
last_updated: 2026-08-10
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Constraints verified 2026-08-10. The typed-spawn
> constraint (#2) was resolved later the same day by the pin fix; #1 holds.

- [ ] CONSTRAINT (RE-VERIFIED 2026-08-10, six controlled probes): spawn-message
      payloads do not arrive in this runtime (the agent itself reported "no
      message payload arrived with this turn"). Fork-based spawns
      (fork_turns all/2/1) DO receive context and act on it, but drift to the
      broadest visible task and none completed the exact artifact within
      bounded waits. Typed gsd-executor launched post-pin-fix but never
      completed a turn. Structural mitigation: execute plans inline; if
      dispatching, use the fork + brief file + disk-verification recipe
      (.planning/notes/2026-08-10-subagent-dispatch-recipe.md) and fall back
      to inline after one failed handoff.
- [x] CONSTRAINT (RESOLVED 2026-08-10): Typed GSD agent spawns
      (`agent_type="gsd-executor"`, etc.) were refused because roles pinned
      deepseek-v4-flash at `xhigh` (router accepts only `low|high|max`). The
      pins in C:/Users/wayba/.codex/agents/*.toml were fixed to `max`; typed
      spawns should launch now. If one is still refused, use the generic-agent
      workaround once, then inline.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Subagent payload loss / drift | Spawn-message payloads dropped; fork agents receive context but drift and hang | blocking | Inline execution is production; fork + brief-on-disk + byte-for-byte disk verification, inline fallback after one handoff |
| Typed GSD spawn refusal (RESOLVED) | gsd-executor/gsd-planner typed spawns were rejected at launch while roles pinned xhigh | resolved 2026-08-10 — pins now max; fall back to generic-agent workaround only if a typed spawn is still refused |
| Broad staging | Working tree carries pre-existing uncommitted .planning docs from prior sessions | advisory | Stage only plan-touched files by explicit path; never `git add -A` |
| STATE.md shell tricks | A PowerShell splice mis-indexed and truncated STATE.md | blocking | Use apply_patch for STATE.md edits; never build arrays with `$lines[0..($x-2)]` |

## Current State

Autonomous milestone v1.0 run in progress. Phase 03.1 (Lesson Rich Blocks,
Glossary & Style) is at **plan 5 of 7** (4/7 summaries committed).

**Phase 03.1 plans done:**
- 03.1-01: tokens/CSS migration + callout container (previous session)
- 03.1-02: TERMS/glossary/glossable/term_lookup — closed `f25df4a`
- 03.1-03: [!KEY]/Anki export/key_review/objective C7 — closed `cde3b0b`
- 03.1-04: five-style registry (house + expository/worked-example/checked-
  prose/artifact-first/case-narrative, render_style, Rule 6) — closed inline
  `b4aef5d`/`f0e55a9`/`9944872`/`f79da03`, UAT `fcb8228`

**Remaining plans:** 03.1-05 (style lint enforcement), 03.1-06 (vendored fonts
+ lesson_layout fold into 09-02), 03.1-07 (requirement-proof fixture suite).
Then phase verification, code review, transition, and the next phases (3.2,
5, 6, 06.1, 6.2, 7, 8, 9, 9.1, 10, 11, 13).

**Deferred (not autonomous):** `$gsd-verify-work 2.1 / 3 / 4` — human
verification rows in STATE.md.

## Key Decisions This Session (recorded in STATE.md)

- term_lookup carries explicit source provenance ('reader'|'session').
- /gloss route 404s suppressed terms (no per-term disclosure, no event).
- Key ids mint through the shared item taken-set (no separate namespace).
- Authored cloze grammar is any `{{text}}` marker -> `{{c1::text}}`
  sequential; `{{n::text}}` keeps n.
- C7 closure: public_item() no longer carries the syllabus [OBJECTIVE:];
  study's pre-answer objective chip removed; objectives render only in
  explain_payload() behind the verdict.
- `export <bank> --format keys` (out omitted -> `<bank>_keys.tsv`).

## Commits This Session (main)

Plan 03.1-02: `467a9ce` test / `5b88b9c` feat (TERMS lint), `7f8b125` test /
`10ca45f` feat (glossable+term_lookup), `25fe9d3` test / `5eabaa2` feat
(glossary render + /gloss), metadata `f25df4a`.
Plan 03.1-03: `8c85b5a` test / `01f0c9d` feat (key parse+lint),
`2b13399` test / `4e79778` feat (Anki keys export), `05022db` test /
`11d41e3` feat (key card+review+drill+objective), metadata `cde3b0b`.

## Next Action

Plan 03.1-04 is closed inline (7 commits, UAT `fcb8228`: 5 passed, 2 blocked
on assistive-tech/visual review). Next: execute plan 03.1-05 (style lint
enforcement) inline — production mode. The unlock-executors probe ran and
concluded executors stay LOCKED (typed fork drift deleted probe artifacts);
subagent experiments are stopped. Run `$gsd-resume-work` or continue the
current session; do not re-open subagent experiments without a harness fix.
Before starting the cost-effectiveness experiment, read:
`.planning/notes/2026-08-10-cost-effectiveness-inline-vs-subagents.md`.

## Required Reading (in order)

1. `.planning/notes/2026-08-10-cost-effectiveness-inline-vs-subagents.md`
2. `.planning/notes/2026-08-10-codex-runtime-typed-gsd-agents.md`
3. `.planning/phases/03.1-lesson-rich-blocks-glossary-style/03.1-04-PLAN.md`
4. `.planning/STATE.md` — Deferred Verification table and decisions log.

## Infrastructure State

- Environment: Windows PowerShell; project root C:/Users/wayba/Downloads/CTF/itembank.
- gsd-tools: `node C:/Users/wayba/.codex/gsd-core/bin/gsd-tools.cjs` (no repo-local gsd-core).
- Git: branch `main`; branching_strategy "none" — commit directly on main. commit_docs true.
- Worktrees: disabled (workflow.use_worktrees unset -> false). Sequential execution only.
- Pre-existing dirty working tree: many uncommitted .planning docs from
  prior sessions — preserve them; stage explicit paths only.
