# Phase 3.1: Lesson Rich Blocks, Glossary & Style - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-10
**Phase:** 03.1-lesson-rich-blocks-glossary-style
**Mode:** autonomous. Weibao's standing instruction (`PLANNING-DIRECTIVES.md` §1, 2026-08-10) was to pick the most useful/comprehensive option, ship all of them where they conflict and let the learner choose later, and keep going without stopping. No question was put to the user; every row below was resolved by the agent against the research artifacts and Directive §2/§3.
**Areas discussed:** font vendoring (ruling 1), lesson body size (ruling 2), Bottom-Up definition (ruling 6), `lesson_layout` placement (ruling 7), `[!KEY]` scope (ruling 8), `[!CHECK:]` scope (ruling 9), style-file resolution, prompt-imperative cap, warning calibration policy

---

## Ruling 1 — vendor Source Serif 4 + iA Writer Quattro?

| Option | Description | Selected |
|--------|-------------|----------|
| Vendor both under OFL | Pinned, checksummed, license reviewed per the KaTeX precedent | ✓ |
| Fallback stack only | `--font-paper` → Georgia, `--font-ledger` → `ui-monospace` | |

**Choice:** vendor. **Notes:** `UI-SPEC.md:438-439` already records both licenses as OFL 1.1 from primary sources, so the review cost is already paid. Directive §4a requires pinning + checksum + license review for every third-party artifact regardless. The fallback stays specified (`UI-SPEC.md:454`), which is what makes this reversible.

---

## Ruling 2 — 18px lesson body vs the locked 16px `text-body`

| Option | Description | Selected |
|--------|-------------|----------|
| Already settled | `UI-SPEC.md:324` defines `text-lesson` 18px/1.65, lesson prose only | ✓ |
| Re-open | Discuss 16 vs 18 again | |

**Choice:** treat as settled. **Notes:** this ruling was answered in UI-SPEC on 2026-08-10 with the reason recorded (Butterick's 15–25px window; serif reading sits at 17–19px). Re-opening it would be churn. `text-body` is unchanged.

---

## Ruling 6 — is "The Bottom Up" Wienand-style runnable-artifact-first?

| Option | Description | Selected |
|--------|-------------|----------|
| Adopt as assumption, falsify early | Underwrites `artifact-first` only; check it in the first plan task that touches it | ✓ |
| Block until confirmed | Wait for Weibao | |
| Drop `artifact-first` | Avoid the dependency | |

**Choice:** adopt and flag. **Notes:** Directive §2 — a blocking question costs a round trip, a recorded assumption costs a line. Blast radius is one style.

---

## Ruling 7 — `lesson_layout` into a closed `subject_profiles`

| Option | Description | Selected |
|--------|-------------|----------|
| Fold into `09-02-PLAN.md` now | Phase 9 is planned but unexecuted (`STATE.md` at Phase 04), so the field is free today | ✓ |
| Registry version bump later | Add the field after Phase 9 ships | |

**Choice:** fold. **Notes:** this is the roadmap's own "default if unruled" and it is also the only cheap option. Rated `one-way` in CONTEXT.md because after Phase 9 executes it becomes a published-schema migration.

---

## Ruling 8 — is `[!KEY]` legal inside item rationales?

| Option | Description | Selected |
|--------|-------------|----------|
| Lesson-only | `key.duplicate_id` scopes to lesson files | ✓ |
| Lessons and rationales | Wider now | |

**Choice:** lesson-only (the stated default). **Notes:** narrow-now/additive-later is the reversible direction. Widening is accepting something the linter currently rejects; narrowing would break authored banks.

---

## Ruling 9 — may `[!CHECK:]` reference another bank?

| Option | Description | Selected |
|--------|-------------|----------|
| Same bank only | Lint error on cross-bank reference | ✓ |
| Cross-bank allowed | Gated lessons can pull from a shared bank | |

**Choice:** same bank only (the stated default). **Notes:** shared verbatim with Phase 6.2 D-01. The two phases must not diverge.

---

## Style-file resolution order

| Option | Description | Selected |
|--------|-------------|----------|
| Bank-adjacent → user data dir → bundled house | First match by id wins; duplicate id warns naming both paths | ✓ |
| Bundled only | No user-authored styles | |
| User data dir only | No per-bank override | |

**Choice:** three-level with first-match. **Notes:** matches the existing settings base-dir convention. A silent shadow is the failure mode, hence the warning.

---

## Prompt-imperative cap (criterion 3d)

| Option | Description | Selected |
|--------|-------------|----------|
| Setting, default 7 | Measurable against the 3.2 corpus without a code change | ✓ |
| Hard constant 7 | Simpler | |

**Choice:** setting. **Notes:** the roadmap itself records seven as extrapolated from format-compliance research and *not* measured on pedagogical structure. Freezing an unmeasured number as a constant is how it becomes folklore.

---

## Warning calibration policy

| Option | Description | Selected |
|--------|-------------|----------|
| FP rate recorded; >~20% ships disabled; `<!-- style-ignore: -->` from day one | Suppression counts become a report that retires bad checks | ✓ |
| Ship all warnings enabled | Simpler | |

**Choice:** calibrated. **Notes:** the named failure mode is an unsuppressable warning getting its whole category globally disabled — which loses the good checks with the bad.

---

## Claude's Discretion

Module factoring, internal names, the lexical metrics chosen, and the style-pass cache shape. Everything the CONTEXT.md discretion paragraph lists as *not* open was decided above or by the roadmap.

## Deferred Ideas

Model-driven `restyle` (Phase 11); the gating loop (Phase 6.2); `## SCENARIO` (Phase 9); the bilingual reader reading `zh=` (999.2); warning calibration evidence (Phase 3.2).
