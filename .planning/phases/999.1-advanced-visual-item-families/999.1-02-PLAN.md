---
phase: 999.1-advanced-visual-item-families
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - runtime.py
  - model.py
  - surfaces/quiz_page.py
  - schemas/item.schema.json
  - schemas/visual_interaction.schema.json
  - fixtures/advanced_visual_bank.md
  - tests/timeline_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT / UI-INDEPENDENT
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-06, VIS-07, VIS-08, VIS-09]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "A served timeline item places one authored event label at a canonical SCALAR time value on an axis, through the same protocol-1 envelope, interact path, and visual_action evidence as plot/numberline/hotspot"
    - "Timeline responses are exactly {\"kind\":\"timeline_event\",\"event\":ID,\"value\":SCALAR}; event ids are stable authored strings and values follow the exact SCALAR grammar"
    - "Timeline scoring is deterministic and runtime-owned: exact event id plus per-coordinate tolerance on value, with boundary cases pinned by tests; tolerance 0 means exact tick compare"
    - "The public contract carries axis, events, initial placements, actions, and accessibility only -- never accepted events/values, tolerance, or feedback anchors"
    - "The timeline renderer keeps the 06.1 modality rule: SVG time axis with draggable/steppable event chips plus an operable adjacent semantic control (event select + value select), one serializer, no canvas"
  artifacts:
    - path: "fixtures/advanced_visual_bank.md"
      provides: "Synthetic timeline items (place one event at a time value; move it) appended to the shared advanced bank"
      contains: "[INTERACTION: timeline]"
    - path: "tests/timeline_roundtrip.py"
      provides: "Parser -> public contract -> SCALAR canonicalization -> deterministic scoring/tolerance -> observation -> interact/evidence -> renderer assertions for timeline"
      contains: "def main"
    - path: "runtime.py"
      provides: "timeline interaction/kind/actions in the allowlists, per-family canonicalization, _timeline_verdict, extended observation invariants (known_event, in_bounds)"
      contains: "timeline"
    - path: "surfaces/quiz_page.py"
      provides: "asVisual timeline renderer: SVG time axis + event chips + semantic select controls"
      contains: "timeline"
  key_links:
    - from: "runtime.py"
      to: "model.py"
      via: "the interaction-aware scene allowlist (timeline accepts version/axis/events/initial/actions/accessibility) drives lint without re-implementing the grammar"
      pattern: "timeline"
    - from: "runtime.py"
      to: "surfaces/quiz_page.py"
      via: "interaction_contract.renderer_config (axis/events/initial) and response_schema drive the timeline renderer"
      pattern: "interaction_contract"
    - from: "surfaces/quiz_page.py"
      to: "surfaces/session.py"
      via: "/api/interact carries place_timeline_event / move_timeline_event with canonical state"
      pattern: "place_timeline_event"
    - from: "surfaces/session.py"
      to: "evidence.py"
      via: "visual_action_event records timeline action types and canonical after_state with the locked field set"
      pattern: "visual_action"
---

<objective>
Add the `timeline` family to protocol 1: a time-series visual where the learner
places one authored event label at a canonical SCALAR time value on an axis.
Same additive-extension seam as plan 01 (grow the allowlists), same envelope,
same interact/evidence path, same SVG-plus-semantic-control renderer rule.

Purpose: prove that a *continuous-valued* family (value + tolerance) fits the
protocol-1 extension pattern, which plan 03's trace family (also
continuous-valued, vector-valued) will build on.
Output: timeline interaction/kind/actions, per-family canonicalizer/verdict/
observation, renderer dispatch, schema/spec coverage, synthetic fixtures, and
an executable roundtrip test.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/UI-SPEC.md
@.planning/phases/999.1-advanced-visual-item-families/999.1-CONTEXT.md
@.planning/phases/999.1-advanced-visual-item-families/999.1-RESEARCH.md
@.planning/phases/999.1-advanced-visual-item-families/999.1-01-SUMMARY.md
@.planning/phases/06.1-interactive-visual-assessment-protocol/06.1-01-PLAN.md
</context>

<interfaces>
Consumed unchanged from plan 01/06.1:

- `runtime.public_item(q) -> interaction_contract` (key-free projection).
- `runtime.score_response(q, answer) -> bool` sole scorer; `_timeline_verdict`
  reached only through it.
- `session.do_interact` five-field request; `VISUAL_ACTIONS` grows.
- `evidence.visual_action_event` / `visual_actions` (family-neutral).
- `_visual_interaction_contract` (accepts the timeline scene shape).
- The interaction-aware scene-member allowlist added by plan 01.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Timeline protocol seam — allowlist, contract, canonicalizer, verdict</name>
  <precondition>Plan 01 committed; `python tests/hotspot_roundtrip.py` and `python tests/visual_roundtrip.py` pass.</precondition>
  <reversibility rating="costly">Grown allowlists and the timeline scene shape become a shared author/browser/agent contract; extend additively.</reversibility>
  <files>runtime.py, model.py, fixtures/advanced_visual_bank.md, tests/timeline_roundtrip.py</files>
  <behavior>
    - `VISUAL_INTERACTIONS` gains `timeline`; `VISUAL_KINDS` gains
      `timeline_event`; `VISUAL_ACTIONS` gains `place_timeline_event` and
      `move_timeline_event`. Protocol integer stays 1 (D-999.1-01).
    - A timeline scene accepts exactly `version`, `axis`, `events`, `initial`,
      `actions`, `accessibility`. `axis` reuses `canonical_axis` (SCALAR
      min/max, positive step, <= 201 ticks). `events` is a non-empty list of
      `{id, label}` with unique `[A-Za-z0-9_-]{1,64}` ids and non-empty
      labels; `_reject_visual_exec` covers ids/labels.
    - `canonical_visual_response` accepts exactly
      `{"kind":"timeline_event","event":ID,"value":SCALAR}`; extra keys,
      unknown event ids, malformed scalars, or out-of-axis values are
      invalid. `_timeline_verdict` (reached only through `score_response()`)
      compares the event id exactly and the value within the per-coordinate
      private tolerance; tolerance 0 is exact tick compare; boundary cases
      pass on both borders and adjacent representable values fail.
    - `visual_observation` gains `known_event` and reuses `in_bounds`/
      `off_grid`/`out_of_domain`; `visual_state_in_domain` /
      `visual_state_on_grid` gain the timeline branch.
  </behavior>
  <action>Start `tests/timeline_roundtrip.py` with the timeline happy path RED, then implement the seam. Extend the fixture `fixtures/advanced_visual_bank.md` with two timeline items: one exact placement (`place_timeline_event`, value pinned by an integer-tick axis) and one tolerance-bounded placement (half-step axis, `1/2` tolerance, boundaries pass and adjacent fail). Keep every 06.1/plan-01 behavior byte-identical. Route through `_visual_verdict` dispatch, `score_response()`, `visual_observation`, `interaction_result`. Reject unknown scene members, malformed events, script-bearing content, and forged authority fields (T-999.1-05).</action>
  <verify>
    <automated>python tests/timeline_roundtrip.py</automated>
    <automated>python tests/hotspot_roundtrip.py</automated>
    <automated>python tests/visual_roundtrip.py</automated>
  </verify>
  <done>Timeline items parse, project key-free, score exactly and with pinned tolerance borders, and produce runtime-bounded observations through the same envelope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Timeline commit path and evidence</name>
  <files>schemas/item.schema.json, schemas/visual_interaction.schema.json, tests/timeline_roundtrip.py</files>
  <behavior>
    - `do_interact` accepts `place_timeline_event`/`move_timeline_event`
      actions with canonical timeline state; dedupe, conflict, and named
      refusals behave exactly as for the other families (no session.py
      behavioral change beyond the grown allowlist).
    - One `visual_action` event appends per commit with the locked field set,
      `before_state` from the trail, canonical `after_state`; a moved event
      records the before/after values.
    - The two schemas grow additively: interaction enum `timeline`, action
      enum `place_timeline_event`/`move_timeline_event`, kind enum
      `timeline_event`, and a timeline `renderer_config` definition
      (axis/events/initial/actions/accessibility); `visual_interaction.schema.json`
      documents the timeline state shape.
  </action>
  <action>Extend `tests/timeline_roundtrip.py` with the session-level interact/evidence section (mirror the plan-01 test helpers) and schema validation. Assert the event field set is unchanged and no pointer/private material appears.</action>
  <verify>
    <automated>python tests/timeline_roundtrip.py</automated>
    <automated>python schema_validate.py</automated>
  </verify>
  <done>A committed timeline placement (and its later move) appends exactly one linked visual_action event each, queryable and leak-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Timeline renderer, spec, and refusal coverage</name>
  <files>surfaces/quiz_page.py, model.py (SPEC visual section), fixtures/advanced_visual_bank.md, tests/timeline_roundtrip.py</files>
  <behavior>
    - `asVisual` gains the timeline branch: inline SVG horizontal time axis
      with tick labels, authored event chips placed at canonical values,
      keyboard arrows stepping the selected event by declared units,
      Enter/Space commit, Escape cancel; an adjacent semantic control (event
      select + value select) drives the same state object/serializer.
    - Pointer drag/tap, keyboard, and the semantic control serialize the
      identical `{"kind":"timeline_event","event":ID,"value":SCALAR}` state;
      only a changed explicit commit posts the action (place on first
      placement, move afterwards).
    - No canvas; SVG named by the accessibility description; 06.1 gates
      (focus, status, reduced motion, 44px targets) inherited.
    - The `SPEC` visual section names the timeline family (scene members,
      response kind, actions, tolerance rule); GIFT refuses timeline items
      loudly via the generic visual refusal.
  </action>
  <action>Extend `tests/timeline_roundtrip.py` with served/offline renderer assertions (timeline renderer present, dispatch registered, offline refusal copy, no canvas). Update the SPEC text additively. Extend the GIFT fixture with a timeline item and assert loud refusal in default and strict modes.</action>
  <verify>
    <automated>python tests/timeline_roundtrip.py</automated>
    <automated>python tests/visual_accessibility_roundtrip.py</automated>
    <automated>python tests/gift_export_roundtrip.py</automated>
  </verify>
  <done>The timeline family is authorable, renderable, answerable, evidenced, and loudly refused in GIFT through the existing protocol-1 seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bank markdown -> model/runtime | Declarative timeline scene (axis, events) and private accepted placements enter validation; ids/labels must never become executable code. |
| Runtime -> public browser/agent contract | Axis/events/initial cross to an untrusted consumer; accepted placements and tolerance stay server-side. |
| Browser/agent -> runtime scorer | Untrusted timeline responses and forged fields cross into validation and scoring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-999.1-05 | Elevation of Privilege | bank timeline configuration | high | mitigate | Per-interaction scene allowlist + `_reject_visual_exec` on event ids/labels before render. |
| T-999.1-06 | Information Disclosure | timeline public projection | high | mitigate | Public contract carries axis/events/initial/actions/accessibility only; extend the sentinel scan. |
| T-999.1-07 | Tampering | timeline submit | high | mitigate | Exact wire-shape grammar; unknown event ids and out-of-axis values fail closed. |
| T-999.1-08 | Denial of Service | scene/event counts | medium | mitigate | Bound event count (<= 100), id length (<= 64), axis via `canonical_axis`. |
| T-999.1-SC | Tampering | package supply chain | low | accept | No package-manager install; stdlib + browser-native SVG/HTML/JS. |
</threat_model>

<verification>
- `python tests/timeline_roundtrip.py` drives the full timeline path.
- Plan 01 + 06.1 tests stay green: `python tests/hotspot_roundtrip.py`,
  `python tests/visual_roundtrip.py`, `python tests/visual_evidence_roundtrip.py`,
  `python tests/visual_authoring_roundtrip.py`, `python tests/visual_accessibility_roundtrip.py`,
  `python tests/gift_export_roundtrip.py`.
- `python schema_validate.py` validates the grown schemas.
- Full suite `python tests/*.py` + `python itembank.py lint fixtures/sample_bank.md` after the plan.
</verification>

<success_criteria>
- Timeline works end to end through the unmodified protocol-1 envelope and interact/evidence machinery.
- Exact and tolerance-bounded timeline verdicts are deterministic with pinned borders; invalid/forged responses fail closed.
- The renderer is SVG+semantic-control with byte-identical serialization across pointer, keyboard, and controls; no canvas.
- GIFT refuses timeline items loudly; no lossy approximation.
</success_criteria>

<output>
Create `.planning/phases/999.1-advanced-visual-item-families/999.1-02-SUMMARY.md` when done.
</output>
