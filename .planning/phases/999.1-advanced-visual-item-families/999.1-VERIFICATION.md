# Phase 999.1 — Verification

**Phase:** 999.1-advanced-visual-item-families
**Verified:** 2026-08-11 on branch `gsd/phase-999.1` (worktree `.phase9991-wt`)

## Plan-by-plan evidence

| Plan | Shipped | Proof |
|---|---|---|
| 01 hotspot | Additive protocol-1 seam (allowlists, per-interaction scene builder, exact-id verdict, observation, SVG+radio renderer, schemas, fixture, lint codes) | `python tests/hotspot_roundtrip.py` PASS (8 sections); commit `bc82d59` |
| 02 timeline | Event placement + per-coordinate tolerance; SVG+select renderer; place/move actions; fixture; GIFT coverage | `python tests/timeline_roundtrip.py` PASS (8 sections); commit `0aac5ad` |
| 03 diagram + trace | Ordered node connection; element-wise polyline trace with fixed count; two SVG renderers; fixtures; two tests | `python tests/diagram_roundtrip.py` PASS, `python tests/trace_roundtrip.py` PASS; commit `a7ca774` |
| 04 phase verification | This document + UAT + HANDOFF; full-suite run | commits below |

## Requirement coverage (VIS-01..VIS-09, extended to the four new families)

| Requirement | Proof |
|---|---|
| VIS-01 one versioned declarative protocol, no bank JS | Protocol integer stays 1; the four families use the same envelope and the closed per-interaction scene allowlists; `_reject_visual_exec` scans scene/scoring; proven by `hotspot/timeline/diagram/trace_roundtrip` public-contract sections + `visual_authoring_roundtrip` |
| VIS-02 public/private projection | Every family's public item scanned against the sentinel set (`accepted`/`tolerance`/`partial_credit`/`scoring`/`model`/`why`/`reveal`/...); the trace accepted path is never projected (reference polyline is public scene data); proven in all four roundtrip tests + `visual_evidence_roundtrip` |
| VIS-03 modality-independent semantic response | Each family renderer is SVG + operable semantic controls (radio/selects) through ONE serializer; commit boundary tests (`visual_accessibility_roundtrip`) + family served tests |
| VIS-04 runtime-owned deterministic scoring, pinned borders | `score_response()` sole scorer (structural scan in `tests/scoring_roundtrip.py` green); hotspot/diagram exact-id; timeline borders 6/7/8 pass 5/9 fail; trace element-wise borders 1/2 pass / 1 fail |
| VIS-05 shared SVG/HTML slice | One `asVisual` dispatcher; four family renderers, no canvas; proven by served-page assertions in all four tests + `visual_accessibility_roundtrip` |
| VIS-06 committed-action evidence for agents | `select_hotspot`, `place_timeline_event`/`move_timeline_event`, `connect_diagram`, `place_trace_point`/`move_trace_point` append exactly one `visual_action` event with the locked field set; dedupe/conflict/refusal proven per family |
| VIS-07 keyboard/non-drag, accessible, reflow, canvas-fallback rule | Arrows/Enter/Space/Escape + semantic selects in every family renderer; named SVG via accessibility.description; `visual_accessibility_roundtrip` + model_ui no-leak scan green; no canvas introduced |
| VIS-08 spec/lint/schema/golden authoring contract | SPEC documents all four families (19 mentions); new/updated lint codes registered in `LINT_CODES`, SPEC lint table, and `lint_error.schema.json`; schemas extended; `fixtures/advanced_visual_bank.md` lints clean (12 items, 0 errors) |
| VIS-09 GIFT loud refusal | All six visual items in `visual_gift_bank.md` (plot, numberline, hotspot, timeline, diagram, trace) refused by item number with `gift.type_unsupported` in default and strict modes; `tests/gift_export_roundtrip.py` PASS |

## Commands and results (final state)

- `python tests/hotspot_roundtrip.py` — PASS
- `python tests/timeline_roundtrip.py` — PASS
- `python tests/diagram_roundtrip.py` — PASS
- `python tests/trace_roundtrip.py` — PASS
- Full suite chunked as CI does (`tests/*.py`): all PASS except four
  pre-existing/environmental failures, none caused by this phase (see below).
- `python itembank.py lint fixtures/sample_bank.md` — 6 items, 0 errors.
- `python itembank.py lint fixtures/advanced_visual_bank.md` — 12 items,
  0 errors.
- `python itembank.py guard .` — 0 offending files.
- `python itembank.py spec` — prints the extended visual contract.
- Schemas validated via `schema_validate.py` inside the roundtrip tests
  (`item.schema.json` visual branch, `visual_interaction.schema.json`
  request/observation/event for all four families).

## Pre-existing failures observed on this worktree (NOT caused by this phase)

| Test | Failure | Why it is not ours |
|---|---|---|
| `tests/evidence_roundtrip.py` | "index was not rebuilt at version 3 after the stale check" | evidence.py is untouched by this phase's diff; the assertion is in evidence's sqlite index code |
| `tests/gate_roundtrip.py` | "rows must differ only by context in log order, got [None, None]" | evidence `objective_history` context projection; unrelated to visual protocol |
| `tests/packaging_roundtrip.py` | `dist/itembank-sidecar-onedir` missing | requires the local `scripts/build_shell.ps1` build artifact; environmental |
| `tests/phase_062_audit.py` | aggregates the failing suites above; runs >2 min | re-runs other tests; not a phase-999.1 artifact |

## Hard constraint audit

- Renderers receive validated configuration only: every scene member is
  checked against the per-interaction allowlist and scanned for
  script-bearing content before a renderer sees it (REQUIREMENTS VIS-01,
  UI-SPEC.md:609). No bank-authored JavaScript path exists.
- One parser (`model.py`), one scorer (`runtime.score_response()`), one
  interact mutation path (`session.do_interact`), one append-only evidence
  writer (`evidence.append_event`) — none forked by this phase.

---
*Phase: 999.1-advanced-visual-item-families · VERIFICATION 2026-08-11*
