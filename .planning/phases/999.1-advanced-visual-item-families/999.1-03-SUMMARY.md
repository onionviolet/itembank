# 999.1-03 — SUMMARY

**Phase:** 999.1-advanced-visual-item-families · **Plan:** 03
**Date:** 2026-08-11 · **Status:** complete · **Wave:** 3

## What this plan set out to do

Add the `diagram` (ordered node connection, exact-id) and `trace`
(vector-valued polyline re-trace, fixed point count, per-point tolerance)
families to protocol 1, completing the four shipped families of the phase.
Diagram proves the second exact-id family; trace proves the heaviest response
shape — an ordered list of points compared element-wise.

## What shipped

| Artifact | File | Contains |
|---|---|---|
| Trace axes support | `runtime.py` | `visual_axes` gains the `trace` branch (reuses the plot's two-axis geometry); scoring/observation/domain/grid paths for diagram + trace already lived in the plan-01 seam |
| Diagram + trace renderers | `surfaces/quiz_page.py` | `vfNum` helper; `renderDiagram` (SVG nodes, click source→target, from/to selects, no canvas) and `renderTrace` (SVG grid + dashed public reference polyline, click places the next point in order, per-point x/y selects, Clear last point, no canvas) + `asVisual` dispatch. The reference polyline is scene data drawn for orientation only — committed/tentative paths start empty |
| Fixture | `fixtures/advanced_visual_bank.md` | Q10 diagram (`a`→`c`), Q11 exact 3-point V trace, Q12 tolerance-bounded valley trace (step 1/2, borders pinned at half-unit shifts) |
| GIFT coverage | `fixtures/visual_gift_bank.md`, `tests/gift_export_roundtrip.py` | diagram + trace items added; refusal count 4 → 6 |
| Contract tests | `tests/diagram_roundtrip.py`, `tests/trace_roundtrip.py` | Parser → public contract → scoring (ordered pair / element-wise tolerance with pinned borders) → observation → lint → interact/evidence (place then move full-path commits, dedupe, self-loop / short-path refusal) → schemas → served 12-item sitting → offline refusal |
| Family-test maintenance | `tests/hotspot_roundtrip.py`, `tests/timeline_roundtrip.py` | Fixture-size (12) and served-sitting expectations updated; correct-answer maps extended to all 12 items |

## Verification run

- `python tests/diagram_roundtrip.py`, `python tests/trace_roundtrip.py` — PASS.
- `python tests/hotspot_roundtrip.py`, `timeline_roundtrip.py`,
  `visual_roundtrip.py`, `visual_authoring_roundtrip.py`,
  `visual_evidence_roundtrip.py`, `visual_accessibility_roundtrip.py`,
  `gift_export_roundtrip.py`, `scoring_roundtrip.py`,
  `protocol_roundtrip.py`, `model_ui_roundtrip.py`, `lesson_roundtrip.py` —
  all PASS (06.1 seam + hotspot + timeline unchanged; one-scorer scan green).
- `python itembank.py lint fixtures/advanced_visual_bank.md` — 0 errors
  (12 items).

## Bugs found and fixed during execution

1. `visual_axes` returned None for the `trace` interaction, so trace scenes
   failed axis validation — added the trace branch (plot geometry reuse).
2. `model_ui_roundtrip` flagged the word "accepted" in two new renderer
   comments (forbidden glyph "accept" in the served page) — reworded to
   "answer path" in both the block comment and the inline comment.

## Gaps (deferred, per RESEARCH)

- Geometry/construction, dense-simulation-style, diagram label/match
  variants, and free-length/multi-segment trace: named gaps in
  RESEARCH/HANDOFF.
