---
phase: 20-extensible-ui-foundation-and-interaction-clarity-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemas/settings.schema.json
  - surfaces/settings.py
  - surfaces/presentation.py
  - surfaces/home.py
  - surfaces/daemon.py
  - surfaces/study.py
  - surfaces/quiz_page.py
  - surfaces/lesson.py
  - surfaces/theme.py
  - surfaces/looks.py
  - surfaces/ia.py
  - surfaces/palette.py
  - tests/home_roundtrip.py
  - tests/component_primitives_roundtrip.py
  - tests/presentation_profiles_roundtrip.py
  - tests/visual_accessibility_roundtrip.py
autonomous: false
requirements:
  - FLOW-01
  - FLOW-02
  - CAP-02
  - ACTIVITY-01
  - ACTIVITY-03
  - A11Y-01
  - VISUAL-01
  - VISUAL-02
must_haves:
  truths:
    - "D-01: The phase changes hierarchy and interaction comprehension, not only appearance."
    - "D-02: Semantic roles, shared primitives, presentation profiles, and appearance themes remain independent layers."
    - "D-03: Field Guide and Trajectory Deck share one renderer and identical behavior."
    - "D-04: Activity purpose and response format remain separate learner-facing concepts."
    - "D-05: Navigation and profile switching preserve route, session, history, focus, locator, and unsaved work."
    - "D-06: Empty, loading, unavailable, conflict, interruption, pending, invalid, and recovery remain distinct states."
    - "D-07: One bounded capability adapter proves the seam without a general loader."
    - "D-08: Both profiles pass the same comparative viewport, state, and accessibility-oriented matrix."
    - "D-09: Every inherited audit finding has one Phase 20 disposition and owner."
  artifacts:
    - path: schemas/settings.schema.json
      provides: Stable presentation profile values and migration contract
    - path: surfaces/presentation.py
      provides: Shared semantic primitives and two profile recipes
    - path: tests/presentation_profiles_roundtrip.py
      provides: Profile parity, activity separation, migration, fallback, and recovery evidence
---

<objective>
Build one shelf to course overview to lesson to practice slice in two genuinely different presentation profiles over identical semantic state. Make the learner's location, task purpose, response format, disclosure state, and next action obvious without changing parser, scorer, routes, evidence, or canonical content.
</objective>

<execution_boundary>
This is one coarse vertical plan. Do not migrate Agent, Sources, Map, Evidence, authoring, export, or every legacy quiz layout. Do not add an item type, route table, state store, component framework, dynamic loader, or external package mechanism. Stop at the human visual and screen-reader checkpoint after every automated gate passes.
</execution_boundary>

<tasks>

<task type="execute">
  <name>Task 1: Freeze profile, semantic-role, and activity-label contracts in tests</name>
  <read_first>
    <file>.planning/phases/20-extensible-ui-foundation-and-interaction-clarity-pass/20-CONTEXT.md</file>
    <file>.planning/phases/20-extensible-ui-foundation-and-interaction-clarity-pass/20-UI-SPEC.md</file>
    <file>schemas/settings.schema.json</file>
    <file>surfaces/settings.py</file>
    <file>surfaces/presentation.py</file>
    <file>tests/home_roundtrip.py</file>
    <file>tests/component_primitives_roundtrip.py</file>
    <file>.planning/phases/20-extensible-ui-foundation-and-interaction-clarity-pass/20-AUDIT-CROSSWALK.md</file>
    <file>surfaces/study.py</file>
    <file>surfaces/quiz_page.py</file>
  </read_first>
  <action>Reconcile the crosswalk against live code, then add failing contract tests before implementation. Repair the verified baseline defects first: hidden Study action groups must have zero layout and no focusable descendants, table and build selects must use 44px targets and 16px action text, and walkthrough buttons must use the shared control treatment. Require stable values `field-guide` and `trajectory-deck`, missing-value migration, visible unknown-value fallback, and preview-without-save. Define semantic roles for application level, course identity, task purpose, response format, disclosure, position, source, pending, unavailable, conflict, focus, and recovery. Assert that diagnostic, practice, remediation, exam, and drill purpose labels remain separate from `mc`, `multi`, `table`, `build`, `dnd`, `short`, `visual`, and `check` response labels. Assert that the capitalized durable-job Activity area remains distinct from learner activities. Assert identical hrefs, form actions, names, public content, and runtime-controlled disclosure across profiles.</action>
  <verify>python tests/presentation_profiles_roundtrip.py</verify>
  <acceptance_criteria>
    <criterion>The new suite fails before implementation for missing profile settings and profile rendering.</criterion>
    <criterion>The suite names every activity-purpose value and every shipped response type.</criterion>
    <criterion>The suite rejects any profile-dependent route, form action, response name, or disclosure text.</criterion>
    <criterion>The live crosswalk gives every adopted audit finding one task and every routed finding one external owner.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Implement the shared semantic view and recoverable profile setting</name>
  <read_first>
    <file>schemas/settings.schema.json</file>
    <file>surfaces/settings.py</file>
    <file>surfaces/presentation.py</file>
    <file>surfaces/home.py</file>
    <file>surfaces/daemon.py</file>
    <file>surfaces/lesson.py</file>
    <file>surfaces/quiz_page.py</file>
    <file>surfaces/theme.py</file>
    <file>surfaces/looks.py</file>
    <file>surfaces/palette.py</file>
    <file>prototypes/17c-finalists/field-guide.html</file>
    <file>prototypes/17c-finalists/trajectory-deck.html</file>
  </read_first>
  <action>Add `presentation_profile` to the settings contract with stable supported values and migration. Build one profile-neutral semantic view model from existing home, course, lesson, session, and `public_item` state. Add only the primitives exercised by shelf, course overview, lesson, and practice. Render both profiles as composition recipes over those primitives. Keep appearance tokens independent. Apply the inherited five-size typography, spacing, reading-measure, control, and role-based radius contracts. Replace repeated inventory cards with title-led rows where no interaction or state boundary requires a container. Reduce Quiz to one runtime-permitted help entry and do not expose locked reveal vocabulary before entitlement. Add a compact settings preview that changes derived presentation without saving or navigating, retains every existing look, and does not reset theme or accent. Unknown saved values must show a notice and use the safe `field-guide` fallback. Do not copy prototype literal colors into behavior or route handlers.</action>
  <verify>python tests/home_roundtrip.py && python tests/component_primitives_roundtrip.py && python tests/presentation_profiles_roundtrip.py</verify>
  <acceptance_criteria>
    <criterion>Both profiles render the same course, session, evidence, links, controls, and disclosure state.</criterion>
    <criterion>The page exposes application level, course identity, task purpose, response format, disclosure, position, and next action as separate semantic regions.</criterion>
    <criterion>Switching profiles changes no URL, session id, attempt count, evidence bytes, or unsaved-field value.</criterion>
    <criterion>Removing one profile registration leaves canonical course, bank, session, and evidence files unchanged and activates a visible fallback.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Complete navigation, interruption, and degraded-state logic</name>
  <read_first>
    <file>.planning/notes/2026-09-07-home-and-transition-ui-scope.md</file>
    <file>surfaces/daemon.py</file>
    <file>surfaces/home.py</file>
    <file>surfaces/session.py</file>
    <file>surfaces/ia.py</file>
    <file>tests/serve_roundtrip.py</file>
    <file>tests/daemon_roundtrip.py</file>
  </read_first>
  <action>Implement the transition rows exercised by the slice: Home start and exact resume, course-to-lesson, lesson-to-practice, practice correct, wrong, retry, skip, stop, interruption, and later return. Preserve course identity, session id, reading locator, meaningful focus, scroll cue, browser history, and unsaved-work warning. Browser Back must never submit or duplicate an attempt. Render populated, empty, loading, unavailable, conflicted, interrupted, pending-review, invalid-profile, and recovery states with distinct text and next actions. Reuse 16B's offline help and degraded-state language where its trigger matches. Empty areas link only to already supported actions. Keep static and no-script form paths useful.</action>
  <verify>python tests/presentation_profiles_roundtrip.py && python tests/serve_roundtrip.py && python tests/daemon_roundtrip.py</verify>
  <acceptance_criteria>
    <criterion>Every transition in the representative slice has one asserted destination and preserved-context set.</criterion>
    <criterion>Stop and later return resume the same runtime session at the exact cursor.</criterion>
    <criterion>Browser Back produces zero additional response events.</criterion>
    <criterion>Unknown, unavailable, empty, interrupted, conflicted, and pending states have different visible labels and recovery actions.</criterion>
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Comparative visual, responsive, and accessibility gate</name>
  <read_first>
    <file>tools/visual_qa.py</file>
    <file>tests/visual_accessibility_roundtrip.py</file>
    <file>.planning/phases/20-extensible-ui-foundation-and-interaction-clarity-pass/20-UI-SPEC.md</file>
  </read_first>
  <action>Render the identical representative fixture in both profiles at 1280, 768, 375, and 320 CSS pixels with light, dark, OLED, high-contrast, a custom accent, reduced-motion, no-script, offline, 200 percent text, 400 percent zoom, and model-unavailable conditions. Include Study front, revealed, and learn states, Quiz before answer and each permitted hint state, table and build responses, populated and empty course areas, open search, walkthrough controls, a long title, a wide table, a software-keyboard viewport, and the bottom safe area. Run automated keyboard order, visible focus, hidden-descendant, 44px target, 16px action-text, reflow, contrast, semantic parity, settings migration, invalid fallback, and removal recovery checks. Then present both profiles to Weibao for the reserved visual and screen-reader-oriented acceptance. Record whether each profile creates a distinct useful hierarchy and whether the learner can name page level, course, task, expected response, and next action without scanning the whole page.</action>
  <verify>python tests/visual_accessibility_roundtrip.py && python scripts/preflight.py --quick</verify>
  <acceptance_criteria>
    <criterion>Every automated matrix row passes for both profiles, or the plan remains open with the exact failing row.</criterion>
    <criterion>No automated result is recorded as human accessibility certification.</criterion>
    <criterion>Weibao accepts, rejects, or requests revision for each profile and the activity-purpose versus response-format separation.</criterion>
    <criterion>Quick preflight exits 0 before the checkpoint is offered.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
- Profile values are untrusted settings input. Validate against the supported registry and escape the unsupported value in fallback copy.
- Profile selection cannot alter form targets, hidden session identifiers, disclosure state, or assessment payloads.
- No-script and offline fallbacks must not embed keyed content absent from `runtime.public_item`.
- Unsaved-work prompts must not serialize private response text into URLs, logs, or portable diagnostics.
</threat_model>

<source_audit>
CONTEXT D-01 and D-02 are covered by Tasks 1 and 2. D-03 is covered by Tasks 1, 2, and 4. D-04 is covered by Tasks 1 and 2. D-05 and D-06 are covered by Task 3. D-07 is covered by Task 2 and the execution boundary. D-08 is covered by Task 4. D-09 is covered by Task 1 and `20-AUDIT-CROSSWALK.md`. FLOW-01, FLOW-02, CAP-02, ACTIVITY-01, ACTIVITY-03, A11Y-01, VISUAL-01, and VISUAL-02 are named in frontmatter and exercised by the verification matrix.
</source_audit>

<verification>
- `python tests/home_roundtrip.py`
- `python tests/component_primitives_roundtrip.py`
- `python tests/presentation_profiles_roundtrip.py`
- `python tests/visual_accessibility_roundtrip.py`
- `python tests/serve_roundtrip.py`
- `python tests/daemon_roundtrip.py`
- `python3 tests/stylesheet_roundtrip.py`
- `python3 tests/presentation_roundtrip.py`
- `python3 tests/theme_roundtrip.py`
- `python3 tests/ia_route_roundtrip.py`
- `python3 tests/surface_roundtrip.py`
- `python3 tests/hint_roundtrip.py`
- `python scripts/preflight.py --quick`
- At the accepted revision, `python scripts/preflight.py`
</verification>

<success_criteria>
- Field Guide and Trajectory Deck present distinct composition and emphasis over identical semantic and runtime state.
- Activity purpose, response format, disclosure, and position are distinct learner-facing facts.
- The representative transition graph preserves exact resume and never duplicates an attempt.
- Every required degraded state names a next safe action.
- Removing either profile changes no canonical learner or course data.
- Automated gates pass and human visual and accessibility authority is recorded without being impersonated.
</success_criteria>

<must_haves>
  <truths>
    <truth>D-01: The phase changes hierarchy and interaction comprehension, not only appearance.</truth>
    <truth>D-03: Two presentation profiles share one renderer and identical behavior.</truth>
    <truth>D-04: Activity purpose and response format remain separate concepts.</truth>
    <truth>D-05: Profile switching and navigation preserve route, session, history, focus, locator, and unsaved work.</truth>
    <truth>D-07: The slice adds no general plugin loader or authority-bearing extension.</truth>
  </truths>
  <artifacts>
    <artifact>schemas/settings.schema.json contains the stable `presentation_profile` contract.</artifact>
    <artifact>surfaces/presentation.py contains shared semantic primitives and two profile recipes.</artifact>
    <artifact>tests/presentation_profiles_roundtrip.py proves parity, separation, migration, fallback, and recovery.</artifact>
  </artifacts>
</must_haves>
