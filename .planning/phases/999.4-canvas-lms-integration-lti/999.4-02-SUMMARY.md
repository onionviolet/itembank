---
phase: 999.4-canvas-lms-integration-lti
plan: 02
subsystem: lti
tags: [lti-1.3, deep-linking, embedded-player, public-item, frame-ancestors, privacy-copy]

# Dependency graph
requires:
  - phase: 999.4-canvas-lms-integration-lti (plan 01)
    provides: the verified-launch spine (OIDCLoginHandler/LTIHandler, verify_id_token, load_registration, call_json_api) this plan's two surfaces sit on
provides:
  - "surfaces/lti.py: handle_deep_link_request/picker_page/deep_link_response (LtiDeepLinkingRequest -> instructor picker, one objective per assignment), handle_resource_link/player_page (LtiResourceLinkRequest -> embedded quiz surface), frame_ancestors_csp, maybe_passback stub + completion_line, the launch-context token plumbing (BOOT.lti_ctx -> session_id)"
  - "surfaces/quiz.py + surfaces/quiz_page.py: additive page_for(lti_framing=, boot_extra=) and the __LTI_FRAMING__ slot + SERVED_JS objective/lti_ctx passthrough + LTI completion hook -- every existing caller byte-identical"
  - "tests/lti_roundtrip.py: plan-02 fixtures -- deep-link round trip, public_item byte-equality, no-sensitive-content picker, frame-ancestors, refusal lines, completion framing, verbatim copy"
affects: [999.4-03-passback-and-hardening]

actuals:
  tokens: 0 (measured by the runner)
  tasks: 3
  commits: 1

tech-stack:
  added: []
  patterns:
    - "The launch handler resolves the registration from the server-issued state and the message type from the VERIFIED claims -- the state is server-issued, the message_type is signature-attested"
    - "Launch-context plumbing: one-time BOOT.lti_ctx token -> session_id binding, so completion-time passback reaches the verified claims without any learner-controllable channel"
    - "The player is the quiz surface (page_for serve path) -- no new renderer; the picker is a thin surface_shell page listing objective ids/titles only"

key-files:
  created: []
  modified:
    - surfaces/lti.py
    - surfaces/quiz.py
    - surfaces/quiz_page.py
    - tests/lti_roundtrip.py

key-decisions:
  - "The picker's objectives are the distinct [OBJECTIVE:] values per bank -- the exact set handle_api_start/selection.select resolve (D-07's operative clause 'the same objective ids handle_api_start resolves'); the UI-SPEC's '[OBJ:] ids' phrasing names provenance directives that handle_api_start cannot resolve, so D-07 wins"
  - "Objective id == title in the picker rows: an [OBJECTIVE:] reference is self-describing ('Distribution / residual maintenance'), so no second title source exists"
  - "The completion line: not-configured / refused / unreachable all render the verbatim refusal line, because in every one of those states the score was genuinely not sent (D-09 truthfulness); the OK line renders only on a real AGS success signal"
  - "maybe_passback() is a clear-interface stub (status, reason) so plan 03 fills it without touching the copy"
  - "The player page reuses page_for's serve projection with an additive lti_framing/boot_extra -- the daemon path stays byte-identical (serve_roundtrip green)"

verification:
  - "python tests/lti_roundtrip.py -- 18 checks green (deep-link picker + empty registry + response shape, player public_item byte-equality + explain gate, refusals, completion framing, verbatim copy)"
  - "python tests/serve_roundtrip.py -- the reused quiz projection is green"
  - "python tests/daemon_roundtrip.py -- 60 checks green (route scope unchanged; transient socket flake observed under concurrent harness sockets, 3/3 re-runs green)"
  - "python tests/lesson_roundtrip.py + tests/protocol_roundtrip.py -- green"
  - "The picker DOM contains no stem/key/count (fixture asserts absence of known bank substrings); the player page leaks no reveal field before a recorded response"
---

# Plan 999.4-02 Summary — the instructor picker and the embedded learner player

## What landed

The phase's two real surfaces on top of plan 01's verified-launch spine.
Deep linking gives the instructor ownership of which objective runs (D-07);
the embedded player proves the public_item-only rule in the LMS context
(D-03) and the privacy statement (D-09).

## Task map

| Task | Outcome |
|---|---|
| 02-1 Deep-link picker | `LtiDeepLinkingRequest` launch -> picker page listing one 44px row per `[OBJECTIVE:]` value across the registered banks (ids/titles only — no stems, no keys, no counts), the verbatim purpose line, `Link this assignment`, `Cancel`; `deep_link_response` returns exactly one ltiResourceLink carrying the objective custom param (empty list on Cancel); empty registry renders the empty-registry line; banks with no objectives refuse by name |
| 02-2 Embedded learner player | `LtiResourceLinkRequest` with a resolvable custom objective -> the existing quiz serve page for that objective (`page_for` serve projection, no new renderer) with `frame-ancestors <lms-origin>` CSP derived from the registration issuer; the session starts through the in-process JSON-command wrap; the payload is `runtime.public_item()` byte-for-byte with no reveal field before a recorded response, and the evidence-log explain gate holds |
| 02-3 Privacy + completion framing | The D-09 privacy line renders verbatim on the learner surface; the completion framing renders the refusal line when passback is not configured and the OK line only on the (stubbed) AGS success signal; `itembank lti` help/doctor carry the statement; a copy fixture pins the section-4 strings |

## Verification evidence

- `python tests/lti_roundtrip.py` — 18/18 green.
- `python tests/serve_roundtrip.py`, `python tests/daemon_roundtrip.py`,
  `python tests/lesson_roundtrip.py`, `python tests/protocol_roundtrip.py` —
  green; the reused quiz projection and the route scope are unchanged.
- The public_item byte-equality fixture and the explain-gate fixture prove
  D-03 holds in the LMS context.

## Deferred to plan 03

- The real AGS passback signal replaces the `maybe_passback` stub.
- TLS bind test, hosting doc, dependency pin record, manual Canvas checklist.
