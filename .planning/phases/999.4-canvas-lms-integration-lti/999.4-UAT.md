---
status: testing
phase: 999.4-canvas-lms-integration-lti
source: [999.4-VERIFICATION.md, 999.4-VALIDATION.md]
started: 2026-08-11
updated: 2026-08-11
---

## Current Test

number: 1
name: Real Canvas install, launch, deep-link assignment, learner completion, AGS passback
expected: |
  Follow docs/lti-hosting.md: register the tool in Canvas (developer key with
  the public JWK, OIDC initiation URL, launch URL; record the deployment_id
  in lti.platforms[].deployments), enable the LTI bind (stdlib TLS or reverse
  proxy), create an external-tool assignment via deep-link selection of one
  objective, launch it as a learner, complete the sitting, and open the
  assignment gradebook: exactly one score, correct value, no duplicate on a
  re-run, and no reveal before answering.
awaiting: user response

## Tests

### 1. Real Canvas install, launch, deep-link assignment, learner completion, AGS passback
expected: |
  Install the tool in a live Canvas instance (developer key with the tool's
  public JWK, OIDC initiation URL `<public_base_url>/lti/login`, launch URL
  `<public_base_url>/lti/launch/<bank-stem>`), record the deployment_id,
  enable `lti.enabled` with TLS (or a reverse proxy), deep-link one objective
  into an assignment, launch as a learner, complete the sitting, and confirm
  the gradebook shows exactly one score with the correct value and that a
  re-run does not duplicate it. The learner page shows no key/why before
  answering and renders the verbatim privacy line.
result: [pending]

### 2. Pending-prose boundary on a live bank
expected: |
  Run a sitting containing a `short` item on the live install; verify the
  gradebook shows `PendingManual` (or no numeric publish per the R-02
  fallback) and that no auto-grade ever appears for the prose. Also confirm
  the completion line matches the passback outcome (OK line only on success).
result: [pending]

### 3. Unregistered / hostile platform refused by name
expected: |
  Point a launch at the tool from a Canvas site whose issuer/client_id is not
  in `lti.platforms`, or forge a launch outside Canvas; confirm the exact
  refusal lines (`This Canvas site is not registered...` /
  `This launch could not be verified...`) render and no session starts.
result: [pending]

### 4. frame-ancestors allows only the LMS
expected: |
  With the assignment open in Canvas, inspect the player response header:
  `Content-Security-Policy: frame-ancestors <lms-origin>` for the registered
  issuer; confirm a second origin cannot frame the player.
result: [pending]

## Summary

total: 4
passed: 0
pending: 4 (all manual-only, require a live Canvas instance per R-01)

## Approval

pending — the automated spine (24 checks, fake-platform harness) is green;
the manual Canvas items above are the R-01 fallback and cannot be faked.
