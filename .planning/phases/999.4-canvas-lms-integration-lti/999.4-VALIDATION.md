---
phase: 999.4
slug: canvas-lms-integration-lti
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-11
---

# Phase 999.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — direct Python script execution (`tests/*_roundtrip.py`), stdlib only (plus the optional `cryptography`/`PyJWT` pair under the plan-01 optional-import guard) |
| **Config file** | none |
| **Quick run command** | `python tests/lti_roundtrip.py` |
| **Full suite command** | `python tests/lti_roundtrip.py && python tests/daemon_roundtrip.py && python tests/serve_roundtrip.py && python tests/evidence_roundtrip.py && python tests/protocol_roundtrip.py && python tests/scoring_roundtrip.py && python tests/agent_roundtrip.py` |
| **Estimated runtime** | ~{N} seconds — measure at Wave 0 |

---

## Sampling Rate

- **After every task commit:** Run the quick run command
- **After every plan wave:** Run the full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** {N} seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 999.4-01-01 | 01 | 1 | LTI-06, LTI-07 | T-994-SC | Registry + optional crypto guard refuse by name; loopback default untouched | unit | `python tests/lti_roundtrip.py` | ✓ | ✅ green |
| 999.4-01-02 | 01 | 1 | LTI-01 | T-994-01/02/03/06 | JWKS verify + iss/aud/nonce/exp/deployment; every invalid launch refused by name | unit | `python tests/lti_roundtrip.py` | ✓ | ✅ green |
| 999.4-01-03 | 01 | 1 | LTI-02, LTI-01 | T-994-04/05 | OIDC dance round-trip; launch calls daemon handlers in-process; no /api/* change, no second scorer | unit | `python tests/lti_roundtrip.py && python tests/daemon_roundtrip.py` | ✓ | ✅ green |
| 999.4-02-01 | 02 | 2 | LTI-04 | T-994-07/09 | Deep-link picker: one objective per assignment, no sensitive content, verbatim copy | unit | `python tests/lti_roundtrip.py` | ✓ | ✅ green |
| 999.4-02-02 | 02 | 2 | LTI-03 | T-994-08/10/11 | Embedded player = quiz surface; public_item-only; evidence-log explain gate; frame-ancestors | unit | `python tests/lti_roundtrip.py && python tests/serve_roundtrip.py` | ✓ | ✅ green |
| 999.4-02-03 | 02 | 2 | LTI-03 (D-09) | T-994-12 | Privacy line verbatim on the learner surface and in `itembank lti` help | unit | `python tests/lti_roundtrip.py` | ✓ | ✅ green |
| 999.4-03-01 | 03 | 3 | LTI-05 | T-994-13/14/15 | AGS client: opt-in, completion-only, idempotent, score from the runtime report | unit | `python tests/lti_roundtrip.py` | ✓ | ✅ green |
| 999.4-03-02 | 03 | 3 | LTI-05 (D-04) | T-994-16/18 | PendingManual / no-publish for pending prose; completion lines verbatim; evidence byte-identical | unit | `python tests/lti_roundtrip.py && python tests/evidence_roundtrip.py` | ✓ | ✅ green |
| 999.4-03-03 | 03 | 3 | LTI-06, LTI-07 | T-994-17, T-994-SC | Opt-in TLS bind / proxy posture; dependency pins + license review; checklist exists | unit | `python tests/lti_roundtrip.py && python tests/serve_roundtrip.py && python tests/daemon_roundtrip.py` | ✓ | ✅ green |

*Status: ⬜ pending · ✓ green · ✗ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `tests/lti_roundtrip.py` created with the fake-platform harness (plan 01) -- 24 checks green at phase end; measured runtime ~40s

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real Canvas install of the tool (developer key, public JWK or JWKS URL, OIDC initiation URL, launch URL; deployment id granted) | LTI-06 / R-01 | Requires a live Canvas instance the automated fake platform cannot stand in for | Follow `docs/lti-hosting.md`: register the tool, record the deployment_id in `settings.lti.platforms`, enable the LTI bind with TLS (or reverse proxy), run the launch from a Canvas course |
| Real Canvas learner launch → embedded player renders the linked objective | LTI-03, LTI-04 | Live LMS iframe + session flow | Create an external-tool assignment via deep-link selection of an objective; launch it as a learner; complete the sitting; verify no reveal before answering |
| Real Canvas AGS passback → grade appears in the assignment gradebook | LTI-05 | Live AGS line-item/score behavior | Complete the sitting; open the assignment's gradebook; verify one score, correct value, and that a re-run does not duplicate it (idempotence) |
| Pending-prose boundary on a live bank | LTI-05 (D-04) | Live rubric state | Run a sitting containing a `short` item; verify the gradebook shows `PendingManual` (or no numeric publish per R-02 fallback) and no auto-grade |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < {N}s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
