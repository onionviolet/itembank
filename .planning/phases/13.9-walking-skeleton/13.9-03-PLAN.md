---
phase: 13.9-walking-skeleton
plan: 03
type: execute
wave: 3
depends_on: ["13.9-02"]
files_modified:
  - .planning/phases/13.9-walking-skeleton/13.9-CALIBRATION.md
  - .planning/phases/13.9-walking-skeleton/13.9-DECISIONS.md
  - .planning/READINESS-AUDIT-14A.md
  - .planning/STATE.md
  - <course-root>/course.md
autonomous: false
requirements: [READINESS-AUDIT-14A A9]
must_haves:
  truths:
    - "Weibao read the direct-reading treatment and sat the bank through itembank serve, end to end, on the shipped surfaces."
    - "Every response landed in the one evidence store and is queryable per objective; no second store or path exists."
    - "13.9-CALIBRATION.md names the corpus by pointers, counts, and hashes only; no item text, stem, or key appears in the repository."
    - "A9's checkboxes are marked with dated citations to the artifacts that satisfy them, or a waiver with a reason."
    - "At least one later plan obligation (14B, 15A, or warning calibration) is redirected by name to this corpus."
  artifacts:
    - ".planning/phases/13.9-walking-skeleton/13.9-CALIBRATION.md"
    - "A9 checkboxes updated in READINESS-AUDIT-14A.md"
    - "STATE.md walking-skeleton completion record"
  key_links:
    - "itembank guard is the backstop: run it on this repo after writing CALIBRATION; if it flags the file, the file is wrong, not the guard."
    - "Short items stay pending until marked; the report distinguishes auto-graded from pending, and the calibration counts must state both denominators."
    - "The session id prints in the serve startup banner; capture it then, because reconstructing it later means digging through _attempts."
---

<objective>
Walk the skeleton: Weibao studies the reading, sits the bank, and the recorded
evidence becomes the named calibration corpus later plans cite. Then close the
loop administratively: A9 checked with citations, STATE updated. This
completes `READINESS-AUDIT-14A.md` A9 and ROADMAP Phase 13.9.

Decisions already made: the sitting uses `itembank serve` (the graded sitting
surface), practice mode defaults, no model backend required (the loop must
degrade, never block; a hosted hint is optional flavor, not a dependency).
</objective>

<context>
@.planning/phases/13.9-walking-skeleton/13.9-DECISIONS.md
@.planning/phases/13.9-walking-skeleton/13.9-02-SUMMARY.md
@.planning/READINESS-AUDIT-14A.md   (section A9)
@<course-root>/course.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Weibao reads, then sits the bank</name>
  <files>.planning/phases/13.9-walking-skeleton/13.9-DECISIONS.md, <course-root>/course.md</files>
  <action>
(1) Tell Weibao, in this order: first do the reading entry in course.md
(`Read src-NN <locator>`), then sit the quiz. Start the sitting for him:

```bash
python itembank.py serve <course-root>/<slug>_bank.md
```

(2) Capture the `session_id` from the startup banner into
`13.9-DECISIONS.md` immediately, with the date.

(3) Weibao answers every item himself. The executor does not answer, suggest,
or reveal anything during the sitting; the runtime's own hint ladder
(`stumped` control) is his to use or not.

(4) Afterward, ask him for a two-sentence reaction (was it worth sitting;
what was most broken) and record it verbatim in `13.9-DECISIONS.md`. That
reaction is skeleton evidence too.

(5) If the bank contains short items, mark them now with Weibao
(`itembank mark --session <id> --file <marks.ndjson>` per the README
evidence section), so the corpus has settled verdicts.
  </action>
  <verify>
An attempt record exists under `<course-root>/_attempts/`, the session id is
recorded, and `python itembank.py report` on the session shows every item
either auto-graded or marked, none silently missing.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Evidence verification and the calibration record</name>
  <files>.planning/phases/13.9-walking-skeleton/13.9-CALIBRATION.md</files>
  <read_first>
README "Evidence and marking" command list. The A9 calibration clause: the
corpus replaces at least one synthetic-fixture obligation.
  </read_first>
  <action>
(1) Verify the one-store truth per objective:

```bash
python itembank.py evidence --objective obj-01
```

Repeat for every obj-NN. Expected: each returns the responses from the
sitting; a query for an objective with no items returns empty rather than
erroring.

(2) Write `13.9-CALIBRATION.md` with exactly these sections:
  - **What this is:** the walking-skeleton calibration corpus, real learner
    evidence from one real sitting, named by A9.
  - **Pointers only:** course-root path, bank filename, bank SHA-256, lesson
    filename and SHA-256, session id, sitting date. State explicitly: no item
    text, stem, option, or key may ever be copied into this file or this
    repository.
  - **Counts:** items served, auto-graded, human-marked, correct/incorrect
    per objective, hint tiers opened (from the report and evidence queries),
    with both denominators stated wherever pending marks exist.
  - **Named consumers:** (a) the Phase 14B three-domain graph tracer uses
    this course's objective map as one of its domains; (b) the 15A
    four-subject recommendation review includes this course's treatment
    decisions and outcomes; (c) the Phase 3.2-style warning-calibration
    obligation counts this bank in its corpus. Each consumer's plan must cite
    this file rather than minting a synthetic fixture for what it covers.

(3) Run the backstop:

```bash
python itembank.py guard .
```

in the repository root. Expected: exit 0. If it flags 13.9-CALIBRATION.md,
remove the flagged content; the guard wins.
  </action>
  <verify>
Guard exits 0; every count in CALIBRATION reproduces from a re-run of the
report and evidence commands; the file contains no line longer than a pointer,
hash, count, or consumer sentence.
  </verify>
</task>

<task type="auto">
  <name>Task 3: A9 closure and state roll-up</name>
  <files>.planning/READINESS-AUDIT-14A.md, .planning/STATE.md, <course-root>/course.md</files>
  <action>
(1) In `READINESS-AUDIT-14A.md` section A9, mark each checkbox `[x]` with a
trailing dated citation, for example:
`(satisfied 2026-08-NN: 13.9-01..03, see 13.9-CALIBRATION.md)`. The 13.5
checkbox cites 13.5's actual state at that date; if 13.5 waves 3+ are not yet
done and the sitting happened anyway, write a waiver with the reason Weibao
accepted, never a silent check.

(2) In `STATE.md`, add a dated completion block: skeleton walked, session id,
corpus file, Weibao's verbatim two-sentence reaction, and the standing rule
this unlocks: "14B-or-later freezes may now commit; their plans must cite
13.9-CALIBRATION.md where it covers them."

(3) Final Log line in course.md: `sitting complete, evidence recorded,
corpus registered`.
  </action>
  <verify>
Grep `READINESS-AUDIT-14A.md` for `- [ ]` within section A9: expected zero
unchecked boxes without a waiver line. STATE.md block present. No file
outside files_modified changed (`git status` shows only the three repo files).
  </verify>
</task>

</tasks>

<out_of_scope>
No fixes to defects the sitting surfaces (record them in DECISIONS as findings
for 13.5 or an owning plan; the skeleton observes, it does not repair). No
course.md schema work, no new commands, no model backend setup, no second
sitting. No copying any sitting content into the repository.
</out_of_scope>

<summary_obligations>
`13.9-03-SUMMARY.md` records: the sitting date and session id, the calibration
counts table copied from CALIBRATION, Weibao's verbatim reaction, every defect
observed with its routed owner, and the A9 closure state including any waiver.
</summary_obligations>
