---
phase: 13.9-walking-skeleton
plan: 02
status: complete
completed: 2026-08-16
---

# Plan 13.9-02 summary

## Artifacts

Private course root: `C:\Users\wayba\Downloads\Personal\itembank-courses\emt-unit-1`.

- `lesson_ems-practice-authority.md`: an external plain-Markdown lesson covering obj-01 and obj-03, with five glossary terms and one key block.
- `ems-practice-authority_bank.md`: ten source-cited items linked to the lesson and direct-reading assignments.
- `course.md`: updated with artifact pointers, reading entries, dispositions, and validation log.

No private lesson or bank content was written into this repository.

## Item mix

- Total: 10
- Multiple choice: 6
- Multiple response: 4
- Difficulty: 6 application, 3 analysis, 1 recall
- Correct positions across multiple-choice items: A 2, B 1, C 1, D 2

## Objective coverage

```text
4 objectives
  obj-01  Q1, Q2, Q3
  obj-02  Q7, Q8
  obj-03  Q4, Q5, Q6
  obj-04  Q9, Q10
```

Every item has a stable `[ID:]` and `[HASH:]`. Re-running `id-assign` assigned zero IDs and updated zero hashes.

## Accepted lint warnings

- Ten `OBJECTIVE has no subject prefix` warnings: Phase 13.9 explicitly freezes the temporary objective identifiers as `obj-01` through `obj-04`, and the parallel `[OBJ:]` directives resolve them through the source registry. Phase 14B owns durable hierarchical objective identity and may supersede this stub.
- One external `[!KEY]` block lacks an ID: `id-assign` writes only the bank and does not mutate the separately authored lesson source. The lesson remains renderable and the warning is retained rather than hand-minting an ID in violation of the identity contract.

Final lint state: 10 items, 0 errors, 11 understood warnings.

## Render and degradation checks

- `lesson` rendered four sections successfully.
- `build` rendered all ten items successfully.
- With the external lesson temporarily renamed, `lesson` produced a zero-section degraded document without a traceback and named the authored path through the bank directive.
- Restoring the lesson returned the reader to four sections.
- Derived preview HTML files were removed after verification and are recoverable by rerunning `lesson` or `build`.

## Integrity and uncertainty

- The source file remained 19,574 bytes with SHA-256 `ED4250DC7248B241690D102DE5C0D29068E0F359AC3D36378CA4C68A603B1E4C`; before and after inventory snapshots match.
- Lint reported no copied-run or structural-similarity error.
- Accessibility and perceived teaching quality remain human-verification concerns; this plan proves deterministic parsing and rendering only.

## Deviations

- The plan's source-registry instruction said to mirror ID and path only. Objective IDs were also registered because the shipped `[OBJ:]` resolver uses the same registry and otherwise rejects every objective link.
- The Windows course root used a local derived preview path instead of `/tmp`; the preview was removed after the same validation.
