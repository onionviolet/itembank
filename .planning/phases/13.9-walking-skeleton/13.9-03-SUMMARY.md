# 13.9-03 summary

Written 2026-08-25. This summary exists for the two reasons `EXEC-CONTEXT.md`
allows one: part of the plan is not built, and a measured fact contradicted the
plan.

## The sitting

Date 2026-08-24. Session `13d56ab15efb4709821a6dd86357a5dc`, session file
`_attempts/session_4022b08512e0.json`, status complete, mode practice.

| Measure | Count |
|---|---|
| Items served | 10 |
| Auto attempts / correct | 18 / 9 |
| Human marks / passing | 1 / 0 |
| Pending manual at close | 0 |
| Hint events | 4, all on one item |
| Item-level objectives touched | 10 |
| Course objectives exercised | 4 of 4 |

Per-objective counts, hashes, log denominators and the reproduction commands
are in `13.9-CALIBRATION.md`. Nothing from the bank is copied into this
repository; `itembank guard .` exits 0.

## Not built: Weibao's two-sentence reaction

Task 1 step 4 asks for a two-sentence reaction after the sitting (was it worth
sitting, what was most broken), recorded verbatim. It was never given, and no
agent may write it for him. The earlier remark "last I tested it was relatively
well" predates the sitting and is explicitly disqualified by the 2026-08-24
entry in `13.9-DECISIONS.md`.

A9 was closed without it, with the gap named inside the checkbox rather than
hidden: the reaction is evidence about the experience, not about whether the
skeleton was walked, and every other clause of that checkbox is satisfied by
artifacts. If it is later given, it belongs in `13.9-DECISIONS.md` and in the
STATE completion block.

## Contradicted the plan: Task 1's verify asked for a file the code never wrote

Task 1's verify reads "An attempt record exists under `<course-root>/_attempts/`".
None existed for this sitting, and none had ever existed for any `serve`
sitting: `_refresh_attempt_view` was called only from the JSON `/api/submit`
path, while the browser form branch returned its redirect without it, and the
route's own docstring claimed the opposite.

Measured 2026-08-24, fixed 2026-08-25 (`fix(13.9): the browser form path never
wrote the attempt file it promised`), with a test that drives the real served
page rather than a mock, because the pre-existing suite had agreed with the
broken page. The fix is not retroactive. For this sitting the record is
`_evidence/evidence.jsonl` plus the session JSON, which is where the truth
always was, so this was a missing second copy and not data loss.

The plan's other Task 1 clauses verified: `itembank report` shows every item
either auto-graded or marked, none silently missing, and the session id was
captured with its date.

## Defects the sitting opened, and their disposition

Five were fixed on 2026-08-24 into 2026-08-25, listed with reasoning in the
2026-08-25 entry of `13.9-DECISIONS.md` and in full in the commit bodies:
the unwritten attempt file; the discarded answer on a failed submit; missing
own-selection feedback on a held multiple-response attempt (a disclosure
policy, explicitly not partial credit); NREMT option counts as a subject-scoped
lint error; and empirical distractor usage in `itembank stats`.

Six remain open with named owners, also listed in that entry. The largest is
13.5 defect D2 (`Item 1 of 0`), which already has an unexecuted plan at
`.planning/quick/260817-q7d-fix-135-defects-d1-d2/`.

## A9 closure state

All five A9 checkboxes are now `[x]`. Four were closed 2026-08-24 with dated
citations to this sitting and to `13.9-CALIBRATION.md`; the fifth (Phase 13.5
waves 3 and later) was waived by Weibao on 2026-08-24 with its reason recorded
in `13.9-DECISIONS.md`. The reaction gap above is named inside the first
checkbox rather than treated as a silent pass.
