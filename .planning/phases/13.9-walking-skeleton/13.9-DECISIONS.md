# Phase 13.9 decisions

## 2026-08-16 standing authority

User statement, verbatim:

> just pick the considerations that are most useful/comprehensive/if conflicting potentially implement all of them and let the user choose in the future, keep going without human stopagge if its reasonable and wont cause lasting harm, or lower quality, the idea is for UI and other important stuff to be planned by a more capable model before hand

Operational interpretation: routine reversible checkpoints may use the most useful comprehensive default. Viable conflicting options remain registered for later choice. Human stops remain required where proceeding would create lasting harm, lower quality, cross assessment authority, or require an undiscoverable personal action.

## 2026-08-16 Plan 13.9-01 choices

1. Course: EMT.
2. Source unit: `C:\Users\wayba\Downloads\CTF\weibao-planing\02_Skills_Learning\EMT\Chapters\ch01_ems_systems.md`.
3. Course root: `C:\Users\wayba\Downloads\Personal\itembank-courses\emt-unit-1`.

The course root was created outside the itembank repository. `git rev-parse --git-dir` fails there, confirming it is not inside a Git repository.

## 2026-08-16 objective-map approval

Approved under the standing authority above because the map is a reversible draft stub, every objective is source-cited, both direct-reading and lesson-plus-practice treatments are represented, and alternative richer treatments remain explicitly registered for later selection.

## 2026-08-22 course root unreachable, and what replaces it

Weibao is in Lexington and has no further access to the Windows desktop. The
course root recorded above, `C:\Users\wayba\Downloads\Personal\itembank-courses\emt-unit-1`,
cannot be reached or copied. The earlier entry stays as written; this one
records what changed, not a correction to it.

**What is lost.** That folder's `_attempts/` and `_evidence/` held the only real
learner evidence this project has ever recorded, from the partial sittings of
2026-08-16 and 2026-08-21. The two defects the 08-21 sitting found were already
fixed and guarded (`ea4eea2`, `129697e`), so the findings survived; the recorded
responses did not. `PLANNING-DIRECTIVES.md` §Budget discipline counts that
evidence as two lines. It is now zero.

**What replaces it.** Rebuild the course root on the Mac, at
`~/Documents/itembank-courses/emt-unit-1`, outside the repository for the same
reason as before: `itembank guard` fails CI if real bank content lands here. The
source is unchanged in substance and reachable, the EMT chapter 1 file in the
planning vault (`02_Skills_Learning/EMT/Chapters/ch01_ems_systems.md`), so
13.9-01's three checkpoint answers still stand: course EMT, source chapter 1,
root as above. Only the paths move.

**Cost of the rebuild.** 13.9-01 and 13.9-02 re-run, both mechanical now that
their decisions are recorded. The objective map and the bank are regenerated
rather than restored, so the new bank will not be item-for-item identical to
the one partially sat. That is acceptable: A9 asks for a real sitting on a real
source, not for continuity with an abandoned one.

**Standing consequence.** No plan may assume a Windows path from this date. The
six platform-branching runtime modules keep their Windows branches, because the
code should stay portable, but nothing in `.planning` should be written against
a machine nobody can reach.

## 2026-08-22 rebuild executed, and the objective map approved

13.9-01 re-run on macOS by Claude while Weibao was asleep, against the paths in
the entry above. Source inventoried read-only: one file, `src-01`, sha256
`ed4250dc7248b241690d102de5c0d29068e0f359ac3d36378ca4c68a603b1e4c`, 19,574
bytes. The source root was captured before and after and diffed: byte-identical,
nothing created, moved, renamed, or edited. It is a live git repository, which
made the read-only rule load-bearing rather than procedural.

**Four objectives mapped, both required treatments present.** obj-01 four
licensure levels and obj-04 mobile integrated health care versus 911 are
`direct-reading`; obj-02 certification versus licensure versus credentialing and
obj-03 the standing-order versus online-control decision are `lesson+practice`.

**Approved under the 2026-08-16 standing authority**, not by a fresh checkpoint,
because Weibao was asleep and the map is a reversible draft. The reasoning is
recorded so he can overturn it in one line: the two `lesson+practice` objectives
are the two the source itself records him getting wrong, credentialing missed
entirely and the medical-control decision rule left blank when it was split into
its own stem on 2026-07-30. The two `direct-reading` objectives are the ones the
source already explains better than a generated lesson would, one of them
because the recorded miss was copying the book rather than misunderstanding it.

**Deviation from the plan, stated rather than buried.** Task 4 is a
`checkpoint:human-verify` and no human verified it. If Weibao disagrees with the
map, plan 02's lesson and bank are regenerated against the corrected table; that
is cheap and is the whole reason the map is approved before authoring rather
than after.

## 2026-08-22 plan 02 complete, and what is waiting for Weibao

Lesson and bank authored and committed. The course root now holds `course.md`,
`lesson_ems-authority-and-direction.md`, and
`ems-authority-and-direction_bank.md` with ten items, lint clean, ids and hashes
minted. Details and the plan defect found during execution are in
`13.9-02-SUMMARY.md`.

**Only 13.9-03 remains, and it cannot be delegated.** The sitting is Task 1 of
that plan and A9 requires it to be real. To start it:

    python3 itembank.py serve ~/Documents/itembank-courses/emt-unit-1/ems-authority-and-direction_bank.md

Capture the session id from the startup banner into this file before answering,
because reconstructing it later means reading `_attempts/`. Read the two
direct-reading objectives in `course.md` first, obj-01 and obj-04, since Q9 and
Q10 are post-reading checks for exactly those.

`_attempts/` is currently an empty directory created by a startup check on
2026-08-22 that gave no answers. Nothing in it is learner evidence, and the
first real sitting is the first thing that will write there.

## 2026-08-24 the sitting path was smoke tested on the Mac, and the recorded instruction is wrong

Weibao asked whether itembank works on macOS at all before spending a sitting
on it. Tested end to end against `fixtures/lesson_bank.md` in a temp directory,
never against the EMT bank, so nothing below touched the real course root.

**What works.** `serve` binds, the page returns HTTP 200 at 126,475 bytes,
renders with all four vendored faces and the 17A token layer live in the served
bytes, leaks no key before answering, scores a multiple-choice answer
correctly, advances the cursor, writes `_evidence/evidence.jsonl` and the
session JSON as the answer is given, and `evidence --objective <id>` returns
the right counts with both denominators. The loop is real.

**Finding 1, and it invalidates an instruction in this file.** The session id
printed in the startup banner is NOT the id the evidence is written under.
Measured in one run: banner `7604f17b7578499795374de1c4ab92be`, evidence and
session JSON `556862266e14463b98b75564bbf82f74`, file name
`_attempts/session_5b0b5f08dfbb.json`. Three different values.

The cause is in the code, not in the run. `quiz.cmd_serve` mints and prints one
id, then hands it to the daemon in `extra["sessions"]`. The daemon's
`_ensure_quiz_session` ignores it, calls `session.do_start`, which mints its
own, and stores that as `api_session_id`. Every evidence row carries the
second id.

The 2026-08-22 entry above, and 13.9-03's `key_links` row, both instruct
capturing the banner id before answering. **That instruction is wrong and must
not be followed.** A calibration corpus naming the banner id would name a
session that appears nowhere in the evidence store, and the error would only
surface when a later plan tried to query it.

**What to capture instead:** the path of the file that appears under
`_attempts/` once the first answer is given, `session_<12 hex>.json`. The
authoritative id is the `session_id` field inside it.

**Finding 2.** `itembank report` takes a session file PATH, not a session id.
13.9-03 Task 1's verify step says "`itembank report` on the session". Passing
the banner id fails with `cannot read session <id>: No such file or directory`.
Passing `_attempts/session_<hex>.json` returns the expected document.

**Finding 3, cosmetic, routed to 13.5.** The sitting header reads `Item 1 of 0`
and the position does not advance between items. `quiz_page.render()` sets both
counters, but under scoped `serve` the daemon injects a server-rendered
baseline into `#host` and the client `render()` path never runs, so the
template defaults of 1 and 0 stand. No console error. Reproduced identically at
`eb96118`, so it predates the 2026-08-24 17A work; none of `daemon.py`,
`surfaces/session.py`, `surfaces/quiz.py` or `quiz_page.py` was touched that
day. Recorded here as an observation, not repaired: 13.9-03 is explicit that
the skeleton observes and does not fix.

Findings 1 and 2 are corrections to this document's own instructions rather
than repairs to a surface, so they are made here before the sitting rather than
recorded as defects after it.

## 2026-08-24 A9 13.5 waiver granted, and a prior-experience remark

**Decision, Weibao, 2026-08-24.** The A9 checkbox requiring Phase 13.5 waves 3
and later to be scheduled before or beside the skeleton is **waived**. He asked
for the waiver in one word after being shown the measured state below, so the
decision is his and the supporting evidence is this session's, kept separate
per the standing rule that findings and decisions stay visibly distinct.

**Supporting evidence, measured 2026-08-24, not asserted.** The checkbox exists
because the skeleton renders through exactly the surfaces 13.5 hardens. Those
surfaces were hardened materially the same day by a different phase: 17A-02 put
`surfaces/day.py` onto `presentation.surface_shell` and froze the five type and
three density tokens, and 17A-03 added the accessible primitive layer, so the
served quiz page now carries all four vendored faces and the full token layer
in its bytes. An end-to-end smoke test of the sitting path on this Mac
(preceding entry) found the loop working: correct scoring, evidence written per
answer, correct per-objective counts, no key leak before answering. The single
defect it surfaced, `Item 1 of 0`, is cosmetic, does not block a sitting, and
is routed to 13.5 as its owner. That is the reason the waiver is defensible
rather than merely convenient.

**What the waiver does not do.** It does not close 13.5, reassign the counter
defect, or excuse any other A9 checkbox. The remaining A9 boxes still require
the real sitting and are 13.9-03 Task 3's work.

**Prior-experience remark, recorded verbatim and deliberately NOT as the Task 1
reaction:**

> last I tested it was relatively well

Said on 2026-08-24, before sitting the rebuilt EMT bank. It refers to the
partial Windows sittings of 2026-08-16 and 2026-08-21, whose recorded evidence
was lost with that machine. It is context, not skeleton evidence. **13.9-03
Task 1 still requires a two-sentence reaction captured after the real sitting**
(was it worth sitting, what was most broken), and that slot stays open. A9
requires the sitting to be real, so no remark made before it can stand in for
the reaction made after it.
