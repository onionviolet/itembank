# Phase 13.9 decisions

## 2026-08-16 standing authority

User statement, verbatim:

> just pick the considerations that are most useful/comprehensive/if conflicting potentially implement all of them and let the user choose in the future, keep going without human stopagge if its reasonable and wont cause lasting harm, or lower quality, the idea is for UI and other important stuff to be planned by a more capable model before hand

Operational interpretation: routine reversible checkpoints may use the most useful comprehensive default. Viable conflicting options remain registered for later choice. Human stops remain required where proceeding would create lasting harm, lower quality, cross assessment authority, or require an undiscoverable personal action.

## 2026-08-16 Plan 13.9-01 choices

1. Course: EMT.
2. Source unit: `C:\Users\wayba\Downloads\CTF\weibao-planing\02_Skills_Learning\EMT\Chapters\ch01_ems_systems.md`.
3. Course root: `C:\Users\wayba\Downloads\Personal\itembank-courses\emt-unit-1`.

The course root was created outside the itembank repository. `git rev-parse --git-dir` fails there, confirming it is not inside a Git repository.

## 2026-08-16 objective-map approval

Approved under the standing authority above because the map is a reversible draft stub, every objective is source-cited, both direct-reading and lesson-plus-practice treatments are represented, and alternative richer treatments remain explicitly registered for later selection.

## 2026-08-22 course root unreachable, and what replaces it

Weibao is in Lexington and has no further access to the Windows desktop. The
course root recorded above, `C:\Users\wayba\Downloads\Personal\itembank-courses\emt-unit-1`,
cannot be reached or copied. The earlier entry stays as written; this one
records what changed, not a correction to it.

**What is lost.** That folder's `_attempts/` and `_evidence/` held the only real
learner evidence this project has ever recorded, from the partial sittings of
2026-08-16 and 2026-08-21. The two defects the 08-21 sitting found were already
fixed and guarded (`ea4eea2`, `129697e`), so the findings survived; the recorded
responses did not. `PLANNING-DIRECTIVES.md` §Budget discipline counts that
evidence as two lines. It is now zero.

**What replaces it.** Rebuild the course root on the Mac, at
`~/Documents/itembank-courses/emt-unit-1`, outside the repository for the same
reason as before: `itembank guard` fails CI if real bank content lands here. The
source is unchanged in substance and reachable, the EMT chapter 1 file in the
planning vault (`02_Skills_Learning/EMT/Chapters/ch01_ems_systems.md`), so
13.9-01's three checkpoint answers still stand: course EMT, source chapter 1,
root as above. Only the paths move.

**Cost of the rebuild.** 13.9-01 and 13.9-02 re-run, both mechanical now that
their decisions are recorded. The objective map and the bank are regenerated
rather than restored, so the new bank will not be item-for-item identical to
the one partially sat. That is acceptable: A9 asks for a real sitting on a real
source, not for continuity with an abandoned one.

**Standing consequence.** No plan may assume a Windows path from this date. The
six platform-branching runtime modules keep their Windows branches, because the
code should stay portable, but nothing in `.planning` should be written against
a machine nobody can reach.
