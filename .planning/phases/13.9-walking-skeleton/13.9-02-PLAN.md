---
phase: 13.9-walking-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["13.9-01"]
files_modified:
  - <course-root>/course.md
  - <course-root>/lesson_<slug>.md
  - <course-root>/<slug>_bank.md
autonomous: true
requirements: [READINESS-AUDIT-14A A9]
must_haves:
  truths:
    - "The lesson+practice objective has a separate lesson file the bank references via [LESSON-SRC:], readable as plain Markdown on its own."
    - "The bank lints clean (exit 0), has minted [ID:]/[HASH:] fingerprints, and every item carries [OBJ:] and [SRC:] resolving through its ## SOURCES registry."
    - "The direct-reading objective appears in course.md as a reading entry plus two or three post-reading check items in the same bank."
    - "No sentence of eight or more consecutive words is copied from the source; the paraphrase lint passes."
    - "The source root is still byte-identical to its plan-01 inventory."
  artifacts:
    - "<course-root>/lesson_<slug>.md"
    - "<course-root>/<slug>_bank.md with ## SOURCES registry"
    - "course.md Objectives table updated with artifact pointers"
  key_links:
    - "Two-file layout is the default (ROADMAP 3.1 criterion 7): the prose separates, the item never does. The bank points to the lesson with [LESSON-SRC:], never the reverse."
    - "id-assign is the only command that writes into a bank; run it once, after the final lint, or every later edit reminted nothing and drift warnings lie."
    - "The authoring loop is the author-bank skill's write, lint, fix cycle; lint output is the interface, not a suggestion."
---

<objective>
Author the two treatments the approved objective map calls for: one guided
lesson plus practice, and one direct reading with post-reading checks, all in
the course root, all lint-clean, so plan 03 can sit them. Second half of
`READINESS-AUDIT-14A.md` A9's artifact obligations.

Decisions already made: objectives, treatments, and reasons are frozen in
course.md as approved at plan 01 Task 4; this plan authors against that table
and changes it only by adding pointer columns. Item counts below are decided
here so the executor does not choose: 8 to 12 items total, at most 2 `short`
items (bounds plan 03's manual marking), the rest auto-scored types.
</objective>

<context>
@.planning/phases/13.9-walking-skeleton/13.9-DECISIONS.md
@<course-root>/course.md
@.claude/skills/author-bank/SKILL.md
@.claude/skills/absorb-book/SKILL.md
@.claude/skills/lesson-authoring/SKILL.md   (stub; use its intent, author via spec)
</context>

<tasks>

<task type="auto">
  <name>Task 1: The lesson file for the lesson+practice objective</name>
  <files><course-root>/lesson_<slug>.md, <course-root>/course.md</files>
  <read_first>
`python itembank.py spec` output in full (the LESSON, TERMS, [!KEY], callout,
and style grammar). The source sections the objective cites. The absorb-book
skill's synthesis-labeling rule: generated prose is labeled synthesis and
keeps its citations.
  </read_first>
  <action>
(1) Choose `<slug>` from the objective's subject, lowercase with hyphens, and
use it consistently for both files.

(2) Write `lesson_<slug>.md`: a lesson for the `lesson+practice` objective in
`expository` style, 600 to 1200 words, structured as `###` sections sized to
the objective. Include a `## TERMS` block with 3 to 6 terms actually used in
the prose via `[[term]]`, and one `[!KEY]` block for the single most
memorizable fact. Every factual claim that carries a numeral, unit, or dose
cites `[SRC: src-NN <locator>]` inline. Paraphrase; never transcribe. No em
dashes.

(3) Add the pointer into course.md's Objectives table (new column `artifact`)
and a Log line: `2026-08-14 lesson authored: lesson_<slug>.md`.
  </action>
  <verify>
The lesson file reads coherently in a plain-text pager with no itembank
running (`less lesson_<slug>.md`). Lint acceptance happens in Task 2 when the
bank binds it via [LESSON-SRC:].
  </verify>
</task>

<task type="auto">
  <name>Task 2: The bank: practice, post-reading checks, provenance</name>
  <files><course-root>/<slug>_bank.md, <course-root>/course.md</files>
  <read_first>
The author-bank SKILL.md in full (distractor, position-skew, confidence
rules). `python itembank.py spec` for [OBJ:], [SRC:], ## SOURCES, and
[LESSON-SRC:] grammar.
  </read_first>
  <action>
(1) Write `<slug>_bank.md`:
  - `[LESSON-SRC: lesson_<slug>.md]` at the top.
  - `## SOURCES` registry mirroring course.md's src-NN table (id and path
    only, no content).
  - 6 to 9 items for the `lesson+practice` objective, each with
    `[OBJ: obj-NN]`, `[SRC: src-NN <locator>]`, and `LESSON-REF` into the
    lesson's headings. Mix of `mc` and `multi`; at most 2 `short` items in
    the whole bank.
  - 2 to 3 items for the `direct-reading` objective, tagged the same way,
    written as post-reading checks (answerable from the reading, not from
    the lesson).

(2) Run the loop until clean:

```bash
python itembank.py lint <course-root>/<slug>_bank.md
```

Expected final state: exit 0, no errors; warnings only if each is understood
and stated in the summary. Fix items per the lint text; the skill's rules
govern distractor and skew repairs.

(3) Then, exactly once:

```bash
python itembank.py id-assign <course-root>/<slug>_bank.md
python itembank.py stats <course-root>/<slug>_bank.md
python itembank.py coverage <course-root>/<slug>_bank.md
```

Expected: id-assign mints [ID:]/[HASH:] for every item; stats shows the item
mix; coverage shows every obj-NN with at least 2 items and no uncited item.

(4) Update course.md: artifact pointers for both objectives, reading entry
for the direct-reading objective in the form
`Read src-NN <locator>, then sit the post-reading checks in <slug>_bank.md`,
and a Log line.
  </action>
  <verify>
The three commands above, re-run, all exit 0 with stable output. Paraphrase
check: lint reports no copied-run error (the >=8-consecutive-words check from
Phase 3.2). Source-root integrity: regenerate the inventory listing and diff
against `.inventory-before.txt` from plan 01; expected no output, exit 0.
  </verify>
</task>

<task type="auto">
  <name>Task 3: Render check on the shipped surfaces</name>
  <files><course-root>/course.md</files>
  <action>
Prove the artifacts render before Weibao ever sees them:

```bash
python itembank.py lesson <course-root>/<slug>_bank.md
python itembank.py build <course-root>/<slug>_bank.md /tmp/skeleton_preview.html
```

Expected: `lesson` renders the lesson with terms and the [!KEY] block, exit
0; `build` writes the offline quiz, exit 0. Open neither in a browser; that
is plan 03's human pass. Delete `/tmp/skeleton_preview.html` afterward. Log
line in course.md: `2026-08-14 render check passed`.
  </action>
  <verify>
Both commands exit 0. The degraded case is also proven: run
`python itembank.py lesson` on the bank with the lesson file temporarily
renamed; expected: the degraded reader state names the authored path and
exits without a traceback; rename back and re-run to confirm recovery.
  </verify>
</task>

</tasks>

<out_of_scope>
No sitting, no evidence reads, no marking (plan 03). No edits under the
source root. No style beyond `expository`. No second lesson, no extra banks,
no course.md parsing tooling. No commit of course-root files anywhere.
</out_of_scope>

<summary_obligations>
`13.9-02-SUMMARY.md` records: final item count and type mix, lint warning
list with the one-line reason each was accepted, the coverage output verbatim,
the degraded-state check result, and any deviation with its reason.
</summary_obligations>
