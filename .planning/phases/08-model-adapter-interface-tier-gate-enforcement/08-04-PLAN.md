---
phase: 08-model-adapter-interface-tier-gate-enforcement
plan: 04
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
files_modified:
  - runtime.py
  - surfaces/session.py
  - surfaces/cli.py
  - surfaces/evidence_cli.py
  - surfaces/protocol_cli.py
  - schemas/agent_usage.schema.json
  - tests/model_surface_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - TEACH-04
  - TEACH-07
  - TEACH-08
  - TEACH-09
  - MODEL-03
  - MODEL-04
  - MODEL-05
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-10/D-11: the hint orchestration resolves the learner's most recent genuine wrong response and the Phase 6 permitted tier, generates at most once per interaction id, and augments one unlocked authored tier — it never replaces the authored ladder and never computes or advances tier state (D-09)."
    - "D-12: repeated delivery of the same command is idempotent by interaction id; only an explicit parent-linked retry regenerates, and retry lineage is visible in evidence."
    - "D-13/D-14: rubric review is available only for a genuine pending short response and returns pending per-point suggestions; the human accept path goes through the existing mark_event marker='human' with proposal_ref; no model path settles a mark."
    - "D-21/D-22: self-assessment is the shipped default tier-3 pathway; suggestion_reveal ships all three values (after-self-mark default, before-self-mark, after-mark) and the surface renders a suggestion only as a pending token — never a number, fraction, check, or cross glyph."
    - "D-25: auto-accept is impossible; the only accept paths are the learner's explicit self-mark (a human accept) and the reviewer's explicit human action, including batch accept."
    - "MODEL-03: with the default disabled backend, hint and rubric-review commands return typed unavailable results and the authored loop (submit, score, lesson, evidence, report, mark) works unchanged."
    - "MODEL-04: a published agent usage contract states permissions, disclosure limits, retry behavior, manual-grading rules, and forbidden inferences, and is machine-readable and printable from the CLI."
    - "Edge probes resolved as explicit predicates: hint on a session with no genuine wrong response returns authored/unavailable without generating; rubric review on a non-short or already-marked response refuses with a named reason; an empty session file or unknown interaction id is a typed refusal, never a crash; the same command twice returns the same interaction id and the already_recorded evidence status."
  artifacts:
    - "runtime.py exports hint_context(session_data, q) and the orchestration helpers invoke_hint/invoke_rubric_review that sequence adapter -> gate -> evidence -> authored fallback."
    - "surfaces/session.py exports do_hint(session_file, retry=False), cmd_hint, do_rubric_review(session_file), cmd_rubric_review."
    - "surfaces/cli.py registers hint, rubric-review, and usage parsers."
    - "surfaces/evidence_cli.py extends cmd_mark with --proposal and proposal-bearing --marks batch entries."
    - "surfaces/protocol_cli.py and schemas/agent_usage.schema.json publish the agent usage contract; itembank usage prints it."
    - "tests/model_surface_roundtrip.py proves CLI behavior, idempotency, retry lineage, pending-only suggestions, human-only accept, and the usage contract."
  key_links:
    - "CLI command -> surfaces.session do_* -> runtime orchestration -> model_adapter.invoke -> tier_gate.evaluate_candidate -> evidence events"
    - "rubric review -> mark_proposal_event -> pending payload -> existing mark command (proposal_ref) -> accepted evidence"
    - "agent usage contract -> protocol_cli CONTRACTS -> itembank schema --all -> agent consumer"
  prohibitions:
    - statement: "No surface may accept a caller-supplied tier, profile, key, or marker; those are runtime/evidence decisions only."
      status: unverified
      flagged: true
    - statement: "No auto-accept flag or setting exists for tier-3 suggestions."
      status: unverified
      flagged: true
---

<objective>
**As a** learner or agent, **I want** to request optional error-specific guidance and pending rubric suggestions through the same commands a human uses, **so that** the runtime — not the surface — orchestrates generation, gating, evidence, and the human-only accept path.

Purpose: TEACH-04, TEACH-07..09, MODEL-03..05 and D-10..D-14/D-21..D-25 require callable, inspectable surfaces that never move authority (tier, scoring, evidence writing, acceptance) into the CLI.
Output: Source and packaged CLI flows, the runtime orchestration path, retry semantics, the human-only proposal accept, and a machine-readable agent usage contract.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-CONTEXT.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-RESEARCH.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-PATTERNS.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-AI-SPEC.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-UI-SPEC.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-VALIDATION.md
@.planning/phases/06-hint-ladder-cursor-hold-feedback-modes/06-CONTEXT.md

<interfaces>
- `surfaces/session.py` do_*/cmd_* split: do_start (line 54), do_submit (line 111), cmd_submit (line 155) — every new operation follows this shape and returns a dict; cmd_* prints json.dumps(ensure_ascii=False, indent=2).
- `surfaces/cli.py` import block (lines 7-21) and main() sub-parser registration (line 111+).
- `surfaces/evidence_cli.py` cmd_mark (line 253): batch resolve-then-append discipline — every entry resolved before anything appends.
- `surfaces/protocol_cli.py` CONTRACTS tuple (line 23), COMMANDS (line 38), cmd_schema (line 74).
- `evidence.py` mark_event(..., proposal_ref=None) from plan 08-03; model_interaction_event and mark_proposal_event from plan 08-03; model_interactions/proposals_for readers.
- `tier_gate.py` learner_payload, evaluate_candidate, render_hint, render_proposal from plan 08-01; runtime.authored_hint(q, tier) seam from plan 08-01.
- `model_adapter.py` invoke(request, settings) from plan 08-02; surfaces.settings.load_settings and the profile resolver.
- Phase 6 session fields (06-CONTEXT D-03/D-07) carry attempt count, highest tier unlocked/shown, and the last genuine response's canonical form; the executor reads the actual Phase 6 source at execution time and adapts field names — never a paraphrase.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `runtime.py` | `hint_context(session_data, q)` returning {item, tier, wrong_response, picked_option, mode}; `invoke_hint(session_file, retry=False)`; `invoke_rubric_review(session_file)`; interaction-id minting with parent linkage |
| `surfaces/session.py` | `do_hint(session_file, retry=False)`, `cmd_hint(a)`, `do_rubric_review(session_file)`, `cmd_rubric_review(a)` |
| `surfaces/cli.py` | `hint`, `rubric-review`, `usage` sub-parsers; `--retry` flag on hint |
| `surfaces/evidence_cli.py` | cmd_mark `--proposal <event_id>` support and `"proposal"` keys in --marks batch entries |
| `surfaces/protocol_cli.py` | `("agent_usage", ...)` added to CONTRACTS; `cmd_usage(a)` |
| `schemas/agent_usage.schema.json` | Versioned usage contract: permissions, prohibitions, disclosure, retry, manual_grading, forbidden_inferences |
| `tests/model_surface_roundtrip.py` | CLI/orchestration fixtures for TEACH-04/07/08/09, MODEL-03/04/05 |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — itembank hint runs the offline default path end to end through runtime, adapter, gate, evidence, and fallback</name>
  <precondition>Phase 6 has executed and the session dict carries the Phase 6 fields per 06-CONTEXT D-03/D-07 (attempt count, highest tier unlocked/shown, last genuine response canonical); the executor verifies by reading the Phase 6 source/summary and greps for those fields before wiring hint_context.</precondition>
  <files>runtime.py, surfaces/session.py, surfaces/cli.py, tests/model_surface_roundtrip.py</files>
  <read_first>
    - `surfaces/session.py` do_submit (line 111) and cmd_submit (line 155) — the dict-return/JSON-print split to copy
    - `surfaces/cli.py` main() sub-parser registration shape (line 111+)
    - `06-CONTEXT.md` D-03 (session fields) and D-07 (hint payload contract)
    - `08-PATTERNS.md` session.py section (do_*/cmd_* + evidence append flow)
    - `tests/agent_roundtrip.py` run() helper and tests/evidence_roundtrip.py fail() harness
  </read_first>
  <behavior>
    - Test 1: with the default disabled backend, do_hint(session_file) returns {"status": "unavailable", "generated": null, "authored": <tier content or null>, "interaction_id": "<id>"}, appends one model_interaction event with outcome unavailable, and the authored tier content from runtime.authored_hint is present when the item has it (MODEL-03 offline path).
    - Test 2: a second do_hint call without retry returns the same interaction_id and the evidence status already_recorded — one generation per interaction (D-12 idempotency).
    - Test 3: do_hint(retry=True) after a prior attempt mints a child interaction id whose parent_interaction_id references the first, and appends a new model_interaction event (retry lineage).
    - Test 4: cmd_hint prints the payload as JSON and exits 0; the CLI path reaches the same do_hint function the daemon will call (source-inspection parity test).
  </behavior>
  <action>
Implement runtime.hint_context(session_data, q): read the Phase 6 session fields for the current item (attempt count, highest tier unlocked, last genuine response canonical, mode), returning {item, tier, wrong_response, picked_option, mode} or None when no genuine wrong response exists. Implement invoke_hint(session_file, retry=False) as the one orchestration path: resolve session and current item, build the request via model_adapter.request_from_operation with operation hint, call model_adapter.invoke with surfaces.settings.load_settings, validate the result, run tier_gate.evaluate_candidate on any candidate, select tier_gate.learner_payload on pass or the authored fallback via runtime.authored_hint(q, tier) on drop/unavailable, append evidence.model_interaction_event with the gate reason (evidence-only), and return {"status", "generated", "authored", "interaction_id"} where the learner-facing payload never carries a reason code (D-08). Mint interaction ids as uuid4 hex; retry=True mints a child id with parent_interaction_id set to the most recent interaction for this session/item; never generate more than once per interaction id. Add do_hint(session_file, retry=False)/cmd_hint(a) in surfaces/session.py (cmd prints json.dumps(ensure_ascii=False, indent=2), returns 0) and register the hint parser in surfaces/cli.py with --session and --retry flags. Write the four behavior tests first in tests/model_surface_roundtrip.py, including the source-inspection parity check that cmd_hint calls do_hint.
  </action>
  <verify>
    <automated>python tests/model_surface_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Offline hint returns the typed unavailable payload plus authored content, appends exactly one evidence event, and is idempotent per interaction id.
    - Explicit retry produces a parent-linked child interaction and a new evidence event.
    - cmd_hint prints JSON and exits 0; scoring/evidence authority tests stay green.
  </acceptance_criteria>
  <done>The first model command works end to end offline, and the authored ladder is the fallback — no provider required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rubric review, pending suggestions, and the human-only proposal accept</name>
  <files>runtime.py, surfaces/session.py, surfaces/cli.py, surfaces/evidence_cli.py, tests/model_surface_roundtrip.py</files>
  <read_first>
    - `surfaces/evidence_cli.py` cmd_mark (line 253) — batch resolve-then-append discipline and JSON payload printing
    - `08-CONTEXT.md` D-13/D-14/D-21/D-22/D-25
    - `08-UI-SPEC.md` rubric proposal lifecycle (PROPOSAL_PENDING_REVIEW -> HUMAN_ACCEPT | HUMAN_REPLACE) and the --pending token rule
    - `08-PATTERNS.md` evidence_cli.py section (cmd_mark is the batch human-accept model)
  </read_first>
  <behavior>
    - Test 1: do_rubric_review(session_file) works only when the current item is a short response with a pending review_state; it returns {"status": "pending", "points": [...]} and appends a mark_proposal_event linked to the response event; on a non-short or already-marked response it refuses with a named reason and writes nothing.
    - Test 2: cmd_mark --proposal <proposal_event_id> --verdict true resolves the proposal's response, records mark_event(marker="human", proposal_ref=...), and marks_by_event returns the settled mark while the proposal event stays separate (TEACH-08/TEACH-09).
    - Test 3: a --marks batch whose entries carry "proposal" ids validates every entry before appending any (existing batch discipline); an unknown proposal id exits non-zero with nothing appended.
    - Test 4: the suggestion_reveal setting is read and the CLI/surface representation of a suggestion is only a pending token — never a number, fraction, check, or cross glyph (D-22); no flag exists that auto-accepts (D-25).
  </behavior>
  <action>
Implement runtime.invoke_rubric_review(session_file): require the current item type short and the most recent live response's review_state pending (read via evidence readers from plan 08-03); build a rubric_review request with the item's rubric points, call model_adapter.invoke, validate through tier_gate.evaluate_candidate (rubric_proposal shape), append mark_proposal_event, and return {"status": "pending", "points": [...]} where each point carries point_index/status/rationale and the whole payload reads as pending. Refusals return a named reason with no evidence write. Add do_rubric_review/cmd_rubric_review in surfaces/session.py and register the rubric-review parser in surfaces/cli.py (--session). Extend cmd_mark in surfaces/evidence_cli.py: a --proposal <event_id> argument resolves the proposal to its response event and records mark_event with proposal_ref (verdict and optional --rubric JSON of N booleans supplied by the human); --marks batch entries may carry a "proposal" key alongside item_ref/verdict/rubric; every entry in the batch resolves before any append (copy the existing cmd_mark discipline exactly). Read suggestion_reveal from surfaces.settings.load_settings and represent suggestions in command output only as a pending token string (for example "pending") — the render helper never emits a numeric score or a check/cross glyph. Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/model_surface_roundtrip.py && python tests/evidence_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Pending suggestions exist only for genuine pending short responses; model-only paths leave review_state pending and marks_by_event empty.
    - Human accept via --proposal records marker human with proposal_ref; batch resolution is all-or-nothing.
    - Suggestion output is pending-token only, and no auto-accept flag exists.
  </acceptance_criteria>
  <done>The learner and reviewer can work the full tier-3 loop through the CLI, and no model path can settle a mark.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish the agent usage contract</name>
  <files>surfaces/protocol_cli.py, surfaces/cli.py, schemas/agent_usage.schema.json, tests/model_surface_roundtrip.py</files>
  <read_first>
    - `surfaces/protocol_cli.py` CONTRACTS (line 23), COMMANDS (line 38), cmd_schema (line 74) — the no-processing contract-delivery pattern
    - `08-CONTEXT.md` D-03/D-12/D-14/D-15/D-16/D-25 and MODEL-04
    - `08-AI-SPEC.md` section 4 (context window strategy, no repository context) and section 6 (guardrails)
  </read_first>
  <behavior>
    - Test 1: schemas/agent_usage.schema.json validates against schema_validate and declares permissions (request_hint, request_rubric_review, read_pending_proposals, accept_proposals), prohibitions (write_evidence, score_responses, select_items, advance_tiers, read_dropped_text, auto_accept_proposals), disclosure limits, retry rules (one attempt, explicit retry, parent-linked), manual-grading rules (marker human; self-mark and batch accept), and forbidden inferences.
    - Test 2: itembank usage prints the contract bytes verbatim; itembank schema --all includes agent_usage in the contracts object.
    - Test 3: the contract text contains no invented performance figure and no secret reference format beyond env-var names (MODEL-04 + D-19).
  </behavior>
  <action>
Create schemas/agent_usage.schema.json with x-itembank-version 1 and a closed object: permissions enum list, prohibitions enum list, disclosure object (hosted_transit_accepted true, evidence_local_only true, secrets_env_references_only true), retry object (max_attempts const 1, explicit_retry const true, parent_link const true), manual_grading object (marker const human, accept_paths enum list self_mark|batch_mark), forbidden_inferences enum list (tier_state, item_selection, accepted_verdicts, learner_identity), using only SUPPORTED schema keywords. Add ("agent_usage", "the permissions, disclosure, retry, and manual-grading contract for model-facing agents") to CONTRACTS in surfaces/protocol_cli.py so itembank schema --all and itembank schema agent_usage serve it, and add cmd_usage(a) printing the document verbatim plus a one-line human summary; register the usage parser in surfaces/cli.py. Write the three behavior tests in tests/model_surface_roundtrip.py, including a schema-validation test and a verbatim-print assertion comparing command output to the on-disk file.
  </action>
  <verify>
    <automated>python tests/model_surface_roundtrip.py && python schema_validate.py --all</automated>
  </verify>
  <acceptance_criteria>
    - The usage contract validates and prints verbatim from both itembank usage and itembank schema --all.
    - Every MODEL-04 clause is represented by a machine-readable field; no invented figures or inline secrets appear.
  </acceptance_criteria>
  <done>An agent with no repository context can read exactly what it may and may not do — from the CLI alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI args -> session/runtime | User- or agent-supplied flags cross here; only session file paths and retry flags are accepted, never tier/profile/key/marker |
| command output -> learner/agent | Typed payloads only; reason codes and private fields stay out of stdout |
| proposal accept -> evidence | Human-only boundary; batch resolution must be all-or-nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-30 | Tampering | CLI authority fields | high | mitigate | hint/rubric-review parsers accept only --session/--retry; tier, profile, key, and marker are never CLI options (D-09) |
| T-08-31 | Spoofing | proposal accept as auto-mark | critical | mitigate | cmd_mark resolves every proposal/response before any append; mark_event's human-only guard unchanged; no accept flag exists (D-25) |
| T-08-32 | Information Disclosure | reason codes in stdout | high | mitigate | do_hint/do_rubric_review learner payloads carry no detector reason; reason codes are evidence-only (D-08/D-16) |
| T-08-33 | Denial of Service | repeated hint commands | medium | mitigate | One generation per interaction id enforced by evidence dedupe; explicit retries are parent-linked and visible |
| T-08-34 | Information Disclosure | usage contract secrets | low | accept | The contract documents env-var references by name only; no live credential values are needed to print it |
| T-08-SC | Tampering | package operations | high | mitigate | This plan performs no package operation and no network fetch |
</threat_model>

<verification>
- `python tests/model_surface_roundtrip.py` — offline hint, idempotency, retry lineage, pending suggestions, human-only accept, usage contract
- `python tests/evidence_roundtrip.py` — evidence authority unchanged
- `python schema_validate.py --all` — agent_usage schema validates
</verification>

<success_criteria>
- The hint and rubric-review commands work offline end to end with one generation per interaction and parent-linked retries.
- Proposals are pending-only and human-accepted with proposal_ref; no auto-accept exists.
- The agent usage contract is machine-readable and printable from the CLI.
</success_criteria>

<output>
Create `.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-04-SUMMARY.md` when done.
</output>
