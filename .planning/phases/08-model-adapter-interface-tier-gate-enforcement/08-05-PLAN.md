---
phase: 08-model-adapter-interface-tier-gate-enforcement
plan: 05
type: execute
wave: 4
depends_on:
  - "08-04"
files_modified:
  - surfaces/daemon.py
  - surfaces/quiz.py
  - surfaces/quiz_page.py
  - tests/model_ui_roundtrip.py
  - tests/daemon_roundtrip.py
  - .planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-05-UAT-EVIDENCE.md
autonomous: false
ui_classification: UI-DEPENDENT
requirements:
  - TEACH-04
  - TEACH-05
  - TEACH-06
  - TEACH-07
  - TEACH-08
  - TEACH-09
  - MODEL-03
  - MODEL-05
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-09/D-26: the browser receives only typed normalized public payloads; private item fields, manifests, tiers, provider/profile details, candidate bodies, and gate reasons never cross the API/DOM boundary. A locked tier renders as a labeled structural lock with the unlock condition stated, never as model voice."
    - "Generated guidance is opt-in, collapsed, subordinate to authored teaching, disclosed as generated, and represented by the requested/thinking/pass/drop/unavailable/cancelled/retry lifecycle states rather than chat (08-UI-SPEC copy tables are binding)."
    - "D-22: model suggestions render only as a pending token until the learner's chosen suggestion_reveal timing; never a number, fraction, check, or cross glyph."
    - "A drop or unavailable backend shows the approved generic copy and leaves authored hints, response submission, scoring, evidence, and reports usable (MODEL-03)."
    - "Rubric suggestions render per point as pending only; the browser exposes no accepted-mark action of any kind; Record human mark exists only in the trusted local reviewer CLI (08-04)."
    - "Every new API route has a CLI twin and a reserved MCP tool name in one SURFACE_PARITY map, per Extensibility Rule 9(a); the parity test fails on a route without a CLI or tool name."
    - "Edge probes resolved as explicit predicates: zero hints requested renders the collapsed control with no status; an in-flight request announces once and the control is disabled; a retry beyond the first is hidden until the learner explicitly requests it; a suggestion with empty points renders as pending/unknown with the exact incomplete copy; 320px/200% zoom shows no horizontal scroll."
  artifacts:
    - "surfaces/daemon.py exposes POST /api/hint and POST /api/rubric-review via identifier-safe handlers, extends API_FORBIDDEN_FIELDS, adds ROUTE_CLI entries, and defines SURFACE_PARITY."
    - "surfaces/quiz_page.py implements the AgentAssist markup, lifecycle states, pending rubric checklist, and the ASSIST_JS client wiring; surfaces/quiz.py page_for threads the assist payload."
    - "tests/model_ui_roundtrip.py and tests/daemon_roundtrip.py prove API allowlists, DOM privacy, lifecycle, responsive, and accessibility behavior."
    - "08-05-UAT-EVIDENCE.md records the blocking human checkpoint verdict, environment, checklist, and revision notes."
  key_links:
    - "browser opt-in action -> POST /api/hint -> session.do_hint -> runtime/evidence"
    - "runtime public state -> learner-safe copy and generated disclosure -> structural lock / generated support container"
    - "pending rubric point -> browser pending checklist (no accept action) -> trusted local reviewer CLI (plan 08-04)"
    - "08-05 checkpoint -> 08-05-UAT-EVIDENCE.md PASS -> plan 08-06 release gate"
  prohibitions:
    - statement: "No key, tier number, fact manifest, profile, backend class, candidate body, gate reason, or dropped text may appear in any API response, DOM, attribute, or script payload."
      status: unverified
      flagged: true
    - statement: "The browser must expose no accepted-mark or auto-accept control."
      status: unverified
      flagged: true
---

<objective>
**As a** learner, **I want** to open the quiz surface and see optional generated support that is clearly labeled, subordinate, and runtime-gated, **so that** the model's help can never wear a grade's clothes, leak a locked fact, or act like a chat companion.

Purpose: ROADMAP criteria 1 and 6 (D-26) plus 08-UI-SPEC make the learner-facing boundary the proof point: identifier-safe daemon routes, the AgentAssist lifecycle, pending rubric presentation, and a blocking human UAT that records durable evidence before release.
Output: Two new API routes, one parity map, the AgentAssist/rubric UI, no-leak DOM tests, and a recorded UAT evidence file.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-CONTEXT.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-RESEARCH.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-PATTERNS.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-AI-SPEC.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-UI-SPEC.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-VALIDATION.md

<interfaces>
- `surfaces/daemon.py` API_ROUTES (line 70), ROUTE_CLI (line 109), API_FORBIDDEN_FIELDS (line 1070), api_read_json (line 1117), handle_api_start (line 1154), handle_api_submit (line 1273), session_index, DaemonHandler.
- `surfaces/quiz.py` page_for(bank_path, qs, serve=False, reveal=False, post_path="/answer", ...) (line 18) and record_answer (line 91).
- `surfaces/quiz_page.py` TEMPLATE (line 24), OFFLINE_JS (line 194), SERVED_JS (line 581) — the served client receives bootstrap metadata and drives POST /api/start|submit.
- 08-UI-SPEC Copywriting Contract is binding: exact strings for Help and evidence, Get optional guidance, Preparing optional guidance..., Generated support, the generated disclosure sentence, unavailable/policy-drop copy, Pending rubric suggestion — human review required, and Record human mark.
- Plan 08-04's session.do_hint/do_rubric_review return typed payloads; this plan only relays them through routes and renders them.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `surfaces/daemon.py` | `("POST", "/api/hint", "handle_api_hint")` and `("POST", "/api/rubric-review", "handle_api_rubric_review")` in API_ROUTES; ROUTE_CLI entries; SURFACE_PARITY map (route, CLI command, MCP tool name) covering every API route; API_FORBIDDEN_FIELDS extended with tier/profile/facts/candidate/proposal/marker/verdict |
| `surfaces/quiz_page.py` | `AGENT_ASSIST_HTML`, `ASSIST_JS`, assist slots in TEMPLATE, lifecycle/status markup, pending rubric checklist markup, exact UI-SPEC copy constants |
| `surfaces/quiz.py` | page_for assist-state threading; assist payload passed only from daemon-served mode |
| `tests/model_ui_roundtrip.py` | No-leak DOM/JSON/ARIA/CSS-off scans, lifecycle state assertions, responsive checks |
| `tests/daemon_roundtrip.py` | Route containment, forbidden-field 400s, parity-map assertions |
| `08-05-UAT-EVIDENCE.md` | Blocking human checkpoint verdict, environment, numbered checklist, revision notes |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — identifier-safe /api/hint and /api/rubric-review routes with one SURFACE_PARITY map</name>
  <files>surfaces/daemon.py, tests/daemon_roundtrip.py, tests/model_ui_roundtrip.py</files>
  <read_first>
    - `surfaces/daemon.py` API_ROUTES (line 70), ROUTE_CLI (line 109), API_FORBIDDEN_FIELDS (line 1070), api_read_json (line 1117), handle_api_start (line 1154), handle_api_submit (line 1273)
    - `08-PATTERNS.md` daemon.py section (identifier allowlist, SystemExit 400 / Exception 500, no raw paths)
    - `.planning/ROADMAP.md` Extensibility Rule 9(a) (SURFACE_PARITY creation duty)
    - `tests/daemon_roundtrip.py` existing route/parity assertions
  </read_first>
  <behavior>
    - Test 1: POST /api/hint with {"session_id": "..."} returns the typed hint payload from session.do_hint; a body naming tier, profile, facts, candidate, proposal, marker, or verdict is refused 400 before any handler runs (API_FORBIDDEN_FIELDS extension).
    - Test 2: POST /api/rubric-review resolves the session through session_index only; an unknown session id returns 400 without touching evidence; a SystemExit from do_* becomes 400 and any other exception becomes 500, matching handle_api_submit.
    - Test 3: SURFACE_PARITY exists with (route, CLI command, MCP tool name) rows for every API_ROUTES entry including the two new ones (reserved tool names: start, next, submit, report, hint, rubric_review); the parity test fails on a route without a CLI or tool name.
  </behavior>
  <action>
Add the two routes to API_ROUTES and their ROUTE_CLI entries (hint -> "hint", rubric-review -> "rubric-review") in surfaces/daemon.py. Extend API_FORBIDDEN_FIELDS with "tier", "profile", "facts", "candidate", "proposal", "marker", "verdict" so a client can never smuggle authority fields onto the wire (D-09). Implement handle_api_hint and handle_api_rubric_review following handle_api_submit exactly: api_read_json first, resolve the session id through session_index, wrap session.do_hint/do_rubric_review in the two-clause containment (SystemExit -> 400, Exception -> 500), and serialize only the returned typed payload. Create SURFACE_PARITY as a tuple of (route, cli_command, mcp_tool_name) covering every current API_ROUTES entry plus the new routes, and add the parity assertion to tests/daemon_roundtrip.py (keys equal to API_ROUTES, no missing CLI or tool name). Extend tests/daemon_roundtrip.py and tests/model_ui_roundtrip.py with the three behavior tests, including forbidden-field 400 fixtures and an allowlist-resolution fixture (unknown session -> 400, no evidence write).
  </action>
  <verify>
    <automated>python tests/daemon_roundtrip.py && python tests/model_ui_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Both routes return only typed payloads, refuse authority fields with 400, and contain SystemExit/Exception per the house pattern.
    - SURFACE_PARITY covers every API route with a CLI twin and reserved MCP tool name; the parity test passes.
    - Existing daemon tests stay green.
  </acceptance_criteria>
  <done>Two new routes behave exactly like every other API route, and the parity map exists before the UI consumes them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentAssist lifecycle, structural lock, and pending rubric checklist in the quiz page</name>
  <precondition>The plan 08-01 adversarial gate suite and plan 08-02 adapter parity suite are green (the UI-BLOCKED gate from 08-UI-SPEC): the executor asserts python tests/model_gate_roundtrip.py and python tests/model_adapter_roundtrip.py pass before wiring any learner-facing assist rendering.</precondition>
  <files>surfaces/quiz_page.py, surfaces/quiz.py, tests/model_ui_roundtrip.py</files>
  <read_first>
    - `08-UI-SPEC.md` Design System/Information Architecture (DOM order), Annotated Wireframes, Lifecycle State Machines, Copywriting Contract (exact strings), Components and State Inventory
    - `surfaces/quiz_page.py` TEMPLATE (line 24), SERVED_JS (line 581); `surfaces/quiz.py` page_for (line 18)
    - `.planning/UI-SPEC.md` section 8 (the nine accessibility gates)
    - `08-PATTERNS.md` quiz/quiz_page patterns if present; `surfaces/study.py` presentation.surface_shell composition for token use
  </read_first>
  <behavior>
    - Test 1: the served quiz page contains the assist region after the session details in DOM order (activity -> response/hint ladder -> status -> collapsed Help and evidence -> AgentAssist), with the exact UI-SPEC copy strings; the assist is collapsed and opt-in by default.
    - Test 2: requesting guidance renders the lifecycle states (Preparing optional guidance... -> Generated support with the generated disclosure sentence, or the exact unavailable/policy-drop copy); a locked tier renders a labeled structural lock with the unlock condition stated and no model voice.
    - Test 3: a short-response rubric proposal renders one pending row per point with the exact pending heading and no number, fraction, check, or cross glyph; no accept control exists anywhere in the browser DOM.
    - Test 4: no-leak scans over the page HTML, all JSON payloads, and SERVED_JS assert that key strings, tier numbers, fact text, profile names, backend classes, gate reasons, and dropped candidate phrases appear nowhere (flatten() discipline).
    - Test 5: the assist region works at 320px/200% zoom with no horizontal scroll; reduced motion disables nonessential animation; status changes announce once.
  </behavior>
  <action>
Add AGENT_ASSIST_HTML and ASSIST_JS to surfaces/quiz_page.py and an assist slot to TEMPLATE placed after the session details region (the DOM order the UI-SPEC locks). The assist ships as a native details/summary (Help and evidence), a Get optional guidance button, one bounded status line, a Generated support container with the exact disclosure sentence, and a ProvenanceDisclosure (generated label plus interaction id in the reviewer details only). ASSIST_JS wires the served client to POST /api/hint and POST /api/rubric-review, renders only typed payloads, announces each lifecycle state once via a polite aria-live region, and never renders reason codes, tier numbers, or provider detail. The structural lock (D-26) renders as a labeled lock with the unlock condition stated using UI-SPEC copy; the pending rubric checklist renders one semantic row per point with the pending token only. Thread the assist payload through surfaces/quiz.py page_for for daemon-served mode only (build/offline mode ships no assist). Use only the Phase 4 token set (spacing 4/8/16/24/32/48/64; type 12/16/20/32; weights 400/600; semantic tokens). Extend tests/model_ui_roundtrip.py with the five behavior tests, including DOM-order, exact-copy, no-leak, responsive, and reduced-motion assertions.
  </action>
  <verify>
    <automated>python tests/model_ui_roundtrip.py && python tests/daemon_roundtrip.py && python tests/serve_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - All five behavior tests pass, including the no-leak scans over HTML/JSON/JS and the 320px/200% check.
    - The exact UI-SPEC copy strings appear verbatim; no numeric/fractional suggestion rendering and no browser accept control exist.
    - serve_roundtrip and daemon_roundtrip stay green (offline build page unchanged).
  </acceptance_criteria>
  <done>The learner sees runtime-gated support in plain chrome, with the lock visible and the model voiceless.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Blocking human UAT — learner surfaces over the daemon, with durable evidence</name>
  <what-built>The full Phase 8 learner surface: AgentAssist lifecycle (requested/thinking/pass/drop/unavailable/cancelled/retry), generated-support disclosure and provenance, the structural lock, pending rubric checklist, offline/drop behavior, and the no-leak API/DOM boundary over the daemon.</what-built>
  <how-to-verify>
    1. Start the daemon on a temporary lesson bank (python itembank.py daemon . --no-open) and open the quiz at the printed localhost URL.
    2. Answer a question wrong in practice mode and confirm Help and evidence is collapsed, subordinate to the response controls, and opt-in; click Get optional guidance and observe exactly one polite status transition (Preparing optional guidance...) before a typed outcome — never a chat box or typing animation.
    3. With the default disabled backend, confirm the exact unavailable copy appears (Generated help is unavailable. You can keep learning with the lesson and authored hints.) and that the authored hint ladder and answer flow still work.
    4. If a backend profile is enabled, confirm passed guidance renders in the Generated support container with the generated disclosure sentence and no correctness claim; confirm the locked-tier structural lock labels its unlock condition.
    5. Submit a short answer and open the pending rubric suggestion: each point shows Pending rubric suggestion — human review required with pass/fail/uncertain rows, never a number, fraction, check, or cross glyph; confirm no accept button exists in the browser.
    6. Confirm Record human mark exists only in the CLI (itembank mark --session ... --proposal ...), and that after it the report shows the human mark.
    7. Resize to 1280, 768, 375, and 320px at 200% zoom: no horizontal scroll, keyboard-only operation of the disclosure and retry control, and a screen-reader/CSS-off inspection reveals no tier, key, fact, profile, backend, or drop detail.
    8. Record the verdict, environment (OS, browser, Python version), and each checklist result in .planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-05-UAT-EVIDENCE.md.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues found for revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser POST -> /api/* handler | Untrusted client input crosses here; authority fields are refused before handlers run |
| API payload -> DOM | Only typed runtime payloads cross; any private field in HTML/JSON/JS is a leak |
| pending proposal -> reviewer | The browser may render, never settle; acceptance lives only in the trusted local CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-40 | Information Disclosure | API/DOM payload boundary | critical | mitigate | API_FORBIDDEN_FIELDS extension, allowlist session resolution, no-leak scans over HTML/JSON/JS (flatten discipline) |
| T-08-41 | Tampering | authority fields on the wire | high | mitigate | tier/profile/facts/candidate/proposal/marker/verdict refused 400 before any handler (D-09) |
| T-08-42 | Spoofing | model voice / structural lock | high | mitigate | Fixed templates and exact UI-SPEC copy; locked tier renders a labeled structural lock, never provider text (D-26) |
| T-08-43 | Elevation of Privilege | browser accept control | high | mitigate | No accept/mark route or control exists in the browser; HumanMarkAction is CLI-only (08-04) |
| T-08-44 | Denial of Service | repeated assist requests | low | accept | One local user; per-interaction idempotency and explicit retry already bound generation cost |
| T-08-SC | Tampering | package operations | high | mitigate | This plan performs no package operation and no network fetch; vanilla HTML/CSS/JS only (UI-SPEC Registry Safety) |
</threat_model>

<verification>
- `python tests/model_ui_roundtrip.py` — DOM order, exact copy, lifecycle, no-leak, responsive, reduced motion
- `python tests/daemon_roundtrip.py` — route containment, forbidden fields, SURFACE_PARITY
- `python tests/serve_roundtrip.py` — offline build page unchanged
- `08-05-UAT-EVIDENCE.md` — blocking human checkpoint with PASS verdict (required before plan 08-06)
</verification>

<success_criteria>
- Both routes and the parity map are green; the learner UI shows only typed, runtime-gated content.
- Pending rubric suggestions have no accept path in the browser; the structural lock is visible and the model has no typographic voice.
- The blocking human UAT is recorded with a PASS verdict and durable evidence.
</success_criteria>

<output>
Create `.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-05-SUMMARY.md` when done, and record the checkpoint verdict in `08-05-UAT-EVIDENCE.md`.
</output>
