---
phase: 08-model-adapter-interface-tier-gate-enforcement
plan: 06
type: execute
wave: 5
depends_on:
  - "08-05"
files_modified:
  - tests/model_phase_roundtrip.py
autonomous: true
requirements:
  - TEACH-04
  - TEACH-05
  - TEACH-06
  - TEACH-07
  - TEACH-08
  - TEACH-09
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04
  - MODEL-05
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Plan 08-05's blocking human checkpoint has a durable `08-05-UAT-EVIDENCE.md` verdict of PASS with complete reviewer, environment, checklist, privacy, accessibility, and trusted-local-review evidence; a summary assertion is not sufficient."
    - "D-18/D-19: the release gate proves the hosted CLI profile is the default enabled backend and that no invented performance figures exist in code, docs, or plans."
    - "D-20/D-21/D-22/D-25: tier-3 self-assessment is the shipped pathway, suggestion_reveal defaults to after-self-mark, suggestions are --pending tokens, and no auto-accept path exists."
    - "D-23/D-24: mark_proposal events and N-boolean partial credit are proven end to end; no fractional score exists."
    - "One real end-to-end scenario proves latest-wrong-response guidance from hosted and local backends reaches CLI/API/browser only after the same runtime gate and is recoverable from evidence."
    - "The complete adversarial/offline matrix proves leak attempts drop in full and every unavailable provider leaves scoring, lessons, authored hints, evidence, reports, and human marking functional."
    - "Structural authority checks prove there is still one parser/model loader, one scorer, one assessment runner, one append-only evidence store/writer, and no surface/provider gains those responsibilities."
    - "The Phase 8 UI, AI evaluation, security, protocol, packaging, and UAT release gates are all executable and green."
  artifacts:
    - "08-05-UAT-EVIDENCE.md is the required machine-checkable human checkpoint input to this plan."
    - "tests/model_phase_roundtrip.py is the cross-surface Phase 8 release gate."
    - "08-VALIDATION.md provides the corpus, payload, failure, regression, and end-of-phase UAT matrix consumed by the gate."
  key_links:
    - "Plan 08-05 blocking checkpoint -> recorded PASS evidence -> Plan 08-06 precondition/release test"
    - "hosted/local provider -> shared adapter -> strict gate -> evidence -> CLI/API/browser parity"
    - "drop/unavailable -> authored Phase 6 loop -> normal score/evidence/report continuation"
    - "pending rubric proposal -> explicit human mark -> accepted evidence/report"
---

<objective>
Close Phase 8 only after consuming recorded PASS evidence from Plan 08-05's blocking human checkpoint, then run one cross-surface release gate over hosted/local parity, adversarial tier leakage, descriptor-only evidence, offline degradation, pending rubric review, trusted local human acceptance, payload privacy, and single-authority architecture.

Purpose: Passing unit slices is insufficient if surfaces bypass the gate, providers diverge, or model code quietly duplicates scoring/evidence/runner authority.
Output: A deterministic full-phase test that enforces every goal, requirement, locked decision, security boundary, and validation/UAT prerequisite.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-SPEC.md
@.planning/BLOCKER-AUDIT-2026-08-08.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-CONTEXT.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-RESEARCH.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-UI-SPEC.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-AI-SPEC.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-VALIDATION.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-01-SUMMARY.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-02-SUMMARY.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-03-SUMMARY.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-04-SUMMARY.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-05-SUMMARY.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-05-UAT-EVIDENCE.md
@.planning/phases/01-evidence-spine-protocol-foundation/01-01-SUMMARY.md
@.planning/phases/03-lesson-format-in-app-reader/03-CONTEXT.md
@.planning/phases/04-surface-redesign-theming/04-CONTEXT.md
@.planning/phases/05-check-item-type-code-editor/05-CONTEXT.md
@.planning/phases/06-hint-ladder-cursor-hold-feedback-modes/06-CONTEXT.md
@.planning/phases/07-selection-engine/07-CONTEXT.md
@tests/scoring_roundtrip.py
@tests/evidence_roundtrip.py
@tests/protocol_roundtrip.py
</context>

<interfaces>
- This plan adds no second implementation path. It drives the public CLI/daemon/browser interfaces, the loopback/fake-provider seams, and the existing evidence/report readers.
- Private sentinels are generated from real fixture item fields and injected provider candidates; absence is checked recursively in learner payloads and raw evidence where D-16 applies.
- Existing authority tests remain the source of truth for scorer/writer/protocol invariants; the new test adds Phase 8-specific AST/call-path assertions rather than replacing them.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — complete one hosted and one local guidance interaction across every surface</name>
  <precondition>Plans 08-01 through 08-05 test commands and the Phase 5/6 authority tests are green, and `.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-05-UAT-EVIDENCE.md` exists with `verdict: PASS`, complete required checklist rows, reviewer actor/timestamp/environment, privacy result, trusted-local reviewer event ID, and no unresolved failure/revision rows. The 08-05 summary alone does not satisfy this precondition.</precondition>
  <files>tests/model_phase_roundtrip.py</files>
  <behavior>
    - "A genuine wrong response followed by opt-in assist passes the same strict candidate through fake hosted CLI and loopback local HTTP profiles and returns an equal normalized shape in CLI and daemon/browser paths."
    - "Each path creates one retrievable interaction event linked to the response/tier and displays only the gated finite rendering plus generated disclosure."
    - "Changing only `active_profile` changes backend class, not request/result/public document shape or gate behavior."
    - "The Plan 08-05 checkpoint evidence parser rejects missing, FAIL, incomplete, stale, or revision-pending evidence before any aggregate release scenario runs."
  </behavior>
  <action>
    Begin by parsing `08-05-UAT-EVIDENCE.md` according to the required template in `08-VALIDATION.md`. Fail closed before provider setup if the file is absent; verdict is not PASS; plan/checkpoint ID mismatches; reviewer actor/timestamp/environment or automated-command results are absent; any responsive, keyboard, CSS-off, reduced-motion, screen-reader, privacy, adversarial-route, or local-reviewer row is missing/non-PASS; revision notes remain open; or the trusted reviewer event ID is absent. Do not infer approval from `08-05-SUMMARY.md` or a checkpoint resume string alone.

    Then build a standalone temporary-directory tracer using the repository's subprocess, loopback HTTP, real daemon, and packaged executable helpers. Create a lesson item with distinct key/rationale/tier sentinels, sit a practice session, submit a specific wrong response, request optional guidance through hosted CLI, retrieve the interaction by session, then repeat from a clean root with the local profile by changing only `active_profile`. Exercise both direct CLI and daemon endpoints used by the browser. Validate every adapter, response/event, report, usage-contract, and public API document against its published schema. Assert same normalized shapes, same gate decision/rendering, one durable event per interaction, correct backend-class audit metadata, and private sentinel absence in public output.
  </action>
  <verify>
    <automated>python tests/model_phase_roundtrip.py</automated>
  </verify>
  <done>Hosted and local profiles complete the same gated learner interaction through CLI/API/browser and evidence with settings-only switching and no public private-data exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise the adversarial, offline, retry, and pending-review release matrices end to end</name>
  <files>tests/model_phase_roundtrip.py</files>
  <behavior>
    - "Every fixture in `fixtures/model_gate_cases.json` executes through the public orchestration; protected/ambiguous candidates drop in full and permitted candidates alone render."
    - "Disabled, missing executable, non-zero exit, timeout, malformed/oversized/refused output, unreachable local endpoint, and HTTP failure preserve submit/scoring, lessons, authored hints, evidence, report, resume, and human mark."
    - "Duplicate interaction delivery does not regenerate; explicit retry is parent-linked; rubric suggestions stay pending until explicit human acceptance."
  </behavior>
  <action>
    Parameterize the aggregate test over the checked-in adversarial corpus and the full provider-failure matrix from 08-VALIDATION.md. Drive candidates through adapter normalization, gate, evidence, API, and learner envelope rather than calling only a low-level predicate. For every drop, inspect the complete public result and raw JSONL: no candidate fragment renders, and the log contains only the D-16 fingerprint/byte/reason descriptor. For every provider failure, continue through authored hint, correct/incorrect submit, report, resume, and explicit human mark.

    Add end-to-end duplicate-delivery and explicit retry cases, including a failing first child and successful retry. Add a `short` response scenario with all authored rubric points, pass/fail/uncertain pending suggestions, report retrieval, no scoring/selection change, then explicit human accept and replace. Prove accepted evidence changes only after the human action and the proposal remains immutable/auditable.
  </action>
  <verify>
    <automated>python tests/model_phase_roundtrip.py &amp;&amp; python tests/model_gate_roundtrip.py &amp;&amp; python tests/model_adapter_roundtrip.py &amp;&amp; python tests/model_evidence_roundtrip.py</automated>
  </verify>
  <done>The complete corpus/failure/retry/rubric matrices pass through real orchestration, drops are irrecoverable from learner/evidence payloads, and offline core operation is proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce single-authority, security, UI/AI, protocol, and packaging release gates</name>
  <files>tests/model_phase_roundtrip.py</files>
  <behavior>
    - "AST/call-path checks find one provider boundary and no model/surface-owned parser, scorer, assessment runner, event store, JSONL writer, tier progression, or accepted-mark path."
    - "Payload scans find no key/rationale/reserved fact/tier/backend/profile/secret/provider/drop-reason sentinel in API, HTML, DOM, accessible names, data attributes, scripts, or normal retrieval."
    - "All Plan 08 tests, existing scorer/evidence/protocol/hint/daemon/serve/agent/package regressions, AI corpus thresholds, and Phase 8 UI UAT evidence are green."
  </behavior>
  <action>
    Add structural assertions over the Phase 8 modules and surfaces: `model_adapter.invoke_model` is the only provider entry point; `runtime.score_response` remains the sole scorer; `evidence.append_event` remains the sole evidence writer; model code calls the existing loader/parser, Phase 5 assessment runner if present, Phase 6 entitlement/authored hint, and existing evidence/report APIs instead of recreating them. Assert model/provider modules cannot import surface state or accepted-mark functions, surfaces cannot import provider transports or tier-gate internals, and provider/gate paths cannot advance cursor/tier or execute assessment code.

    Execute recursive private/secret sentinel scans across normalized adapter docs, CLI stdout/stderr, daemon JSON, served HTML/JavaScript/ARIA/data attributes, raw evidence, safe readers, reports, and package output with the D-15/D-16 distinction. Run the AI-SPEC corpus count/category gate and the UI-SPEC contract checks; require the human-check result from Plan 08-05 in its summary before phase completion. The project adds no dependency, so package legitimacy has no install checkpoint; verify imports remain stdlib/project-local. Keep each subprocess bounded and use temporary roots so the release test runs in under 60 seconds.
  </action>
  <verify>
    <automated>python tests/model_phase_roundtrip.py &amp;&amp; python tests/model_surface_roundtrip.py &amp;&amp; python tests/model_ui_roundtrip.py &amp;&amp; python tests/scoring_roundtrip.py &amp;&amp; python tests/evidence_roundtrip.py &amp;&amp; python tests/protocol_roundtrip.py &amp;&amp; python tests/hint_roundtrip.py &amp;&amp; python tests/daemon_roundtrip.py &amp;&amp; python tests/serve_roundtrip.py &amp;&amp; python tests/agent_roundtrip.py &amp;&amp; python tests/packaging_roundtrip.py</automated>
  </verify>
  <done>All Phase 8 security/AI/UI/protocol/package gates pass, the UAT result is recorded, and no duplicate authority or private-data path exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| full provider path -> learner surfaces | Aggregate validation must catch bypasses that unit tests miss. |
| Phase 8 modules -> existing authorities | Model work must not gain parsing, scoring, runner, tier, evidence-store, or accepted-mark authority. |
| source build -> packaged executable | Security and protocol behavior must not drift between development and shipped artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-06-01 | Information Disclosure | cross-surface end-to-end path | critical | mitigate | Private-sentinel scans over adapter, evidence, CLI, API, DOM, reports, and package plus full adversarial corpus. |
| T-08-06-02 | Elevation of Privilege | duplicated scorer/runner/tier/mark authority | critical | mitigate | AST/call-path ownership assertions and existing scoring/hint/evidence/runner regressions. |
| T-08-06-03 | Tampering | provider/surface bypass of gate | critical | mitigate | Drive all public surfaces through the same candidate and assert identical gate outcome/rendering/event lineage. |
| T-08-06-04 | Repudiation | incomplete attempt history | high | mitigate | Cross-check requested interactions, retry lineage, safe evidence events, and session retrieval counts. |
| T-08-06-05 | Denial of Service | aggregate failure matrix | medium | mitigate | Bounded fake providers/subprocesses and assertions that core operations continue after every failure. |
| T-08-06-06 | Spoofing | source/package contract drift | medium | mitigate | Execute source and `.pyz` against the same profiles, schemas, usage contract, payload and evidence assertions. |
</threat_model>

<verification>
- `python tests/model_phase_roundtrip.py` is the Phase 8 aggregate release gate.
- Every Plan 08 local test and all existing authority/protocol/surface/package regressions in Task 3 must pass.
- Plan 08-05's recorded human-check must cover the viewport, zoom, keyboard, reduced-motion, CSS-off, screen-reader, provenance, offline, and pending/human-mark UAT in `08-VALIDATION.md`.
</verification>

<success_criteria>
- All 11 Phase 8 requirement IDs are demonstrated end to end.
- Hosted/local parity, settings-only switching, adversarial whole-drop, and offline degradation pass through public surfaces.
- Every interaction is safely durable; rubric proposals remain pending until explicit human acceptance.
- No private payload leak or duplicate parser/scorer/runner/evidence/writer/tier/mark authority exists.
- UI/AI/security/protocol/package/UAT gates are green.
</success_criteria>

## Multi-Source Coverage Audit

### GOAL

| Source item | Coverage | Plan/task |
|---|---|---|
| Model reads private key/rationale and latest specific wrong answer | COVERED | 08-01 T1 manifest; 08-02 T1 request; 08-06 T1 E2E |
| Runtime chooses permitted tier and overreach never renders | COVERED | 08-01 T1-T3; 08-06 T2 |
| Hosted/local one-line configuration parity | COVERED | 08-02 T1-T3; 08-06 T1 |
| Offline core loop remains functional | COVERED | 08-01 T3; 08-02 T1-T2; 08-05 T2; 08-06 T2 |
| Rubric output remains pending until explicit accept | COVERED | 08-03 T3; 08-04 T2; 08-05 T3; 08-06 T2 |
| Every interaction is logged and session-retrievable | COVERED | 08-03 T1-T2; 08-06 T1 |

### REQ

| Requirement | Coverage |
|---|---|
| TEACH-04 | 08-01, 08-02, 08-04, 08-05, 08-06 |
| TEACH-05 | 08-01, 08-05, 08-06 |
| TEACH-06 | 08-01, 08-05, 08-06 |
| TEACH-07 | 08-03, 08-04, 08-05, 08-06 |
| TEACH-08 | 08-03, 08-04, 08-05, 08-06 |
| TEACH-09 | 08-03, 08-04, 08-05, 08-06 |
| MODEL-01 | 08-02, 08-06 |
| MODEL-02 | 08-02, 08-06 |
| MODEL-03 | 08-01, 08-02, 08-03, 08-04, 08-05, 08-06 |
| MODEL-04 | 08-04, 08-06 |
| MODEL-05 | 08-03, 08-04, 08-05, 08-06 |

### RESEARCH

| Research feature/constraint | Coverage |
|---|---|
| Bounded fact-ID/span candidate and finite renderer | 08-01 T1 |
| Deterministic three-layer gate, ambiguity-as-drop, no rewriting | 08-01 T1-T2 |
| 30-case adversarial corpus with category minima | 08-01 T2; 08-06 T2 |
| Stdlib hosted subprocess and bounded local urllib transports | 08-02 T1-T2 |
| Fixed-array named profiles due validator constraints | 08-02 T3 |
| Descriptor-only dropped audit and full pass metadata | 08-03 T1-T2 |
| Pending per-point rubric proposal and human-only mark | 08-03 T3; 08-05 T3 |
| API/DOM private-payload boundary and generated provenance | 08-05 T1-T3 |
| Offline/failure matrix and single-authority structure | 08-06 T2-T3 |
| No new packages | 08-02 implementation; 08-06 T3 release assertion |

### CONTEXT

| Decision | Coverage |
|---|---|
| D-01 unified request/result | 08-02 T1 |
| D-02 config-only backend switch | 08-02 T3 |
| D-03 named profiles/secret references | 08-02 T3 |
| D-04 bounded typed unavailable | 08-02 T1-T2 |
| D-05 prompt is not the security boundary | 08-01 T1 |
| D-06 three-layer whole-candidate gate | 08-01 T1-T2 |
| D-07 claims/spans/source labels and ambiguity drop | 08-01 T1-T2 |
| D-08 pass/drop/unavailable public behavior | 08-01 T1-T3; 08-05 T2 |
| D-09 private fact/tier seam; Phase 6 owns progression | 08-01 T1; 08-04 T1 |
| D-10 latest genuine wrong response | 08-01 T1-T2; 08-04 T1 |
| D-11 authored ladder remains complete | 08-01 T3; 08-05 T2 |
| D-12 once per interaction; explicit retry | 08-03 T2; 08-04 T2 |
| D-13 pending per-point rubric suggestions | 08-03 T3; 08-05 T3 |
| D-14 no model score/accepted mark | 08-03 T3; 08-04 T2; 08-05 T3 |
| D-15 complete safe attempt metadata | 08-03 T1 |
| D-16 descriptor-only drops | 08-03 T2; 08-06 T2 |
| D-17 backend class audit | 08-02 T2; 08-06 T1 |
| D-18 hosted-first default and per-profile caps | 08-02 T3; 08-06 T1 |
| D-19 no invented performance figures; bench deferred | 08-02 T3; 08-06 T3 |
| D-20 tier-3 is not a scorer strategy | 08-03 T3; 08-06 T2 |
| D-21 self-assessment first; alternatives preserved | 08-03 T3; 08-04 T2; 08-05 T2 |
| D-22 suggestion_reveal and pending-token presentation | 08-04 T2; 08-05 T2 |
| D-23 mark_proposal event type; human-only guard unchanged | 08-03 T1/T3 |
| D-24 partial credit = N booleans | 08-03 T3 |
| D-25 auto-accept impossible; batch accept by human | 08-03 T3; 08-04 T2; 08-05 T2 |
| D-26 structural lock presentation | 08-05 T2 |
| D-27 third backend = module + config entry, stub test | 08-02 T3; 08-06 T1 |

Deferred persistent/free-form chat, raw dropped-output export, provider/model selection UI, and automatic model prose scoring are excluded exactly as directed. No GOAL, REQ, RESEARCH in-scope feature/constraint, or locked CONTEXT decision is missing.

<output>
Create `.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-06-SUMMARY.md` when done.
</output>
