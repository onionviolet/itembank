---
phase: 08-model-adapter-interface-tier-gate-enforcement
plan: 06
type: execute
wave: 5
depends_on:
  - "08-05"
files_modified:
  - tests/model_phase_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - TEACH-04
  - TEACH-05
  - TEACH-06
  - TEACH-07
  - TEACH-08
  - TEACH-09
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04
  - MODEL-05
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Plan 08-05's blocking human checkpoint has a durable 08-05-UAT-EVIDENCE.md verdict of PASS with environment, checklist, privacy, accessibility, and trusted-local-review evidence; a summary assertion alone is insufficient."
    - "D-18/D-19: the hosted CLI profile is the documented default when a backend is enabled, and no invented tok/s or latency figure exists in code, docs, or plans; measured elapsed_ms is the only timing field."
    - "D-20/D-21/D-22/D-25: tier-3 self-assessment is the shipped pathway, suggestion_reveal defaults to after-self-mark, suggestions render as pending tokens, and no auto-accept path exists in settings, CLI, daemon, or UI."
    - "D-23/D-24: mark_proposal events and N-boolean partial credit are proven end to end; no fractional score exists; the mark_event human-only guard is unchanged."
    - "One real end-to-end scenario proves latest-wrong-response guidance from hosted and local fake backends reaches CLI/API/browser only after the same runtime gate and is recoverable from evidence."
    - "The complete adversarial/offline matrix proves leak attempts drop in full and every unavailable provider leaves scoring, lessons, authored hints, evidence, reports, and human marking functional."
    - "Structural authority checks prove there is still one parser/model loader, one scorer, one assessment runner, one append-only evidence store/writer, and no surface or provider gained those responsibilities."
    - "All Phase 8 UI, AI-evaluation, security, protocol, packaging, and UAT release gates are executable and green; COVERAGE.md and the agent usage contract validate."
    - "Edge probes resolved as explicit predicates: replaying the full end-to-end scenario twice yields identical evidence statuses (idempotency); the offline matrix runs every row in a stable order; the corpus count is asserted as >= 30, never approximated."
  artifacts:
    - "08-05-UAT-EVIDENCE.md is the required machine-checkable human-checkpoint input to this plan."
    - "tests/model_phase_roundtrip.py is the cross-surface Phase 8 release gate."
    - "08-VALIDATION.md provides the corpus, payload, failure, regression, and end-of-phase UAT matrix consumed by the gate."
  key_links:
    - "plan 08-05 recorded PASS -> precondition of this plan's release test"
    - "hosted/local fake providers -> shared adapter -> strict gate -> evidence -> CLI/API/browser parity"
    - "drop/unavailable -> authored Phase 6 loop -> normal score/evidence/report continuation"
    - "pending rubric proposal -> explicit human mark -> accepted evidence/report"
  prohibitions:
    - statement: "No auto-accept setting, flag, or code path exists for tier-3 suggestions."
      status: unverified
      flagged: true
    - statement: "No invented performance figure appears in any Phase 8 artifact."
      status: unverified
      flagged: true
---

<objective>
**As the** release gate, **I want** one deterministic cross-surface suite that proves every Phase 8 promise at once, **so that** the phase ships only when the adversarial corpus, offline matrix, single-authority invariants, and recorded human UAT all agree.

Purpose: Passing unit slices is insufficient if surfaces bypass the gate, providers diverge, or model code quietly duplicates scoring/evidence authority (ROADMAP criteria 1-14; 08-VALIDATION.md Phase release gate).
Output: A deterministic full-phase test that enforces every requirement, locked decision, security boundary, and validation/UAT prerequisite.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-CONTEXT.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-RESEARCH.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-AI-SPEC.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-UI-SPEC.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-VALIDATION.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/COVERAGE.md

<interfaces>
- `tests/model_gate_roundtrip.py` — the 30-case adversarial corpus runner and payload-boundary scan (plan 08-01).
- `tests/model_adapter_roundtrip.py` — hosted/local parity, failure matrix, stub registration (plan 08-02).
- `tests/model_evidence_roundtrip.py` — interaction/proposal events and the human-accept boundary (plan 08-03).
- `tests/model_surface_roundtrip.py` — CLI orchestration and the agent usage contract (plan 08-04).
- `tests/model_ui_roundtrip.py` and `tests/daemon_roundtrip.py` — API allowlists, DOM privacy, SURFACE_PARITY (plan 08-05).
- Existing authority regressions: scoring_roundtrip, evidence_roundtrip, protocol_roundtrip, hint_roundtrip, daemon_roundtrip, serve_roundtrip, runner_roundtrip (when present).
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `tests/model_phase_roundtrip.py` | `run_full_scenario()` (hosted + local fake backends, gate, evidence, proposal, human mark), `run_offline_matrix()`, `run_authority_regressions()`, `run_payload_scan()`, `run_contract_audit()` (auto-accept absence, suggestion_reveal default, human-only guard, no invented figures, COVERAGE.md/usage-contract validation), and a ROADMAP criterion-to-assertion mapping |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — one cross-surface scenario through hosted and local backends, gate, evidence, proposal, and human mark</name>
  <precondition>08-05-UAT-EVIDENCE.md exists with a PASS verdict (plan 08-05 checkpoint resolved); the executor asserts the file exists and contains the PASS marker before running this plan's release scenario.</precondition>
  <files>tests/model_phase_roundtrip.py</files>
  <read_first>
    - `08-VALIDATION.md` Phase release gate and Verification map
    - `tests/agent_roundtrip.py` run() helper; `tests/evidence_roundtrip.py` fail()/flatten() harness
    - `08-CONTEXT.md` D-18/D-21/D-22/D-23/D-24/D-25 (the decisions this scenario proves end to end)
  </read_first>
  <behavior>
    - Test 1: with a fake hosted CLI profile, a wrong answer on an mc item produces a hint whose learner payload equals the gate-rendered output (status pass, generated text from fixed templates, exact learner span present); the model_interaction event is retrievable by session with the same interaction id.
    - Test 2: with the local loopback profile switched in via settings only, the identical scenario produces the same normalized payload shape (MODEL-02) and the same evidence field set.
    - Test 3: a short answer generates a pending mark_proposal; the human accepts via cmd_mark --proposal; marks_by_event returns the settled mark with marker human and proposal_ref; the response's review_state derives to marked.
    - Test 4: an adversarial candidate (key disclosure) invoked through the same path is dropped whole; the learner payload is the generic unavailable/drop copy and the evidence event stores descriptors only.
  </behavior>
  <action>
Create tests/model_phase_roundtrip.py with run_full_scenario(): build a session, submit a wrong answer, run do_hint with a fake hosted CLI profile (temp executable per plan 08-02 fixtures), assert the payload equals tier_gate.render output, then switch the active profile to a loopback OpenAI-compatible server and repeat with identical normalized shape; for the short item run do_rubric_review, assert pending points, then run cmd_mark --proposal with a human verdict and N-boolean rubric, asserting marks_by_event and review_state; append an adversarial gate drop fixture through the same path and assert the generic copy plus descriptor-only evidence. Reuse the run() subprocess helper and fail() harness; keep the test self-contained with temp directories and no network. Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/model_phase_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Hosted and local scenarios produce identical normalized payloads and evidence shapes; the drop scenario shows generic copy and descriptor-only evidence.
    - The pending proposal is settled only by the human mark with proposal_ref; review_state derives to marked.
    - The full scenario passes with the default disabled backend present in the offline matrix (Task 2 covers that matrix).
  </acceptance_criteria>
  <done>One real scenario proves adapter parity, gate authority, evidence recovery, and human-only acceptance in a single runnable test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline matrix, authority regressions, and the payload-boundary scan</name>
  <files>tests/model_phase_roundtrip.py</files>
  <read_first>
    - `08-VALIDATION.md` Offline and provider-failure matrix and Payload-boundary checks
    - `tests/evidence_roundtrip.py` flatten() discipline (lines 44-57)
    - `08-CONTEXT.md` D-04/D-08/D-11/D-16 and MODEL-03
  </read_first>
  <behavior>
    - Test 1: run_offline_matrix() executes every row (disabled, missing hosted executable, nonzero exit, timeout, malformed JSON, oversized output, refused output, unreachable endpoint, HTTP failure) and asserts submit/scoring, lessons, authored hints, evidence append/read, reports, and explicit human marking still pass — only generated assistance is unavailable.
    - Test 2: run_authority_regressions() invokes scoring_roundtrip, evidence_roundtrip, protocol_roundtrip, hint_roundtrip, daemon_roundtrip, serve_roundtrip (and runner_roundtrip when present) and asserts all pass unchanged.
    - Test 3: run_payload_scan() parses every learner-facing JSON document, HTML response, and rendered assist region produced by the scenario and asserts permitted-tier indexes, item keys, rationales, correct answers, reserved facts, source text, provider-native output, secret references, profile identifiers, gate reasons, and dropped candidate text are absent.
    - Test 4: the corpus count is asserted as at least 30 from fixtures/model_gate_cases.json.
  </behavior>
  <action>
Add run_offline_matrix() iterating the nine provider-failure rows in a fixed order; each row runs the full core loop (submit a correct and a wrong answer, request a hint, read evidence, render a report, record a human mark) through surfaces/session and surfaces/evidence_cli, asserting only the generated-assist payload changes. Add run_authority_regressions() calling each listed test module as a subprocess and failing on any non-zero exit. Add run_payload_scan() reusing the flatten() forbidden-phrase discipline over every JSON/HTML/JS artifact the scenario touches, with forbidden material derived from the fixture items. Add the corpus count assertion (len(load(fixtures/model_gate_cases.json)) >= 30). Wire all four into main() after run_full_scenario.
  </action>
  <verify>
    <automated>python tests/model_phase_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - All nine offline rows pass with only generated assistance unavailable; all authority regressions are green.
    - The payload scan finds no private material in any learner-facing artifact; the corpus count is >= 30.
  </acceptance_criteria>
  <done>The degrade-never-block promise and the no-leak promise are machine-checked across every failure row and every payload surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contract and decision audit — the phase's final authority gate</name>
  <files>tests/model_phase_roundtrip.py</files>
  <read_first>
    - `08-CONTEXT.md` D-18..D-27 (round-two rulings) and `08-ROADMAP.md` Phase 8 criteria 8-14
    - `08-VALIDATION.md` Phase release gate
    - `schemas/settings.schema.json` (post-plan-08-02 shape) and `schemas/agent_usage.schema.json` (plan 08-04)
  </read_first>
  <behavior>
    - Test 1: no auto-accept path exists: grep settings schema and all Phase 8 modules for auto-accept-like keys/flags returns zero matches; the test asserts the absence structurally.
    - Test 2: suggestion_reveal defaults to after-self-mark with all three enum values; mark_event still raises ValueError for any marker other than human (guard regression); no fractional score field exists in any event schema or builder.
    - Test 3: no invented performance figure appears in model_adapter.py, tier_gate.py, evidence.py, or the Phase 8 docs — the only timing field is measured elapsed_ms.
    - Test 4: schemas/agent_usage.schema.json and COVERAGE.md validate/parse; the hosted CLI profile is documented as the default enabled backend (D-18); every ROADMAP Phase 8 criterion 1-14 maps to a green assertion recorded in the test.
  </behavior>
  <action>
Add run_contract_audit() to tests/model_phase_roundtrip.py: assert schemas/settings.schema.json contains no property whose name matches an auto-accept pattern (read the schema as JSON and walk keys); assert suggestion_reveal enum equals the three locked values with default after-self-mark; assert evidence.mark_event raises ValueError for marker "model"; assert the new event builders' output dicts contain no score key; run a targeted scan over model_adapter.py, tier_gate.py, evidence.py, and the plan/COVERAGE docs asserting no tok/s-style or fabricated latency literal appears (the only timing field is elapsed_ms); validate schemas/agent_usage.schema.json and parse COVERAGE.md's matrix table; assert the agent_usage contract names hosted_cli as the default-profile documentation. Record a ROADMAP-criterion-to-assertion mapping dict in the test (each of criteria 1-14 keys to the assertion that proves it) and print it on completion.
  </action>
  <verify>
    <automated>python tests/model_phase_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - All four audit assertions pass, including the auto-accept absence scan and the invented-figure scan.
    - The criterion-to-assertion mapping covers ROADMAP criteria 1-14 and prints green.
    - schema_validate.py --all and the full suite pass.
  </acceptance_criteria>
  <done>The release gate closes Phase 8 only on recorded UAT PASS, green adversarial/offline evidence, and structural proof that the runtime — not the model — still owns every authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| release gate -> phase completion | The gate's own assertions are the last authority; a false green here ships a broken promise |
| learner-facing artifacts -> scan | Every payload surface is re-checked for private material at release time |
| decision audit -> settings/code | Auto-accept and invented-figure absence must be structural, not conventional |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-50 | Tampering | release gate completeness | high | mitigate | Criterion-to-assertion mapping covers ROADMAP 1-14; corpus count and offline matrix are counted, not approximated |
| T-08-51 | Information Disclosure | payload re-scan | critical | mitigate | flatten()-style scan over every learner-facing artifact with fixture-derived forbidden material (VALIDATION.md Payload-boundary checks) |
| T-08-52 | Tampering | auto-accept reintroduction | high | mitigate | Structural key walk over settings schema plus code scan; absence asserted by the test, not by convention (D-25) |
| T-08-53 | Spoofing | fabricated performance claims | medium | mitigate | Invented-figure scan over modules/docs; elapsed_ms is the only timing field (D-19) |
| T-08-54 | Repudiation | UAT evidence | medium | mitigate | 08-05-UAT-EVIDENCE.md must contain the PASS verdict and environment details; the gate reads the file, not a chat summary |
| T-08-SC | Tampering | package operations | high | mitigate | This plan performs no package operation and no network fetch |
</threat_model>

<verification>
- `python tests/model_phase_roundtrip.py` — full scenario, offline matrix, authority regressions, payload scan, contract audit
- Full suite: `for t in tests/*.py; do python "$t" || exit 1; done`
- `python schema_validate.py --all`
- `08-05-UAT-EVIDENCE.md` PASS precondition
</verification>

<success_criteria>
- The cross-surface scenario is green for hosted and local fake backends; drops stay generic and descriptor-only.
- The offline matrix and all authority regressions pass; the payload scan is clean.
- The contract audit proves no auto-accept, no invented figures, pending-token rendering, N-boolean credit, and the unchanged human-only mark guard.
</success_criteria>

<output>
Create `.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-06-SUMMARY.md` when done.
</output>
