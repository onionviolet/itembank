---
phase: 03.2
slug: seeding-import-provenance
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-10
---

# Phase 03.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Executed to completion 2026-08-11 (plans 01–05 committed to main:
> `f799c05`, `dc9c64c`, `b8fed76`, `07e6596`, `b301f0e`). Closed out
> 2026-08-11 on branch `gsd/phase-03.2-close` (worktree `.phase032-wt`):
> every automated command in the matrix below was run **live** in the
> worktree (WSL python 3.13.3) and recorded green; the full suite and the
> repo gates were re-run at close-out with honest attribution in
> `03.2-GATES.md`, `03.2-VERIFICATION.md` and `03.2-UAT.md`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — direct Python script execution (`tests/*_roundtrip.py`), stdlib only |
| **Config file** | none |
| **Quick run command** | `python tests/import_roundtrip.py && python tests/seeding_roundtrip.py` |
| **Full suite command** | `python tests/protocol_roundtrip.py && python tests/scoring_roundtrip.py && python tests/evidence_roundtrip.py && python tests/serve_roundtrip.py && python tests/day_roundtrip.py && python tests/agent_roundtrip.py && python tests/import_roundtrip.py && python tests/seeding_roundtrip.py` |
| **Estimated runtime** | ~3 min measured at close-out (phase files: import 3.9s, seeding 2.5s, lesson 16.4s, config 17.0s, daemon 32.8s, protocol 13.2s; full 45-file suite ≈ 8.5 min, dominated by `phase_062_audit.py` re-running the suite internally) |

---

## Sampling Rate

- **After every task commit:** Run the quick run command
- **After every plan wave:** Run the full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** < 240s per-file (measured at close-out: longest
  single phase file 32.8s; the 257s outlier is `phase_062_audit.py`, which
  re-runs the whole suite inside itself)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 032-01-01 | 01 | 1 | SEED-01 | T-032-01 / T-032-02 / T-032-04 | Container reader: ZIP member detection, conditional zstd decode through the pinned+checksummed dependency, legacy SQLite member dependency-free, invalid container fails by name, media names resolve through the media map with path-traversal refusal | unit | `python tests/import_roundtrip.py` | ✅ | ✅ green |
| 032-01-02 | 01 | 1 | SEED-01 | T-032-01 / T-032-03 | Note-type map with loud per-note refusal (never an approximation); every candidate passes `model.lint()` before convertible; exhaustive per-note report with zero silent drops | unit | `python tests/import_roundtrip.py` | ✅ | ✅ green |
| 032-01-03 | 01 | 1 | SEED-09 | T-032-01 | Import CLI stages + reports without ever writing a bank directly; writes happen only through the accept loop (D-06) | unit | `python tests/import_roundtrip.py` | ✅ | ✅ green |
| 032-02-01 | 02 | 1 | SEED-04 | T-032-05 / T-032-06 | `## SOURCES` registry is additive; `[SRC:]/[OBJ:]` resolve through it; an unresolvable id is a lint error naming the id and the file; registry shape validates through the supported keyword set | unit | `python tests/lesson_roundtrip.py` | ✅ | ✅ green |
| 032-02-02 | 02 | 1 | SEED-08 | T-032-07 | Objective coverage map computed on demand from bank + registry, never stored; reflects current bank state | unit | `python tests/import_roundtrip.py` | ✅ | ✅ green |
| 032-02-03 | 02 | 1 | SEED-09 | — | Compatibility floor: a bank using none of the new constructs parses and renders byte-identically | unit | `python tests/lesson_roundtrip.py` | ✅ | ✅ green |
| 032-03-01 | 03 | 2 | SEED-02 | T-032-08 / T-032-10 | Six-stage pipeline with locked order; the human accept is the only write path; a cancelled run writes nothing | unit | `python tests/seeding_roundtrip.py` | ✅ | ✅ green |
| 032-03-02 | 03 | 2 | SEED-03 | T-032-09 | One accept endpoint behind two surfaces (CLI + daemon route); loopback-only authority gate; lint validation on every candidate | unit | `python tests/seeding_roundtrip.py && python tests/daemon_roundtrip.py` | ✅ | ✅ green |
| 032-03-03 | 03 | 2 | SEED-02 / SEED-03 | T-032-11 | Batch-framed UI (no chat affordance, no fill bar, staged progress) + degrade contract: backend unreachable refuses by name, every other command still works | unit | `python tests/seeding_roundtrip.py` | ✅ | ✅ green |
| 032-04-01 | 04 | 2 | SEED-05 | T-032-12 | Winnowing paraphrase lint over fingerprints only: ≥8 consecutive copied words errors `prov.paraphrase_copy`, Jaccard > 0.25 warns `prov.paraphrase_overlap`, both thresholds are settings; source text never stored or echoed | unit | `python tests/lesson_roundtrip.py && python tests/config_roundtrip.py` | ✅ | ✅ green |
| 032-04-02 | 04 | 2 | SEED-06 | T-032-13 / T-032-14 | `style.unsourced_specific` is structural (numerals/units without a resolved `[SRC:]` error); `[CASE:]`/`[PREREQ:]` grammar with cycle + unresolvable-target lint naming id/file/path | unit | `python tests/lesson_roundtrip.py` | ✅ | ✅ green |
| 032-04-03 | 04 | 2 | SEED-05 | T-032-12 | Deterministic stage-4 gate checks paraphrase + unsourced drafts before any critique; headless and deterministic | unit | `python tests/seeding_roundtrip.py` | ✅ | ✅ green |
| 032-05-01 | 05 | 3 | SEED-07 | T-032-15 / T-032-16 | Extended guard refuses corpus-shaped content anywhere outside fixtures/; calibration runs every style warning over the corpus, records per-warning FP rates, and sets ship-state (disabled above 0.20) | unit | `python tests/import_roundtrip.py && python itembank.py guard .` | ✅ | ✅ green |
| 032-05-02 | 05 | 3 | SEED-01..09 | T-032-17 | SEED audit 9/9 (each id in a plan frontmatter with a named verification); D-19 separation fixture (item write + prose write never contend one file); full suite + schema validation | integration | `for f in tests/*_roundtrip.py; do python "$f" || exit 1; done` | ✅ | ✅ green (03.2 files; suite-level attribution in GATES) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Wave 1 (plans 01–02) landed with green quick runs: `import_roundtrip.py` (20) and `lesson_roundtrip.py` (ok) — recorded in 03.2-01/02-SUMMARY.md and re-run green at close-out.
- [x] Wave 2 (plans 03–04) landed with green runs: `seeding_roundtrip.py` (24), `daemon_roundtrip.py` (ok), `lesson_roundtrip.py` (ok), `config_roundtrip.py` (ok) — re-run green at close-out.
- [x] Wave 3 (plan 05) closed with the guard extension, calibration, the SEED 9/9 audit, and the full suite — re-run at close-out (see 03.2-GATES.md §Plan-05 gates).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real-corpus calibration of every Phase 3.1 style warning | SEED-07 (D-17/D-18) | The real EMT/Math/CS corpus lives beside the private bank, outside this repository, by design (guard keeps it out of CI). The synthetic-corpus measurement is automated (calibration fixture, `import_roundtrip.py`); the private corpus cannot be fetched by CI or a public worktree. | On a machine holding the private corpus: `python itembank.py calibrate <path-to-private-corpus-dir> --out <report>` and record the per-warning FP rates in `03.2-CALIBRATION.md`; the guard extension (`python itembank.py guard .`) proves the corpus stays out of CI. Human/private-bank action. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (14/14 tasks; every row above ran green live at close-out)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (wave 1: 6/6 automated, wave 2: 6/6, wave 3: 2/2)
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 240s (measured: max phase-file run 32.8s; only `phase_062_audit.py`'s internal suite re-run exceeds it, and that file is phase 06.2's)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved — validated 2026-08-11 at close-out on
`gsd/phase-03.2-close` (worktree `.phase032-wt`). Phase tests re-run live
all green: import 20/20, seeding 24/24, lesson/config/daemon/protocol ok;
`lint fixtures/sample_bank.md` 0 errors; `guard .` 0 offending files.
Full-suite result and failure attribution in `03.2-VERIFICATION.md`,
`03.2-UAT.md`, and `03.2-GATES.md` (all four suite failures are
post-03.2 phases / environment, none are 03.2 regressions).
