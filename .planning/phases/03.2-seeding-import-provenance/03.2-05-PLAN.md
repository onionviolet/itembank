---
phase: 03.2-seeding-import-provenance
plan: 05
type: verify
wave: 3
depends_on:
  - 03.2-01
  - 03.2-02
  - 03.2-03
  - 03.2-04
files_modified:
  - itembank.py
  - tests/import_roundtrip.py
  - tests/seeding_roundtrip.py
  - tests/lesson_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
  - SEED-04
  - SEED-05
  - SEED-06
  - SEED-07
  - SEED-08
  - SEED-09
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "The real EMT / Math / CS corpus lives beside the private bank, outside this repository, and itembank guard's extended globs keep it out of CI (D-17, SEED-07)."
    - "The calibration run records a false-positive rate for every Phase 3.1 style warning; a warning ships enabled only with a recorded rate, and above ~20% it ships disabled by default (D-18)."
    - "Seeding writes items and style passes write prose - the two-file layout means they never contend for one file (D-19)."
    - "Every one of SEED-01..SEED-09 is addressed by a plan and verified by a named fixture or command."
    - "The full test suite passes green at phase end, including all pre-existing tests."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
  prohibitions:
    - statement: "Real corpus data must never enter the repository or CI."
      status: unverified
      flagged: true
    - statement: "A style warning must not ship enabled without a recorded false-positive rate."
      status: unverified
      flagged: true
---

<objective>
As the phase, I want the corpus residency guard, the calibration deliverable, and the full suite all proven - so seeding seals on evidence that real content is staged to arrive safely and every Phase 3.1 warning is calibrated against it.

Purpose: The phase-level verification plan: the guard extension, the calibration run, the requirement audit, and the full-suite run.
Output: The extended guard, the recorded calibration rates, 03.2-GATES.md, and the green full-suite run.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.2-seeding-import-provenance/03.2-CONTEXT.md
@.planning/phases/03.2-seeding-import-provenance/03.2-RESEARCH.md
@.planning/phases/03.2-seeding-import-provenance/03.2-UI-SPEC.md

<interfaces>
- `itembank.py cmd_guard` extends its globs; it remains the one CI gate.
- The calibration command reads the corpus (outside the repo) and writes recorded rates beside the checks.
- The fixture harness is the repo's stdlib fail() pattern.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `itembank.py` | Extended guard globs for the corpus residency rule (D-17) |
| Phase directory | `03.2-CALIBRATION.md` - the recorded per-warning false-positive rates (D-18) |
| Phase directory | `03.2-GATES.md` - the verification-gate mapping |
| `tests/import_roundtrip.py` / `tests/seeding_roundtrip.py` / `tests/lesson_roundtrip.py` | The finalized fixtures |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the guard and run the calibration</name>
  <files>itembank.py, tests/import_roundtrip.py</files>
  <read_first>
    - `itembank.py` cmd_guard - the existing globs and CI gate
    - `03.2-CONTEXT.md` D-17/D-18
    - `model.py` - the Phase 3.1 style warning codes being calibrated
  </read_first>
  <behavior>
    - Test 1: the extended guard fails CI on any real-corpus-shaped file (a fixture with a corpus marker in a temp dir is refused).
    - Test 2: the calibration command runs against the corpus and records a false-positive rate for every style warning; a warning above the 20% threshold is recorded as disabled-by-default.
  </behavior>
  <action>
Extend `itembank guard`'s globs to cover the corpus residency rule (D-17): real-corpus markers (the private bank directory and its known shapes) fail CI, extending the existing guard rather than adding a second one. Implement the calibration command: run each Phase 3.1 style warning over the corpus, compute the false-positive rate per warning, record the rates in `03.2-CALIBRATION.md`, and mark warnings above ~20% disabled by default (D-18). Write the two behavior tests first.
  </action>
  <verify>
    <automated>python tests/import_roundtrip.py &amp;&amp; python itembank.py guard</automated>
  </verify>
  <acceptance_criteria>
    - The guard fixture refuses corpus-shaped content.
    - 03.2-CALIBRATION.md records a rate for every style warning with the ship-state decision.
  </acceptance_criteria>
  <done>Real content stays out of the repo, and every style warning's usability is measured, not assumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Requirement audit, D-19 separation, and the full suite</name>
  <files>tests/import_roundtrip.py, tests/seeding_roundtrip.py, tests/lesson_roundtrip.py</files>
  <read_first>
    - `REQUIREMENTS.md` - the SEED-01..09 rows and traceability
    - `ROADMAP.md` - the Phase 3.2 section
    - The four plan files' requirements fields and acceptance criteria
  </read_first>
  <behavior>
    - Test 1: every one of SEED-01..SEED-09 appears in a plan with a named verification.
    - Test 2: the D-19 separation fixture proves seeding writes items and the lesson-layout keeps prose separate - no single file receives both item and prose writes in one pass.
    - Test 3: the full suite (`for f in tests/*_roundtrip.py; do python "$f" || exit 1; done`) exits 0.
  </behavior>
  <action>
Write the SEED-01..09 coverage audit (fail() harness), add the D-19 separation fixture (an item write and a prose write in the two-file layout never contend for one file), run the full suite plus the schema validator, and record the results in `03.2-GATES.md` with the deterministic edge-probe skip noted (grep each PLAN.md for the skip sentence). Fix any failures this surfaces (fixtures only - no product-code changes in this plan). Write the three behavior tests first.
  </action>
  <verify>
    <automated>for f in tests/*_roundtrip.py; do python "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - The audit exits 0 with 9/9 SEED-IDs covered.
    - The D-19 separation fixture passes; the full suite and schema validator pass.
  </acceptance_criteria>
  <done>Phase 3.2 seals on evidence: corpus residency, calibration, separation, and full coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| corpus -> repository | Real learner data and AAOS-derivative content must never enter the repo or CI |
| calibration rates -> shipped warnings | A warning must not ship enabled on an unmeasured rate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-032-15 | Information Disclosure | corpus in CI | high | mitigate | The extended guard fails on corpus-shaped content (D-17); the fixture proves it |
| T-032-16 | Spoofing | calibration rates | medium | mitigate | 03.2-CALIBRATION.md records per-warning rates with the ship-state decision (D-18) |
| T-032-17 | Integrity | item/prose contention | medium | mitigate | The D-19 separation fixture proves no single file receives both writes |
| T-032-SC | Tampering | package operations | high | mitigate | This plan performs no package operation |
</threat_model>

<verification>
- The full-suite loop.
- `python itembank.py guard`.
- The 9/9 requirement-coverage line.
</verification>

<success_criteria>
- The corpus residency guard is extended and proven; calibration rates are recorded.
- 9/9 SEED-IDs map to plans and named verifications.
- The full suite passes with the D-19 separation proof.
</success_criteria>

<output>
Create `.planning/phases/03.2-seeding-import-provenance/03.2-05-SUMMARY.md` when done.
</output>
