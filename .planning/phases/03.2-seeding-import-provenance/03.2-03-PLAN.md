---
phase: 03.2-seeding-import-provenance
plan: 03
type: execute
wave: 2
depends_on:
  - 03.2-01
files_modified:
  - surfaces/seeding.py
  - surfaces/cli.py
  - surfaces/daemon.py
  - tests/seeding_roundtrip.py
autonomous: true
ui_classification: AUTHORING-SURFACE
requirements:
  - SEED-02
  - SEED-03
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Draft - lint - retry - explicit human accept, one item at a time; nothing writes to a bank without an accept, and a cancelled run writes nothing (D-06, SEED-02). The widening and re-sequencing of this phase stands - seeding lands before Phase 5, because content and adherence, not features, are the real bottleneck (D-01)."
    - "The accept loop ships as both a CLI command and a daemon route - one accept endpoint, two surfaces, never two implementations of the decision (D-07, SEED-03)."
    - "The six-stage order is locked: deterministic source selection - outline-only - per-section drafting - deterministic checks before any model critique - independent CoVe-style verification - human accept (D-08)."
    - "Authoring is a batch operation and the UI says so: staged progress and a cancel, never interactive chat (D-09, 03.2-UI-SPEC 2.2)."
    - "With no model backend reachable, seeding refuses by name and every other command in the phase still works (D-10)."
    - "The default authoring backend is a hosted coding-agent-class model; prompt-size and latency budgets are written against it (Directive 1)."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
  prohibitions:
    - statement: "No item may be written to a bank without an explicit human accept."
      status: unverified
      flagged: true
    - statement: "The six-stage order may never be reordered."
      status: unverified
      flagged: true
---

<objective>
As the author, I want to draft real items against the corpus and accept them one at a time - with the runtime checking every draft before a model critiques it, and a human holding the pen at the end.

Purpose: The seeding-loop plan: the six-stage pipeline (D-08), the one accept endpoint behind both the CLI and the daemon (D-07), the batch-framed UI (D-09), and the refuse-by-name degrade (D-10).
Output: surfaces/seeding.py, the accept route + CLI, the batch UI, and tests/seeding_roundtrip.py.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.2-seeding-import-provenance/03.2-CONTEXT.md
@.planning/phases/03.2-seeding-import-provenance/03.2-RESEARCH.md
@.planning/phases/03.2-seeding-import-provenance/03.2-UI-SPEC.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-CONTEXT.md

<interfaces>
- The model adapter interface (Phase 8's contract, hosted-first per Directive 1) is the backend seam the pipeline calls; a stub/fake adapter satisfies tests.
- `model.lint()` is the deterministic gate before any model critique (D-08 stage 4).
- The daemon's loopback authority gate covers the accept route (mutating write).
- `03.2-UI-SPEC.md` sections 2 (accept loop surface) and 4 (copy contract) are the binding renderer contract.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `surfaces/seeding.py` | `run_seeding_run(...)` - the six-stage pipeline; `accept_candidate(...)` - the one accept endpoint; the draft-set store (in-memory per run) |
| `surfaces/cli.py` | `itembank seed <bank>` CLI |
| `surfaces/daemon.py` | `POST /seed/accept` route + the batch-surface render |
| `tests/seeding_roundtrip.py` | Six-stage order, accept-gate, cancel, degrade, and batch-framing fixtures |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The six-stage pipeline with the locked order</name>
  <files>surfaces/seeding.py, tests/seeding_roundtrip.py</files>
  <read_first>
    - `03.2-CONTEXT.md` D-08 (the locked order) and D-06
    - `08-CONTEXT.md` - the adapter interface contract (hosted-first)
    - `model.py` lint() - the deterministic gate
  </read_first>
  <behavior>
    - Test 1: a run walks the six stages in the locked order - deterministic source selection, outline-only, per-section drafting, deterministic checks before any model critique, independent verification, human accept - with each stage's output visible.
    - Test 2: a draft that fails the deterministic checks (stage 4) is retried before any model critique; a retry cap is enforced.
    - Test 3: with a fake adapter returning deterministic output, the pipeline is fully testable headless; a cancelled run writes nothing.
  </behavior>
  <action>
Implement `run_seeding_run(...)` in `surfaces/seeding.py`: the six stages in the exact locked order (D-08) - (1) deterministic source selection from the ## SOURCES registry, (2) outline-only draft, (3) per-section drafting, (4) deterministic checks via `model.lint()` and the paraphrase/unsourced checks (plan 03.2-04) BEFORE any model critique, (5) independent CoVe-style verification (a second model pass over the deterministic-clean draft), (6) human accept. A draft failing stage 4 retries within the configured cap; a cancelled run writes nothing (D-09). The adapter seam is the Phase 8 contract (hosted-first per Directive 1); tests use a fake adapter. Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/seeding_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The stage order is asserted structurally (a fixture walks the stages and fails on reorder).
    - Retry-before-critique and cancel-writes-nothing pass.
  </acceptance_criteria>
  <done>The pipeline is deterministic-first, model-second, human-last - in that locked order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The one accept endpoint behind two surfaces</name>
  <files>surfaces/seeding.py, surfaces/cli.py, surfaces/daemon.py, tests/seeding_roundtrip.py</files>
  <read_first>
    - `03.2-CONTEXT.md` D-06/D-07
    - `surfaces/daemon.py` - the loopback authority gate and route registration
    - `surfaces/cli.py` - ROUTE_CLI parity
  </read_first>
  <behavior>
    - Test 1: `itembank seed <bank>` and the daemon's accept route call the SAME accept_candidate function - one implementation, two surfaces (D-07).
    - Test 2: accepting an item with lint errors is refused; accepting a clean item writes it to the bank exactly once; skipping defers (reported at the end, never discarded silently).
    - Test 3: the route sits behind the loopback authority gate (no network writes).
  </behavior>
  <action>
Implement `accept_candidate(...)` as the single accept path (D-07): validate the candidate through `model.lint()` (refusing on errors), append the accepted item to the bank, and record the accept in the run's report. Register `itembank seed <bank>` in `surfaces/cli.py` and the accept route in `surfaces/daemon.py` behind the loopback authority gate - both call the same function. Skipped items stay in the draft set and are reported at the end (UI-SPEC 2.1). Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/seeding_roundtrip.py &amp;&amp; python tests/daemon_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The CLI and route share one implementation (import-grep proof).
    - Accept-gate and skip-deferral fixtures pass; the route is loopback-only.
  </acceptance_criteria>
  <done>One accept decision, two surfaces, no drift between them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The batch-framed UI and the degrade contract</name>
  <files>surfaces/daemon.py, surfaces/seeding.py, tests/seeding_roundtrip.py</files>
  <read_first>
    - `03.2-UI-SPEC.md` sections 2 (the accept-loop surface), 4 (copy), 6 (verification gates)
    - `03.2-CONTEXT.md` D-09/D-10
  </read_first>
  <behavior>
    - Test 1: the batch surface renders the exact framing line, staged progress, Accept/Skip/Cancel controls, and no chat affordance and no percentage/fill bar (UI-SPEC 2.2).
    - Test 2: with the backend unreachable, the exact refusal line renders and no draft starts; import/lint/coverage still work (D-10).
    - Test 3: the copy fixture asserts every UI-SPEC 4 string verbatim in its assigned voice.
  </behavior>
  <action>
Render the accept-loop browser surface per 03.2-UI-SPEC sections 2 and 4: the batch-framing line, the stage line, the candidate item, the lint block, Accept/Skip/Cancel controls (Accept disabled with the reason on lint errors - never a silent dead button), and the cancelled summary `Nothing was written.` Add the degrade branch: with no backend reachable, render the exact refusal line and never start a draft (D-10). Wire the copy fixture asserting the section 4 strings verbatim. Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/seeding_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The batch-framing, cancel, and degrade fixtures pass with verbatim copy.
    - No chat affordance and no fill bar exist anywhere in the surface markup.
  </acceptance_criteria>
  <done>The loop reads as the batch operation it is - honest, staged, cancellable, and human-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| model drafts -> bank | Model output is never accepted evidence or a bank write without the human accept |
| accept route -> bank | The mutating write must be loopback-only and lint-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-032-08 | Elevation of Privilege | model output writing a bank | high | mitigate | The human accept is the only write path (D-06); the deterministic lint gate runs before any model critique (D-08) |
| T-032-09 | Tampering | accept route | high | mitigate | Loopback-only authority gate + lint validation on every candidate |
| T-032-10 | Integrity | cancelled run | medium | mitigate | A cancelled run writes nothing; the fixture asserts it |
| T-032-11 | Availability | backend unreachable | medium | mitigate | Refuse-by-name for seeding; every other command still works (D-10) |
| T-032-SC | Tampering | package operations | high | mitigate | This plan performs no package operation |
</threat_model>

<verification>
- `python tests/seeding_roundtrip.py`
- `python tests/daemon_roundtrip.py`
- Import-grep: the CLI and route share accept_candidate.
</verification>

<success_criteria>
- The six stages run in the locked order with deterministic checks before critique.
- One accept endpoint serves both surfaces; nothing writes without human accept.
- The batch UI is honest, cancellable, and degrades by name.
</success_criteria>

<output>
Create `.planning/phases/03.2-seeding-import-provenance/03.2-03-SUMMARY.md` when done.
</output>
