---
phase: 03.2-seeding-import-provenance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - model.py
  - surfaces/cli.py
  - tests/lesson_roundtrip.py
  - tests/import_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - SEED-04
  - SEED-08
  - SEED-09
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "[SRC: <source-id> <locators>] and [OBJ: framework/objective-id] resolve through an additive ## SOURCES registry; an unresolvable id is a lint error naming the id and the file (D-11, SEED-04)."
    - "The objective coverage map is computed on demand from the bank and the registry and never stored (D-12, SEED-08)."
    - "A bank using none of ## SOURCES, [SRC:], or [OBJ:] parses and renders byte-identically (D-20, SEED-09)."
    - "The ## SOURCES registry validates through schema_validate.py's supported keyword set (03.2-CONTEXT canonical refs)."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
  prohibitions:
    - statement: "The coverage map must never be stored; it is computed on demand."
      status: unverified
      flagged: true
    - statement: "An unresolvable [SRC:] or [OBJ:] id must never silently pass lint."
      status: unverified
      flagged: true
---

<objective>
As the bank, I want every item to name where it came from, and every objective to be countable - so nothing in the corpus is an unattributed transcription and the auditor later reads facts, not re-derivations.

Purpose: The provenance plan: the ## SOURCES registry, [SRC:]/[OBJ:] parsing and resolution, the unresolvable-id lint errors, the on-demand coverage map, and the compatibility fixture.
Output: model.py provenance grammar + lint codes, the coverage-map command, and the fixtures.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.2-seeding-import-provenance/03.2-CONTEXT.md
@.planning/phases/03.2-seeding-import-provenance/03.2-RESEARCH.md
@.planning/phases/03.2-seeding-import-provenance/03.2-UI-SPEC.md

<interfaces>
- `model.lint()` and the LINT_CODES catalogue are the one validation surface; provenance codes register additively.
- `schema_validate.py` validates the ## SOURCES registry shape.
- Phase 11's auditor reads [OBJ:] and the coverage map; it never re-derives them.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `model.py` | `parse_sources(bank_path)` (## SOURCES registry reader), `[SRC:]`/`[OBJ:]` parsing, lint codes `prov.src_unknown`, `prov.obj_unknown`, `prov.src_duplicate` |
| `surfaces/cli.py` | `itembank coverage <bank>` command (on-demand map) |
| `tests/lesson_roundtrip.py` | Provenance grammar + compatibility fixtures |
| `tests/import_roundtrip.py` | Registry resolution fixtures |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse ## SOURCES and the [SRC:]/[OBJ:] directives</name>
  <files>model.py, tests/lesson_roundtrip.py</files>
  <read_first>
    - `model.py` - parse_lesson()/parse_terms() (the independent-reader precedent), lint(), LINT_CODES
    - `03.2-CONTEXT.md` D-11/D-20
  </read_first>
  <behavior>
    - Test 1: a ## SOURCES registry parses into {source_id: locators}; [SRC: id loc] and [OBJ: framework/objective-id] directives resolve against it; a bank with none of these parses byte-identically.
    - Test 2: an unresolvable [SRC:] or [OBJ:] id is a lint error naming the id and the file; a duplicate source id is a lint error.
    - Test 3: the registry validates through the supported schema keyword set.
  </behavior>
  <action>
Implement `parse_sources(bank_path)` in `model.py` following the independent-reader precedent (like parse_lesson/parse_terms): read the `## SOURCES` registry from the bank preamble into {source_id: locators}, parse `[SRC: <id> <locators>]` and `[OBJ: framework/objective-id]` directives, and extend `lint()` with `prov.src_unknown`, `prov.obj_unknown`, and `prov.src_duplicate` (additive LINT_CODES additions). Validate the registry shape through the supported keyword set. Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Resolution and the three lint codes fire on fixtures; a no-provenance bank parses byte-identically.
    - The registry validates through schema_validate.py.
  </acceptance_criteria>
  <done>Every item can name its source, and an unattributed or unresolvable one fails loudly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The on-demand objective coverage map</name>
  <files>model.py, surfaces/cli.py, tests/import_roundtrip.py</files>
  <read_first>
    - `03.2-CONTEXT.md` D-12 (computed on demand, never stored)
    - `model.py` - the parsed objective field and the item walk
  </read_first>
  <behavior>
    - Test 1: `itembank coverage <bank>` computes the map from the bank and registry at request time - objectives x items - and writes nothing to disk.
    - Test 2: the map reflects the current bank state on every run (a changed bank changes the next run's map - proving no stale store).
  </behavior>
  <action>
Implement the on-demand coverage map: `coverage_map(bank_path)` reads the bank's items and their resolved [OBJ:] values and returns the objective-to-items map; register `itembank coverage <bank>` in `surfaces/cli.py`. The map is computed at request time and never stored (D-12). Write the two behavior tests first.
  </action>
  <verify>
    <automated>python tests/import_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The map is correct on demand and writes nothing.
    - The change-reflects-next-run fixture proves no stored state.
  </acceptance_criteria>
  <done>The coverage map cannot go stale because it does not exist between runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The provenance compatibility floor</name>
  <files>tests/lesson_roundtrip.py</files>
  <read_first>
    - `03.2-CONTEXT.md` D-20 and the Phase 3.1 byte-identity fixture (reuse its bank)
  </read_first>
  <behavior>
    - Test 1: a bank using none of ## SOURCES, [SRC:], or [OBJ:] renders byte-identically to the Phase 3.1 fixture output and parses identically.
  </behavior>
  <action>
Extend the compatibility fixture: reuse the Phase 3.1 fixture bank, render it through the provenance-updated parser, and assert byte-identity (content region, style block excluded per 3.1 section 12) plus parse identity. Write the fixture first.
  </action>
  <verify>
    <automated>python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The byte-identity and parse-identity assertions pass.
  </acceptance_criteria>
  <done>Provenance grammar is additive, proven by fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| bank text -> provenance registry | Author-written source ids become resolution facts; unresolvable ids must fail loudly |
| coverage map -> auditor | The map must be derivable, never stale |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-032-05 | Spoofing | source attribution | high | mitigate | Unresolvable [SRC:]/[OBJ:] ids are lint errors naming the id and file; the auditor reads facts, not re-derivations |
| T-032-06 | Tampering | registry shape | medium | mitigate | Schema validation through the supported keyword set |
| T-032-07 | Integrity | stale coverage map | medium | mitigate | The map is computed on demand and never stored (D-12) |
| T-032-SC | Tampering | package operations | high | mitigate | This plan performs no package operation |
</threat_model>

<verification>
- `python tests/lesson_roundtrip.py`
- `python tests/import_roundtrip.py`
- `python schema_validate.py --all`
</verification>

<success_criteria>
- ## SOURCES, [SRC:], and [OBJ:] resolve with named lint errors on failure.
- The coverage map is on-demand and never stored.
- The compatibility floor passes.
</success_criteria>

<output>
Create `.planning/phases/03.2-seeding-import-provenance/03.2-02-SUMMARY.md` when done.
</output>
