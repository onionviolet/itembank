---
status: passed
phase: 03.2-seeding-import-provenance
source: [03.2-VERIFICATION.md]
started: 2026-08-11
updated: 2026-08-11
---

## Current Test

[all automated UAT tests run live in the close-out worktree `.phase032-wt`
(branch `gsd/phase-03.2-close`, WSL python 3.13.3); one human-verify item
recorded — real-corpus calibration on the private bank]

## Tests

### 1. Import a real Anki deck with a full per-note account
expected: A modern `.apkg` imports losslessly — container reads ZIP members, zstd decodes through the pinned `zstandard` binding (legacy SQLite members dependency-free); unknown note types refuse loudly per note with the note id; every candidate passes `model.lint()` before convertible; the per-note report is exhaustive with zero silent drops; `itembank import anki` stages + reports and never writes a bank directly.
result: pass
source: automated, live
evidence: "`python tests/import_roundtrip.py` — 20/20 PASS, incl. `test_zstd_member_decodes_or_refuses_by_name`, `test_legacy_collection_reads_with_no_extra_dependency`, `test_zstandard_pinned_with_checksum_and_license`, `test_note_type_map_and_loud_per_note_refusal`, `test_every_candidate_passes_lint_before_convertible`, `test_report_is_exhaustive_no_silent_drop`, `test_cli_import_stages_reports_without_writing_a_bank`, `test_import_writes_only_through_the_accept_loop`."

### 2. Trace a claim to a source (provenance)
expected: `## SOURCES` registry is additive; `[SRC:]`/`[OBJ:]` resolve through it; an unresolvable id is a lint error naming the id and the file; the registry validates through the supported keyword set; the objective coverage map is computed on demand and never stored.
result: pass
source: automated, live
evidence: "`python tests/lesson_roundtrip.py` — ok (provenance grammar + compatibility floor); `python tests/import_roundtrip.py` — `test_registry_validates_through_supported_keyword_set`, `test_coverage_map_on_demand_and_writes_nothing`, `test_coverage_map_reflects_current_bank_state` all PASS."

### 3. Seed one item at a time under human accept
expected: Six-stage pipeline in locked order; nothing writes to a bank without an explicit accept; a cancelled run writes nothing; retry cap enforced; skip defers and never discards; an item with lint errors cannot be accepted and one with zero errors accepts exactly once.
result: pass
source: automated, live
evidence: "`python tests/seeding_roundtrip.py` — 24/24 PASS, incl. `test_six_stage_order_is_locked`, `test_cancelled_run_writes_nothing`, `test_accept_refuses_lint_error_candidates`, `test_accept_appends_clean_item_exactly_once`, `test_skip_defers_and_reports_never_discards`, `test_retry_cap_enforced_and_no_critique_of_a_dirty_draft`."

### 4. One accept endpoint behind two surfaces (UI-SPEC gates 1–5)
expected: CLI command and daemon route share `seeding.accept_candidate`; the route sits behind the loopback authority gate; unknown bank is a 404; the batch surface has no chat affordance, no fill bar, and renders the exact framing line; with the backend unreachable the exact refusal line renders and no draft starts while import/lint/provenance still work; every copy string renders in the assigned voice token.
result: pass
source: automated, live
evidence: "seeding_roundtrip: `test_cli_and_route_share_accept_candidate`, `test_route_sits_behind_the_loopback_authority_gate`, `test_route_unknown_bank_is_a_404`, `test_batch_surface_verbatim_no_chat_no_fill_bar`, `test_cancelled_and_degraded_surfaces_verbatim`, `test_copy_contract_verbatim` — PASS; `python tests/daemon_roundtrip.py` — ok (69 checks)."

### 5. Paraphrase lint with tunable thresholds, no source text stored
expected: Winnowing over fingerprints only; ≥8 consecutive copied words errors `prov.paraphrase_copy`; Jaccard > 0.25 warns `prov.paraphrase_overlap`; both thresholds are settings that round-trip; the report never stores or echoes source text; the deterministic stage-4 gate retries paraphrase + unsourced drafts before critique, headless.
result: pass
source: automated, live
evidence: "`python tests/lesson_roundtrip.py` — ok (winnowing fixtures); `python tests/config_roundtrip.py` — ok (threshold settings round-trip); seeding_roundtrip `test_stage4_retries_paraphrase_and_unsourced_drafts_before_critique`, `test_stage4_gate_deterministic_and_headless`, `test_provenance_directives_checked_deterministically_in_stage4` — PASS."

### 6. CASE/PREREQ edges authorable and linted
expected: `[CASE:]` grouping + `[PREREQ:]` edges lint: unknown group → `prov.case_unknown`, unresolvable target → `prov.prereq_unknown`, cycle → `prov.prereq_cycle`, each naming the id/file/path.
result: pass
source: automated, live
evidence: "lesson_roundtrip provenance fixtures — ok."

### 7. Guard keeps the real corpus out of CI; style warnings calibrated
expected: `itembank guard` refuses corpus-shaped content (`## SOURCES`/`## LESSON` files, private-bank-named dirs) outside fixtures/ while a fixtures/ subtree passes; `guard .` exits 0; `calibrate` records a rate for every calibrated style code with the ship-state decision (≥0.20 disabled by default).
result: pass (synthetic corpus, live) / human (real corpus)
source: automated, live
evidence: "`test_guard_refuses_corpus_shaped_content_and_fixtures_pass` — PASS; `python itembank.py guard .` — 0 offending files; calibration fixture — PASS (sentence_length 0.50 disabled, require_marker 1.00 disabled, unsourced_specific 0.00 enabled, all 11 codes recorded; see 03.2-CALIBRATION.md). Real-corpus run on the private bank → human."

### 8. Compatibility floor
expected: A bank using none of `## SOURCES`/`[SRC:]`/`[OBJ:]`/`[CASE:]`/`[PREREQ:]` parses and renders byte-identically.
result: pass
source: automated, live
evidence: "lesson_roundtrip compatibility-floor fixtures — ok."

### 9. Requirement-coverage audit and full suite
expected: `test_seed_requirements_covered_9_of_9` exits 0 (SEED-01..09 each in a plan frontmatter with a named verification); the full suite and schema validation pass.
result: pass (03.2 scope) — full-suite attribution recorded
source: automated, live
evidence: "import_roundtrip prints `seed coverage: 9/9 SEED ids` — PASS. Full suite: 41/45 — every 03.2 file green; the 4 failures are provably post-03.2 or environmental (evidence index version assertion from 06.2-01 `376dae4` vs `INDEX_VERSION = 2` restored by 08-06 `5b7f397`; `gate_roundtrip.py` is 06.2's own test; `packaging_roundtrip.py` missing onedir build; `phase_062_audit.py` cascades from those three — see 03.2-VERIFICATION.md). Schema: `schema_validate.py --all` does not exist; equivalent coverage `python tests/protocol_roundtrip.py` — ok (validates every schema doc + runtime output against schemas/)."

## Summary

total: 9
passed: 9 (all automated UAT items run live and green; 1 carries a human
follow-up — real-corpus calibration on the private bank, recorded not claimed)
blocked: 0
