---
phase: 03.2-seeding-import-provenance
plan: 04
type: execute
wave: 2
depends_on:
  - 03.2-02
files_modified:
  - model.py
  - surfaces/settings.py
  - schemas/settings.schema.json
  - itembank.json
  - tests/lesson_roundtrip.py
  - tests/seeding_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - SEED-05
  - SEED-06
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Paraphrase lint uses stdlib winnowing over fingerprints only - source text is never stored (D-13, SEED-05)."
    - "The thresholds are settings: error at >=8 consecutive copied words, warn above Jaccard 0.25, both with those defaults (D-13)."
    - "style.unsourced_specific is a structural check - numerals, units, and doses require a [SRC:] - registering into the closed LINT_CODES catalogue Phase 3.1 defines, without opening it (D-14)."
    - "[CASE:] grouping and [PREREQ:] edges are authorable and linted here - cycle detection and unresolvable-target detection - ahead of Phases 6/7 consuming them (D-15/D-16, SEED-06)."
    - "A bank using none of the new constructs parses byte-identically (D-20)."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
  prohibitions:
    - statement: "Source text must never be stored on disk by the paraphrase lint."
      status: unverified
      flagged: true
    - statement: "A [PREREQ:] cycle or unresolvable target must never pass lint silently."
      status: unverified
      flagged: true
---

<objective>
As the linter, I want to catch a paraphrase that is really a transcription, require a source for specific facts, and keep the case and prerequisite structure honest - all without ever storing the source text itself.

Purpose: The paraphrase-and-edges plan: winnowing-based paraphrase lint with setting thresholds, style.unsourced_specific, and the [CASE:]/[PREREQ:] grammar with cycle/unresolvable linting.
Output: model.py checks + grammar, the threshold settings, and the fixtures.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.2-seeding-import-provenance/03.2-CONTEXT.md
@.planning/phases/03.2-seeding-import-provenance/03.2-RESEARCH.md
@.planning/phases/03.2-seeding-import-provenance/03.2-UI-SPEC.md

<interfaces>
- `model.lint()` and the closed LINT_CODES catalogue are the one validation surface; new codes register additively.
- `surfaces.settings.py` follows the SETTINGS_CODES/validate-then-write precedent for the thresholds.
- Phase 7 consumes [PREREQ:] edges; Phase 6/7 consume [CASE:] - this phase owns grammar + validity only.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `model.py` | Winnowing paraphrase check (`paraphrase_check`), `[CASE:]`/`[PREREQ:]` parsing, lint codes `prov.paraphrase_copy`, `prov.paraphrase_overlap`, `style.unsourced_specific`, `prov.case_unknown`, `prov.prereq_unknown`, `prov.prereq_cycle` |
| `surfaces/settings.py` | `paraphrase.winnow_threshold` (8), `paraphrase.jaccard_threshold` (0.25) settings |
| `tests/lesson_roundtrip.py` | Paraphrase, unsourced-specific, CASE/PREREQ fixtures |
| `tests/seeding_roundtrip.py` | The stage-4 deterministic checks integration |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The winnowing paraphrase lint with setting thresholds</name>
  <files>model.py, surfaces/settings.py, schemas/settings.schema.json, itembank.json, tests/lesson_roundtrip.py</files>
  <read_first>
    - `03.2-CONTEXT.md` D-13 (winnowing, thresholds, never storing source text)
    - `model.py` - lint() and LINT_CODES
    - `surfaces/settings.py` - the SETTINGS_CODES precedent
  </read_first>
  <behavior>
    - Test 1: a candidate sharing >=8 consecutive copied words with a source fingerprint errors (`prov.paraphrase_copy`); a Jaccard overlap above 0.25 warns (`prov.paraphrase_overlap`); below both passes.
    - Test 2: the thresholds are settings with defaults 8 and 0.25, round-tripping through itembank config.
    - Test 3: the lint never writes source text anywhere - a fixture asserts no source text appears on disk or in the report (fingerprints only).
  </behavior>
  <action>
Implement the winnowing paraphrase check in `model.py` per D-13: k-gram fingerprints of the candidate and the source locator's text (read transiently, hashed, never stored), with a window size at executor discretion; error at >=8 consecutive copied words, warn above Jaccard 0.25, both from settings (`paraphrase.winnow_threshold` default 8, `paraphrase.jaccard_threshold` default 0.25) registered through the validate-then-write precedent. Register the two codes in LINT_CODES. Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/lesson_roundtrip.py &amp;&amp; python tests/config_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Both thresholds fire on fixtures and round-trip through config.
    - The no-source-text fixture passes (fingerprints only on disk).
  </acceptance_criteria>
  <done>Transcription is caught structurally, tunably, and without storing a word of the source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: style.unsourced_specific and the CASE/PREREQ grammar</name>
  <files>model.py, tests/lesson_roundtrip.py</files>
  <read_first>
    - `03.2-CONTEXT.md` D-14 (structural check, closed catalogue), D-15/D-16
    - `model.py` - the provenance parsing from plan 03.2-02 and LINT_CODES
  </read_first>
  <behavior>
    - Test 1: an item carrying numerals, units, or doses without a [SRC:] errors with `style.unsourced_specific`; with a source it passes - and the code registers into the closed LINT_CODES catalogue without opening it.
    - Test 2: [CASE:] groups parse and lint (unknown group id errors); [PREREQ:] edges lint for unresolvable targets (`prov.prereq_unknown`) and cycles (`prov.prereq_cycle`).
    - Test 3: a bank with none of the constructs parses byte-identically.
  </behavior>
  <action>
Implement `style.unsourced_specific` per D-14: a structural regex-based check for numerals/units/doses in item text without a resolved [SRC:], registering into the closed LINT_CODES catalogue (Phase 3.1's contract) without opening it. Implement the [CASE:] and [PREREQ:] grammar per D-15/D-16: parse the directives, lint unknown targets and cycles (a small graph walk over the bank's edges), and leave selection semantics to Phase 7. Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - All four new codes fire on fixtures and exist in LINT_CODES.
    - Cycle and unresolvable-target fixtures pass; the compatibility floor holds.
  </acceptance_criteria>
  <done>Fabricated specifics fail structurally, and the case/prereq structure is valid before any consumer reads it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the deterministic checks into the seeding stage-4 gate</name>
  <files>model.py, tests/seeding_roundtrip.py</files>
  <read_first>
    - `03.2-CONTEXT.md` D-08 stage 4 (deterministic checks before any model critique)
    - `surfaces/seeding.py` from plan 03.2-03
  </read_first>
  <behavior>
    - Test 1: a draft failing paraphrase or unsourced-specific is retried at stage 4 and never reaches the model critique stage.
    - Test 2: the stage-4 gate is deterministic and headless (no model involved).
  </behavior>
  <action>
Wire the new checks into the seeding pipeline's stage-4 deterministic gate (plan 03.2-03): a draft failing `prov.paraphrase_*` or `style.unsourced_specific` retries before any model critique (D-08). Write the two behavior tests first.
  </action>
  <verify>
    <automated>python tests/seeding_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The stage-4 retry fixtures pass; the gate is model-free.
  </acceptance_criteria>
  <done>The deterministic gate catches transcription and fabrication before a model ever critiques it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source text -> lint | Source text must never persist; fingerprints only |
| bank edges -> consumers | Invalid CASE/PREREQ structure must fail before Phases 6/7 read it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-032-12 | Information Disclosure | source text storage | high | mitigate | Winnowing over fingerprints only; the no-source-text fixture asserts nothing persists (D-13) |
| T-032-13 | Spoofing | unsourced specifics | high | mitigate | style.unsourced_specific is structural (D-14), registered into the closed catalogue |
| T-032-14 | Tampering | prereq cycles | medium | mitigate | Cycle and unresolvable-target lint errors (D-16) before Phase 7 consumes edges |
| T-032-SC | Tampering | package operations | high | mitigate | This plan performs no package operation |
</threat_model>

<verification>
- `python tests/lesson_roundtrip.py`
- `python tests/seeding_roundtrip.py`
- `python tests/config_roundtrip.py`
</verification>

<success_criteria>
- Paraphrase lint is winnowing-based, tunable, and never stores source text.
- style.unsourced_specific and the CASE/PREREQ checks register and fire.
- The stage-4 gate retries deterministic failures before critique.
</success_criteria>

<output>
Create `.planning/phases/03.2-seeding-import-provenance/03.2-04-SUMMARY.md` when done.
</output>
