---
phase: 03.2-seeding-import-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - surfaces/import_anki.py
  - surfaces/cli.py
  - requirements.txt
  - tests/import_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - SEED-01
  - SEED-09
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "A modern .apkg imports losslessly with an exhaustive per-note account - converted / skipped / refused, each with the note id and reason - and no note is silently dropped (D-03, SEED-01)."
    - "zstd-compressed collection.anki21b members decode through one pinned, checksummed, license-reviewed dependency (zstandard); legacy plain-SQLite members read with zero extra dependencies. D-02's stdlib-only claim is unmeetable for modern Anki exports and the handoff's default applies: take one dependency, pin it, record it (03.2-RESEARCH section 1, Directive 4a)."
    - "Unknown or unsupported Anki note types produce a loud named refusal per note, following the GIFT pattern - never a lossy approximation (D-04)."
    - "The importer is a client of model.lint(): nothing is written without passing the same lint the human path uses (D-05)."
    - "A bank using none of the new constructs parses byte-identically; the import path writes only through the accept loop, never directly to a bank (D-06, D-20)."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
  prohibitions:
    - statement: "No note may be silently dropped; every note appears in the per-note report with its id and reason."
      status: unverified
      flagged: true
    - statement: "The importer must never write to a bank directly - the accept loop owns all writes."
      status: unverified
      flagged: true
---

<objective>
As the importer, I want a real Anki deck to become real itembank candidates - every note accounted for, nothing silently dropped, nothing written without passing the same lint the human path uses.

Purpose: The .apkg import plan: the container reader (ZIP + zstd + sqlite3), the note-type mapping with loud refusals, the lint gate, the exhaustive per-note report, and the CLI command.
Output: surfaces/import_anki.py, the pinned zstandard dependency, the import CLI, and tests/import_roundtrip.py with synthetic .apkg fixtures.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/03.2-seeding-import-provenance/03.2-CONTEXT.md
@.planning/phases/03.2-seeding-import-provenance/03.2-RESEARCH.md
@.planning/phases/03.2-seeding-import-provenance/03.2-UI-SPEC.md
@.planning/phases/03.2-seeding-import-provenance/03.2-DISCUSSION-LOG.md

<interfaces>
- `model.lint()` and `model.parse_question()` are the one parser/validator the importer is a client of.
- `surfaces/gift.py`'s loud-refusal pattern is the D-04 template.
- `itembank.py` cmd_guard extends its globs for the corpus residency rule (plan 03.2-05).
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `surfaces/import_anki.py` | `import_apkg(path, out, force)` - the container reader, note-type mapper (`anki_note_type_map`), per-note report builder |
| `surfaces/cli.py` | `itembank import anki <file> --out <dir>` command |
| `requirements.txt` | Pinned `zstandard` with checksum and license note |
| `tests/import_roundtrip.py` | Synthetic .apkg fixtures (zstd + legacy), per-note report, refusal, and lint-gate fixtures |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read the container - ZIP, member detection, zstd decode</name>
  <files>surfaces/import_anki.py, requirements.txt, tests/import_roundtrip.py</files>
  <read_first>
    - `03.2-RESEARCH.md` section 1 (the member-format table and the zstd finding)
    - `surfaces/gift.py` - the refusal pattern to mirror
    - `tests/import_roundtrip.py` - to be created; follow the fail() harness
  </read_first>
  <behavior>
    - Test 1: a synthetic .apkg with a zstd-compressed collection.anki21b member decodes and opens as SQLite; a legacy collection.anki21/anki2 member reads with no extra dependency.
    - Test 2: a .apkg that is not a valid ZIP, or has no recognized collection member, fails with a named error naming the file - never a silent empty import.
    - Test 3: the zstandard dependency is pinned with a recorded checksum and license review (requirements.txt + a recorded note).
  </behavior>
  <action>
Implement the container reader in `surfaces/import_anki.py`: `zipfile` opens the .apkg; inspect members; `collection.anki21b` decodes through the pinned `zstandard` binding (a conditional import - legacy members never touch it); `collection.anki21`/`collection.anki2` open through stdlib `sqlite3` directly. A missing/invalid container or unrecognized member fails with a named error. Add `zstandard` to `requirements.txt` pinned with the recorded checksum and license note per Directive 4a (03.2-RESEARCH A2). Write the three behavior tests first, building synthetic .apkg fixtures in-test (never real user data).
  </action>
  <verify>
    <automated>python tests/import_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Both member types decode; invalid containers fail by name.
    - zstandard is pinned with checksum + license note; the legacy path never imports it.
  </acceptance_criteria>
  <done>The container opens on both formats, and the zstd dependency is conditional and pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map note types, refuse loudly, and gate through lint</name>
  <files>surfaces/import_anki.py, tests/import_roundtrip.py</files>
  <read_first>
    - `model.py` - parse_question() and lint() (the one validator)
    - `surfaces/gift.py` - the unsupported-type refusal copy/shape
    - `03.2-CONTEXT.md` D-04/D-05
  </read_first>
  <behavior>
    - Test 1: known Anki note types map to itembank item types through `anki_note_type_map` (e.g. Basic -> mc, Cloze -> short/cloze mapping per the executor's documented table); unknown types produce a loud named refusal per note with the note id, never an approximation.
    - Test 2: every candidate passes `model.lint()` before it is reported as convertible; lint failures report per candidate with the named errors, and nothing is written.
    - Test 3: the per-note report is exhaustive - converted / skipped / refused rows each carry the note id and reason, and the total asserts no silent drop.
  </behavior>
  <action>
Implement the note-type mapping (`anki_note_type_map`) and the candidate builder: read the notes and note_types tables, map each note to an itembank item shape, and run every candidate through `model.lint()` (D-05). Unknown note types take the GIFT-style loud refusal per note (D-04). Build the exhaustive per-note report (D-03) with the exact UI-SPEC 3 shape (`Converted` / `Skipped` / `Refused` + the total line `{n} notes accounted for, 0 dropped silently.`). Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/import_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The mapping table is named and documented; refusals are loud and per-note.
    - Lint-gated candidates: nothing convertible reports without passing lint.
    - The report asserts zero silent drops.
  </acceptance_criteria>
  <done>Every note is accounted for, refused loudly when unsupported, and linted before it can be written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The import CLI and the write-through-accept contract</name>
  <files>surfaces/import_anki.py, surfaces/cli.py, tests/import_roundtrip.py</files>
  <read_first>
    - `surfaces/cli.py` - command registration and ROUTE_CLI
    - `03.2-CONTEXT.md` D-06 (nothing writes without accept) and D-10 (degrade, never block)
  </read_first>
  <behavior>
    - Test 1: `itembank import anki <file> --out <dir>` writes the per-note report and stages candidates for the accept loop; it never writes a bank directly.
    - Test 2: the import command works with no model backend (D-10) - it is not part of the authoring loop.
    - Test 3: a re-import of the same file is idempotent at the report level (the same notes are accounted for with the same reasons).
  </behavior>
  <action>
Register `itembank import anki <file> --out <dir>` in `surfaces/cli.py` (and the ROUTE_CLI parity table with a report route if warranted): the command reads the container, lints candidates, writes the exhaustive report, and stages convertible candidates for the accept loop (plan 03.2-03) - it never writes to a bank directly (D-06). It must work with no model backend (D-10). Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/import_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The CLI stages + reports without writing a bank; the no-backend run works.
    - Re-import is report-idempotent.
  </acceptance_criteria>
  <done>Import is a client of the runtime: report, stage, and hand to the human - never a direct writer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| .apkg file -> parsed candidates | An untrusted import file becomes bank candidates; the lint gate is the validation surface |
| importer -> bank | The importer must never write a bank directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-032-01 | Tampering | malicious .apkg content | high | mitigate | The lint gate (D-05) is the one validation surface; candidates are text, never executed; the importer is a client of the parser |
| T-032-02 | Tampering | zstd dependency | high | mitigate | Pinned version + recorded checksum + license review (Directive 4a); conditional import keeps the legacy path dependency-free |
| T-032-03 | Integrity | silent note drops | high | mitigate | The exhaustive per-note report asserts zero silent drops (D-03, SEED-01) |
| T-032-04 | Spoofing | media references | medium | mitigate | Referenced media resolve by name against the media map; no path traversal outside the import output directory |
| T-032-SC | Tampering | package operations | high | mitigate | zstandard is the only addition, pinned and recorded |
</threat_model>

<verification>
- `python tests/import_roundtrip.py`
- Grep the staged candidates for any direct bank-write path - must return none.
</verification>

<success_criteria>
- Both .apkg collection formats import with per-note accounting.
- Unknown types refuse loudly; lint gates every candidate.
- The CLI stages and reports without writing a bank.
</success_criteria>

<output>
Create `.planning/phases/03.2-seeding-import-provenance/03.2-01-SUMMARY.md` when done.
</output>
