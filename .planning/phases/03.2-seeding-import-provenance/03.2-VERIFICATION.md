---
phase: 03.2-seeding-import-provenance
verified: 2026-08-11
status: verified
score: 9/9 must-haves verified live
behavior_unverified: 0 # every roadmap truth below ran green live in the close-out worktree
overrides_applied: 0
human_verification:
  - test: "Real-corpus calibration of every Phase 3.1 style warning (SEED-07 / D-17/D-18): per-warning false-positive rates measured on the private EMT/Math/CS corpus."
    expected: "Every calibrated style code carries a recorded FP rate and ship-state in 03.2-CALIBRATION.md; the guard keeps the corpus out of CI (0 offending files)."
    why_human: "The real corpus lives beside the private bank, outside this repository by design; the synthetic-corpus measurement is automated and green (calibration fixture in import_roundtrip.py)."
---

# Phase 3.2: Seeding, Import & Provenance — Verification Report

**Phase Goal:** As an author, I want real content to flow into itembank under
human control — a real Anki deck imports losslessly with an exhaustive
per-note account and never writes a bank directly, every claim of fact can be
traced to a source (`## SOURCES` / `[SRC:]` / `[OBJ:]`), a six-stage
draft→lint→retry→accept seeding loop is the only write path and ships on both
CLI and daemon surfaces, paraphrase reuse is caught by stdlib winnowing over
fingerprints (source text never stored), `[CASE:]`/`[PREREQ:]` edges are
authorable and linted ahead of Phases 6/7, and the real corpus stays out of
CI while calibrating every style warning.

**Verified:** 2026-08-11 — **live**, in the close-out worktree
(`.phase032-wt`, branch `gsd/phase-03.2-close`, WSL python 3.13.3). Every
command below actually ran in this session; results are recorded as observed.
**Status:** verified (9/9 truths; 1 human-verify item = real-corpus
calibration on the private bank).

## User Flow Coverage

| Step | Expected | Evidence | Status |
|------|----------|----------|--------|
| Import a real Anki deck | `.apkg` container reads ZIP members, decodes zstd through the pinned `zstandard` binding or reads legacy SQLite dependency-free; unknown note types refuse loudly per note with the id; every candidate passes `model.lint()` before convertible; the per-note report is exhaustive (converted/skipped/refused + `{n} notes accounted for, 0 dropped silently.`); `itembank import anki` stages + reports and never writes a bank directly | `python tests/import_roundtrip.py` — **20/20 PASS** (container, zstd/legacy, pinned+checksummed dep, media path-traversal refusal, note-type map + loud refusal, lint gate, exhaustive report, CLI stage-only, reimport idempotency, write-through-accept) | ✅ VERIFIED (live) |
| Trace a claim to a source | `## SOURCES` registry is additive; `[SRC: <source-id> <locators>]` and `[OBJ: framework/objective-id]` resolve; an unresolvable id is a lint error naming the id and the file; the registry validates through the supported keyword set | `python tests/lesson_roundtrip.py` — **ok** (provenance grammar + compatibility floor); `python tests/import_roundtrip.py` registry + coverage fixtures — **PASS** | ✅ VERIFIED (live) |
| See objective coverage without a stored map | The coverage map is computed on demand from bank + registry, never stored, and reflects current bank state | `test_coverage_map_on_demand_and_writes_nothing`, `test_coverage_map_reflects_current_bank_state` (import_roundtrip) — **PASS** | ✅ VERIFIED (live) |
| Seed one item at a time under human accept | Six-stage pipeline in locked order; nothing writes to a bank without an explicit accept; a cancelled run writes nothing; retry cap enforced; skip defers and never discards | `python tests/seeding_roundtrip.py` — **24/24 PASS** (order lock, headless adapter, retry/critique caps, stage-4 provenance checks, cancel-writes-nothing, accept-refuses-lint-errors, accept-appends-exactly-once, skip-defers, D-19 separation) | ✅ VERIFIED (live) |
| One accept endpoint, two surfaces | CLI and daemon route share `seeding.accept_candidate`; the route sits behind the loopback authority gate; unknown bank is a 404; batch UI has no chat affordance/fill bar and renders the exact framing line | `python tests/seeding_roundtrip.py` (CLI/route share, route authority, verbatim batch/degraded copy) — **PASS**; `python tests/daemon_roundtrip.py` — **ok (69 checks)** | ✅ VERIFIED (live) |
| Catch paraphrase reuse without storing source text | Winnowing over fingerprints only: ≥8 consecutive copied words errors `prov.paraphrase_copy`, Jaccard > 0.25 warns `prov.paraphrase_overlap`; both thresholds are settings that round-trip; the report never stores/echoes source text; stage-4 retries paraphrase + unsourced drafts before critique, deterministically and headless | `python tests/lesson_roundtrip.py` — **ok**; `python tests/config_roundtrip.py` — **ok**; `python tests/seeding_roundtrip.py` stage-4 fixtures — **PASS** | ✅ VERIFIED (live) |
| Author and lint CASE/PREREQ edges | `[CASE:]` grouping + `[PREREQ:]` edges authorable; unknown group → `prov.case_unknown`, unresolvable target → `prov.prereq_unknown`, cycle → `prov.prereq_cycle`, each naming id/file/path | lesson_roundtrip provenance fixtures — **PASS (ok)** | ✅ VERIFIED (live) |
| Keep the real corpus out of CI | Extended guard refuses corpus-shaped content (`## SOURCES`/`## LESSON` files, private-bank-named dirs) anywhere outside fixtures/ while a fixtures/ subtree passes; `python itembank.py guard .` exits 0 | `test_guard_refuses_corpus_shaped_content_and_fixtures_pass` — **PASS**; `python itembank.py guard .` — **0 offending files** | ✅ VERIFIED (live) |
| Calibrate every style warning | `calibrate` runs every Phase 3.1 style warning over the corpus, computes FP = FP/(TP+FP) per code, records a rate for all 11 calibrated codes, and sets ship-state (≥0.20 → disabled by default); synthetic corpus: `sentence_length` 0.50 disabled, `require_marker` 1.00 disabled, `unsourced_specific` 0.00 enabled, all others 0.00 enabled | calibration fixture (import_roundtrip) — **PASS**; recorded in 03.2-CALIBRATION.md; real-corpus run → human (private bank) | ✅ VERIFIED (live; real-corpus run human) |
| Compatibility floor | A bank using none of `## SOURCES`/`[SRC:]`/`[OBJ:]`/`[CASE:]`/`[PREREQ:]` parses and renders byte-identically | lesson_roundtrip compatibility-floor fixtures — **PASS (ok)** | ✅ VERIFIED (live) |

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A modern Anki `.apkg` imports losslessly with an exhaustive per-note account (converted/skipped/refused, each with note id + reason), no silent drops, through one pinned+checksummed+license-reviewed zstd dependency (legacy path dependency-free) and loud per-note refusal for unknown types (SEED-01). | ✓ VERIFIED (live) | `python tests/import_roundtrip.py` — 20/20 PASS: `test_zstd_member_decodes_or_refuses_by_name`, `test_legacy_collection_reads_with_no_extra_dependency`, `test_zstandard_pinned_with_checksum_and_license`, `test_note_type_map_and_loud_per_note_refusal`, `test_every_candidate_passes_lint_before_convertible`, `test_report_is_exhaustive_no_silent_drop`. |
| 2 | Draft → lint → retry → explicit human accept, one item at a time; nothing writes to a bank without an accept; a cancelled run writes nothing (SEED-02). | ✓ VERIFIED (live) | seeding_roundtrip 24/24: `test_cancelled_run_writes_nothing`, `test_accept_refuses_lint_error_candidates`, `test_accept_appends_clean_item_exactly_once`, `test_retry_cap_enforced_and_no_critique_of_a_dirty_draft`. |
| 3 | The accept loop ships as CLI command and daemon route — one accept endpoint, two surfaces, never two implementations (SEED-03). | ✓ VERIFIED (live) | `test_cli_and_route_share_accept_candidate`, `test_route_accept_calls_the_same_accept_candidate`, `test_route_sits_behind_the_loopback_authority_gate` (seeding_roundtrip); daemon_roundtrip **ok (69 checks)**. |
| 4 | `[SRC:]`/`[OBJ:]` resolve through an additive `## SOURCES` registry; unresolvable ids are lint errors naming the id and the file (SEED-04). | ✓ VERIFIED (live) | lesson_roundtrip provenance grammar **ok**; import_roundtrip registry fixtures; lint codes `prov.src_unknown`/`prov.obj_unknown`/`prov.src_duplicate` registered. |
| 5 | Paraphrase lint uses stdlib winnowing over fingerprints only, errors at ≥8 consecutive copied words, warns above Jaccard 0.25 (both thresholds settings), never stores source text (SEED-05). | ✓ VERIFIED (live) | lesson_roundtrip winnowing fixtures **ok**; config_roundtrip threshold settings round-trip **ok**; stage-4 deterministic gate fixtures in seeding_roundtrip **PASS**. |
| 6 | `[CASE:]` grouping and `[PREREQ:]` edges are authorable and linted — cycle + unresolvable-target detection — ahead of Phases 6/7 (SEED-06). | ✓ VERIFIED (live) | lesson_roundtrip `prov.case_unknown`/`prov.prereq_unknown`/`prov.prereq_cycle` fixtures **ok**. |
| 7 | The real EMT/Math/CS corpus lives outside the repository (guard keeps it out of CI) and calibrates every Phase 3.1 style warning with recorded FP rates (SEED-07). | ✓ VERIFIED (live; real-corpus run human) | `test_guard_refuses_corpus_shaped_content_and_fixtures_pass` **PASS**; `python itembank.py guard .` **0 offending files**; calibration fixture + 03.2-CALIBRATION.md (synthetic corpus, all 11 codes); real-corpus run = human/private-bank action. |
| 8 | The objective coverage map is computed on demand from bank + registry, never stored (SEED-08). | ✓ VERIFIED (live) | `test_coverage_map_on_demand_and_writes_nothing`, `test_coverage_map_reflects_current_bank_state` — **PASS**. |
| 9 | A bank using none of the new constructs parses and renders byte-identically (SEED-09). | ✓ VERIFIED (live) | lesson_roundtrip compatibility-floor fixtures **ok**; import write-through-accept fixture + static source check — **PASS**. |

**Score:** 9/9 truths verified live (0 present-but-behavior-unverified). The
only human-pending item is the real-corpus calibration, which by design
cannot run inside this repository.

### Deferred Items

| Item | Why deferred | Owner |
|------|--------------|-------|
| Real-corpus calibration (SEED-07 / D-18) | The private EMT/Math/CS corpus lives beside the private bank, outside the repo by design (D-17); the synthetic-corpus measurement is automated and green | Human on the private-bank machine (`python itembank.py calibrate <corpus-dir> --out ...`) |
| Full-suite green at HEAD (41/45) | 4 failures all attributable to **post-03.2** commits or environment, none to 03.2 (see below) | The phases that own them: 06.2 (evidence index version + gate context), 08-06 (index restore), 13/packaging (onedir build) |
| Deterministic edge probe | The gate requires pre-assigned requirement IDs; IDs were assigned at plan time in this run (skip sentence recorded in every PLAN.md, per plan-05) | Recorded, not silently skipped |

## Full-suite result (run live at close-out, 2026-08-11)

`for f in tests/*_roundtrip.py; do python "$f" || exit 1; done` (driven by a
python subprocess driver, all 45 `tests/*.py` files):

- **41 PASS / 4 FAIL.** All phase-03.2 files pass: `import_roundtrip` 20/20,
  `seeding_roundtrip` 24/24, `lesson_roundtrip` ok, `config_roundtrip` ok,
  `daemon_roundtrip` ok, `protocol_roundtrip` ok.
- `packaging_roundtrip.py` — **environmental, pre-existing** (missing
  `dist/itembank-sidecar-onedir`; run `scripts/build_shell.ps1` first).
  Already attributed in `03.2-GATES.md`.
- `evidence_roundtrip.py` — **post-03.2 regression**: `index was not rebuilt
  at version 3 after the stale check`. The "expects 3" assertion was added by
  `376dae4 feat(06.2-01)` (strictly after 03.2's phase-end `b301f0e` per
  `git log --ancestry-path b301f0e..HEAD`); HEAD `evidence.py` carries
  `INDEX_VERSION = 2` restored by `5b7f397 fix(08-06)`. 03.2's diff never
  touches `evidence.py` or this test file.
- `gate_roundtrip.py` — **post-03.2 regression**: `rows must differ only by
  context in log order, got [None, None]`. The file is phase 06.2's own test
  (`698f5df test(06.2-04)`); the assertion came from `376dae4`.
- `phase_062_audit.py` — **cascade**: it re-runs the full suite internally
  and inherits the three failures above (its own 6/6 ID audit and schema
  checks print PASS first).

**Schema validation:** `schema_validate.py --all` does not exist; the real
interface is per-schema (`schema_validate.py <schema.json> <instance>`), as
CI uses it. The self-contained full coverage is `python
tests/protocol_roundtrip.py`, which validates every schema document with
`schema_validate.check_schema` and validates runtime output against
`schemas/` — **ok** at close-out (83 lint codes declared, schema versions
pinned, EVID-07 field set asserted).

## Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `surfaces/import_anki.py` | container reader, note-type map, lint gate, exhaustive per-note report | ✓ PRESENT | 471 lines added (plan 01); no bank write path |
| `surfaces/seeding.py` | six-stage pipeline + one accept endpoint | ✓ PRESENT | 676 lines added (plan 03); CLI + daemon share `accept_candidate` |
| `surfaces/cli.py` | `import anki`, `seed`, `calibrate`, `guard` commands | ✓ PRESENT | extended by plans 01/03/05 |
| `surfaces/daemon.py` | accept route behind loopback authority gate | ✓ PRESENT | plan 03 |
| `model.py` | `prov.*` lint codes, `style.unsourced_specific`, winnowing, CASE/PREREQ grammar | ✓ PRESENT | plan 02/04 |
| `schemas/lint_error.schema.json`, `schemas/settings.schema.json` | new codes + threshold settings | ✓ PRESENT | plans 04/05 |
| `03.2-CALIBRATION.md` | synthetic-corpus rates, all 11 codes, ship-state | ✓ PRESENT | sentence_length 0.50 / require_marker 1.00 disabled; unsourced_specific 0.00 enabled |
| `03.2-GATES.md` | per-plan gates + plan-05 table + attribution | ✓ PRESENT | full-suite attribution updated with the close-out re-run |
| `tests/import_roundtrip.py`, `tests/seeding_roundtrip.py` | 20 + 24 fixtures incl. guard-refusal, calibration, SEED 9/9, D-19 | ✓ PRESENT | all run green live at close-out |
| `03.2-VALIDATION.md` | per-task matrix, status validated | ✓ PRESENT | filled + signed off this session |
