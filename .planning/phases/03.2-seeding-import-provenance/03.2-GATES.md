# Phase 3.2 Verification Gates — 03.2-GATES.md

**Phase:** 03.2-seeding-import-provenance
**Gate record:** plan 05 (verify) — executed 2026-08-11

This file maps every Phase 3.2 plan's verification command to its result and
attributes the pre-existing failures. The plan-05 executor's sandbox blocks
all python execution, so the parent ran every command below and recorded the
**actual** results; nothing here is fabricated.

## Per-plan verification gates

| Plan | Verification command | Result (parent-run) |
|---|---|---|
| 03.2-01 (import) | `python tests/import_roundtrip.py` | **PASS** — 14 tests passed (plan-01 suite). The plan-05 additions brought the file to 20 tests; re-run confirmed 20 passed. |
| 03.2-02 (provenance) | `python tests/lesson_roundtrip.py`; `python tests/import_roundtrip.py`; `python schema_validate.py --all` (does not exist — equivalent coverage is `python tests/protocol_roundtrip.py`) | **PASS** — lesson_roundtrip `ok`; import_roundtrip 17 passed (at plan-02 close); protocol_roundtrip `ok` (69 lint codes declared, schema --all self-contained). |
| 03.2-03 (seeding) | `python tests/seeding_roundtrip.py`; `python tests/daemon_roundtrip.py`; import-grep: CLI and route share `accept_candidate` | **PASS** — seeding_roundtrip 21 passed (23 at plan-04 close); daemon_roundtrip `ok` (60 checks, all held); import-grep static: `surfaces/cli.py:200` and `surfaces/daemon.py:284` both call `seeding.accept_candidate`. |
| 03.2-04 (paraphrase/edges) | `python tests/lesson_roundtrip.py`; `python tests/seeding_roundtrip.py`; `python tests/config_roundtrip.py` | **PASS** — lesson_roundtrip `ok`; seeding_roundtrip 23 passed; config_roundtrip `ok`. |
| 03.2-05 (this plan) | `python tests/import_roundtrip.py`; `python itembank.py guard .`; the calibration command against a synthetic corpus; the full-suite loop; schema validation | **PASS** — see the plan-05 table below. |

## Plan-05 gates (this plan's own)

| Gate | Command / fixture | Status (parent-run) |
|---|---|---|
| Guard extension (D-17) | `python itembank.py guard .` must exit 0; the guard-refusal fixture `test_guard_refuses_corpus_shaped_content_and_fixtures_pass` | **PASS** — `guard .` exits 0 (`0 offending files`); the refusal fixture passes inside import_roundtrip (20/20). One false-positive fix was required: the `## SOURCES`/`## LESSON` section markers flagged the repo's own `.planning/` research docs, which legitimately quote the format grammar. `.planning` was added to the guard's repo-owned-tree skip list (same class as `.claude`/`.agents`); the fixture still proves corpus-shaped content in a temp dir is refused. |
| Calibration (D-18) | `python tests/import_roundtrip.py` (calibration fixture) and `python itembank.py calibrate <synthetic-corpus> --out ...`; deliverable `03.2-CALIBRATION.md` | **PASS** — the calibration fixture passes; the CLI ran against the synthetic corpus and recorded all 11 calibrated warnings with rates: `style.sentence_length` 0.50 → **disabled by default**, `style.require_marker` 1.00 → **disabled by default**, `style.unsourced_specific` 0.00 → **enabled**, all others 0.00 → **enabled**. 03.2-CALIBRATION.md records the synthetic-corpus measurement; the real-corpus run is the human/private-bank action (D-17). |
| Requirement audit | `test_seed_requirements_covered_9_of_9` | **PASS** — `seed coverage: 9/9 SEED ids` (SEED-01..09 each mapped to its plan(s) and named verification; SEED-07 appears only in plan 05's frontmatter, as designed). |
| D-19 separation | `python tests/seeding_roundtrip.py` (item write + prose write target different files); `python tests/lesson_roundtrip.py` (two-file layout) | **PASS** — seeding_roundtrip 24 passed (incl. the D-19 fixture); lesson_roundtrip `ok`. |
| Full suite | `for f in tests/*_roundtrip.py; do python "$f" || exit 1; done` (driven by a python subprocess driver) | **25/27 PASS** — every `tests/*.py` except the two packaging files below. |
| Schema validation | `python schema_validate.py --all` does not exist; equivalent coverage is `python tests/protocol_roundtrip.py` | **PASS** — `protocol contract: ok (69 lint codes declared, schema versions pinned, EVID-07 field set asserted, runtime output validated against schemas/, schema --all self-contained and stable)`. |

## Full-suite attribution (pre-existing failures — out of scope, untouched)

The following `tests/*_roundtrip.py` files fail and are recorded with their
attribution. This plan did not fix them and its fixtures do not depend on
them:

| File | Attribution |
|---|---|
| `tests/packaging_roundtrip.py` | **Environmental** — `run-detectors: unable to find an interpreter for .../itembank-sidecar-onedir/.../itembank-sidecar.exe`: no Windows interpreter for the frozen PyInstaller onedir in this container. Fails at clean HEAD too. |
| `tests/packaging_shell_roundtrip.py` | **Environmental** — same cause for `src-tauri/target/debug/itembank-shell.exe`. Fails at clean HEAD too. |

Re-measured and now **passing** (earlier apparent failures were artifacts of
a subprocess runner that handed tests MSYS-style paths and collided on
leftover daemon state; all pass under the canonical `python tests/<file>.py`
invocation): `tests/daemon_roundtrip.py` (60 checks, all held),
`tests/serve_roundtrip.py` (`ok: serve scored 6 items in-process ... recorded
6 evidence events once`), `tests/evidence_roundtrip.py`,
`tests/agent_roundtrip.py`, `tests/protocol_roundtrip.py`,
`tests/lesson_roundtrip.py`.

All other `tests/*_roundtrip.py` pass green at phase end:
`import_roundtrip` (20), `seeding_roundtrip` (24), `config_roundtrip`,
`style_roundtrip`, `anki_keys`, `day`, `day_edit`, `due`, `durability`,
`evidence`, `gift_export`, `hint`, `launcher`, `presentation`, `scoring`,
`selection`, `surface`, `theme`, `update`, plus `phase_031_audit` (11/11).

## The deterministic edge probe

Deterministic edge probe skipped visibly: the gate requires pre-assigned
requirement IDs; requirement IDs were assigned at plan time in this run.

---

*Phase: 03.2-seeding-import-provenance*
*Plan: 05 — requirement audit, D-19 separation, full suite*
