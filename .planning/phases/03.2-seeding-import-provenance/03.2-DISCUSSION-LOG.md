# Phase 3.2: Seeding, Import & Provenance - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.

**Date:** 2026-08-10
**Phase:** 03.2-seeding-import-provenance
**Mode:** autonomous, per `PLANNING-DIRECTIVES.md` §1/§2. No question was put to the user.
**Areas discussed:** ruling 4 (widen and move), importer dependency, unsupported-note handling, accept-loop surfaces, paraphrase thresholds, corpus residency

---

## Ruling 4 — widen and move 03.2 ahead of Phase 5?

| Option | Description | Selected |
|--------|-------------|----------|
| Adopt | Full seeding + import + provenance, landing before Phase 5 | ✓ |
| Decline | Shrink to provenance only; Phase 11 keeps the seeding loop | |

**Choice:** adopt. **Notes:** research named content and adherence, not features, as the bottleneck, and no success criterion elsewhere measures actual use. Building the check type, hint ladder and selector first means tuning them against fixtures and re-tuning against reality.

---

## Importer dependency

| Option | Description | Selected |
|--------|-------------|----------|
| stdlib ZIP + `sqlite3` | No dependency on Anki's internal schema churn | ✓ |
| `anki` package | Schema handled for us | |
| AnkiConnect | Requires Anki running | |

**Choice:** stdlib. **Notes:** this is *not* the stdlib-only preference talking (Directive §4a forbids that as a whole reason). The real argument is that the `anki` package couples us to internal schema churn we would then have to track, and AnkiConnect makes import require a running app.

---

## Unsupported note types

| Option | Description | Selected |
|--------|-------------|----------|
| Loud named refusal per note | Same posture `surfaces/gift.py` already takes | ✓ |
| Best-effort approximation | More notes converted | |

**Choice:** refuse by name. **Notes:** a lossy approximation of a note the learner will study is worse than a note they know is missing.

---

## Accept-loop surfaces

| Option | Description | Selected |
|--------|-------------|----------|
| CLI + daemon route, one endpoint | Both surfaces, one decision | ✓ |
| CLI only | Cheaper | |
| Browser only | Nicer | |

**Choice:** both. **Notes:** `CLAUDE.md` — every capability has a route and a command, and neither is the real one.

---

## Paraphrase thresholds

| Option | Description | Selected |
|--------|-------------|----------|
| Settings, defaults ≥8 words / Jaccard 0.25 | Tunable against the real corpus without a code change | ✓ |
| Hard constants | Simpler | |

**Choice:** settings with those defaults. **Notes:** these will need tuning against real AAOS-derivative and course text; a code change per tune is the wrong cost. What is *not* tunable: source text is never stored.

---

## Corpus residency

| Option | Description | Selected |
|--------|-------------|----------|
| Beside the private bank, outside the repo; `itembank guard` enforces | Directive §4.3 | ✓ |
| In-repo behind .gitignore | Convenient | |

**Choice:** outside the repo. **Notes:** the guard already exists; extend its globs rather than adding a second guard.

---

## Claude's Discretion

Winnowing window and k-gram size, report layout, importer factoring, source-registry cache shape.

## Deferred Ideas

Autonomous multi-item writes (Phase 11); curriculum auditor (Phase 11); selection over `[PREREQ:]` (Phase 7); exam-sim presets over `[CASE:]` (Phases 6/7).
