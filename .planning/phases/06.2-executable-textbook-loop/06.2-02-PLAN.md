---
phase: 06.2-executable-textbook-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 06.2-01
files_modified:
  - surfaces/lesson.py
  - surfaces/presentation.py
  - tests/gate_roundtrip.py
  - tests/lesson_roundtrip.py
autonomous: true
ui_classification: READING-SURFACE
requirements:
  - GATE-01
  - GATE-04
  - GATE-05
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
    - "The gate is a presentation policy over existing item types: one render path with a policy argument, never a second reader (D-05)."
    - "[GATE: required] still ships and is not a lie about enforcement: it is a stronger default posture with a more deliberate skip affordance, never marketed or documented as making skipping impossible (D-03)."
    - "A recorded skip preserves the loop's value better than a hard gate does: skipped-and-still-unclear is a prioritizable signal, while a hard gate learns nothing (D-04)."
    - "The gate never hides content from a screen reader that a sighted learner can reach, and never reveals through a hidden label what the visual path withholds - the no-leak fixture asserts absence across ARIA and accessible names (D-13, UI-SPEC section 8 item 4)."
    - "[GATE: required] selects G1 truncate-and-reveal: prose past an uncleared check is NOT emitted by the server at all - not hidden, not aria-hidden, not display:none, not in any JSON payload, not in any accessible name (06.2-UI-SPEC section 5.2 DOM ruling, C6)."
    - "[GATE: recommended] renders G2 inline-and-continue: the whole lesson is in the DOM, the band sits in the flow, and clearing marks the band cleared (06.2-UI-SPEC section 5.2)."
    - "[GATE: off] renders Phase 3.1's reader unchanged - the anchor is the inert D2 slot with 3.1's copy (06.2-UI-SPEC section 5.0)."
    - "The zero-reflow invariant holds: the emitted bytes preceding the band's opening tag are byte-identical across the inert, open, cleared, and skipped states, and the band's box metrics are identical across all four (06.2-UI-SPEC section 5.1, section 13 gate 2)."
    - "In diagnostic and exam feedback modes, [GATE: required] degrades to recommended: the complete lesson renders, no truncation boundary, no skip control, no gate_skip, and the exact line 'This sitting holds feedback until it ends, so checks do not gate reading here.' (06.2-UI-SPEC section 5.7)."
    - "A bank whose lessons declare no gates renders byte-identically to Phase 3.1 output, reusing Phase 3.1's own fixture bank with the style-block exclusion named and 3.1 section 12 cited (GATE-05, D-14)."
    - "This phase needs no JavaScript at all - every element is a server-rendered form POST and a re-render (06.2-UI-SPEC section 1)."
  prohibitions:
    - statement: "Below-gate content under required must never be hidden client-side; the server must not emit it."
      status: unverified
      flagged: true
    - statement: "No plan may substitute a CSS/details-based hide for server-side truncation under required."
      status: unverified
      flagged: true
---

<objective>
As a learner, I want a gated lesson to read like a textbook that holds the next idea until the check is cleared — and never like a wall, a trick, or a second kind of question.

Purpose: The render policy plan: activate 3.1's D2 slot into the live gate band, implement the three [GATE:] renderings, the truncation boundary, the mode-degrade rule, the zero-reflow invariant, and the compatibility floor.
Output: The gate band and truncation rendering in surfaces/lesson.py, the .gate CSS addition, and the render-layer fixtures.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/06.2-executable-textbook-loop/06.2-CONTEXT.md
@.planning/phases/06.2-executable-textbook-loop/06.2-RESEARCH.md
@.planning/phases/06.2-executable-textbook-loop/06.2-PATTERNS.md
@.planning/phases/06.2-executable-textbook-loop/06.2-UI-SPEC.md
@.planning/phases/06.2-executable-textbook-loop/06.2-VALIDATION.md

<interfaces>
- `surfaces.lesson.lesson_page()` is the one reader entry point; the gate policy threads through it as an argument, not a fork.
- Phase 3.1's D2 slot markup (the reserved `[!CHECK:]` render) is the exact box 6.2 activates; read 3.1's shipped source at execution time and reuse its class names and metrics.
- `gate_state()` from plan 06.2-01 is the state source for every render.
- `surfaces.presentation.py` SHARED_CSS is the token layer; 6.2 adds exactly one rule block (.gate) plus its boundary rule, with no new family, size, weight, or color literal.
- Phase 6's feedback-mode state machine (mode) drives the diagnostic/exam degrade rule.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `surfaces/lesson.py` | Gate-policy argument on the lesson render; the live gate band (activates 3.1's D2 slot into a form with the check item); the truncation boundary under required; the mode-degrade branch; TOC/glossary filtering for gated reads |
| `surfaces/presentation.py` | `.gate` rule block + truncation-boundary rule in the lesson CSS layer |
| `tests/gate_roundtrip.py` | No-leak, zero-reflow, mode-degrade, and render-policy fixtures |
| `tests/lesson_roundtrip.py` | Compatibility-floor fixture reusing Phase 3.1's bank |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate the D2 slot into the live gate band</name>
  <files>surfaces/lesson.py, surfaces/presentation.py, tests/gate_roundtrip.py</files>
  <read_first>
    - Phase 3.1's shipped `surfaces/lesson.py` at execution time - the D2 reserved slot markup, class names, and LESSON_CSS (the box 6.2 activates)
    - `surfaces/presentation.py` - SHARED_CSS token names (no new family/size/weight/color literals)
    - `tests/gate_roundtrip.py` - the evidence-layer fixtures from plan 06.2-01
    - `06.2-UI-SPEC.md` sections 4 (typography/voice), 5.1 (ACTIVATED), 5.2 (the band), 15 (copy)
  </read_first>
  <behavior>
    - Test 1: with a session, the D2 slot renders as the live band: `<section class="gate">` containing the check item at text-body 16/1.5 Paper voice (public_item() projection), the Ledger header `Check · required to continue` or `Check · recommended`, and a single `<form method="post">` with two named submit buttons (`name="action" value="check"` first, `value="skip"` second) at 44px minimum.
    - Test 2: the band's box metrics (padding space-3, border 1px --line, radius --r-3, block margin space-4, 66ch measure) are identical to 3.1's D2 slot - the activation changes the contents, not the box.
    - Test 3: with no session, or [GATE: off], the anchor renders exactly as 3.1's inert D2 slot with its unchanged copy.
    - Test 4: no `<script>` element and no JavaScript dependency exists anywhere in the rendered page.
  </behavior>
  <action>
Read Phase 3.1's shipped D2 slot markup and LESSON_CSS at execution time, then activate it: when a session exists and the lesson's resolved gate policy is required or recommended, render the live band - the same box (padding space-3, 1px --line border, --r-3 radius, space-4 block margin, 66ch measure) with the check item's public_item() projection at text-body 16/1.5 (the item-size ruling: a gated check is the same object typographically as a quiz check), the Ledger band header (exact copy per section 15), and one `<form method="post">` whose two named submit buttons are `name="action" value="check"` first and `value="skip"` second, both >=44px, check button first in DOM order so Enter submits the check. Add exactly one rule block (`.gate`) plus the boundary rule to the lesson CSS layer with no new family, size, weight, or color literal. When no session exists or the policy resolves to off, keep 3.1's inert slot byte-identical. No JavaScript anywhere - form POST and server re-render only. Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/gate_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The live band renders the exact locked markup/copy; the inert and off paths match 3.1 byte-for-byte.
    - The band box metrics match the D2 slot's (fixture asserts computed values via the CSS tokens).
    - No script element and no script-dependent behavior exists.
  </acceptance_criteria>
  <done>The reserved slot is live: one form, two buttons, zero JavaScript, same box.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the three renderings and the truncation boundary</name>
  <files>surfaces/lesson.py, tests/gate_roundtrip.py, tests/lesson_roundtrip.py</files>
  <read_first>
    - `surfaces/lesson.py` - the lesson render after Task 1, the section walk, and 3.1's render_markdown()/lesson_page()
    - `gate_state()` from plan 06.2-01 - the state source
    - `06.2-UI-SPEC.md` sections 5.2 (DOM ruling + what is withheld), 5.3 (truncation boundary), 5.5 (filtered TOC), 5.6 (glossary in gated reads), 13 gates 1-3
  </read_first>
  <behavior>
    - Test 1: under required with an uncleared check, every heading, paragraph, list item, table, code block, [!KEY] id, [!EXAMPLE] block, further [!CHECK:] id, backlink, TOC entry, and glossary entry belonging to a section below the gate is absent from the HTML, JSON, ARIA, CSS-off text, and accessible names (the no-leak fixture, 06.2-UI-SPEC section 13 gate 1).
    - Test 2: the truncation boundary renders `{n} more sections below this check.` (and `1 more section below this check.` for n=1), Ledger voice, 1px --line full-measure rule.
    - Test 3: the zero-reflow fixture - the emitted bytes preceding the band's opening tag are byte-identical across inert, open, cleared, and skipped states, and the band's box metrics are identical across all four (section 13 gate 2).
    - Test 4: under recommended, the whole lesson is present in the DOM with the band in flow; under off, 3.1's reader renders unchanged.
  </behavior>
  <action>
Implement the three renderings in `surfaces/lesson.py` as one render path with a policy argument (D-05): `required` selects G1 - the server renders up to the first uncleared `[!CHECK:]` anchor, then the band, then the truncation boundary, and emits nothing below; `recommended` selects G2 - the full lesson renders with the band in flow; `off` (or no session) renders 3.1's inert slot unchanged. The truncation boundary copy is `{n} more sections below this check.` with the singular `1 more section below this check.` variant, Ledger text-xs, 1px --line full-measure rule. Filter the reader_nav TOC and glossary appendix for gated reads per section 5.5/5.6 (the TOC lists only revealed sections plus `More sections appear as you clear each check.`). Preserve the zero-reflow invariant structurally: the render path emits the pre-band document identically regardless of state, with only the band contents and what follows varying. Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/gate_roundtrip.py &amp;&amp; python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The no-leak fixture passes across all seven surfaces it asserts.
    - The boundary copy has both plural/singular rows and no third variant.
    - The zero-reflow fixture passes byte-for-byte.
  </acceptance_criteria>
  <done>Required truncates honestly, recommended flows, off is untouched - one path, three policies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mode-degrade, compatibility floor, and the render-layer fixtures</name>
  <files>surfaces/lesson.py, tests/gate_roundtrip.py, tests/lesson_roundtrip.py</files>
  <read_first>
    - Phase 6's feedback-mode state machine (mode values and the no-feedback-until-sitting-ends rule)
    - Phase 3.1's shipped compatibility-floor fixture bank and its byte-identity fixture (reuse, never re-author)
    - `06.2-UI-SPEC.md` sections 5.7 (mode-degrade), 13 gates 3, 8 (mode-degrade fixture), 12 (compatibility floor)
  </read_first>
  <behavior>
    - Test 1: [GATE: required] in diagnostic mode and in exam mode renders the complete lesson, emits no truncation boundary, offers no skip control, records no gate_skip, and renders the exact line `This sitting holds feedback until it ends, so checks do not gate reading here.`
    - Test 2: the compatibility-floor pair - a bank with no [GATE:] directive and no [!CHECK:] anchors renders byte-identically to Phase 3.1's output for the same bank, with the style-block exclusion named and 3.1 section 12 cited; the parse is unchanged.
    - Test 3: a lesson whose check id resolves to an item not in the bank renders the D1 labelled rule with the warn-tone line `This check refers to an item that is not in this bank. Run itembank lint <bank> for details.` and does not gate - reading continues.
  </behavior>
  <action>
Implement the mode-degrade branch: when the session's feedback mode is diagnostic or exam, a declared `required` gate renders as recommended (complete lesson, no truncation boundary, no skip control, no gate_skip emission) with the exact Ledger line `This sitting holds feedback until it ends, so checks do not gate reading here.` (06.2-UI-SPEC section 5.7). Add the unresolvable-check render: a `[!CHECK: <id>]` naming an id not in the bank renders as the D1 labelled rule on screen with the warn-tone line and never gates (06.2-UI-SPEC section 14 error row). Reuse Phase 3.1's fixture bank for the compatibility-floor fixture: render it through the 6.2-updated reader and assert byte-identity to 3.1's output with the style-block exclusion named. Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/gate_roundtrip.py &amp;&amp; python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The mode-degrade fixture passes for both diagnostic and exam modes with no gate_skip and no boundary.
    - The compatibility-floor pair passes (byte-identity + parse identity, style exclusion named).
    - The unresolvable-check render degrades with the exact copy and never gates.
  </acceptance_criteria>
  <done>Every edge renders honestly: no dead gate, no fake wall, no silent divergence from 3.1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| lesson content below a required gate -> learner | Answer-adjacent material must not reach the learner before legal disclosure |
| bank-authored check text -> rendered band | Untrusted author text becomes learner-facing HTML through the existing escape discipline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-062-06 | Information Disclosure | below-gate prose under required | high | mitigate | The server does not emit the bytes (DOM ruling); the no-leak fixture asserts absence across HTML, JSON, ARIA, CSS-off text, and accessible names (06.2-UI-SPEC section 13 gate 1) |
| T-062-07 | Tampering | check item key/objective leak | high | mitigate | The band renders public_item() only - the same projection the quiz surface uses; the check's key, rationale, and objective are absent before the verdict |
| T-062-08 | Injection | check stem/options into the band | high | mitigate | Existing html.escape()/public_item() discipline extends to the band render; the stored-XSS fixture pattern from 3.1 applies |
| T-062-09 | Spoofing | mode-degrade bypass | medium | mitigate | The degrade branch reads the session's feedback mode from the runtime state, not a client flag; a diagnostic/exam session cannot strengthen a gate |
| T-062-SC | Tampering | package operations | high | mitigate | This plan performs no package operation and no network fetch |
</threat_model>

<verification>
- `python tests/gate_roundtrip.py`
- `python tests/lesson_roundtrip.py`
- Grep the rendered page for `<script` - must return nothing.
</verification>

<success_criteria>
- The live band activates 3.1's slot with zero JavaScript and identical box metrics.
- required truncates at the server, recommended flows, off is untouched; the mode-degrade rule holds.
- The compatibility floor reuses 3.1's fixture and passes byte-for-byte.
</success_criteria>

<output>
Create `.planning/phases/06.2-executable-textbook-loop/06.2-02-SUMMARY.md` when done.
</output>
