---
phase: 06.2-executable-textbook-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model.py
  - evidence.py
  - schemas/response.schema.json
  - surfaces/cli.py
  - tests/gate_roundtrip.py
  - tests/evidence_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "[GATE: required|recommended|off] parses as a single lesson-preamble directive following the exact [LESSON-SRC:] grab pattern (model.py:213); an invalid value is a lint error, never a render-time crash; absent means default recommended (06.2-RESEARCH.md section 1)."
    - "gate_skip is a new event type appended to KNOWN_EVENT_TYPES (evidence.py:43), built by gate_skip_event() mirroring day_tick_event()/mark_event() with a narrow (session_id, check_item_id) dedupe key and - structurally - no score key at all, not even None (D-07, criterion 8)."
    - "response_event() gains an additive context field (\"quiz\" default, \"lesson_gate\" from the one new call site) with the schema's required/properties updated in the same commit because additionalProperties: false (06.2-RESEARCH.md section 2 - a published-contract change, PROTO-01)."
    - "gate state is derived from the evidence log per session (gate_state(log, session_id, check_item_id) -> open|cleared|skipped), never stored as per-lesson progress (D-06)."
    - "A cleared inline check writes ordinary response evidence with the check item's own objective, so objective_history(log, objective) returns it alongside quiz responses on the same objective in one undifferentiated list differing only by context - proving Phase 10 needs no branch (GATE-03, D-08)."
    - "A skipped gate is visible to Phase 10 as a prioritization signal: this phase writes the gate_skip event and defines its meaning; it does not implement the scheduler's response to it (D-09)."
    - "The shared cross-bank [!CHECK:] lint constant and message are imported from Phase 3.1's shipped code, never restated as string literals (D-01, 06.2-UI-SPEC section 13 gate 12)."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
  prohibitions:
    - statement: "A gate_skip event must never carry a score key, and must never be conflated with a response carrying a null score."
      status: unverified
      flagged: true
    - statement: "No second store of cleared-gate progress may be created; gate state is a read over the one evidence log."
      status: unverified
      flagged: true
---

<objective>
As the runtime, I want the gate's grammar, evidence shapes, and state derivation to exist and be locked before any learner-facing pixel ships — because the evidence log is append-only and a wrong field set is a one-way door.

Purpose: The foundation plan: the [GATE:] directive, the gate_skip event type, the response-event context field, the read-side gate_state derivation, and the GATE-01..04 evidence-layer tests.
Output: model.py [GATE:] parsing, evidence.py gate_skip_event() + context field, schema updates, gate_state(), and tests/gate_roundtrip.py.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/06.2-executable-textbook-loop/06.2-CONTEXT.md
@.planning/phases/06.2-executable-textbook-loop/06.2-RESEARCH.md
@.planning/phases/06.2-executable-textbook-loop/06.2-PATTERNS.md
@.planning/phases/06.2-executable-textbook-loop/06.2-UI-SPEC.md
@.planning/phases/06.2-executable-textbook-loop/06.2-VALIDATION.md

<interfaces>
- `model.grab()` is the one directive reader; `[GATE:]` follows the `[LESSON-SRC:]` shape exactly (06.2-RESEARCH.md section 1, Pitfall 3).
- `evidence.append_event()` remains the one evidence writer; `gate_skip_event()` builds an envelope like `day_tick_event()`/`mark_event()`, never via `response_event()`.
- `runtime.score_response()` is untouched by this phase - the gate calls it, it does not extend it.
- `schemas/response.schema.json` follows the established pattern: one enforced top-level `response` shape, descriptive `$defs` for the other event types (mark/retraction/day_tick precedent).
- Phase 3.1's shipped `[!CHECK:]` parse output and cross-bank lint constant are read from source at execution time (3.1 executes before 6.2); this plan never trusts a paraphrase.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `model.py` | `[GATE:]` directive grab in the lesson preamble, `GATE_VALUES = ("required", "recommended", "off")`, lint code for invalid gate value and for `[!CHECK:]` references to ids not in the bank (importing the shared constant from 3.1) |
| `evidence.py` | `"gate_skip"` in `KNOWN_EVENT_TYPES`; `gate_skip_event(session_id, bank, lesson_slug, check_item_id, check_item_ref, objective, gate_mode)`; `context="quiz"` parameter on `response_event()`; `gate_state(log, session_id, check_item_id)` read-side derivation |
| `schemas/response.schema.json` | `context` in the response `required`/`properties` (enum `["quiz", "lesson_gate"]`); `gate_skip_event` $def |
| `tests/gate_roundtrip.py` | GATE-01..04 evidence-layer fixtures |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse [GATE:] and wire the shared [!CHECK:] lint constant</name>
  <files>model.py, surfaces/cli.py, tests/gate_roundtrip.py</files>
  <read_first>
    - `model.py` - the [LESSON-SRC:] grab at line 213, parse_lesson(), LINT_CODES, lint()
    - Phase 3.1's shipped code at execution time - `[!CHECK:]` parsing, the cross-bank lint constant it defines (read from source, never a paraphrase)
    - `surfaces/cli.py` - cmd_lint (lines 31-55)
    - `tests/gate_roundtrip.py` - to be created; follow the fail() harness in tests/evidence_roundtrip.py
    - `06.2-RESEARCH.md` section 1 (the [GATE:] interface) and Pitfall 3
  </read_first>
  <behavior>
    - Test 1: a lesson carrying `[GATE: required]` / `[GATE: recommended]` / `[GATE: off]` in its preamble parses the value; a lesson with none yields the default `recommended`; an invalid value (e.g. `[GATE: maybe]`) is a lint error with a named code, never a render-time crash.
    - Test 2: a bank with no `[GATE:]` directive parses byte-identically to pre-change output (additive grammar).
    - Test 3: the `[!CHECK:]` cross-bank/unresolvable lint code and message are the exact constants Phase 3.1's tests assert - imported, not restated (the cross-phase divergence guard).
  </behavior>
  <action>
Add the `[GATE:]` directive to `model.py`'s lesson parsing: `gate = grab(r"(?m)^\[GATE:\s*(.*?)\s*\]", head) or "recommended"`, validating the value against `GATE_VALUES = ("required", "recommended", "off")`. An invalid value is a lint error naming the rule and the value; a missing directive is the default `recommended` (CONTEXT D-02). Add `[!CHECK:]` resolution linting: every `[!CHECK: <id>]` in the lesson must resolve to an item in the same bank (D-01); an unresolvable or cross-bank reference is a lint error whose code and message are imported from Phase 3.1's shipped constant (read the actual 3.1 source at execution time - if 3.1 shipped a different name, use that name; never define a duplicate string literal). Thread the gate value through the parse output dict for the renderer and the report. Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/gate_roundtrip.py &amp;&amp; python tests/lesson_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The three gate values parse and default correctly; the invalid-value lint error fires with a named code.
    - A no-gate bank parses byte-identically.
    - The [!CHECK:] lint code/message strings match Phase 3.1's shipped constants (grep equality against 3.1's tests).
  </acceptance_criteria>
  <done>The gate grammar exists, is additive, and cannot drift from 3.1's check contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add gate_skip_event and the response-event context field</name>
  <files>evidence.py, schemas/response.schema.json, tests/gate_roundtrip.py, tests/evidence_roundtrip.py</files>
  <read_first>
    - `evidence.py` - KNOWN_EVENT_TYPES (line 43), response_event() (line 355), day_tick_event() (line 1350), mark_event() (line 1035), append_event() (line 398), live_events() (lines 996-1011)
    - `schemas/response.schema.json` - the required/properties arrays and the mark/retraction/day_tick $defs
    - `tests/evidence_roundtrip.py` - the fixture harness
    - `06.2-RESEARCH.md` section 3 (gate_skip shape) and section 2 (context field)
  </read_first>
  <behavior>
    - Test 1: `gate_skip_event(...)` produces an event with every key in the proposed shape (schema_version, event_id, event_type="gate_skip", ts, session_id, bank, lesson_slug, check_item_id, check_item_ref, objective, gate_mode, dedupe_key) and no `score` key at all - asserted structurally (`"score" not in ev`).
    - Test 2: a second gate_skip_event for the same (session_id, check_item_id) has the same dedupe_key and append_event() reports already_recorded rather than appending a second fact.
    - Test 3: `response_event(..., context="lesson_gate")` carries context and validates against the updated schema; every existing caller without the argument still works and records context="quiz".
    - Test 4: objective_history(log, objective) returns a lesson-gate response and a quiz response on the same objective in one undifferentiated, correctly ordered list, differing only by context (GATE-03's "Phase 10 needs no branch" proof).
  </behavior>
  <action>
Append `"gate_skip"` to `KNOWN_EVENT_TYPES` (the one-line addition). Implement `gate_skip_event(session_id, bank, lesson_slug, check_item_id, check_item_ref, objective, gate_mode)` mirroring `day_tick_event()`/`mark_event()` exactly: own envelope, `ValueError` on a gate_mode outside `("required", "recommended")` ("off" never offers a skip), `dedupe_key` = sha256 of `session_id|check_item_id`, and no score key in the returned dict - not even None (criterion 8's structural form). Add the `context="quiz"` keyword parameter to `response_event()` and emit it in the event dict; update `schemas/response.schema.json`'s response `required`/`properties` with `context` (enum `["quiz", "lesson_gate"]`) in the same commit (additionalProperties: false makes this a published-contract change), and add the descriptive `gate_skip_event` $def following the mark/retraction/day_tick precedent. Update every existing `response_event()` call site that needs the lesson_gate value (the one new call site lands in plan 06.2-03; other sites keep the default). Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/gate_roundtrip.py &amp;&amp; python tests/evidence_roundtrip.py &amp;&amp; python schema_validate.py --all</automated>
  </verify>
  <acceptance_criteria>
    - gate_skip events append, dedupe within a session, read back through live_events(), and contain no score key.
    - The schema validates both context values and rejects any other value; the validator passes on the full document set.
    - The objective_history fixture proves the two surfaces collapse to one list differing only by context.
  </acceptance_criteria>
  <done>The evidence layer is locked: skip is its own fact, context is explicit, and one store feeds Phase 10 unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derive gate_state from the evidence log</name>
  <files>evidence.py, tests/gate_roundtrip.py</files>
  <read_first>
    - `evidence.py` - live_events() (lines 996-1011), retracted_ids() (lines 980-993), the read-side filter discipline
    - `06.2-RESEARCH.md` section 8 (pair-level aggregation) and the reader-behavior table in section 3
    - `06.2-UI-SPEC.md` section 14 (session-scoped derivation DEFAULT)
  </read_first>
  <behavior>
    - Test 1: gate_state(log, session_id, check_item_id) returns "open" with no events, "cleared" after a live response with context="lesson_gate" for the pair, "skipped" after a gate_skip with no subsequent response, and "cleared" after a skip-then-clear sequence (pair-level resolution).
    - Test 2: gate state is session-scoped - the same check cleared in an earlier sitting gates again in a new session (the 06.2-UI-SPEC section 14 DEFAULT).
    - Test 3: a retracted response for the pair returns the gate to its prior state (read-side filter discipline; no second store to go stale).
  </behavior>
  <action>
Implement `gate_state(log, session_id, check_item_id)` in `evidence.py` as a pure read over `live_events()`: collect response events with `event_type == "response"`, `context == "lesson_gate"`, and the matching check_item_id, plus gate_skip events for the pair; apply the existing retraction discipline (retracted_ids collected before anything is emitted); resolve per pair - any live cleared response means "cleared" (regardless of a prior skip), a live gate_skip with no subsequent response means "skipped", otherwise "open". Session-scoped by construction (the session_id argument); never cached across renders, never written anywhere (D-06). Expose it for the lesson renderer and the report surface. Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/gate_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - All four states and the skip-then-clear resolution pass; retraction returns the gate to its prior state.
    - gate_state performs no writes and keeps no module-level cache (grep/AST fixture).
  </acceptance_criteria>
  <done>Gate state is a query, not a store - the one evidence log is the whole mechanism.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| lesson directive -> parse/lint | Bank-authored [GATE:]/[!CHECK:] text becomes runtime policy; invalid values must fail loudly and additively |
| evidence events -> downstream counts | A skip must never look like a pending response in any consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-062-01 | Tampering | [GATE:] directive | medium | mitigate | Value validated against GATE_VALUES; invalid values are named lint errors, never render-time crashes or silent defaults |
| T-062-02 | Spoofing | gate_skip as pending response | high | mitigate | gate_skip_event structurally omits the score key; existing readers' explicit type-equality filters exclude it by construction (06.2-RESEARCH.md section 3 reader table) |
| T-062-03 | Tampering | response schema | high | mitigate | The context field and schema update land in one commit (additionalProperties: false); schema_validate.py --all is in the verify loop |
| T-062-04 | Tampering | [!CHECK:] resolution drift | high | mitigate | The lint constant/message are imported from Phase 3.1's shipped code; the cross-phase divergence guard test asserts string equality against 3.1's tests |
| T-062-05 | Repudiation | skip provenance | low | mitigate | gate_skip carries bank, lesson_slug, check_item_id/ref, objective, gate_mode, session_id, ts, and a narrow dedupe key |
| T-062-SC | Tampering | package operations | high | mitigate | This plan performs no package operation and no network fetch |
</threat_model>

<verification>
- `python tests/gate_roundtrip.py`
- `python tests/evidence_roundtrip.py`
- `python schema_validate.py --all`
- Grep equality: 6.2's [!CHECK:] lint strings vs Phase 3.1's shipped constants.
</verification>

<success_criteria>
- [GATE:] parses additively with a named invalid-value lint error.
- gate_skip and context are locked in evidence and schema before any render ships.
- gate_state derives all four states from the log with no second store.
</success_criteria>

<output>
Create `.planning/phases/06.2-executable-textbook-loop/06.2-01-SUMMARY.md` when done.
</output>
