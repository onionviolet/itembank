---
phase: 06.2-executable-textbook-loop
plan: 04
type: verify
wave: 3
depends_on:
  - 06.2-01
  - 06.2-02
  - 06.2-03
files_modified:
  - tests/gate_roundtrip.py
  - tests/lesson_roundtrip.py
  - tests/daemon_roundtrip.py
  - tests/evidence_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
  - GATE-05
  - GATE-06
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "The twelve required verification gates from 06.2-UI-SPEC section 13 all have executable fixtures: no-leak, zero-reflow, compatibility-floor pair, focus, degraded, print, skip, mode-degrade, responsive, calm-progress, voice, and cross-phase divergence guard."
    - "Every one of the six GATE-01..GATE-06 requirement IDs is addressed by at least one plan and verified by a fixture or command named in that plan's acceptance criteria."
    - "The full test suite (`for f in tests/*_roundtrip.py; do python \"$f\" || exit 1; done`) passes green at phase end, including all pre-existing tests."
    - "The print path is a second server render with the gate policy forced to off: the complete ungated document prints, checks print as 3.1's D1 rule, no truncation boundary appears, and the evidence log length is unchanged across the request (06.2-UI-SPEC section 8)."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run (recorded in each PLAN.md)."
  prohibitions:
    - statement: "The phase must not seal with a required verification gate that has no executable fixture and no recorded manual-verification item."
      status: unverified
      flagged: true
    - statement: "No plan may claim a requirement is verified by a test that does not exist."
      status: unverified
      flagged: true
---

<objective>
As the phase, I want the twelve gate fixtures, the requirement audit, and the full suite all green together — so execution seals on evidence that the gate is honest, accessible, and additive.

Purpose: The phase-level verification plan: the twelve 06.2-UI-SPEC section 13 gates, the GATE-01..06 coverage audit, the print-path evidence check, and the full-suite run.
Output: The completed fixture set, 06.2-GATES.md, and the green full-suite run.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/06.2-executable-textbook-loop/06.2-CONTEXT.md
@.planning/phases/06.2-executable-textbook-loop/06.2-RESEARCH.md
@.planning/phases/06.2-executable-textbook-loop/06.2-PATTERNS.md
@.planning/phases/06.2-executable-textbook-loop/06.2-UI-SPEC.md
@.planning/phases/06.2-executable-textbook-loop/06.2-VALIDATION.md

<interfaces>
- The fixture harness is the repo's stdlib fail() pattern; each tests/*_roundtrip.py runs standalone.
- The twelve gates map to the fixtures named in 06.2-UI-SPEC section 13; each plan names the fixtures it owns.
- The requirement audit reads each plan's requirements field and each fixture file's assertions.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `tests/gate_roundtrip.py` | The twelve gate fixtures consolidated |
| `tests/lesson_roundtrip.py` | The compatibility-floor pair and the print-path evidence check |
| `tests/daemon_roundtrip.py` | The route-level fixtures finalized |
| `tests/evidence_roundtrip.py` | The evidence-layer fixtures finalized |
| Phase directory | `06.2-GATES.md` recording the twelve gates and their fixture/command mapping |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assemble the twelve verification gates</name>
  <files>tests/gate_roundtrip.py, tests/lesson_roundtrip.py, tests/daemon_roundtrip.py, tests/evidence_roundtrip.py</files>
  <read_first>
    - `06.2-UI-SPEC.md` section 13 (the twelve gates) and sections 8 (print), 9 (calm progress), 15 (voice)
    - `tests/gate_roundtrip.py` and siblings - the fixture conventions and the daemon-start helper
    - `06.2-VALIDATION.md` - the phase requirements to test map
    - The other three plans' files_modified and acceptance criteria
  </read_first>
  <behavior>
    - Test 1: the twelve gates each have a named fixture and pass: no-leak (all seven surfaces), zero-reflow (byte-identical pre-band bytes across four states), compatibility-floor pair (reusing 3.1's bank), focus (required reveal, recommended stay, no re-submit on reload), degraded (daemon stopped), print (complete ungated, D1 rules, no boundary, log length unchanged), skip (one gate_skip, no response, band live), mode-degrade (diagnostic + exam), responsive (1280/768/375 + 320px and 200% zoom, [GATE:] x gate_skip combinations, reader_nav: column in at least one), calm-progress (no count, percentage, track, streak, check/cross glyph, exclamation, or transition on the band/reveal/boundary), voice (computed font-family token names), and cross-phase divergence guard (6.2's [!CHECK:] lint strings equal 3.1's).
    - Test 2: the print request changes no evidence - the log length is identical before and after `?print=1` (06.2-UI-SPEC section 8.3).
  </behavior>
  <action>
Consolidate and complete the twelve gate fixtures per the ownership in plans 06.2-01..03, implementing each exactly as 06.2-UI-SPEC section 13 enumerates - no gate may be recorded as executed without its test passing. The print fixture serves `?print=1` on a lesson with an uncleared required gate and asserts: the complete ungated document, every check as 3.1's D1 labelled rule, no truncation boundary, and no evidence-log growth across the request. Write the two behavior tests first.
  </action>
  <verify>
    <automated>python tests/gate_roundtrip.py &amp;&amp; python tests/lesson_roundtrip.py &amp;&amp; python tests/daemon_roundtrip.py &amp;&amp; python tests/evidence_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - All twelve gates have a named fixture file and pass.
    - The print-path fixture proves the log length is unchanged.
    - The cross-phase divergence guard asserts string equality with 3.1's shipped constants.
  </acceptance_criteria>
  <done>The phase's twelve gates are executable evidence, not intentions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the requirement-coverage audit and the full suite</name>
  <files>tests/gate_roundtrip.py, tests/lesson_roundtrip.py, tests/daemon_roundtrip.py, tests/evidence_roundtrip.py</files>
  <read_first>
    - `REQUIREMENTS.md` - the six GATE-01..06 rows and the traceability table
    - `ROADMAP.md` - the Phase 6.2 section with the requirement IDs
    - The four plan files' requirements fields and acceptance criteria
    - `06.2-VALIDATION.md` - the phase requirements to test map
  </read_first>
  <behavior>
    - Test 1: every one of GATE-01..GATE-06 appears in at least one plan's requirements field, and each such plan's acceptance criteria name a test command or fixture that proves it.
    - Test 2: the full suite - `for f in tests/*_roundtrip.py; do python "$f" || exit 1; done` - exits 0 with all pre-existing and new tests green.
    - Test 3: `python schema_validate.py --all` passes (the context field and gate_skip $def validate).
    - Test 4: the deterministic edge-probe skip is recorded in each PLAN.md (grep for the skip sentence).
  </behavior>
  <action>
Write the audit as an executable check (extend a test file or add `tests/phase_062_audit.py` in the fail() harness): parse each PLAN.md's requirements field, cross-reference the six IDs, and fail on any ID with no plan and no named verification. Run the full suite and schema validation, fixing any failures this surfaces (fixtures only - no new product code). Write `06.2-GATES.md` recording the twelve gates, their fixture file, and any human-verify items (true screen-reader announcement behavior is a human-verify item per 06.2-UI-SPEC section 14; responsive behavior is automated by snapshot). Record that the deterministic edge probe was skipped visibly per the handoff. Write the four behavior tests first.
  </action>
  <verify>
    <automated>for f in tests/*_roundtrip.py; do python "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - The audit exits 0 with 6/6 IDs covered and every plan's named verification exists.
    - The full suite and `schema_validate.py --all` pass.
    - 06.2-GATES.md exists with the twelve gates mapped and the human-verify items named.
  </acceptance_criteria>
  <done>Phase 6.2 seals on evidence: every requirement covered, every gate executable, the whole suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixtures -> suite result | A fixture that asserts nothing would launder a regression |
| gate checklist -> phase seal | An unexecuted gate must not be recorded as executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-062-15 | Spoofing | fixture coverage | high | mitigate | The requirement-coverage audit cross-references plan requirements against named verifications; a claim without a fixture fails the audit |
| T-062-16 | Tampering | gate checklist | medium | mitigate | Each gate maps to a fixture file and a command; the phase cannot record a gate as executed without its test passing |
| T-062-17 | Repudiation | human-verify items | low | mitigate | 06.2-GATES.md records automated vs human-verified items |
| T-062-SC | Tampering | package operations | high | mitigate | This plan performs no package operation and no network fetch |
</threat_model>

<verification>
- The full-suite loop command (all tests/*_roundtrip.py).
- `python schema_validate.py --all`.
- The audit's 6/6 requirement-coverage line.
</verification>

<success_criteria>
- The twelve gates are executable and green.
- 6/6 requirement IDs map to plans and named verifications.
- The full suite passes together with all pre-existing tests.
</success_criteria>

<output>
Create `.planning/phases/06.2-executable-textbook-loop/06.2-04-SUMMARY.md` when done.
</output>
