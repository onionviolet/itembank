---
phase: 06.2-executable-textbook-loop
plan: 03
type: execute
wave: 2
depends_on:
  - 06.2-01
  - 06.2-02
files_modified:
  - surfaces/daemon.py
  - surfaces/cli.py
  - surfaces/lesson.py
  - surfaces/settings.py
  - schemas/settings.schema.json
  - itembank.json
  - tests/gate_roundtrip.py
  - tests/daemon_roundtrip.py
autonomous: true
ui_classification: READING-SURFACE
requirements:
  - GATE-04
  - GATE-06
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "A skip appends exactly one gate_skip event, appends no response event, advances no hint tier, produces no card or count in the hint ladder, and leaves the band's response controls live and submittable (06.2-UI-SPEC section 6.3, C17)."
    - "A learner can always read ahead by an explicit, recorded choice; the gate is a default, not a lock, and skipping is evidence too (D-11)."
    - "The skip affordance is a real focusable control with a visible label, announced through the single role=status region - never a hover-only or keyboard-inaccessible escape hatch (D-12)."
    - "The skip control is a real focusable submit button with the exact label 'Read ahead without answering', never styled as a transgression - no --bad/--warn/accent fill, no icon, no confirmation (06.2-UI-SPEC section 6.1, C16)."
    - "gate_skip: always | after-attempt (default always) and gate_policy: as-authored | off (default as-authored) are the two registered settings; gate_policy may only weaken a declared gate, never strengthen one (06.2-UI-SPEC sections 6.2, 10.1)."
    - "After a skip, the band stays answerable; a skip moves the reading position and does not dismiss the question (06.2-UI-SPEC section 6.3)."
    - "A skip that was not recorded never advances the reading position - no reveal without its event (06.2-UI-SPEC sections 6.3, 12)."
    - "Focus moves only when the document grew: under required a cleared or skipped POST moves focus to the newly revealed section's h2; under recommended focus stays on the submitted control (06.2-UI-SPEC section 7.1)."
    - "The check-submit route calls runtime.score_response() and records response evidence with context=\"lesson_gate\" - the same functions the quiz surface uses (CONTEXT D-08)."
    - "This phase's usage measure is the gate outcome split - cleared vs skipped over gates encountered, with a stated denominator, no target shown to the learner, and no streak (D-10)."
    - "Every capability has both a route and a CLI command (SURF-04): the skip action gets a CLI twin registered in ROUTE_CLI."
  prohibitions:
    - statement: "A skip must never be styled as a transgression or gated behind a confirmation dialog."
      status: unverified
      flagged: true
    - statement: "A reveal must never happen without its recorded event."
      status: unverified
      flagged: true
---

<objective>
As a learner, I want to read ahead by an explicit, recorded choice that is never punished, never confirmed, and never final — the check stays open, the evidence is honest, and the loop keeps its signal.

Purpose: The interaction plan: the recorded-skip control, the two settings that govern it, the daemon routes for check/skip with the loopback authority gate, the focus/announcement behavior, and CLI parity.
Output: The skip control in the band, the /lesson/<stem>/check and /lesson/<stem>/skip routes, the gate_skip/gate_policy settings, and the interaction fixtures.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/06.2-executable-textbook-loop/06.2-CONTEXT.md
@.planning/phases/06.2-executable-textbook-loop/06.2-RESEARCH.md
@.planning/phases/06.2-executable-textbook-loop/06.2-PATTERNS.md
@.planning/phases/06.2-executable-textbook-loop/06.2-UI-SPEC.md
@.planning/phases/06.2-executable-textbook-loop/06.2-VALIDATION.md

<interfaces>
- `runtime.score_response()` and `evidence.append_event()` are the one verdict/one writer path; the routes call them, never a second copy (D-05, D-08).
- `gate_skip_event()` from plan 06.2-01 is the skip writer; `gate_state()` is the render state source.
- The daemon's existing loopback-only authority gate (surfaces/daemon.py:518-543) covers the two mutating routes.
- `surfaces/cli.py` ROUTE_CLI gains the skip twin; `surfaces/settings.py` registers gate_skip and gate_policy following the validate-then-write precedent.
- The skip control lives in the band built in plan 06.2-02; this plan adds the interaction behavior and the routes.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `surfaces/daemon.py` | `POST /lesson/<stem>/check` (score + response evidence with context="lesson_gate") and `POST /lesson/<stem>/skip` (gate_skip event + re-render with the next section), both behind the loopback authority gate |
| `surfaces/cli.py` | CLI twin for the skip action in ROUTE_CLI |
| `surfaces/lesson.py` | The skip control conditional (gate_skip: always | after-attempt), the announcement/focus handling, and the gate_policy: off weakening branch |
| `surfaces/settings.py` | `gate_skip` (always default) and `gate_policy` (as-authored default) settings with schema rows and checked-in defaults |
| `tests/gate_roundtrip.py` | Skip fixture, focus fixture, degraded fixture, mode-degrade fixture |
| `tests/daemon_roundtrip.py` | Route-level check/skip fixtures |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The recorded-skip control and its settings</name>
  <files>surfaces/lesson.py, surfaces/settings.py, schemas/settings.schema.json, itembank.json, tests/gate_roundtrip.py</files>
  <read_first>
    - `surfaces/lesson.py` - the band from plan 06.2-02
    - `surfaces/settings.py` - the SETTINGS_CODES/validate-then-write precedent (from Phase 02.3)
    - `schemas/settings.schema.json` - the settings group shape
    - `06.2-UI-SPEC.md` sections 6.1 (never a transgression), 6.2 (gate_skip), 6.3 (what a skip does), 10 (settings), 15 (copy)
  </read_first>
  <behavior>
    - Test 1: the skip control is a real submit button with the exact label `Read ahead without answering`, same .actions row as the check button, space-3 apart, both >=44px, no --bad/--warn/accent fill, no icon, no confirmation.
    - Test 2: gate_skip: always renders the control from the first render; gate_skip: after-attempt renders the Ledger line `Read ahead becomes available after one attempt.` until one recorded attempt exists, never a disabled button.
    - Test 3: gate_policy: off renders any lesson ungated (the whole lesson in the DOM, band as the inert 3.1 slot) and can only weaken, never strengthen - a `recommended` lesson cannot become required.
    - Test 4: both settings validate through itembank config (validate-then-write) and appear in `itembank config` output with their locked defaults.
  </behavior>
  <action>
Implement the skip control in the band (plan 06.2-02's form): the second named submit button `name="action" value="skip"` with the exact label `Read ahead without answering`, ordinary control register (no warning color, no accent fill, no icon, no aria-describedby warning, no confirmation - 06.2-UI-SPEC section 6.1). Register the `gate_skip` setting (`always` default, `after-attempt` value renders the Ledger line `Read ahead becomes available after one attempt.` until a recorded attempt exists - never a disabled control, C9) and the `gate_policy` setting (`as-authored` default; `off` renders every lesson ungated and may only weaken, never strengthen - a strengthening value is rejected per section 10.1). Mirror both in `schemas/settings.schema.json` and `itembank.json`, following the validate-then-write precedent so invalid values are rejected by `itembank config set`. Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/gate_roundtrip.py &amp;&amp; python tests/config_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The skip control matches section 6.1's register exactly (fixture asserts the class/token usage and the exact label).
    - Both settings round-trip through itembank config with the locked defaults.
    - gate_policy: off weakens only; no path strengthens a declared gate.
  </acceptance_criteria>
  <done>Reading ahead is a normal button and a setting, never a transgression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The check and skip routes with focus and announcement</name>
  <files>surfaces/daemon.py, surfaces/cli.py, surfaces/lesson.py, tests/gate_roundtrip.py, tests/daemon_roundtrip.py</files>
  <read_first>
    - `surfaces/daemon.py` - ROUTES/API_ROUTES (lines 40-130), the authority gate (lines 518-543), existing route handlers calling runtime/evidence
    - `surfaces/cli.py` - ROUTE_CLI parity table (lines 99-125)
    - `surfaces/lesson.py` - the band and truncation render from plan 06.2-02
    - `tests/daemon_roundtrip.py` - the daemon-start helper and route fixtures
    - `06.2-UI-SPEC.md` sections 7 (focus/announcement), 12.1 (degraded), 13 gates 4, 5, 7
  </read_first>
  <behavior>
    - Test 1: POST /lesson/<stem>/check with the check action scores via runtime.score_response(), records response evidence with context="lesson_gate", and re-renders; under required with a cleared check, the next section appears and focus targets its h2.
    - Test 2: POST /lesson/<stem>/skip records exactly one gate_skip event (no response event), re-renders with the next section appended, the band stays live and answerable, and the status region announces `Read ahead recorded. This check stays open.` once.
    - Test 3: the skip fixture - one skip appends exactly one gate_skip, no response, no hint-tier advance, no card/count anywhere in the hint ladder (06.2-UI-SPEC section 13 gate 7).
    - Test 4: with the daemon stopped, the band renders `This check cannot be submitted while the runtime is unreachable. Run itembank daemon and reload.`, no control is a disabled button with no reason, no spinner persists, and no section is revealed (section 13 gate 5).
    - Test 5: the reload-after-POST path does not re-submit the check (section 13 gate 4).
  </behavior>
  <action>
Add `POST /lesson/<stem>/check` and `POST /lesson/<stem>/skip` to the daemon's route table behind the existing loopback-only authority gate. The check route resolves the check item from the bank, calls `runtime.score_response()` (the one verdict path), records response evidence via `evidence.append_event(response_event(..., context="lesson_gate"))`, and re-renders the lesson with the next section when the gate clears; under recommended, the band marks cleared and focus stays on the submitted control (the DOM did not grow). The skip route validates the gate mode (never "off"), records exactly one `gate_skip_event(...)` per (session, check) via the existing dedupe (already_recorded on repeat), and re-renders with the next section appended - the band stays in place, live, and answerable (06.2-UI-SPEC section 6.3). Focus/announcement: under required, a cleared or skipped POST renders the page with focus on the newly revealed section's `<h2 tabindex="-1">` and the status region announcing the exact copy (`The next section is below.` appended to the verdict announcement; `Read ahead recorded. This check stays open.` for a skip); the announcement composes through the single role=status region (section 7.2). Register the skip action's CLI twin in ROUTE_CLI (SURF-04). When the daemon is unreachable the band renders the section 12.1 copy with no reveal. Write the five behavior tests first.
  </action>
  <verify>
    <automated>python tests/gate_roundtrip.py &amp;&amp; python tests/daemon_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The check route scores and records through the one runtime/evidence path with context="lesson_gate".
    - The skip route appends exactly one gate_skip, never a response, and the band stays answerable.
    - Focus/announcement behavior matches section 7.1/7.2; the degraded and reload fixtures pass.
    - The CLI twin exists in ROUTE_CLI.
  </acceptance_criteria>
  <done>Check and skip are real runtime actions with honest evidence, honest focus, and honest degradation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The gate outcome split report surface</name>
  <files>surfaces/daemon.py, surfaces/lesson.py, tests/gate_roundtrip.py</files>
  <read_first>
    - `gate_state()` and the pair-level aggregation design from 06.2-RESEARCH.md section 8
    - `06.2-UI-SPEC.md` section 11 (the report surface: shape, denominator, exclusion) and section 14 (empty state)
    - The Phase 6 report readout precedent (depth_readout setting) referenced by section 11
  </read_first>
  <behavior>
    - Test 1: the outcome split is computed per distinct (session_id, check_item_id) pair: a pair with any live cleared response counts as cleared (even with a prior skip); a pair with a live gate_skip and no subsequent response counts as skipped; the denominator is the count of distinct required-gate pairs encountered.
    - Test 2: recommended gates are excluded from the denominator and the exclusion line renders verbatim.
    - Test 3: the report renders `Of {n} required gates encountered` with the count table (rows cleared/skipped, columns count and share), no target, no streak, no percentage-fill bar against a 100% track, and `Of 0 required gates encountered` with no ratio for the empty state.
  </behavior>
  <action>
Implement `gate_outcome_split(log, bank, session_id)` (in the report path, computed from live events at request time - derived, never stored, Extensibility Rule 5): aggregate per distinct (session_id, check_item_id) pair, resolving each pair to exactly one of {cleared, skipped} per the pair-level rule (a cleared response wins over a prior skip; a skip with no subsequent response is skipped). The denominator is the distinct required-gate pairs encountered; recommended gates are excluded entirely (reading past one produces no event - C8 forbids inventing the count). Render the report as a plain Ledger-voice table (rows cleared/skipped, columns count and share) under the exact denominator copy `Of {n} required gates encountered` and the exclusion line `Recommended gates are not counted - reading past one is not a recorded choice.`; the empty state renders `Of 0 required gates encountered` and no ratio. Respect the Phase 6 depth_readout setting for the optional aria-hidden bar (never a fill against a 100% track). Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/gate_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The pair-level aggregation fixture passes (skip-then-clear resolves to cleared; the denominator counts distinct pairs, not events).
    - The denominator and exclusion lines render verbatim; recommended gates never appear.
    - No target, streak, or progress-fill exists anywhere in the report markup.
  </acceptance_criteria>
  <done>The outcome split is a derived, honest table with a stated denominator and nothing to game.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| learner form POST -> evidence | Mutating routes must be loopback-only and must record before revealing |
| skip action -> reading position | A reveal without its event would corrupt the outcome split |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-062-10 | Spoofing | skip/check POST from the network | high | mitigate | Both mutating routes sit behind the existing loopback-only authority gate (surfaces/daemon.py:518-543) |
| T-062-11 | Integrity | reveal without a recorded event | high | mitigate | The skip route records gate_skip before re-rendering; a failed write means no reveal (06.2-UI-SPEC sections 6.3, 12) |
| T-062-12 | Tampering | gate_policy strengthening | medium | mitigate | gate_policy may only weaken; a strengthening value is rejected at config validation (06.2-UI-SPEC section 10.1) |
| T-062-13 | Repudiation | skip provenance | low | mitigate | gate_skip carries bank, lesson_slug, check ids, objective, gate_mode, session_id, ts, dedupe key |
| T-062-14 | Information Disclosure | outcome split | low | mitigate | Report-only surface, never the reading column; no target shown to the learner (D-10) |
| T-062-SC | Tampering | package operations | high | mitigate | This plan performs no package operation and no network fetch |
</threat_model>

<verification>
- `python tests/gate_roundtrip.py`
- `python tests/daemon_roundtrip.py`
- `python tests/config_roundtrip.py`
</verification>

<success_criteria>
- The skip control, settings, and routes behave per the locked spec with honest evidence.
- The outcome split is derived, pair-level, and games nothing.
- Every interaction degrades per section 12 with no reveal without an event.
</success_criteria>

<output>
Create `.planning/phases/06.2-executable-textbook-loop/06.2-03-SUMMARY.md` when done.
</output>
