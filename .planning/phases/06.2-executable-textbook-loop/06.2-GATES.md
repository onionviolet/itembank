# Phase 06.2 — Required Verification Gates (UI-SPEC §13)

Every gate below is executable evidence, not an intention: each maps to a
named fixture and a command that runs green. One gate carries a
human-verify item (the true screen-reader announcement behavior), recorded
in the table and in the phase VERIFICATION.

| # | Gate | Fixture(s) | Command | Status |
|---|------|-----------|---------|--------|
| 1 | No-leak (C6): below-gate content absent from HTML/JSON/ARIA/CSS-off/names; key/rationale/objective absent before the verdict | `tests/gate_roundtrip.py#test_gate_required_truncation_no_leak` | `python tests/gate_roundtrip.py` | ✅ automated |
| 2 | Zero-reflow: pre-band bytes byte-identical across inert/open/cleared/skipped; band box metrics identical | `tests/gate_roundtrip.py#test_gate_zero_reflow` + `test_gate_band_box_metrics_match_slot` | `python tests/gate_roundtrip.py` | ✅ automated |
| 3 | Compatibility-floor pair: no-gate bank renders byte-identically to Phase 3.1 (style-block exclusion named, 3.1 §12) | `tests/gate_roundtrip.py#test_gate_compatibility_floor`; `tests/lesson_roundtrip.py#test_lesson_content_region_byte_identical_phase3` | `python tests/gate_roundtrip.py && python tests/lesson_roundtrip.py` | ✅ automated |
| 4 | Focus: required clear/skip moves to the revealed `<h2>`; recommended submit does not grow the DOM; reload-after-POST never re-submits | `tests/gate_roundtrip.py#test_gate_focus_recommended_no_growth`; `tests/daemon_roundtrip.py#check_gate_route_check_and_reveal` + `check_gate_route_reload_no_resubmit` | `python tests/gate_roundtrip.py && python tests/daemon_roundtrip.py` | ✅ automated |
| 5 | Degraded: daemon unreachable — 12.1 copy verbatim, no disabled-without-reason control, no spinner, no reveal | `tests/daemon_roundtrip.py#check_gate_route_unreachable_no_reveal`; `tests/gate_roundtrip.py#test_gate_band_live_markup` (unreachable branch) | `python tests/daemon_roundtrip.py` | ✅ automated |
| 6 | Print: `?print=1` serves the complete ungated document, every check as the D1 rule with `Check · <objective>`, no boundary, log length unchanged | `tests/gate_roundtrip.py#test_gate_print_fixture`; `tests/daemon_roundtrip.py#check_gate_print_no_evidence` | `python tests/gate_roundtrip.py && python tests/daemon_roundtrip.py` | ✅ automated |
| 7 | Skip: one gate_skip, no response, no hint-tier advance, no card/count in the hint ladder, band stays live | `tests/daemon_roundtrip.py#check_gate_route_skip_single_event`; `tests/gate_roundtrip.py#test_gate_skip_dedupe_within_session` | `python tests/daemon_roundtrip.py && python tests/gate_roundtrip.py` | ✅ automated |
| 8 | Mode-degrade: `required` in diagnostic and exam renders complete, no boundary, no skip control, no gate_skip, exact line | `tests/gate_roundtrip.py#test_gate_mode_degrade` | `python tests/gate_roundtrip.py` | ✅ automated |
| 9 | Responsive snapshots: 1280/768/375/320 + 200% zoom, `[GATE:]` all three values × `gate_skip` both values, `reader_nav: column` in at least one | `tests/gate_roundtrip.py#test_gate_responsive_snapshots` | `python tests/gate_roundtrip.py` | ✅ automated (server-rendered snapshot: viewport meta, wrap-only controls, no nowrap/ellipsis, both buttons at every combination) |
| 10 | Calm-progress: no count/percentage/track/streak/check-glyph/exclamation/transition/animation/smooth-scroll on the band, reveal, or boundary | `tests/gate_roundtrip.py#test_gate_calm_progress` | `python tests/gate_roundtrip.py` | ✅ automated |
| 11 | Voice: every §15 string renders in its §4.1 voice, asserted by computed font-family **token name** | `tests/gate_roundtrip.py#test_gate_voice_tokens` | `python tests/gate_roundtrip.py` | ✅ automated |
| 12 | Cross-phase divergence guard: the `[!CHECK:]` lint code/message are the shared `model.CHECK_UNRESOLVED_COPY` object, imported by identity | `tests/gate_roundtrip.py#test_gate_cross_phase_divergence_guard` + `test_check_ref_lint_constant_single_source` | `python tests/gate_roundtrip.py` | ✅ automated |

## Human-verify items (UI-SPEC §14, section 7.2 backstop)

| Behavior | Why manual | Test instructions |
|----------|-----------|-------------------|
| `role=status` announcement on a fresh server-rendered page | Live-region announcement on a full page load is browser/screen-reader dependent; the focus move is the primary channel and must carry the information on its own | With NVDA/VoiceOver, load `/lesson/<stem>?reveal=skip` after a skip POST and confirm the skip string is read; load after a cleared-check reveal and confirm focus lands on the revealed `<h2>` and the section heading is read. The `?reveal=` status text is additive (section 7.2). |

## Deterministic edge probe

The deterministic edge probe is skipped visibly: the gate requires
pre-assigned requirement IDs; GATE-01..GATE-06 were assigned at plan time
(REQUIREMENTS.md §Gate) and are recorded in each PLAN.md.

---

*Phase 06.2 — Executable Textbook Loop · gates assembled 2026-08-10*
