---
phase: 11-closed-authoring-loop-curriculum-auditor
plan: 05
type: execute
wave: 4
depends_on:
  - 11-03
  - 11-04
files_modified:
  - model_adapter.py
  - audit_writer.py
  - surfaces/audit_cli.py
  - surfaces/cli.py
  - schemas/model_adapter.schema.json
  - schemas/write_manifest.schema.json
  - tests/audit_writer_roundtrip.py
  - tests/audit_cli_roundtrip.py
  - fixtures/audit/writer_bank.md
autonomous: false
ui_classification: UI-DEPENDENT
ai_spec_required: true
ai_spec: .planning/phases/11-closed-authoring-loop-curriculum-auditor/11-AI-SPEC.md
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUDIT-05
  - AUDIT-06
  - AUDIT-07
  - AUDIT-08
  - AUDIT-09
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "AUDIT-05: report_only, draft_and_approve, and explicit full autonomy share one validation/diff/audit pipeline and differ only at the final permission decision."
    - "AUDIT-09: full autonomy reports proposed volume before generation and completed, rejected, retried, and written volumes afterward, with enforced per-run and per-objective caps."
    - "AUDIT-08: every derived report or accepted item shows its diff and receives a write id that one `itembank audit undo` command routes to Git revert or guarded shadow restoration."
    - "D-15/D-16: a tracked clean bank writes one accepted item per machine-authored Git commit; every other target gets a content-addressed before-image and manifest before atomic replacement; multi-item units are declared and visible."
    - "AUTH-01/AUTH-02: duplicate, interrupted, and parallel invocations cannot create an undisclosed prefix, duplicate item, stale overwrite, or out-of-scope write."
    - "AUTH-01/AUTH-03: the public `python itembank.py audit author ...` process resolves `model_backend` in `surfaces.cli.main`, calls Phase 8's `model_adapter.invoke(request, profile)`, repairs from structured findings, and completes without any callable supplied by the test or user."
    - "Any later browser preview, diff, approval, publish, or revert surface remains UI-BLOCKED on the approved 11-UI-SPEC; configured Phase 11 model authoring consumes 11-AI-SPEC.md and remains blocked until Phase 8 artifacts/tests and the Phase 11 AI evaluation fixtures are green."
  artifacts:
    - path: "model_adapter.py"
      provides: "Phase 8 public `invoke(request, profile)` adapter with additive strict author operation"
    - path: "audit_writer.py"
      provides: "Git/shadow transactional writer, recovery journal, manifest lookup, and one-step undo router"
    - path: "surfaces/audit_cli.py"
      provides: "Audit source/author/material/undo command handlers and explicit approval prompt"
    - path: "surfaces/cli.py"
      provides: "Real CLI composition-root registration, settings resolution, and configured adapter binding"
    - path: "tests/audit_writer_roundtrip.py"
      provides: "Git, shadow, report-artifact, stale undo, interruption, duplicate, and parallel writer tests"
    - path: "tests/audit_cli_roundtrip.py"
      provides: "Whole-command audit/author/approve/full/undo contract suite"
  key_links:
    - from: "authoring.apply_autonomy"
      to: "audit_writer.write_units"
      via: "only clean, fully preflighted proposals with mode-specific permission can cross the writer boundary"
    - from: "audit_writer.write_units"
      to: "Git or shadow backend"
      via: "exact target-path worktree/tracked/clean detection under an OS lock"
    - from: "surfaces.audit_cli.cmd_audit_undo"
      to: "audit_writer.undo"
      via: "manifest backend lookup by write id; callers never choose a restore mechanism"
    - from: "surfaces.cli.main"
      to: "model_adapter.invoke"
      via: "load_settings(base)['model_backend'] supplies the profile and the composition root closes over `invoke(request, profile)` for `audit author`"
    - from: "tests/audit_cli_roundtrip.py"
      to: "python itembank.py audit author"
      via: "subprocess with a configured fake hosted profile; no monkeypatch, direct handler call, or callable injection"
    - from: "approved Phase 11 AI evaluation contract"
      to: "Task 1 configured-adapter integration"
      via: "the blocking precondition resolves and reads `11-AI-SPEC.md`, or the exact approved equivalent path recorded in 11-VALIDATION.md, before any Task 1 edit or integration test"
  prohibitions:
    - requirement_id: "AUDIT-05"
      category: "transparency"
      status: "unresolved"
      verification: null
      statement: "MUST NOT imply approval, silently escalate autonomy, weaken validation at a higher mode, or hide the exact diff and machine-authored provenance."
    - requirement_id: "AUDIT-08"
      category: "safety"
      status: "unresolved"
      verification: null
      statement: "MUST NOT let undo overwrite later human work, revert unrelated Git changes, or conceal an interrupted subset of declared write units."
    - requirement_id: "AUDIT-09"
      category: "transparency"
      status: "unresolved"
      verification: null
      statement: "MUST NOT make high-volume autonomous generation appear as isolated approvals or omit rejected and retried volume from the run record."
---

<objective>
Close the authoring/auditing loop by binding the public Phase 8 model adapter through the real CLI composition root, then enforcing transactional Git-or-shadow writes and one public undo command.

Purpose: Integrate the two Wave 3 expansions into one configured, permissioned, transactional workflow while preserving identical citation/lint/quality rigor in every mode and making every artifact or bank mutation visible, recoverable, and concurrency-safe.
Output: Additive adapter author operation, real composition-root binding, durable writer/recovery/undo implementation, strict manifests, and subprocess roundtrip coverage.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-CONTEXT.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-AI-SPEC.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-RESEARCH.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-PATTERNS.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-VALIDATION.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-CONTEXT.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-AI-SPEC.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-RESEARCH.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-03-SUMMARY.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-04-SUMMARY.md
</context>

## Artifacts this plan produces

- Additive `author` support on Phase 8's strict `model_adapter.invoke(request, profile)` contract.
- Declarative `audit` command registration and configured adapter construction in `surfaces.cli.main`, the codebase's real composition root.
- A path-precise Git writer and content-addressed shadow writer behind one manifest/undo API.
- Explicit approval and undo handlers over the shared policy/writer boundary.
- Standalone stdlib integration tests proving a no-DI configured hosted-adapter CLI retry plus both writer backends, report-stage artifact undo, autonomous undo, stale refusal, recovery, idempotency, and concurrency.

<assumption_probes>
- AUDIT-05 unclassified review: use the existing stored enum `report_only`, `draft_and_approve`, `audit_draft_lint_fix_commit`; present the last value as user-facing `full`, require explicit opt-in, and never infer it from model output or a prior run.
- AUDIT-09 unclassified review: volume means proposed, completed/accepted, rejected, retried attempts, and written units, broken down by objective and total; show proposed counts before a full-mode model call and final counts on every exit.
- AUDIT-08 data-boundary review: exact current fingerprint equality permits shadow undo and any mismatch refuses; absent/null/malformed manifest values refuse; a valid singleton item/report unit is reversible; manifest/unit listings are stable sorted; an empty before-image is distinct from a missing before-image for a newly created report artifact.
- AUTH-01/AUTH-02 stateful review: a per-bank OS lock serializes final fingerprint/scope/write; a durable prepared run lists every unit before the first mutation; interruption reports completed units and resumes idempotently; concurrent losers return existing-id or stale-preflight and cannot bypass scope checks.
- AUTH-01/AUTH-03 integration review: the approved `11-AI-SPEC.md` now satisfies the design-contract half of the gate. Phase 8 still exists only as context/research in this checkout, so Task 1 remains blocked until Phase 8 artifacts/tests and the executable Phase 11 AI fixtures are green. No adapter/schema/surface edit or configured-adapter test may begin before then. Once both implementation gates pass, the subprocess test must configure the same fake hosted profile shape used by `tests/model_adapter_roundtrip.py` and may not pass a callable, monkeypatch `invoke`, or call `cmd_audit_author` directly.
- D-15 reversibility is the locked one-way public contract. Assumption-delta decision: no change; do not reopen Git-or-shadow identifiers/undo semantics and do not invent an alternate restore bypass.
</assumption_probes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind the configured Phase 8 adapter through the real CLI composition root</name>
  <precondition>Both upstream gates pass before any Task 1 work: (1) Phase 8 `model_adapter.py`, `schemas/model_adapter.schema.json`, and `tests/model_adapter_roundtrip.py` exist and `python tests/model_adapter_roundtrip.py` exits 0; and (2) `.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-AI-SPEC.md`, or the exact equivalent path explicitly recorded in `11-VALIDATION.md`, exists with approved status and explicitly covers author-operation scope, structured retries, prompt-injection/untrusted-output cases, volume caps, and model-unavailable behavior.</precondition>
  <files>model_adapter.py, schemas/model_adapter.schema.json, surfaces/cli.py, surfaces/audit_cli.py, tests/audit_cli_roundtrip.py</files>
  <read_first>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-UI-SPEC.md</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-VALIDATION.md</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-AI-SPEC.md</file>
    <file>model_adapter.py</file>
    <file>schemas/model_adapter.schema.json</file>
    <file>tests/model_adapter_roundtrip.py</file>
    <file>surfaces/cli.py</file>
    <file>surfaces/audit_cli.py</file>
    <file>surfaces/settings.py</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-PATTERNS.md</file>
  </read_first>
  <behavior>
    - The Phase 8 adapter's strict request operation set additively accepts `author` while existing hint/rubric operations and both hosted/local transports keep the same public `invoke(request, profile)` function.
    - `surfaces.cli.main` registers `audit source|material|author|undo`, resolves `load_settings(base)["model_backend"]`, and supplies a closure over `model_adapter.invoke(request, profile)` only for the author handler.
    - A subprocess invocation of `python itembank.py audit author ...` with a configured fake hosted adapter sends the public contract and bounded request, receives a malformed first result, sends versioned structured findings on retry, then writes exactly one clean unit after every gate.
    - The subprocess test supplies configuration/files/arguments only: it does not inject a callable, monkeypatch imports, or call the command handler in-process.
    - Typed adapter unavailable/refusal/exhaustion produces a retained report and zero bank writes without exposing repository paths or private modules in the adapter request.
    - The public command renders report-only, exact-write-id approval, and explicit full-autonomy outcomes from Plan 11-04's shared domain policy without duplicating validation or permission logic in the surface.
  </behavior>
  <action>Fail closed on the precondition before editing `model_adapter.py`, its schema, either CLI surface, or the configured-adapter tests: read the approved canonical `11-AI-SPEC.md`, or the exact approved equivalent contract path recorded in `11-VALIDATION.md`, and stop with no Task 1 changes if either upstream gate is absent, unapproved, incomplete, or failing. After both gates pass, apply `11-PATTERNS.md`'s exact `surfaces/cli.py` composition-root and thin `surfaces/audit_cli.py` controller patterns: the latter converts arguments/approval and renders deterministic UTF-8 JSON while calling Plan 11-04's domain policy, never reimplementing source, scope, lint, quality, volume, or writer rules. Extend Phase 8's public adapter contract additively with operation `author`: keep the single `model_adapter.invoke(request, profile)` entry point and existing hosted/local transport normalization, and add strict author request/response variants to `schemas/model_adapter.schema.json` without weakening the existing hint/rubric branches. Implement and preserve the approved AI evaluation contract's author-operation allowlist, structured retry/error contract, adversarial prompt-injection and untrusted-output cases, pre/post volume caps, and typed model-unavailable retention/no-write behavior. The author request carries a deterministic interaction/run attempt id plus only `model.SPEC`, the bounded authoring request, attempt number, and sorted structured findings per D-05/D-06; provider output remains untrusted and is passed into Phase 11 response-scope/lint/quality validation rather than accepted by the adapter. In `surfaces/cli.py::main`, import/register the audit command family declaratively, parse an explicit settings base (default current working directory), resolve `profile = load_settings(base)["model_backend"]`, and bind the author handler to a closure that calls `model_adapter.invoke(request, profile)`. Do not create a Phase 11 subprocess/HTTP transport or choose a backend in `authoring.py`/`audit_cli.py`. Build `tests/audit_cli_roundtrip.py --case configured-adapter` as a real subprocess test using the same configured fake hosted executable/profile fixture shape as Phase 8's adapter suite: the fake records its two strict requests, returns one malformed/out-of-scope draft and then one clean draft, and the assertions prove structured retry, no repository context, both gates, exactly one permitted write, and AUTH-01/AUTH-03 completion without any test-supplied callable. Add adversarial prompt-injection/untrusted-output and typed unavailable cases with no write, assert configured caps before/after the model boundary, and make the existing `--case modes` subprocess coverage prove report-only, exact approval, and explicit full/capped volume behavior through the registered command.</action>
  <verify>
    <automated>python tests/model_adapter_roundtrip.py; if ($LASTEXITCODE -eq 0) { python tests/audit_cli_roundtrip.py --case configured-adapter }; if ($LASTEXITCODE -eq 0) { python tests/audit_cli_roundtrip.py --case modes }</automated>
  </verify>
  <acceptance_criteria>The Phase 11 AI evaluation contract is read during the read-only precondition check and no implementation begins until both upstream gates pass; the Phase 8 suite remains green; the public subprocess author command resolves configured hosted adapter state in `surfaces.cli.main`, retries from structured failures, completes one clean gated write with no injected callable, passes no repository context, enforces caps, and writes nothing for prompt-injected/untrusted output or an unavailable model.</acceptance_criteria>
  <done>Both blocking preconditions are recorded as satisfied and all focused commands exit 0; the no-DI subprocess proves AUTH-01/AUTH-03 through the actual `surfaces.cli.main` to `model_adapter.invoke(request, profile)` path, and the approved contract's adversarial, unavailable-model, permission, retry, and volume behaviors pass.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm execution of the locked D-15 Git/shadow contract</name>
  <decision>Confirm whether execution may implement the already-locked D-15 contract exactly: tracked-clean targets use Git, all other targets use content-addressed shadow state, deterministic write IDs address strict manifests, and one public undo operation routes restoration.</decision>
  <context>This checkpoint confirms execution consent and does not reopen the selected design. Proceeding publishes the write-ID/manifest/backend/undo contract that later audits and runs consume; changing it afterward requires manifest and audit-record migration plus coordinated rollback handling. Aborting stops before the writer implementation and performs no contract or bank mutation.</context>
  <options>
    <option id="proceed">
      <name>Proceed with locked contract</name>
      <pros>Implements D-15/D-16/D-17 exactly and preserves the agreed one-step undo and recovery semantics.</pros>
      <cons>Later contract changes require migration of persisted manifests/write IDs and coordinated rollback compatibility.</cons>
    </option>
    <option id="abort">
      <name>Abort before implementation</name>
      <pros>Leaves the one-way persisted contract and all banks untouched.</pros>
      <cons>Stops Phase 11 before transactional writes and one-step undo can be delivered; source decisions must be revised outside this execution.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed or abort. `proceed` authorizes only the locked contract above; `abort` ends Plan 11-05 before Task 3.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement atomic Git-or-shadow writes, recovery, and guarded undo</name>
  <files>audit_writer.py, schemas/write_manifest.schema.json, tests/audit_writer_roundtrip.py, fixtures/audit/writer_bank.md, surfaces/audit_cli.py</files>
  <read_first>
    <file>audit_writer.py</file>
    <file>schemas/write_manifest.schema.json</file>
    <file>surfaces/audit_cli.py</file>
    <file>runtime.py</file>
    <file>surfaces/evidence_cli.py</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-PATTERNS.md</file>
  </read_first>
  <behavior>
    - A bank path inside a Git worktree uses Git only when that exact path is tracked and has no staged or unstaged change; every accepted item/additive edit plus its manifest is one machine-authored commit.
    - Any other target creates and durably records a content-addressed before-image plus prepared manifest before temp-file atomic replacement.
    - One `undo(write_id)` routes Git manifests to documented `git revert` and shadow manifests to after-fingerprint-guarded atomic restore; later edits/conflicts refuse without override.
    - Duplicate writes, injected interruption at each durable state, two-process contention, null/malformed manifests, empty-before report artifacts, singleton units, and reordered unit lists have deterministic outcomes.
  </behavior>
  <action>Apply `11-PATTERNS.md`'s all-or-nothing preflight and UTF-8 tmp-plus-`os.replace` patterns, then finish `audit_writer.py` per D-15/D-16/D-17. Resolve the absolute target, acquire a per-bank cross-platform stdlib OS lock (`msvcrt` on Windows and `fcntl` where available) around current-fingerprint revalidation and mutation, and persist a prepared run manifest listing sorted declared units before any write. Detect the exact target's Git root, tracked status, and staged/unstaged cleanliness immediately under the lock. For tracked+clean, stage only the one accepted item change and its manifest, create one machine-authored commit per item with write/request/source metadata, and record the commit hash; use a recoverable prepared journal so interruption before commit cannot leave an unreported dirty bank. For every other case, durably write the SHA-256-addressed before-image and manifest, flush/fsync as supported, write same-directory temp bytes, and call `os.replace`; record after fingerprint and completed state. Represent a newly created derived report with an explicit `before_exists: false`, not a missing fingerprint. Use deterministic write ids so duplicates return the existing result. On multi-item interruption, retain sorted planned/completed unit IDs and resume remaining clean units after fresh full-bank/scope preflight; never label a prefix as a complete batch. Implement the sole public `undo(write_id)`: Git uses non-interactive `git revert` and refuses conflicts/dirty target; shadow verifies exact after-fingerprint then restores/removes atomically; no force/restore-anyway path exists. Exercise all paths only in temporary directories, initializing a temporary Git repository for the Git cases.</action>
  <reversibility rating="one-way">D-15 fixes public write IDs, manifest schema, backend choice, and one-step undo semantics; changing them later requires manifest/audit migration. This plan implements the locked no-change decision.</reversibility>
  <verify>
    <automated>python tests/audit_writer_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>Tracked-clean and shadow tests each show a diff, apply exactly one unit, create a strict manifest, and undo in one call; dirty Git chooses shadow; stale shadow and Git conflict refuse; duplicate/interruption/parallel tests produce no duplicate or hidden mutation.</acceptance_criteria>
  <done>`python tests/audit_writer_roundtrip.py` exits 0 after both backends apply and undo one declared unit, unsafe undo states refuse, and duplicate/interruption/parallel cases leave no duplicate or hidden mutation.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Any unresolved critical or high threat blocks plan completion; full autonomy remains disabled until all mitigations pass.

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-11-16 | Elevation of privilege | Autonomy policy | high | mitigate | Existing setting plus explicit full opt-in; one gate pipeline; exact write-id approval in approval mode; model cannot select mode. |
| T-11-17 | Tampering | Undo/current human work | critical | mitigate | Shadow after-fingerprint equality; Git dirty/conflict refusal; no force override; blocking stale/conflict tests. |
| T-11-18 | Tampering | Git backend selection/staging | high | mitigate | Detect exact worktree/tracked/staged/unstaged status under lock and stage only target+manifest; otherwise shadow. |
| T-11-19 | Repudiation | Full-autonomy volume/interruption | high | mitigate | Pre-work proposed counts, all-exit final counts, prepared unit list, completed IDs, deterministic write/run IDs, machine-authored commit metadata. |
| T-11-20 | Denial of service | Parallel writer/lock | medium | mitigate | Bounded lock timeout, owner metadata, explicit busy result, and recovery of durable prepared states without unsafe stale-lock deletion. |
| T-11-25 | Information disclosure | Configured adapter request | high | mitigate | Strict author request allowlist plus external fake-process capture asserts no repository paths, credentials, unrelated source, or private modules cross `model_adapter.invoke`. |
| T-11-26 | Spoofing | CLI adapter binding | high | mitigate | Public subprocess test proves settings-selected profile and real `surfaces.cli.main` composition; no caller-supplied callable or provider-native parsing in the surface. |
</threat_model>

<verification>
- Before Task 1, read-only checks prove both upstream gates: Phase 8 artifacts exist with a green adapter suite, and the canonical `11-AI-SPEC.md` or exact approved equivalent recorded in `11-VALIDATION.md` is approved and covers author-operation scope, structured retries, prompt-injection/untrusted-output cases, volume caps, and model-unavailable behavior; otherwise Task 1 stops without edits.
- `python tests/model_adapter_roundtrip.py`, `python tests/audit_cli_roundtrip.py --case configured-adapter`, `python tests/audit_cli_roundtrip.py --case modes`, and `python tests/audit_writer_roundtrip.py` pass.
- The configured-adapter case starts `python itembank.py audit author` as a child process and supplies no injectable callable or import monkeypatch.
- The D-15 checkpoint resolves to `proceed` before the one-way writer task begins; `abort` leaves the contract and banks untouched.
- A report-only-stage derived artifact and a full-autonomy bank item are each addressed by the same undo API; stale human edits are never overwritten.
- Git/shadow selection, manifest state, duplicate/interruption handling, and volume accounting remain deterministic under reordered and parallel cases.
</verification>

<success_criteria>
- Configured-adapter integration starts only after both the green Phase 8 adapter gate and approved Phase 11 AI evaluation contract gate are satisfied and read.
- The same gates/citations/diff/audit evidence run in every autonomy mode; only final permission changes.
- The real public command selects the configured Phase 8 profile inside `surfaces.cli.main` and reaches `model_adapter.invoke(request, profile)` with no dependency injection.
- Tracked-clean and shadow writes are individually atomic, visible, recoverable, idempotent, parallel-safe, and one-step reversible.
- Full autonomy is explicit, capped, and transparent about proposed/completed/rejected/retried/written volume.
- D-15/D-16/D-17 are implemented only after explicit proceed/abort confirmation of the locked migration-bearing contract.
- No frontend, daemon route, package, ORM schema push, or PDF/DOCX adapter is introduced.
</success_criteria>

<output>
Create `.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-05-SUMMARY.md` when done.
</output>
