---
phase: 11-closed-authoring-loop-curriculum-auditor
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - schemas/authoring_request.schema.json
  - schemas/normalized_document.schema.json
  - schemas/audit_report.schema.json
  - schemas/quality_finding.schema.json
  - schemas/write_manifest.schema.json
  - tests/audit_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUDIT-06
  - AUDIT-07
  - AUDIT-08
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every Phase 11 public payload is schema-versioned, strict, UTF-8 JSON validated with the repository validator."
    - "Unknown fields and missing required citation, scope, finding, provenance, or write data fail before they can cross the command or writer boundary."
    - "The Plan 11-01 tracer payloads validate against the published schemas before any command or writer boundary accepts them."
    - "The repository-blind spy author receives only the public format contract, bounded request, attempt number, and structured findings."
  artifacts:
    - path: "schemas/authoring_request.schema.json"
      provides: "Strict bounded-request contract"
    - path: "schemas/normalized_document.schema.json"
      provides: "Strict normalized source and citation contract"
    - path: "schemas/audit_report.schema.json"
      provides: "Strict coverage, gap, and volume report contract"
    - path: "schemas/quality_finding.schema.json"
      provides: "Strict named-detector finding contract"
    - path: "schemas/write_manifest.schema.json"
      provides: "Strict write provenance, recovery, and undo contract"
    - path: "tests/audit_roundtrip.py"
      provides: "Standalone strict-schema and runtime-version parity suite"
  key_links:
    - from: "tests/audit_roundtrip.py"
      to: "schemas/*.schema.json"
      via: "runtime-version parity plus accepted/rejected payload validation through schema_validate.py"
    - from: "tests/audit_roundtrip.py"
      to: "authoring.py/auditor.py/audit_writer.py"
      via: "runtime payload examples are validated through schema_validate.py and compared to each schema version"
  prohibitions:
    - requirement_id: "AUTH-02"
      category: "safety"
      status: "unresolved"
      verification: null
      statement: "MUST NOT accept an extra, omitted, or malformed scope/citation field merely because the remaining payload is lint-clean."
    - requirement_id: "AUTH-03"
      category: "privacy"
      status: "unresolved"
      verification: null
      statement: "MUST NOT expose repository contents, credentials, filesystem discovery, or fixture paths to the repository-blind author."
---

<objective>
Publish the tracer's strict public contracts without widening the provider-neutral author boundary.

Purpose: Turn the production tracer proven in Plan 11-01 into a version-checked Wave 0 contract that later audit, repair, writer, and CLI expansions consume deterministically.
Output: Five strict JSON Schemas and focused accepted/rejected runtime-parity tests.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-CONTEXT.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-AI-SPEC.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-RESEARCH.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-PATTERNS.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-VALIDATION.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-01-SUMMARY.md
</context>

## Artifacts this plan produces

- Five strict versioned JSON Schema documents for request, normalized document, report, quality finding, and write manifest payloads.
- A standalone schema/runtime-parity suite that remains safe against real banks.

<assumption_probes>
- AUTH-02 data-boundary review: schema acceptance never substitutes for runtime scope validation; both are required and negative examples prove strict object shape.
- AUTH-03 unclassified review: schema publication does not widen the Plan 11-01 provider-neutral callable payload; its allowlist spy remains authoritative until Plan 11-05 binds the configured Phase 8 adapter.
- D-15 contract review: the write manifest schema publishes the locked Git/shadow backend tag, deterministic write id, prepared/applied state, and one-step undo provenance without changing the writer choice.
- Assumption-delta decision: no change. JSON Schema publication does not add an ORM schema, transport, PDF/DOCX adapter, or identity mechanism.
</assumption_probes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish strict request, source, and quality contracts</name>
  <files>schemas/authoring_request.schema.json, schemas/normalized_document.schema.json, schemas/quality_finding.schema.json, tests/audit_roundtrip.py</files>
  <read_first>
    <file>schemas/lint_error.schema.json</file>
    <file>schemas/report.schema.json</file>
    <file>schemas/update_manifest.schema.json</file>
    <file>schema_validate.py</file>
    <file>tests/audit_roundtrip.py</file>
  </read_first>
  <behavior>
    - Every runtime payload declares `schema_version` matching the schema's `x-itembank-version`.
    - Unknown fields and missing required provenance/citation/scope/finding/write fields fail validation.
    - Only the keyword subset supported by `schema_validate.py` appears in the documents.
  </behavior>
  <action>Create the request, normalized-document, and quality-finding JSON Schemas with `$id`, `x-itembank-version`, `type: object`, `additionalProperties: false`, explicit `required`, bounded integer/config fields, and descriptions. Model D-01/D-02 source/citation data, D-07 request bounds, and D-09/D-10 named measured quality evidence explicitly. Require source/request/bank fingerprints where applicable and stable arrays for citations/findings. Extend the Wave 0 schema case with accepted and rejected examples plus runtime/schema version parity. These are repository JSON Schema assets only: do not run or plan an ORM migration/schema push, add a package, or register PDF/DOCX adapters.</action>
  <verify>
    <automated>python tests/audit_roundtrip.py --case schemas</automated>
  </verify>
  <acceptance_criteria>The three schemas validate their corresponding tracer payloads, reject an extra field and a missing required field, and are themselves accepted by the local schema validator without unsupported keywords.</acceptance_criteria>
  <done>`python tests/audit_roundtrip.py --case schemas` exits 0 after the request, normalized-document, and quality-finding schemas accept runtime payloads and reject both an extra property and an omitted required property.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish strict report and write-manifest contracts</name>
  <files>schemas/audit_report.schema.json, schemas/write_manifest.schema.json, tests/audit_roundtrip.py</files>
  <read_first>
    <file>schemas/report.schema.json</file>
    <file>schemas/update_manifest.schema.json</file>
    <file>schema_validate.py</file>
    <file>audit_writer.py</file>
    <file>tests/audit_roundtrip.py</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Audit reports require cited coverage rows, input fingerprints, reproducible diffs, autonomy/volume records, and explicit stale state.
    - Write manifests require deterministic write id, backend, prepared/applied state, declared units, before/after fingerprints, request/source/tool provenance, and Git commit data when applicable.
    - Missing or extra report/manifest fields fail through the same local validator used by all public contracts.
  </behavior>
  <action>Create the audit-report and write-manifest schemas using the same strict supported-keyword pattern mapped in `11-PATTERNS.md`. Encode D-02/D-04 covered-versus-unknown and staleness fields, D-13 volume/caps, and D-15/D-16/D-17 backend, durable state, unit, fingerprint, diff, and provenance fields without allowing caller-selected restore bypasses. Extend the same focused schema test so valid tracer report/manifest records pass, missing/extra data fails, reordered stable arrays serialize equivalently, and runtime versions equal `x-itembank-version`. Keep public CLI registration and configured model binding in the final integration Plan 11-05, where the real composition root and Phase 8 API can be exercised end to end.</action>
  <verify>
    <automated>python tests/audit_roundtrip.py --case schemas</automated>
  </verify>
  <acceptance_criteria>The report and manifest schemas validate complete runtime records, reject missing and extra data, preserve stable serialization, and expose all citation, volume, reversibility, and provenance fields required by D-02/D-04/D-13/D-15/D-16/D-17.</acceptance_criteria>
  <done>`python tests/audit_roundtrip.py --case schemas` exits 0 after all five Phase 11 schemas pass runtime-version parity and accepted/rejected contract cases.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Any unresolved critical or high threat blocks task acceptance and later command/writer expansion.

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-11-22 | Tampering | Public schema/runtime parity | high | mitigate | Compare every runtime `schema_version` to `x-itembank-version`; reject unknown and missing required fields with accepted/rejected fixtures. |
| T-11-23 | Information disclosure | Public request/source contracts | high | mitigate | Require exact bounded request and cited spans; retain the Plan 11-01 allowlist spy and reject repository paths, credentials, or unrelated source. |
| T-11-24 | Elevation of privilege | Manifest/report contract | medium | mitigate | Require autonomy, declared units, permission outcome, and provenance; do not expose a caller-selected backend or unsafe undo override. |
</threat_model>

<verification>
- `python tests/audit_roundtrip.py --case schemas` proves strict accepted/rejected schema behavior and version parity.
- `python tests/audit_roundtrip.py --case tracer` remains green so schema publication does not widen the repository-blind callable payload.
- No package, ORM migration, PDF/DOCX adapter, daemon route, frontend asset, or live model is introduced.
</verification>

<success_criteria>
- All five public payload families are strict, schema-versioned, machine-readable, and validated with the existing stdlib validator.
- The Wave 0 tracer and all five public schemas share exact runtime versions and strict accepted/rejected payload behavior.
- Later plans can consume stable contracts without reinterpreting D-01/D-02, D-07, D-09/D-10, D-13, or D-15/D-17.
</success_criteria>

<output>
Create `.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-02-SUMMARY.md` when done.
</output>
