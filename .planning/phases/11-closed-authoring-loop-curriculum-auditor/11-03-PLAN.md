---
phase: 11-closed-authoring-loop-curriculum-auditor
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - auditor.py
  - surfaces/audit_cli.py
  - schemas/normalized_document.schema.json
  - schemas/audit_report.schema.json
  - tests/audit_coverage_roundtrip.py
  - fixtures/audit/syllabus.md
  - fixtures/audit/syllabus.txt
  - fixtures/audit/coverage_bank.md
  - fixtures/audit/locator_fidelity_cases.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "AUDIT-01: UTF-8 Markdown and text sources become versioned normalized documents that retain source id, SHA-256, heading/line locator, exact character span, verbatim text, and objective candidates including body-level subtleties."
    - "AUDIT-02/AUDIT-03: every objective has a deterministic covered, partial, conflicting, gap, or unknown result; covered requires both exact source-span citations and exact stable bank item IDs."
    - "AUDIT-03: absent, null, or inadequate citations always produce unknown; confidence cannot upgrade that state."
    - "AUDIT-04: candidate supporting materials remain report data until obtained material is supplied in a separate explicit authoring request."
    - "Original syllabus/text sources and bank inputs are never modified, and report staleness is detectable from source and bank fingerprints."
    - "PDF/DOCX are reported as explicit unsupported/lossy results with no spans or fabricated locator until every required structure either round-trips exactly or fails explicitly under the fixture matrix and cross-platform locator proof in 11-AI-SPEC.md; AI extraction confidence cannot waive this gate."
  artifacts:
    - path: "auditor.py"
      provides: "Markdown/text adapters, objective extraction, citation-first coverage states, and material-to-request handoff"
    - path: "schemas/normalized_document.schema.json"
      provides: "Strict stable-locator extraction contract"
    - path: "schemas/audit_report.schema.json"
      provides: "Strict cited coverage/gap/material report contract"
    - path: "tests/audit_coverage_roundtrip.py"
      provides: "Standalone fidelity, state-boundary, and read-only audit suite"
    - path: "fixtures/audit/syllabus.md"
      provides: "Synthetic Markdown source with heading and body-level objectives"
    - path: "fixtures/audit/locator_fidelity_cases.py"
      provides: "Deterministic redistributable synthetic PDF/DOCX byte fixtures plus gold structure, locator, Unicode span, reading-order, fingerprint, exact-round-trip, and explicit unsupported/lossy expectations required by 11-AI-SPEC.md"
  key_links:
    - from: "auditor.normalize_source"
      to: "schemas/normalized_document.schema.json"
      via: "schema-versioned locator/span/fingerprint record"
    - from: "auditor.coverage_report"
      to: "model.parse_bank"
      via: "stable item IDs and objective fields from the exact fingerprinted bank"
    - from: "auditor.coverage_report"
      to: "schemas/audit_report.schema.json"
      via: "sorted objective rows with source-span and bank-item citation arrays"
    - from: "auditor.material_request"
      to: "schemas/authoring_request.schema.json"
      via: "new explicit request object only after obtained material is supplied; Plan 11-05's public author command consumes this same contract"
  prohibitions:
    - requirement_id: "AUDIT-03"
      category: "transparency"
      status: "unresolved"
      verification: null
      statement: "MUST NOT present inferred, confidence-weighted, stale, or uncited curriculum coverage as covered."
    - requirement_id: "AUDIT-04"
      category: "safety"
      status: "unresolved"
      verification: null
      statement: "MUST NOT convert a detected gap, a weak-objective signal, or a suggested material pointer into authorization to generate or write curriculum."
---

<objective>
Expand the tracer into a reproducible, citation-first curriculum audit for first-class UTF-8 Markdown and text sources.

Purpose: Produce objective-by-objective coverage and gap results that preserve uncertainty, detect stale inputs, and keep material discovery separate from authoring authority per D-01 through D-04 and D-18/D-19.
Output: Locator-faithful source adapters, coverage/report logic, a separate obtained-material handoff, and isolated roundtrip fixtures/tests.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-CONTEXT.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-AI-SPEC.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-RESEARCH.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-PATTERNS.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-01-SUMMARY.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-02-SUMMARY.md
</context>

## Artifacts this phase produces

- First-class Markdown and UTF-8 text normalization with stable locators and fingerprints.
- Citation-first audit reports with explicit covered/partial/conflicting/gap/unknown states.
- A candidate-material section and a separate obtained-material-to-authoring-request handoff.
- Synthetic Markdown/text fidelity fixtures, the separate required PDF/DOCX architectural-gate matrix, and one standalone coverage roundtrip script.

<assumption_probes>
- AUDIT-01 unclassified review: test a heading objective, a bullet objective, and a body-paragraph subtle objective; no assumption that headings alone are sufficient.
- AUDIT-02 unclassified review: use exact normalized objective keys and stable bank objective/item IDs as deterministic evidence; semantic similarity may propose review evidence but cannot itself establish covered.
- AUDIT-04 unclassified review: a material pointer is inert report data; only bytes supplied later with a new source fingerprint/citation can seed a separate request.
- AUDIT-03 data-boundary review: exact equality of cited source id/fingerprint/span and bank fingerprint/item ID is required; empty and null citation sides are unknown; a valid singleton on each side can be covered; citation arrays and objective rows have stable deterministic ordering.
</assumption_probes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize Markdown and text without losing objective subtleties</name>
  <files>auditor.py, tests/audit_coverage_roundtrip.py, fixtures/audit/syllabus.md, fixtures/audit/syllabus.txt, fixtures/audit/locator_fidelity_cases.py</files>
  <read_first>
    <file>auditor.py</file>
    <file>schemas/normalized_document.schema.json</file>
    <file>model.py</file>
    <file>tests/audit_roundtrip.py</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Markdown and text bytes decode strictly as UTF-8; malformed encoding returns a versioned error and no derived artifact.
    - Reconstructing every span from recorded offsets yields byte-for-byte-equivalent Unicode text and stable heading/line locators.
    - Objective candidates include explicit headings, list entries, and fixture body prose that states a detailed learning outcome.
    - The source file's bytes and timestamp remain unchanged after normalization.
    - Every required PDF/DOCX fixture has a gold manifest, and the current unregistered adapters return the same explicit unsupported/lossy result on repeated runs with no spans, no locator, and unchanged source bytes.
  </behavior>
  <action>Extend the Plan 01 source primitives into an adapter registry containing only `.md`/Markdown and `.txt`/UTF-8 text per D-03. Read original bytes once, decode with strict UTF-8, calculate SHA-256 over the original bytes, preserve a caller-supplied stable source id, and emit ordered spans with heading path when present, start/end line, start/end character offset, and exact verbatim text per D-01. Extract candidate objectives from headings, bullets/numbered lines, and declarative body sentences in the synthetic fixture so AUDIT-01 does not collapse to heading scraping; retain each candidate's exact originating span rather than only normalized prose. Keep normalization as a derived, versioned record and never write the source per D-04. Create `fixtures/audit/locator_fidelity_cases.py` as the checked-in deterministic, redistributable architectural-gate corpus required by `11-AI-SPEC.md`: it materializes exact synthetic bytes into the test temporary directory and publishes the gold expectations for PDFs covering born-digital single column, reordered two-column text, table, footnote, repeated header/footer, rotated page, scanned/image-only, encrypted/unsupported, and malformed/truncated input, plus DOCX packages covering headings/lists, nested table, footnote/endnote, header/footer, tracked changes/comments, text box/drawing, embedded object, and malformed package. Keep the fixture module stdlib-only and repository-independent; its immutable case table includes expected page/section/paragraph/table-cell locator, exact Unicode text/span, reading order, intentionally unsupported structures, and original-byte SHA-256 for every materialized file. Do not implement PDF/DOCX adapters in this task: invoke the registry twice for every fixture and assert an identical typed unsupported/lossy result, no derived span or locator, no AI call, exact fingerprint, and unchanged bytes/timestamp. Future adapters may change an individual expectation only by proving exact locator round-trip for every structure they claim to support; an unsupported structure must continue to fail explicitly.</action>
  <verify>
    <automated>python tests/audit_coverage_roundtrip.py --case normalize</automated>
  </verify>
  <acceptance_criteria>Both first-class formats normalize deterministically; all expected heading/list/body objectives retain exact locators and verbatim spans; invalid UTF-8 fails explicitly; the complete PDF/DOCX gold matrix exists; repeated unsupported/lossy results are byte-stable, locator-free, and source bytes/timestamps remain unchanged.</acceptance_criteria>
  <done>`python tests/audit_coverage_roundtrip.py --case normalize` exits 0 with deterministic Markdown/text normalization, exact heading/list/body spans, explicit invalid-UTF-8 failure, complete PDF/DOCX gold fixtures, repeated explicit unsupported/lossy outcomes with no fabricated locators, and unchanged source bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Calculate cited objective coverage with unknown as the fail-closed state</name>
  <files>auditor.py, schemas/audit_report.schema.json, surfaces/audit_cli.py, tests/audit_coverage_roundtrip.py, fixtures/audit/coverage_bank.md</files>
  <read_first>
    <file>auditor.py</file>
    <file>schemas/audit_report.schema.json</file>
    <file>model.py</file>
    <file>surfaces/audit_cli.py</file>
    <file>fixtures/audit/syllabus.md</file>
  </read_first>
  <behavior>
    - Covered is reachable only with an exact current source citation and at least one exact current bank item ID citation.
    - Missing either citation side, stale fingerprints, or unresolvable IDs yields unknown regardless of supplied confidence.
    - Partial and conflicting evidence remain distinct; an objective with valid source evidence and no covering bank item is a gap.
    - Equality, empty, null, singleton, and reordered-input cases yield the specified state and stable sorted JSON output.
  </behavior>
  <action>Implement the AUDIT-02/AUDIT-03 coverage engine as a deterministic pure transform over a normalized document, the exact parsed bank, and optional evidence assertions. Canonical objective-key equality and bank objective fields may identify candidate rows, but a covered result requires both source id/fingerprint/exact span equality and bank fingerprint/existing stable item IDs per D-01/D-02. Make `unknown` the outcome for absent/null/invalid/stale citations, not a numeric-confidence fallback. Represent partial and conflicting explicitly and represent a source-backed objective with no covering items as a gap. Sort objective rows by source locator and objective key, and sort/deduplicate citation arrays by exact identifiers so input order does not affect output. Include source and bank fingerprints plus tool/schema version so D-04 staleness can be recomputed. Keep the surface adapter limited to argument conversion and UTF-8 JSON rendering.</action>
  <verify>
    <automated>python tests/audit_coverage_roundtrip.py --case coverage</automated>
  </verify>
  <acceptance_criteria>The suite proves every coverage state, exact dual-citation requirements, confidence non-escalation, stale-report detection, equality/empty/null/singleton boundaries, and byte-stable ordering across reordered inputs.</acceptance_criteria>
  <done>`python tests/audit_coverage_roundtrip.py --case coverage` exits 0 and proves every coverage state, fail-closed dual citations, stale-fingerprint handling, boundary cases, and byte-stable ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep gap materials and weak-objective priority separate from authoring authority</name>
  <files>auditor.py, schemas/audit_report.schema.json, surfaces/audit_cli.py, tests/audit_coverage_roundtrip.py</files>
  <read_first>
    <file>auditor.py</file>
    <file>schemas/audit_report.schema.json</file>
    <file>schemas/authoring_request.schema.json</file>
    <file>surfaces/audit_cli.py</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Gap rows may list candidate material references but never include a generated draft or writer authorization.
    - Obtained material creates a fresh normalized source and a separate bounded authoring request preserving exact citations.
    - Phase 10 weak-objective inputs only reorder already-extracted objective IDs and cannot create, rename, or cover an objective.
  </behavior>
  <action>Implement the explicit AUDIT-04 bridge per D-18: reports may carry inert candidate material references; accepting bytes for obtained material is a separate operation that normalizes the new source and returns a new strict authoring request with objectives/count/types/citations, but does not call the author or writer. Accept optional weak-objective records only as a stable priority ordering over objective IDs already present in the normalized source per D-19; unknown IDs are reported and ignored, never added to curriculum. Preserve D-17 provenance in the handoff. Add assertions that audit-only and gap-discovery paths cannot reach any author/writer spy.</action>
  <verify>
    <automated>python tests/audit_coverage_roundtrip.py --case material</automated>
  </verify>
  <acceptance_criteria>A gap can point to material, supplied material can produce a separate cited request, and neither a gap nor a weak-objective signal can generate, write, create, or mark an objective covered.</acceptance_criteria>
  <done>`python tests/audit_coverage_roundtrip.py --case material` exits 0 while candidate materials remain inert, supplied material creates only a separate cited request, and author/writer spies record zero calls.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Any unresolved critical or high threat blocks task acceptance and downstream authoring.

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-11-06 | Tampering | Source/report freshness | high | mitigate | SHA-256 original bytes and exact bank fingerprint; recompute and mark stale before reuse. |
| T-11-07 | Spoofing | Covered claim | high | mitigate | Require exact current source-span and bank-item citations; absent/invalid/stale evidence yields unknown. |
| T-11-08 | Elevation of privilege | Gap/material/weak-objective flow | high | mitigate | Pure reports cannot invoke author/writer; obtained material requires a new bounded request; weak signals only reorder existing IDs. |
| T-11-09 | Information disclosure | Source spans | medium | mitigate | Include only cited spans needed by the explicit report/request and never forward whole unrelated source files to the author. |
| T-11-10 | Denial of service | Source ingestion | medium | mitigate | Schema-bounded source size/objective count and strict registered-adapter dispatch. |
| T-11-27 | Spoofing | PDF/DOCX locator fidelity | critical | mitigate | Full gold structural matrix, original-byte SHA-256, repeated exact-or-explicit-failure checks, zero spans/locators on unsupported/lossy input, and no AI/confidence waiver. |
</threat_model>

<verification>
- `python tests/audit_coverage_roundtrip.py` passes without packages and without mutating committed fixtures.
- Reports are deterministic under input reordering and become stale when either exact fingerprint changes.
- Every required PDF/DOCX architectural fixture has gold locator/span/reading-order/fingerprint expectations, and each unsupported structure deterministically fails explicitly without a fabricated locator or changed source byte.
- Empty/null citations never become covered; a valid singleton source citation plus singleton bank ID can.
- The test author/writer spies remain untouched during normalization, audit, candidate-material, and weak-objective operations.
</verification>

<success_criteria>
- AUDIT-01 through AUDIT-04 are covered with Markdown/text as the only supported formats; PDF/DOCX remain an explicit, fully fixture-backed architectural gate rather than a support claim.
- Original inputs are read-only and every derived artifact is reproducible and fingerprinted.
- Coverage uncertainty is explicit and uncitable is always unknown.
- The material handoff preserves citations while remaining a distinct explicit authoring request.
</success_criteria>

<output>
Create `.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-03-SUMMARY.md` when done.
</output>
