---
phase: 11-closed-authoring-loop-curriculum-auditor
plan: 04
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - authoring.py
  - schemas/authoring_request.schema.json
  - schemas/quality_finding.schema.json
  - tests/audit_quality_roundtrip.py
  - tests/audit_authoring_roundtrip.py
  - fixtures/audit/quality_cases.json
  - fixtures/audit/retry_drafts.json
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUDIT-05
  - AUDIT-06
  - AUDIT-07
  - AUDIT-09
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "AUDIT-07: exactly four independently named deterministic detectors run after lint with the Research thresholds and return versioned measured evidence, never an opaque aggregate score."
    - "AUDIT-06: lint errors and configured blocking warnings are structured retry findings; no candidate bank or prefix reaches a writer unless the full set is lint-clean."
    - "AUTH-01: retries stop at the configured cap, retain the full failed draft/report, and duplicate/resumed runs use deterministic request/write identities rather than duplicating work."
    - "AUTH-02: scope validation precedes lint on every attempt and is rechecked on the complete candidate immediately before the serialized writer boundary."
    - "AUTH-03: the callable contract remains provider-neutral, repository-blind, and sufficient to repair from structured findings alone."
    - "AUDIT-05/AUDIT-09: report_only, draft_and_approve, and explicit full autonomy share the same validated proposal and differ only at permission; full reports bounded proposed and final volumes on every exit."
  artifacts:
    - path: "authoring.py"
      provides: "Complete deterministic quality profile and bounded retry/preflight state machine"
    - path: "schemas/quality_finding.schema.json"
      provides: "Strict finding codes, detector versions, measured evidence, severity, item refs, and remediation"
    - path: "tests/audit_quality_roundtrip.py"
      provides: "Exact detector threshold and ordering suite"
    - path: "tests/audit_authoring_roundtrip.py"
      provides: "Scope/retry/idempotency/interruption/parallel contract suite with fake writer"
  key_links:
    - from: "authoring.quality_gate"
      to: "schemas/quality_finding.schema.json"
      via: "one record per contributing detector with no combined score"
    - from: "authoring.run_authoring"
      to: "model.lint"
      via: "existing lint findings preserved as structured retry input before quality gate"
    - from: "authoring.run_authoring"
      to: "authoring.preflight_candidate"
      via: "every clean response is recomposed against the full current bank before writer handoff"
    - from: "authoring.run_authoring"
      to: "injected author_callable"
      via: "public contract/request/attempt/findings allowlist only"
    - from: "authoring.apply_autonomy"
      to: "writer handoff contract"
      via: "one validated proposal and audit record; permission mode changes only whether an exact declared unit may cross the boundary"
  prohibitions:
    - requirement_id: "AUDIT-07"
      category: "transparency"
      status: "unresolved"
      verification: null
      statement: "MUST NOT hide detector disagreement, measured evidence, or threshold provenance behind a single quality score or model judgment."
    - requirement_id: "AUTH-01"
      category: "safety"
      status: "unresolved"
      verification: null
      statement: "MUST NOT silently discard exhausted drafts, conceal a partially completed interrupted run, or turn a duplicate invocation into duplicate curriculum."
---

<objective>
Complete the bounded repair loop, separate deterministic machine-authoring quality gate, and three-mode permission contract, with explicit threshold boundaries and stateful run guarantees.

Purpose: Ensure a lint-clean draft can still be rejected for the four researched pedagogical defects, while retries and final permission remain repository-blind, scoped, bounded, reproducible, transparent, idempotent, and safe to resume.
Output: Full quality/retry/autonomy logic, strict finding/report contracts, and standalone focused tests/fixtures.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-CONTEXT.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-AI-SPEC.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-RESEARCH.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-PATTERNS.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-VALIDATION.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-01-SUMMARY.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-02-SUMMARY.md
</context>

## Artifacts this phase produces

- Four exact versioned detectors with independent evidence and blocking dispositions.
- A bounded state machine for scope validation, lint feedback, quality feedback, full-candidate preflight, exhaustion retention, and writer handoff.
- Three permission modes over that same state machine, with explicit approval and pre/post full-autonomy volume accounting.
- Focused stdlib test scripts for threshold, boundary, ordering, permission, volume, idempotency, interruption, and concurrency contracts.

<assumption_probes>
- AUDIT-07 unclassified review: the four researched detectors are the complete Phase 11 quality profile; adding a fifth detector requires a later explicit schema/profile decision. Detector profiles may change severity, but cannot erase evidence.
- AUDIT-06 data-boundary review: compare the exact lint finding records/codes, treat an empty result as clean, reject null/malformed lint output, preserve singleton findings, and emit deterministic stable finding order. Existing lint semantics are reused, not reimplemented.
- AUTH-01 stateful review: the same request+bank+adapter+profile fingerprints identify a duplicate run; interruption states are explicit and resumable; concurrent generations may proceed, but final preflight/write is serialized and a loser revalidates or returns an idempotent result.
- AUTH-02 stateful review: every attempt and every post-lock candidate is scope-checked; interrupted or parallel execution cannot authorize out-of-scope content through stale preflight.
- AUTH-03 remains an unclassified review: contract sufficiency is proven with a spy callable and malformed-first/clean-second fixture, not assumed from prompt prose.
- AUDIT-05/AUDIT-09 unclassified review: use the existing stored enum `report_only`, `draft_and_approve`, `audit_draft_lint_fix_commit`; require exact write-id approval in the middle mode and explicit opt-in plus per-run/per-objective caps and proposed/final counts in the highest mode.
- Assumption-delta decision: no change. `quality_gate()` consumes stable IDs/content but is not an identity generalization mechanism; it must not mint/merge IDs, broaden objective identity, or alter `model.content_fingerprint()`.
</assumption_probes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the exact four-detector quality profile and thresholds</name>
  <files>authoring.py, schemas/quality_finding.schema.json, tests/audit_quality_roundtrip.py, fixtures/audit/quality_cases.json</files>
  <read_first>
    <file>authoring.py</file>
    <file>model.py</file>
    <file>schemas/quality_finding.schema.json</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-RESEARCH.md</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-PATTERNS.md</file>
    <file>tests/audit_roundtrip.py</file>
  </read_first>
  <behavior>
    - Answer-position skew evaluates the proposed full bank only when MC count is at least 12 and blocks only when one keyed position is above 40 percent; equality at 40 percent passes.
    - Near-duplicate stems block at normalized non-stopword token Jaccard at least 0.85 only when each stem has at least six tokens; a score just below or a five-token stem passes.
    - Answer leak blocks only when a correct option contributes a contiguous run of at least three normalized content tokens to the stem and no incorrect option contributes that same run.
    - Distractor-rationale completeness blocks every non-correct MC option whose rationale is absent or lacks a condition stating when that option would be correct.
    - Findings are separate, stable-sorted, versioned, evidence-rich records even when several detectors flag one item.
  </behavior>
  <action>Apply `11-PATTERNS.md`'s `authoring.py` analog: compose `model.py`'s public parser/lint/identity functions and keep independent named quality records rather than adding surface dependencies or a second parser. Implement exactly the four Research detectors as pure functions called by a separate versioned `quality_gate`; do not add a fifth research check, fold results into lint, or compute an aggregate score per D-09/D-10. Use full proposed-bank keyed counts for skew: inactive below 12 and blocking only above 0.40. For near duplicates, normalize case/punctuation, remove a documented fixed stopword set, require six or more remaining tokens on both sides, and block Jaccard scores at or above 0.85 while reporting the sets/intersection/union/score and both item IDs. For answer leak, compare normalized content-token sequences and block a correct-option run of three or more appearing contiguously in the stem only when no incorrect option shares it; report the run and competing-option comparison. For distractor rationale completeness, promote the existing lint-observable missing-rationale/missing-would-be-correct condition into an independent named quality finding instead of duplicating parser/identity logic. Give every record detector id/version, severity, item ref, measured values, threshold, concise remediation, and stable code; sort by item ref, detector id, and evidence key. Machine-authored findings block per D-11. Assert the gate never mutates IDs, objectives, questions, or content fingerprints.</action>
  <verify>
    <automated>python tests/audit_quality_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>All four detectors fail their known-bad fixtures, pass their equality/floor controls exactly as specified, co-report without score averaging, serialize deterministically under reordered input, and leave identity/fingerprints unchanged.</acceptance_criteria>
  <done>`python tests/audit_quality_roundtrip.py` exits 0 with all four named detectors blocking their bad fixtures, passing exact threshold controls, preserving separate deterministic findings, and leaving IDs/fingerprints unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finish bounded structured retry and full-candidate preflight</name>
  <files>authoring.py, schemas/authoring_request.schema.json, tests/audit_authoring_roundtrip.py, fixtures/audit/retry_drafts.json</files>
  <read_first>
    <file>authoring.py</file>
    <file>model.py</file>
    <file>schemas/authoring_request.schema.json</file>
    <file>schemas/quality_finding.schema.json</file>
    <file>fixtures/audit/tracer_drafts.json</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Each attempt is response-shape and scope validated before lint; structured scope/lint/quality records become the next callable input without prose relay.
    - The retry cap is exact: success on the last allowed attempt proceeds, one more call never occurs, and exhaustion retains the entire draft/report with no writer call.
    - A clean prefix in a dirty batch does not write; empty/null response is malformed, a valid singleton request can complete, and finding order is stable.
    - Full-candidate preflight reparses, lints, and quality-gates existing plus all candidate items immediately before producing an immutable proposal.
    - Prompt-like text inside cited source spans and instruction-like fields in provider output are untrusted data; they cannot change operation, scope, tools, autonomy, output schema, retry cap, or writer permission.
  </behavior>
  <action>Apply `11-PATTERNS.md`'s all-or-nothing preflight pattern and expand `run_authoring` into a strict state machine implementing D-05 through D-08. Validate the callable's JSON/result schema, exact item count, allowed item types, requested objective identities, and request citation membership before parsing/lint. Treat cited source text and every provider-returned field as untrusted data per `11-AI-SPEC.md`: add adversarial fixtures whose source spans demand changed instructions, tools, scope, autonomy, or output shape and assert the fixed system operation, request bounds, no-tool contract, mode, cap, and writer permission remain unchanged. Preserve current lint records/codes and chosen blocking warnings as versioned retry findings; run the separate quality gate only after lint is clean. At each retry, pass the same public format contract and bounded request plus the sorted machine-readable failures, never hand-composed advice or repository context per D-06/AUTH-03. Make retry cap/run/objective caps schema-bounded. On exhaustion, persist/return the complete final draft and reason report with no writer call. On success, compose all candidate items with the exact current bank, assign IDs using existing functions, parse and run both gates over the complete proposed bank, then create an immutable proposal carrying request/source/bank/adapter/profile/tool fingerprints and diff per D-17. Do not write a clean prefix or make the quality gate an ID merger.</action>
  <verify>
    <automated>python tests/audit_authoring_roundtrip.py --case retry</automated>
  </verify>
  <acceptance_criteria>Malformed, prompt-injected, out-of-scope, lint-failing, and quality-failing drafts yield structured retries; source/provider instructions cannot change authority; last-attempt success works; exhaustion makes no writer call; full-batch failure writes no prefix; empty/null/singleton and stable-order cases are explicit.</acceptance_criteria>
  <done>`python tests/audit_authoring_roundtrip.py --case retry` exits 0 and proves structured retry for every failure class, source/provider prompt-injection resistance, exact-cap success/exhaustion, zero prefix writes, and complete candidate preflight.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce transparent autonomy over the idempotent writer handoff</name>
  <files>authoring.py, tests/audit_authoring_roundtrip.py</files>
  <read_first>
    <file>authoring.py</file>
    <file>audit_writer.py</file>
    <file>tests/audit_authoring_roundtrip.py</file>
    <file>schemas/write_manifest.schema.json</file>
    <file>schemas/settings.schema.json</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-CONTEXT.md</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Deterministic run and proposed write IDs derive from request, source, bank-before, adapter-profile, quality-profile, and tool-version fingerprints.
    - Repeating an identical completed fake-writer run returns the existing result and does not invoke generation or mutation again.
    - Interrupted prepared/generated/preflighted states retain enough data to resume or abort visibly; no state is reported complete without a matching writer result.
    - Two parallel fake-writer handoffs serialize the final fingerprint check; one applies and the other returns idempotent or stale-preflight, never both mutate.
    - Report-only, draft-and-approve, and `audit_draft_lint_fix_commit` share byte-equivalent gate/diff evidence and differ only at final permission.
    - Exact write-id approval is required per unit in draft-and-approve; full autonomy requires explicit opt-in, enforces per-run/per-objective caps, and reports proposed and completed/rejected/retried/written volume on every exit.
  </behavior>
  <action>Apply `11-PATTERNS.md`'s all-or-nothing domain/write boundary: define the writer handoff/state contract that Plan 11-05 implements against files/Git using deterministic run/write IDs and explicit `prepared`, `preflighted`, `writing`, `completed`, `failed`, and `interrupted` records. Permit generation outside the bank lock, but require the writer to serialize current-fingerprint comparison, rerun AUTH-02 scope validation on the immutable full proposal, and either apply exactly one unit, recognize the same completed id, or return `stale_preflight`; it must never accept caller attestation. Model multi-item interruption as a durable list of declared units and completed unit IDs so a visible partial run can resume idempotently rather than hiding a prefix per D-16. Implement D-12 through D-14 as a final domain permission decision over that contract: `report_only` never mutates a bank; `draft_and_approve` requires exact write-id acceptance for each unit; the existing `audit_draft_lint_fix_commit` setting is exposed as higher-risk full autonomy only with explicit opt-in and bounded proposed volume before generation. Emit completed/accepted, rejected, retried-attempt, and written totals/per-objective counts on success, exhaustion, rejection, interruption, and error per D-13/AUDIT-09. Use fake-writer/barrier tests for every permission/interruption boundary. Keep the callable injected and provider-neutral here; Plan 11-05 applies the mapped thin-controller pattern and alone binds `model_adapter.invoke(request, profile)` through `surfaces/cli.py::main`.</action>
  <verify>
    <automated>python tests/audit_authoring_roundtrip.py --case stateful</automated>
  </verify>
  <acceptance_criteria>Duplicate-run, every interruption point, and two-call interleaving tests establish AUTH-01/AUTH-02; all three modes share identical gate results, enforce exact permission, and full reports capped pre/post volume on every exit; no provider is selected and no run completes without one matching scope-valid writer result.</acceptance_criteria>
  <done>`python tests/audit_authoring_roundtrip.py --case stateful` exits 0 with at most one matching scope-valid writer result, exact report/approval/full permissions, and deterministic proposed/completed/rejected/retried/written volume.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Any unresolved critical or high threat blocks task acceptance and writer integration.

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-11-11 | Tampering | Lint/quality gate ordering | high | mitigate | Enforce response scope, lint, quality, full-candidate preflight in code and assert zero writer calls for every failing stage. |
| T-11-12 | Elevation of privilege | Parallel/stale proposal | high | mitigate | Immutable proposal plus writer-held fingerprint check and repeated scope validation; stale loser cannot write. |
| T-11-13 | Repudiation | Exhausted/interrupted/duplicate run | high | mitigate | Deterministic IDs and explicit durable state/result records; complete only with matching writer result. |
| T-11-14 | Denial of service | Similarity/retry work | medium | mitigate | Request/objective/retry caps, six-token floor, token-set reuse, and bounded proposed-bank size. |
| T-11-15 | Spoofing | Opaque quality assertion | medium | mitigate | Independent named detector records with measured evidence and schema version; no caller/model pass attestation. |
| T-11-21 | Elevation of privilege | Autonomy policy | high | mitigate | Existing setting plus explicit full opt-in, exact write-id approval, identical gate pipeline, and model-inaccessible mode selection. |
</threat_model>

<verification>
- `python tests/audit_quality_roundtrip.py` and `python tests/audit_authoring_roundtrip.py` pass in under 60 seconds total.
- Threshold equality/floor fixtures prove the exact four detector boundaries.
- Fake-writer tests prove scope-before-lint, no clean-prefix write, duplicate idempotency, interruption visibility, and serialized stale-preflight handling.
- Identity and fingerprint output is byte-identical before/after quality analysis.
</verification>

<success_criteria>
- AUTH-01 through AUTH-03 and AUDIT-06/AUDIT-07 have deterministic, machine-readable executable contracts.
- A repository-blind callable can repair from public contract plus structured findings alone.
- The second gate blocks lint-clean defects without becoming an identity generalization or opaque score.
- Stateful edge assumptions are resolved in tests before real Git/shadow writer expansion.
- All three autonomy modes and full-autonomy volume accounting are resolved before the public configured-adapter/writer integration.
</success_criteria>

<output>
Create `.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-04-SUMMARY.md` when done.
</output>
