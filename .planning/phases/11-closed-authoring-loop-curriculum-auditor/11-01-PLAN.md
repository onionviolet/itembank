---
phase: 11-closed-authoring-loop-curriculum-auditor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - auditor.py
  - authoring.py
  - audit_writer.py
  - surfaces/audit_cli.py
  - tests/audit_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUDIT-06
  - AUDIT-07
  - AUDIT-08
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "AUTH-01/AUTH-03: one injected repository-blind author callable can take the public contract through scope validation, lint, the deterministic second gate, diff, one atomic synthetic-bank write, and one-step undo without a human relaying findings."
    - "AUTH-02: an item outside the requested count, objective, type, or source citations is rejected before lint and before any writer call."
    - "AUDIT-06/AUDIT-07: the tracer cannot reach its writer unless existing lint and all four named quality detectors pass."
    - "AUDIT-08: the tracer exposes the exact diff and restores its non-Git synthetic bank through one undo operation."
    - "The tracer request, normalized-document, report, finding, and manifest payloads expose explicit schema versions consumed by the strict contracts published in Plan 11-02."
  artifacts:
    - path: "authoring.py"
      provides: "Provider-neutral bounded authoring pipeline and deterministic quality contract"
    - path: "auditor.py"
      provides: "UTF-8 source and citation primitives used by the tracer"
    - path: "audit_writer.py"
      provides: "Atomic write/undo boundary"
    - path: "surfaces/audit_cli.py"
      provides: "Thin command adapter over the Phase 11 domain APIs"
    - path: "tests/audit_roundtrip.py"
      provides: "Wave 0 standalone stdlib tracer and contract suite"
  key_links:
    - from: "surfaces/audit_cli.py"
      to: "authoring.run_authoring"
      via: "parsed args plus injected author callable; no provider import in the domain module"
    - from: "authoring.py"
      to: "model.SPEC/model.parse_bank/model.lint/model.assign_ids"
      via: "existing public contract, parser, linter, and identity functions"
    - from: "authoring.py"
      to: "audit_writer.py"
      via: "writer is invoked only after full-candidate preflight and reproducible diff creation"
    - from: "authoring.py"
      to: "audit_writer.py"
      via: "immutable preflighted proposal, deterministic write id, and expected bank fingerprint"
  prohibitions:
    - requirement_id: "AUTH-02"
      category: "safety"
      status: "unresolved"
      verification: null
      statement: "MUST NOT let model output expand the requested curriculum, count, item types, objectives, or source evidence even when the output is otherwise lint-clean."
    - requirement_id: "AUTH-03"
      category: "privacy"
      status: "unresolved"
      verification: null
      statement: "MUST NOT expose repository contents, private bank contents beyond the scoped candidate context, credentials, or filesystem discovery capability to the repository-blind author."
---

<objective>
Deliver the production tracer for the Phase 11 user story: one scoped item travels from a UTF-8 cited request through a provider-neutral author callable, structured retry feedback, both quality gates, a visible diff, one atomic synthetic-bank write, and one-step undo.

Purpose: Prove every architectural boundary before expanding coverage, detector cases, autonomy modes, and Git handling. The callable remains injected until Phase 8 publishes a concrete adapter; this plan creates no frontend or daemon route.
Output: Root-level domain modules, a thin command adapter, and a standalone standard-library end-to-end tracer test.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-CONTEXT.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-AI-SPEC.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-RESEARCH.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-PATTERNS.md
@.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-VALIDATION.md
</context>

## Artifacts this phase produces

- `auditor.py`, `authoring.py`, and `audit_writer.py` as the reusable domain/write seams.
- `surfaces/audit_cli.py` as the thin in-process command adapter; no browser, daemon, or frontend artifacts.
- `tests/audit_roundtrip.py`, runnable with Python alone and safe against real banks.

<assumption_probes>
- AUTH-01 stateful review is not silently defaulted: duplicate-run idempotency, interruption recovery, and parallel-run serialization are contract obligations. The tracer proves one duplicate shadow run; Plan 04 defines the complete state-machine probes and Plan 05 proves them against both writers.
- AUTH-02 stateful review is not silently defaulted: interruption or a racing writer must not bypass response-scope checks. The writer interface therefore accepts only an immutable preflighted proposal plus expected bank fingerprint and rechecks it under the write lock.
- AUTH-03 is an unclassified-review assumption: the injected callable receives only the public format contract, bounded request, attempt number, and structured findings. A spy callable fails the test if repository paths, modules, or source text outside cited request spans are passed.
- Assumption-delta decision: no change. The second quality gate is content-quality analysis and does not generalize item identity, rewrite stable IDs, or change `content_fingerprint()` semantics.
</assumption_probes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one scoped item through every Phase 11 layer and undo it</name>
  <files>auditor.py, authoring.py, audit_writer.py, surfaces/audit_cli.py, tests/audit_roundtrip.py</files>
  <read_first>
    <file>model.py</file>
    <file>schema_validate.py</file>
    <file>surfaces/evidence_cli.py</file>
    <file>tests/protocol_roundtrip.py</file>
    <file>.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-RESEARCH.md</file>
  </read_first>
  <behavior>
    - A repository-blind spy callable receives the public contract and one bounded cited request, returns a malformed draft once, receives versioned structured scope/lint findings, then returns one clean item.
    - The complete proposed bank is assigned IDs, parsed, linted, quality-gated, and diffed in memory before the writer is called.
    - The synthetic non-Git bank is atomically replaced only after every gate passes, then `undo(write_id)` restores the exact before bytes.
    - Repeating the identical request/write id produces no second mutation or manifest.
  </behavior>
  <action>Start with a failing end-to-end test, then implement the keep-for-production tracer. In `auditor.py`, expose UTF-8 bytes-to-normalized-source and exact citation record primitives without any model/provider dependency. In `authoring.py`, expose a provider-neutral `run_authoring(request, author_callable, bank_text, writer, config)` shape: pass only `model.SPEC`, the serialized bounded request, attempt number, and versioned structured failures to the callable per D-05/D-06; validate count, allowed item types, exact objectives, and cited source spans before lint per D-07/AUTH-02; compose the entire candidate bank, reuse `model.assign_ids`, `model.parse_bank`, and `model.lint`, and run all four Research detectors before invoking the writer per D-09/D-11/AUDIT-06/AUDIT-07. Use the exact initial detector rules even on this one-path tracer: MC answer-position skew applies only at 12 or more items and blocks only above 40%; near-duplicate stems use normalized non-stopword token Jaccard at least 0.85 with at least six tokens in each stem; answer leak requires a unique contiguous run of at least three normalized content tokens from the correct option in the stem; each non-correct MC rationale must exist and state the condition under which it would be correct. Emit separate named findings with detector version and measured evidence, never an aggregate score per D-10. Retain failed drafts/reports and write nothing when the retry cap is exhausted or any member of the batch fails per D-08. In `audit_writer.py`, implement the narrow shadow path with a deterministic write id, prepared manifest, content-addressed before-image, tmp-plus-`os.replace`, after-fingerprint guard, and one-step undo per D-15/D-17. The tracer may exercise one clean full-autonomy synthetic unit only; broader permission policy follows in Plan 04. Keep `surfaces/audit_cli.py` a thin function adapter and do not add frontend/daemon work.</action>
  <verify>
    <automated>python tests/audit_roundtrip.py --case tracer</automated>
  </verify>
  <acceptance_criteria>The tracer test first demonstrates failure, then passes while proving one structured retry, zero out-of-scope writer calls, full-candidate preflight, one atomic synthetic-bank mutation, duplicate-run idempotency, and exact one-step restoration.</acceptance_criteria>
  <done>`python tests/audit_roundtrip.py --case tracer` exits 0 and proves the scoped malformed-first/clean-second request performs exactly one reversible synthetic-bank mutation with no out-of-scope writer call.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Any unresolved critical or high threat blocks task acceptance and later write enablement.

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-11-01 | Elevation of privilege | Model response scope | high | mitigate | Enforce exact request count/type/objective/citation bounds in code before lint and again before writer invocation; assert zero writer calls on mismatch. |
| T-11-02 | Tampering | Candidate bank/write target | high | mitigate | Full-candidate preflight, expected bank fingerprint, temp file plus atomic replace, prepared manifest, and temp-only synthetic fixture test. |
| T-11-03 | Information disclosure | Author callable payload | high | mitigate | Allowlist public contract/request/findings fields; spy test rejects repository paths, credentials, unrelated source text, and module access. |
| T-11-04 | Denial of service | Retry loop | medium | mitigate | Schema-bounded retry cap and item/objective caps; deterministic retained failure report at exhaustion. |
| T-11-05 | Repudiation | Write/undo | high | mitigate | Deterministic write id, source/request/bank fingerprints, diff, tool version, and prepared/applied manifest state. |
</threat_model>

<verification>
- `python tests/audit_roundtrip.py --case tracer` passes with no external packages or live model.
- The tracer proves response-scope rejection, retry feedback, both gates, atomic shadow write, idempotent duplicate run, and undo.
- No `surfaces/daemon.py`, frontend asset, ORM migration, package manifest, PDF/DOCX adapter, or `COVERAGE.md` is created.
</verification>

<success_criteria>
- A single synthetic happy path touches source normalization, public author contract, response scope, lint, all four quality checks, diff, command adapter, writer, manifest, and undo.
- The author callable is provider-neutral and repository-blind until Phase 8 exposes a concrete adapter.
- Every tracer payload is machine-readable and carries the explicit version that Plan 11-02 validates against its strict public schema.
- High-severity scope, disclosure, tampering, and repudiation threats have executable blocking tests.
</success_criteria>

<output>
Create `.planning/phases/11-closed-authoring-loop-curriculum-auditor/11-01-SUMMARY.md` when done.
</output>
