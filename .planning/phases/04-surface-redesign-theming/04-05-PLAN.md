---
phase: 04-surface-redesign-theming
plan: 05
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - surfaces/study.py
  - tests/surface_roundtrip.py
  - tests/theme_roundtrip.py
  - tests/presentation_roundtrip.py
autonomous: true
requirements:
  - SURF-06
  - SURF-07
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After deliberate reveal, study shows answer and concise why first, every option with its rationale beside it, then labeled expandable second-best, discriminator/trap, and notes sections; no runtime explanation field is discarded (D-12, SURF-06)."
    - "For choice items, the learner may mark a local choice before reveal; chosen and correct option rationales open by default, while other rationales remain available collapsed (D-13)."
    - "Keyboard and screen-reader users can select, reveal, expand, rate, and navigate with the same information/state as pointer users (D-03, D-13)."
    - "Study and quiz use the shared explanation class/token vocabulary while retaining their distinct intent: study deliberately reveals/reviews and never submits a verdict; quiz feedback remains response-driven (D-14)."
    - "Study renders `No study cards match this bank.` for empty data and preserves card/orientation plus a retry action for loading/render errors; reveal uses `Revealing explanation...` in the shared polite status region."
    - statement: "Long stems, options, rationales, notes, rubric/model text, and many disclosures wrap at 320px/200% zoom without clipped or hover-only explanation content."
      verification: backstop
    - "The study view supplies behavior-free public/explanation data to the shared semantic default adapter and remains restylable without moving scoring or queue behavior into a presentation adapter."
    - "Study loads the same validated local settings and generated semantic-token CSS as quiz; its component rules introduce no palette owner and never use the custom accent to mean correctness (D-04, SURF-07)."
  artifacts:
    - path: "surfaces/study.py"
      provides: "Complete runtime-derived study payload and accessible progressive explanation UI"
      contains: "explain_payload"
    - path: "tests/surface_roundtrip.py"
      provides: "Explanation completeness, default disclosure state, keyboard semantics, and quiz/study intent separation checks"
    - path: "tests/theme_roundtrip.py"
      provides: "Study token provenance, local-settings/default, and semantic-state independence assertions"
  key_links:
    - from: "surfaces/study.py"
      to: "runtime.explain_payload"
      via: "study_item composes the canonical public item and full deliberate-review explanation"
      pattern: "explain_payload"
    - from: "surfaces/study.py"
      to: "surfaces/theme.py"
      via: "study CLI/scoped renderer loads local settings, calls theme_css, and uses shared explanation/disclosure/focus/semantic token classes"
      pattern: "theme_css|SURFACE"
---

<objective>
Expose the explanation data study currently drops, present it progressively/accessibly, and migrate study onto the same generated palette and visual primitives as quiz per D-04 and D-12 through D-14.

Purpose: Make study a complete review surface without inventing a second explanation source, palette, or scorer.
Output: Runtime-derived study payload, per-option rationales, labeled disclosures, accessible local selection/reveal controls, and study migration onto the one validated palette/presentation seam.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-surface-redesign-theming/04-CONTEXT.md
@.planning/phases/04-surface-redesign-theming/04-RESEARCH.md
@.planning/phases/04-surface-redesign-theming/04-UI-SPEC.md
@.planning/phases/04-surface-redesign-theming/04-04-SUMMARY.md
@runtime.py
@surfaces/study.py
@surfaces/quiz_page.py
@surfaces/theme.py
@surfaces/presentation.py
@fixtures/sample_bank.md
@tests/surface_roundtrip.py

<interfaces>
Existing canonical payloads:

- `runtime.public_item(q, shuffle_seed=0)` supplies safe stem/options/response shape.
- `runtime.explain_payload(q, reveal=True)` supplies `answer_text`, `why`, `disc`, `second`, `trap`, `notes`, plus per-type `correct`, `da`, `row_cats`, `steps`, or short-answer model/rubric.

`study_item(q)` must compose these functions rather than separately copying selected question fields. Study's deliberate-review page may carry the full returned explanation, but it must not call `runtime.score_response` or claim a correctness verdict for the learner's local choice.
</interfaces>
</context>

## Artifacts this phase produces

- `surfaces/study.py`: canonical full-explanation view data plus polished semantic deliberate-reveal/recall presentation and explicit states.
- `tests/surface_roundtrip.py`: payload completeness, safe embedding, progressive disclosure, intent separation, accessibility, responsiveness, and state contracts.

<wave_zero_contract>
Plan 04-01 Task 1 already created/extended the surface, theme, and presentation harnesses. Each task here first adds its canonical-payload/reveal/palette assertions and observes RED before changing `surfaces/study.py`.
</wave_zero_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build every study card from the canonical complete explanation payload</name>
  <read_first>
    - `.planning/phases/04-surface-redesign-theming/04-CONTEXT.md` — D-12 through D-14 and study/quiz intent boundary.
    - `.planning/phases/04-surface-redesign-theming/04-UI-SPEC.md` — full explanation order, per-type states, exact copy, long-content and safety contracts.
    - `.planning/phases/04-surface-redesign-theming/04-04-SUMMARY.md` — implemented semantic adapter/primitives and shared token interfaces.
    - `runtime.py` — canonical `public_item` and `explain_payload` shapes for every item type.
    - `surfaces/study.py` and `tests/surface_roundtrip.py` — current dropped-field projection, embedded data, renderer, and test conventions.
  </read_first>
  <files>surfaces/study.py, tests/surface_roundtrip.py</files>
  <behavior>
    - Test 1: For mc/multi, `study_item` contains public options plus `answer_text`, `why`, `correct`, every non-empty `da` entry, `second`, `disc`, `trap`, and all notes.
    - Test 2: Table/dnd/build/short cards preserve every field their `explain_payload` branch returns; empty optional fields stay empty rather than being fabricated.
    - Test 3: A sentinel added to each explanation field in a synthetic question appears exactly once in both generated study data and the current reveal output; no independent hand-copied projection can drift.
    - Test 4: Bank text that contains HTML or a script terminator is safely serialized and later inserted through text/escaped rendering rather than executable markup.
    - Test 5: Empty input produces `No study cards match this bank.` through the shared state panel, while malformed/render-error fixtures preserve bank context and a retry/back action rather than a blank page.
  </behavior>
  <action>Replace `study_item`'s hand-selected `answer/why/disc/trap` dict with a behavior-free view composition of `runtime.public_item(q)` and `runtime.explain_payload(q, reveal=True)` under a clear `explain` member. Retain the stable id/type/stem/objective shape through the public payload. Use script-safe JSON serialization so bank prose cannot close the data element, and feed only escaped presentation data into shared semantic adapter/primitives; no scorer, queue mutation, path, or evidence behavior enters the adapter. Extend the existing reveal output in the same task so answer/why, every option rationale, second-best, discriminator/trap, notes, and non-choice model/rubric fields are already visible through labeled semantic sections after deliberate reveal; Task 2 will refine their relevance/default-open order and complete navigation/accessibility, not defer any payload field. Add shared-shell empty and safe render-error states with the UI-SPEC copy/recovery action. Extend `tests/surface_roundtrip.py` with synthetic sentinels across every branch. D-12 is enforced as a single-source composition: every field already allowed by the runtime reaches the learner without another explanation allowlist.</action>
  <verify>
    <automated>python tests/surface_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Study data and the existing deliberate-reveal output contain the complete canonical safe public plus reveal payload for every item type, with no handwritten field allowlist.
    - Embedded and DOM rendering remain inert for HTML/script-like bank text.
    - Empty/error states use the shared shell, exact empty copy, retained context, and keyboard-reachable recovery.
    - Presentation view data contains no scorer, evidence writer, file path, or mutation callback.
  </acceptance_criteria>
  <done>Study data is a complete projection of the canonical runtime public/explanation payload, including option rationales, second-best, and notes (D-12, SURF-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render relevant rationales and advanced explanation progressively</name>
  <read_first>
    - `.planning/phases/04-surface-redesign-theming/04-CONTEXT.md` — D-03 and locked D-12 through D-14 interaction semantics.
    - `.planning/phases/04-surface-redesign-theming/04-UI-SPEC.md` — exact reveal sequence, copy, controls, states, breakpoint, focus/live-region, and backstop requirements.
    - `surfaces/study.py` — Task 1 canonical view data and current Flash/Learn navigation/queue behavior.
    - `surfaces/presentation.py` and `surfaces/theme.py` — shared semantic fallback, status/disclosure primitives, tokens, and progressive enhancement hooks.
    - `surfaces/quiz_page.py` and `tests/surface_roundtrip.py` — response-driven comparison and structural tests.
    - `tests/theme_roundtrip.py` and `tests/presentation_roundtrip.py` — Wave 0 token-provenance and adapter harnesses to extend RED-first.
  </read_first>
  <files>surfaces/study.py, tests/surface_roundtrip.py, tests/theme_roundtrip.py, tests/presentation_roundtrip.py</files>
  <behavior>
    - Test 1: Reveal order is answer, concise why, option list with adjacent rationales, then labeled details for second-best, discriminator/trap, and notes.
    - Test 2: Before reveal, mc/multi options are native toggle buttons for local recall only; after reveal, details for selected and correct choices are open and other rationale details are closed but keyboard-reachable.
    - Test 3: Non-choice cards show their canonical answer/explanation without inventing option controls, and short cards retain model/rubric deliberate-review content.
    - Test 4: Flash/Learn tabs, reveal, previous/next, Got it/Missed, option selection, and disclosures are native controls with visible focus, semantic labels, status announcements, and reduced-motion behavior.
    - Test 5: No study JavaScript imports/calls a scorer or sends a response; local selected/got/missed state affects only card presentation/queue behavior per D-14.
    - Test 6: Reveal announces `Revealing explanation...`; empty/error states, long content at 320px/200%, focus movement, visible focus, native disclosure state, and reduced motion preserve all explanation information.
    - Test 7: Unrevealed, revealed, and Learn-rating states each expose one primary next action, while option rationales/advanced fields progressively disclose without hiding them from semantic fallback or adding an automatic pedagogy sequence.
    - Test 8: `study_page` consumes `theme.theme_css` from settings beside the bank (schema defaults when absent), matches quiz accent tokens in system/light/dark, and contains no component color literals or accent-as-correctness styling.
  </behavior>
  <action>Rework the study card markup so the front displays one focused stem/prompt and public choice controls where applicable and records a local recall selection without declaring a verdict. Replace the click-only flipping div with an explicit Reveal explanation button and semantic state; while transitioning announce `Revealing explanation...`. Apply the shared teaching-step primitive so unrevealed, revealed, and Learn-rating states each have one unmistakable primary next action and immediate visible status, with secondary navigation/details kept quiet. On reveal, render answer and concise why first; render each option once with its native `details` rationale immediately adjacent, opening correct and locally selected choices by default per D-13 and leaving every other rationale collapsed but reachable; then render labeled Second-best answer, Discriminator/Trap, and Notes disclosures. Non-choice types render their canonical answer/model/rubric and advanced sections without fake options. Change `study_page` and its CLI/scoped call sites to load settings beside the bank (schema defaults when missing), call the one `theme.theme_css` generator, and express component colors only through semantic custom properties; custom accent means interaction, never correct/incorrect. Reuse the shared semantic fallback/explanation/disclosure/focus/status/token primitives and stable enhancement hooks, preserving D-14: study deliberately reviews, and Flash/Learn/Got it/Missed/previous/next affect only local presentation/queue behavior, while quiz alone is response-driven. Keep native controls at least 44px, one heading hierarchy, visible text labels/icons in addition to state color, one polite status region, focus restoration, reduced motion, 720px measure, and wrapping at 320px/200% zoom. This borrows concise progressive teaching presentation only; presentation adapters add no automatic sequence, simulation, canvas, scorer, answer submission, or evidence logic.</action>
  <verify>
    <automated>@('tests/surface_roundtrip.py','tests/theme_roundtrip.py','tests/presentation_roundtrip.py') | ForEach-Object { python $_; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }</automated>
  </verify>
  <acceptance_criteria>
    - Reveal order and labels match the UI contract; correct/chosen rationales open and all others remain natively reachable.
    - Flash/Learn/reveal/rating/navigation remain keyboard/screen-reader equivalent and never claim a local correctness verdict.
    - Loading/empty/error/reveal status, long content, 320px/200%, focus, and reduced-motion cases preserve complete information.
    - Study and quiz share presentation primitives but retain deliberate-review versus API-response authority without duplicated behavior.
    - Study resolves the same source accent/settings defaults as quiz and has no independent palette or accent-derived verdict meaning.
  </acceptance_criteria>
  <done>The learner can deliberately reveal a complete, progressively organized explanation; selected/correct rationales open by relevance and every other rationale remains accessible (D-12 through D-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bank/runtime payload -> embedded study data | Human-authored text and full deliberate-review explanation enter an HTML script/data context. |
| Local study controls -> learner interpretation | Selection/reveal/rating state must not be misrepresented as a scored verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Tampering | embedded study JSON/DOM | medium | mitigate | Escape script terminators during serialization and use text nodes/escaping for every rendered bank string. |
| T-04-18 | Spoofing | local choice and Got it/Missed state | medium | mitigate | Label controls as recall/review state, never use correctness verdict copy, and make no scorer/API call from study. |
| T-04-19 | Information Disclosure | full study explanation | low | accept | Study is explicitly the deliberate reveal/review surface; quiz key withholding remains separately enforced and tested. |
| T-04-20 | Information Integrity | color-only option state | medium | mitigate | Pair semantic colors with Correct/Selected labels, icons/borders, native expanded state, and live status text. |
| T-04-26 | Information Integrity | study palette migration | medium | mitigate | Consume `theme_css` from validated local settings, prohibit component palette literals, and keep semantic verdict tokens independent of accent. |
| T-04-SC | Tampering | package supply chain | low | accept | No external UI or accessibility package is installed. |
</threat_model>

<verification>
- `python tests/surface_roundtrip.py` proves payload completeness, progressive order/default state, safe rendering, keyboard semantics, and no study scoring path.
- `python tests/theme_roundtrip.py` and `python tests/presentation_roundtrip.py` prove study token provenance, semantic adapter/fallback, and no independent palette.
- `python tests/daemon_roundtrip.py` proves `/study/<stem>` still renders through `study_page` and CLI output remains equivalent.
</verification>

<success_criteria>
- `study_item` consumes the canonical runtime payload and drops none of `da`, `second`, `disc`, `trap`, or `notes` (SURF-06, D-12).
- Correct/chosen rationales open by default and every other rationale remains available to all input modes (D-13).
- Quiz and study share visual explanation primitives while maintaining response-driven versus deliberate-review behavior (D-14).
- Study consumes the single generated palette and local-settings/default path without adding a palette owner (D-04, SURF-07).
</success_criteria>

<output>
Create `.planning/phases/04-surface-redesign-theming/04-05-SUMMARY.md` when done.
</output>
