---
phase: 06-hint-ladder-cursor-hold-feedback-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime.py
  - evidence.py
  - schemas/session.schema.json
  - schemas/response.schema.json
  - schemas/report.schema.json
  - tests/hint_roundtrip.py
autonomous: false
requirements: [TEACH-01, TEACH-02, TEACH-03, MODE-01, MODE-02, MODE-03, MODE-04, MODE-05, MODE-06]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One mode-keyed runtime transition is the sole authority for scoring, cursor movement, tier unlock/show state, completion, and disclosure (D-01/D-02)."
    - "Practice records only materially distinct, non-empty canonical attempts; duplicate, empty, and deduped replays unlock nothing, while a correct retry advances (D-04/D-05/D-06, TEACH-01)."
    - "The six fixed authored tiers remain lesson pointer, objective, trap, picked-option rationale, discriminator, reveal; an unavailable tier stays in its numbered slot (D-07/D-08/D-09, TEACH-02)."
    - "Drill, practice, diagnostic, and exam produce different runtime actions and disclosure payloads, with feedback mode immutable for the sitting and separate from selection_mode (D-10 through D-14, MODE-01 through MODE-05)."
    - "Every genuine response records feedback mode and the highest tier actually shown using null-versus-zero semantics; every newly shown hint is a linked append-only event (D-15/D-16, MODE-06)."
    - "Reports derive first-try, retry, tier-reached, stumped, revealed, pending, and unresolved outcomes from live response/hint/mark events, never from a mutable hint counter (D-17, TEACH-03)."
  artifacts:
    - path: runtime.py
      provides: "FEEDBACK_POLICIES, HINT_TIERS, versioned teaching state, authored tier resolver, and the one teaching transition"
    - path: evidence.py
      provides: "hint_event(), response hint-tier snapshots, replay reconciliation inputs, and derived teaching outcomes"
    - path: schemas/session.schema.json
      provides: "Versioned persisted teaching-state contract and v1-to-v2 upgrade target"
    - path: schemas/response.schema.json
      provides: "Versioned response and hint event contracts"
    - path: schemas/report.schema.json
      provides: "Derived per-item teaching outcome contract"
    - path: tests/hint_roundtrip.py
      provides: "Executable Phase 6 transition, evidence, resume, leakage, and reporting contract"
  key_links:
    - from: runtime.py
      to: runtime.score_response
      via: "teaching_transition canonicalizes and scores through the existing canonical_response()/score_response() only"
      pattern: "canonical_response|score_response"
    - from: runtime.py
      to: "Phase 3 q['lesson_slug']"
      via: "tier 0 returns the existing renderer-neutral lesson pointer; Phase 6 does not parse lesson text"
      pattern: "lesson_slug"
    - from: evidence.py
      to: evidence.append_event
      via: "response_event() and hint_event() are persisted through the one append-only writer"
      pattern: "append_event"
    - from: evidence.py
      to: schemas/response.schema.json
      via: "response/hint event versions and required fields match the published schema"
      pattern: "hint|hint_tier"
    - from: evidence.teaching_outcomes
      to: schemas/report.schema.json
      via: "report rows are reconstructed from live response, hint, and accepted mark events"
      pattern: "teaching_outcomes"
---

> **Phase goal (quoted verbatim from `.planning/ROADMAP.md`):** A wrong answer holds
> the session open for a real second attempt, hints unlock one authored tier at a time,
> and feedback behavior changes correctly by session mode.

<objective>
Create the runtime-owned feedback policy and its append-only evidence/reporting contract.

Purpose: make cursor movement, tier gating, and disclosure deterministic before any CLI,
HTTP, or browser adapter consumes them. The runtime remains the authority; competitor
patterns such as Brilliant's diagnose-then-guide loop are teaching benchmarks, not a
feature-parity target.

Output: a versioned teaching transition in `runtime.py`, response and hint evidence in
`evidence.py`, upgraded JSON contracts, and a runnable core roundtrip suite.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-hint-ladder-cursor-hold-feedback-modes/06-CONTEXT.md
@.planning/phases/06-hint-ladder-cursor-hold-feedback-modes/06-RESEARCH.md
@.planning/phases/03-lesson-format-in-app-reader/03-CONTEXT.md
@.planning/phases/01-evidence-spine-protocol-foundation/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

@runtime.py
@evidence.py
@schemas/session.schema.json
@schemas/response.schema.json
@schemas/report.schema.json
@tests/evidence_roundtrip.py
@tests/protocol_roundtrip.py

<interfaces>
Existing contracts to preserve and reuse:

- `runtime.canonical_response(q, answer) -> str | None` defines answer equivalence.
- `runtime.score_response(q, answer) -> bool | None` is the only scorer; `None` means manual review is pending.
- `runtime.public_item(q)` is the pre-answer payload; Phase 3 adds `lesson_slug` to this/public question shape.
- `runtime.explain_payload(q, reveal=True)` is private post-response material and must never be passed wholesale to practice, diagnostic, or unmarked-exam clients.
- `runtime.SESSION_UPGRADES` upgrades stored session dictionaries one version at a time.
- `evidence.attempt_number(log, session_id, item_key, canon)` and `evidence.append_event(log, event)` provide replay-safe numbering and the one writer.
- `evidence.live_events(log)` is the only counting source; retracted events must not appear in reports.
- Phase 3 publishes `q['lesson_slug']` and `/lesson/<bank>#<slug>`; this plan consumes that pointer and does not recreate lesson parsing.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Preflight Phase 3 and confirm the append-only evidence contract</name>
  <decision>Confirm the irreversible Phase 6 response/hint evidence fields, linkage, and schema compatibility only after Phase 3's lesson-pointer dependency passes executable checks.</decision>
  <context>
    Before presenting the choice, run `python tests/lesson_roundtrip.py` and `python -c "import model,runtime;qs=model.load('fixtures/lesson_bank.md');q=next(q for q in qs if q.get('lesson_slug'));assert runtime.public_item(q)['lesson_slug']==q['lesson_slug']"`. If either command fails, stop: Phase 3 is not executable and Phase 6 must not begin. If both pass, inspect the proposed v2 contract: every response records integer-or-null `hint_tier`; every hint records session/item identity, attempt number, linked response event id and canonical response, fixed tier index/name, availability, source `authored`, and unlock path; readers continue accepting v1 events while writers emit v2. D-15/D-16 make omission unrecoverable once append-only events exist, so this gate confirms the exact compatibility boundary rather than reopening whether evidence is recorded.
  </context>
  <options>
    <option id="confirm-contract">
      <name>Confirm contract</name>
      <pros>Phase 3 is present; the required linkage distinguishes null from tier 0, attempt from stumped, response-specific tier 3, and supports v1 reads with v2 writes.</pros>
      <cons>Changing required fields after real v2 events are appended requires a compatibility reader or migration.</cons>
    </option>
    <option id="revise-contract">
      <name>Revise before append</name>
      <pros>Field/linkage/schema corrections happen before irreversible evidence is written.</pros>
      <cons>Execution pauses until the revised contract is reflected in Task 3 and both schemas.</cons>
    </option>
  </options>
  <resume-signal>Select `confirm-contract`, or describe the field/linkage/schema revision required.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: TRACER — complete runtime policy, fixed authored tiers, and resumable state</name>
  <reversibility rating="costly">D-01 through D-14 define a shared policy/state contract used by every sitting client; changing its shape later requires coordinated caller and session-upgrade changes.</reversibility>
  <files>runtime.py, schemas/session.schema.json, tests/hint_roundtrip.py</files>
  <behavior>
    - "A genuine wrong practice response returns hold and unlocks at most one tier; empty/canonical-identical input changes nothing; hint and stumped reveal exactly one fixed tier; a correct retry advances (D-01 through D-09)."
    - "Tier 0 consumes Phase 3's lesson_slug, tier 3 follows the latest genuine picked option, unavailable tiers keep their numbered slots, and tier 5 is the authored reveal (D-07/D-08/D-09)."
    - "Drill reveals and advances; diagnostic defers until completion; exam defers until an accepted mark; short remains pending; feedback mode is immutable and separate from selection_mode (D-10 through D-14)."
  </behavior>
  <action>
    Create `tests/hint_roundtrip.py` first in the direct-script project style. Drive the pure runtime from a version-2 session over `fixtures/lesson_bank.md` through wrong/duplicate/empty submit, hint, stumped, changed MC response, correct retry, every fixed tier including unavailable content, tier-5 completion, manual-review pending, v1 upgrade/resume, drill, diagnostic completion, exam before/after accepted-mark state, and attempted mode mutation.

    In `runtime.py`, bump `SESSION_VERSION` to 2, register the v1-to-v2 upgrade, and define persisted `teaching_state` keyed by stable item identity. Each item stores attempt_count, highest tier unlocked/shown, last genuine canonical response, last response event id, and ordered shown-tier snapshots (D-03). Add the exact `HINT_TIERS` table and one `FEEDBACK_POLICIES` table keyed only by feedback mode (D-01/D-08).

    Implement `teaching_transition(session, q, action, evidence_state=None)` as the only policy boundary. It accepts only the Phase 6 action kinds submit, hint, and stumped; calls existing `canonical_response()` and `score_response()`; returns hold/advance/reveal_tier/defer_feedback/complete plus next state; and never writes files. No renderer, canvas, observation, or metadata parameter reaches this function. Concrete interactive visual action/observation semantics and schemas belong exclusively to Phase 06.1.

    Implement `authored_hint(q, tier, canonical)` as the sole private-tier resolver: Phase 3 lesson_slug, objective, trap, latest picked-option analysis, discriminator, reveal. Missing content returns available false at the same index. Implement evidence-state reconciliation as a pure input fold so Plan 06-02 can repair a crash window without replaying disclosure. Preserve `score_response() is None` as pending and never consult selection_mode.
  </action>
  <verify>
    <automated>python tests/hint_roundtrip.py</automated>
  </verify>
  <done>The pure production transition covers all modes and fixed tiers, consumes the executable Phase 3 pointer, preserves resume state, and exposes no renderer authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derive teaching outcomes and schema-valid reports from live events</name>
  <reversibility rating="one-way">D-15/D-16 are already locked: append-only evidence cannot reconstruct a shown tier or its unlock path if this build fails to record it.</reversibility>
  <files>evidence.py, schemas/response.schema.json, schemas/report.schema.json, tests/hint_roundtrip.py</files>
  <behavior>
    - "Every hint event is linked to the response/attempt state that authorized it, distinguishes attempt from stumped unlock, and is suppressed by retraction through live_events (D-16)."
    - "teaching_outcomes reports first_try_correct, correct_after_attempts, correct_after_tier, stumped_at_tier, revealed, pending, or unresolved without counting stumped as a wrong response (D-17)."
    - "Two otherwise-identical correct responses at tier 1 and tier 4 produce different report rows and hints_used values (TEACH-03)."
    - "Diagnostic completion review is derived from recorded responses; exam review remains withheld until a live accepted mark event exists (D-12/D-13)."
  </behavior>
  <action>
    After Task 1 resumes with `confirm-contract`, extend `tests/hint_roundtrip.py` with a real temporary log containing first-try correct, multiple genuine wrong attempts, an attempt-unlocked hint, a stumped-unlocked hint, a shown unavailable tier, a reveal, a pending short response, an accepted short mark, and retracted response/hint/mark events. Validate every newly written response and hint event against `schemas/response.schema.json` and the derived report against `schemas/report.schema.json`.

    Bump `EVENT_SCHEMA_VERSION`, add `hint` to `KNOWN_EVENT_TYPES`, allow `response_event(..., hint_tier=None)` to write integer or null, and add `hint_event()` with exactly the Task 1-confirmed required linkage and an idempotent dedupe key. Both are data constructors; persistence remains exclusively `append_event()` (D-15/D-16). Add `evidence.hint_events(log, session_id, item_key=None)` and `evidence.teaching_outcomes(log, session_id)` over `live_events()` only. Join hints to response/attempt and marks to response event; never trust session teaching_state or a mutable hints-used counter. Derive hints_used, highest tier, genuine attempts, stumped/reveal state, score/mark resolution, and one D-17 outcome label. Preserve null-versus-zero exactly.

    Update the response schema in the same commit: v2 response accepts integer-or-null hint_tier, and hint is a fully required closed event definition matching the confirmed fields while v1 event reads remain supported. Extend the report schema with required teaching_outcomes matching the derivation. Existing aggregate accuracy fields remain additive and unchanged.
  </action>
  <verify>
    <automated>python tests/hint_roundtrip.py &amp;&amp; python tests/evidence_roundtrip.py &amp;&amp; python tests/protocol_roundtrip.py</automated>
  </verify>
  <done>Schema-valid live events reconstruct every D-17 teaching outcome, distinguish tier 1 from tier 4, respect retractions/marks, and never rely on mutable counters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| learner action -> runtime transition | Untrusted Phase 6 submit/hint/stumped action data crosses into policy and scoring |
| private authored item fields -> public transition payload | The runtime selects the only content a learner may receive |
| session JSON/evidence JSONL -> reconciliation/report | Persisted local state may be stale, replayed, retracted, or malformed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `authored_hint` / `teaching_transition` | high | mitigate | Resolve only the authorized fixed tier, return explicit unavailable slots, and test that practice/diagnostic/exam payloads omit later tiers, key, and explanation fields. |
| T-06-02 | Tampering | genuine-attempt and tier progression | high | mitigate | Canonical distinctness, append-event dedupe, and live-event reconciliation precede unlock/cursor changes; empty, duplicate, and replay inputs are regression-tested. |
| T-06-03 | Tampering | feedback mode | high | mitigate | Mode is persisted at sitting start, action envelopes carry no mode authority, and a mismatch is rejected; selection_mode is a separate untouched field. |
| T-06-04 | Repudiation | hint usage and stumped actions | medium | mitigate | Each shown tier produces an idempotent append-only hint event linked to session/item/attempt/response and unlock path; reports read live events after retractions. |
| T-06-05 | Integrity | manual-review responses | medium | mitigate | Preserve `score_response() is None`; no wrong outcome, hint progression, or exam feedback exists until a live accepted mark is joined. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager task exists; Phase 6 remains Python-stdlib-only with no install step. |
</threat_model>

<verification>
- `python tests/hint_roundtrip.py` proves all transitions, tiers, modes, evidence, replay recovery, report outcomes, and leakage absences.
- `python tests/evidence_roundtrip.py` proves existing append/dedupe/retraction/index behavior remains green.
- `python tests/protocol_roundtrip.py` proves session/response/report versions match the published schemas.
- No file under `.planning/phases/04-*` or `.planning/phases/06.1-*` is modified.
</verification>

<success_criteria>
- `runtime.py` contains one mode-keyed policy transition and one authored-tier resolver; no surface policy exists yet.
- Practice holds, advances, and reveals exactly as D-04 through D-09 require; the other three modes withhold or reveal exactly as D-10 through D-13 require.
- Response and hint evidence is append-only, replay-safe, versioned, and sufficient to derive every D-17 outcome.
- The runtime accepts no renderer/canvas observation contract; concrete visual action/observation semantics and schemas are explicitly deferred to Phase 06.1.
</success_criteria>

<output>
Create `.planning/phases/06-hint-ladder-cursor-hold-feedback-modes/06-01-SUMMARY.md` when done.
</output>
