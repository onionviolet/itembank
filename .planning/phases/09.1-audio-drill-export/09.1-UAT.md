---
status: complete
phase: 09.1-audio-drill-export
source: [09.1-VERIFICATION.md]
started: 2026-08-11T00:00:00Z
updated: 2026-08-11T12:00:00Z
---

## Current Test

[testing complete — all automated criteria pass; manual audio-quality items deferred to the user]

## Tests

### 1. `itembank export audio --objective <id>` writes a pack + transcript (ROADMAP SC 1)

expected: For an objective, the command writes a plain-text transcript always, plus an audio file (per-pack default) sequenced stem → timed pause → key → why; with `--engine transcript-only` it writes the transcript and exits 0 with no audio.

why_human: none — automated.

how_to_run: `python tests/audio_export_roundtrip.py` (test_cli_transcript_only_and_legacy_byte_compat, test_assemble_pack_silence_and_split, test_transcript_diff_all_modes_and_containers).

result: pass

### 2. The engine is a settings entry behind one interface; swapping is config-only (ROADMAP SC 2)

expected: engine/container/split/pause live in the settings schema and itembank.json; switching engines is a settings change; `--engine` overrides without touching the schema.

why_human: none — automated.

how_to_run: `python tests/audio_export_roundtrip.py` (test_settings_schema_audio_block, test_engine_switch_is_settings_change_only); `python tests/config_roundtrip.py`.

result: pass

### 3. With no engine installed or reachable, transcript + named refusal, never a half-write (ROADMAP SC 3)

expected: a missing/unreachable/unknown engine emits the transcript, names the engine and reason, exits non-zero, and leaves no partial audio.

why_human: none — automated.

how_to_run: `python tests/audio_export_roundtrip.py` (test_failing_engine_leaves_no_partial_audio, test_edge_tts_refuses_missing_or_unreachable_by_name, test_failure_paths_same_named_refusal_contract).

result: pass

### 4. Playback is any podcast app; no player/sync/mobile build (ROADMAP SC 4)

expected: MP3 default output consumable by any podcast app; no player, sync, mobile, or playback-position code exists in the shipped audio modules.

why_human: none — automated (structural grep).

how_to_run: `python tests/audio_export_roundtrip.py` (test_surface_inventory_no_player_sync_mobile, test_assemble_pack_container_digest_and_atomicity).

result: pass

### 5. Listening records nothing (ROADMAP SC 5)

expected: the evidence store is byte-identical before and after an export, success or failure.

why_human: none — automated.

how_to_run: `python tests/audio_export_roundtrip.py` (test_no_evidence_write, test_no_evidence_success_and_failure, test_no_evidence_import_in_shipped_module).

result: pass

### 6. A real edge-tts pack sounds right (voice, pause lengths) — DEFERRED to user

expected: install `edge-tts==7.2.8`, run `itembank export audio <bank> --objective <id> --engine edge-tts`, play the MP3: the voice is intelligible and the timed pauses read as deliberate think-time, not glitches.

why_human: subjective audio quality; the offline fixtures prove the mechanics (segment order, silence durations recorded by the fake engine), not how a specific voice sounds.

how_to_run: `pip install edge-tts==7.2.8` then `python itembank.py export audio <bank> --objective <id> --engine edge-tts --out <dir>` and play the result.

result: deferred

decision_shape: accept the shipped voice default, or set `audio.edge_tts.voice` in itembank.json.

### 7. A real Piper pack is truly offline — DEFERRED to user

expected: with the bundled piper binary and an MIT voice model installed, `--engine piper` produces a pack with no network use.

why_human: needs the bundled binary + a downloaded MIT model (the recorded sidecar decision); the stubbed-runner fixtures prove the invocation shape offline.

how_to_run: install the bundled piper binary + `en_US-lessac-medium` model, then `python itembank.py export audio <bank> --objective <id> --engine piper --out <dir>` with the network disconnected.

result: deferred

## Summary

total: 7
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0
deferred: 2 (manual audio-quality/offline-pack checks — see HANDOFF-PHASE09.1.md)

## Gaps

- Manual audio-quality verification (edge-tts voice + pause feel) deferred to the user; the offline fixtures prove the mechanics.
- Real-Piper offline run deferred until the bundled binary/model are installed.
