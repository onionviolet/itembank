---
phase: 09.1-audio-drill-export
plan: 04
type: execute
wave: 3
depends_on:
  - 09.1-01-PLAN.md
  - 09.1-02-PLAN.md
  - 09.1-03-PLAN.md
files_modified:
  - tests/audio_export_roundtrip.py
  - tests/daemon_roundtrip.py
  - surfaces/audio.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - AUDIO-04
  - AUDIO-05
  - AUDIO-06
  - AUDIO-07
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "The transcript-diff fixture proves the pack's spoken text equals the transcript's text in the same order - D-11 is a diffable check, not a claim (D-11, AUDIO-06)."
    - "The no-evidence fixture proves an export leaves the evidence store byte-identical - listening records nothing (D-13, AUDIO-07)."
    - "The atomic-write fixture proves a failing engine leaves no partial audio file and the transcript still writes (D-05, D-04, AUDIO-04)."
    - "The requirement audit maps each of AUDIO-01..AUDIO-07 to a shipped artifact or test; the decision-coverage gate passes for all 16 CONTEXT decisions (D-01..D-16, AUDIO-01..AUDIO-07)."
    - "The supply-chain audit lists every third-party artifact (edge-tts, Piper, lameenc, Piper model files) with its pin, checksum, and named license review - the 09-03 KaTeX precedent (D-15, AUDIO-07)."
    - "A bank that uses none of the audio constructs exports nothing new; the phase's only surfaces are the CLI and the daemon route, and no player, sync, or mobile build exists (D-08, D-14, AUDIO-05, AUDIO-07)."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
    - "UI gate skipped visibly: ui-plan-gate reports frontend=true, which is a false positive (D-14 refuses a player/sync/mobile surface; the only user-facing copy is CLI refusal text locked by D-04/D-05/D-16), so this phase plans without a UI-SPEC per the 2026-08-10 handoff decision 1."
  prohibitions:
    - statement: "No evidence write may exist anywhere in the shipped audio modules."
      status: unverified
      flagged: true
    - statement: "No unpinned third-party artifact may ship; the audit must find none."
      status: unverified
      flagged: true
---

<objective>
As the phase verifier, I want every requirement and every locked decision proven by a fixture - so the phase can honestly report that a pack speaks exactly what its transcript says, that listening records nothing, and that no dependency shipped unpinned.

Purpose: The phase verification plan. It closes the loop on the whole phase with the four fixtures the research named (fake engine, transcript-diff, atomic-write, no-evidence), audits the supply chain and the requirements, and re-runs the full suite including the route/CLI parity.
Output: the completed verification fixtures in tests/audio_export_roundtrip.py and tests/daemon_roundtrip.py, the supply-chain and requirement audit notes, and the phase verification record.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/09.1-audio-drill-export/09.1-CONTEXT.md
@.planning/phases/09.1-audio-drill-export/09.1-RESEARCH.md
@.planning/phases/09.1-audio-drill-export/09.1-01-PLAN.md
@.planning/phases/09.1-audio-drill-export/09.1-02-PLAN.md
@.planning/phases/09.1-audio-drill-export/09.1-03-PLAN.md
@.planning/REQUIREMENTS.md
</context>

<interfaces>
- The fake engine registers through `TTSEngines` (plan 09.1-01) and makes every path testable offline (research Validation Architecture).
- `tests/`' stdlib fail() harness is the framework; `tests/daemon_roundtrip.py` owns the route parity assertion.
- `requirements.txt` is the supply-chain surface the audit reads.
- `evidence.py` is the store the no-evidence fixture snapshots before and after export.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `tests/audio_export_roundtrip.py` | Completed fixtures: fake engine, transcript-diff (audio text == transcript text, in order), atomic-write (failing engine leaves no partial file), no-evidence (store byte-identical), split/container/idempotency |
| `tests/daemon_roundtrip.py` | Route/CLI parity + one end-to-end export through the daemon |
| `surfaces/audio.py` | Any small fixes the fixtures surface (defect closure only) |
| `.planning/phases/09.1-audio-drill-export/09.1-VALIDATION.md` | The requirement audit (AUDIO-01..07 -> artifact/test), the supply-chain audit (pin + checksum + license per artifact), and the decision-coverage record |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The transcript-diff and atomic-write fixtures</name>
  <files>tests/audio_export_roundtrip.py, surfaces/audio.py</files>
  <read_first>
    - `09.1-CONTEXT.md` D-11 (transcript diffable) and D-04/D-05 (named refusal, atomic write)
    - `09.1-RESEARCH.md` Validation Architecture (the transcript-diff and atomic-write fixtures)
    - `tests/audio_export_roundtrip.py` from plans 09.1-01..03
  </read_first>
  <behavior>
    - Test 1: for a fixture objective, the pack's spoken text (fake-engine segments) equals the transcript's text in the same order - the transcript is a diffable check on what the engine was given, in both split modes and both containers (D-11, AUDIO-06).
    - Test 2: a fake engine that fails mid-pack leaves no partial audio file anywhere in the output directory and the transcript still writes; the refusal names the engine and reason and the exit code is non-zero (D-04, D-05, AUDIO-04).
    - Test 3: every engine failure path in the registry (missing dependency, unreachable endpoint, unknown engine) exercises the same named-refusal contract (D-04).
  </behavior>
  <action>
Complete the two fixtures in `tests/audio_export_roundtrip.py`: the transcript-diff fixture renders a pack with a fake engine and asserts the transcript equals the spoken text in order across per-pack/per-item and mp3/wav (D-11); the atomic-write fixture fails an engine mid-pack and asserts zero partial files plus the transcript and named refusal (D-04/D-05). Fix any defect the fixtures surface in surfaces/audio.py - this is defect closure, not feature work. Write the tests first.
  </action>
  <verify>
    <automated>python tests/audio_export_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The transcript-diff holds in every mode/container combination.
    - No failure path leaves partial audio; all refusals name the engine and reason.
  </acceptance_criteria>
  <done>The two load-bearing behavioral contracts - transcript honesty and fail-closed atomicity - are proven by fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The no-evidence fixture and the both-surfaces proof</name>
  <files>tests/audio_export_roundtrip.py, tests/daemon_roundtrip.py</files>
  <read_first>
    - `09.1-CONTEXT.md` D-13 (no evidence write) and D-08 (CLI + daemon route)
    - `evidence.py` - the store the fixture snapshots
    - `tests/daemon_roundtrip.py` - the existing route parity test
  </read_first>
  <behavior>
    - Test 1: snapshot the evidence store, run a full export (including a failing-engine run), snapshot again - the two snapshots are byte-identical; no listened event, no export event, nothing (D-13, AUDIO-07).
    - Test 2: `POST /api/export_audio` and the CLI produce identical file names and transcript for the same fixture - one implementation, two surfaces (D-08, AUDIO-05); the ROUTE_CLI parity assertion still passes.
    - Test 3: the phase's surface inventory contains only the CLI command and the daemon route - no player, no sync, no mobile build, no playback position code exists anywhere in surfaces/ (D-14, AUDIO-07).
  </behavior>
  <action>
Add the no-evidence fixture: snapshot the evidence store before and after both a successful export and a failing-engine export, asserting byte-identity (D-13). Confirm the daemon route and CLI produce identical packs through one runtime call and the ROUTE_CLI inventory holds (D-08). Grep the shipped surface modules for player/sync/mobile/position symbols and assert none exist (D-14). Write the tests first.
  </action>
  <verify>
    <automated>python tests/audio_export_roundtrip.py && python tests/daemon_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Exports leave the evidence store byte-identical, success or failure.
    - Route and CLI are one implementation; the surface inventory has exactly the two surfaces.
  </acceptance_criteria>
  <done>The phase's refusal boundaries are structural: nothing recorded, nothing built beyond the two surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The requirement and supply-chain audits, and the full suite</name>
  <files>tests/audio_export_roundtrip.py, tests/daemon_roundtrip.py</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` AUDIO-01..AUDIO-07 definitions
    - `09.1-CONTEXT.md` D-15 (supply chain, Directive 4a) and D-16 (network honesty)
    - `requirements.txt` - every new pin from plan 09.1-02
    - `.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-03-PLAN.md` - the KaTeX audit precedent
  </read_first>
  <behavior>
    - Test 1: the requirement audit maps every AUDIO-01..AUDIO-07 to a shipped artifact or test and none is orphaned; the audit is a written record in 09.1-VALIDATION.md, not a claim in a summary.
    - Test 2: the supply-chain audit lists every third-party artifact (edge-tts, Piper/phonemize, lameenc, Piper model files) with pin, checksum, and named license review; the edge-tts license posture (read from the pinned version's license text) is recorded (D-15, Directive 4a).
    - Test 3: the D-16 disclosure string is present in the shipped help text and tested; the full repo test suite passes with the audio modules present.
  </behavior>
  <action>
Write `.planning/phases/09.1-audio-drill-export/09.1-VALIDATION.md`: the requirement-to-artifact audit table (AUDIO-01..07), the supply-chain audit table (artifact, pin, checksum, license review - per the 09-03 KaTeX precedent, D-15), the D-16 disclosure check, and the decision-coverage record for D-01..D-16. Assert the disclosure string in a test, then run the full suite (`python tests/*.py`) and record the results. Fix only defects; do not add features.
  </action>
  <verify>
    <automated>python tests/audio_export_roundtrip.py && python tests/daemon_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Every AUDIO requirement and D-decision is traceable in the written audit.
    - Every third-party artifact is pinned, checksummed, and license-reviewed; none is missing.
    - The full suite passes.
  </acceptance_criteria>
  <done>The phase closes with written, fixture-backed proof for every requirement and every locked decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verification fixtures -> shipped modules | Fixtures exercise the shipped modules only through their public interfaces; no test-only bypass |
| audit record -> release gate | The audits are written artifacts the phase handoff cites, not summaries to be trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-091-13 | Repudiation | unverified claims | high | mitigate | Every requirement and decision maps to a fixture or audit row (09.1-VALIDATION.md) |
| T-091-14 | Tampering | dependency drift | high | mitigate | The supply-chain audit re-checks every pin + checksum before the phase closes (D-15) |
| T-091-15 | Information disclosure | disclosure string rot | medium | mitigate | The D-16 help-text string is test-asserted, not prose (D-16) |
| T-091-SC | Tampering | evidence integrity | high | mitigate | The no-evidence fixture snapshots the store before/after and asserts byte-identity (D-13) |
</threat_model>

<verification>
- `python tests/audio_export_roundtrip.py` and `python tests/daemon_roundtrip.py`
- Run the full suite (`python tests/*.py`) and record the result in 09.1-VALIDATION.md.
- Re-run `gsd-tools check.decision-coverage-plan` on the phase - must pass with all 16 decisions covered.
</verification>

<success_criteria>
- The transcript-diff, atomic-write, no-evidence, and both-surfaces fixtures all pass.
- The requirement and supply-chain audits are written and complete.
- The full suite passes and the decision-coverage gate reports all 16 decisions covered.
</success_criteria>

<output>
Create `.planning/phases/09.1-audio-drill-export/09.1-04-SUMMARY.md` when done.
</output>
