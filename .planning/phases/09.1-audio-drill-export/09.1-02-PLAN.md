---
phase: 09.1-audio-drill-export
plan: 02
type: execute
wave: 2
depends_on:
  - 09.1-01-PLAN.md
files_modified:
  - surfaces/audio_edge_tts.py
  - surfaces/audio_piper.py
  - surfaces/audio.py
  - surfaces/cli.py
  - requirements.txt
  - tests/audio_export_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - AUDIO-02
  - AUDIO-04
  - AUDIO-06
  - AUDIO-07
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "edge-tts and Piper are registered by name through the TTSEngines registry from plan 09.1-01; Kokoro is documented as a registration target for the 7900 XTX and is not built (D-02, AUDIO-02)."
    - "Each third-party artifact is pinned to a version with a recorded checksum and a named license review before any byte ships - Directive 4a; the edge-tts license (GPL-3.0 reported on PyPI) is read from the pinned version's actual license text at vendoring time and the posture is recorded, never assumed (D-15, AUDIO-07, 09.1-RESEARCH A2)."
    - "An unreachable or missing engine reports through EngineError with the engine name and reason; the command never silently falls back to another voice (D-04, AUDIO-04)."
    - "The edge-tts engine sends item text to a Microsoft endpoint, and the command's own help text states that disclosure - the same accepted risk already recorded for hosted models in CLAUDE.md (D-16, AUDIO-07)."
    - "MP3 is the default container: edge-tts streams MP3 natively with no encoder; the Piper engine converts its native WAV to MP3 through one pinned encoder (lameenc) only when the container setting says mp3, and passes WAV through when it says wav (D-09, AUDIO-06, 09.1-RESEARCH section 2)."
    - "Piper's Windows wheel gap is a recorded decision, not a silent workaround: if no clean wheel exists for the pinned Python, the engine uses the bundled binary/model path from the Phase 13 sidecar pattern and says so (09.1-RESEARCH A3)."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
    - "UI gate skipped visibly: ui-plan-gate reports frontend=true, which is a false positive (D-14 refuses a player/sync/mobile surface; the only user-facing copy is CLI refusal text locked by D-04/D-05/D-16), so this phase plans without a UI-SPEC per the 2026-08-10 handoff decision 1."
  prohibitions:
    - statement: "No engine may ship unpinned, unchecksummed, or without a named license review."
      status: unverified
      flagged: true
    - statement: "No silent engine fallback may exist; a failure must surface by name."
      status: unverified
      flagged: true
---

<objective>
As a learner, I want edge-tts for the best network voice and Piper for a local, offline voice behind the same interface - so switching engines is a configuration change, and I am never handed a different voice without being told.

Purpose: The two real engines. This plan registers edge-tts (network, MP3-native) and Piper (local, WAV-native with an engine-scoped MP3 path) through the plan 09.1-01 registry, resolves the supply-chain pins and license postures under Directive 4a, and lands the D-16 network disclosure in the command's own help text.
Output: surfaces/audio_edge_tts.py and surfaces/audio_piper.py, registry wiring, the pinned dependency set in requirements.txt, the help-text disclosure, and engine-level fixtures.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/09.1-audio-drill-export/09.1-CONTEXT.md
@.planning/phases/09.1-audio-drill-export/09.1-RESEARCH.md
@.planning/phases/09.1-audio-drill-export/09.1-01-PLAN.md
@.planning/phases/09-subject-invariant-loop-emt-math-cs-integration/09-03-PLAN.md
</context>

<interfaces>
- `TTSEngine` and `TTSEngines` from surfaces/audio.py (plan 09.1-01) are the only shape the engines implement; they are new modules plus a registry entry (D-01).
- `09-03-PLAN.md` is the KaTeX license-review precedent D-15 explicitly copies (pin + checksum + named license review).
- `requirements.txt` is where the pinned artifacts land; Phase 3.2's zstandard row is the format precedent.
- `surfaces/cli.py`'s export-audio parser carries the help text D-16 requires.
- The Phase 13 sidecar pattern (13-01/02-PLAN.md) is the fallback shape for Piper's bundled binary if the wheel gap is real.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `surfaces/audio_edge_tts.py` | `edge_tts()` engine factory: `container="mp3"`, `available()` (import + endpoint probe), `speak(text)` streaming MP3 bytes via the pinned edge-tts |
| `surfaces/audio_piper.py` | `piper()` engine factory: `container="wav"`, `available()` (import + model presence), `speak(text)` WAV bytes; MP3 conversion via pinned lameenc when the container setting says mp3 |
| `surfaces/audio.py` | Registry entries for `edge-tts` and `piper`; Kokoro documented as a registration target only |
| `surfaces/cli.py` | `--engine edge-tts` help text stating item text is sent to Microsoft's endpoint (D-16) |
| `requirements.txt` | Pinned edge-tts, piper (or its phonemize wheel), lameenc - each with checksum and license note, or an explicit recorded decision to ship without one |
| `tests/audio_export_roundtrip.py` | Engine fixtures: unavailable/missing dependencies refuse by name; edge-tts stream and Piper WAV/MP3 unit paths with fake network and fake subprocess |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The edge-tts engine - pin, license review, stream, and the help-text disclosure</name>
  <files>surfaces/audio_edge_tts.py, surfaces/audio.py, surfaces/cli.py, requirements.txt, tests/audio_export_roundtrip.py</files>
  <read_first>
    - `09.1-RESEARCH.md` section 1 (edge-tts row) and section 2 (streams MP3 natively), assumptions A1/A2 (endpoint reachable; license unresolved until read)
    - `09.1-CONTEXT.md` D-02, D-04, D-09, D-15, D-16
    - `09-03-PLAN.md` - the KaTeX pin + checksum + license-review precedent to copy exactly
    - `requirements.txt` - the Phase 3.2 zstandard row format
  </read_first>
  <behavior>
    - Test 1: `edge_tts()` registers by name, declares container "mp3", and `speak(text)` returns MP3 bytes from the pinned library (fake the endpoint via a stub communicator so the test is offline); the engine never transcribes, only speaks.
    - Test 2: when the pinned library is missing or the endpoint is unreachable, `available()` is False and `speak()` raises EngineError naming the engine and the reason - the command refuses by name, never silently substituting (D-04).
    - Test 3: the CLI help for `--engine edge-tts` contains the disclosure that item text is sent to Microsoft's endpoint (D-16); the disclosure is a tested string, not prose that can rot.
    - Test 4: requirements.txt pins edge-tts with a recorded checksum and a named license review note; the pinned version's actual license text is read at vendoring time and the posture (documented GPL optional engine vs MIT alternative) is recorded in the requirements note (Directive 4a, D-15).
  </behavior>
  <action>
Implement `surfaces/audio_edge_tts.py` following the TTSEngine interface: register `edge-tts` in the registry (D-02), container "mp3" (streams MP3 natively - no encoder, per research section 2), `available()` checks the pinned import and endpoint reachability, `speak(text)` streams the MP3 bytes and raises EngineError with engine name + reason on any failure (D-04). Add the D-16 disclosure sentence to the export-audio CLI help for the edge-tts engine and pin it with a test. Add the edge-tts dependency to requirements.txt pinned with checksum + license review per the 09-03 KaTeX precedent (D-15); the executor reads the pinned version's license text and records the posture before shipping any byte (research A2 - the PyPI record reports GPL-3.0, and the plan treats the license as unresolved until read). Write the four behavior tests first with the endpoint stubbed.
  </action>
  <verify>
    <automated>python tests/audio_export_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - edge-tts registers, speaks MP3, and refuses by name when missing or unreachable.
    - The network disclosure is in the command's own help text and tested.
    - The dependency is pinned, checksummed, and license-reviewed before shipping.
  </acceptance_criteria>
  <done>edge-tts is a registered, pinned, honestly-disclosed network engine with a named refusal path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Piper engine - local WAV, engine-scoped MP3, and the Windows wheel decision</name>
  <files>surfaces/audio_piper.py, surfaces/audio.py, requirements.txt, tests/audio_export_roundtrip.py</files>
  <read_first>
    - `09.1-RESEARCH.md` section 1 (Piper row) and section 2 (WAV native; MP3 needs lameenc), assumption A3 (Windows wheel gap) and A4 (lameenc covers the MP3 path)
    - `09.1-CONTEXT.md` D-02, D-09, D-15
    - `.planning/phases/13-desktop-packaging-tauri-sidecar/13-01-PLAN.md` and `13-02-PLAN.md` - the bundled-binary sidecar fallback shape
  </read_first>
  <behavior>
    - Test 1: `piper()` registers by name, declares container "wav", and `speak(text)` returns WAV bytes through a stubbed Piper invocation; when the model file or the phonemize wheel is absent, `available()` is False and `speak()` raises EngineError naming the engine and the missing piece (D-04).
    - Test 2: when the container setting is mp3, the engine's MP3 path converts the WAV through one pinned encoder (lameenc) and returns MP3 bytes; when the setting is wav, it passes WAV through untouched - the encoder is engine-scoped (D-09, research section 2).
    - Test 3: the Piper dependency and model artifact are pinned with checksum and license review (MIT per research, but each pinned model file is recorded); the Windows wheel verification is a recorded step - if no clean wheel exists for the pinned Python, the bundled-binary fallback is a recorded plan decision, not a silent workaround (research A3, D-15).
  </behavior>
  <action>
Implement `surfaces/audio_piper.py`: register `piper` in the registry (D-02), container "wav", `available()` checks the pinned import and the configured model file, `speak(text)` invokes Piper locally (stubbed in tests) and returns WAV bytes; when the container setting is mp3, convert through the pinned lameenc encoder (D-09, research section 2); any missing piece raises EngineError by name (D-04). Record the Windows wheel verification result and, if needed, the bundled-binary fallback per the Phase 13 sidecar pattern - as an explicit recorded decision (research A3). Add piper (or its wheel-bearing phonemize) and lameenc to requirements.txt pinned with checksum + license note per Directive 4a (D-15). Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/audio_export_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Piper registers, speaks WAV locally, and refuses by name when deps or models are missing.
    - The MP3 path is engine-scoped through one pinned encoder; WAV passes through.
    - Piper/lameenc pins and the wheel decision are recorded.
  </acceptance_criteria>
  <done>Piper is a registered local engine whose MP3 conversion is a pinned, recorded, engine-scoped decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The roster contract - three engines registered, Kokoro documented, no silent fallback</name>
  <files>surfaces/audio.py, surfaces/cli.py, tests/audio_export_roundtrip.py</files>
  <read_first>
    - `09.1-CONTEXT.md` D-01/D-02/D-03 (registry, roster, transcript-only) and D-04 (no silent fallback)
    - `09.1-RESEARCH.md` section 1 (the engine table)
    - `surfaces/audio.py` from plan 09.1-01 - the registry and transcript-only engine
  </read_first>
  <behavior>
    - Test 1: the registry contains exactly `edge-tts`, `piper`, and `transcript-only`; Kokoro appears only in the documented registration-target note and resolves to a named refusal if selected (D-02).
    - Test 2: with the configured engine unavailable, the command emits the transcript, names the engine and reason, and exits non-zero - and never substitutes another engine's voice (D-04, AUDIO-04).
    - Test 3: switching engines is a settings change only: the same command text produces the same pack shape through a different registered engine (D-01, AUDIO-01); the `--engine` flag overrides the settings entry without touching the schema.
  </behavior>
  <action>
Wire the roster contract in `surfaces/audio.py`: register edge-tts and piper alongside transcript-only, add the Kokoro registration-target documentation note (D-02), and assert the registry contents in a test. Confirm the named-refusal path from plan 09.1-01 composes with both real engines' EngineError (D-04). The `--engine` CLI flag selects by registry name and overrides the settings entry (D-01). Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/audio_export_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Exactly the three engines are registered; Kokoro is documented, not built.
    - No silent fallback exists at any level.
    - Engine switching is a settings/flag change with no code change.
  </acceptance_criteria>
  <done>The roster is complete and the interface guarantee holds: swap the engine, never the pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| item text -> engine | Item text leaves the machine when edge-tts is configured; the help text states it (D-16) |
| engine bytes -> filesystem | MP3/WAV bytes become files through the plan 09.1-01 writer |
| PyPI artifacts -> environment | Third-party wheels and models enter the install; pins and license reviews are the gate (D-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-091-05 | Information disclosure | edge-tts network call | high | mitigate | D-16 disclosure in the command's own help text; Piper/transcript-only are the local alternatives |
| T-091-06 | Tampering | third-party TTS artifacts | high | mitigate | Every artifact pinned with checksum + named license review (D-15, Directive 4a); the 09-03 KaTeX precedent is copied exactly |
| T-091-07 | Spoofing | engine substitution | medium | mitigate | Registry-by-name selection; no silent fallback exists (D-04) |
| T-091-08 | Tampering | Piper model files | medium | mitigate | Model artifacts pinned with checksum + license review, same as wheels (D-15) |
| T-091-SC | Tampering | dependency operations | high | mitigate | Pins are recorded in requirements.txt with the Phase 3.2 zstandard row as format precedent |
</threat_model>

<verification>
- `python tests/audio_export_roundtrip.py`
- Grep requirements.txt for every new pin and confirm each carries a checksum and a named license review note (D-15).
- Grep surfaces/audio*.py for any fallback-to-another-engine branch - must return none (D-04).
</verification>

<success_criteria>
- edge-tts and Piper are registered engines with pinned, license-reviewed artifacts.
- The network disclosure lives in the command's help text.
- No silent fallback exists; every failure names the engine and reason.
</success_criteria>

<output>
Create `.planning/phases/09.1-audio-drill-export/09.1-02-SUMMARY.md` when done.
</output>
