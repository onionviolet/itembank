---
phase: 09.1-audio-drill-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - surfaces/audio.py
  - surfaces/cli.py
  - schemas/settings.schema.json
  - itembank.json
  - tests/audio_export_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - AUDIO-04
  - AUDIO-05
  - AUDIO-06
  - AUDIO-07
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "One TTSEngine interface with named registered implementations; adding an engine is a new module plus a settings entry, never a branch in a dispatcher (D-01, AUDIO-01)."
    - "transcript-only is a real registered engine that always works with zero third-party dependencies, which is what makes 'refuse cleanly, still emit the transcript' fall out of the interface (D-02, D-03, AUDIO-02)."
    - "The pack sequence is stem -> timed pause -> key -> why, with pause durations read from a per-item-type settings map rather than a constant (D-06, D-07, AUDIO-03)."
    - "An unreachable or missing engine never silently falls back: the command writes the transcript, names the engine and the reason, and exits non-zero (D-04, AUDIO-04)."
    - "No audio file is ever half-written: audio goes to a temp path and is atomically renamed on success, matching runtime.write_session(); the transcript still writes when audio fails (D-05, AUDIO-04)."
    - "`itembank export audio --objective <id>` is the CLI surface, and the legacy flat `itembank export <bank> <out> --format <f>` form stays byte-compatible (D-08, AUDIO-05)."
    - "One plain-text transcript per pack is emitted always, containing the same text in the same order the audio speaks; files are named from the objective id and a content digest (D-11, D-12, AUDIO-06)."
    - "Listening records nothing: no evidence event exists for audio export, and the evidence store stays the record of what was actually answered (D-13, AUDIO-07)."
    - "No player, no sync, no mobile build, no playback position - the pack is a one-way export (D-14, AUDIO-07)."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
    - "UI gate skipped visibly: ui-plan-gate reports frontend=true, which is a false positive (D-14 refuses a player/sync/mobile surface; the only user-facing copy is CLI refusal text locked by D-04/D-05/D-16), so this phase plans without a UI-SPEC per the 2026-08-10 handoff decision 1."
  prohibitions:
    - statement: "No engine dispatch branch may be added; engines register by name through the registry."
      status: unverified
      flagged: true
    - statement: "No evidence write may be added; audio export must never touch the evidence store."
      status: unverified
      flagged: true
---

<objective>
As a learner on a commute, I want `itembank export audio --objective <id>` to turn an objective into a drill pack - stem, timed pause, key, why - plus a plain-text transcript, with an engine I can swap by configuration, and with the tool still working when no TTS engine is installed at all.

Purpose: The tracer plan. It proves the whole audio-export spine with zero third-party dependencies: the TTSEngine interface and registry (D-01), the transcript-only engine (D-02/D-03), the per-item-type pause settings (D-07), the sequence builder (D-06), the CLI surface (D-08), the transcript contract (D-11), digest naming (D-12), the named-refusal and atomic-write guarantees (D-04/D-05), and the no-evidence boundary (D-13/D-14).
Output: surfaces/audio.py (interface + registry + transcript-only + sequence builder + pack writer spine), the CLI command, the audio settings block in the schema and itembank.json, and tests/audio_export_roundtrip.py with fake-engine and transcript-only fixtures.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/09.1-audio-drill-export/09.1-CONTEXT.md
@.planning/phases/09.1-audio-drill-export/09.1-RESEARCH.md
@.planning/phases/09.1-audio-drill-export/09.1-DISCUSSION-LOG.md
@.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-01-PLAN.md
</context>

<interfaces>
- `runtime.write_session()` (runtime.py:178-185) is the write-temp-then-`os.replace` pattern D-05 copies.
- `runtime.answer_text()` (runtime.py:219-227) is the key renderer the sequence builder uses.
- `surfaces/cli.py`'s existing `export` parser is the parent surface; the legacy flat form must keep parsing identically (README.md:105-106).
- `surfaces/anki.py cmd_export()` is the existing export command whose refusal and write discipline this module mirrors.
- The model-backend adapter shape (Phase 8 plans, 08-01..06) is the registry idiom D-01 copies; do not invent a second one.
- `evidence.py` is read to confirm nothing in this phase writes to it (D-13).
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `surfaces/audio.py` | `TTSEngine` (interface), `TTSEngines` (registry), `transcript_only()` engine, `resolve_objective_items(bank, objective_id)`, `build_sequence(items, pause_map)`, `pack_transcript(sequence)`, `digest_name(objective_id, content)`, `write_pack(...)` (temp-then-rename spine), `cmd_export_audio(a)` |
| `surfaces/cli.py` | `itembank export audio <bank> --objective <id> [--out DIR] [--engine NAME] [--split per-pack\|per-item] [--container mp3\|wav]`; legacy flat export form unchanged |
| `schemas/settings.schema.json` | New `audio` object: `engine` (enum, default edge-tts), `container` (mp3/wav, default mp3), `split` (per-pack/per-item, default per-pack), `pause` (per-item-type map with documented starting values), `edge_tts.voice`, `piper.voice`/`piper.model` — all marked `x-itembank-phase: 9.1` |
| `itembank.json` | The `audio` defaults block |
| `tests/audio_export_roundtrip.py` | Fake-engine + transcript-only fixtures: sequence, transcript, atomicity, refusal, no-evidence |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The TTSEngine interface, the registry, and transcript-only</name>
  <files>surfaces/audio.py, tests/audio_export_roundtrip.py</files>
  <read_first>
    - `09.1-CONTEXT.md` D-01/D-02/D-03 (the interface, the roster, transcript-only as a real engine)
    - `09.1-RESEARCH.md` section 4 (the ordering risk: copy the registry idiom from Phase 8's plans, not shipped code)
    - `.planning/phases/08-model-adapter-interface-tier-gate-enforcement/08-01-PLAN.md` - the registry idiom to copy
    - `runtime.py` write_session() (the atomic pattern) and answer_text() (the key renderer)
  </read_first>
  <behavior>
    - Test 1: registering an engine is adding one module plus one settings entry - a fake engine registered by name resolves through `TTSEngines` with no branch in any dispatcher; the interface exposes `name`, `container`, `available()`, and `speak(text) -> bytes` (raising `EngineError(reason)` when it cannot).
    - Test 2: `transcript-only` is registered by name, `available()` is always True, and `speak()` returns None (it produces no audio) - it is a real engine, not an error path (D-03).
    - Test 3: an unknown engine name resolves to a named refusal listing the engine name - never a KeyError or a silent default (D-04).
  </behavior>
  <action>
Implement `surfaces/audio.py`: a `TTSEngine` interface (name, container, available(), speak(text) raising EngineError) and a `TTSEngines` registry mapping engine name to a factory, copying the Phase 8 model-backend registry shape (08-01-PLAN.md) rather than inventing a second idiom. Register `transcript-only` as a real engine returning None from speak() and always-available (D-02, D-03). An unknown engine name produces a named refusal. Write the three behavior tests first in `tests/audio_export_roundtrip.py`, following the repo's stdlib fail() harness; the fake engine used by later tests registers through the same registry.
  </action>
  <verify>
    <automated>python tests/audio_export_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The registry resolves engines by name with no dispatcher branches; transcript-only is a first-class entry.
    - Unknown engines refuse by name.
  </acceptance_criteria>
  <done>The interface and registry exist, transcript-only is a real registered engine, and the refusal path falls out of the interface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Objective resolution, the sequence builder, and the audio settings block</name>
  <files>surfaces/audio.py, schemas/settings.schema.json, itembank.json, tests/audio_export_roundtrip.py</files>
  <read_first>
    - `09.1-CONTEXT.md` D-06/D-07 (sequence and per-item-type pauses), integration points (objective ids from Phase 3.2's [OBJ:], else the Educational Objective line)
    - `schemas/settings.schema.json` model_backend block - the object-shape and x-itembank-phase marker pattern to copy for `audio`
    - `surfaces/settings.py` defaults_from_schema() - the schema default is the single source of truth
    - `.planning/phases/03.2-seeding-import-provenance/03.2-02-PLAN.md` - the [OBJ:] grammar this reads when 3.2 has shipped
  </read_first>
  <behavior>
    - Test 1: `resolve_objective_items(bank, objective_id)` returns the objective's items in bank order via the `[OBJ:]` grammar when present, else via the item `[OBJECTIVE:]`/Educational Objective line; an unknown objective id is a named refusal listing the id (degrade, never block - D-04's spirit).
    - Test 2: `build_sequence(items, pause_map)` yields stem -> timed pause -> key -> why per item (D-06) with the pause duration read from the per-item-type map (D-07), and the transcript text equals the sequence text in the same order (D-11's diffable contract).
    - Test 3: the settings schema validates the `audio` block: engine enum defaults to edge-tts, container to mp3, split to per-pack; a hand-edited invalid value is rejected by the one validator, and `itembank config` prints the new keys.
  </behavior>
  <action>
Implement objective resolution and the sequence builder in `surfaces/audio.py`. `resolve_objective_items` reads objective ids from Phase 3.2's `[OBJ:]` registry where available (03.2-02-PLAN.md grammar) and falls back to the item `[OBJECTIVE:]`/Educational Objective line, refusing unknown ids by name (D-04). `build_sequence` emits one segment per field - stem, key (via runtime.answer_text), why (q.get("why") or q.get("disc")) - with a timed pause after the stem per D-06, duration from the per-item-type `pause` map (D-07). Add the `audio` block to `schemas/settings.schema.json` (engine/container/split enums with defaults, per-item-type pause map with recorded starting values: mc 1.5s, multi 2.0s, short 2.0s, cloze 2.5s, build 4.0s, table 3.0s, dnd 3.0s, check 3.0s - starting values tuned against real use, not invented precisely) plus edge_tts.voice and piper.voice/model, all marked x-itembank-phase 9.1; add the defaults to itembank.json. Write the three behavior tests first.
  </action>
  <verify>
    <automated>python tests/audio_export_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Objective resolution is bank-ordered with named refusals; the sequence is stem -> pause -> key -> why with per-type pauses.
    - The audio settings block validates and prints through the one settings surface.
  </acceptance_criteria>
  <done>An objective resolves to a pause-sequenced drill, and the engine/container/split/pause settings are part of the one documented config contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The CLI command, transcript, digest naming, atomic write, and no-evidence boundary</name>
  <files>surfaces/audio.py, surfaces/cli.py, tests/audio_export_roundtrip.py</files>
  <read_first>
    - `09.1-CONTEXT.md` D-04/D-05 (named refusal, atomic write), D-08 (CLI + daemon route - this task is the CLI half), D-11/D-12 (transcript + digest naming), D-13/D-14 (no evidence, no player)
    - `surfaces/cli.py` - the existing export parser (line ~277) and how a surface cmd_* registers
    - `surfaces/anki.py` - the export command's refusal and write discipline to mirror
    - `runtime.py` write_session() - the temp-then-os.replace pattern
    - `evidence.py` - read to confirm no evidence write exists or is added
  </read_first>
  <behavior>
    - Test 1: `itembank export audio <bank> --objective <id> --engine transcript-only` writes the transcript file and exits 0 with no audio file; the legacy flat `itembank export <bank> <out> --format basic` still works byte-identically (README form).
    - Test 2: a fake engine that raises EngineError mid-run leaves no partial audio file (the temp path is cleaned up), the transcript still writes, and the exit code is non-zero with the engine name and reason in the message (D-04/D-05).
    - Test 3: the transcript file is named from the objective id and a content digest (sha256 short form); re-exporting an unchanged objective produces the same name, and the audio bytes land via temp-then-os.replace.
    - Test 4: after a successful export, the evidence store (evidence.py's log) is byte-identical - listening records nothing (D-13).
  </behavior>
  <action>
Register the `audio` subcommand on the existing `export` parser in `surfaces/cli.py` such that `itembank export audio <bank> --objective <id> [--out DIR] [--engine NAME] [--split ...] [--container ...]` dispatches to `cmd_export_audio`, while the legacy flat `itembank export <bank> <out> --format <f>` keeps parsing and behaving exactly as today (README.md:105-106 and surface_roundtrip tests must pass unchanged). Implement `cmd_export_audio` in `surfaces/audio.py`: resolve the objective, build the sequence, call the configured engine through the registry, write the transcript always, write audio temp-then-rename with `os.replace` (D-05), name files from objective id + content digest (D-12), and on EngineError write the transcript, print a named refusal, and exit non-zero (D-04). Nothing in this module imports or calls evidence.py (D-13); no player, sync, or position is produced (D-14). Write the four behavior tests first.
  </action>
  <verify>
    <automated>python tests/audio_export_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The audio CLI works with transcript-only (zero dependencies) and the legacy export form is unchanged.
    - Failed engines leave no partial audio, still write the transcript, and exit non-zero by name.
    - Names are digest-stable; the evidence store is untouched.
  </acceptance_criteria>
  <done>The tracer proves the whole spine with transcript-only: an objective becomes a digest-named pack plus transcript, or a named refusal with the transcript intact - and the evidence store never sees it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| bank markdown -> sequence | Bank content becomes text spoken aloud and written to a transcript; the sequence builder is the reading surface |
| engine output -> filesystem | Engine bytes become files; the temp-then-rename path is the only writer |
| export command -> evidence store | The command must never write evidence (D-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-091-01 | Spoofing | engine name from settings | medium | mitigate | Engine names resolve through the registry by exact name; unknown names refuse (D-04) |
| T-091-02 | Integrity | partial audio on engine failure | high | mitigate | temp-then-`os.replace` (D-05); failure cleans the temp path |
| T-091-03 | Information disclosure | item text sent to an engine | high | mitigate | CLI help states the network disclosure (D-16, landed with the engines in 09.1-02); transcript-only and Piper are the local alternatives |
| T-091-04 | Tampering | evidence store | high | mitigate | No evidence import or write exists in this module (D-13); the no-evidence fixture asserts byte-identity |
| T-091-SC | Tampering | third-party TTS dependencies | high | mitigate | Engines and their pins land in 09.1-02 under Directive 4a (D-15) |
</threat_model>

<verification>
- `python tests/audio_export_roundtrip.py`
- Grep surfaces/audio.py for any evidence.py import or write call - must return none (D-13).
- Run the legacy export command against a fixture bank and diff its output against a pre-change run - must be byte-identical (D-08).
</verification>

<success_criteria>
- The interface and registry exist; transcript-only is a real registered engine.
- `itembank export audio --objective <id>` works with zero third-party dependencies.
- Named refusal, atomic writes, transcript-always, digest naming, and no-evidence are proven by fixtures.
</success_criteria>

<output>
Create `.planning/phases/09.1-audio-drill-export/09.1-01-SUMMARY.md` when done.
</output>
