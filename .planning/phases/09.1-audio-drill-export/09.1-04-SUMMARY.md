---
phase: 09.1-audio-drill-export
plan: 04
subsystem: audio-export
tags: [verification, transcript-diff, no-evidence, supply-chain-audit, requirement-audit, full-suite]

requires:
  - phase: 09.1-audio-drill-export
    provides: plans 09.1-01..03's shipped modules and fixtures; the fake-engine registration seam; the daemon route
provides:
  - tests/audio_export_roundtrip.py: transcript-diff across split/container modes, no-evidence success+failure, surface inventory, failure-path refusal contract (completed verification fixtures)
  - tests/daemon_roundtrip.py: route/CLI parity + invalid-body refusals (already landed in plan 09.1-03; re-verified)
  - .planning/phases/09.1-audio-drill-export/09.1-VALIDATION.md: the requirement audit (AUDIO-01..07 -> artifact/test), the supply-chain audit (pin + checksum + license per artifact), and the D-01..D-16 decision-coverage record
affects: [phase UAT, HANDOFF-PHASE09.1]

actuals:
  tokens: 16000
  tasks: 3
  commits: 1

tech-stack:
  added: []
  patterns:
    - Transcript-diff as a recoverable check: the fake engine embeds the exact spoken text in its audio bytes, so 'audio speaks == transcript text in order' is asserted from the file, not claimed
    - No-evidence as a before/after byte-identity snapshot over BOTH a successful and a failing-engine export
    - Surface inventory as a structural grep: no player/sync/mobile/position symbol may exist in the shipped audio modules
    - Written audits (requirement + supply chain + decision coverage) rather than claims in a summary

key-files:
  created:
    - .planning/phases/09.1-audio-drill-export/09.1-VALIDATION.md
  modified:
    - tests/audio_export_roundtrip.py

key-decisions:
  - "The transcript-diff fixture drives the full export_audio path (not assemble_pack directly) so the transcript file on disk is part of the diff."
  - "The no-evidence fixture runs a failing-engine export too -- a refusal must also leave the store byte-identical (D-13)."
  - "The surface-inventory check greps the three shipped audio modules for player/playback/position_ms/sync/mobile/podcast_player -- D-14 is structural, not prose."
  - "The failure-path sweep collects all three registry refusal paths (unknown engine, missing dependency, unreachable endpoint) and asserts each names its engine."

patterns-established:
  - "Verification fixtures recover ground truth from the artifact (audio bytes, evidence file, module source), never from the code under test's own claims."

requirements-completed: [AUDIO-01, AUDIO-02, AUDIO-03, AUDIO-04, AUDIO-05, AUDIO-06, AUDIO-07]

coverage:
  - id: D1
    description: "The transcript-diff fixture proves the pack's spoken text equals the transcript's text in the same order across per-pack/per-item and mp3/wav -- D-11 is a diffable check, not a claim (AUDIO-06)."
    requirement: AUDIO-06
    verification:
      - kind: integration
        ref: "tests/audio_export_roundtrip.py#test_transcript_diff_all_modes_and_containers"
        status: pass
    human_judgment: false
  - id: D2
    description: "The no-evidence fixture snapshots the evidence store before and after BOTH a successful export and a failing-engine export -- byte-identical; nothing is recorded (D-13, AUDIO-07)."
    requirement: AUDIO-07
    verification:
      - kind: integration
        ref: "tests/audio_export_roundtrip.py#test_no_evidence_success_and_failure"
        status: pass
    human_judgment: false
  - id: D3
    description: "The atomic-write fixture proves a failing engine leaves no partial audio file and the transcript still writes; the failure-path sweep shows every registry refusal path (unknown engine, missing dependency, unreachable endpoint) uses the same named-refusal contract (D-04/D-05, AUDIO-04)."
    requirement: AUDIO-04
    verification:
      - kind: unit
        ref: "tests/audio_export_roundtrip.py#test_failure_paths_same_named_refusal_contract"
        status: pass
      - kind: unit
        ref: "tests/audio_export_roundtrip.py#test_assemble_pack_container_digest_and_atomicity"
        status: pass
    human_judgment: false
  - id: D4
    description: "The surface inventory contains only the CLI command and the daemon route -- no player, no sync, no mobile build, no playback position symbol anywhere in surfaces/audio*.py (D-14, AUDIO-07)."
    requirement: AUDIO-07
    verification:
      - kind: unit
        ref: "tests/audio_export_roundtrip.py#test_surface_inventory_no_player_sync_mobile"
        status: pass
    human_judgment: false
  - id: D5
    description: "The requirement audit maps every AUDIO-01..AUDIO-07 to a shipped artifact or test with none orphaned; the supply-chain audit lists every third-party artifact (edge-tts, lameenc, Piper decision) with pin, checksum, and named license review; the decision-coverage record covers D-01..D-16 (09.1-VALIDATION.md)."
    requirement: AUDIO-01
    verification:
      - kind: files
        ref: ".planning/phases/09.1-audio-drill-export/09.1-VALIDATION.md"
        status: pass
    human_judgment: false
  - id: D6
    description: "The full repo suite passes with the audio modules present; the only two failures are documented pre-existing environment gaps (model_surface_roundtrip needs the uncommitted 08-04 feat; packaging_roundtrip needs the PyInstaller build), neither touching audio code."
    requirement: AUDIO-07
    verification:
      - kind: integration
        ref: "tests/daemon_roundtrip.py"
        status: pass
      - kind: integration
        ref: "tests/audio_export_roundtrip.py"
        status: pass
    human_judgment: false

duration: ~40min
completed: 2026-08-11
status: complete
---

# Phase 09.1 — Plan 09.1-04: Phase verification — transcript-diff, no-evidence, surface inventory, failure-path contract, and the written audits

**The phase closes with fixture-backed proof for every requirement and every locked decision: a pack speaks exactly what its transcript says, listening records nothing (success or failure), no player/sync/mobile exists, every registry refusal names its engine, and the requirement/supply-chain/decision audits are written, not claimed.**

## Performance

- **Duration:** ~40 min
- **Completed:** 2026-08-11
- **Tasks:** 3/3
- **Files modified:** 1 test file updated, 1 audit document created

## Accomplishments
- `test_transcript_diff_all_modes_and_containers`: a `_WordEngine` embeds the exact spoken text in its audio bytes; the fixture recovers it from the written files and asserts it equals the transcript in order across per-pack/per-item × mp3/wav (D-11 diffable contract), plus the on-disk transcript file equals the pack transcript.
- `test_no_evidence_success_and_failure`: the evidence log is byte-identical before/after a successful export AND after a failing-engine export (D-13).
- `test_surface_inventory_no_player_sync_mobile`: structural grep over the three shipped audio modules for player/playback/position_ms/sync/mobile/podcast_player — none may exist (D-14).
- `test_failure_paths_same_named_refusal_contract`: all three registry refusal paths (unknown engine, missing dependency, unreachable endpoint) raise EngineError naming the engine (D-04).
- `09.1-VALIDATION.md`: requirement audit (AUDIO-01..07 → artifact/test), supply-chain audit (edge-tts, lameenc, Piper decision — pin + checksum + named license review), D-01..D-16 decision-coverage record, full-suite record, and validation sign-off.

## Task Commits

1. **Task 1: transcript-diff and atomic-write fixtures** — `252a65b`
2. **Task 2: no-evidence fixture and the both-surfaces proof** — `252a65b`
3. **Task 3: requirement and supply-chain audits, and the full suite** — `252a65b` (audit document committed separately below)

**Plan metadata:** one atomic commit per plan; `test(09.1-04)` + `docs(09.1-04)`.

## Files Created/Modified
- `tests/audio_export_roundtrip.py` - 4 new verification fixtures
- `.planning/phases/09.1-audio-drill-export/09.1-VALIDATION.md` - requirement/supply-chain/decision audits + full-suite record

## Decisions Made
- Verification recovers ground truth from the artifact (audio bytes, evidence file, module source), never from the code under test's own claims.
- The no-evidence fixture covers both success and failure.
- The surface inventory is structural.

## Deviations from Plan

### Auto-fixed Issues

**1. Transcript-diff fixture drove the wrong entry point**
- **Found during:** Task 1 (transcript-diff fixture)
- **Issue:** The first draft called `assemble_pack` directly, which writes only audio; the plan's transcript-diff fixture must also verify the transcript file on disk, which `export_audio` writes before the engine attempt (D-04/D-11).
- **Fix:** The fixture registers a `fake-word` engine through the registry and drives the full `export_audio` path with per-mode settings (engine/container/split). The diff then covers both the audio-embeds-text check and the on-disk transcript file.
- **Files modified:** tests/audio_export_roundtrip.py (test only)
- **Verification:** `test_transcript_diff_all_modes_and_containers` passes
- **Committed in:** `252a65b`

**2. Full suite exceeds the 2-minute foreground cap — run per-suite**
- **Found during:** Task 3 (full suite)
- **Issue:** The plan's `python tests/*.py` glob cannot run in one foreground call in this environment (each suite spawns its own subprocess; total > 2 min).
- **Fix:** Ran every suite independently in slices; recorded the results in 09.1-VALIDATION.md. All audio-relevant and repo suites pass; the two documented environment gaps are unchanged.
- **Files modified:** none (verification only)
- **Verification:** per-suite runs, recorded in 09.1-VALIDATION.md
- **Committed in:** n/a

---

**Total deviations:** 2 auto-fixed (1 test-entry-point correction, 1 suite-execution method)
**Impact on plan:** No scope creep; all Task 1-3 acceptance criteria met.

## Issues Encountered
- The two pre-existing environment gaps (model_surface_roundtrip, packaging_roundtrip) are unchanged and documented; neither touches audio code.

## User Setup Required

None.

## Next Phase Readiness
- Phase 09.1 is verification-complete: 24 audio fixtures + 61 daemon checks + the written audits. Ready for UAT, the phase VERIFICATION report, config.json/STATE.md update, and the handoff document.

---
*Phase: 09.1-audio-drill-export*
*Completed: 2026-08-11*

---

# Self-Check

- [x] Task 1: transcript-diff holds in every split/container combination; no failure path leaves partial audio; all refusals name the engine and reason
- [x] Task 2: exports leave the evidence store byte-identical (success and failure); route and CLI are one implementation; the surface inventory has exactly the two surfaces
- [x] Task 3: every AUDIO requirement and D-decision is traceable in the written audit; every third-party artifact is pinned, checksummed, and license-reviewed; the full suite passes (modulo documented environment gaps)
- [x] Verification list green: `python tests/audio_export_roundtrip.py`, `python tests/daemon_roundtrip.py`, full suite record in 09.1-VALIDATION.md
- [x] SUMMARY.md written; committed as `test(09.1-04)` + docs commit

**Self-Check: PASSED**
