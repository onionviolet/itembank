---
status: partial
phase: 06.1-interactive-visual-assessment-protocol
source: [06.1-VALIDATION.md, 06.1-VERIFICATION.md]
started: 2026-08-11
updated: 2026-08-11T06:30:00Z
---

## Current Test

[automated layer complete; manual interaction matrix pending physical device]

## Tests

### 1. Keyboard-only plot and number-line completion
expected: Without a mouse, Tab reaches the SVG and the semantic controls; arrows step by declared units; Enter/Space commits; Escape cancels an uncommitted move; the committed state and final verdict are announced in order; the item can be answered entirely without a pointer.
result: pending
source: manual
evidence: "Structural proxies automated: asVisual sets SVG tabindex=0, arrow keys step valueTicks by one unit, Enter/Space call commitMove, Escape calls revertTentative, status region is role=status aria-live=polite, Check response submits the committed state. The real keyboard walkthrough needs a live browser and is recorded here on completion."

### 2. Touch and non-drag equivalence
expected: Tap places/selects, a changed drag commits on pointer-up, a cancelled drag appends nothing, the native stepper/select path reproduces every state; only successful changed commits appear in the evidence query.
result: pending
source: manual
evidence: "Structural proxies automated: pointerup after changed drag calls commitMove, pointercancel/pointerleave restore the committed state, unchanged commits are explicit no-ops, and the four protocol action types are the only commits. Touch-emulation walkthrough pending a live device."

### 3. Screen-reader graphic/state feedback
expected: The SVG's name/description, native state controls, committed-state status, error observation, and final verdict read sensibly; no private accepted value or tolerance is announced.
result: pending
source: manual
evidence: "Structural proxies automated: SVG has role=img + aria-label from accessibility.description, one polite live region, text-plus-structure verdicts, and served public payloads carry no key/scoring material (sentinel scans in tests/visual_roundtrip.py). Narrator/NVDA/VoiceOver walkthrough pending assistive tech."

### 4. Responsive, narrow, and zoomed layout
expected: Both interactions complete at 1280, 768, and 375 CSS px, then at the strict 320 CSS px with 200% zoom; instructions, semantic SVG, operable HTML controls, status, and the primary action stay in the same DOM order with no page-level horizontal scroll.
result: pending
source: manual
evidence: "Automated layer covers the structural contract (single serializer, committed/tentative reducer, semantic controls not hidden when SVG runs, no canvas, offline refusal). Pixel-level reflow/no-horizontal-scroll judgment at the four widths requires rendering; screenshots to be attached on completion."

### 5. Public payload and static/degraded refusal
expected: The browser JSON/DOM for ready/tentative/committed/error/final states contains no accepted state, tolerance, key, solution path, forged authority field, or private scorer data; the static/offline build refuses interaction loudly, keeps prompt/scene/accessibility text readable, and directs the learner to the served runtime without a dead control or invented verdict.
result: pass (automated)
source: automated
evidence: "Served public payloads pass sentinel scans in tests/visual_roundtrip.py and tests/visual_accessibility_roundtrip.py; page_item(offline=True) returns served_required with no key; the static build page renders asVisualOffline with the exact served-runtime-required copy and no 'accepted'/'tolerance'/'partial_credit'/'key' material (tests/visual_accessibility_roundtrip.py check_offline_build_page)."

### 6. Modality equivalence end to end
expected: Mouse, touch, keyboard, and adjacent semantic controls reach the same committed action, canonical response, runtime observation, and verdict; equivalent states produce byte-identical canonical SCALAR action requests.
result: pass (automated)
source: automated
evidence: "One serializer and one committed/tentative reducer serve every input path in asVisual (asserted structurally); tests/visual_evidence_roundtrip.py proves CLI and /api/interact return identical observations for the same committed action."

### 7. GIFT refusal end to end
expected: Default and strict GIFT export refuse plot/number-line items by original item number with gift.type_unsupported; no lossy approximation; expressible items still export.
result: pass
source: automated
evidence: "tests/gift_export_roundtrip.py test_visual_items_refused_by_item_number — Q2/Q3 refused by number with the stable code in both modes, Q1 exported intact, no private/scene sentinel in the partial document."

## Notes

The four manual-only checks (keyboard, touch, screen-reader, responsive/zoom
matrix) are per 06.1-VALIDATION.md's Manual-Only Verifications: they require
real interaction with a live served fixture. Their structural proxies are all
automated and green; the human walkthroughs are recorded here (or in
06.1-VERIFICATION.md) once a physical device/browser and assistive tech are
available. This document stays `status: partial` until then.
