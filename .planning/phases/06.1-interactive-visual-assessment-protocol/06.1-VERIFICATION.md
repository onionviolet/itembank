---
phase: 06.1-interactive-visual-assessment-protocol
verified: 2026-08-11T06:30:00Z
status: passed
score: 8/8 tasks verified
behavior_unverified: 0
---

# Phase 06.1: Interactive Visual Assessment Protocol — Verification Report

**Phase Goal:** Prove one vertical visual-assessment tracer: declarative plot and
number-line items become key-free semantic public payloads, accept
modality-independent semantic actions, are scored only by the runtime, and
append meaningful committed-action evidence.
**Verified:** 2026-08-11T06:30:00Z
**Status:** passed

## Goal Achievement

### Observable Truths (per 06.1-VALIDATION.md task map)

| # | Task | Secure/observable behavior | Status | Evidence |
|---|------|---------------------------|--------|----------|
| 1 | 06.1-01-T1 | One declarative plot reaches the key-free public contract, semantic SVG/HTML response, sole runtime scorer, normalized result, and response evidence; author code and forged authority fields fail closed | ✅ VERIFIED | `tests/visual_roundtrip.py` — parser → public contract (sentinel scan) → scoring → `interaction_result` → served submit + evidence; forged `hint_tier`/`score` fields rejected by the strict grammar |
| 2 | 06.1-01-T2 | Plot and number line share one contract and canonical SCALAR serializer; point/interval responses are domain data rather than pixels | ✅ VERIFIED | `tests/visual_roundtrip.py` — one `interaction_contract` envelope, locked wire shapes `{"kind":"point","x":"2","y":"3"}`, `{"kind":"numberline_point","value":"1/2"}`, interval with closure swap |
| 3 | 06.1-01-T3 | `fractions.Fraction` normalization, snapped-grid alignment, exact tolerance borders, invalid numeric rejection, and Phase-6-bounded feedback intent are runtime-owned | ✅ VERIFIED | `tests/visual_roundtrip.py` — `canonical_scalar` grammar, 1/2 boundary pass + adjacent fail, exponent/NaN/precision/domain refusal, `visual_observation` entitlement input |
| 4 | 06.1-02-T1 | SPEC/schema/golden artifacts define the closed grammar; named field-addressed lint errors reject executable, malformed, inaccessible, and unsupported items | ✅ VERIFIED | `tests/visual_authoring_roundtrip.py` — SPEC docs, 12 `item.visual_*` codes, 13 invalid families each field-addressed, item.schema.json scoring-material refusal |
| 5 | 06.1-02-T2 | Exactly the locked successful state-changing commits append; duplicate retry is idempotent, conflicting ID reuse fails, and evidence contains no pointer/private authority data | ✅ VERIFIED | `tests/visual_evidence_roundtrip.py` — locked event field set, already_recorded/conflict, per-session action count, banned-material scan |
| 6 | 06.1-02-T3 | CLI and `/api/interact` accept the exact five-field request, resolve authority server-side, share `do_interact`, and return the same observation/evidence result | ✅ VERIFIED | `tests/visual_evidence_roundtrip.py` — CLI/API parity, authority-field 400 refusal, refusals (id/version/action/state/domain/nonvisual) |
| 7 | 06.1-03-T1 | Mouse, touch, keyboard, and semantic controls emit byte-identical commits/responses; cancellation appends nothing; SVG retains operable HTML fallback and static mode leaks no key | ✅ VERIFIED (automated layer) | `tests/visual_accessibility_roundtrip.py` — one serializer, committed/tentative reducer, pointercancel/Escape restore, no canvas, offline `served_required` refusal |
| 8 | 06.1-03-T2 | Default and strict GIFT paths refuse plot/numberline by original item number and stable code without lossy output | ✅ VERIFIED | `tests/gift_export_roundtrip.py` — `test_visual_items_refused_by_item_number`, both modes, expressible half intact, no private/scene leakage |

**Score:** 8/8 tasks verified (automated layer). The manual-only layer of
06.1-03-T1 (keyboard-only and touch-emulation walkthrough, screen-reader
announcements, 1280/768/375 CSS px plus the strict 320 CSS px at 200% zoom
case) is a real-interaction human check; its structural proxies are automated
and the manual matrix is recorded in 06.1-UAT.md, not claimed as automated
evidence here.

## Required Artifacts (per plan must_haves)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `fixtures/visual_bank.md` | `[TYPE: visual]` synthetic plot + numberline | ✅ EXISTS + SUBSTANTIVE | integer plot grid, half-step numberline point/interval, tolerance case |
| `tests/visual_roundtrip.py` | `def main` end-to-end protocol/leakage/scoring/evidence | ✅ EXISTS + SUBSTANTIVE | parser → contract → grammar → scoring → observation → served submit |
| `runtime.py` | `VISUAL_PROTOCOL_VERSION`, visual projection/canonicalization/scoring/observation/result | ✅ EXISTS + SUBSTANTIVE | protocol integer 1, SCALAR grammar, `_visual_verdict` under `score_response`, `visual_observation`, `interaction_result` |
| `surfaces/quiz_page.py` | `asVisual` renderer-registry adapter | ✅ EXISTS + SUBSTANTIVE | committed/tentative state, one serializer, `/api/interact` commits, `asVisualOffline` |
| `schemas/visual_interaction.schema.json` | `x-itembank-version` request/observation/event contract | ✅ EXISTS + SUBSTANTIVE | five-field request, semantic_state, observation, visual_action_event |
| `fixtures/visual_authoring_golden.md` | `[INTERACTION:` valid + isolated invalid cases | ✅ EXISTS + SUBSTANTIVE | 2 valid + 13 invalid families |
| `tests/visual_authoring_roundtrip.py` | `def main` SPEC/schema/lint/golden proof | ✅ EXISTS + SUBSTANTIVE | contract-only consumer, no quiz_page import |
| `tests/visual_evidence_roundtrip.py` | `def main` event/action/dedupe/CLI-API/query proof | ✅ EXISTS + SUBSTANTIVE | 6 checks including CLI/API parity and schema validation |
| `tests/visual_accessibility_roundtrip.py` | `def main` modality/serializer/cancel/refusal/reflow proxies | ✅ EXISTS + SUBSTANTIVE | 7 checks incl. static build page |
| `fixtures/visual_gift_bank.md` | mixed expressible/plot/numberline | ✅ EXISTS + SUBSTANTIVE | Q1 mc + Q2 plot + Q3 numberline |

## Threat Register Disposition (06.1 plans)

| Threat | Severity | Disposition | Evidence |
|--------|----------|-------------|----------|
| T-06.1-01 Information Disclosure (public projection) | high | mitigated | public payload sentinel scans in all three visual tests |
| T-06.1-02 Tampering (submit/interact envelope) | high | mitigated | strict wire grammar rejects forged fields; server-side authority in `do_interact`; API authority-field 400 |
| T-06.1-03 Elevation of Privilege (bank config) | high | mitigated | closed VISUAL/SCORING allowlists, `_reject_visual_exec`, lint `item.visual_executable_member` |
| T-06.1-04 DoS (normalization) | medium | mitigated | SCALAR numerator/denominator bounds, 201-tick cap |
| T-06.1-05 EoP (authoring grammar) | high | mitigated | golden fixture + field-addressed lint codes |
| T-06.1-06 Tampering (`/api/interact`) | high | mitigated | exact five-field request, server-side session/item resolution, observation/tier from runtime only |
| T-06.1-07 Information Disclosure (action evidence query) | high | mitigated | positive allowlist event shape; banned-material scan |
| T-06.1-08 DoS (interaction stream) | medium | mitigated | commit-only API, no movement-stream route, dedupe |
| T-06.1-09 Tampering (modality handlers) | high | mitigated | one reducer/serializer shared by every input path |
| T-06.1-10 DoS (pointer/touch) | medium | mitigated | explicit commits only; cancel restores; unchanged no-op |
| T-06.1-11 Information Disclosure (static/offline) | high | mitigated | `page_item` `served_required` refusal, no key, honest copy |
| T-06.1-12 Tampering (GIFT export) | high | mitigated | `gift.type_unsupported` refusal in both modes, item-number-addressed |
| T-06.1-SC Supply chain | low | accepted | stdlib + browser-native only |

## Full-Suite Regression

Every test that passes at the base commit (`5aab199`) still passes with all
three plans applied. Two pre-existing failures are unrelated to this phase:
`tests/model_surface_roundtrip.py` fails at the base commit itself (it
references `session.do_rubric_review`, which landed on `main` after this
branch's base was cut), and `tests/packaging_roundtrip.py` requires the
PowerShell-built `dist/itembank-sidecar-onedir` artifact that does not exist in
this checkout. `tests/daemon_roundtrip.py` has a flaky hostile-path check under
parallel temp-dir pressure and passes standalone.

**Approval:** pending — manual UI matrix in 06.1-UAT.md before final sign-off.
