---
phase: 06.1-interactive-visual-assessment-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model.py
  - runtime.py
  - surfaces/quiz_page.py
  - fixtures/visual_bank.md
  - tests/visual_roundtrip.py
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-05, VIS-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A served plot item travels from declarative bank fields through a key-free interaction contract, an SVG/HTML response, runtime.score_response(), and the existing append-only response evidence"
    - "Plot and number-line items use one protocol envelope and submit semantic coordinates, points, or intervals rather than pixels or renderer objects"
    - "Accepted states, tolerance, misconception mapping, reveal text, and solution paths never appear in a pre-answer public payload"
    - "Exact and tolerance-boundary verdicts are deterministic, dichotomous, and independent of viewport or input modality"
    - "The runtime returns a structured observation and Phase-6-bounded feedback intent without delegating verdict or tier selection to the browser or an agent"
  artifacts:
    - path: "fixtures/visual_bank.md"
      provides: "Synthetic plot and number-line items using the declarative visual grammar"
      contains: "[TYPE: visual]"
    - path: "tests/visual_roundtrip.py"
      provides: "End-to-end protocol, leakage, canonicalization, scoring, and evidence assertions"
      contains: "def main"
    - path: "runtime.py"
      provides: "Visual public projection, semantic canonicalization, deterministic score, observation, and bounded feedback intent"
      contains: "VISUAL_PROTOCOL_VERSION"
    - path: "surfaces/quiz_page.py"
      provides: "Renderer-registry adapter for declarative plot/number-line contracts"
      contains: "asVisual"
  key_links:
    - from: "model.py"
      to: "runtime.py"
      via: "the private parsed visual scene/scoring envelope is projected by public_item and interpreted only by runtime scoring helpers"
      pattern: "visual"
    - from: "runtime.py"
      to: "surfaces/quiz_page.py"
      via: "interaction_contract.renderer_config and response_schema drive asVisual without renderer-source knowledge"
      pattern: "interaction_contract"
    - from: "surfaces/quiz_page.py"
      to: "runtime.py"
      via: "the existing submit API carries interaction_version plus a semantic response and consumes interaction_result"
      pattern: "interaction_version"
    - from: "runtime.py"
      to: "evidence.py"
      via: "the existing Phase-5 submit/evidence path records the canonical semantic response and interaction protocol version"
      pattern: "interaction_result"
---

<objective>
Establish the one visual interaction protocol and prove it end to end with a real plot response before expanding the same contract to number lines and tolerance-aware feedback.

Purpose: Make the reusable asset a semantic, agent-observable assessment contract rather than a renderer widget. Phase 4 supplies the behavior-free semantic presentation seam, Phase 5 supplies the renderer-independent `interaction_contract`, and Phase 6 supplies the runtime feedback-policy entitlement; this plan connects them without moving scoring or answer release into the browser.
Output: The declarative visual grammar, key-free public projection, shared SVG/HTML plot/number-line adapter, deterministic runtime scorer/observation shape, synthetic fixtures, and an executable end-to-end contract test.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06.1-interactive-visual-assessment-protocol/06.1-RESEARCH.md
@.planning/phases/04-surface-redesign-theming/04-04-PLAN.md
@.planning/phases/05-check-item-type-code-editor/05-01-PLAN.md
@.planning/phases/05-check-item-type-code-editor/05-05-PLAN.md
@.planning/phases/06-hint-ladder-cursor-hold-feedback-modes/06-CONTEXT.md

<interfaces>
Upstream contracts to consume after Phases 4-6 execute:

- `presentation.render_surface(view, adapter=None) -> html`; enhancement hosts retain equivalent semantic fallback children (Phase 4 D-03 and plan 04-04).
- `runtime.public_item(q) -> interaction_contract` with integer `version`, `type`, declarative `renderer_config`, and `response_schema` (Phase 5).
- `runtime.interaction_result(q, response, verdict, observations) -> dict` is the normalized result envelope consumed by browser and agent submit paths (Phase 5).
- Phase 6's one policy transition returns explicit actions and owns the highest feedback/hint tier permitted for the current session (Phase 6 D-01/D-02).
- `runtime.score_response(q, answer) -> bool` remains the only verdict authority.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive one declarative plot point through every layer</name>
  <precondition>Phases 4, 5, and 6 are complete: `surfaces/presentation.py` exists, `runtime.public_item()` emits the Phase-5 interaction contract, and the Phase-6 feedback-policy tests pass.</precondition>
  <reversibility rating="costly">The versioned interaction envelope becomes a shared author/browser/agent contract; changing it requires coordinated schema and consumer updates, so extend it additively.</reversibility>
  <files>model.py, runtime.py, surfaces/quiz_page.py, fixtures/visual_bank.md, tests/visual_roundtrip.py</files>
  <behavior>
    - A synthetic `[TYPE: visual]`/`[INTERACTION: plot]` item with JSON `VISUAL` and private `SCORING` fields parses into one private item representation; no authored executable field is accepted.
    - `public_item()` emits protocol integer 1 with `type: visual`, plot scene, initial state, semantic actions, response schema, and accessibility description, while the private scoring envelope is absent.
    - The renderer registry reads only that contract, draws an inline SVG axis/point scene, retains adjacent semantic coordinate controls, and serializes exactly `{"kind":"point","x":SCALAR,"y":SCALAR}` with canonical exact scalar strings.
    - A real served submit of the accepted point reaches `runtime.score_response()`, returns a boolean verdict in `interaction_result`, and appends one response event carrying the same canonical semantic state and interaction version.
    - Forged client verdict, accepted state, tolerance, hint tier, key, or scoring fields do not influence the runtime result.
  </behavior>
  <action>Start `tests/visual_roundtrip.py` with the complete plot happy-path assertion and observe RED. Extend the established Phase-5 interaction envelope rather than defining a visual-only transport: parse `INTERACTION`, `VISUAL`, and `SCORING` JSON as declarative data; set `VISUAL_PROTOCOL_VERSION = 1`; and implement the exact SCALAR/plot response grammar locked in 06.1-RESEARCH.md `Resolved Questions` item 1. Project only scene/initial-state/semantic-action/response/accessibility data and keep the private scoring envelope on the server item. Register `asVisual` beside the Phase-5 renderer using the Phase-4 enhancement host and semantic fallback. The SVG maps pointer geometry to a snapped domain point but submits only canonical exact scalar strings; the adjacent native coordinate controls call the identical serializer. Route the response through the existing served submit path, `runtime.score_response()`, `interaction_result`, and Phase-5 evidence fields. Reject unknown top-level visual configuration members and any executable/script-bearing member before rendering. Keep the tracer to one plot point path, with real error containment and no client-side score or answer comparison.</action>
  <verify>
    <automated>python tests/visual_roundtrip.py</automated>
  </verify>
  <done>One learner-selected plot point is parsed, projected without scoring secrets, submitted as semantic state, scored only by the runtime, returned as a normalized result, and found in response evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the same protocol to number-line point and interval responses</name>
  <files>runtime.py, surfaces/quiz_page.py, fixtures/visual_bank.md, tests/visual_roundtrip.py</files>
  <behavior>
    - Plot and number-line items share the same interaction envelope, renderer registry, action vocabulary, response version, and result envelope.
    - A number-line point serializes exactly `{"kind":"numberline_point","value":SCALAR}`; an interval serializes exactly `{"kind":"interval","start":SCALAR,"end":SCALAR,"start_closed":BOOL,"end_closed":BOOL}`.
    - Reversing an interval during input is normalized by the runtime to ordered endpoints, not interpreted differently by the renderer.
    - Mouse/tap selection and the native point/endpoint controls generate byte-identical canonical semantic responses for the same state.
    - Plot and number-line responses remain boolean-scored; intermediate invariant/error observations do not become fractional mastery.
  </behavior>
  <action>Add the exact number-line response variants locked in 06.1-RESEARCH.md `Resolved Questions` item 1 inside the same visual protocol and `asVisual` registry entry. Reuse common domain-to-SVG mapping, focus/status, semantic action dispatch, and response serialization; branch only on the declared interaction/response kind. Keep state as canonical SCALAR strings and stable IDs, never CSS pixels or SVG paths. Include the locked integer plot and `"1/2"` number-line golden cases. For intervals, runtime canonicalization orders endpoints and swaps their closure flags when the endpoints reverse, then emits deterministic JSON key ordering. Return structured invariant observations without creating another score function or numeric partial-credit field.</action>
  <verify>
    <automated>python tests/visual_roundtrip.py</automated>
  </verify>
  <done>The fixture's plot and number-line items are independently answerable through one versioned protocol and produce stable semantic results through the same scorer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make tolerance and feedback intent deterministic and runtime-bounded</name>
  <files>runtime.py, tests/visual_roundtrip.py</files>
  <behavior>
    - Discrete grid/tick answers compare exactly after runtime normalization; continuous answers compare against a finite private tolerance whose policy version is recorded in the result observation.
    - Values exactly on both tolerance boundaries pass and the immediately adjacent representable fixture values fail; browser floating-point formatting cannot move the boundary.
    - Non-finite numbers, excessive precision, values outside the authored domain, unknown state/action kinds, and malformed intervals are rejected as invalid responses rather than scored by approximation.
    - The result observation contains submitted semantic state, deterministic error/invariant category, opaque authored feedback anchor IDs, and only the highest hint tier already permitted by the Phase-6 policy.
    - No result field exposes the accepted state, tolerance amount, solution path, reveal text, or a higher tier than the policy supplied.
  </behavior>
  <action>Implement the locked SCALAR grammar in `runtime.py` with `fractions.Fraction`: canonicalize signed integer/decimal-or-rational strings, reduce fractions, strip decimal trailing zeroes, map negative zero to `"0"`, bound numerator/denominator magnitude, require a positive axis step, cap an axis at 201 ticks, and reject exponents/non-finite/symbolic values. Discrete values must align exactly to `(value - min) / step`; the versioned private tolerance is a finite SCALAR in the same units. Do not compare browser pixel coordinates or binary floats. Have the existing `score_response()` visual branch perform the only boolean comparison. Add a pure observation/result builder that classifies authored invariant/error cases and maps them to opaque feedback anchors. Its entitlement input comes from the Phase-6 policy transition already evaluated for the session; client or agent fields cannot raise it. Record the policy/protocol version in the normalized observation so evidence remains interpretable after additive upgrades.</action>
  <verify>
    <automated>python tests/visual_roundtrip.py</automated>
  </verify>
  <done>Exact and tolerant visual responses have stable boundary behavior, invalid numeric/state input fails closed, and the returned teaching observation is informative without leaking answers or widening Phase-6 feedback entitlement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bank markdown -> model/runtime | Declarative scene and private scoring data enter validation and must never become executable code. |
| Runtime -> public browser/agent contract | Key-free scene/action data crosses to an untrusted consumer while accepted states and tolerance remain server-side. |
| Browser/agent -> runtime scorer | Untrusted semantic responses and forged authority fields cross into validation, canonicalization, feedback policy, and scoring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-01 | Information Disclosure | `runtime.public_item` visual projection | high | mitigate | Construct a positive allowlist for the public visual contract and assert private scoring sentinels never occur in served payloads. |
| T-06.1-02 | Tampering | visual submit/result envelope | high | mitigate | Ignore client authority fields, validate the semantic response against the declared grammar, and derive verdict/feedback entitlement only in runtime code. |
| T-06.1-03 | Elevation of Privilege | bank visual configuration | high | mitigate | Accept declarative JSON members only; reject executable/script-bearing or unknown configuration before render. |
| T-06.1-04 | Denial of Service | numeric/state normalization | medium | mitigate | Bound precision, collection sizes, domain ranges, and accepted action/state kinds before canonicalization. |
| T-06.1-SC | Tampering | package supply chain | low | accept | No package-manager install occurs; the implementation uses Python stdlib and browser-native HTML/SVG/JS. |
</threat_model>

<verification>
- `python tests/visual_roundtrip.py` drives parser -> public contract -> renderer response -> runtime score/result -> response evidence for plot and number line.
- Inspect the served payload through the test's private sentinel set; it contains only the declared public allowlist.
- Run the repository's CI-equivalent standalone scripts after the focused test to catch additive parser/scorer regressions.
</verification>

<success_criteria>
- One production plot path works end to end before the number-line expansion is added.
- Both interactions share one versioned declarative contract and one runtime scoring/result path.
- Public/private separation, numeric bounds, exact tolerance borders, and dichotomous verdicts are executable tests.
- The structured observation can support a teaching agent while the Phase-6 runtime policy remains the sole feedback-entitlement authority.
</success_criteria>

<output>
Create `.planning/phases/06.1-interactive-visual-assessment-protocol/06.1-01-SUMMARY.md` when done.
</output>
