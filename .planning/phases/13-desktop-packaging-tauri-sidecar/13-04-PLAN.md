---
phase: 13-desktop-packaging-tauri-sidecar
plan: 04
type: execute
wave: 3
depends_on:
  - 13-03
files_modified:
  - src-tauri/
  - tauri.conf.json
  - scripts/build_shell.ps1
  - surfaces/update.py
  - tests/packaging_roundtrip.py
  - tests/update_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - DEL-12
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "tauri-plugin-updater (signed) and the Phase 2.1 Python updater are two consumers of one GitHub Releases channel: the release pipeline publishes SHA256SUMS.txt (Python updater) and latest.json + minisign signature (Tauri updater) from the same tag (D-08, 13-RESEARCH section 2)."
    - "One notified_at disclosure record covers both consumers; the shell renders the locked 02.1 disclosure text plus the one additive line naming the settings file path, as a StatusNotice (not a modal), and the launch that shows it performs no check (13-UI-SPEC 7.2)."
    - "The shell and sidecar versions are checked at handshake; a mismatch renders the crashed/unreachable state rather than running mismatched halves (13-RESEARCH section 2, 13-UI-SPEC 3.3)."
    - "The Tauri updater replaces the shell + bundled sidecar; the Python updater updates the .pyz - neither replaces the other's artifact (13-RESEARCH section 2)."
    - "The update_policy divergence in CLAUDE.md stays unchanged; the shell does not get a third policy (D-08)."
  prohibitions:
    - statement: "A second disclosure record or consent surface must not be created for the shell."
      status: unverified
      flagged: true
    - statement: "No telemetry language may appear in updater copy; the forbidden-words list is a checkable fixture."
      status: unverified
      flagged: true
---

<objective>
As the packaged app, I want to update from the same release channel the CLI already uses, disclose once, and never run mismatched shell and runtime halves.

Purpose: The updater plan: the latest.json + minisign release assets, the tauri-plugin-updater wiring, the one-disclosure StatusNotice in the window, the version-coherence check, and the update-policy inheritance.
Output: The release-pipeline updater assets, the shell updater wiring, the disclosure integration, and the updater fixtures.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-CONTEXT.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-RESEARCH.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-UI-SPEC.md
@.planning/phases/02.1-packaging-self-update-interop-export/02.1-CONTEXT.md

<interfaces>
- `surfaces/update.py` stays the Python updater; `tauri-plugin-updater` is the shell updater - same channel, separate manifest formats (13-RESEARCH section 2 table).
- The disclosure record (`updates/check_state.json`) stays daemon-owned; the shell only renders it.
- The handshake version line from plan 13-01 feeds the coherence check.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `scripts/build_shell.ps1` | The latest.json generator + minisign signing step in the release pipeline |
| `src-tauri/` / `tauri.conf.json` | tauri-plugin-updater wired with the endpoint, signature verification, and the update-available flow |
| `surfaces/update.py` | The StatusNotice render hook (the daemon exposes the disclosure state the shell renders) |
| `tests/packaging_roundtrip.py` | latest.json validation, disclosure-render, and coherence fixtures |
| `tests/update_roundtrip.py` | Regression: the Python updater still works unchanged |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish latest.json and the minisign signature alongside SHA256SUMS.txt</name>
  <files>scripts/build_shell.ps1, tests/packaging_roundtrip.py</files>
  <read_first>
    - `13-RESEARCH.md` section 2 (the updater table and the latest.json format)
    - `build.py` - the existing SHA256SUMS.txt output
    - `surfaces/update.py` - the Python updater's channel consumption
  </read_first>
  <behavior>
    - Test 1: the release pipeline emits latest.json with the documented shape ({version, notes, pub_date, platforms: {windows-x86_64: {signature, url}}}) for the NSIS installer, plus the minisign signature, alongside the existing SHA256SUMS.txt, from the same tag.
    - Test 2: a fresh checkout + pipeline run is reproducible and the latest.json validates against the updater format.
  </behavior>
  <action>
Extend the release pipeline (scripts/build_shell.ps1 + build.py) to generate `latest.json` for the NSIS installer and sign it with minisign (the key material is a build secret, documented in the pipeline), publishing alongside the existing SHA256SUMS.txt from the same release tag - one channel, two consumers (13-RESEARCH section 2). The url points at the release asset; the signature covers the installer bytes. Write the two behavior tests first.
  </action>
  <verify>
    <automated>powershell -File scripts/build_shell.ps1 &amp;&amp; python tests/packaging_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - latest.json + signature + SHA256SUMS.txt all publish from one tag with the documented shapes.
    - The pipeline is reproducible from the lockfile.
  </acceptance_criteria>
  <done>One release channel serves both updaters with their own manifest formats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire tauri-plugin-updater and the one-disclosure StatusNotice</name>
  <files>src-tauri/, tauri.conf.json, surfaces/update.py, tests/packaging_roundtrip.py</files>
  <read_first>
    - `13-UI-SPEC.md` section 7 (the updater contract, disclosure, restart copy)
    - `surfaces/update.py` - background_check and the notified_at record
    - `02.1-CONTEXT.md` - the locked disclosure wording
  </read_first>
  <behavior>
    - Test 1: tauri-plugin-updater is wired with the endpoint and signature verification; an update-available state renders the exact copy "A new itembank version is available: v<latest> (you're on v<current>)." with the restart behavior documented.
    - Test 2: the disclosure renders in the window as a StatusNotice (role=status, not alert) with the 02.1 locked text plus exactly the one additive line "Your settings file is at <resolved path>.", and the launch that shows it performs no check - one notified_at record, daemon-owned.
    - Test 3: the forbidden-words fixture - none of usage, analytics, telemetry, diagnostics, anonymous, help us improve, opt out of data collection, crash reports appears anywhere in updater copy.
  </behavior>
  <action>
Wire `tauri-plugin-updater` in the shell with the endpoint pointing at the published latest.json and signature verification enabled. Implement the update-available flow with the exact 13-UI-SPEC 7.3 copy and the documented restart behavior (the updater auto-exits the app during install - a Windows limitation the copy states, not hides). Expose the daemon's existing disclosure state (`updates/check_state.json` notified_at) to the shell as a render hook: the shell renders the 02.1 locked disclosure text plus the single additive settings-path line as a StatusNotice on the first surface, exactly once, and the showing launch performs no check (13-UI-SPEC 7.2). Add the forbidden-words fixture. Write the three behavior tests first.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml &amp;&amp; python tests/packaging_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The updater renders the exact copy and verifies signatures; the disclosure is one StatusNotice with one record.
    - The forbidden-words fixture passes.
  </acceptance_criteria>
  <done>The packaged app updates honestly, discloses once, and never lies about what leaves the machine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Version coherence and the Python-updater regression</name>
  <files>src-tauri/, surfaces/update.py, tests/update_roundtrip.py</files>
  <read_first>
    - `13-RESEARCH.md` section 2 (coherence at handshake)
    - `tests/update_roundtrip.py` - the existing Python updater tests
  </read_first>
  <behavior>
    - Test 1: the handshake version-coherence fixture - shell and sidecar versions matching proceed; a mismatch renders the crashed/unreachable state.
    - Test 2: the Python updater's existing tests pass unchanged - the Tauri updater's presence changes nothing about the .pyz updater's behavior.
  </behavior>
  <action>
Implement the version-coherence check at handshake (the sidecar's version line from plan 13-01 vs the shell's own version) per 13-RESEARCH section 2, routing a mismatch to the honest state. Run the existing `tests/update_roundtrip.py` to prove the Python updater is untouched. Write the two behavior tests first.
  </action>
  <verify>
    <automated>python tests/update_roundtrip.py &amp;&amp; cargo test --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - The coherence fixture passes; the Python updater suite stays green.
  </acceptance_criteria>
  <done>Mismatched halves cannot run silently, and the CLI updater is untouched by the shell's.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| release assets -> installed app | The update path is a code-execution path; signatures must verify before install |
| disclosure -> learner | The one consent record must stay one, and the copy must not imply telemetry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-13 | Tampering | unsigned update payload | high | mitigate | minisign signature verified by tauri-plugin-updater before install; SHA256SUMS.txt verified by the Python updater (existing) |
| T-13-14 | Spoofing | update endpoint | high | mitigate | HTTPS endpoint + signature verification; the endpoint is the same GitHub Releases the Python updater already trusts |
| T-13-15 | Privacy | telemetry language | medium | mitigate | The forbidden-words fixture is checkable; the only true statement is the locked one |
| T-13-16 | Integrity | version mismatch | medium | mitigate | The handshake coherence check routes mismatches to the honest state |
| T-13-SC | Tampering | package operations | high | mitigate | minisign is pinned; key material is a build secret |
</threat_model>

<verification>
- `python tests/packaging_roundtrip.py`
- `python tests/update_roundtrip.py`
- `cargo test --manifest-path src-tauri/Cargo.toml`
</verification>

<success_criteria>
- One channel publishes both manifest formats; the Tauri updater verifies signatures.
- One disclosure, one record, verbatim copy.
- Version coherence is checked; the Python updater is untouched.
</success_criteria>

<output>
Create `.planning/phases/13-desktop-packaging-tauri-sidecar/13-04-SUMMARY.md` when done.
</output>
