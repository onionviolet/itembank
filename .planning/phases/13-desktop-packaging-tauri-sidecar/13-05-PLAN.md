---
phase: 13-desktop-packaging-tauri-sidecar
plan: 05
type: verify
wave: 3
depends_on:
  - 13-01
  - 13-02
  - 13-03
  - 13-04
files_modified:
  - tests/packaging_roundtrip.py
  - tests/packaging_shell_roundtrip.py
  - tests/daemon_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - DEL-09
  - DEL-10
  - DEL-11
  - DEL-12
  - DEL-13
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "The executed AV/code-signing checklist is recorded: certificate named or unsigned shipped honestly, Microsoft false-positive submission path recorded, VirusTotal baseline recorded, and the install-notice copy renders real values (D-10/D-11, 13-UI-SPEC 6.1)."
    - "The full loop runs headless without the shell (D-12); the lifecycle fixture proves kill-the-shell kills-the-sidecar and releases the port (criterion 3)."
    - "The installed size is measured and within the 25-45 MB target (D-09)."
    - "Every one of DEL-09..DEL-13 is addressed by a plan and verified by a named fixture or command."
    - "Deterministic edge probe skipped visibly: the gate requires pre-assigned requirement IDs; requirement IDs were assigned at plan time in this run."
  prohibitions:
    - statement: "The AV checklist must not be recorded as executed when it was not."
      status: unverified
      flagged: true
    - statement: "No plan may claim a requirement is verified by a test that does not exist."
      status: unverified
      flagged: true
---

<objective>
As the phase, I want the packaging claims proven - lifecycle, size, signing, headless CLI, and the full suite - so the phase seals on executed evidence, not intentions.

Purpose: The phase-level verification plan: the executed AV/signing checklist, the lifecycle and headless fixtures, the size measurement, the DEL-09..13 coverage audit, and the full-suite run.
Output: 13-GATES.md, the completed fixtures, and the green full-suite run.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-CONTEXT.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-RESEARCH.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-UI-SPEC.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-VALIDATION.md

<interfaces>
- The fixture harness is the repo's stdlib fail() pattern plus cargo test for the shell.
- The checklist rows map to the fixtures in plans 13-01..04; no row may be recorded without its evidence.
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `tests/packaging_roundtrip.py` / `tests/packaging_shell_roundtrip.py` | The lifecycle, headless, size, and updater fixtures finalized |
| Phase directory | `13-GATES.md` - the executed AV/signing checklist and the verification-gate mapping |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute and record the AV/code-signing checklist</name>
  <files>tests/packaging_roundtrip.py</files>
  <read_first>
    - `13-UI-SPEC.md` section 6.1 (install notice) and `13-RESEARCH.md` section 4 (the checklist)
    - `13-CONTEXT.md` D-10/D-11
  </read_first>
  <behavior>
    - Test 1: the checklist records each row with real evidence - certificate named (subject + fingerprint in the notice) OR unsigned branch shipped with the SmartScreen warning and real SHA-256; Microsoft false-positive reporting path recorded; VirusTotal baseline recorded at release.
    - Test 2: the install-notice copy is verbatim and the values are real (fixture grep + value check).
  </behavior>
  <action>
Execute the AV/signing checklist per 13-RESEARCH section 4 and record it in `13-GATES.md`: the certificate decision (named and used, or unsigned shipped honestly per D-11), the Authenticode signature on the shell and installer when signed, the Microsoft false-positive submission path, and the VirusTotal baseline. Assert the install-notice renders real values verbatim. Write the two behavior tests first.
  </action>
  <verify>
    <automated>python tests/packaging_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - Every checklist row has recorded evidence; the unsigned branch never pretends the problem is solved.
    - The notice values are real and the copy is verbatim.
  </acceptance_criteria>
  <done>The AV story is executed, recorded, and honest - the named top risk is closed with evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Requirement audit, lifecycle/size/headless evidence, and the full suite</name>
  <files>tests/packaging_roundtrip.py, tests/packaging_shell_roundtrip.py, tests/daemon_roundtrip.py</files>
  <read_first>
    - `REQUIREMENTS.md` - the DEL-09..13 rows and traceability
    - `ROADMAP.md` - the Phase 13 section
    - The four plan files' requirements fields and acceptance criteria
    - `13-VALIDATION.md` if present
  </read_first>
  <behavior>
    - Test 1: every one of DEL-09..DEL-13 appears in a plan with a named verification.
    - Test 2: the lifecycle fixture (taskkill the shell, sidecar dies, port released), the size measurement (25-45 MB), and the headless CLI loop all pass and their results are recorded.
    - Test 3: the full suite (`for f in tests/*_roundtrip.py; do python "$f" || exit 1; done` plus cargo test) exits 0.
  </behavior>
  <action>
Write the DEL-09..13 coverage audit (fail() harness), run the lifecycle/size/headless evidence fixtures, run the full suite plus `cargo test`, and record the results in `13-GATES.md` with the measured size and teardown time. Fix any failures this surfaces (fixtures/build scripts only - no product-code changes in this plan). Record the deterministic edge-probe skip visibly (grep each PLAN.md for the skip sentence). Write the three behavior tests first.
  </action>
  <verify>
    <automated>for f in tests/*_roundtrip.py; do python "$f" || exit 1; done &amp;&amp; cargo test --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - The audit exits 0 with 5/5 DEL-IDs covered and every named verification exists.
    - The lifecycle/size/headless results are recorded in 13-GATES.md.
    - The full suite and cargo test pass.
  </acceptance_criteria>
  <done>Phase 13 seals on executed evidence: lifecycle, size, signing, headless CLI, and full coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| checklist -> phase seal | An unexecuted checklist row must not be recorded as executed |
| fixtures -> suite result | A fixture that asserts nothing would launder a regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-17 | Spoofing | checklist/coverage claims | high | mitigate | Each row maps to a fixture or recorded value; the audit fails on any claim without evidence |
| T-13-18 | Repudiation | signing decision | medium | mitigate | The certificate decision and the unsigned fallback are recorded in 13-GATES.md at phase time |
| T-13-19 | Tampering | release artifacts | high | mitigate | The build is reproducible from the lockfile; SHA256SUMS.txt/latest.json validate |
| T-13-SC | Tampering | package operations | high | mitigate | This plan performs no package operation and no network fetch |
</threat_model>

<verification>
- The full-suite loop + cargo test.
- The recorded size and teardown-time lines.
- The 5/5 requirement-coverage line.
</verification>

<success_criteria>
- The AV checklist is executed and recorded honestly.
- 5/5 DEL-IDs map to plans and named verifications.
- The full suite passes with lifecycle, size, and headless evidence.
</success_criteria>

<output>
Create `.planning/phases/13-desktop-packaging-tauri-sidecar/13-05-SUMMARY.md` when done.
</output>
