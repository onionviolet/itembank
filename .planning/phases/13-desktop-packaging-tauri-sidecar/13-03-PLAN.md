---
phase: 13-desktop-packaging-tauri-sidecar
plan: 03
type: execute
wave: 2
depends_on:
  - 13-02
files_modified:
  - build.py
  - requirements-build.txt
  - scripts/build_shell.ps1
  - installers/nsis/
  - tests/packaging_roundtrip.py
autonomous: true
ui_classification: UPSTREAM-CONTRACT
requirements:
  - DEL-11
  - DEL-13
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "The sidecar is a PyInstaller onedir build (never onefile) wrapping the existing Phase 2.1 artifact; the installed size is measured and reported, landing in the 25-45 MB target (D-09, 13-RESEARCH section 3)."
    - "PyInstaller and the build toolchain are pinned with recorded checksums and named license review per Directive 4a (D-14)."
    - "The installer is NSIS, per-user by default with a machine-wide option; uninstalling removes the application but never the evidence store or banks (D-07, 13-UI-SPEC 6.2)."
    - "The install-notice copy renders the real certificate subject/fingerprint (signed) or the real published SHA-256 (unsigned), verbatim per 13-UI-SPEC 6.1 - never a placeholder, and the unsigned branch never implies the problem is solved (D-11)."
    - "Every capability stays reachable from the CLI without the shell installed; the headless loop test runs the full flow without the shell (D-12)."
    - "Linux (WebKitGTK) and macOS are deferred to hardware, with Electron recorded as the named Linux fallback rather than silently unresolved (D-13)."
  prohibitions:
    - statement: "The uninstaller must never be able to delete the evidence store."
      status: unverified
      flagged: true
    - statement: "No onefile PyInstaller build may ship; onedir is the locked shape."
      status: unverified
      flagged: true
---

<objective>
As the installer, I want the app to install per-user, uninstall without touching the learner's data, ship a measured onedir sidecar, and state its signing status honestly.

Purpose: The distribution plan: the pinned PyInstaller onedir build, the NSIS installer with the evidence-store guard, the signed/unsigned install notice, and the CLI-without-shell headless test.
Output: The build scripts, the pinned toolchain records, the NSIS installer, and the packaging fixtures.
</objective>

<execution_context>
@C:/Users/wayba/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wayba/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-CONTEXT.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-RESEARCH.md
@.planning/phases/13-desktop-packaging-tauri-sidecar/13-UI-SPEC.md
@.planning/phases/02.1-packaging-self-update-interop-export/02.1-CONTEXT.md

<interfaces>
- `build.py` STAGE_FILES/STAGE_DIRS is the allowlist; the shell build extends it, never walks the tree.
- The Phase 2.1 `.pyz` artifact is what PyInstaller wraps - this plan does not invent a second build of the runtime.
- The evidence store and banks live in the user profile; the installer never relocates or deletes them (Directive 4.3).
</interfaces>

## Artifacts this phase produces

| Path | Created symbols / contract |
|---|---|
| `requirements-build.txt` | Pinned PyInstaller (+ build deps) with checksums and license notes |
| `scripts/build_shell.ps1` | The onedir sidecar build + Tauri bundle + NSIS invocation |
| `installers/nsis/` | The NSIS script with the per-user/machine-wide choice and the evidence-store guard |
| `build.py` | The release-assets step (SHA256SUMS.txt + latest.json hook placeholder for 13-04) |
| `tests/packaging_roundtrip.py` | Size measurement, uninstaller-guard, and headless CLI fixtures |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The pinned PyInstaller onedir sidecar build</name>
  <files>requirements-build.txt, scripts/build_shell.ps1, build.py, tests/packaging_roundtrip.py</files>
  <read_first>
    - `build.py` - the allowlist staging and sha256sums() output
    - `13-RESEARCH.md` section 3 (toolchain verification, onedir shape)
    - `13-CONTEXT.md` D-09/D-14
  </read_first>
  <behavior>
    - Test 1: the build produces a PyInstaller onedir directory (not a single exe), the daemon entry runs from it and serves the marker path, and the measured size is within 25-45 MB.
    - Test 2: the pinned PyInstaller version and checksum are recorded in requirements-build.txt; the build is reproducible from the lockfile.
    - Test 3: the sidecar binary carries the `-$TARGET_TRIPLE` suffix the Tauri externalBin requires.
  </behavior>
  <action>
Add the pinned build toolchain (PyInstaller + Tauri CLI invocation) to `requirements-build.txt` with recorded versions, checksums, and license review per D-14. Write `scripts/build_shell.ps1` that: runs the existing runtime build, freezes the daemon entry with PyInstaller onedir (never onefile), renames the sidecar binary with the `-$TARGET_TRIPLE` suffix, invokes the Tauri bundle, and records the installed-size measurement. Extend `build.py`'s release step to keep SHA256SUMS.txt and add the latest.json generation hook (wired fully in 13-04). Write the three behavior tests first.
  </action>
  <verify>
    <automated>powershell -File scripts/build_shell.ps1 &amp;&amp; python tests/packaging_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The onedir sidecar runs and serves; the measured size is recorded and within target.
    - The toolchain pins are recorded with checksums and license notes.
    - The sidecar binary has the target-triple suffix.
  </acceptance_criteria>
  <done>The sidecar freezes reproducibly, measures honestly, and ships with the right name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The NSIS installer with the evidence-store guard and the install notice</name>
  <files>installers/nsis/, tests/packaging_roundtrip.py</files>
  <read_first>
    - `13-UI-SPEC.md` section 6 (installer, AV/signing screen, uninstaller guard)
    - `13-CONTEXT.md` D-07/D-10/D-11
  </read_first>
  <behavior>
    - Test 1: the NSIS script installs per-user by default with a machine-wide option; the installed app launches and the daemon serves.
    - Test 2: the uninstaller removes the application directory only - a fixture proves the user-profile evidence store and banks survive uninstall (13-UI-SPEC 6.2).
    - Test 3: the install-notice page renders the signed branch (certificate subject + sha256 fingerprint) when a certificate is configured, or the unsigned branch (published SHA-256 + SmartScreen warning) verbatim when not - never a placeholder.
  </behavior>
  <action>
Write the NSIS installer script: per-user default install (no UAC), machine-wide option, the install-notice page per 13-UI-SPEC 6.1 (signed or unsigned branch driven by whether a certificate is configured - the copy is verbatim and the values are real), and an uninstaller that removes only the application directory and explicitly never touches the user-profile evidence/bank directories (13-UI-SPEC 6.2). Wire the certificate variables (subject, fingerprint) or the published SHA-256 into the notice. Write the three behavior tests first (including an uninstall simulation against a temp profile).
  </action>
  <verify>
    <automated>python tests/packaging_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The install/uninstall fixtures pass with the evidence store intact.
    - The notice renders real values per the signed/unsigned branch, verbatim copy.
  </acceptance_criteria>
  <done>The installer is honest, per-user, and cannot eat the learner's data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The CLI-without-shell headless proof</name>
  <files>tests/packaging_roundtrip.py, tests/daemon_roundtrip.py</files>
  <read_first>
    - `13-CONTEXT.md` D-12 (every capability stays reachable from the CLI without the shell)
    - The existing `tests/daemon_roundtrip.py` and `tests/launcher_roundtrip.py` loops
  </read_first>
  <behavior>
    - Test 1: the full loop - lint, build, serve/daemon, start, submit, report, evidence - runs headless with no shell installed (the existing roundtrip suite is the proof).
    - Test 2: the shell's absence never changes CLI behavior or the evidence store's location.
  </behavior>
  <action>
Add the headless proof to the packaging fixture: run the existing full-loop tests against a build tree with no shell present and assert every capability still works, and assert the evidence store resolves to the same user-profile location with and without the shell. Write the two behavior tests first.
  </action>
  <verify>
    <automated>python tests/packaging_roundtrip.py &amp;&amp; python tests/daemon_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - The headless loop passes with no shell installed.
    - Evidence location is identical with and without the shell.
  </acceptance_criteria>
  <done>The desktop app is a client, never a precondition - proven headless.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| installer -> user machine | The installer must not elevate by default, delete data, or misstate signing |
| sidecar binary -> frozen runtime | The frozen artifact must be the same runtime, not a second build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-09 | Tampering | uninstaller deleting evidence | high | mitigate | Uninstaller scoped to the application directory; fixture proves the profile data survives (13-UI-SPEC 6.2) |
| T-13-10 | Spoofing | signing claims | high | mitigate | The notice renders real certificate values or the real SHA-256; unsigned branch is explicit (D-11) |
| T-13-11 | Tampering | build supply chain | high | mitigate | PyInstaller pinned with checksum + license review; build reproducible from the lockfile (D-14) |
| T-13-12 | Elevation of Privilege | UAC/machine-wide install | medium | mitigate | Per-user default; machine-wide is an explicit choice, never silent elevation (D-07) |
| T-13-SC | Tampering | package operations | high | mitigate | All additions are pinned and recorded; the build runs offline from the lockfile |
</threat_model>

<verification>
- `powershell -File scripts/build_shell.ps1`
- `python tests/packaging_roundtrip.py`
- The uninstall-simulation fixture output.
</verification>

<success_criteria>
- The onedir sidecar builds reproducibly within the size target.
- The installer installs/uninstalls cleanly without touching the evidence store, with honest signing copy.
- The full loop runs headless without the shell.
</success_criteria>

<output>
Create `.planning/phases/13-desktop-packaging-tauri-sidecar/13-03-SUMMARY.md` when done.
</output>
