# Phase 999.5 Plan 01 — External-User Agent On-Ramp Summary

**Phase:** 999.5-agent-onboarding-skill-library · **Plan:** 01
**Date:** 2026-08-11 · **Status:** complete

## What shipped

1. **Skills synced to the shipped command surface** (ROADMAP 999.5 trigger
   points for Phases 6/7/8/11, all now cleared), in both `.agents/skills/`
   and `.claude/skills/` (byte-identical):
   - `guiding-questions` gained the tier-gated hint ladder
     (`hint --session s.json [--retry]`, D-09/D-12), `rubric-review` (D-25,
     `pending`-token rule), and the inspectable `select` preview (Phase 7,
     `--explain` trace).
   - `curriculum-design` gained the on-demand `coverage` map (D-12) tied to
     the `## SOURCES` registry; coverage claims must cite an item.
   - `author-bank` gained the `guard .` ship gate and the `seed` one-accept
     drafting loop (D-07/D-08), with the "drafting aid, not a bypass" rule.
2. **No machine-specific config ships.** `reasonix.toml` (permissions grant,
   sandbox mirror, OCR plugin — all absolute paths to this machine) is
   gitignored and untracked; `reasonix.toml.example` documents that Reasonix
   needs no config for this repo (auto-discovers `.agents/skills/` as a
   convention root) and shows the optional OCR wiring portably.
   `scripts/ocr_mcp.py`'s registration example no longer hard-codes a path.
3. **Per-tool discovery documented.** README and AGENTS.md name all five
   skills (including `ocr`, previously missing from both tables) and the
   location each tool reads: Claude Code → `.claude/skills/`; Codex, Gemini
   CLI, Cursor, GitHub Copilot and other agents.md readers →
   `.agents/skills/` (auto-discovered); Reasonix → `.agents/skills/`
   convention root; anything else → point the skill root at
   `.agents/skills/`. AGENTS.md's "Reasonix runtime notes" was generalized
   (machine launcher path removed).
4. **CI enforces the convention:** `diff -rq .agents/skills .claude/skills`
   fails on mirror drift, and a path-leak grep fails on a machine-specific
   path (`C:/Users`, `C:\Users`, `wayba`) appearing in agent docs/config.

## Verification

- `diff -rq .agents/skills .claude/skills` — clean.
- `python itembank.py lint fixtures/sample_bank.md` — 6 items, 0 errors.
- `python itembank.py guard .` — 0 offending files.
- Every command named in the skills (`spec lint build start next submit hint
  rubric-review report select coverage guard seed id-assign stats study`)
  exists in `itembank.py --help`.
- Path-leak grep over `AGENTS.md README.md .agents .claude
  reasonix.toml.example` — clean at write time.
- Full test suite (`tests/*.py`) run — see suite result below.

## Caveats / follow-ups

- **Concurrent writers:** multiple headless Reasonix phase sessions
  (06.1/06.2/09.1/11/03.1) were live in parallel worktrees during this
  change. One of them was actively appending machine-specific operational
  notes (headless-launch flags, WSL2 interop, prompt caching) to the main
  tree's `AGENTS.md` "Reasonix runtime notes" section. Those notes belong in
  `.reasonix/REASONIX.md` (which they already reference), not in the
  external-user-facing `AGENTS.md`; the CI path-leak step will flag them if
  committed. Recommend relocating that content and treating the notes
  section as personal.
- `.agents/skills/ocr/` and `.claude/skills/ocr/` were untracked at plan
  start (added locally, never committed); they are part of the five-skill
  set and must be committed with this phase.
- `reasonix.toml` removal is a staged index change (`git rm --cached`); the
  local file remains on disk and keeps working untracked.
