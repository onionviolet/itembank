---
phase: 999.5-agent-onboarding-skill-library
plan: 02
type: execute
wave: 1
depends_on: ["999.5-01"]
files_modified:
  - README.md
  - AGENTS.md
  - .planning/ROADMAP.md
  - .planning/WINDOWS.md
  - GRADING.md
  - .claude/CLAUDE.md
  - model.py
  - tests/lesson_roundtrip.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [MODEL-04]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 10
  confidence: medium

must_haves:
  truths:
    - "Execution starts only on a settled tree: no `UU` files and no conflict markers in any file this plan touches. If the tree is mid-merge at execution start (as it is on 2026-08-11, with phase 06.1 work unresolved in the index), stop and wait; never edit around or through conflict markers."
    - "README's command documentation names only commands `python itembank.py --help` actually registers, proven by a CI claim-word gate (precedent: 05-07's claim-word gate, 999.5-01's mirror/path gates). Every top-level subcommand is either documented in the README command index or deliberately grouped/omitted with a stated reason — the inventory is re-frozen at execution time, not copied from this plan."
    - "`itembank spec`'s type-list heading matches the shipped count (expect 'THE SEVEN ITEM TYPES' today: `mc`, `multi`, `table`, `build`, `dnd`, `short`, `visual`), the one assertion pinning it in `tests/lesson_roundtrip.py` is updated to match, and the README's item-type section agrees with the spec at execution time. No type is documented before it ships (`check` stays out until Phase 5 lands)."
    - "The 'This does not do spaced repetition' Design-boundary bullet is reconciled against what `retention.py` actually ships (FSRS-derived pacing and daily caps, `itembank trends`, `day` recommendations) and states what the tool still does not do — it is not a per-card recall scheduler and `study`'s Learn loop stays session-only — with no invented tok/s, latency, or capacity figure anywhere (D-19)."
    - "A new honest Design-boundary bullet covers the model adapter: an optional hosted or local backend may produce hints and marking suggestions, but the runtime gates tiers and nothing a model produces is ever accepted as a mark (`pending` rubric review only). The runtime-not-the-model sentence is preserved, not diluted."
    - "The Layout/repository-tree section lists only real files and directories, verified with `ls` at execution time: the root modules, `surfaces/`, `schemas/` (12 schemas), `styles/` (6 lesson styles), `launchers/`, `installers/`, `scripts/`, `src-tauri/`. No invented paths."
    - "ROADMAP.md's phase-status table is corrected to match `STATE.md` (3 Complete, 3.1 Complete 7/7, 4 Complete 6/6, 6.1 In Progress with 06.1-01/02 committed, 8 In Progress 5/6, 10 Complete) and contains no conflict markers; the ROADMAP 999.5 description says five skills, not four (`ocr` shipped in 999.5-01)."
    - "CI schema coverage is closed: every `schemas/*.json` is exercised in CI — the seven previously uncovered (`visual_interaction`, `agent_usage`, `model_adapter`, `tier_gate`, `selection`, `settings`, `update_manifest`) either validate (self-check plus at least one instance where a fixture or real payload exists) or carry a stated exemption reason in the workflow; `schema_validate.py` gains an `--all` mode if that is the mechanism chosen."
    - "`.planning/WINDOWS.md` is audited and updated to truth: broken window 1 (daemon reserved-port/EACCES branch) is closed by an automated test if a portable repro exists within this plan's scope, or stays open with a stated reason; windows 2 and 3 (LAN phone reachability, GIFT sandbox manual import) are recorded as genuinely manual with the plan's assessment and left open for a human waive/verify — `open_count` and the frontmatter match the table, never silently stale."
    - "AGENTS.md's item-type list includes `visual` and every command it references exists in `python itembank.py --help`; the two skill trees stay byte-identical mirrors (`diff -rq` clean) — no skill text is changed without its mirror, and ideally skills are not touched at all."
    - "The diff is docs plus two behavior-preserving exceptions — the spec-heading string in `model.py` and the one test assertion that pins it, and any new test that closes broken window 1 — nothing else changes behavior; the full `tests/*.py` suite, `itembank lint fixtures/sample_bank.md`, `schema_validate.py`, and `guard` all pass."
  prohibitions:
    - statement: "MUST NOT document a command, item type, or feature that does not ship at execution time — the maintenance convention (skills and README document the shipped surface only)."
      status: unverified
      flagged: true
    - statement: "MUST NOT touch unresolved merge conflict markers or any in-flight phase 06.1 file; a mid-merge tree is a stop-and-wait precondition, not an obstacle to work around."
      status: unverified
      flagged: true
    - statement: "MUST NOT invent performance, capacity, or latency figures anywhere in the docs (D-19); measured `elapsed_ms` is the only timing field that exists."
      status: unverified
      flagged: true
    - statement: "MUST NOT change code, tests, or schemas except the two carve-outs: the `THE FIVE ITEM TYPES` heading in `model.py`'s spec text (→ the shipped count) plus the one assertion in `tests/lesson_roundtrip.py` that pins it, and a new test that closes broken window 1 if a portable repro exists. Everything else in this plan is documentation."
      status: unverified
      flagged: true
  artifacts:
    - path: ".planning/phases/999.5-agent-onboarding-skill-library/999.5-02-PLAN.md"
      provides: "this plan"
      contains: "999.5"
    - path: "README.md"
      provides: "the rewritten front door: 10-second summary with a sample bank, grouped command index covering the shipped surface, seven item types, reconciled Design boundaries, real repository tree"
      contains: "sample bank"
    - path: "model.py"
      provides: "spec text heading corrected to the shipped type count (expect 'THE SEVEN ITEM TYPES')"
      contains: "ITEM TYPES"
    - path: "tests/lesson_roundtrip.py"
      provides: "the assertion pinning the spec heading, updated to match"
      contains: "ITEM TYPES"
    - path: ".github/workflows/ci.yml"
      provides: "claim-word gate (README command lines must not name a subcommand absent from `--help`) and full schemas/ CI coverage"
      contains: "itembank.py --help"
    - path: ".planning/WINDOWS.md"
      provides: "broken-windows ledger audited to truth (window 1 closed if automatable, windows 2-3 recorded manual)"
      contains: "open_count"
  key_links:
    - from: "README.md (command index)"
      to: "surfaces/cli.py"
      via: "the `sub.add_parser(...)` registrations are the ground truth the index must match"
      pattern: "add_parser"
    - from: "README.md + model.py (item types)"
      to: "model.py"
      via: "`itembank spec`'s type list (seven types incl. `visual`, 06.1-01/02) is what both the heading and the README section document"
      pattern: "TYPE: visual"
    - from: "README.md (Design boundaries)"
      to: "retention.py"
      via: "the spaced-repetition claim is reconciled against the FSRS scheduler strategy and daily-cap derivation it ships"
      pattern: "FSRS"
    - from: ".planning/WINDOWS.md"
      to: "surfaces/daemon.py"
      via: "broken window 1 is the reserved-port/EACCES branch of start_server()"
      pattern: "EACCES"
---

<objective>
**As the** reader of a fresh checkout, **I want** the README to say what itembank actually is, does, and ships in the first ten seconds, document the real command surface, and stop contradicting the code, **so that** the front door is as informative and its prose as clear and direct as it can be — and the surrounding repo docs, contract text, and CI gates stop drifting behind it.

Purpose: the README is the front door, and it is stale in the direction of under-information. Verified 2026-08-11 against the shipped surface: the README documents 13 subcommands while `surfaces/cli.py` registers 44; the item-type section says six types while `itembank spec` ships seven (`visual` landed in HEAD via 06.1-01/02) and the spec's own heading still says "THE FIVE ITEM TYPES"; the Design-boundary bullet "This does not do spaced repetition" is now contradicted by `retention.py` (FSRS-derived pacing, daily caps, `itembank trends`, `day` recommendations); the Layout tree omits `schemas/`, `styles/`, `launchers/`, `installers/`, `scripts/`, `src-tauri/`, `retention.py`, `evidence.py`, `selection.py`, `model_adapter.py`, `tier_gate.py` and half of `surfaces/`; the opening is architecture-first (model/runtime/server/surfaces) before any reader has seen a bank. The audit pass extends one step further, to what is obvious in hindsight: CI schema-validates only 5 of 12 schemas (7 can rot silently), and the broken-windows ledger that blocks `/gsd-ship` holds three open `unrun-verify` items whose assessment is stale. The prose is often excellent — the failure stories, the honest-limits register, the "no agent around" section — and the rewrite keeps that voice while making the facts current and the structure user-first.

Output: a README that (1) opens with a 10-second summary and a five-line sample bank, (2) documents the shipped command surface in grouped, aligned blocks, (3) lists the shipped item types in agreement with a corrected `itembank spec`, (4) reconciles the Design boundaries with `retention.py` and the Phase 8 model adapter, (5) shows the real repository tree; plus a bounded drift pass over `AGENTS.md`, `ROADMAP.md`, `GRADING.md` and `CLAUDE.md`; two CI gates (no invented README commands, full `schemas/` coverage); and a truthful assessment of `.planning/WINDOWS.md` with the automatable window closed if a portable repro exists.
</objective>

<execution_context>
@.planning/PLANNING-DIRECTIVES.md
@.planning/ROADMAP.md (Phase 999.5 section, lines ~1363-1403)
@.planning/STATE.md (phase-status truth for the ROADMAP table fix)
@.planning/WINDOWS.md (broken-windows ledger; `/gsd-ship` blocks while `open_count > 0`)
</execution_context>

<context>
@README.md
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@surfaces/cli.py
@surfaces/daemon.py
@model.py
@retention.py
@surfaces/quiz.py
@tests/lesson_roundtrip.py
@tests/daemon_roundtrip.py
@schemas/
@.github/workflows/ci.yml
@GRADING.md
@.claude/CLAUDE.md
</context>

## Tasks

### Task 1: Freeze the shipped surface at execution time

Before writing a word of prose, capture ground truth from the settled tree:

- `python itembank.py --help` — the full subcommand list.
- `python itembank.py spec` — the item-type list and contract headlines.
- `grep -n 'add_parser(' surfaces/cli.py` — cross-check no subcommand is
  missing from `--help` output.
- `ls` of the repo root, `surfaces/`, `schemas/`, `styles/`, `launchers/`,
  `installers/`, `scripts/`, `tests/` — the real tree for the Layout section.
- `ls schemas/` — the full schema list for the CI-coverage task.
- `git status --short` — confirm no `UU` files and no conflict markers in
  any file this plan touches; if the tree is mid-merge, **stop and report**,
  do not proceed.

Write the frozen command list, type list, and schema list into the plan's
verification truths (replace the 2026-08-11 inventory in this plan with what
the tree actually ships at execution time).

### Task 2: Rewrite the README opening — 10-second summary, then a bank

Keep the existing voice (imperative, concrete, honest-limits register) but
reorder for the reader who has never seen the tool:

- **First screen**: one sentence on what it is ("Author, validate and render
  exam-style question banks written in plain markdown — and sit them as a
  graded quiz that scores every answer and writes it to disk."), then one
  line each for what you get: author in markdown → `lint` with actionable
  errors by item number → offline quiz (`build`) or graded sitting (`serve`)
  → JSON sessions for AI tutors → `day` cockpit. No architecture yet.
- **A five-line sample bank in a code block** immediately after, so a first
  author sees the format before reading a spec. Model it on
  `fixtures/sample_bank.md`'s simplest `mc` item (stem, options, `CORRECT:`),
  with one `[TYPE: multi]` or `[TYPE: short]` line to show a second shape.
  Follow it with "that is the whole format — `itembank spec` prints the rest".
- Then "Why this exists" (keep the story nearly verbatim — it is the best
  prose in the file), then Install, then Use.
- Move the four-layer model/runtime/server/surfaces paragraph into the
  Design section ("How it is built"), where a reader who wants it can find
  it, instead of front-loading it before any concrete example.

### Task 3: Reconcile the command index with the shipped surface

Replace the flat 13-command block with grouped, aligned blocks. Group by
reader goal, and within each group keep the `command` / one-clause
explanation / alignment format the README already uses:

- **The core loop**: `spec`, `lint`, `build`, `serve`, `stats`, `guard`
- **JSON sessions for agents**: `start`, `next`, `submit`, `report`,
  `hint`, `rubric-review`, `select`, `override`, `interact`, `usage`,
  `schema`
- **Evidence and marking**: `evidence`, `trends`, `mark`, `retract`,
  `render`, `id-assign`
- **Learning surfaces**: `study`, `lesson`, `render-style`, `gloss`,
  `key-review`, `day`
- **Data and tools**: `export`, `import anki`, `seed`, `config`, `theme`,
  `migrate`, `update`, `daemon`, `sidecar`, `cli-twin`, `disclosure`,
  `calibrate`

Rules: every line names a real subcommand from Task 1's frozen list; any
command left out of the README is a deliberate call with a stated reason
(e.g. `calibrate` may be grouped as "authoring utilities" rather than
omitted); no invented flags; fix the misaligned lines in the current block
(`start`, `study`, `day` rows). Update the "intended loop with an LLM"
paragraph to reference the grouped index instead of re-listing commands, and
keep the "For an agent, use the JSON session interface" walkthrough as-is —
it is correct and short.

### Task 4: Item types — fix the contract heading, document what ships

- **Fix the source first**: in `model.py`'s spec text, change the heading
  `THE FIVE ITEM TYPES` (model.py:1641) to the actual count of listed types
  (expect `THE SEVEN ITEM TYPES` today). Update the one assertion that pins
  it in `tests/lesson_roundtrip.py` (line ~2414) to match. This is the
  plan's explicit behavior-preserving carve-out; nothing else in that file
  changes.
- In the README, replace "Six. Five are the NREMT set, chosen because it
  spans familiarity through discrimination and because scoring is uniform
  across it" — it is dense and now wrong — with the shipped list, in the
  same table format: `mc`, `multi`, `table`, `build`, `dnd`, `short`
  (unchanged wording), and `visual` (one line: interactive plot or
  number-line item; the scene and scoring envelope are declarative JSON
  parsed as data, never executed).
- Keep the dichotomous-scoring paragraph but say it directly: "Two of three
  correct scores zero" stays; the NREMT framing stays only if the executor
  confirms it is still the design note (check `model.py`'s spec text before
  repeating it).
- Do not document `check` (Phase 5, not shipped) — one sentence may say
  "the authoring contract grows additively; see `itembank spec`" but no
  invented type.

### Task 5: Reconcile the Design boundaries with what actually ships

- **Spaced repetition**: rewrite the bullet. What it now does: evidence-
  derived pacing and retention at the objective level — `retention.py`
  derives due state and daily caps by replaying captured evidence through one
  scheduler strategy (FSRS is the registered default), surfaced as
  `itembank trends`, the `day` cockpit's recommendations, and the retention
  report variant. What it still does not do: it is not a per-card recall
  scheduler; `study`'s Learn loop remains session-only; a learner who wants
  spaced-repetition recall still graduates misses to a dedicated tool. No
  invented figures (D-19).
- **Model adapter** (new bullet, Phase 8 shipped): an optional hosted or
  local backend may generate hints and per-point marking suggestions; the
  runtime gates tiers (`tier_gate.py`) and a suggestion is a `pending` token
  until a human marks it — nothing a model produces is accepted as evidence
  or a score. Keep the existing "This does not auto-grade prose" bullet
  intact and non-overlapping.
- Keep the other boundaries ("This is an assessment protocol...", "This
  does not generate questions", "This does keep private local learning
  evidence", "This repository never contains question banks") — verify each
  claim still holds (e.g. the guard/CI statement) and tighten wording only.

### Task 6: Fix the Layout / repository tree

Rebuild the tree block from Task 1's `ls` output: root modules
(`itembank.py`, `model.py`, `runtime.py`, `server.py`, plus `evidence.py`,
`retention.py`, `selection.py`, `model_adapter.py`, `tier_gate.py`,
`build.py`, `schema_validate.py`), the surfaces that matter to a reader
(`quiz.py`, `quiz_page.py`, `session.py`, `study.py`, `day.py`, `cli.py`,
`theme.py`, and the grouping note that `surfaces/` holds ~25 client
modules), the `schemas/` directory (published JSON contracts), `styles/`
(lesson render styles), `launchers/` and `installers/` (packaging),
`scripts/`, `src-tauri/` (desktop shell), plus the existing fixture, test,
`GRADING.md`, `AGENTS.md`, skill-tree and `ROADMAP.md` lines. Keep the
"session files are private output" note.

### Task 7: Bounded drift pass over the repo docs

Only where they duplicate or contradict the public docs; no restructuring:

- **AGENTS.md**: add `visual` to the item-type list in the authoring
  paragraph; verify every command it names exists in `--help`; if the skills
  table or discovery matrix drifted, reconcile to README (they were synced
  in 999.5-01 — expect no change).
- **ROADMAP.md**: fix the phase-status table to match `STATE.md` (rows
  cited in the truths above; 6.1 shows the committed 06.1-01/02 plans, not
  0/3); change the 999.5 description from "four repo skills" to five
  (`ocr`); at plan completion, update the 999.5 entry's plan count 1 → 2
  with the summary link. If ROADMAP is still carrying conflict markers when
  this task runs, stop and report (Task 1 precondition).
- **GRADING.md / CLAUDE.md**: read both; fix only statements that are now
  false or that contradict README/AGENTS (e.g. a stale command or a stale
  item-type list). If neither drifts, leave them untouched and say so in
  the SUMMARY.
- Do not touch `.agents/skills/` or `.claude/skills/` content; if any edit
  to a skill becomes unavoidable, mirror both trees and assert `diff -rq`.

### Task 8: Two CI gates — command index and schema coverage

In `.github/workflows/ci.yml` add two steps (precedent: 999.5-01's mirror
and path-leak steps, 05-07's claim-word gate):

- **Claim-word gate**: run `python itembank.py --help`, collect the
  subcommand names, extract bare command tokens from README code blocks
  (`itembank <name>` / `python itembank.py <name>`), and fail if any token
  is not a registered subcommand. Flags (`--port`, `--format`, ...) are not
  checked; only bare command names. One small self-contained script (inline
  python or `scripts/check_readme_commands.py`).
- **Schema coverage**: every `schemas/*.json` is exercised in CI. The seven
  currently uncovered (`visual_interaction`, `agent_usage`, `model_adapter`,
  `tier_gate`, `selection`, `settings`, `update_manifest`) either
  self-validate (add an `--all` mode to `schema_validate.py` that checks
  each schema document parses and passes its own structural checks) plus at
  least one instance validation where a fixture or real payload exists, or
  carry a stated exemption reason in the workflow comment. No schema is
  silently absent.

### Task 9: Audit the broken-windows ledger to truth

`.planning/WINDOWS.md` holds three open `unrun-verify` windows and
`/gsd-ship` blocks while `open_count > 0`. Assess each and update the file
to truth (frontmatter `open_count`/`fixed_count` must match the table):

- **Window 1 (daemon reserved-port/EACCES branch)**: if a portable repro
  exists — bind a port, attempt `start_server()`, assert the fallback — add
  it to `tests/daemon_roundtrip.py` (this is the plan's second carve-out)
  and mark the window fixed. If the repro is not portable within this
  plan's scope, leave it open and state exactly why in the ledger.
- **Windows 2 and 3 (LAN phone reachability; GIFT sandbox manual import)**:
  these are genuinely manual checks. Record the plan's assessment (what
  would close each, who must do it), leave them open, and note in the
  SUMMARY that a human waive/verify is owed before `/gsd-ship`.

Do not run `/gsd-ship` and do not waive windows yourself — a human decision
owns the waiver.

### Task 10: Verify and close

- `for f in tests/*_roundtrip.py; do python "$f" || exit 1; done` (or the
  repo's `test_command` from `.planning/config.json`) — must include the
  updated `lesson_roundtrip.py` and `daemon_roundtrip.py`.
- `python schema_validate.py` against the schemas the new CI step covers.
- `python itembank.py lint fixtures/sample_bank.md` clean.
- `python itembank.py guard .` (note the 07 deferred-items caveat: guard
  findings on `.agents/skills/*/SKILL.md` are pre-existing and out of
  scope; the gate must pass on the fixtures at minimum).
- The two new CI gates pass locally.
- `diff -rq .agents/skills .claude/skills` clean.
- `git grep -n "THE FIVE ITEM TYPES"` returns nothing outside this plan.
- Read README.md top to bottom once for the Task 2 voice rules: first
  screen concrete, sample bank present, architecture moved to Design, no
  hedging, no invented commands, alignment consistent.
- Write `999.5-02-SUMMARY.md` (delivered files, per-task evidence, the
  ledger assessment, any deliberate omissions with reasons) and update the
  ROADMAP 999.5 plan count to 2.
- Commit exactly once, staging only this plan's files by name:
  `docs(999.5-02): ...`.

## Deferred (recorded, not acted on)

- **Repo hygiene** (audited 2026-08-11, deliberately deferred by decision):
  untracked scratch clutters `git status` — `tmp_*.patch` ×8, `tmp/`,
  `fake_hosted_unused.py` (imported nowhere), `.planning/probes/`,
  `.planning/research/.cache/` (~42 blobs), `.gsd/`, `.playwright-mcp/`,
  `.pytest_cache/`, and 10 `.phase*-wt` worktrees; most are not gitignored
  and a `git add -A` would sweep them in. Left untouched because the tree is
  mid-merge and concurrent agents use it. Revisit after the merge settles.
