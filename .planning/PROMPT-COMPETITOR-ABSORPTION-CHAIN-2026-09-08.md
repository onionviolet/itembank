# Competitor absorption implementation chain

Use `$efficient-agent-routing`, `$daisy-chain-work`, and `$evidence-gated-handoff`.

## GOAL and outcome

Deliver a coherent, source-grounded interactive lesson slice that absorbs the best relevant competitor capabilities into Itembank's direction. Preserve LiaScript and the entire existing landscape in the evaluation. Do not reduce this to OpenMAIC generation or another research report. The first link must produce a runnable, inspectable teaching experience. Subsequent links harden and integrate the selected capability where the live architecture and evidence support it.

## OWNER and execution

The user owns product direction. The current link's single builder owns its bounded implementation and verification. Existing course, reader, and presentation workstreams retain their authority. The next task uses this saved local checkout because required research and prototypes are uncommitted. No commit, push, publication, remote fork, or paid service is authorized by this packet.

Use the current task's configured model directly. Apply the routing skill before any delegation. A balanced model at medium reasoning is the suggested tier for ordinary bounded implementation when selection is supported. Do not add a coordinator-worker pair for the same investigation. Add an independent reviewer only for a named acceptance risk, with read-only ownership and the routing skill's model caps. Do not equate more agents with savings. Runtime multi-agent classroom behavior is excluded regardless of how development work is routed.

## CONTEXT and authority

Read AGENTS.md and the current-position section of `.planning/STATE.md`. Read the relevant sections of `.planning/AGENT-WORKFLOW.md`, `.planning/SOURCE-TO-COURSE.md`, and the active milestone. Inspect git status and live competing work before writing. Locate symbols and tests before reading large modules.

Sample USER-VISION's source-to-course, visual ambition, portable lesson, external-reader, selective generation, and interactive visual teaching entries. Do not claim a whole-vision audit from this sample. The latest direction retains bright text highlighting and interaction without character dialogue or simulated discussion. Subtle transitions and staged reveals are interpretations to test, not exact user mandates. Direct reading can remain the best treatment. Generation must not rewrite every source.

The Phase 20 state records outstanding human review and a separately owned parity backend issue. The source-to-reading packet records unresolved exact undo prerequisites. Do not silently waive or repair these by changing their acceptance criteria. Visual emphasis is not learner evidence. An agent cannot certify human-only accessibility review.

## AUDITS and evidence to reuse

Read these existing owners before new research:

| Artifact | Status and use |
| --- | --- |
| `.planning/research/competition-2026-09-08/ABSORPTION.md` | Current reconciled direction and promotion gates. Update this owner as implementation evidence changes. |
| `.planning/research/competition-2026-09-08/LANDSCAPE.md` | Nineteen adjacent candidates plus the separate OpenMAIC assessment. Documentation survey, not twenty completed trials. |
| `.planning/research/competition-2026-09-08/REUSE-AUDIT.md` | Pinned component and licensing boundaries. Verify changed upstream inputs before reuse. |
| `.planning/research/competition-2026-09-08/HANDS-ON.md` | Actual OpenMAIC and DeepTutor observations, unequal model settings, partial generation, reader failure, and local timeout limitations. |
| `.planning/research/competition-2026-09-08/PROTOTYPE-REVIEW.md` | Three grounding findings fixed in the bounded prototype. Production operation manifests, persistence, and broader document selection remain unproved. |
| `.planning/research/OPENMAIC-ECOSYSTEM-COMPARISON-2026-09-08.md` | Original code-level assessment and discovery gap. Preserve original evidence. |
| `.planning/SOURCE-TO-READING-NEXT-PACKET-2026-09-08.md` | Existing reading and recovery dependencies. Coordinate instead of creating a parallel source model. |

The isolated prototypes live in `prototypes/competition/`. Grounding adapts DeepTutor with attribution and an Apache-2.0 license copy. Generation pins `@openmaic/generation` 0.3.6 and exposes caller-owned outline and teaching-scene functions. Twenty grounding tests, four HTTP tests, and four deterministic generation tests passed. Both actual local model trials timed out after 30 seconds with no candidate. Fixtures do not prove live generation quality. Quick preflight failed on the pre-existing `author-bank/_attempts` skill-mirror mismatch. Later live state may differ, so report current results separately.

## SCOPE: preserve breadth, select by capability

LiaScript must receive a concrete interaction trial, not a passing mention. Inspect current primary documentation and licensing for Markdown-based progressive explanations and interactive treatments. Compare the smallest useful pattern with Itembank's existing renderer and the OpenMAIC interaction already observed. Do not import its assessment authority or whole runtime without a demonstrated need.

Reconcile every existing landscape row by capability, status, evidence, owner, and next action or revisit trigger. This includes OpenMAIC, DeepTutor, OpenTutor, Open Notebook, NotebookLM, LiaScript, H5P, Lumi AI Editor, eXeLearning, Docling, MinerU, Marker, Unstructured, Anki, FSRS, LearnHouse, Moodle, Open edX, Kolibri, and Excalidraw. Use existing research rather than reinstalling all twenty. Keep institutional publishing, extraction, note construction, and review scheduling visible without implementing all of them in this slice. Reuse may mean a pattern, adapter, dependency, or attributed small component. Compare maintenance and compatibility before copying code. No forced dependency quota.

The learner slice should demonstrate source-linked explanation, bright semantic highlighting, one learner-controlled interactive treatment, and a useful static representation. Examples include a parameter control updating a diagram and a staged explanation with direct reveal controls. Use a synthetic or rights-cleared source with explicit objectives. Separate source statements from generated synthesis. Keep source references visible and allow the learner to study without conversation. Editing and recovery belong to the existing artifact model.

## DO NOT TOUCH

Preserve all unrelated and concurrent dirty files. In particular, do not take ownership of Phase 20 repairs, configuration, capability manifests, daemon changes, or reading recovery merely because they are dirty. Record the live baseline before edits. Start link one in a new `prototypes/competition/interactive-lesson/` subtree and the existing absorption research owner. Add production paths only in a subsequent concrete packet after symbol-level mapping and collision checks.

Do not create simulated teachers, classmates, role rosters, chat-first lessons, turn-taking, compulsory narration, or agent discussion. Do not add a second parser, scorer, evidence store, accepted-content model, or generic classroom shell. Do not auto-grade prose or reveal keyed content early. Do not copy protected course content, use real banks as fixtures, send private sources remotely, install unreviewed executable lesson content, or spend paid credits. Do not alter root dependencies for a prototype. Preserve source identity, rights, accepted revisions, and exact recovery requirements.

## NEXT ACTION: link one

Inspect current state and the existing lesson/interaction capability symbols. Record a compact capability-to-owner mapping in ABSORPTION.md. Run one bounded LiaScript example using synthetic or rights-cleared material and record observed behavior separately from documentation claims. Build a coherent interactive-lesson prototype that exercises the selected patterns without conversation. Prefer one writer and a small reusable implementation over an app fork. Use browser inspection to verify the result.

## GATE

Link one completes only when the runnable artifact visibly presents a source-linked explanation, readable bright highlighting, one meaningful direct interaction, and a study-usable static fallback. Verify keyboard operation, reduced-motion behavior, reset/reopen behavior, and no model call on routine interaction. Check contrast and color-independent meaning. Report touch and screen-reader checks precisely, with human-only review still owed where applicable. Verify that interaction creates no score, mastery claim, or accepted evidence. Preserve attribution and dependency pins for anything reused.

Run focused tests for observed risks and review the actual diff. Record the exact commands and results. Do not repeat full repository suites in every link. At the final integrated candidate, run the applicable repository preflight and report independent existing failures separately. No synthetic fixture success may be described as successful live lesson generation.

## Continuation and reconciliation

After the first gate passes, decide the next smallest useful unit from evidence. A likely next unit is native integration of the chosen lesson interaction into existing semantic roles and renderers, with portable content and recovery preserved. Generation hardening is a distinct unit only if live model output remains necessary and its failure is understood. Avoid chaining an endless series of surveys or cosmetic prototypes.

Before creating a successor, update this packet with a concise link result and exact next scope, paths, exclusions, and gate. Update ABSORPTION.md with each finding's disposition and reconcile overlapping audits. Preserve original evidence. The next task must invoke both routing and daisy-chain skills and reference this packet. Use the same local project while required work remains uncommitted. Check the successor's activity once. Keep one active writer per shared file.

Stop when the selected capability is integrated and its applicable gates pass, when a required user or external decision blocks progress, or when the same blocker survives three consecutive links. Do not use failed live generation to block independent deterministic interaction work. Do not substitute a saved report for the implementation goal. Preserve deferred and human-only checks explicitly.

## RETURN

Report the runnable result, changed paths, upstream patterns actually absorbed, license handling, tests and browser observations, remaining limitations, reconciled audit dispositions, and whether a successor was created. Link the artifact and updated owner. Distinguish prototype, integrated code, validated generation, and accepted learner content. Never claim an unexecuted stage passed.

## Link one result, 2026-09-08

Status: prototype gate passed with human legs owed. The original first-link scope above is preserved as history. Execute the link-two scope below next.

`prototypes/competition/interactive-lesson/` now contains the runnable rainfall comparison, exact synthetic-source link, bright semantic emphasis, direct parameter control, native explanation reveals and complete Markdown fallback. Its README contains exact commands, browser observations, LiaScript input and limits. Four Node tests and four Python tests pass. Quick preflight fails only on the pre-existing skill-mirror mismatch. No production file, root dependency, real learner content, commit or push was changed.

The official LiaScript LiveEditor compiled the saved input and keyboard activation advanced the two progressive blocks. The new local page performs no network call during interaction. Reset/reopen, reduced-motion emulation, JavaScript-disabled fallback, source integrity and a 390-pixel layout were checked. Human screen reader, physical touch and aesthetic review remain owed. In-app touch injection was unsupported. Browser source text preview was not established, although its HTTP target and unchanged quote were verified. No live generation success or clean-machine restore is claimed.

ABSORPTION.md now reconciles all 20 candidates and retains source extraction, notebook construction, review scheduling and institutional publishing lanes. Earlier grounding fixes, generation failures, reading R1 through R3 and Phase 20 human/parity debt remain explicit.

## Link two: native deterministic lesson comparison

GOAL: make one source-linked, explicitly authored comparison treatment work through Itembank's existing lesson renderer, with bright semantic emphasis and direct learner controls over a useful portable static representation. End with the real native lesson page, not another detached demo or survey.

OWNER: the next task's single builder owns the native renderer slice. The user owns product direction. Existing source/recovery, runtime and Phase 20 owners retain their authority. Use the configured model directly with `$efficient-agent-routing`, `$daisy-chain-work` and `$evidence-gated-handoff`. This chain is authorized. Keep this saved local checkout because the required input remains uncommitted.

SCOPE: start by rereading live state, dirty files and `ABSORPTION.md`'s capability mapping. Candidate write paths are `surfaces/lesson.py`, one new small `surfaces/lesson_interaction.py` if separation helps, `capabilities.py`, `tests/lesson_interaction_roundtrip.py`, a synthetic fixture in `fixtures/`, and the two existing absorption/chain owner documents. If a declarative marker is necessary, extend only the existing parser/linter and published contract in `model.py`, with focused tests. No second bank parser. Any format addition must be additive, narrowly specified, bounded and inspected under the existing capability contract before implementation. Do not infer an arbitrary-code extension from this packet.

NEXT ACTION: inspect `_inline`, `_callout_html`, `_render_blocks`, `guided_stages`, `_stage_html`, capability profiles and their immediate tests. Reuse a compatible existing semantic or declarative representation where possible. Implement the smallest explicit opt-in comparison treatment. Keep static explanation and examples in the portable artifact. Reuse existing rendering, style and source-link owners. The fixed rainfall prototype establishes the interaction behavior, not a new accepted-content or source model. Do not automatically convert every table or bold phrase into an interaction.

DO NOT TOUCH: all concurrently dirty files, especially `capabilities.json`, `itembank.json`, daemon/config/manifest tests, `tools/capabilities_manifest.py`, Phase 20 documents, source import/undo, scores, evidence and active assessment disclosure. Refresh collisions before edits. If a listed candidate file has become actively owned, coordinate or choose a smaller disjoint slice. Do not add root dependencies, generated executable HTML, remote imports, model calls, chat, roles, narration, persistence, migration or automatic acceptance. No commit, push, publication, paid service or real bank. Preserve the original prototype as evidence.

GATE: a synthetic artifact parsed and linted by the existing authority renders through the native lesson route. It shows source and synthesis labels, bright emphasis with non-color meaning, a directly controlled comparison and a study-usable static path. Verify invalid/missing treatment handling, escaped hostile text, finite bounded parameters, keyboard/reset/reopen, no routine network/model calls, no score/evidence writes, reduced motion and narrow layout. The treatment must not reveal keyed or higher-tier content in restricted contexts. Run the relevant lesson, capability and disclosure regression gates, then the applicable repository preflight once for the integrated candidate. Separate existing failures from regressions. Actual human touch/screen-reader review remains owed, not self-certified. Since no durable learner mutation is introduced, prove accepted input bytes unchanged instead of claiming that source-import undo is fixed.

REVIEW: one builder by default. A read-only independent reviewer is permitted only for the named native disclosure or executable-content boundary risk after a concrete diff exists, following routing caps. Do not create a coordinator-worker pair for the implementation.

RETURN: runnable native route and changed files, tests and exact limits, compatibility/authority review, all-candidate reconciliation update and production-vs-prototype status. Stop the chain once this selected capability is integrated and applicable gates pass, with human legs preserved. Do not start generation hardening merely because prior local generation timed out. Create another bounded task only if concrete implementation or repair remains and the continuation conditions above hold.

## Link two result and chain stop, 2026-09-08

Status: selected deterministic comparison integrated. Applicable gates pass. Human legs remain owed. No successor is needed or created.

Runnable native route: `http://127.0.0.1:8767/lesson/lesson_comparison`, launched by `python3 tests/lesson_interaction_roundtrip.py --serve`. This harness uses the real daemon handler and no sitting, model call or startup update check. The portable input is `fixtures/lesson_comparison.md`. `python3 itembank.py lesson fixtures/lesson_comparison.md --out /tmp/itembank-native-comparison.html` also renders the native CLI twin.

The existing Example role accepts an explicit bounded `[COMPARE: A,B,maximum,unit]` body header. Its native slider, reset, labelled meters and signed difference enhance an authored static explanation. `model.parse_lesson_comparison` owns scalar validation. Runtime `glossable` checks the entire block before rendering, and required lesson gates still truncate it. No authored code execution, evidence writes, persistence, remote imports, model calls, migration or acceptance was added.

Changed implementation paths: `model.py`, `surfaces/lesson.py`, new `surfaces/lesson_interaction.py`, `capabilities.py`, new `fixtures/lesson_comparison.md` and new `tests/lesson_interaction_roundtrip.py`. The new lint code required one enum member in `schemas/lint_error.schema.json` and the eighteen-code assertion in `tests/lesson_roundtrip.py`. The existing absorption owner and this packet carry reconciliation. All initially dirty excluded paths remain outside this link's writes.

Twelve focused tests pass, including real HTTP no-write verification, hostile text, non-finite and out-of-range values, missing fallback, runtime-held content, required gates, quoted markers and unclosed fences. Lesson, capability-profile and gate regressions pass. Browser evidence covers keyboard changes/reset, reload, no-script content, exact excerpt navigation, reduced motion, 390-pixel layout, 13.71:1 highlight contrast and zero routine network requests. The synthetic source hash and fixture bytes remain unchanged through rendering. Physical touch, screen-reader output, print-device output and human aesthetic review remain unverified.

One permitted read-only Sol review examined the concrete disclosure/executable-content boundary. No such defect was found. Its two lint-boundary findings were fixed and tested. The builder sampled large modules by symbol and reran focused and lesson checks after the final repairs.

Full preflight ran once and completed all 118 Python scripts plus JavaScript. It failed on the pre-existing skill-mirror mismatch, binding/configuration/manifest findings and expected dirty tree. The binding test passed a bounded rerun. The manifest diff is only the unrelated GET `/courses` route. The configuration test copies the existing custom accent but expects the schema default. These exact dispositions and owners are in `ABSORPTION.md` under F9. No full-repository green claim is made.

All 20 candidates, earlier unequal trials, grounding limitations, failed generation, R1 through R3, and Phase 20 human/parity debt remain visible in the absorption owner. The native code is original pattern reuse, with no upstream source or dependencies copied into production. Existing prototype pins and notices remain intact. Stop here rather than starting generation hardening or repairing excluded work.
