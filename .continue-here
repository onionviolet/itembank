# .continue-here

**Status: Phase 15A is FROZEN. Phase 15B is unblocked and executable. Continue
at 15B-01.**

- Branch `main`, tree clean, 98 of 98 test files green (313 seconds, measured
  2026-08-30 on Darwin 27.0.0 arm64, Python 3.14.6).
- Frozen: 14A, 14B, 14C, **15A**, 16A, 16B, 16C.
- **The next executable work is Phase 15B**, seven plans. Its 15B-01
  precondition halted until a `15A-FREEZE.md` existed; it now does.

**What Weibao needs to look at, in order of consequence.**

1. **Four consecutive phases have now closed their human-review leg with an
   agent signature**: 16A and 16B under a 2026-08-28 standing delegation, then
   16C and 15A under the 2026-08-30 instruction to proceed without him. That is
   a pattern, not an exception, and it is worth deciding deliberately.
2. **Phase 15A additionally had an agent make two one-way checkpoint
   decisions**, D-15A-1 and D-15A-2, both recorded in `15A-DECISIONS.md` with
   their reversal costs. D-15A-1 changed `schemas/model_adapter.schema.json`, a
   published contract that Phase 15B and any external agent client will build
   against. Reversing it after operations are journaled is a migration, not an
   edit. Both took the plan's recommended default.
3. **17A-04 still needs Weibao and is NOT waived.** It needs the LGPL
   browser-driver supply-chain decision
   (`.planning/DECISIONS-17A04-DRIVER-2026-08-25.md`) and an A11Y-01 review.
   An agent never self-certifies accessibility; that rule is a project
   non-negotiable rather than a plan-level gate, so it was not waived along
   with the others.

**Reading order for anything that builds on 15A:** `15A-FREEZE.md` first, and
`15A-REVIEW.md`'s Provenance section before relying on the freeze. Striking
that section reopens the review leg.

**Open findings carried out of 15A**, all in `15A-FREEZE.md`'s open-items
table: recommendation quality is untested by anything (the provider is a modulo
rule, so the first run against a real model is an unproven step);
`coverage_claims_for` cannot reach every coverage state because binding rows
carry no assertion column; an untreated entry still displays a treatment kind;
and `journal.undo` raises an untyped `FileNotFoundError` on a missing
before-image, which 15A was forbidden from fixing.

**Also open:** all of 17B, and the paced-lesson subphase whose decisions are
recorded and whose plan is unwritten.

Full detail is `.planning/STATE.md`; read its current-phase section, not the
whole file.
